/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/
console.debug = function() {};

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  createLLMClient: () => createLLMClient,
  default: () => LLMWikiPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian15 = require("obsidian");

// src/types.ts
var WIKI_LANGUAGES = {
  "en": "English",
  "zh": "\u4E2D\u6587",
  "zh-Hant": "\u7E41\u9AD4\u4E2D\u6587",
  // v1.22.0: Traditional Chinese
  "ja": "\u65E5\u672C\u8A9E",
  "ko": "\uD55C\uAD6D\uC5B4",
  "de": "Deutsch",
  "fr": "Fran\xE7ais",
  "es": "Espa\xF1ol",
  "pt": "Portugu\xEAs",
  "it": "Italiano"
};
var VALID_ENTITY_TAGS = ["person", "organization", "project", "product", "event", "place", "other"];
var VALID_CONCEPT_TAGS = ["theory", "method", "field", "phenomenon", "standard", "term", "other"];
var VALID_SOURCE_TAGS = [
  "paper",
  "article",
  "book",
  "transcript",
  "clippings",
  "notes",
  "other"
];
var PREDEFINED_PROVIDERS = {
  openai: {
    id: "openai",
    name: "OpenAI",
    nameEn: "OpenAI",
    nameZh: "OpenAI",
    baseUrl: "https://api.openai.com/v1",
    apiKeyPlaceholder: "sk-...",
    apiKeyPlaceholderEn: "sk-...",
    apiKeyPlaceholderZh: "sk-...",
    requiresBaseUrl: false
  },
  anthropic: {
    id: "anthropic",
    name: "Anthropic (Claude)",
    nameEn: "Anthropic (Claude)",
    nameZh: "Anthropic (Claude)",
    baseUrl: "",
    apiKeyPlaceholder: "sk-ant-...",
    apiKeyPlaceholderEn: "sk-ant-...",
    apiKeyPlaceholderZh: "sk-ant-...",
    requiresBaseUrl: false
  },
  gemini: {
    id: "gemini",
    name: "Google Gemini",
    nameEn: "Google Gemini",
    nameZh: "Google Gemini",
    baseUrl: "https://generativelanguage.googleapis.com/v1beta/openai",
    apiKeyPlaceholder: "AIza...",
    apiKeyPlaceholderEn: "AIza...",
    apiKeyPlaceholderZh: "AIza...",
    requiresBaseUrl: false
  },
  openrouter: {
    id: "openrouter",
    name: "OpenRouter",
    nameEn: "OpenRouter",
    nameZh: "OpenRouter",
    baseUrl: "https://openrouter.ai/api/v1",
    apiKeyPlaceholder: "sk-or-...",
    apiKeyPlaceholderEn: "sk-or-...",
    apiKeyPlaceholderZh: "sk-or-...",
    requiresBaseUrl: false
  },
  deepseek: {
    id: "deepseek",
    name: "DeepSeek",
    nameEn: "DeepSeek",
    nameZh: "DeepSeek",
    baseUrl: "https://api.deepseek.com/v1",
    apiKeyPlaceholder: "sk-...",
    apiKeyPlaceholderEn: "sk-...",
    apiKeyPlaceholderZh: "sk-...",
    requiresBaseUrl: false
  },
  minimax: {
    id: "minimax",
    name: "MiniMax",
    nameEn: "MiniMax",
    nameZh: "MiniMax",
    baseUrl: "https://api.minimaxi.com/v1",
    apiKeyPlaceholder: "sk-cp-...",
    apiKeyPlaceholderEn: "sk-cp-...",
    apiKeyPlaceholderZh: "sk-cp-...",
    requiresBaseUrl: false
  },
  kimi: {
    id: "kimi",
    name: "Kimi (Moonshot)",
    nameEn: "Kimi (Moonshot)",
    nameZh: "Kimi (Moonshot)",
    baseUrl: "https://api.moonshot.cn/v1",
    apiKeyPlaceholder: "sk-...",
    apiKeyPlaceholderEn: "sk-...",
    apiKeyPlaceholderZh: "sk-...",
    requiresBaseUrl: false
  },
  glm: {
    id: "glm",
    name: "GLM (Zhipu AI)",
    nameEn: "GLM (Zhipu AI)",
    nameZh: "GLM (\u667A\u8C31AI)",
    baseUrl: "https://open.bigmodel.cn/api/paas/v4",
    apiKeyPlaceholder: "...",
    apiKeyPlaceholderEn: "...",
    apiKeyPlaceholderZh: "...",
    requiresBaseUrl: false
  },
  ollama: {
    id: "ollama",
    name: "Ollama (Local)",
    nameEn: "Ollama (Local)",
    nameZh: "Ollama (\u672C\u5730)",
    baseUrl: "http://localhost:11434/v1",
    apiKeyPlaceholder: "ollama (no Key required)",
    apiKeyPlaceholderEn: "ollama (no Key required)",
    apiKeyPlaceholderZh: "ollama (\u65E0\u9700Key)",
    requiresBaseUrl: false
  },
  lmstudio: {
    id: "lmstudio",
    name: "LM Studio (Local)",
    nameEn: "LM Studio (Local)",
    nameZh: "LM Studio\uFF08\u672C\u5730\uFF09",
    baseUrl: "http://localhost:1234/v1",
    apiKeyPlaceholder: "lmstudio",
    apiKeyPlaceholderEn: "lmstudio (optional)",
    apiKeyPlaceholderZh: "lmstudio\uFF08\u53EF\u9009\uFF09",
    requiresBaseUrl: false
  },
  custom: {
    id: "custom",
    name: "Custom OpenAI-Compatible",
    nameEn: "Custom OpenAI-Compatible",
    nameZh: "\u81EA\u5B9A\u4E49 OpenAI \u517C\u5BB9",
    baseUrl: "",
    apiKeyPlaceholder: "API Key",
    apiKeyPlaceholderEn: "API Key",
    apiKeyPlaceholderZh: "API Key",
    requiresBaseUrl: true
  },
  "anthropic-compatible": {
    id: "anthropic-compatible",
    name: "Custom Anthropic-Compatible",
    nameEn: "Custom Anthropic-Compatible",
    nameZh: "\u81EA\u5B9A\u4E49 Anthropic \u517C\u5BB9",
    baseUrl: "",
    apiKeyPlaceholder: "API Key",
    apiKeyPlaceholderEn: "API Key",
    apiKeyPlaceholderZh: "API Key",
    requiresBaseUrl: true
  }
};
var DEFAULT_SETTINGS = {
  provider: "anthropic",
  apiKey: "",
  baseUrl: "",
  model: "",
  // No hardcoded default — user must fetch models or enter manually
  wikiFolder: "wiki",
  language: "en",
  wikiLanguage: "en",
  useCustomWikiLanguage: false,
  availableModels: [],
  useCustomModel: false,
  maxConversationHistory: 30,
  queryHistory: [],
  // Schema
  enableSchema: true,
  // Issue #85: tag vocabulary
  tagVocabularyMode: "default",
  customEntityTags: "",
  customConceptTags: "",
  // Extraction
  extractionGranularity: "standard",
  // Auto-maintenance
  autoWatchSources: false,
  autoWatchMode: "notify",
  autoWatchDebounceMs: 5e3,
  watchedFolders: [],
  periodicLint: "off",
  startupCheck: true,
  // Issue #81: default ON for low-level format fixes
  autoSmartFix: false,
  // Ingestion acceleration (default: 3 parallel for most providers)
  pageGenerationConcurrency: 3,
  batchDelayMs: 500,
  // Query dedup
  lastOfferedQueryHash: "",
  // LLM readiness
  llmReady: false,
  // Issue #75: cap max_tokens per LLM call. 0 = no cap (cloud default).
  // Local model users can set this when the provider is Ollama, LM Studio,
  // custom, or anthropic-compatible.
  maxTokensPerCall: 0,
  // v1.20.0: default false. The plugin does NOT send any thinking-control
  // field unless the user explicitly enables "Disable thinking" in Custom
  // Advanced Settings. The provider decides its own reasoning behavior; any
  // reasoning that does appear in the response is folded into a collapsible
  // <details> block in the Query Wiki UI so it never visually intrudes on
  // the answer. Setting name kept for v1.18.2 data.json backward compat.
  disableThinking: false,
  // Advanced settings mode — default hides the toggles, custom reveals them.
  advancedSettingsMode: "default",
  // Issue #111: default to 'lower' for backwards compatibility.
  slugCase: "lower"
};

// src/constants.ts
var WIKI_SUBFOLDERS = {
  entities: "entities",
  concepts: "concepts",
  sources: "sources"
};
var COMPATIBLE_SOURCE_EXTENSIONS = ["md", "markdown", "txt", "text"];
var PAGES_CACHE_TTL_MS = 5e3;
var CUSTOM_LIMIT_MAX = 500;
var CUSTOM_LIMIT_MIN = 1;
var CUSTOM_BATCH_SIZE_MAX = 50;
var TOKENS_PER_ITEM_BUDGET = 400;
var SOURCE_ANALYZER_RETRY_MULTIPLIER = 3;
var MAX_TOKENS_BATCH = 16e3;
var TOKENS_PAGE_GENERATION = 8e3;
var TOKENS_APPEND_REVIEWED = 4e3;
var TOKENS_CONTRADICTION = 4e3;
var TOKENS_CONVERSATION_EXTRACTION = 5e3;
var TOKENS_CONVERSATION_PAGE = 8e3;
var TOKENS_DEDUP_RESOLUTION = 1e3;
var TOKENS_LINT_ALIAS_BATCH = 500;
var TOKENS_LINT_DEDUP_LLM = 4e3;
var TOKENS_LINT_PAGE_FIX = 8e3;
var TOKENS_LINT_ORPHAN_FIX = 800;
var TOKENS_QUERY_MODEL_DETECT = 100;
var TOKENS_QUERY_PAGE_SELECT = 500;
var TOKENS_QUERY_LLM_SELECT = 3e3;
var TOKENS_QUERY_SAVE_DEDUP = 300;
var TOKENS_SCHEMA_SUGGESTION = 4096;
var MAX_PAGE_CONTENT_CHARS = 12800;
var MAX_RETRIES = 2;
var RETRY_BASE_DELAY_MS = 1e3;
var NOTICE_BRIEF = 2e3;
var NOTICE_SHORT = 3e3;
var NOTICE_WATCHER = 4e3;
var NOTICE_NORMAL = 5e3;
var NOTICE_ABORT = 6e3;
var NOTICE_ERROR = 8e3;
var NOTICE_RATE_LIMIT = 1e4;

// src/llm-client.ts
var import_obsidian = require("obsidian");

// src/texts/en.ts
var EN_TEXTS = {
  // Plugin Info
  pluginTitle: "Karpathy LLM Wiki Settings",
  pluginIntro: "This plugin implements {{link}} concept for Obsidian. It reads your notes, uses AI to extract entities and concepts, and builds a structured, interlinked Wiki in your vault.",
  karpathyLinkText: "Andrej Karpathy's LLM Wiki",
  // Features Section
  featuresTitle: "How It Works",
  workflow1Title: "1. Ingest",
  workflow1Desc: "Select a note \u2014 AI extracts entities, concepts, and relationships into Wiki pages.",
  workflow2Title: "2. Query",
  workflow2Desc: "Chat with your Wiki \u2014 answers are grounded in your ingested knowledge.",
  workflow3Title: "3. Maintain",
  workflow3Desc: "Auto-generated index, cross-links, and lint checks keep the Wiki healthy.",
  // Language Switcher
  languageTitle: "Interface Language",
  languageDesc: "Select your preferred language for settings panel. Restart plugin to apply language changes to command palette.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "LLM Client Status",
  llmWikiStatusSection: "LLM-Wiki Status",
  statusInitialized: "Initialized",
  statusNotInitialized: "Not initialized",
  statusReady: "LLM Ready",
  statusNotReady: "LLM not configured \u2014 complete setup and pass connection test",
  currentProvider: "Current Provider",
  // Provider Configuration
  providerSection: "LLM Configuration",
  providerName: "LLM Provider",
  providerDesc: "Select predefined provider or custom OpenAI-compatible service. For Coding Plan or similar bundles, choose Custom OpenAI/Anthropic and enter the provider's Base URL and API Key manually",
  // API Key
  apiKeyName: "API Key",
  apiKeyDesc: "Enter your API key from provider",
  apiKeyPlaceholder: "Enter API Key",
  // Base URL
  baseUrlName: "API Base URL",
  baseUrlDescCustom: "Required: Custom OpenAI-compatible endpoint",
  baseUrlDescOverride: "Optional: Override preset Base URL",
  // Ollama Hint
  ollamaHint: "Ollama runs locally, no API Key required",
  // LM Studio Hint
  lmstudioHint: "LM Studio runs locally, API Key is optional",
  // LLM execution cap
  maxTokensPerCallName: "Context Window",
  maxTokensPerCallDesc: "Limit generation tokens to fit your model's context window. 0 = no cap (cloud default).",
  // Issue #111: slug casing
  slugCaseName: "File Name Casing",
  slugCaseDesc: 'Controls whether generated wiki filenames are lowercased. "Preserve" is recommended for languages where lowercase changes meaning (e.g. German nouns).',
  slugCaseLower: "Lowercase (default)",
  slugCasePreserve: "Preserve case",
  // Model Selection
  modelSection: "Model Selection",
  fetchModelsName: "Fetch Available Models",
  fetchModelsDesc: "Get latest model list from Provider API",
  fetchModelsButton: "Fetch Models",
  fetchingModels: "Fetching...",
  fetchSuccess: "Success! {} models available",
  fetchFailed: "Failed or empty list, please input model name manually",
  fetchNotSupported: "Provider doesn't support model list query",
  fetchErrorAuth: "Authentication failed (HTTP 401/403). Verify your API Key, or enter a Model ID below and click Test Connection to validate.",
  fetchErrorEndpoint: "Endpoint not found (HTTP 404). Verify the BaseURL, or enter a Model ID and click Test Connection to validate.",
  fetchErrorServer: "Provider server error (HTTP 5xx). Try again later, or enter a Model ID and click Test Connection to validate.",
  fetchErrorEmpty: "Provider has no model list endpoint. Enter a Model ID below and click Test Connection to validate.",
  fetchErrorNetwork: "Network request failed. Check your internet connection, BaseURL, or proxy settings. You can also enter a Model ID below and click Test Connection to validate.",
  selectModelName: "Select Model",
  selectModelDesc: "Choose from {} available models",
  customInputOption: "Custom input...",
  customInputHint: 'To use other models, select "Custom input..."',
  modelName: "Model Name",
  modelDescCustom: "Using custom model (click above button to re-fetch list)",
  modelDescFetchFailed: "Model list fetch failed. Verify your API Key and Endpoint URL, or enter a Model ID below and click Test Connection to validate.",
  modelInputPlaceholder: "Enter Model ID, then Test Connection",
  switchToDropdown: "Switch to Dropdown Selection",
  useDropdownButton: "Use Dropdown",
  // Test & Save
  testConnectionName: "Test Connection",
  testConnectionDesc: "Validate configuration can successfully call LLM API",
  testButton: "Test Connection",
  testing: "Testing...",
  saveSettingsName: "Save Settings",
  saveSettingsDesc: "Save current configuration",
  saveButton: "Save Settings",
  savedNotice: "Settings saved!",
  // Test Connection
  testConnectionSuccessful: "Connection successful",
  testConnectionFailed: "Connection failed",
  testConnectionProvider: "Provider: ",
  errorUnknown: "Unknown error",
  // Issue #137: LLM fallback notices (shown when thinking-dialect
  // fallback or param-stripping happens during a request).
  fallbackThinkingDialect: 'Thinking control: switched to "{dialect}" dialect (this provider uses a different thinking-control format). Output is unchanged.',
  fallbackThinkingNone: "Thinking control fully disabled for this provider. Reasoning content may still appear; if so, try a different model.",
  fallbackParamStripped: 'Parameter "{field}" not supported by this provider. Stripped from the request; behavior may differ from configuration.',
  // Wiki Init Status
  wikiInitStatusReady: "Wiki initialized",
  wikiInitStatusNotReady: "Wiki not initialized \u2014 will auto-create on first ingestion",
  // Wiki Folder
  wikiSection: "Wiki Configuration",
  wikiFolderName: "Wiki Folder",
  wikiFolderDesc: "Location for generated Wiki pages",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "Please configure API Key first",
  llmNotReady: "LLM is not configured. Please go to Settings \u2192 Karpathy LLM Wiki to configure your provider, fetch available models, and pass the connection test.",
  errorFetchFailed: "Failed: {}",
  // Query Settings
  querySectionTitle: "Wiki Query Configuration",
  maxConversationHistoryName: "Max Conversation History",
  maxConversationHistoryDesc: "Limit conversation messages to avoid token overflow",
  maxConversationHistoryHint: "Recommended: not exceed 50 rounds",
  numberRangeValidation: "Please enter a number between 1-50",
  numberRangeClamped: "Value exceeds range (1-500), automatically set to {}",
  // Query Modal UI
  queryModalTitle: "Query Wiki - Conversational Query",
  queryModalPlaceholder: "Enter question...",
  queryModalSendButton: "Send",
  queryModalStopButton: "Stop",
  queryModalSaveButton: "Save to Wiki",
  queryModalClearButton: "Clear History",
  queryModalHistoryCount: "Conversation history: {}/{} rounds",
  queryModalStreaming: "Streaming...",
  queryModalFallbackStreaming: "Streaming not supported, switched to non-streaming. Please wait...",
  queryPhaseSearching: "Analyzing Wiki index, selecting relevant pages...",
  queryPhaseFoundPages: "Found {count} page(s): {pages}",
  queryPhaseLoadingPages: "Loading page content...",
  queryPhaseContextReady: "Context ready. Generating answer...",
  queryPhaseGenerating: "Generating... (elapsed {time}s)",
  queryPhaseNonStreaming: "Non-streaming mode, generating response... (elapsed {time}s)",
  queryModalErrorPrefix: "Error: ",
  queryModalHint: 'Queries based on Wiki content. Click "Save to Wiki" to extract valuable conversations as Wiki pages.',
  // v1.20.0: Query Wiki thinking-block collapsible summary
  queryThinkingSummary: "Thinking process",
  queryThinkingSteps: "steps",
  // Error Messages
  errorLLMClientNotInit: "LLM Client not initialized. Please save settings.",
  errorIngestFailed: "Ingest failed: ",
  errorQueryFailed: "Query failed: ",
  fileWriteFailed: "Could not create or update file: {path}",
  // Success Messages
  ingestSuccess: "Ingest successful: {} pages created, {} pages updated",
  querySuccess: "Query completed",
  lintSuccess: "Wiki lint completed",
  lintReadingPages: "Reading {count} Wiki pages...",
  lintReadingPagesProgress: "Reading Wiki pages: {current}/{total}...",
  lintScanningLinks: "Scanning dead links...",
  lintScanningLinksProgress: "Scanning dead links: {current}/{total}...",
  lintCheckingDuplicates: "Checking for duplicate pages...",
  lintCheckingDuplicatesProgress: "Verifying duplicates: batch {current} ...",
  lintFixingPolluted: "Fixing polluted page {current}/{total}: {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} Fix polluted pages ({count})",
  lintDuplicateCheckFailed: "Duplicate detection failed \u2014 see console for details",
  lintDuplicateCheckFailedDetail: "Duplicate check failed at {step}: {error}",
  lintMergeItemFailed: "Merge failed: {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "Aliases missing: {count} page(s) without aliases",
  lintAliasesSection: "Pages missing aliases [{count}]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "Complete aliases ({count})",
  lintAliasesFilling: "Generating aliases {current}/{total}: {page}",
  lintAliasesFilled: "Alias completion complete. Filled {filled}/{total} pages.",
  lintAliasesFillFailed: "Alias generation failed: {page} \u2014 {error}",
  // Issue #85 v7: tag-violation retag notifications
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "Pages with out-of-vocabulary tags [{count}]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintFixItemFailed: "Fix failed: [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "Link failed: {page} \u2014 {error}",
  lintRetrying: "Retrying ({attempt}/{max}) after error...",
  lintAnalyzingLLM: "LLM analyzing Wiki health...",
  saveToWikiSuccess: "Conversation saved to Wiki!",
  saveSummary: "{entities} entities, {concepts} concepts, {pages} pages",
  aliasAdded: "Added alias '{alias}' to page '{page}'",
  // Status Messages
  ingestingSources: "Ingesting sources...",
  queryingWiki: "Querying Wiki...",
  lintingWiki: "Linting Wiki...",
  savingToWiki: "Saving conversation to Wiki...",
  clearingHistory: "Clearing conversation history...",
  // Buttons
  ingestButton: "Ingest",
  queryButton: "Query",
  lintButton: "Lint",
  cancelButton: "Cancel",
  // Links
  karpathyGistLink: "Karpathy's LLM Wiki Gist",
  obsidianPluginAPI: "Obsidian Plugin API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // Other
  availableModelsLoading: "Loading available models...",
  noModelsAvailable: "No models available",
  // LLM Language Hint
  llmLanguageHint: "Please answer in English.",
  // Schema Configuration
  schemaSection: "Schema Configuration",
  enableSchemaName: "Enable Schema",
  enableSchemaDesc: "Generate and inject schema/config.md into all LLM prompts for structured Wiki output",
  viewSchemaButton: "View / Edit Schema",
  regenerateSchemaButton: "Regenerate Default Schema",
  schemaRegeneratedNotice: "Default schema regenerated.",
  schemaRegenerateFailed: "Schema generation failed",
  // Wiki Output Language
  wikiLanguageName: "Wiki Output Language",
  wikiLanguageDesc: "All generated wiki pages, index, log, and query responses use this language",
  customWikiLanguageOption: "Custom...",
  customWikiLanguageHint: "Enter a language name (e.g. Italian, Arabic). It will be passed to the LLM as the output language directive.",
  customWikiLanguagePlaceholder: "e.g. Italian",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "Extraction",
  extractionGranularityName: "Extraction Granularity",
  extractionGranularityDesc: "Controls entities/concepts extracted per source file. Higher = more pages, more API tokens.\nFine: deep analysis. Standard: daily notes. Coarse: quick overview. Minimal: batch 100+ files. Custom: set your own (up to 500).\nTip: Use Minimal/Coarse for folders with many files to save time and cost.",
  extractionGranularityFine: "Fine \u2014 deep analysis (\u2264100 items)",
  extractionGranularityStandard: "Standard \u2014 daily notes (\u226450 items)",
  extractionGranularityCoarse: "Coarse \u2014 quick overview (\u226410 items)",
  extractionGranularityMinimal: "Minimal \u2014 batch 100+ files (\u22645 items)",
  extractionGranularityCustom: "Custom \u2014 set your own limits (1~500)",
  customEntityLimitName: "Custom Entity Limit",
  customEntityLimitDesc: "Maximum number of entities to extract per source file (1-500)",
  customConceptLimitName: "Custom Concept Limit",
  customConceptLimitDesc: "Maximum number of concepts to extract per source file (1-500)",
  // Issue #85 v2: Tag Vocabulary (chip input UX, embedded in Wiki Configuration)
  tagVocabularyInlineDesc: 'Controlled vocabulary for entity and concept frontmatter tags. Obsidian nested tags with "/" are preserved.',
  tagVocabularyModeName: "Tag Vocabulary Configuration",
  tagVocabularyModeDescDefault: "Default uses built-in tags: {}. Switch to Custom to define your own.",
  tagVocabularyModeDescCustom: "Custom: define your own entity and concept tags below. Use the chip input \u2014 Enter or comma to add, \xD7 to remove.",
  tagVocabularyModeDefault: "Default (built-in subtype tags)",
  tagVocabularyModeCustom: "Custom (user-defined)",
  customEntityTagsName: "Custom Entity Tags",
  customEntityTagsDesc: 'Press Enter or comma to add a chip. Click \xD7 to remove. Nested tags with "/" are preserved.',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "Custom Concept Tags",
  customConceptTagsDesc: 'Press Enter or comma to add a chip. Click \xD7 to remove. Nested tags with "/" are preserved.',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "Duplicate tag skipped",
  // Ingestion Acceleration
  accelerationSectionTitle: "Ingestion Acceleration",
  pageGenerationConcurrencyName: "LLM Concurrency",
  pageGenerationConcurrencyDesc: "Number of LLM calls in parallel during ingestion and lint operations. Higher values speed up processing but increase API costs and may trigger rate limits.",
  concurrencyValueSingular: "Current concurrency: {} (serial \u2014 safest)",
  concurrencyValuePlural: "Current concurrency: {} (parallel)",
  batchDelayName: "Batch Delay (ms)",
  batchDelayDesc: "Delay between parallel batches to prevent API rate limiting (100-2000ms). Current: {}ms. Increase if you see 429 errors.",
  // Auto Maintenance
  autoMaintainSection: "Auto Maintenance",
  autoMaintainBetaBadge: "BETA \u2014 Experimental feature. May have issues. Recommended for advanced users only.",
  autoWatchName: "Watch Folders",
  autoWatchDesc: "Automatically detect new or changed .md files in the watched folders and notify or auto-ingest",
  watchedFoldersName: "Watched Folders",
  watchedFoldersDesc: 'Folders to watch for new content. Click "Add Folder" to select from your vault.',
  addWatchedFolderButton: "Add Folder",
  removeWatchedFolderButton: "Remove",
  webClipperPresetName: "Watch Clippings (Web Clipper)",
  webClipperPresetDesc: "Add the Clippings/ folder used by Obsidian Web Clipper to the watch list. Your web clips will be auto-ingested into the Wiki.",
  noWatchedFoldersHint: "No folders configured. Add a folder or enable the Clippings preset.",
  autoWatchModeName: "Watch Mode",
  autoWatchModeDesc: '"Notify Only" shows a prompt. "Auto Ingest" processes silently.',
  watchModeNotify: "Notify Only",
  watchModeAuto: "Auto Ingest",
  autoWatchDebounceName: "Debounce Delay (seconds)",
  autoWatchDebounceDesc: "Wait time before triggering ingest after a file change (1-60 seconds)",
  periodicLintName: "Periodic Lint",
  periodicLintDesc: "Run LLM lint on schedule, only when source files have changed since last check",
  periodicLintOff: "Off",
  periodicLintHourly: "Hourly",
  periodicLintDaily: "Daily",
  periodicLintWeekly: "Weekly",
  autoSmartFixName: "Auto Smart Fix",
  autoSmartFixDesc: "When lint runs, automatically apply all fixes (Smart Fix All) without showing the report modal. The fix summary is still shown on completion.",
  autoSmartFixNotice: "Auto Smart Fix: applying all fixes...",
  startupCheckName: "Run quick fixes on startup",
  startupCheckDesc: "Auto-fix low-level format issues (sources, double-nested links) on plugin load. Verifies Wiki folder structure. Default ON.",
  suggestSchemaCommand: "Suggest Schema Updates",
  autoMaintainCostWarning: '\u26A0\uFE0F Cost Notice: Auto-maintenance features consume API tokens. "Auto Ingest" triggers LLM calls on every source file change. "Periodic Lint" runs LLM health checks on schedule (only when source changes are detected). Configure carefully to avoid unexpected charges.',
  // Notices
  startupCheckSummary: "Wiki has {pages} pages ({entities} entities, {concepts} concepts, {sources} sources)",
  watcherActiveNotice: "Wiki: file watcher active \u2014 monitoring watched folders",
  watchIngestNotice: 'Wiki: {count} file(s) changed in sources/. Run "Ingest Sources" to process.',
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Wiki quick fixes complete",
  startupCheckStructureLabel: "Wiki structure",
  startupCheckStructureOk: "complete",
  startupCheckStructureMissing: "incomplete \u2014 will auto-create on first ingestion",
  startupCheckSourcesLabel: "Sources normalized",
  startupCheckSourcesClean: "already clean",
  startupCheckSourcesCleaned: "cleaned {files} file(s), {entries} entry(ies)",
  startupCheckIncompleteClean: "incomplete pages: none",
  startupCheckIncompleteArchived: "incomplete pages: archived {count} (recoverable from .trash)",
  startupCheckDisableHint: "To disable, go to Settings \u2192 Auto Maintenance \u2192 Run quick fixes on startup",
  autoIngestRunning: "Auto-ingesting {count} changed file(s)...",
  autoIngestComplete: "Auto-ingest complete: {success} succeeded, {fail} failed",
  scheduledLintRunning: "Running scheduled wiki lint...",
  wikiLintStats: "Wiki lint: {pages} pages ({entities} entities, {concepts} concepts, {sources} sources)",
  wikiHealthStats: "Wiki health: {pages} pages ({entities} entities, {concepts} concepts, {sources} sources){indexStatus}",
  lintWikiStart: "Starting wiki lint...",
  lintWikiComplete: "Wiki lint complete",
  lintWikiFailed: "Wiki lint failed",
  analyzingSchema: "Analyzing Wiki and generating schema suggestions...",
  schemaSuggestionGenerated: "Schema suggestions generated, see wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "No schema updates needed.",
  schemaSuggestionFailed: "Schema suggestion failed",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Schema update preview",
  schemaDiffSummary: "+{add} / -{del} lines",
  schemaDiffRegenerateLabel: "Refine the suggestion (optional):",
  schemaDiffRegenerateBtn: "Regenerate",
  schemaDiffOpenFileBtn: "Open file",
  schemaDiffApplyBtn: "Apply",
  schemaDiffApplied: "Schema updated. Backup: {path}",
  schemaDiffEmptyTitle: "No changes recommended",
  schemaDiffFailed: "Schema apply failed: {reason}",
  schemaDiffRestoreHint: 'Backup saved to {path}. To restore, rename that file back to "wiki/schema/config.md" in your file explorer. The most recent 3 backups are kept.',
  schemaRegenerateNoBody: "Regeneration succeeded but the LLM did not return a new body.",
  schemaNotFoundNotice: "Schema file not found. Enable schema to create it.",
  selectFolderNoMdFiles: "No Markdown files in folder: {path}",
  batchIngestSkipNotice: "Skipping {skipped}/{total} already-ingested files. Ingesting {new} new files...",
  batchIngestAllIngested: "All {total} files in this folder have already been ingested.",
  batchIngestStarting: 'Ingesting {count} file(s) from "{folder}" \u2014 this may take several minutes. A report will appear when complete.',
  batchIngestComplete: "Batch ingest complete: {success}/{total} succeeded, {fail} failed",
  batchIngestFailedFiles: "Failed files:",
  historyTruncated: "History truncated to last {max} rounds",
  historyCleared: "History cleared",
  // User Feedback Loop
  reviewedPagePreserved: "Preserving user-reviewed content for: {}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "Save to Wiki?",
  querySuggestSaveDesc: "This conversation contains valuable knowledge. Save it to your Wiki?",
  querySuggestSaveYes: "Save",
  querySuggestSaveNo: "Dismiss",
  // Ingestion Report
  ingestReportElapsedTime: "Elapsed time",
  ingestReportSkippedFiles: "Skipped (already ingested)",
  ingestReportRejectedFiles: "Skipped",
  rejectionReasonEmpty: "empty",
  rejectionReasonType: "unsupported type",
  rejectionReasonDuplicate: "duplicate content",
  ingestReportFailedGuidance: "These items could not be automatically created. You can manually create the corresponding pages, or lower the extraction granularity and re-ingest the source file.",
  ingestReportCollisions: "Cross-type collisions (merged as aliases)",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "Ingest single source",
  cmdIngestFolder: "Ingest from folder",
  cmdQueryWiki: "Query wiki",
  cmdLintWiki: "Lint wiki",
  cmdRegenerateIndex: "Regenerate index",
  cmdSuggestSchema: "Suggest schema updates",
  cmdCancelIngestion: "Cancel current ingestion",
  cmdIngestActiveFile: "Ingest current file",
  cmdViewHistory: "View operation history",
  noActiveFile: "No file is currently open",
  mdOnlyFile: "Only Markdown files can be ingested",
  // Ingestion status bar
  ingestionStatusBar: "Ingesting... click to cancel",
  lintStatusBar: "Linting... click to cancel",
  ingestStatusAnalyzing: "Ingesting\u2026 (click to cancel)",
  lintStatusReading: "Linting\u2026 (click to cancel)",
  lintStatusDuplicates: "Linting\u2026 (click to cancel)",
  lintStatusScanningLinks: "Linting\u2026 (click to cancel)",
  lintStatusAnalyzing: "Linting\u2026 (click to cancel)",
  ingestionCancelling: "Cancelling \u2014 will stop after current batch completes",
  ingestionCancelled: "Ingestion cancelled",
  crossTypeCollisionNotice: "{count} items merged as cross-type aliases (entity \u2194 concept duplicates prevented)",
  // Lint Report
  lintReportTitle: "Wiki lint report",
  lintReportSummary: "Wiki status overview: {total} pages total, {aliasesMissing} pages missing aliases, {duplicates} duplicate pages, {deadLinks} dead links ({deadLinkFromDup} involve duplicates), {orphans} orphan pages ({orphanFromDup} are duplicates), {emptyPages} empty pages, {ungroundedQuotes} ungrounded quotes, {tagViolations} out-of-vocabulary tags. Lint elapsed: {elapsedSeconds}s",
  // Advanced LLM Settings (v1.20.0: default = no provider-specific overrides)
  advancedSettingsModeName: "Advanced parameter settings",
  advancedSettingsModeDesc: "Default uses your provider's own behavior \u2014 no thinking-control, temperature, or repetition-penalty fields are sent. This works for most users. Switch to Custom only if you need to explicitly override the provider defaults (e.g. force a specific thinking-control dialect, set a non-default temperature, etc.).",
  advancedSettingsDefault: "Default (use provider behavior)",
  advancedSettingsCustom: "Custom (override provider defaults)",
  disableThinkingName: "Disable thinking",
  disableThinkingDesc: "Opt-in. When enabled, the plugin sends a thinking-control directive to the provider and walks a 3-tier dialect fallback chain (anthropic \u2192 openai \u2192 none) if the provider rejects it. Use this only if your provider leaks reasoning content into the answer and you need to suppress it. Most providers handle reasoning correctly on their own \u2014 leaving this off gives better quality.",
  // Issue #137: compatibility hints for advanced settings (kept short; no
  // provider list to avoid maintenance burden when providers change).
  extractionTemperatureName: "Extraction temperature",
  extractionTemperatureDesc: "Range 0\u20132. Lower values make LLM output more deterministic and faithful. Higher values make it more creative. Leave blank to use your provider's default. If your provider rejects this value, the plugin automatically strips the field and shows a one-time notice.",
  chatTemperatureName: "Query temperature",
  chatTemperatureDesc: "Range 0\u20132. Same as Extraction temperature, but only affects chat/query responses. Leave blank to use your provider's default. If your provider rejects this value, the plugin automatically strips the field and shows a one-time notice.",
  repetitionPenaltyName: "Repetition penalty",
  repetitionPenaltyDesc: "Range 0\u20132. Higher values reduce repetitive patterns. Leave blank to use your provider's default. Compatibility note: many cloud providers do not accept this field. The plugin auto-strips it on 400 and shows a one-time notice.",
  temperaturePlaceholder: "blank = provider default",
  lintDeadLinkSection: "Dead links (detected) [{count}]",
  lintEmptyPageSection: "Empty pages (detected) [{count}]",
  lintOrphanSection: "Orphan pages (detected) [{count}]",
  lintContradictionSection: "Contradictions (detected)",
  lintDuplicateSection: "Duplicate pages (detected)",
  lintPollutedSection: "Polluted pages (detected) [{count}]",
  lintPollutedItem: '- [[{page}]] \u2192 should be "{clean}"',
  lintSourcesNormalizedSection: "Sources normalized (auto-fixed) [{files} files / {entries} entries]",
  lintSourcesNormalizedItem: "Cleaned {entries} polluted sources entries across {files} file(s) (external paths, .md extensions, alias pipes removed and deduplicated).",
  lintNoIssuesFound: "No duplicates, dead links, empty pages, orphan pages, or ungrounded quotes detected.",
  lintQuoteGroundingSection: "Ungrounded quotes (detected) [{count}]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}: "{quote}"',
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (page does not exist){dupFlag}",
  lintDeadLinkMore: "- ... {count} more dead links",
  lintEmptyPageItem: "- [[{page}]] \u2014 less than 50 characters of substantive content",
  lintOrphanItem: "- [[{page}]] \u2014 no other Wiki pages link here{dupFlag}",
  lintDuplicateItem: "- [[{target}]] and [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F involves duplicate page)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F duplicate page)",
  lintContradictionOpen: "Open contradictions: {count}",
  lintContradictionAutoFixed: "({count} auto-fixed this run)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "Detected",
  lintContradictionStatusPendingFix: "Pending fix",
  lintLLMAnalysisHeading: "## LLM analysis",
  // Lint Analysis Prompt
  lintAnalysisPrompt: 'You are a Wiki maintenance assistant. Check the Wiki health based on the following information.\n\nWiki Index:\n{index}\n\nWiki Page Content Sample ({total} pages total, showing {sample} pages):\n{contentSample}\n\nProgrammatic check results (already verified, do not repeat):\n{progReport}\n\nCheck the following aspects (skip dead links/empty/orphans already detected by programmatic checks):\n1. **Contradictions** \u2014 whether different pages contradict each other on the same facts\n2. **Staleness** \u2014 whether any claims are clearly outdated\n3. **Missing** \u2014 which important concepts lack standalone pages\n4. **Structure** \u2014 whether page structure is reasonable and cross-references are adequate\n\nOutput format: Use Markdown, starting with "## LLM analysis". Each finding on one line "- [specific issue]". If no issues, write "No obvious issues found."',
  // Lint Fix Progress
  lintFixProgress: "Fixing {current}/{total}: [[{target}]]",
  lintFixDeadComplete: "Dead link fix complete. Fixed {fixed}/{total} items.",
  lintFillProgress: "Expanding {current}/{total}: {page}",
  lintFillComplete: "Page expansion complete. Filled {filled}/{total} pages.",
  lintDeleteCompleted: "Deleted {count} empty stubs",
  lintDeleteFailed: "Failed to delete {failed}/{total} empty stubs (see console for details)",
  lintFillFailed: "Failed to expand: {page} \u2014 {error}",
  lintLinkProgress: "Linking {current}/{total}: {page}",
  lintLinkComplete: "Orphan linking complete. Linked {linked} pages.",
  lintFixNoAction: "No action taken (no client)",
  lintFixIndexUpdated: "Wiki index and log updated.",
  lintFixAllComplete: "All fixes complete. See log for details.",
  lintPollutedFixed: "Polluted pages fixed: {fixed}/{total}. Index regenerated.",
  regenerateIndexCompleted: "Index regenerated",
  operationFailed: "Failed: ",
  // Lint Report Modal
  lintModalActionsTitle: "Fix suggestions (requires LLM tokens):",
  lintLogReference: "Full report saved to log.md",
  lintModalFixDeadLinks: "Fix dead links ({count})",
  lintModalExpandEmpty: "Expand empty pages ({count})",
  lintModalDeleteEmpty: "Delete empty stubs ({count})",
  lintModalLinkOrphans: "Link orphan pages ({count})",
  lintModalAnalyzeSchema: "Analyze schema",
  lintModalMergeDuplicates: "Merge duplicates ({count})",
  lintModalFixAll: "Smart fix all ({count} issues)",
  lintMergeProgress: "Merging {current}/{total}: {source} \u2192 {target}",
  lintMergeComplete: "Duplicate merge complete. Merged {merged}/{total} pairs.",
  // Ingest Report Modal
  ingestReportTitle: "Ingest report",
  ingestReportSourceFile: "Source file",
  ingestReportCreated: "Created",
  ingestReportUpdated: "Updated",
  ingestReportContradictions: "Contradictions found",
  ingestReportFailedTitle: "Failed to ingest",
  ingestReportErrorDetail: "Error detail",
  ingestReportClose: "Close",
  ingestReportCreatedPages: "Created pages: {count}",
  ingestReportUpdatedPages: "Updated pages: {count}",
  ingestReportEntitiesCount: "{count} entities",
  ingestReportConceptsCount: "{count} concepts",
  ingestReportContradictionsFound: "Contradictions found: {count}",
  ingestReportEntityType: "Entity",
  ingestReportConceptType: "Concept",
  timeMinutes: "min",
  timeSeconds: "sec",
  // Rate Limit Warnings
  rateLimitDetected: "\u26A0\uFE0F Rate limit detected: {count} page(s) failed with 429 errors. Try: (1) Lower concurrency to {suggestedConcurrency} or 1 (serial), (2) Increase batch delay to {suggestedDelay}ms, (3) Switch to a provider with higher rate limits.",
  rateLimitDetectedShort: "\u26A0\uFE0F Rate limit hit \u2014 consider lowering concurrency or increasing batch delay in Settings \u2192 Ingestion Acceleration.",
  // Long source warning
  sourceRejectedEmpty: `\u23ED\uFE0F "{filename}" has no content to ingest \u2014 skipped. Empty or frontmatter-only notes don't create wiki pages.`,
  sourceRejectedType: '\u23ED\uFE0F "{filename}" is not a supported file type \u2014 skipped. Only text notes (e.g. .md, .txt) can be ingested.',
  sourceRejectedDuplicate: '\u23ED\uFE0F "{filename}" skipped \u2014 its content is already in the wiki.',
  ingestRejectedSummary: "{count} file(s) skipped (empty, duplicate, or unsupported type).",
  reingestConfirmTitle: "Re-ingest this file?",
  reingestConfirmBody: 'The content of "{filename}" is already in the wiki. Re-ingest it anyway?',
  reingestConfirmYes: "Re-ingest",
  reingestConfirmNo: "Skip",
  longSourceNotice: '\u{1F4C4} "{filename}" has {lines} lines ({size}). Long texts require iterative batch extraction \u2014 the LLM reads the full document in multiple passes. This may take several minutes. Please be patient.',
  longSourceNoticeShort: "\u{1F4C4} Large file detected ({lines} lines). Ingestion may take a while.",
  // Ingestion History Panel (#122) — UI keys
  historyButton: "Operation History",
  historyButtonDesc: "View recent ingestions, lint reports, and other changes your Wiki has undergone",
  historyButtonOpen: "View History",
  historyModalTitle: "Operation History",
  historyModalSubtitle: "Recent operations from your LLM-Wiki",
  historyEmpty: "No operations recorded yet. Ingest a note or run Lint to populate this list.",
  historyReadError: "Could not read operation log: {error}",
  historyEntryKindIngest: "Ingest",
  historyEntryKindMaintenance: "Maintenance",
  historyEntryKindFix: "Fix",
  historyEntryKindOther: "Operation",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "Source: {source}",
  historyEntrySourceUnknown: "Untitled ingest",
  historyEntryCreatedLabel: "Created",
  historyEntryUpdatedLabel: "Updated",
  historyEntryCreatedCount: "{count} created",
  historyEntryUpdatedCount: "{count} updated",
  historyEntryNoChanges: "No page changes",
  historyEntryContradictions: "\u26A0\uFE0F {count} contradiction(s)",
  historyEntrySectionCreated: "Created pages",
  historyEntrySectionUpdated: "Updated pages",
  historyEntrySectionContradictions: "Contradictions",
  historyEntrySectionDetails: "Details",
  historyEntrySectionReport: "Report findings",
  historyEntryOpenPage: "Open",
  historyEntryDetailsNoContradictions: "No contradictions found",
  historySearchPlaceholder: "\u{1F50D} Search by source title or page path\u2026",
  historyFilterAll: "All",
  historyFilterIngest: "Ingest only",
  historyFilterMaintenance: "Maintenance only",
  historyFilterFix: "Fix only",
  historyFilterContradictions: "\u26A0\uFE0F Has contradictions",
  historyRefreshButton: "\u27F3 Refresh",
  historyExpandDay: "Click to expand",
  historyCollapseDay: "Click to collapse",
  historyShowMore: "Show {count} older entries",
  historyNoMatch: "No operations match your search/filter.",
  historyCloseButton: "Close",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  // KPI labels (Operation History Panel v2)
  historyKpiPages: "Pages",
  historyKpiDeadLinks: "Dead links",
  historyKpiOrphans: "Orphans",
  historyKpiEmpty: "Empty",
  historyKpiDuplicates: "Duplicates",
  historyKpiTagViolations: "Tag issues",
  historyKpiUnsourced: "Unsourced",
  historyKpiDuration: "Lint time",
  historyKpiDurationSec: "{seconds}s",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "Dead links ({count})",
  historySectionTagViolations: "Tag issues ({count})",
  historySectionOrphans: "Orphan pages ({count})",
  historySectionEmptyPages: "Empty pages ({count})",
  historySectionLlmAnalysis: "LLM analysis ({count})",
  historyDeadLinkSource: "Source",
  historyDeadLinkTarget: "Missing",
  historyOpenInLog: "Open in log.md",
  historyShowMoreItems: "Show {count} more",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 same",
  historyChipContradiction: "Contradiction",
  historyChipOutdated: "Outdated",
  historyChipMissing: "Missing",
  historyChipStructure: "Structure",
  historySeverityHigh: "High",
  historySeverityMedium: "Medium",
  historySeverityLow: "Low",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Your Wiki has {dead} dead link(s), {orphans} orphan(s), and {tags} tag issue(s). Last Lint took {duration}.",
  historyGlobalInsightClean: "Your Wiki is clean \u2014 no dead links, orphans, or tag issues detected.",
  historyGlobalInsightNoData: "No maintenance reports yet \u2014 run a Lint to populate.",
  // v3.1 — modal title, subtitle w/ count, time-range filter
  historyModalHeaderTitle: "Operation History",
  historyModalSubtitleWithCount: "Recent operations from your LLM-Wiki \xB7 {count} entries",
  historyTimeRangeAll: "All time",
  historyTimeRange1d: "Past day",
  historyTimeRange3d: "Past 3 days",
  historyTimeRange1w: "Past week",
  historyTimeRange1m: "Past month",
  // v3.1 — ingest metric cards
  historyIngestTotal: "Total pages",
  historyIngestByType: "By type",
  historyIngestSource: "Source file",
  historyIngestNoTimestamp: "No timestamp",
  historyIngestFirstTime: "First ingest",
  historyIngestLatestTime: "Latest ingest",
  historyTimeRangeCustom: "Custom range",
  historyCustomRangeFrom: "From",
  historyCustomRangeTo: "To",
  historyCustomRangeApply: "Apply",
  historyCustomRangeClear: "Clear"
};

// src/texts/zh.ts
var ZH_TEXTS = {
  // 插件信息
  pluginTitle: "Karpathy LLM Wiki \u8BBE\u7F6E",
  pluginIntro: "\u672C\u63D2\u4EF6\u4E3A Obsidian \u5B9E\u73B0\u4E86 {{link}} \u6982\u5FF5\u3002\u5B83\u8BFB\u53D6\u4F60\u7684\u7B14\u8BB0\uFF0C\u7528 AI \u63D0\u53D6\u5B9E\u4F53\u548C\u6982\u5FF5\uFF0C\u5728 Vault \u4E2D\u6784\u5EFA\u7ED3\u6784\u5316\u3001\u4E92\u8054\u7684\u77E5\u8BC6\u5E93\u3002",
  karpathyLinkText: "Andrej Karpathy \u7684 LLM Wiki",
  // 功能部分
  featuresTitle: "\u4F7F\u7528\u65B9\u5F0F",
  workflow1Title: "1. \u6444\u5165 (Ingest)",
  workflow1Desc: "\u9009\u62E9\u7B14\u8BB0 \u2192 AI \u63D0\u53D6\u5B9E\u4F53\u3001\u6982\u5FF5\u3001\u5173\u7CFB \u2192 \u751F\u6210 Wiki \u9875\u9762\u3002",
  workflow2Title: "2. \u67E5\u8BE2 (Query)",
  workflow2Desc: "\u4E0E Wiki \u5BF9\u8BDD\uFF0C\u7B54\u6848\u57FA\u4E8E\u4F60\u5DF2\u6444\u5165\u7684\u77E5\u8BC6\uFF0C\u800C\u975E\u901A\u7528\u77E5\u8BC6\u3002",
  workflow3Title: "3. \u7EF4\u62A4 (Maintain)",
  workflow3Desc: "\u81EA\u52A8\u751F\u6210\u7D22\u5F15\u3001\u4EA4\u53C9\u94FE\u63A5\u3001\u5065\u5EB7\u68C0\u67E5\uFF0C\u4FDD\u6301 Wiki \u6574\u6D01\u3002",
  // 语言切换
  languageTitle: "\u754C\u9762\u8BED\u8A00",
  languageDesc: "\u9009\u62E9\u8BBE\u7F6E\u9762\u677F\u7684\u663E\u793A\u8BED\u8A00\u3002\u91CD\u542F\u63D2\u4EF6\u4EE5\u5C06\u8BED\u8A00\u66F4\u6539\u5E94\u7528\u5230\u547D\u4EE4\u9762\u677F\u3002",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // 状态
  statusTitle: "LLM Client \u72B6\u6001",
  llmWikiStatusSection: "LLM-Wiki \u72B6\u6001",
  statusInitialized: "\u5DF2\u521D\u59CB\u5316",
  statusReady: "LLM \u5DF2\u5C31\u7EEA",
  statusNotReady: "LLM \u672A\u914D\u7F6E \u2014 \u8BF7\u5B8C\u6210\u4E0A\u65B9\u8BBE\u7F6E\u5E76\u901A\u8FC7\u8FDE\u63A5\u6D4B\u8BD5",
  statusNotInitialized: "\u672A\u521D\u59CB\u5316",
  currentProvider: "\u5F53\u524D\u63D0\u4F9B\u5546",
  // Provider 设置
  providerSection: "LLM \u914D\u7F6E",
  providerName: "LLM Provider",
  providerDesc: "\u9009\u62E9\u9884\u5B9A\u4E49\u63D0\u4F9B\u5546\u6216\u81EA\u5B9A\u4E49 OpenAI \u517C\u5BB9\u670D\u52A1\u3002\u5BF9\u4E8E Coding Plan \u7B49\u5957\u9910\uFF0C\u5EFA\u8BAE\u9009\u62E9\u81EA\u5B9A\u4E49 OpenAI/Anthropic \u5E76\u624B\u5DE5\u586B\u5199\u5BF9\u5E94 URL \u548C API Key",
  // API Key
  apiKeyName: "API Key",
  apiKeyDesc: "\u8F93\u5165\u63D0\u4F9B\u5546\u7684 API Key",
  apiKeyPlaceholder: "\u8F93\u5165 API Key",
  // Base URL
  baseUrlName: "API Base URL",
  baseUrlDescCustom: "\u5FC5\u586B\uFF1A\u81EA\u5B9A\u4E49 OpenAI \u517C\u5BB9\u670D\u52A1\u7684 endpoint",
  baseUrlDescOverride: "\u53EF\u9009\uFF1A\u8986\u76D6\u9884\u8BBE\u7684 Base URL",
  // Ollama 提示
  ollamaHint: "Ollama \u672C\u5730\u8FD0\u884C\uFF0C\u65E0\u9700 API Key",
  lmstudioHint: "LM Studio \u672C\u5730\u8FD0\u884C\uFF0CAPI Key \u53EF\u9009",
  maxTokensPerCallName: "\u4E0A\u4E0B\u6587\u7A97\u53E3",
  maxTokensPerCallDesc: "\u9650\u5236\u751F\u6210 Token \u4EE5\u9002\u914D\u6A21\u578B\u4E0A\u4E0B\u6587\u7A97\u53E3\u30020 = \u65E0\u9650\u5236\uFF08\u4E91\u7AEF\u9ED8\u8BA4\uFF09\u3002",
  slugCaseName: "\u6587\u4EF6\u540D\u5927\u5C0F\u5199",
  slugCaseDesc: "\u63A7\u5236\u751F\u6210\u7684 Wiki \u6587\u4EF6\u540D\u662F\u5426\u8F6C\u4E3A\u5C0F\u5199\u3002\u5BF9\u4E8E\u5927\u5C0F\u5199\u6709\u8BED\u4E49\u7684\u8BED\u8A00\uFF08\u5982\u5FB7\u8BED\u540D\u8BCD\uFF09\uFF0C\u5EFA\u8BAE\u9009\u62E9\u201C\u4FDD\u7559\u201D\u3002",
  slugCaseLower: "\u5C0F\u5199\uFF08\u9ED8\u8BA4\uFF09",
  slugCasePreserve: "\u4FDD\u7559\u5927\u5C0F\u5199",
  // 模型选择
  modelSection: "\u6A21\u578B\u9009\u62E9",
  fetchModelsName: "\u83B7\u53D6\u53EF\u7528\u6A21\u578B",
  fetchModelsDesc: "\u4ECE Provider API \u83B7\u53D6\u6700\u65B0\u7684\u6A21\u578B\u5217\u8868",
  fetchModelsButton: "\u83B7\u53D6\u6A21\u578B\u5217\u8868",
  fetchingModels: "\u83B7\u53D6\u4E2D...",
  fetchSuccess: "\u83B7\u53D6\u6210\u529F\uFF01\u5171 {} \u4E2A\u53EF\u7528\u6A21\u578B",
  fetchFailed: "\u83B7\u53D6\u5931\u8D25\u6216\u5217\u8868\u4E3A\u7A7A\uFF0C\u8BF7\u624B\u52A8\u8F93\u5165\u6A21\u578B\u540D\u79F0",
  fetchNotSupported: "\u8BE5 Provider \u4E0D\u652F\u6301\u6A21\u578B\u5217\u8868\u67E5\u8BE2",
  fetchErrorAuth: "\u8BA4\u8BC1\u5931\u8D25\uFF08HTTP 401/403\uFF09\u3002\u8BF7\u68C0\u67E5 API Key \u662F\u5426\u6B63\u786E\uFF0C\u6216\u76F4\u63A5\u8F93\u5165\u6A21\u578B ID \u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5\u8FDB\u884C\u9A8C\u8BC1\u3002",
  fetchErrorEndpoint: "\u63A5\u5165\u70B9\u672A\u627E\u5230\uFF08HTTP 404\uFF09\u3002\u8BF7\u68C0\u67E5 BaseURL \u662F\u5426\u6B63\u786E\uFF0C\u6216\u76F4\u63A5\u8F93\u5165\u6A21\u578B ID \u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5\u3002",
  fetchErrorServer: "Provider \u670D\u52A1\u7AEF\u9519\u8BEF\uFF08HTTP 5xx\uFF09\u3002\u7A0D\u540E\u91CD\u8BD5\uFF0C\u6216\u76F4\u63A5\u8F93\u5165\u6A21\u578B ID \u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5\u3002",
  fetchErrorEmpty: "\u8BE5\u63A5\u5165\u70B9\u4E0D\u652F\u6301\u5217\u51FA\u6A21\u578B\u5217\u8868\u3002\u8BF7\u76F4\u63A5\u8F93\u5165\u6A21\u578B ID \u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5\u8FDB\u884C\u9A8C\u8BC1\u3002",
  fetchErrorNetwork: "\u7F51\u7EDC\u8BF7\u6C42\u5931\u8D25\u3002\u8BF7\u68C0\u67E5\u7F51\u7EDC\u8FDE\u63A5\u3001BaseURL \u6216\u4EE3\u7406\u8BBE\u7F6E\u3002\u60A8\u4E5F\u53EF\u4EE5\u76F4\u63A5\u8F93\u5165\u6A21\u578B ID \u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5\u8FDB\u884C\u9A8C\u8BC1\u3002",
  selectModelName: "\u9009\u62E9\u6A21\u578B",
  selectModelDesc: "\u4ECE {} \u4E2A\u53EF\u7528\u6A21\u578B\u4E2D\u9009\u62E9",
  customInputOption: "\u81EA\u5B9A\u4E49\u8F93\u5165...",
  customInputHint: '\u5982\u9700\u4F7F\u7528\u5176\u4ED6\u6A21\u578B\uFF0C\u8BF7\u9009\u62E9"\u81EA\u5B9A\u4E49\u8F93\u5165..."',
  modelName: "\u6A21\u578B\u540D\u79F0",
  modelDescCustom: "\u5F53\u524D\u4F7F\u7528\u81EA\u5B9A\u4E49\u6A21\u578B\uFF08\u53EF\u91CD\u65B0\u83B7\u53D6\u5217\u8868\uFF09",
  modelDescFetchFailed: "\u6A21\u578B\u5217\u8868\u83B7\u53D6\u5931\u8D25\u3002\u8BF7\u68C0\u67E5 API Key \u548C\u63A5\u5165\u70B9 URL\uFF0C\u6216\u76F4\u63A5\u8F93\u5165\u6A21\u578B ID \u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5\u8FDB\u884C\u9A8C\u8BC1\u3002",
  modelInputPlaceholder: "\u8F93\u5165\u6A21\u578B ID\uFF0C\u7136\u540E\u70B9\u51FB\u6D4B\u8BD5\u8FDE\u63A5",
  switchToDropdown: "\u5207\u6362\u5230\u4E0B\u62C9\u9009\u62E9",
  useDropdownButton: "\u4F7F\u7528\u4E0B\u62C9\u9009\u62E9",
  // 测试 & 保存
  testConnectionName: "\u6D4B\u8BD5\u8FDE\u63A5",
  testConnectionDesc: "\u9A8C\u8BC1\u914D\u7F6E\u80FD\u5426\u6210\u529F\u8C03\u7528 LLM API",
  testButton: "\u6D4B\u8BD5\u8FDE\u63A5",
  testing: "\u6D4B\u8BD5\u4E2D...",
  saveSettingsName: "\u4FDD\u5B58\u8BBE\u7F6E",
  saveSettingsDesc: "\u4FDD\u5B58\u5F53\u524D\u914D\u7F6E",
  saveButton: "\u4FDD\u5B58\u8BBE\u7F6E",
  testConnectionSuccessful: "\u8FDE\u63A5\u6210\u529F",
  testConnectionFailed: "\u8FDE\u63A5\u5931\u8D25",
  testConnectionProvider: "\u63D0\u4F9B\u5546\uFF1A",
  errorUnknown: "\u672A\u77E5\u9519\u8BEF",
  savedNotice: "\u8BBE\u7F6E\u5DF2\u4FDD\u5B58\uFF01",
  // Issue #137: LLM 回退通知（请求中触发 thinking-dialect 回退或字段剥离时显示）
  fallbackThinkingDialect: '\u601D\u8003\u63A7\u5236\uFF1A\u5DF2\u5207\u6362\u81F3 "{dialect}" \u65B9\u8A00\uFF08\u8BE5\u63D0\u4F9B\u5546\u4F7F\u7528\u4E0D\u540C\u7684\u601D\u8003\u63A7\u5236\u5B57\u6BB5\u683C\u5F0F\uFF09\u3002\u8F93\u51FA\u5185\u5BB9\u4E0D\u53D7\u5F71\u54CD\u3002',
  fallbackThinkingNone: "\u8BE5\u63D0\u4F9B\u5546\u5DF2\u5B8C\u5168\u5173\u95ED\u601D\u8003\u63A7\u5236\u3002\u6A21\u578B\u4ECD\u53EF\u80FD\u4EA7\u751F\u63A8\u7406\u5185\u5BB9\uFF1B\u82E5\u51FA\u73B0\uFF0C\u8BF7\u5C1D\u8BD5\u5176\u4ED6\u6A21\u578B\u3002",
  fallbackParamStripped: '\u53C2\u6570 "{field}" \u4E0D\u88AB\u8BE5\u63D0\u4F9B\u5546\u652F\u6301\u3002\u5DF2\u4ECE\u8BF7\u6C42\u4E2D\u79FB\u9664\uFF1B\u884C\u4E3A\u53EF\u80FD\u4E0E\u914D\u7F6E\u4E0D\u540C\u3002',
  // Wiki 文件夹
  wikiSection: "Wiki \u914D\u7F6E",
  wikiFolderName: "Wiki \u6587\u4EF6\u5939",
  wikiFolderDesc: "\u5B58\u653E\u751F\u6210\u7684 Wiki \u9875\u9762",
  wikiFolderPlaceholder: "wiki",
  // 错误
  errorNoApiKey: "\u8BF7\u5148\u914D\u7F6E API Key",
  llmNotReady: "LLM \u5C1A\u672A\u914D\u7F6E\u3002\u8BF7\u524D\u5F80 \u8BBE\u7F6E \u2192 Karpathy LLM Wiki \u5B8C\u6210 Provider \u914D\u7F6E\u3001\u83B7\u53D6\u6A21\u578B\u5217\u8868\u5E76\u901A\u8FC7\u8FDE\u63A5\u6D4B\u8BD5\u3002",
  errorFetchFailed: "\u83B7\u53D6\u5931\u8D25\uFF1A{}",
  // Query 设置
  querySectionTitle: "Wiki \u67E5\u8BE2\u914D\u7F6E",
  maxConversationHistoryName: "\u5BF9\u8BDD\u5386\u53F2\u4E0A\u9650",
  maxConversationHistoryDesc: "\u9650\u5236\u5BF9\u8BDD\u6D88\u606F\u6570\uFF0C\u907F\u514D\u8D85\u51FALLM token\u9650\u5236",
  maxConversationHistoryHint: "\u63A8\u8350\uFF1A\u4E0D\u8D85\u8FC750\u8F6E",
  numberRangeValidation: "\u8BF7\u8F93\u51651-50\u4E4B\u95F4\u7684\u6570\u5B57",
  numberRangeClamped: "\u6570\u503C\u8D85\u51FA\u8303\u56F4\uFF081-500\uFF09\uFF0C\u5DF2\u81EA\u52A8\u8BBE\u5B9A\u4E3A {}",
  // Query Modal UI
  queryModalTitle: "Query Wiki - \u5BF9\u8BDD\u5F0F\u67E5\u8BE2",
  queryModalPlaceholder: "\u8F93\u5165\u95EE\u9898...",
  queryModalSendButton: "\u53D1\u9001",
  queryModalStopButton: "\u505C\u6B62",
  queryModalSaveButton: "\u4FDD\u5B58\u5230Wiki",
  queryModalClearButton: "\u6E05\u7A7A\u5386\u53F2",
  queryModalHistoryCount: "\u5BF9\u8BDD\u5386\u53F2: {}/{} \u8F6E",
  queryModalStreaming: "\u6D41\u5F0F\u751F\u6210\u4E2D...",
  queryModalFallbackStreaming: "\u7AEF\u70B9\u4E0D\u652F\u6301\u6D41\u5F0F\u4F20\u8F93\uFF0C\u5DF2\u56DE\u9000\u81F3\u975E\u6D41\u5F0F\u6A21\u5F0F\uFF0C\u8BF7\u8010\u5FC3\u7B49\u5F85...",
  queryPhaseSearching: "\u6B63\u5728\u5206\u6790 Wiki \u7D22\u5F15\uFF0C\u67E5\u627E\u76F8\u5173\u8D44\u6599...",
  queryPhaseFoundPages: "\u627E\u5230 {count} \u4E2A\u8D44\u6599\uFF1A{pages}",
  queryPhaseLoadingPages: "\u6B63\u5728\u52A0\u8F7D\u9875\u9762\u5185\u5BB9...",
  queryPhaseContextReady: "\u8D44\u6599\u5C31\u7EEA\uFF0C\u6B63\u5728\u751F\u6210\u56DE\u590D...",
  queryPhaseGenerating: "\u6B63\u5728\u751F\u6210\u56DE\u590D...\uFF08\u5DF2\u7B49\u5F85 {time}s\uFF09",
  queryPhaseNonStreaming: "\u975E\u6D41\u5F0F\u6A21\u5F0F\uFF0C\u751F\u6210\u4E2D\uFF0C\u8BF7\u8010\u5FC3\u7B49\u5F85...\uFF08\u5DF2\u7B49\u5F85 {time}s\uFF09",
  queryModalErrorPrefix: "\u9519\u8BEF\uFF1A",
  queryModalHint: '\u67E5\u8BE2\u57FA\u4E8EWiki\u5185\u5BB9\u3002\u70B9\u51FB"\u4FDD\u5B58\u5230Wiki"\u53EF\u5C06\u6709\u4EF7\u503C\u5BF9\u8BDD\u63D0\u70BC\u4E3AWiki\u9875\u9762\u3002',
  // v1.20.0: Query Wiki 思考内容折叠摘要
  queryThinkingSummary: "\u601D\u8003\u8FC7\u7A0B",
  queryThinkingSteps: "\u6B65",
  // 错误消息
  errorLLMClientNotInit: "LLM Client \u672A\u521D\u59CB\u5316\u3002\u8BF7\u4FDD\u5B58\u8BBE\u7F6E\u3002",
  errorIngestFailed: "\u6444\u5165\u5931\u8D25\uFF1A",
  errorQueryFailed: "\u67E5\u8BE2\u5931\u8D25\uFF1A",
  fileWriteFailed: "\u65E0\u6CD5\u521B\u5EFA\u6216\u66F4\u65B0\u6587\u4EF6\uFF1A{path}",
  // 成功消息
  ingestSuccess: "\u6444\u5165\u6210\u529F\uFF1A\u521B\u5EFA{}\u9875\uFF0C\u66F4\u65B0{}\u9875",
  querySuccess: "\u67E5\u8BE2\u5B8C\u6210",
  lintSuccess: "Wiki \u7EF4\u62A4\u5B8C\u6210",
  lintReadingPages: "\u6B63\u5728\u8BFB\u53D6 {count} \u4E2A Wiki \u9875\u9762...",
  lintReadingPagesProgress: "\u6B63\u5728\u8BFB\u53D6 Wiki \u9875\u9762\uFF1A{current}/{total}...",
  lintScanningLinks: "\u6B63\u5728\u626B\u63CF\u65AD\u94FE...",
  lintScanningLinksProgress: "\u6B63\u5728\u626B\u63CF\u65AD\u94FE\uFF1A{current}/{total}...",
  lintCheckingDuplicates: "\u6B63\u5728\u68C0\u67E5\u91CD\u590D\u9875\u9762...",
  lintCheckingDuplicatesProgress: "\u6B63\u5728\u6821\u9A8C\u91CD\u590D\u9879\uFF1A\u6279\u6B21 {current} ...",
  lintFixingPolluted: "\u6B63\u5728\u4FEE\u590D\u6C61\u67D3\u9875\u9762 {current}/{total}\uFF1A{title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} \u4FEE\u590D\u6C61\u67D3\u9875\u9762\uFF08{count}\uFF09",
  lintDuplicateCheckFailed: "\u91CD\u590D\u68C0\u6D4B\u5931\u8D25 \u2014 \u8BE6\u89C1\u63A7\u5236\u53F0",
  lintDuplicateCheckFailedDetail: "\u91CD\u590D\u68C0\u6D4B\u5728{step}\u5931\u8D25\uFF1A{error}",
  lintMergeItemFailed: "\u5408\u5E76\u5931\u8D25\uFF1A{source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "\u68C0\u6D4B\u5230 {count} \u4E2A\u9875\u9762\u7F3A\u5C11\u522B\u540D",
  lintAliasesSection: "\u7F3A\u5931\u522B\u540D\u7684\u9875\u9762\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "\u8865\u5168\u522B\u540D\uFF08{count}\uFF09",
  lintAliasesFilling: "\u751F\u6210\u522B\u540D {current}/{total}\uFF1A{page}",
  lintAliasesFilled: "\u522B\u540D\u8865\u5168\u5B8C\u6210\u3002\u5DF2\u586B\u5145 {filled}/{total} \u9875\u3002",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "\u6807\u7B7E\u8D8A\u754C\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "\u522B\u540D\u751F\u6210\u5931\u8D25\uFF1A{page} \u2014 {error}",
  lintFixItemFailed: "\u4FEE\u590D\u5931\u8D25\uFF1A[[{target}]] \u2014 {error}",
  lintLinkItemFailed: "\u94FE\u63A5\u5931\u8D25\uFF1A{page} \u2014 {error}",
  lintRetrying: "\u51FA\u9519\u540E\u91CD\u8BD5\u4E2D\uFF08{attempt}/{max}\uFF09...",
  lintAnalyzingLLM: "LLM \u6B63\u5728\u5206\u6790 Wiki \u5065\u5EB7\u72B6\u6001...",
  saveToWikiSuccess: "\u5BF9\u8BDD\u5DF2\u4FDD\u5B58\u5230Wiki\uFF01",
  saveSummary: "{entities} \u4E2A\u5B9E\u4F53, {concepts} \u4E2A\u6982\u5FF5, {pages} \u4E2A\u9875\u9762",
  aliasAdded: "\u5DF2\u4E3A\u9875\u9762'{page}'\u6DFB\u52A0\u522B\u540D'{alias}'",
  // 状态消息
  ingestingSources: "\u6B63\u5728\u6444\u5165\u6E90\u6587\u4EF6...",
  queryingWiki: "\u6B63\u5728\u67E5\u8BE2 Wiki...",
  lintingWiki: "\u6B63\u5728\u7EF4\u62A4 Wiki...",
  savingToWiki: "\u6B63\u5728\u4FDD\u5B58\u5BF9\u8BDD\u5230Wiki...",
  clearingHistory: "\u6E05\u7A7A\u5BF9\u8BDD\u5386\u53F2...",
  // 按钮
  ingestButton: "\u6444\u5165",
  queryButton: "\u67E5\u8BE2",
  lintButton: "\u7EF4\u62A4",
  cancelButton: "\u53D6\u6D88",
  // 链接
  karpathyGistLink: "Karpathy's LLM Wiki Gist",
  obsidianPluginAPI: "Obsidian Plugin API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // 其他
  availableModelsLoading: "\u52A0\u8F7D\u53EF\u7528\u6A21\u578B...",
  noModelsAvailable: "\u65E0\u53EF\u7528\u6A21\u578B",
  // LLM 语言提示
  llmLanguageHint: "\u8BF7\u7528\u4E2D\u6587\u56DE\u7B54\u3002",
  // Schema 配置
  schemaSection: "Schema \u914D\u7F6E",
  enableSchemaName: "\u542F\u7528 Schema",
  enableSchemaDesc: "\u751F\u6210 schema/config.md \u5E76\u6CE8\u5165\u5230\u6240\u6709 LLM \u63D0\u793A\u8BCD\u4E2D\uFF0C\u6307\u5BFC Wiki \u7ED3\u6784\u5316\u8F93\u51FA",
  viewSchemaButton: "\u67E5\u770B / \u7F16\u8F91 Schema",
  regenerateSchemaButton: "\u91CD\u65B0\u751F\u6210\u9ED8\u8BA4 Schema",
  schemaRegeneratedNotice: "\u9ED8\u8BA4 Schema \u5DF2\u91CD\u65B0\u751F\u6210\u3002",
  schemaRegenerateFailed: "Schema \u751F\u6210\u5931\u8D25",
  // Wiki 初始化状态
  wikiInitStatusReady: "Wiki \u5DF2\u521D\u59CB\u5316",
  wikiInitStatusNotReady: "Wiki \u672A\u521D\u59CB\u5316 \u2014 \u5C06\u5728\u9996\u6B21\u6444\u5165\u65F6\u81EA\u52A8\u521B\u5EFA",
  // Wiki 输出语言
  wikiLanguageName: "Wiki \u8F93\u51FA\u8BED\u8A00",
  wikiLanguageDesc: "\u6240\u6709\u751F\u6210\u7684 Wiki \u9875\u9762\u3001\u7D22\u5F15\u3001\u65E5\u5FD7\u548C\u67E5\u8BE2\u56DE\u590D\u5747\u4F7F\u7528\u6B64\u8BED\u8A00",
  customWikiLanguageOption: "\u81EA\u5B9A\u4E49...",
  customWikiLanguageHint: "\u8F93\u5165\u8BED\u8A00\u540D\u79F0\uFF08\u5982 Italian\u3001Arabic\uFF09\uFF0C\u5C06\u4F5C\u4E3A\u8F93\u51FA\u8BED\u8A00\u6307\u4EE4\u4F20\u9012\u7ED9 LLM\u3002",
  customWikiLanguagePlaceholder: "\u4F8B\u5982\uFF1AItalian",
  // 索引 & 日志标签（各语言）
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // 知识提取
  extractionSectionTitle: "\u77E5\u8BC6\u63D0\u53D6",
  extractionGranularityName: "\u63D0\u53D6\u9897\u7C92\u5EA6",
  extractionGranularityDesc: "\u63A7\u5236\u6BCF\u7BC7\u6E90\u6587\u4EF6\u63D0\u53D6\u7684\u5B9E\u4F53/\u6982\u5FF5\u6570\u91CF\u3002\u8D8A\u9AD8 = \u751F\u6210\u9875\u9762\u8D8A\u591A\uFF0CAPI\u6D88\u8017\u8D8A\u5927\u3002\n\u7CBE\u7EC6\uFF1A\u5355\u7BC7\u6DF1\u5EA6\u3002\u6807\u51C6\uFF1A\u65E5\u5E38\u7B14\u8BB0\u3002\u7C97\u653E\uFF1A\u5FEB\u901F\u6982\u89C8\u3002\u6781\u7B80\uFF1A\u6279\u91CF100+\u6587\u4EF6\u3002\u81EA\u5B9A\u4E49\uFF1A\u81EA\u884C\u8BBE\u5B9A\uFF08\u6700\u9AD8300\u4E2A\uFF09\u3002\n\u63D0\u793A\uFF1A\u6279\u91CF\u5904\u7406\u5927\u91CF\u6587\u4EF6\u65F6\uFF0C\u5EFA\u8BAE\u4F7F\u7528\u6781\u7B80\u6216\u7C97\u653E\u4EE5\u8282\u7701\u65F6\u95F4\u548C\u6210\u672C\u3002",
  extractionGranularityFine: "\u7CBE\u7EC6 \u2014 \u5355\u7BC7\u6DF1\u5EA6\u5206\u6790\uFF08\u2264100\u4E2A\uFF09",
  extractionGranularityStandard: "\u6807\u51C6 \u2014 \u65E5\u5E38\u7B14\u8BB0\uFF08\u226450\u4E2A\uFF09",
  extractionGranularityCoarse: "\u7C97\u653E \u2014 \u5FEB\u901F\u6982\u89C8\uFF08\u226410\u4E2A\uFF09",
  extractionGranularityMinimal: "\u6781\u7B80 \u2014 \u6279\u91CF100+\u6587\u4EF6\uFF08\u22645\u4E2A\uFF09",
  extractionGranularityCustom: "\u81EA\u5B9A\u4E49 \u2014 \u81EA\u884C\u8BBE\u5B9A\u4E0A\u9650\uFF081~500\uFF09",
  customEntityLimitName: "\u81EA\u5B9A\u4E49\u5B9E\u4F53\u4E0A\u9650",
  customEntityLimitDesc: "\u6BCF\u7BC7\u6E90\u6587\u4EF6\u6700\u591A\u63D0\u53D6\u7684\u5B9E\u4F53\u6570\u91CF\uFF081-500\uFF09",
  customConceptLimitName: "\u81EA\u5B9A\u4E49\u6982\u5FF5\u4E0A\u9650",
  customConceptLimitDesc: "\u6BCF\u7BC7\u6E90\u6587\u4EF6\u6700\u591A\u63D0\u53D6\u7684\u6982\u5FF5\u6570\u91CF\uFF081-500\uFF09",
  // Issue #85 v2: 标签词汇表 (chip input 交互, 嵌入 Wiki 配置)
  tagVocabularyInlineDesc: '\u4E3A entity \u548C concept frontmatter \u6807\u7B7E\u8BBE\u7F6E\u53D7\u63A7\u8BCD\u6C47\u3002Obsidian \u5D4C\u5957\u6807\u7B7E "/" \u4F1A\u88AB\u4FDD\u7559\u3002',
  tagVocabularyModeName: "\u6807\u7B7E\uFF08Tags)\u8BCD\u6C47\u8868\u914D\u7F6E",
  tagVocabularyModeDescDefault: '\u9ED8\u8BA4\u4F7F\u7528\u5185\u7F6E\u6807\u7B7E: {}\u3002\u5207\u6362\u5230"\u81EA\u5B9A\u4E49"\u53EF\u5B9A\u4E49\u60A8\u81EA\u5DF1\u7684\u8BCD\u6C47\u3002',
  tagVocabularyModeDescCustom: "\u81EA\u5B9A\u4E49\uFF1A\u5728\u4E0B\u65B9\u4F7F\u7528 chip \u8F93\u5165\u5B9A\u4E49\u60A8\u81EA\u5DF1\u7684 entity \u4E0E concept \u6807\u7B7E\u3002\u56DE\u8F66\u6216\u9017\u53F7\u6DFB\u52A0, \xD7 \u5220\u9664\u3002",
  tagVocabularyModeDefault: "\u9ED8\u8BA4 (\u5185\u7F6E\u5B50\u7C7B\u578B\u6807\u7B7E)",
  tagVocabularyModeCustom: "\u81EA\u5B9A\u4E49 (\u7528\u6237\u5B9A\u4E49)",
  customEntityTagsName: "\u81EA\u5B9A\u4E49\u5B9E\u4F53\u6807\u7B7E",
  customEntityTagsDesc: '\u6309\u56DE\u8F66\u6216\u9017\u53F7\u6DFB\u52A0 chip, \u70B9 \xD7 \u5220\u9664\u3002\u5D4C\u5957\u6807\u7B7E "/" \u4F1A\u88AB\u4FDD\u7559\u3002',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "\u81EA\u5B9A\u4E49\u6982\u5FF5\u6807\u7B7E",
  customConceptTagsDesc: '\u6309\u56DE\u8F66\u6216\u9017\u53F7\u6DFB\u52A0 chip, \u70B9 \xD7 \u5220\u9664\u3002\u5D4C\u5957\u6807\u7B7E "/" \u4F1A\u88AB\u4FDD\u7559\u3002',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "\u91CD\u590D\u6807\u7B7E\u5DF2\u8DF3\u8FC7",
  // 摄入加速
  accelerationSectionTitle: "\u6444\u5165\u52A0\u901F",
  pageGenerationConcurrencyName: "LLM \u5E76\u53D1\u5EA6",
  pageGenerationConcurrencyDesc: "\u6444\u5165\u548C Lint \u64CD\u4F5C\u4E2D\u7684\u5E76\u884C LLM \u8C03\u7528\u6570\u3002\u6570\u503C\u8D8A\u9AD8\u5904\u7406\u8D8A\u5FEB\uFF0C\u4F46\u4F1A\u589E\u52A0 API \u6D88\u8017\u5E76\u53EF\u80FD\u89E6\u53D1\u9650\u6D41\u3002",
  concurrencyValueSingular: "\u5F53\u524D\u5E76\u53D1\u5EA6\uFF1A{}\uFF08\u4E32\u884C \u2014 \u6700\u5B89\u5168\uFF09",
  concurrencyValuePlural: "\u5F53\u524D\u5E76\u53D1\u5EA6\uFF1A{}\uFF08\u5E76\u884C\uFF09",
  batchDelayName: "\u6279\u6B21\u5EF6\u8FDF (ms)",
  batchDelayDesc: "\u5E76\u884C\u6279\u6B21\u95F4\u7684\u5EF6\u8FDF\uFF0C\u9632\u6B62 API \u9650\u6D41\uFF08100-2000ms\uFF09\u3002\u5F53\u524D\u503C\uFF1A{}ms\u3002\u5982\u9047 429 \u9519\u8BEF\u8BF7\u589E\u5927\u6B64\u503C\u3002",
  // 自动维护
  autoMaintainSection: "\u81EA\u52A8\u7EF4\u62A4",
  autoMaintainBetaBadge: "BETA \u6D4B\u8BD5 \u2014 \u5B9E\u9A8C\u6027\u529F\u80FD\uFF0C\u53EF\u80FD\u5B58\u5728\u95EE\u9898\uFF0C\u4EC5\u5EFA\u8BAE\u9AD8\u7EA7\u7528\u6237\u4F7F\u7528\u3002",
  autoWatchName: "\u76D1\u542C\u6587\u4EF6\u5939",
  autoWatchDesc: "\u81EA\u52A8\u68C0\u6D4B\u76D1\u542C\u5217\u8868\u4E2D\u6587\u4EF6\u5939\u7684\u65B0\u589E\u6216\u53D8\u66F4 .md \u6587\u4EF6\uFF0C\u901A\u77E5\u6216\u81EA\u52A8\u6444\u5165",
  watchedFoldersName: "\u76D1\u542C\u6587\u4EF6\u5939\u5217\u8868",
  watchedFoldersDesc: "\u76D1\u542C\u65B0\u5185\u5BB9\u7684\u6587\u4EF6\u5939\u3002\u70B9\u51FB\u300C\u6DFB\u52A0\u6587\u4EF6\u5939\u300D\u4ECE\u4ED3\u5E93\u4E2D\u9009\u62E9\u3002",
  addWatchedFolderButton: "\u6DFB\u52A0\u6587\u4EF6\u5939",
  removeWatchedFolderButton: "\u79FB\u9664",
  webClipperPresetName: "\u76D1\u542C Clippings\uFF08Web Clipper\uFF09",
  webClipperPresetDesc: "\u5C06 Obsidian Web Clipper \u4F7F\u7528\u7684 Clippings/ \u6587\u4EF6\u5939\u52A0\u5165\u76D1\u542C\u5217\u8868\u3002\u7F51\u9875\u526A\u85CF\u5185\u5BB9\u5C06\u81EA\u52A8\u6444\u5165 Wiki\u3002",
  noWatchedFoldersHint: "\u5C1A\u672A\u914D\u7F6E\u76D1\u542C\u6587\u4EF6\u5939\u3002\u8BF7\u6DFB\u52A0\u6587\u4EF6\u5939\u6216\u542F\u7528 Clippings \u9884\u8BBE\u3002",
  autoWatchModeName: "\u76D1\u542C\u6A21\u5F0F",
  autoWatchModeDesc: '"\u4EC5\u901A\u77E5"\u663E\u793A\u63D0\u793A\u3002"\u81EA\u52A8\u6444\u5165"\u9759\u9ED8\u5904\u7406\u3002',
  watchModeNotify: "\u4EC5\u901A\u77E5",
  watchModeAuto: "\u81EA\u52A8\u6444\u5165",
  autoWatchDebounceName: "\u9632\u6296\u5EF6\u8FDF (\u79D2)",
  autoWatchDebounceDesc: "\u6587\u4EF6\u53D8\u66F4\u540E\u7B49\u5F85\u591A\u4E45\u89E6\u53D1\u6444\u5165 (1-60\u79D2)",
  periodicLintName: "\u5B9A\u65F6\u7EF4\u62A4",
  periodicLintDesc: "\u5B9A\u671F\u68C0\u67E5 Wiki \u5065\u5EB7\u72B6\u51B5\uFF0C\u6709\u6E90\u6587\u4EF6\u53D8\u66F4\u65F6\u624D\u6267\u884C LLM \u7EF4\u62A4",
  periodicLintOff: "\u5173\u95ED",
  periodicLintHourly: "\u6BCF\u5C0F\u65F6",
  periodicLintDaily: "\u6BCF\u5929",
  periodicLintWeekly: "\u6BCF\u5468",
  autoSmartFixName: "\u81EA\u52A8\u667A\u80FD\u4FEE\u590D",
  autoSmartFixDesc: "\u8FD0\u884C Lint \u65F6\uFF0C\u81EA\u52A8\u6267\u884C\u6240\u6709\u4FEE\u590D\uFF08\u667A\u80FD\u4E00\u952E\u4FEE\u590D\uFF09\uFF0C\u65E0\u9700\u663E\u793A\u62A5\u544A\u5F39\u7A97\u3002\u4FEE\u590D\u5B8C\u6210\u540E\u4ECD\u4F1A\u663E\u793A\u6458\u8981\u3002",
  autoSmartFixNotice: "\u81EA\u52A8\u667A\u80FD\u4FEE\u590D\uFF1A\u6B63\u5728\u5E94\u7528\u6240\u6709\u4FEE\u590D...",
  startupCheckName: "\u542F\u52A8\u65F6\u6267\u884C\u5FEB\u901F\u4FEE\u590D",
  startupCheckDesc: "\u63D2\u4EF6\u52A0\u8F7D\u65F6\u81EA\u52A8\u4FEE\u590D\u4F4E\u7EA7\u683C\u5F0F\u95EE\u9898\uFF08sources \u5B57\u6BB5\u3001\u53CC\u5C42 wikilink\uFF09\uFF0C\u5E76\u9A8C\u8BC1 Wiki \u76EE\u5F55\u7ED3\u6784\u3002\u9ED8\u8BA4\u5F00\u542F\u3002",
  suggestSchemaCommand: "\u5EFA\u8BAE Schema \u66F4\u65B0",
  autoMaintainCostWarning: '\u26A0\uFE0F \u8D39\u7528\u63D0\u9192\uFF1A\u81EA\u52A8\u7EF4\u62A4\u529F\u80FD\u4F1A\u6D88\u8017 API Token\u3002"\u81EA\u52A8\u6444\u5165"\u6A21\u5F0F\u5728\u6BCF\u6B21\u6E90\u6587\u4EF6\u53D8\u66F4\u65F6\u89E6\u53D1 LLM \u8C03\u7528\u3002"\u5B9A\u65F6\u7EF4\u62A4"\u5B9A\u671F\u8FD0\u884C LLM \u5065\u5EB7\u68C0\u67E5\uFF08\u4EC5\u5728\u6709\u65B0\u53D8\u66F4\u65F6\u6267\u884C\uFF09\u3002\u8BF7\u8C28\u614E\u914D\u7F6E\u4EE5\u907F\u514D\u610F\u5916\u8D39\u7528\u3002',
  // 通知
  startupCheckSummary: "Wiki \u5171 {pages} \u9875\uFF08{entities} \u4E2A\u5B9E\u4F53\u3001{concepts} \u4E2A\u6982\u5FF5\u3001{sources} \u4E2A\u6765\u6E90\uFF09",
  watcherActiveNotice: "Wiki: \u6587\u4EF6\u76D1\u542C\u5DF2\u542F\u52A8 \u2014 \u6B63\u5728\u76D1\u63A7\u6307\u5B9A\u6587\u4EF6\u5939",
  watchIngestNotice: 'Wiki: sources/ \u4E2D\u6709 {count} \u4E2A\u6587\u4EF6\u53D8\u66F4\u3002\u8BF7\u6267\u884C"\u6444\u5165\u6E90\u6587\u4EF6"\u5904\u7406\u3002',
  autoIngestRunning: "\u6B63\u5728\u81EA\u52A8\u6444\u5165 {count} \u4E2A\u53D8\u66F4\u6587\u4EF6...",
  autoIngestComplete: "\u81EA\u52A8\u6444\u5165\u5B8C\u6210\uFF1A\u6210\u529F {success}\uFF0C\u5931\u8D25 {fail}",
  scheduledLintRunning: "\u6B63\u5728\u6267\u884C\u5B9A\u65F6 Wiki \u7EF4\u62A4...",
  wikiLintStats: "Wiki \u7EF4\u62A4: \u5171 {pages} \u9875\uFF08{entities} \u5B9E\u4F53, {concepts} \u6982\u5FF5, {sources} \u6765\u6E90\uFF09",
  wikiHealthStats: "Wiki \u5065\u5EB7: \u5171 {pages} \u9875\uFF08{entities} \u5B9E\u4F53, {concepts} \u6982\u5FF5, {sources} \u6765\u6E90\uFF09{indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Wiki \u5FEB\u901F\u4FEE\u590D\u5B8C\u6210",
  startupCheckStructureLabel: "Wiki \u7ED3\u6784",
  startupCheckStructureOk: "\u5B8C\u6574",
  startupCheckStructureMissing: "\u4E0D\u5B8C\u6574 \u2014 \u5C06\u5728\u9996\u6B21\u6444\u5165\u65F6\u81EA\u52A8\u521B\u5EFA",
  startupCheckSourcesLabel: "Sources \u5F52\u4E00\u5316",
  startupCheckSourcesClean: "\u5DF2\u89C4\u8303",
  startupCheckSourcesCleaned: "\u6E05\u7406\u4E86 {files} \u4E2A\u6587\u4EF6\uFF0C{entries} \u5904\u6761\u76EE",
  startupCheckIncompleteClean: "\u672A\u5B8C\u6210\u9875\u9762\uFF1A\u65E0",
  startupCheckIncompleteArchived: "\u672A\u5B8C\u6210\u9875\u9762\uFF1A\u5F52\u6863\u4E86 {count} \u4E2A\uFF08\u53EF\u4ECE .trash \u6062\u590D\uFF09",
  startupCheckDisableHint: "\u5982\u9700\u5173\u95ED\uFF0C\u524D\u5F80 \u8BBE\u7F6E \u2192 \u81EA\u52A8\u7EF4\u62A4 \u2192 \u542F\u52A8\u65F6\u6267\u884C\u5FEB\u901F\u4FEE\u590D",
  lintWikiStart: "\u5F00\u59CB\u7EF4\u62A4 wiki...",
  lintWikiComplete: "\u7EF4\u62A4\u5B8C\u6210",
  lintWikiFailed: "\u7EF4\u62A4\u5931\u8D25",
  analyzingSchema: "\u6B63\u5728\u5206\u6790 Wiki \u5E76\u751F\u6210 Schema \u5EFA\u8BAE...",
  schemaSuggestionGenerated: "Schema \u5EFA\u8BAE\u5DF2\u751F\u6210\uFF0C\u8BF7\u67E5\u770B wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "\u672A\u68C0\u6D4B\u5230 Schema \u9700\u8981\u66F4\u65B0\u3002",
  schemaSuggestionFailed: "Schema \u5EFA\u8BAE\u751F\u6210\u5931\u8D25",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Schema \u66F4\u65B0\u9884\u89C8",
  schemaDiffSummary: "+{add} / -{del} \u884C",
  schemaDiffRegenerateLabel: "\u6539\u8FDB\u5EFA\u8BAE\uFF08\u53EF\u9009\uFF09\uFF1A",
  schemaDiffRegenerateBtn: "\u91CD\u65B0\u751F\u6210",
  schemaDiffOpenFileBtn: "\u6253\u5F00\u6587\u4EF6",
  schemaDiffApplyBtn: "\u5E94\u7528",
  schemaDiffApplied: "Schema \u5DF2\u66F4\u65B0\u3002\u5907\u4EFD\uFF1A{path}",
  schemaDiffEmptyTitle: "LLM \u5EFA\u8BAE\u65E0\u9700\u4FEE\u6539",
  schemaDiffFailed: "Schema \u5E94\u7528\u5931\u8D25\uFF1A{reason}",
  schemaDiffRestoreHint: '\u5DF2\u5907\u4EFD\u5230 {path}\u3002\u82E5\u9700\u6062\u590D\uFF0C\u8BF7\u5728\u6587\u4EF6\u7BA1\u7406\u5668\u4E2D\u5C06\u8BE5\u6587\u4EF6\u91CD\u547D\u540D\u4E3A "wiki/schema/config.md"\u3002\u7CFB\u7EDF\u4F1A\u4FDD\u7559\u6700\u8FD1 3 \u4E2A\u5907\u4EFD\u3002',
  schemaRegenerateNoBody: "\u91CD\u65B0\u751F\u6210\u6210\u529F\uFF0C\u4F46 LLM \u672A\u8FD4\u56DE\u65B0\u7684 Schema\u3002",
  schemaNotFoundNotice: "Schema \u6587\u4EF6\u672A\u627E\u5230\u3002\u542F\u7528 Schema \u529F\u80FD\u4EE5\u521B\u5EFA\u5B83\u3002",
  selectFolderNoMdFiles: "\u6587\u4EF6\u5939 {path} \u4E2D\u6CA1\u6709 Markdown \u6587\u4EF6",
  batchIngestSkipNotice: "\u8DF3\u8FC7 {skipped}/{total} \u4E2A\u5DF2\u6444\u5165\u6587\u4EF6\u3002\u6B63\u5728\u6444\u5165 {new} \u4E2A\u65B0\u6587\u4EF6...",
  batchIngestAllIngested: "\u8BE5\u6587\u4EF6\u5939\u4E2D\u7684\u6240\u6709 {total} \u4E2A\u6587\u4EF6\u5747\u5DF2\u6444\u5165\u3002",
  batchIngestStarting: '\u6B63\u5728\u4ECE "{folder}" \u6444\u5165 {count} \u4E2A\u6587\u4EF6 \u2014 \u53EF\u80FD\u9700\u8981\u6570\u5206\u949F\u3002\u5B8C\u6210\u540E\u4F1A\u663E\u793A\u62A5\u544A\u3002',
  batchIngestComplete: "\u6279\u91CF\u6444\u5165\u5B8C\u6210: \u6210\u529F {success}/{total}, \u5931\u8D25 {fail}",
  batchIngestFailedFiles: "\u5931\u8D25\u7684\u6587\u4EF6:",
  historyTruncated: "\u5386\u53F2\u5DF2\u622A\u65AD\u81F3\u6700\u8FD1 {max} \u8F6E\u5BF9\u8BDD",
  historyCleared: "\u5386\u53F2\u5DF2\u6E05\u7A7A",
  // 用户反馈闭环
  reviewedPagePreserved: "\u5DF2\u4FDD\u7559\u7528\u6237\u5BA1\u9605\u5185\u5BB9: {}",
  // Query 回流
  querySuggestSaveTitle: "\u4FDD\u5B58\u5230 Wiki\uFF1F",
  querySuggestSaveDesc: "\u6B64\u5BF9\u8BDD\u5305\u542B\u6709\u4EF7\u503C\u7684\u77E5\u8BC6\u70B9\uFF0C\u662F\u5426\u4FDD\u5B58\u5230 Wiki\uFF1F",
  querySuggestSaveYes: "\u4FDD\u5B58",
  querySuggestSaveNo: "\u5FFD\u7565",
  // 摄入报告
  ingestReportElapsedTime: "\u8017\u65F6",
  ingestReportSkippedFiles: "\u8DF3\u8FC7\uFF08\u5DF2\u6444\u5165\uFF09",
  ingestReportRejectedFiles: "\u5DF2\u8DF3\u8FC7",
  rejectionReasonEmpty: "\u7A7A\u5185\u5BB9",
  rejectionReasonType: "\u4E0D\u652F\u6301\u7684\u7C7B\u578B",
  rejectionReasonDuplicate: "\u91CD\u590D\u5185\u5BB9",
  ingestReportFailedGuidance: "\u8FD9\u4E9B\u6761\u76EE\u672A\u80FD\u81EA\u52A8\u521B\u5EFA\u3002\u60A8\u53EF\u624B\u52A8\u521B\u5EFA\u5BF9\u5E94\u9875\u9762\uFF0C\u6216\u964D\u4F4E\u63D0\u53D6\u9897\u7C92\u5EA6\u540E\u91CD\u65B0\u6444\u5165\u6E90\u6587\u4EF6\u3002",
  ingestReportCollisions: "\u8DE8\u7C7B\u578B\u78B0\u649E\uFF08\u5DF2\u5408\u5E76\u4E3A\u522B\u540D\uFF09",
  // 命令名称（sentence case 遵循 Obsidian Bot 规则）
  cmdIngestSource: "\u6444\u5165\u5355\u4E2A\u6E90\u6587\u4EF6",
  cmdIngestFolder: "\u4ECE\u6587\u4EF6\u5939\u6444\u5165",
  cmdQueryWiki: "\u67E5\u8BE2 Wiki",
  cmdLintWiki: "\u7EF4\u62A4 Wiki",
  cmdRegenerateIndex: "\u91CD\u65B0\u751F\u6210\u7D22\u5F15",
  cmdSuggestSchema: "\u5EFA\u8BAE Schema \u66F4\u65B0",
  cmdCancelIngestion: "\u53D6\u6D88\u5F53\u524D\u63D0\u53D6",
  cmdIngestActiveFile: "\u6444\u5165\u5F53\u524D\u6587\u4EF6",
  cmdViewHistory: "\u67E5\u770B\u6444\u5165\u5386\u53F2",
  noActiveFile: "\u5F53\u524D\u6CA1\u6709\u6253\u5F00\u7684\u6587\u4EF6",
  mdOnlyFile: "\u4EC5\u652F\u6301\u6444\u5165 Markdown \u6587\u4EF6",
  // Ingestion status bar
  ingestionStatusBar: "\u63D0\u53D6\u4E2D... \u70B9\u51FB\u53D6\u6D88",
  lintStatusBar: "\u7EF4\u62A4\u4E2D... \u70B9\u51FB\u53D6\u6D88",
  ingestStatusAnalyzing: "\u63D0\u53D6\u4E2D\u2026 (\u70B9\u51FB\u53D6\u6D88)",
  lintStatusReading: "\u7EF4\u62A4\u4E2D\u2026 (\u70B9\u51FB\u53D6\u6D88)",
  lintStatusDuplicates: "\u7EF4\u62A4\u4E2D\u2026 (\u70B9\u51FB\u53D6\u6D88)",
  lintStatusScanningLinks: "\u7EF4\u62A4\u4E2D\u2026 (\u70B9\u51FB\u53D6\u6D88)",
  lintStatusAnalyzing: "\u7EF4\u62A4\u4E2D\u2026 (\u70B9\u51FB\u53D6\u6D88)",
  ingestionCancelling: "\u6B63\u5728\u53D6\u6D88 \u2014 \u5F53\u524D\u6279\u6B21\u5B8C\u6210\u540E\u5C06\u505C\u6B62",
  ingestionCancelled: "\u63D0\u53D6\u5DF2\u53D6\u6D88",
  crossTypeCollisionNotice: "{count} \u4E2A\u6761\u76EE\u5408\u5E76\u4E3A\u8DE8\u7C7B\u578B\u522B\u540D\uFF08\u5B9E\u4F53 \u2194 \u6982\u5FF5\u91CD\u590D\u5DF2\u9632\u6B62\uFF09",
  // 维护报告
  lintReportTitle: "Wiki \u7EF4\u62A4\u62A5\u544A",
  lintReportSummary: "Wiki \u72B6\u6001\u6982\u89C8\uFF1A\u5171 {total} \u4E2A\u9875\u9762\uFF0C{aliasesMissing} \u4E2A\u7F3A\u5931\u522B\u540D\uFF0C\u91CD\u590D {duplicates} \u4E2A\uFF0C\u65AD\u94FE {deadLinks} \u4E2A\uFF08\u5176\u4E2D {deadLinkFromDup} \u4E2A\u6D89\u53CA\u91CD\u590D\u9875\u9762\uFF09\uFF0C\u5B64\u7ACB {orphans} \u4E2A\uFF08\u5176\u4E2D {orphanFromDup} \u4E2A\u662F\u91CD\u590D\u9875\u9762\uFF09\uFF0C\u7A7A\u6D1E {emptyPages} \u4E2A\uFF0C\u65E0\u6765\u6E90\u5F15\u8BC1 {ungroundedQuotes} \u4E2A\uFF0C\u6807\u7B7E\u8D8A\u754C {tagViolations} \u4E2A\u3002\u672C\u6B21 Lint \u8017\u65F6 {elapsedSeconds} \u79D2",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "\u9AD8\u7EA7\u53C2\u6570\u8BBE\u7F6E",
  advancedSettingsModeDesc: "\u9ED8\u8BA4\u6A21\u5F0F\u4F7F\u7528 provider \u81EA\u8EAB\u7684\u884C\u4E3A\uFF0C\u4E0D\u53D1\u9001\u4EFB\u4F55\u601D\u8003\u63A7\u5236\u3001\u6E29\u5EA6\u6216\u91CD\u590D\u60E9\u7F5A\u5B57\u6BB5\u3002\u9002\u5408\u5927\u591A\u6570\u7528\u6237\u3002\u4EC5\u5F53\u9700\u8981\u663E\u5F0F\u8986\u76D6 provider \u9ED8\u8BA4\u503C\u65F6\uFF08\u4F8B\u5982\u5F3A\u5236\u7279\u5B9A\u601D\u8003\u63A7\u5236\u65B9\u8A00\u3001\u8BBE\u7F6E\u975E\u9ED8\u8BA4\u6E29\u5EA6\u7B49\uFF09\uFF0C\u518D\u5207\u6362\u5230\u81EA\u5B9A\u4E49\u6A21\u5F0F\u3002",
  advancedSettingsDefault: "\u9ED8\u8BA4\uFF08\u4F7F\u7528 provider \u884C\u4E3A\uFF09",
  advancedSettingsCustom: "\u81EA\u5B9A\u4E49\uFF08\u8986\u76D6 provider \u9ED8\u8BA4\uFF09",
  disableThinkingName: "\u7981\u7528\u601D\u8003",
  disableThinkingDesc: "\u663E\u5F0F\u542F\u7528\u3002\u5F00\u542F\u540E\uFF0C\u63D2\u4EF6\u4F1A\u5411 provider \u53D1\u9001\u601D\u8003\u63A7\u5236\u6307\u4EE4\uFF0C\u5E76\u5728 provider \u62D2\u7EDD\u65F6\u56DE\u9000\u5230 3 \u5C42\u7EA7\u65B9\u8A00\u94FE\uFF08anthropic \u2192 openai \u2192 none\uFF09\u3002\u4EC5\u5F53\u4F60\u7684 provider \u628A\u601D\u8003\u5185\u5BB9\u6CC4\u9732\u5230\u56DE\u7B54\u4E2D\u5E76\u9700\u8981\u6291\u5236\u65F6\u624D\u4F7F\u7528\u3002\u5927\u591A\u6570 provider \u81EA\u8EAB\u80FD\u6B63\u786E\u5904\u7406\u601D\u8003\uFF0C\u5173\u95ED\u5B83\uFF08\u9ED8\u8BA4\uFF09\u901A\u5E38\u80FD\u83B7\u5F97\u66F4\u9AD8\u8D28\u91CF\u3002",
  // Issue #137: 兼容性提示（简短；不列举 provider，避免维护负担）
  extractionTemperatureName: "\u63D0\u53D6\u6E29\u5EA6",
  extractionTemperatureDesc: "\u8303\u56F4 0\u20132\u3002\u8D8A\u4F4E\u5219 LLM \u8F93\u51FA\u8D8A\u786E\u5B9A\u3001\u8D8A\u5FE0\u4E8E\u539F\u6587\uFF1B\u8D8A\u9AD8\u5219\u8D8A\u6709\u521B\u610F\u3002\u7559\u7A7A\u8868\u793A\u4F7F\u7528\u63D0\u4F9B\u5546\u9ED8\u8BA4\u503C\u3002\u82E5\u63D0\u4F9B\u5546\u62D2\u7EDD\u6B64\u503C\uFF0C\u63D2\u4EF6\u4F1A\u81EA\u52A8\u5265\u79BB\u8BE5\u5B57\u6BB5\u5E76\u663E\u793A\u4E00\u6B21\u6027\u901A\u77E5\u3002",
  chatTemperatureName: "\u67E5\u8BE2\u6E29\u5EA6",
  chatTemperatureDesc: "\u8303\u56F4 0\u20132\u3002\u4E0E\u63D0\u53D6\u6E29\u5EA6\u76F8\u540C\uFF0C\u4F46\u53EA\u5F71\u54CD\u804A\u5929/\u67E5\u8BE2\u56DE\u590D\u3002\u7559\u7A7A\u8868\u793A\u4F7F\u7528\u63D0\u4F9B\u5546\u9ED8\u8BA4\u503C\u3002\u82E5\u63D0\u4F9B\u5546\u62D2\u7EDD\u6B64\u503C\uFF0C\u63D2\u4EF6\u4F1A\u81EA\u52A8\u5265\u79BB\u8BE5\u5B57\u6BB5\u5E76\u663E\u793A\u4E00\u6B21\u6027\u901A\u77E5\u3002",
  repetitionPenaltyName: "\u91CD\u590D\u60E9\u7F5A",
  repetitionPenaltyDesc: "\u8303\u56F4 0\u20132\u3002\u8D8A\u9AD8\u5219\u8D8A\u80FD\u51CF\u5C11\u91CD\u590D\u6A21\u5F0F\u3002\u7559\u7A7A\u8868\u793A\u4F7F\u7528\u63D0\u4F9B\u5546\u9ED8\u8BA4\u503C\u3002\u517C\u5BB9\u6027\u63D0\u793A\uFF1A\u8BB8\u591A\u4E91\u7AEF\u63D0\u4F9B\u5546\u4E0D\u63A5\u53D7\u6B64\u5B57\u6BB5\uFF0C\u63D2\u4EF6\u4F1A\u5728 400 \u65F6\u81EA\u52A8\u5265\u79BB\u5E76\u663E\u793A\u4E00\u6B21\u6027\u901A\u77E5\u3002",
  temperaturePlaceholder: "\u7559\u7A7A = \u63D0\u4F9B\u5546\u9ED8\u8BA4",
  lintDeadLinkSection: "\u65AD\u94FE\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintEmptyPageSection: "\u7A7A\u6D1E\u9875\u9762\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintOrphanSection: "\u5B64\u7ACB\u9875\u9762\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintContradictionSection: "\u77DB\u76FE\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09",
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}**\uFF08\u9875\u9762\u4E0D\u5B58\u5728\uFF09{dupFlag}",
  lintDeadLinkMore: "- ... \u5171 {count} \u5904\u65AD\u94FE",
  lintEmptyPageItem: "- [[{page}]] \u2014 \u5185\u5BB9\u4E0D\u8DB3 50 \u5B57\u7B26",
  lintOrphanItem: "- [[{page}]] \u2014 \u65E0\u5176\u4ED6 Wiki \u9875\u9762\u94FE\u63A5\u81F3\u6B64{dupFlag}",
  lintContradictionAutoFixed: "\uFF08\u672C\u6B21\u81EA\u52A8\u4FEE\u590D {count} \u4E2A\uFF09",
  lintDeadLinkAffectedByDup: " \uFF08\u26A0\uFE0F \u6D89\u53CA\u91CD\u590D\u9875\u9762\uFF09",
  lintOrphanIsDuplicate: " \uFF08\u26A0\uFE0F \u91CD\u590D\u9875\u9762\uFF09",
  lintNoIssuesFound: "\u672A\u68C0\u6D4B\u5230\u91CD\u590D\u3001\u65AD\u94FE\u3001\u7A7A\u6D1E\u3001\u5B64\u7ACB\u9875\u9762\u6216\u65E0\u6765\u6E90\u5F15\u8BC1\u3002",
  lintQuoteGroundingSection: "\u65E0\u6765\u6E90\u5F15\u8BC1\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}\uFF1A"{quote}"',
  lintContradictionOpen: "\u672A\u89E3\u51B3\u7684\u77DB\u76FE\uFF1A{count} \u4E2A",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "\u5F85\u5904\u7406",
  lintContradictionStatusPendingFix: "\u5F85\u4FEE\u590D",
  lintLLMAnalysisHeading: "## LLM \u5206\u6790",
  // 维护分析 Prompt
  lintAnalysisPrompt: '\u4F60\u662F\u4E00\u4E2A Wiki \u7EF4\u62A4\u52A9\u624B\u3002\u8BF7\u57FA\u4E8E\u4EE5\u4E0B\u4FE1\u606F\u68C0\u67E5 Wiki \u7684\u5065\u5EB7\u72B6\u51B5\u3002\n\nWiki \u7D22\u5F15\uFF1A\n{index}\n\nWiki \u9875\u9762\u5185\u5BB9\u6837\u672C\uFF08\u5171 {total} \u9875\uFF0C\u5C55\u793A {sample} \u9875\uFF09\uFF1A\n{contentSample}\n\n\u7A0B\u5E8F\u68C0\u6D4B\u7ED3\u679C\uFF08\u5DF2\u9A8C\u8BC1\uFF0C\u8BF7\u52FF\u91CD\u590D\u62A5\u544A\uFF09\uFF1A\n{progReport}\n\n\u8BF7\u68C0\u67E5\u4EE5\u4E0B\u65B9\u9762\uFF08\u8DF3\u8FC7\u7A0B\u5E8F\u5DF2\u68C0\u6D4B\u7684\u65AD\u94FE/\u7A7A\u6D1E/\u5B64\u7ACB\uFF09\uFF1A\n1. **\u77DB\u76FE** \u2014 \u4E0D\u540C\u9875\u9762\u5BF9\u540C\u4E00\u4E8B\u5B9E\u7684\u8BF4\u6CD5\u662F\u5426\u77DB\u76FE\n2. **\u8FC7\u65F6** \u2014 \u662F\u5426\u6709\u58F0\u660E\u660E\u663E\u8FC7\u65F6\n3. **\u7F3A\u5931** \u2014 \u54EA\u4E9B\u91CD\u8981\u6982\u5FF5\u7F3A\u5C11\u72EC\u7ACB\u9875\u9762\n4. **\u7ED3\u6784** \u2014 \u9875\u9762\u7ED3\u6784\u662F\u5426\u5408\u7406\uFF0C\u4EA4\u53C9\u5F15\u7528\u662F\u5426\u5145\u5206\n\n\u8F93\u51FA\u683C\u5F0F\uFF1A\u4F7F\u7528 Markdown\uFF0C\u4EE5 "## LLM \u5206\u6790" \u5F00\u5934\u3002\u6BCF\u4E2A\u53D1\u73B0\u7528\u4E00\u884C "- [\u5177\u4F53\u95EE\u9898]"\u3002\u5982\u65E0\u95EE\u9898\u5219\u5199 "\u2705 \u672A\u53D1\u73B0\u660E\u663E\u95EE\u9898\u3002"',
  // 维护修复进度
  lintFixProgress: "\u4FEE\u590D\u65AD\u94FE {current}/{total}\uFF1A[[{target}]]",
  lintFixDeadComplete: "\u65AD\u94FE\u4FEE\u590D\u5B8C\u6210\u3002\u5DF2\u4FEE\u590D {fixed}/{total} \u9879\u3002",
  lintFillProgress: "\u6269\u5145\u7A7A\u6D1E {current}/{total}\uFF1A{page}",
  lintFillComplete: "\u9875\u9762\u6269\u5145\u5B8C\u6210\u3002\u5DF2\u586B\u5145 {filled}/{total} \u9875\u3002",
  lintDeleteCompleted: "\u5DF2\u5220\u9664 {count} \u4E2A\u7A7A\u6D1E\u9875\u9762",
  lintDeleteFailed: "\u5220\u9664\u5931\u8D25 {failed}/{total} \u4E2A\u7A7A\u767D stub\uFF08\u8BE6\u60C5\u89C1\u63A7\u5236\u53F0\uFF09",
  lintFillFailed: "\u6269\u5145\u5931\u8D25\uFF1A{page} \u2014 {error}",
  lintLinkProgress: "\u94FE\u63A5\u5B64\u7ACB {current}/{total}\uFF1A{page}",
  lintLinkComplete: "\u5B64\u7ACB\u9875\u9762\u94FE\u63A5\u5B8C\u6210\u3002\u5DF2\u94FE\u63A5 {linked} \u9875\u3002",
  lintFixNoAction: "\u672A\u6267\u884C\u64CD\u4F5C\uFF08\u65E0 client\uFF09",
  lintFixIndexUpdated: "Wiki \u7D22\u5F15\u548C\u65E5\u5FD7\u5DF2\u66F4\u65B0\u3002",
  lintPollutedFixed: "\u5DF2\u4FEE\u590D\u6C61\u67D3\u9875\u9762\uFF1A{fixed}/{total}\u3002\u7D22\u5F15\u5DF2\u91CD\u5EFA\u3002",
  regenerateIndexCompleted: "\u7D22\u5F15\u5DF2\u91CD\u5EFA",
  operationFailed: "\u5931\u8D25\uFF1A",
  lintFixAllComplete: "\u6240\u6709\u4FEE\u590D\u5DF2\u5B8C\u6210\u3002\u8BE6\u60C5\u89C1\u65E5\u5FD7\u3002",
  // 维护报告弹窗
  lintModalActionsTitle: "\u4FEE\u590D\u5EFA\u8BAE\uFF08\u9700\u6D88\u8017LLM Token\uFF09\uFF1A",
  lintLogReference: "\u5B8C\u6574\u62A5\u544A\u5DF2\u4FDD\u5B58\u81F3 log.md",
  lintModalFixDeadLinks: "\u4FEE\u590D\u65AD\u94FE\uFF08{count}\uFF09",
  lintModalExpandEmpty: "\u6269\u5145\u7A7A\u6D1E\u9875\u9762\uFF08{count}\uFF09",
  lintModalDeleteEmpty: "\u5220\u9664\u7A7A\u6D1E\u9875\u9762\uFF08{count}\uFF09",
  lintModalLinkOrphans: "\u94FE\u63A5\u5B64\u7ACB\u9875\u9762\uFF08{count}\uFF09",
  lintModalAnalyzeSchema: "\u5206\u6790 Schema",
  lintModalMergeDuplicates: "\u5408\u5E76\u91CD\u590D\u9875\u9762\uFF08{count}\uFF09",
  lintModalFixAll: "\u4E00\u952E\u4FEE\u590D\u6240\u6709\u95EE\u9898\uFF08{count}\uFF09",
  lintDuplicateSection: "\u91CD\u590D\u9875\u9762\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09",
  lintPollutedSection: "\u6C61\u67D3\u9875\u9762\uFF08\u7A0B\u5E8F\u68C0\u6D4B\uFF09[\u5171 {count} \u4E2A]",
  lintPollutedItem: '- [[{page}]] \u2192 \u5E94\u4E3A "{clean}"',
  lintSourcesNormalizedSection: "Sources \u5DF2\u5F52\u4E00\u5316\uFF08\u81EA\u52A8\u4FEE\u590D\uFF09[\u5171 {files} \u4E2A\u6587\u4EF6 / {entries} \u5904]",
  lintSourcesNormalizedItem: "\u6E05\u7406\u4E86 {files} \u4E2A\u6587\u4EF6\u4E2D\u7684 {entries} \u5904 sources \u6761\u76EE\uFF08\u5DF2\u79FB\u9664\u5916\u90E8\u8DEF\u5F84\u3001.md \u540E\u7F00\u3001\u522B\u540D\u7BA1\u9053\uFF0C\u5E76\u53BB\u91CD\uFF09\u3002",
  lintDuplicateItem: "- [[{target}]] \u4E0E [[{source}]] \u2014 {reason}",
  lintMergeProgress: "\u5408\u5E76 {current}/{total}\uFF1A{source} \u2192 {target}",
  lintMergeComplete: "\u91CD\u590D\u9875\u9762\u5408\u5E76\u5B8C\u6210\u3002\u5DF2\u5408\u5E76 {merged}/{total} \u5BF9\u3002",
  // 摄入报告弹窗
  ingestReportTitle: "\u6444\u5165\u62A5\u544A",
  ingestReportSourceFile: "\u6E90\u6587\u4EF6",
  ingestReportCreated: "\u5DF2\u521B\u5EFA",
  ingestReportUpdated: "\u5DF2\u66F4\u65B0",
  ingestReportContradictions: "\u53D1\u73B0\u77DB\u76FE",
  ingestReportFailedTitle: "\u672A\u80FD\u6444\u5165",
  ingestReportErrorDetail: "\u9519\u8BEF\u8BE6\u60C5",
  ingestReportClose: "\u5173\u95ED",
  ingestReportCreatedPages: "\u521B\u5EFA\u9875\u9762\uFF1A{count}",
  ingestReportUpdatedPages: "\u66F4\u65B0\u9875\u9762\uFF1A{count}",
  ingestReportEntitiesCount: "{count} \u4E2A\u5B9E\u4F53",
  ingestReportConceptsCount: "{count} \u4E2A\u6982\u5FF5",
  ingestReportContradictionsFound: "\u53D1\u73B0\u77DB\u76FE\uFF1A{count}",
  ingestReportEntityType: "\u5B9E\u4F53",
  ingestReportConceptType: "\u6982\u5FF5",
  timeMinutes: "\u5206",
  timeSeconds: "\u79D2",
  // 速率限制警告
  rateLimitDetected: "\u26A0\uFE0F \u68C0\u6D4B\u5230\u901F\u7387\u9650\u5236\uFF1A{count} \u4E2A\u9875\u9762\u56E0 429 \u9519\u8BEF\u5931\u8D25\u3002\u5EFA\u8BAE\uFF1A(1) \u964D\u4F4E\u5E76\u53D1\u5EA6\u81F3 {suggestedConcurrency} \u6216 1\uFF08\u4E32\u884C\uFF09\uFF0C(2) \u589E\u5927\u6279\u6B21\u5EF6\u8FDF\u81F3 {suggestedDelay}ms\uFF0C(3) \u5207\u6362\u5230\u901F\u7387\u9650\u5236\u66F4\u9AD8\u7684\u63D0\u4F9B\u5546\u3002",
  rateLimitDetectedShort: "\u26A0\uFE0F \u89E6\u53D1\u901F\u7387\u9650\u5236 \u2014 \u5EFA\u8BAE\u5728\u8BBE\u7F6E \u2192 \u6444\u5165\u52A0\u901F\u4E2D\u964D\u4F4E\u5E76\u53D1\u5EA6\u6216\u589E\u5927\u6279\u6B21\u5EF6\u8FDF\u3002",
  // 长文本摄入提示
  sourceRejectedEmpty: '\u23ED\uFE0F "{filename}" \u6CA1\u6709\u53EF\u63D0\u53D6\u7684\u5185\u5BB9\uFF0C\u5DF2\u8DF3\u8FC7\u3002\u7A7A\u7B14\u8BB0\u6216\u4EC5\u542B frontmatter \u7684\u7B14\u8BB0\u4E0D\u4F1A\u751F\u6210 Wiki \u9875\u9762\u3002',
  sourceRejectedType: '\u23ED\uFE0F "{filename}" \u4E0D\u662F\u53D7\u652F\u6301\u7684\u6587\u4EF6\u7C7B\u578B\uFF0C\u5DF2\u8DF3\u8FC7\u3002\u4EC5\u53EF\u63D0\u53D6\u6587\u672C\u7B14\u8BB0\uFF08\u5982 .md\u3001.txt\uFF09\u3002',
  sourceRejectedDuplicate: '\u23ED\uFE0F "{filename}" \u5DF2\u8DF3\u8FC7\u2014\u2014\u5176\u5185\u5BB9\u5DF2\u5B58\u5728\u4E8E Wiki \u4E2D\u3002',
  ingestRejectedSummary: "\u5DF2\u8DF3\u8FC7 {count} \u4E2A\u6587\u4EF6\uFF08\u7A7A\u6587\u4EF6\u3001\u91CD\u590D\u6216\u4E0D\u652F\u6301\u7684\u7C7B\u578B\uFF09\u3002",
  reingestConfirmTitle: "\u91CD\u65B0\u63D0\u53D6\u8BE5\u6587\u4EF6\uFF1F",
  reingestConfirmBody: '"{filename}" \u7684\u5185\u5BB9\u5DF2\u5B58\u5728\u4E8E Wiki \u4E2D\u3002\u4ECD\u8981\u91CD\u65B0\u63D0\u53D6\u5417\uFF1F',
  reingestConfirmYes: "\u91CD\u65B0\u63D0\u53D6",
  reingestConfirmNo: "\u8DF3\u8FC7",
  longSourceNotice: '\u{1F4C4} "{filename}" \u5305\u542B {lines} \u884C\uFF08{size}\uFF09\u3002\u957F\u6587\u672C\u9700\u8981\u591A\u8F6E\u8FED\u4EE3\u63D0\u53D6\uFF0CLLM \u5C06\u5B8C\u6574\u8BFB\u53D6\u6587\u6863\u8FDB\u884C\u591A\u6B21\u5206\u6790\uFF0C\u8017\u65F6\u53EF\u80FD\u8F83\u957F\uFF0C\u8BF7\u8010\u5FC3\u7B49\u5F85\u3002',
  longSourceNoticeShort: "\u{1F4C4} \u68C0\u6D4B\u5230\u5927\u6587\u4EF6\uFF08{lines} \u884C\uFF09\uFF0C\u6444\u5165\u53EF\u80FD\u9700\u8981\u8F83\u957F\u65F6\u95F4\u3002",
  // 摄入历史面板 (#122) — UI 文案
  historyButton: "\u64CD\u4F5C\u5386\u53F2",
  historyButtonDesc: "\u67E5\u770B LLM-Wiki \u6700\u8FD1\u7684\u6444\u5165\u3001Lint \u62A5\u544A\u548C\u5176\u4ED6\u53D8\u66F4\u8BB0\u5F55",
  historyButtonOpen: "\u67E5\u770B\u5386\u53F2",
  historyModalTitle: "\u64CD\u4F5C\u5386\u53F2",
  historyModalSubtitle: "LLM-Wiki \u6700\u8FD1\u7684\u53D8\u66F4\u8BB0\u5F55",
  historyEmpty: "\u6682\u65E0\u64CD\u4F5C\u8BB0\u5F55\u3002\u6444\u5165\u7B14\u8BB0\u6216\u8FD0\u884C Lint \u4EE5\u586B\u5145\u6B64\u5217\u8868\u3002",
  historyReadError: "\u65E0\u6CD5\u8BFB\u53D6\u64CD\u4F5C\u65E5\u5FD7\uFF1A{error}",
  historyEntryKindIngest: "\u6444\u5165",
  historyEntryKindMaintenance: "\u7EF4\u62A4",
  historyEntryKindFix: "\u4FEE\u590D",
  historyEntryKindOther: "\u5176\u4ED6",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "\u6765\u6E90\uFF1A{source}",
  historyEntrySourceUnknown: "\u672A\u547D\u540D\u6444\u5165",
  historyEntryCreatedLabel: "\u5DF2\u521B\u5EFA",
  historyEntryUpdatedLabel: "\u5DF2\u66F4\u65B0",
  historyEntryCreatedCount: "\u65B0\u5EFA {count} \u4E2A",
  historyEntryUpdatedCount: "\u66F4\u65B0 {count} \u4E2A",
  historyEntryNoChanges: "\u65E0\u9875\u9762\u53D8\u66F4",
  historyEntryContradictions: "\u26A0\uFE0F {count} \u5904\u77DB\u76FE",
  historyEntrySectionCreated: "\u521B\u5EFA\u7684\u9875\u9762",
  historyEntrySectionUpdated: "\u66F4\u65B0\u7684\u9875\u9762",
  historyEntrySectionContradictions: "\u77DB\u76FE",
  historyEntrySectionDetails: "\u8BE6\u60C5",
  historyEntryOpenPage: "\u6253\u5F00",
  historyEntryDetailsNoContradictions: "\u672A\u53D1\u73B0\u77DB\u76FE",
  historySearchPlaceholder: "\u{1F50D} \u6309\u6765\u6E90\u6807\u9898\u6216\u9875\u9762\u8DEF\u5F84\u641C\u7D22\u2026",
  historyFilterAll: "\u5168\u90E8",
  historyFilterIngest: "\u4EC5\u6444\u5165",
  historyFilterMaintenance: "\u4EC5\u7EF4\u62A4\u62A5\u544A",
  historyFilterFix: "\u4EC5\u4FEE\u590D",
  historyFilterContradictions: "\u26A0\uFE0F \u542B\u77DB\u76FE",
  historyRefreshButton: "\u27F3 \u5237\u65B0",
  historyExpandDay: "\u70B9\u51FB\u5C55\u5F00",
  historyCollapseDay: "\u70B9\u51FB\u6536\u8D77",
  historyShowMore: "\u663E\u793A\u66F4\u65E9\u7684 {count} \u6761",
  historyNoMatch: "\u6CA1\u6709\u64CD\u4F5C\u5339\u914D\u60A8\u7684\u641C\u7D22/\u7B5B\u9009\u6761\u4EF6\u3002",
  historyCloseButton: "\u5173\u95ED",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "\u62A5\u544A\u53D1\u73B0",
  historyKpiPages: "\u9875\u9762\u6570",
  historyKpiDeadLinks: "\u65AD\u94FE",
  historyKpiOrphans: "\u5B64\u7ACB",
  historyKpiEmpty: "\u7A7A\u6D1E",
  historyKpiDuplicates: "\u91CD\u590D",
  historyKpiTagViolations: "\u6807\u7B7E\u95EE\u9898",
  historyKpiUnsourced: "\u65E0\u6765\u6E90",
  historyKpiDuration: "Lint \u8017\u65F6",
  historyKpiDurationSec: "{seconds} \u79D2",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "\u65AD\u94FE ({count})",
  historySectionTagViolations: "\u6807\u7B7E\u8D8A\u754C ({count})",
  historySectionOrphans: "\u5B64\u7ACB\u9875\u9762 ({count})",
  historySectionEmptyPages: "\u7A7A\u6D1E\u9875\u9762 ({count})",
  historySectionLlmAnalysis: "LLM \u5206\u6790 ({count})",
  historyDeadLinkSource: "\u6765\u6E90",
  historyDeadLinkTarget: "\u7F3A\u5931",
  historyOpenInLog: "\u5728 log.md \u4E2D\u67E5\u770B",
  historyShowMoreItems: "\u5C55\u5F00\u5269\u4F59 {count} \u9879",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 \u6301\u5E73",
  historyChipContradiction: "\u77DB\u76FE",
  historyChipOutdated: "\u8FC7\u65F6",
  historyChipMissing: "\u7F3A\u5931",
  historyChipStructure: "\u7ED3\u6784",
  historySeverityHigh: "\u9AD8",
  historySeverityMedium: "\u4E2D",
  historySeverityLow: "\u4F4E",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "\u60A8\u7684 Wiki \u5F53\u524D\u6709 {dead} \u4E2A\u65AD\u94FE\u3001{orphans} \u4E2A\u5B64\u7ACB\u9875\u9762\u3001{tags} \u4E2A\u6807\u7B7E\u95EE\u9898\u3002\u6700\u8FD1\u4E00\u6B21 Lint \u8017\u65F6 {duration}\u3002",
  historyGlobalInsightClean: "\u60A8\u7684 Wiki \u5F53\u524D\u72B6\u6001\u826F\u597D\u2014\u2014\u6CA1\u6709\u65AD\u94FE\u3001\u5B64\u7ACB\u9875\u9762\u6216\u6807\u7B7E\u95EE\u9898\u3002",
  historyGlobalInsightNoData: "\u6682\u65E0\u7EF4\u62A4\u62A5\u544A\u2014\u2014\u8FD0\u884C Lint \u4EE5\u751F\u6210\u3002",
  historyModalHeaderTitle: "\u64CD\u4F5C\u5386\u53F2",
  historyModalSubtitleWithCount: "LLM-Wiki \u6700\u8FD1\u7684\u53D8\u66F4\u8BB0\u5F55 \xB7 \u5171 {count} \u6761",
  historyTimeRangeAll: "\u5168\u90E8\u65F6\u95F4",
  historyTimeRange1d: "\u8FD1 1 \u5929",
  historyTimeRange3d: "\u8FD1 3 \u5929",
  historyTimeRange1w: "\u8FD1 1 \u5468",
  historyTimeRange1m: "\u8FD1 1 \u4E2A\u6708",
  historyIngestTotal: "\u9875\u9762\u603B\u6570",
  historyIngestByType: "\u6309\u7C7B\u578B\u5206\u5E03",
  historyIngestSource: "\u6765\u6E90\u6587\u4EF6",
  historyIngestNoTimestamp: "\u65E0\u65F6\u95F4\u6233",
  historyIngestFirstTime: "\u9996\u6B21\u6444\u5165",
  historyIngestLatestTime: "\u6700\u8FD1\u6444\u5165",
  historyTimeRangeCustom: "\u81EA\u5B9A\u4E49\u8303\u56F4",
  historyCustomRangeFrom: "\u8D77\u59CB",
  historyCustomRangeTo: "\u7ED3\u675F",
  historyCustomRangeApply: "\u5E94\u7528",
  historyCustomRangeClear: "\u6E05\u9664"
};

// src/texts/zh-hant.ts
var ZH_HANT_TEXTS = {
  // 插件信息
  pluginTitle: "Karpathy LLM Wiki \u8A2D\u5B9A",
  pluginIntro: "\u672C\u5916\u639B\u70BA Obsidian \u5BE6\u73FE\u4E86 {{link}} \u6982\u5FF5\u3002\u5B83\u8B80\u53D6\u4F60\u7684\u7B46\u8A18\uFF0C\u7528 AI \u63D0\u53D6\u5BE6\u9AD4\u548C\u6982\u5FF5\uFF0C\u5728 Vault \u4E2D\u69CB\u5EFA\u7D50\u69CB\u5316\u3001\u4E92\u806F\u7684\u77E5\u8B58\u5EAB\u3002",
  karpathyLinkText: "Andrej Karpathy \u7684 LLM Wiki",
  // 功能部分
  featuresTitle: "\u4F7F\u7528\u65B9\u5F0F",
  workflow1Title: "1. \u651D\u5165 (Ingest)",
  workflow1Desc: "\u9078\u64C7\u7B46\u8A18 \u2192 AI \u63D0\u53D6\u5BE6\u9AD4\u3001\u6982\u5FF5\u3001\u95DC\u4FC2 \u2192 \u751F\u6210 Wiki \u9801\u9762\u3002",
  workflow2Title: "2. \u67E5\u8A62 (Query)",
  workflow2Desc: "\u8207 Wiki \u5C0D\u8A71\uFF0C\u7B54\u6848\u57FA\u65BC\u4F60\u5DF2\u651D\u5165\u7684\u77E5\u8B58\uFF0C\u800C\u975E\u901A\u7528\u77E5\u8B58\u3002",
  workflow3Title: "3. \u7DAD\u8B77 (Maintain)",
  workflow3Desc: "\u81EA\u52D5\u751F\u6210\u7D22\u5F15\u3001\u4EA4\u53C9\u9023\u7D50\u3001\u5065\u5EB7\u6AA2\u67E5\uFF0C\u4FDD\u6301 Wiki \u6574\u6F54\u3002",
  // 语言切换
  languageTitle: "\u4ECB\u9762\u8A9E\u8A00",
  languageDesc: "\u9078\u64C7\u8A2D\u5B9A\u9762\u677F\u7684\u986F\u793A\u8A9E\u8A00\u3002\u91CD\u555F\u5916\u639B\u4EE5\u5C07\u8A9E\u8A00\u66F4\u6539\u61C9\u7528\u5230\u547D\u4EE4\u9762\u677F\u3002",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // 状态
  statusTitle: "LLM Client \u72C0\u614B",
  llmWikiStatusSection: "LLM-Wiki \u72C0\u614B",
  statusInitialized: "\u5DF2\u521D\u59CB\u5316",
  statusReady: "LLM \u5DF2\u5C31\u7DD2",
  statusNotReady: "LLM \u672A\u914D\u7F6E \u2014 \u8ACB\u5B8C\u6210\u4E0A\u65B9\u8A2D\u5B9A\u4E26\u900F\u904E\u9023\u7DDA\u6E2C\u8A66",
  statusNotInitialized: "\u672A\u521D\u59CB\u5316",
  currentProvider: "\u7576\u524D\u63D0\u4F9B\u5546",
  // Provider 设置
  providerSection: "LLM \u914D\u7F6E",
  providerName: "LLM Provider",
  providerDesc: "\u9078\u64C7\u9810\u5B9A\u7FA9\u63D0\u4F9B\u5546\u6216\u81EA\u5B9A\u7FA9 OpenAI \u76F8\u5BB9\u670D\u52D9\u3002\u5C0D\u65BC Coding Plan \u7B49\u5957\u9910\uFF0C\u5EFA\u8B70\u9078\u64C7\u81EA\u5B9A\u7FA9 OpenAI/Anthropic \u4E26\u624B\u5DE5\u586B\u5BEB\u5C0D\u61C9 URL \u548C API Key",
  // API Key
  apiKeyName: "API Key",
  apiKeyDesc: "\u8F38\u5165\u63D0\u4F9B\u5546\u7684 API Key",
  apiKeyPlaceholder: "\u8F38\u5165 API Key",
  // Base URL
  baseUrlName: "API Base URL",
  baseUrlDescCustom: "\u5FC5\u586B\uFF1A\u81EA\u5B9A\u7FA9 OpenAI \u76F8\u5BB9\u670D\u52D9\u7684 endpoint",
  baseUrlDescOverride: "\u53EF\u9078\uFF1A\u8986\u84CB\u9810\u8A2D\u7684 Base URL",
  // Ollama 提示
  ollamaHint: "Ollama \u672C\u5730\u57F7\u884C\uFF0C\u7121\u9700 API Key",
  lmstudioHint: "LM Studio \u672C\u5730\u57F7\u884C\uFF0CAPI Key \u53EF\u9078",
  maxTokensPerCallName: "\u4E0A\u4E0B\u6587\u8996\u7A97",
  maxTokensPerCallDesc: "\u9650\u5236\u751F\u6210 Token \u4EE5\u9069\u914D\u6A21\u578B\u4E0A\u4E0B\u6587\u8996\u7A97\u30020 = \u7121\u9650\u5236\uFF08\u96F2\u7AEF\u9810\u8A2D\uFF09\u3002",
  slugCaseName: "\u6A94\u540D\u5927\u5C0F\u5BEB",
  slugCaseDesc: "\u63A7\u5236\u751F\u6210\u7684 Wiki \u6A94\u540D\u662F\u5426\u8F49\u70BA\u5C0F\u5BEB\u3002\u5C0D\u65BC\u5927\u5C0F\u5BEB\u6709\u8A9E\u7FA9\u7684\u8A9E\u8A00\uFF08\u5982\u5FB7\u8A9E\u540D\u8A5E\uFF09\uFF0C\u5EFA\u8B70\u9078\u64C7\u201C\u4FDD\u7559\u201D\u3002",
  slugCaseLower: "\u5C0F\u5BEB\uFF08\u9810\u8A2D\uFF09",
  slugCasePreserve: "\u4FDD\u7559\u5927\u5C0F\u5BEB",
  // 模型选择
  modelSection: "\u6A21\u578B\u9078\u64C7",
  fetchModelsName: "\u7372\u53D6\u53EF\u7528\u6A21\u578B",
  fetchModelsDesc: "\u5F9E Provider API \u7372\u53D6\u6700\u65B0\u7684\u6A21\u578B\u5217\u8868",
  fetchModelsButton: "\u7372\u53D6\u6A21\u578B\u5217\u8868",
  fetchingModels: "\u7372\u53D6\u4E2D...",
  fetchSuccess: "\u7372\u53D6\u6210\u529F\uFF01\u5171 {} \u500B\u53EF\u7528\u6A21\u578B",
  fetchFailed: "\u7372\u53D6\u5931\u6557\u6216\u5217\u8868\u70BA\u7A7A\uFF0C\u8ACB\u624B\u52D5\u8F38\u5165\u6A21\u578B\u540D\u7A31",
  fetchNotSupported: "\u8A72 Provider \u4E0D\u652F\u63F4\u6A21\u578B\u5217\u8868\u67E5\u8A62",
  fetchErrorAuth: "\u8A8D\u8B49\u5931\u6557\uFF08HTTP 401/403\uFF09\u3002\u8ACB\u6AA2\u67E5 API Key \u662F\u5426\u6B63\u78BA\uFF0C\u6216\u76F4\u63A5\u8F38\u5165\u6A21\u578B ID \u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA\u9032\u884C\u9A57\u8B49\u3002",
  fetchErrorEndpoint: "\u63A5\u5165\u9EDE\u672A\u627E\u5230\uFF08HTTP 404\uFF09\u3002\u8ACB\u6AA2\u67E5 BaseURL \u662F\u5426\u6B63\u78BA\uFF0C\u6216\u76F4\u63A5\u8F38\u5165\u6A21\u578B ID \u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA\u3002",
  fetchErrorServer: "Provider \u670D\u52D9\u7AEF\u932F\u8AA4\uFF08HTTP 5xx\uFF09\u3002\u7A0D\u5F8C\u91CD\u8A66\uFF0C\u6216\u76F4\u63A5\u8F38\u5165\u6A21\u578B ID \u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA\u3002",
  fetchErrorEmpty: "\u8A72\u63A5\u5165\u9EDE\u4E0D\u652F\u63F4\u5217\u51FA\u6A21\u578B\u5217\u8868\u3002\u8ACB\u76F4\u63A5\u8F38\u5165\u6A21\u578B ID \u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA\u9032\u884C\u9A57\u8B49\u3002",
  fetchErrorNetwork: "\u7DB2\u8DEF\u8ACB\u6C42\u5931\u6557\u3002\u8ACB\u6AA2\u67E5\u7DB2\u8DEF\u9023\u7DDA\u3001BaseURL \u6216\u4EE3\u7406\u8A2D\u5B9A\u3002\u60A8\u4E5F\u53EF\u4EE5\u76F4\u63A5\u8F38\u5165\u6A21\u578B ID \u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA\u9032\u884C\u9A57\u8B49\u3002",
  selectModelName: "\u9078\u64C7\u6A21\u578B",
  selectModelDesc: "\u5F9E {} \u500B\u53EF\u7528\u6A21\u578B\u4E2D\u9078\u64C7",
  customInputOption: "\u81EA\u5B9A\u7FA9\u8F38\u5165...",
  customInputHint: '\u5982\u9700\u4F7F\u7528\u5176\u4ED6\u6A21\u578B\uFF0C\u8ACB\u9078\u64C7"\u81EA\u5B9A\u7FA9\u8F38\u5165..."',
  modelName: "\u6A21\u578B\u540D\u7A31",
  modelDescCustom: "\u7576\u524D\u4F7F\u7528\u81EA\u5B9A\u7FA9\u6A21\u578B\uFF08\u53EF\u91CD\u65B0\u7372\u53D6\u5217\u8868\uFF09",
  modelDescFetchFailed: "\u6A21\u578B\u5217\u8868\u7372\u53D6\u5931\u6557\u3002\u8ACB\u6AA2\u67E5 API Key \u548C\u63A5\u5165\u9EDE URL\uFF0C\u6216\u76F4\u63A5\u8F38\u5165\u6A21\u578B ID \u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA\u9032\u884C\u9A57\u8B49\u3002",
  modelInputPlaceholder: "\u8F38\u5165\u6A21\u578B ID\uFF0C\u7136\u5F8C\u9EDE\u9078\u6E2C\u8A66\u9023\u7DDA",
  switchToDropdown: "\u5207\u63DB\u5230\u4E0B\u62C9\u9078\u64C7",
  useDropdownButton: "\u4F7F\u7528\u4E0B\u62C9\u9078\u64C7",
  // 测试 & 保存
  testConnectionName: "\u6E2C\u8A66\u9023\u7DDA",
  testConnectionDesc: "\u9A57\u8B49\u914D\u7F6E\u80FD\u5426\u6210\u529F\u547C\u53EB LLM API",
  testButton: "\u6E2C\u8A66\u9023\u7DDA",
  testing: "\u6E2C\u8A66\u4E2D...",
  saveSettingsName: "\u5132\u5B58\u8A2D\u5B9A",
  saveSettingsDesc: "\u5132\u5B58\u7576\u524D\u914D\u7F6E",
  saveButton: "\u5132\u5B58\u8A2D\u5B9A",
  testConnectionSuccessful: "\u9023\u7DDA\u6210\u529F",
  testConnectionFailed: "\u9023\u7DDA\u5931\u6557",
  testConnectionProvider: "\u63D0\u4F9B\u5546\uFF1A",
  errorUnknown: "\u672A\u77E5\u932F\u8AA4",
  savedNotice: "\u8A2D\u5B9A\u5DF2\u5132\u5B58\uFF01",
  // Issue #137: LLM 回退通知（请求中触发 thinking-dialect 回退或字段剥离时显示）
  fallbackThinkingDialect: '\u601D\u8003\u63A7\u5236\uFF1A\u5DF2\u5207\u63DB\u81F3 "{dialect}" \u65B9\u8A00\uFF08\u8A72\u63D0\u4F9B\u5546\u4F7F\u7528\u4E0D\u540C\u7684\u601D\u8003\u63A7\u5236\u6B04\u4F4D\u683C\u5F0F\uFF09\u3002\u8F38\u51FA\u5167\u5BB9\u4E0D\u53D7\u5F71\u97FF\u3002',
  fallbackThinkingNone: "\u8A72\u63D0\u4F9B\u5546\u5DF2\u5B8C\u5168\u95DC\u9589\u601D\u8003\u63A7\u5236\u3002\u6A21\u578B\u4ECD\u53EF\u80FD\u7522\u751F\u63A8\u7406\u5167\u5BB9\uFF1B\u82E5\u51FA\u73FE\uFF0C\u8ACB\u5617\u8A66\u5176\u4ED6\u6A21\u578B\u3002",
  fallbackParamStripped: '\u5F15\u6578 "{field}" \u4E0D\u88AB\u8A72\u63D0\u4F9B\u5546\u652F\u63F4\u3002\u5DF2\u5F9E\u8ACB\u6C42\u4E2D\u79FB\u9664\uFF1B\u884C\u70BA\u53EF\u80FD\u8207\u914D\u7F6E\u4E0D\u540C\u3002',
  // Wiki 文件夹
  wikiSection: "Wiki \u914D\u7F6E",
  wikiFolderName: "Wiki \u8CC7\u6599\u593E",
  wikiFolderDesc: "\u5B58\u653E\u751F\u6210\u7684 Wiki \u9801\u9762",
  wikiFolderPlaceholder: "wiki",
  // 错误
  errorNoApiKey: "\u8ACB\u5148\u914D\u7F6E API Key",
  llmNotReady: "LLM \u5C1A\u672A\u914D\u7F6E\u3002\u8ACB\u524D\u5F80 \u8A2D\u5B9A \u2192 Karpathy LLM Wiki \u5B8C\u6210 Provider \u914D\u7F6E\u3001\u7372\u53D6\u6A21\u578B\u5217\u8868\u4E26\u900F\u904E\u9023\u7DDA\u6E2C\u8A66\u3002",
  errorFetchFailed: "\u7372\u53D6\u5931\u6557\uFF1A{}",
  // Query 设置
  querySectionTitle: "Wiki \u67E5\u8A62\u914D\u7F6E",
  maxConversationHistoryName: "\u5C0D\u8A71\u6B77\u53F2\u4E0A\u9650",
  maxConversationHistoryDesc: "\u9650\u5236\u5C0D\u8A71\u8A0A\u606F\u6578\uFF0C\u907F\u514D\u8D85\u51FALLM token\u9650\u5236",
  maxConversationHistoryHint: "\u63A8\u85A6\uFF1A\u4E0D\u8D85\u904E50\u8F2A",
  numberRangeValidation: "\u8ACB\u8F38\u51651-50\u4E4B\u9593\u7684\u6578\u5B57",
  numberRangeClamped: "\u6578\u503C\u8D85\u51FA\u7BC4\u570D\uFF081-500\uFF09\uFF0C\u5DF2\u81EA\u52D5\u8A2D\u5B9A\u70BA {}",
  // Query Modal UI
  queryModalTitle: "Query Wiki - \u5C0D\u8A71\u5F0F\u67E5\u8A62",
  queryModalPlaceholder: "\u8F38\u5165\u554F\u984C...",
  queryModalSendButton: "\u50B3\u9001",
  queryModalStopButton: "\u505C\u6B62",
  queryModalSaveButton: "\u5132\u5B58\u5230Wiki",
  queryModalClearButton: "\u6E05\u7A7A\u6B77\u53F2",
  queryModalHistoryCount: "\u5C0D\u8A71\u6B77\u53F2: {}/{} \u8F2A",
  queryModalStreaming: "\u6D41\u5F0F\u751F\u6210\u4E2D...",
  queryModalFallbackStreaming: "\u7AEF\u9EDE\u4E0D\u652F\u63F4\u6D41\u5F0F\u50B3\u8F38\uFF0C\u5DF2\u56DE\u9000\u81F3\u975E\u6D41\u5F0F\u6A21\u5F0F\uFF0C\u8ACB\u8010\u5FC3\u7B49\u5F85...",
  queryPhaseSearching: "\u6B63\u5728\u5206\u6790 Wiki \u7D22\u5F15\uFF0C\u67E5\u8A62\u76F8\u95DC\u8CC7\u6599...",
  queryPhaseFoundPages: "\u627E\u5230 {count} \u500B\u8CC7\u6599\uFF1A{pages}",
  queryPhaseLoadingPages: "\u6B63\u5728\u8F09\u5165\u9801\u9762\u5167\u5BB9...",
  queryPhaseContextReady: "\u8CC7\u6599\u5C31\u7DD2\uFF0C\u6B63\u5728\u751F\u6210\u56DE\u8986...",
  queryPhaseGenerating: "\u6B63\u5728\u751F\u6210\u56DE\u8986...\uFF08\u5DF2\u7B49\u5F85 {time}s\uFF09",
  queryPhaseNonStreaming: "\u975E\u6D41\u5F0F\u6A21\u5F0F\uFF0C\u751F\u6210\u4E2D\uFF0C\u8ACB\u8010\u5FC3\u7B49\u5F85...\uFF08\u5DF2\u7B49\u5F85 {time}s\uFF09",
  queryModalErrorPrefix: "\u932F\u8AA4\uFF1A",
  queryModalHint: '\u67E5\u8A62\u57FA\u65BCWiki\u5167\u5BB9\u3002\u9EDE\u9078"\u5132\u5B58\u5230Wiki"\u53EF\u5C07\u6709\u50F9\u503C\u5C0D\u8A71\u63D0\u7149\u70BAWiki\u9801\u9762\u3002',
  // v1.20.0: Query Wiki 思考内容折叠摘要
  queryThinkingSummary: "\u601D\u8003\u904E\u7A0B",
  queryThinkingSteps: "\u6B65",
  // 错误消息
  errorLLMClientNotInit: "LLM Client \u672A\u521D\u59CB\u5316\u3002\u8ACB\u5132\u5B58\u8A2D\u5B9A\u3002",
  errorIngestFailed: "\u651D\u5165\u5931\u6557\uFF1A",
  errorQueryFailed: "\u67E5\u8A62\u5931\u6557\uFF1A",
  fileWriteFailed: "\u7121\u6CD5\u5EFA\u7ACB\u6216\u66F4\u65B0\u6A94\u6848\uFF1A{path}",
  // 成功消息
  ingestSuccess: "\u651D\u5165\u6210\u529F\uFF1A\u5EFA\u7ACB{}\u9801\uFF0C\u66F4\u65B0{}\u9801",
  querySuccess: "\u67E5\u8A62\u5B8C\u6210",
  lintSuccess: "Wiki \u7DAD\u8B77\u5B8C\u6210",
  lintReadingPages: "\u6B63\u5728\u8B80\u53D6 {count} \u500B Wiki \u9801\u9762...",
  lintReadingPagesProgress: "\u6B63\u5728\u8B80\u53D6 Wiki \u9801\u9762\uFF1A{current}/{total}...",
  lintScanningLinks: "\u6B63\u5728\u6383\u63CF\u65B7\u93C8...",
  lintScanningLinksProgress: "\u6B63\u5728\u6383\u63CF\u65B7\u93C8\uFF1A{current}/{total}...",
  lintCheckingDuplicates: "\u6B63\u5728\u6AA2\u67E5\u91CD\u8907\u9801\u9762...",
  lintCheckingDuplicatesProgress: "\u6B63\u5728\u6821\u9A57\u91CD\u8907\u9805\uFF1A\u6279\u6B21 {current} ...",
  lintFixingPolluted: "\u6B63\u5728\u4FEE\u5FA9\u6C59\u67D3\u9801\u9762 {current}/{total}\uFF1A{title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} \u4FEE\u5FA9\u6C59\u67D3\u9801\u9762\uFF08{count}\uFF09",
  lintDuplicateCheckFailed: "\u91CD\u8907\u6AA2\u6E2C\u5931\u6557 \u2014 \u8A73\u898B\u63A7\u5236\u6AAF",
  lintDuplicateCheckFailedDetail: "\u91CD\u8907\u6AA2\u6E2C\u5728{step}\u5931\u6557\uFF1A{error}",
  lintMergeItemFailed: "\u5408\u4F75\u5931\u6557\uFF1A{source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "\u6AA2\u6E2C\u5230 {count} \u500B\u9801\u9762\u7F3A\u5C11\u5225\u540D",
  lintAliasesSection: "\u7F3A\u5931\u5225\u540D\u7684\u9801\u9762\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "\u88DC\u5168\u5225\u540D\uFF08{count}\uFF09",
  lintAliasesFilling: "\u751F\u6210\u5225\u540D {current}/{total}\uFF1A{page}",
  lintAliasesFilled: "\u5225\u540D\u88DC\u5168\u5B8C\u6210\u3002\u5DF2\u586B\u5145 {filled}/{total} \u9801\u3002",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "\u6A19\u7C64\u8D8A\u754C\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "\u5225\u540D\u751F\u6210\u5931\u6557\uFF1A{page} \u2014 {error}",
  lintFixItemFailed: "\u4FEE\u5FA9\u5931\u6557\uFF1A[[{target}]] \u2014 {error}",
  lintLinkItemFailed: "\u9023\u7D50\u5931\u6557\uFF1A{page} \u2014 {error}",
  lintRetrying: "\u51FA\u932F\u5F8C\u91CD\u8A66\u4E2D\uFF08{attempt}/{max}\uFF09...",
  lintAnalyzingLLM: "LLM \u6B63\u5728\u5206\u6790 Wiki \u5065\u5EB7\u72C0\u614B...",
  saveToWikiSuccess: "\u5C0D\u8A71\u5DF2\u5132\u5B58\u5230Wiki\uFF01",
  saveSummary: "{entities} \u500B\u5BE6\u9AD4, {concepts} \u500B\u6982\u5FF5, {pages} \u500B\u9801\u9762",
  aliasAdded: "\u5DF2\u70BA\u9801\u9762'{page}'\u65B0\u589E\u5225\u540D'{alias}'",
  // 状态消息
  ingestingSources: "\u6B63\u5728\u651D\u5165\u539F\u59CB\u6A94...",
  queryingWiki: "\u6B63\u5728\u67E5\u8A62 Wiki...",
  lintingWiki: "\u6B63\u5728\u7DAD\u8B77 Wiki...",
  savingToWiki: "\u6B63\u5728\u5132\u5B58\u5C0D\u8A71\u5230Wiki...",
  clearingHistory: "\u6E05\u7A7A\u5C0D\u8A71\u6B77\u53F2...",
  // 按钮
  ingestButton: "\u651D\u5165",
  queryButton: "\u67E5\u8A62",
  lintButton: "\u7DAD\u8B77",
  cancelButton: "\u53D6\u6D88",
  // 链接
  karpathyGistLink: "Karpathy's LLM Wiki Gist",
  obsidianPluginAPI: "Obsidian Plugin API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // 其他
  availableModelsLoading: "\u8F09\u5165\u53EF\u7528\u6A21\u578B...",
  noModelsAvailable: "\u7121\u53EF\u7528\u6A21\u578B",
  // LLM 语言提示
  llmLanguageHint: "\u8ACB\u7528\u4E2D\u6587\u56DE\u7B54\u3002",
  // Schema 配置
  schemaSection: "Schema \u914D\u7F6E",
  enableSchemaName: "\u555F\u7528 Schema",
  enableSchemaDesc: "\u751F\u6210 schema/config.md \u4E26\u6CE8\u5165\u5230\u6240\u6709 LLM \u63D0\u793A\u8A5E\u4E2D\uFF0C\u6307\u5C0E Wiki \u7D50\u69CB\u5316\u8F38\u51FA",
  viewSchemaButton: "\u6AA2\u8996 / \u7DE8\u8F2F Schema",
  regenerateSchemaButton: "\u91CD\u65B0\u751F\u6210\u9810\u8A2D Schema",
  schemaRegeneratedNotice: "\u9810\u8A2D Schema \u5DF2\u91CD\u65B0\u751F\u6210\u3002",
  schemaRegenerateFailed: "Schema \u751F\u6210\u5931\u6557",
  // Wiki 初始化状态
  wikiInitStatusReady: "Wiki \u5DF2\u521D\u59CB\u5316",
  wikiInitStatusNotReady: "Wiki \u672A\u521D\u59CB\u5316 \u2014 \u5C07\u5728\u9996\u6B21\u651D\u5165\u6642\u81EA\u52D5\u5EFA\u7ACB",
  // Wiki 输出语言
  wikiLanguageName: "Wiki \u8F38\u51FA\u8A9E\u8A00",
  wikiLanguageDesc: "\u6240\u6709\u751F\u6210\u7684 Wiki \u9801\u9762\u3001\u7D22\u5F15\u3001\u65E5\u8A8C\u548C\u67E5\u8A62\u56DE\u8986\u5747\u4F7F\u7528\u6B64\u8A9E\u8A00",
  customWikiLanguageOption: "\u81EA\u5B9A\u7FA9...",
  customWikiLanguageHint: "\u8F38\u5165\u8A9E\u8A00\u540D\u7A31\uFF08\u5982 Italian\u3001Arabic\uFF09\uFF0C\u5C07\u4F5C\u70BA\u8F38\u51FA\u8A9E\u8A00\u6307\u4EE4\u50B3\u905E\u7D66 LLM\u3002",
  customWikiLanguagePlaceholder: "\u4F8B\u5982\uFF1AItalian",
  // 索引 & 日志标签（各语言）
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52D5\u751F\u6210\u7684\u77E5\u8B58\u5EAB\u76EE\u9304", entities: "\u5BE6\u9AD4", concepts: "\u6982\u5FF5", sources: "\u4F86\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u5EFA\u7ACB\u9801\u9762", updatedPages: "\u66F4\u65B0\u9801\u9762", contradictionsFound: "\u767C\u73FE\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // 知识提取
  extractionSectionTitle: "\u77E5\u8B58\u63D0\u53D6",
  extractionGranularityName: "\u63D0\u53D6\u9846\u7C92\u5EA6",
  extractionGranularityDesc: "\u63A7\u5236\u6BCF\u7BC7\u539F\u59CB\u6A94\u63D0\u53D6\u7684\u5BE6\u9AD4/\u6982\u5FF5\u6578\u91CF\u3002\u8D8A\u9AD8 = \u751F\u6210\u9801\u9762\u8D8A\u591A\uFF0CAPI\u6D88\u8017\u8D8A\u5927\u3002\n\u7CBE\u7D30\uFF1A\u55AE\u7BC7\u6DF1\u5EA6\u3002\u6A19\u6E96\uFF1A\u65E5\u5E38\u7B46\u8A18\u3002\u7C97\u653E\uFF1A\u5FEB\u901F\u6982\u89BD\u3002\u6975\u7C21\uFF1A\u6279\u6B21100+\u6A94\u6848\u3002\u81EA\u5B9A\u7FA9\uFF1A\u81EA\u884C\u8A2D\u5B9A\uFF08\u6700\u9AD8300\u500B\uFF09\u3002\n\u63D0\u793A\uFF1A\u6279\u6B21\u8655\u7406\u5927\u91CF\u6A94\u6848\u6642\uFF0C\u5EFA\u8B70\u4F7F\u7528\u6975\u7C21\u6216\u7C97\u653E\u4EE5\u7BC0\u7701\u6642\u9593\u548C\u6210\u672C\u3002",
  extractionGranularityFine: "\u7CBE\u7D30 \u2014 \u55AE\u7BC7\u6DF1\u5EA6\u5206\u6790\uFF08\u2264100\u500B\uFF09",
  extractionGranularityStandard: "\u6A19\u6E96 \u2014 \u65E5\u5E38\u7B46\u8A18\uFF08\u226450\u500B\uFF09",
  extractionGranularityCoarse: "\u7C97\u653E \u2014 \u5FEB\u901F\u6982\u89BD\uFF08\u226410\u500B\uFF09",
  extractionGranularityMinimal: "\u6975\u7C21 \u2014 \u6279\u6B21100+\u6A94\u6848\uFF08\u22645\u500B\uFF09",
  extractionGranularityCustom: "\u81EA\u5B9A\u7FA9 \u2014 \u81EA\u884C\u8A2D\u5B9A\u4E0A\u9650\uFF081~500\uFF09",
  customEntityLimitName: "\u81EA\u5B9A\u7FA9\u5BE6\u9AD4\u4E0A\u9650",
  customEntityLimitDesc: "\u6BCF\u7BC7\u539F\u59CB\u6A94\u6700\u591A\u63D0\u53D6\u7684\u5BE6\u9AD4\u6578\u91CF\uFF081-500\uFF09",
  customConceptLimitName: "\u81EA\u5B9A\u7FA9\u6982\u5FF5\u4E0A\u9650",
  customConceptLimitDesc: "\u6BCF\u7BC7\u539F\u59CB\u6A94\u6700\u591A\u63D0\u53D6\u7684\u6982\u5FF5\u6578\u91CF\uFF081-500\uFF09",
  // Issue #85 v2: 标签词汇表 (chip input 交互, 嵌入 Wiki 配置)
  tagVocabularyInlineDesc: '\u70BA entity \u548C concept frontmatter \u6A19\u7C64\u8A2D\u5B9A\u53D7\u63A7\u8A5E\u5F59\u3002Obsidian \u5DE2\u72C0\u6A19\u7C64 "/" \u6703\u88AB\u4FDD\u7559\u3002',
  tagVocabularyModeName: "\u6A19\u7C64\uFF08Tags)\u8A5E\u5F59\u8868\u914D\u7F6E",
  tagVocabularyModeDescDefault: '\u9810\u8A2D\u4F7F\u7528\u5167\u5EFA\u6A19\u7C64: {}\u3002\u5207\u63DB\u5230"\u81EA\u5B9A\u7FA9"\u53EF\u5B9A\u7FA9\u60A8\u81EA\u5DF1\u7684\u8A5E\u5F59\u3002',
  tagVocabularyModeDescCustom: "\u81EA\u5B9A\u7FA9\uFF1A\u5728\u4E0B\u65B9\u4F7F\u7528 chip \u8F38\u5165\u5B9A\u7FA9\u60A8\u81EA\u5DF1\u7684 entity \u8207 concept \u6A19\u7C64\u3002\u56DE\u8ECA\u6216\u9017\u865F\u65B0\u589E, \xD7 \u522A\u9664\u3002",
  tagVocabularyModeDefault: "\u9810\u8A2D (\u5167\u5EFA\u5B50\u578B\u5225\u6A19\u7C64)",
  tagVocabularyModeCustom: "\u81EA\u5B9A\u7FA9 (\u4F7F\u7528\u8005\u5B9A\u7FA9)",
  customEntityTagsName: "\u81EA\u5B9A\u7FA9\u5BE6\u9AD4\u6A19\u7C64",
  customEntityTagsDesc: '\u6309\u56DE\u8ECA\u6216\u9017\u865F\u65B0\u589E chip, \u9EDE \xD7 \u522A\u9664\u3002\u5DE2\u72C0\u6A19\u7C64 "/" \u6703\u88AB\u4FDD\u7559\u3002',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "\u81EA\u5B9A\u7FA9\u6982\u5FF5\u6A19\u7C64",
  customConceptTagsDesc: '\u6309\u56DE\u8ECA\u6216\u9017\u865F\u65B0\u589E chip, \u9EDE \xD7 \u522A\u9664\u3002\u5DE2\u72C0\u6A19\u7C64 "/" \u6703\u88AB\u4FDD\u7559\u3002',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "\u91CD\u8907\u6A19\u7C64\u5DF2\u8DF3\u904E",
  // 摄入加速
  accelerationSectionTitle: "\u651D\u5165\u52A0\u901F",
  pageGenerationConcurrencyName: "LLM \u4F75\u767C\u5EA6",
  pageGenerationConcurrencyDesc: "\u651D\u5165\u548C Lint \u64CD\u4F5C\u4E2D\u7684\u4E26\u884C LLM \u547C\u53EB\u6578\u3002\u6578\u503C\u8D8A\u9AD8\u8655\u7406\u8D8A\u5FEB\uFF0C\u4F46\u6703\u589E\u52A0 API \u6D88\u8017\u4E26\u53EF\u80FD\u89F8\u767C\u9650\u6D41\u3002",
  concurrencyValueSingular: "\u7576\u524D\u4F75\u767C\u5EA6\uFF1A{}\uFF08\u5E8F\u5217 \u2014 \u6700\u5B89\u5168\uFF09",
  concurrencyValuePlural: "\u7576\u524D\u4F75\u767C\u5EA6\uFF1A{}\uFF08\u4E26\u884C\uFF09",
  batchDelayName: "\u6279\u6B21\u5EF6\u9072 (ms)",
  batchDelayDesc: "\u4E26\u884C\u6279\u6B21\u9593\u7684\u5EF6\u9072\uFF0C\u9632\u6B62 API \u9650\u6D41\uFF08100-2000ms\uFF09\u3002\u7576\u524D\u503C\uFF1A{}ms\u3002\u5982\u9047 429 \u932F\u8AA4\u8ACB\u589E\u5927\u6B64\u503C\u3002",
  // 自动维护
  autoMaintainSection: "\u81EA\u52D5\u7DAD\u8B77",
  autoMaintainBetaBadge: "BETA \u6E2C\u8A66 \u2014 \u5BE6\u9A57\u6027\u529F\u80FD\uFF0C\u53EF\u80FD\u5B58\u5728\u554F\u984C\uFF0C\u50C5\u5EFA\u8B70\u9AD8\u968E\u4F7F\u7528\u8005\u4F7F\u7528\u3002",
  autoWatchName: "\u76E3\u807D\u8CC7\u6599\u593E",
  autoWatchDesc: "\u81EA\u52D5\u6AA2\u6E2C\u76E3\u807D\u5217\u8868\u4E2D\u8CC7\u6599\u593E\u7684\u65B0\u589E\u6216\u8B8A\u66F4 .md \u6A94\u6848\uFF0C\u901A\u77E5\u6216\u81EA\u52D5\u651D\u5165",
  watchedFoldersName: "\u76E3\u807D\u8CC7\u6599\u593E\u5217\u8868",
  watchedFoldersDesc: "\u76E3\u807D\u65B0\u5167\u5BB9\u7684\u8CC7\u6599\u593E\u3002\u9EDE\u9078\u300C\u65B0\u589E\u8CC7\u6599\u593E\u300D\u5F9E\u5009\u5EAB\u4E2D\u9078\u64C7\u3002",
  addWatchedFolderButton: "\u65B0\u589E\u8CC7\u6599\u593E",
  removeWatchedFolderButton: "\u79FB\u9664",
  webClipperPresetName: "\u76E3\u807D Clippings\uFF08Web Clipper\uFF09",
  webClipperPresetDesc: "\u5C07 Obsidian Web Clipper \u4F7F\u7528\u7684 Clippings/ \u8CC7\u6599\u593E\u52A0\u5165\u76E3\u807D\u5217\u8868\u3002\u7DB2\u9801\u526A\u85CF\u5167\u5BB9\u5C07\u81EA\u52D5\u651D\u5165 Wiki\u3002",
  noWatchedFoldersHint: "\u5C1A\u672A\u914D\u7F6E\u76E3\u807D\u8CC7\u6599\u593E\u3002\u8ACB\u65B0\u589E\u8CC7\u6599\u593E\u6216\u555F\u7528 Clippings \u9810\u8A2D\u3002",
  autoWatchModeName: "\u76E3\u807D\u6A21\u5F0F",
  autoWatchModeDesc: '"\u50C5\u901A\u77E5"\u986F\u793A\u63D0\u793A\u3002"\u81EA\u52D5\u651D\u5165"\u975C\u9ED8\u8655\u7406\u3002',
  watchModeNotify: "\u50C5\u901A\u77E5",
  watchModeAuto: "\u81EA\u52D5\u651D\u5165",
  autoWatchDebounceName: "\u9632\u6296\u5EF6\u9072 (\u79D2)",
  autoWatchDebounceDesc: "\u6A94\u6848\u8B8A\u66F4\u5F8C\u7B49\u5F85\u591A\u4E45\u89F8\u767C\u651D\u5165 (1-60\u79D2)",
  periodicLintName: "\u5B9A\u6642\u7DAD\u8B77",
  periodicLintDesc: "\u5B9A\u671F\u6AA2\u67E5 Wiki \u5065\u5EB7\u72C0\u6CC1\uFF0C\u6709\u539F\u59CB\u6A94\u8B8A\u66F4\u6642\u624D\u57F7\u884C LLM \u7DAD\u8B77",
  periodicLintOff: "\u95DC\u9589",
  periodicLintHourly: "\u6BCF\u5C0F\u6642",
  periodicLintDaily: "\u6BCF\u5929",
  periodicLintWeekly: "\u6BCF\u9031",
  autoSmartFixName: "\u81EA\u52D5\u667A\u6167\u4FEE\u5FA9",
  autoSmartFixDesc: "\u57F7\u884C Lint \u6642\uFF0C\u81EA\u52D5\u57F7\u884C\u6240\u6709\u4FEE\u5FA9\uFF08\u667A\u6167\u4E00\u9375\u4FEE\u5FA9\uFF09\uFF0C\u7121\u9700\u986F\u793A\u5831\u544A\u5F48\u7A97\u3002\u4FEE\u5FA9\u5B8C\u6210\u5F8C\u4ECD\u6703\u986F\u793A\u6458\u8981\u3002",
  autoSmartFixNotice: "\u81EA\u52D5\u667A\u6167\u4FEE\u5FA9\uFF1A\u6B63\u5728\u61C9\u7528\u6240\u6709\u4FEE\u5FA9...",
  startupCheckName: "\u555F\u52D5\u6642\u57F7\u884C\u5FEB\u901F\u4FEE\u5FA9",
  startupCheckDesc: "\u5916\u639B\u8F09\u5165\u6642\u81EA\u52D5\u4FEE\u5FA9\u4F4E\u968E\u683C\u5F0F\u554F\u984C\uFF08sources \u6B04\u4F4D\u3001\u96D9\u5C64 wikilink\uFF09\uFF0C\u4E26\u9A57\u8B49 Wiki \u76EE\u9304\u7D50\u69CB\u3002\u9810\u8A2D\u958B\u555F\u3002",
  suggestSchemaCommand: "\u5EFA\u8B70 Schema \u66F4\u65B0",
  autoMaintainCostWarning: '\u26A0\uFE0F \u8CBB\u7528\u63D0\u9192\uFF1A\u81EA\u52D5\u7DAD\u8B77\u529F\u80FD\u6703\u6D88\u8017 API Token\u3002"\u81EA\u52D5\u651D\u5165"\u6A21\u5F0F\u5728\u6BCF\u6B21\u539F\u59CB\u6A94\u8B8A\u66F4\u6642\u89F8\u767C LLM \u547C\u53EB\u3002"\u5B9A\u6642\u7DAD\u8B77"\u5B9A\u671F\u57F7\u884C LLM \u5065\u5EB7\u6AA2\u67E5\uFF08\u50C5\u5728\u6709\u65B0\u8B8A\u66F4\u6642\u57F7\u884C\uFF09\u3002\u8ACB\u8B39\u614E\u914D\u7F6E\u4EE5\u907F\u514D\u610F\u5916\u8CBB\u7528\u3002',
  // 通知
  startupCheckSummary: "Wiki \u5171 {pages} \u9801\uFF08{entities} \u500B\u5BE6\u9AD4\u3001{concepts} \u500B\u6982\u5FF5\u3001{sources} \u500B\u4F86\u6E90\uFF09",
  watcherActiveNotice: "Wiki: \u6A94\u6848\u76E3\u807D\u5DF2\u555F\u52D5 \u2014 \u6B63\u5728\u76E3\u63A7\u6307\u5B9A\u8CC7\u6599\u593E",
  watchIngestNotice: 'Wiki: sources/ \u4E2D\u6709 {count} \u500B\u6A94\u6848\u8B8A\u66F4\u3002\u8ACB\u57F7\u884C"\u651D\u5165\u539F\u59CB\u6A94"\u8655\u7406\u3002',
  autoIngestRunning: "\u6B63\u5728\u81EA\u52D5\u651D\u5165 {count} \u500B\u8B8A\u66F4\u6A94\u6848...",
  autoIngestComplete: "\u81EA\u52D5\u651D\u5165\u5B8C\u6210\uFF1A\u6210\u529F {success}\uFF0C\u5931\u6557 {fail}",
  scheduledLintRunning: "\u6B63\u5728\u57F7\u884C\u5B9A\u6642 Wiki \u7DAD\u8B77...",
  wikiLintStats: "Wiki \u7DAD\u8B77: \u5171 {pages} \u9801\uFF08{entities} \u5BE6\u9AD4, {concepts} \u6982\u5FF5, {sources} \u4F86\u6E90\uFF09",
  wikiHealthStats: "Wiki \u5065\u5EB7: \u5171 {pages} \u9801\uFF08{entities} \u5BE6\u9AD4, {concepts} \u6982\u5FF5, {sources} \u4F86\u6E90\uFF09{indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Wiki \u5FEB\u901F\u4FEE\u5FA9\u5B8C\u6210",
  startupCheckStructureLabel: "Wiki \u7D50\u69CB",
  startupCheckStructureOk: "\u5B8C\u6574",
  startupCheckStructureMissing: "\u4E0D\u5B8C\u6574 \u2014 \u5C07\u5728\u9996\u6B21\u651D\u5165\u6642\u81EA\u52D5\u5EFA\u7ACB",
  startupCheckSourcesLabel: "Sources \u6B78\u4E00\u5316",
  startupCheckSourcesClean: "\u5DF2\u898F\u7BC4",
  startupCheckSourcesCleaned: "\u6E05\u7406\u4E86 {files} \u500B\u6A94\u6848\uFF0C{entries} \u8655\u689D\u76EE",
  startupCheckIncompleteClean: "\u672A\u5B8C\u6210\u9801\u9762\uFF1A\u7121",
  startupCheckIncompleteArchived: "\u672A\u5B8C\u6210\u9801\u9762\uFF1A\u6B78\u6A94\u4E86 {count} \u500B\uFF08\u53EF\u5F9E .trash \u6062\u5FA9\uFF09",
  startupCheckDisableHint: "\u5982\u9700\u95DC\u9589\uFF0C\u524D\u5F80 \u8A2D\u5B9A \u2192 \u81EA\u52D5\u7DAD\u8B77 \u2192 \u555F\u52D5\u6642\u57F7\u884C\u5FEB\u901F\u4FEE\u5FA9",
  lintWikiStart: "\u958B\u59CB\u7DAD\u8B77 wiki...",
  lintWikiComplete: "\u7DAD\u8B77\u5B8C\u6210",
  lintWikiFailed: "\u7DAD\u8B77\u5931\u6557",
  analyzingSchema: "\u6B63\u5728\u5206\u6790 Wiki \u4E26\u751F\u6210 Schema \u5EFA\u8B70...",
  schemaSuggestionGenerated: "Schema \u5EFA\u8B70\u5DF2\u751F\u6210\uFF0C\u8ACB\u6AA2\u8996 wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "\u672A\u6AA2\u6E2C\u5230 Schema \u9700\u8981\u66F4\u65B0\u3002",
  schemaSuggestionFailed: "Schema \u5EFA\u8B70\u751F\u6210\u5931\u6557",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Schema \u66F4\u65B0\u9810\u89BD",
  schemaDiffSummary: "+{add} / -{del} \u884C",
  schemaDiffRegenerateLabel: "\u6539\u9032\u5EFA\u8B70\uFF08\u53EF\u9078\uFF09\uFF1A",
  schemaDiffRegenerateBtn: "\u91CD\u65B0\u751F\u6210",
  schemaDiffOpenFileBtn: "\u958B\u555F\u6A94\u6848",
  schemaDiffApplyBtn: "\u5957\u7528",
  schemaDiffApplied: "Schema \u5DF2\u66F4\u65B0\u3002\u5099\u4EFD\uFF1A{path}",
  schemaDiffEmptyTitle: "LLM \u5EFA\u8B70\u7121\u9700\u4FEE\u6539",
  schemaDiffFailed: "Schema \u5957\u7528\u5931\u6557\uFF1A{reason}",
  schemaDiffRestoreHint: '\u5DF2\u5099\u4EFD\u5230 {path}\u3002\u82E5\u9700\u6062\u5FA9\uFF0C\u8ACB\u5728\u6A94\u6848\u7E3D\u7BA1\u4E2D\u5C07\u8A72\u6A94\u6848\u91CD\u65B0\u547D\u540D\u70BA "wiki/schema/config.md"\u3002\u7CFB\u7D71\u6703\u4FDD\u7559\u6700\u8FD1 3 \u500B\u5099\u4EFD\u3002',
  schemaRegenerateNoBody: "\u91CD\u65B0\u751F\u6210\u6210\u529F\uFF0C\u4F46 LLM \u672A\u50B3\u56DE\u65B0\u7684 Schema\u3002",
  schemaNotFoundNotice: "Schema \u6A94\u6848\u672A\u627E\u5230\u3002\u555F\u7528 Schema \u529F\u80FD\u4EE5\u5EFA\u7ACB\u5B83\u3002",
  selectFolderNoMdFiles: "\u8CC7\u6599\u593E {path} \u4E2D\u6C92\u6709 Markdown \u6A94\u6848",
  batchIngestSkipNotice: "\u8DF3\u904E {skipped}/{total} \u500B\u5DF2\u651D\u5165\u6A94\u6848\u3002\u6B63\u5728\u651D\u5165 {new} \u500B\u65B0\u6A94\u6848...",
  batchIngestAllIngested: "\u8A72\u8CC7\u6599\u593E\u4E2D\u7684\u6240\u6709 {total} \u500B\u6A94\u6848\u5747\u5DF2\u651D\u5165\u3002",
  batchIngestStarting: '\u6B63\u5728\u5F9E "{folder}" \u651D\u5165 {count} \u500B\u6A94\u6848 \u2014 \u53EF\u80FD\u9700\u8981\u6578\u5206\u9418\u3002\u5B8C\u6210\u5F8C\u6703\u986F\u793A\u5831\u544A\u3002',
  batchIngestComplete: "\u6279\u6B21\u651D\u5165\u5B8C\u6210: \u6210\u529F {success}/{total}, \u5931\u6557 {fail}",
  batchIngestFailedFiles: "\u5931\u6557\u7684\u6A94\u6848:",
  historyTruncated: "\u6B77\u53F2\u5DF2\u622A\u65B7\u81F3\u6700\u8FD1 {max} \u8F2A\u5C0D\u8A71",
  historyCleared: "\u6B77\u53F2\u5DF2\u6E05\u7A7A",
  // 用户反馈闭环
  reviewedPagePreserved: "\u5DF2\u4FDD\u7559\u4F7F\u7528\u8005\u5BE9\u95B1\u5167\u5BB9: {}",
  // Query 回流
  querySuggestSaveTitle: "\u5132\u5B58\u5230 Wiki\uFF1F",
  querySuggestSaveDesc: "\u6B64\u5C0D\u8A71\u5305\u542B\u6709\u50F9\u503C\u7684\u77E5\u8B58\u9EDE\uFF0C\u662F\u5426\u5132\u5B58\u5230 Wiki\uFF1F",
  querySuggestSaveYes: "\u5132\u5B58",
  querySuggestSaveNo: "\u5FFD\u7565",
  // 摄入报告
  ingestReportElapsedTime: "\u8017\u6642",
  ingestReportSkippedFiles: "\u8DF3\u904E\uFF08\u5DF2\u651D\u5165\uFF09",
  ingestReportRejectedFiles: "\u5DF2\u8DF3\u904E",
  rejectionReasonEmpty: "\u7A7A\u5167\u5BB9",
  rejectionReasonType: "\u4E0D\u652F\u63F4\u7684\u578B\u5225",
  rejectionReasonDuplicate: "\u91CD\u8907\u5167\u5BB9",
  ingestReportFailedGuidance: "\u9019\u4E9B\u689D\u76EE\u672A\u80FD\u81EA\u52D5\u5EFA\u7ACB\u3002\u60A8\u53EF\u624B\u52D5\u5EFA\u7ACB\u5C0D\u61C9\u9801\u9762\uFF0C\u6216\u964D\u4F4E\u63D0\u53D6\u9846\u7C92\u5EA6\u5F8C\u91CD\u65B0\u651D\u5165\u539F\u59CB\u6A94\u3002",
  ingestReportCollisions: "\u8DE8\u578B\u5225\u78B0\u649E\uFF08\u5DF2\u5408\u4F75\u70BA\u5225\u540D\uFF09",
  // 命令名称（sentence case 遵循 Obsidian Bot 规则）
  cmdIngestSource: "\u651D\u5165\u55AE\u500B\u539F\u59CB\u6A94",
  cmdIngestFolder: "\u5F9E\u8CC7\u6599\u593E\u651D\u5165",
  cmdQueryWiki: "\u67E5\u8A62 Wiki",
  cmdLintWiki: "\u7DAD\u8B77 Wiki",
  cmdRegenerateIndex: "\u91CD\u65B0\u751F\u6210\u7D22\u5F15",
  cmdSuggestSchema: "\u5EFA\u8B70 Schema \u66F4\u65B0",
  cmdCancelIngestion: "\u53D6\u6D88\u7576\u524D\u63D0\u53D6",
  cmdIngestActiveFile: "\u651D\u5165\u7576\u524D\u6A94\u6848",
  cmdViewHistory: "\u6AA2\u8996\u651D\u5165\u6B77\u53F2",
  noActiveFile: "\u7576\u524D\u6C92\u6709\u958B\u555F\u7684\u6A94\u6848",
  mdOnlyFile: "\u50C5\u652F\u63F4\u651D\u5165 Markdown \u6A94\u6848",
  // Ingestion status bar
  ingestionStatusBar: "\u63D0\u53D6\u4E2D... \u9EDE\u9078\u53D6\u6D88",
  lintStatusBar: "\u7DAD\u8B77\u4E2D... \u9EDE\u9078\u53D6\u6D88",
  ingestStatusAnalyzing: "\u63D0\u53D6\u4E2D\u2026 (\u9EDE\u9078\u53D6\u6D88)",
  lintStatusReading: "\u7DAD\u8B77\u4E2D\u2026 (\u9EDE\u9078\u53D6\u6D88)",
  lintStatusDuplicates: "\u7DAD\u8B77\u4E2D\u2026 (\u9EDE\u9078\u53D6\u6D88)",
  lintStatusScanningLinks: "\u7DAD\u8B77\u4E2D\u2026 (\u9EDE\u9078\u53D6\u6D88)",
  lintStatusAnalyzing: "\u7DAD\u8B77\u4E2D\u2026 (\u9EDE\u9078\u53D6\u6D88)",
  ingestionCancelling: "\u6B63\u5728\u53D6\u6D88 \u2014 \u7576\u524D\u6279\u6B21\u5B8C\u6210\u5F8C\u5C07\u505C\u6B62",
  ingestionCancelled: "\u63D0\u53D6\u5DF2\u53D6\u6D88",
  crossTypeCollisionNotice: "{count} \u500B\u689D\u76EE\u5408\u4F75\u70BA\u8DE8\u578B\u5225\u5225\u540D\uFF08\u5BE6\u9AD4 \u2194 \u6982\u5FF5\u91CD\u8907\u5DF2\u9632\u6B62\uFF09",
  // 维护报告
  lintReportTitle: "Wiki \u7DAD\u8B77\u5831\u544A",
  lintReportSummary: "Wiki \u72C0\u614B\u6982\u89BD\uFF1A\u5171 {total} \u500B\u9801\u9762\uFF0C{aliasesMissing} \u500B\u7F3A\u5931\u5225\u540D\uFF0C\u91CD\u8907 {duplicates} \u500B\uFF0C\u65B7\u93C8 {deadLinks} \u500B\uFF08\u5176\u4E2D {deadLinkFromDup} \u500B\u6D89\u53CA\u91CD\u8907\u9801\u9762\uFF09\uFF0C\u5B64\u7ACB {orphans} \u500B\uFF08\u5176\u4E2D {orphanFromDup} \u500B\u662F\u91CD\u8907\u9801\u9762\uFF09\uFF0C\u7A7A\u6D1E {emptyPages} \u500B\uFF0C\u7121\u4F86\u6E90\u5F15\u8B49 {ungroundedQuotes} \u500B\uFF0C\u6A19\u7C64\u8D8A\u754C {tagViolations} \u500B\u3002\u672C\u6B21 Lint \u8017\u6642 {elapsedSeconds} \u79D2",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "\u9AD8\u968E\u5F15\u6578\u8A2D\u5B9A",
  advancedSettingsModeDesc: "\u9810\u8A2D\u6A21\u5F0F\u4F7F\u7528 provider \u81EA\u8EAB\u7684\u884C\u70BA\uFF0C\u4E0D\u50B3\u9001\u4EFB\u4F55\u601D\u8003\u63A7\u5236\u3001\u6EAB\u5EA6\u6216\u91CD\u8907\u61F2\u7F70\u6B04\u4F4D\u3002\u9069\u5408\u5927\u591A\u6578\u4F7F\u7528\u8005\u3002\u50C5\u7576\u9700\u8981\u986F\u5F0F\u8986\u84CB provider \u9810\u8A2D\u503C\u6642\uFF08\u4F8B\u5982\u5F37\u5236\u7279\u5B9A\u601D\u8003\u63A7\u5236\u65B9\u8A00\u3001\u8A2D\u5B9A\u975E\u9810\u8A2D\u6EAB\u5EA6\u7B49\uFF09\uFF0C\u518D\u5207\u63DB\u5230\u81EA\u5B9A\u7FA9\u6A21\u5F0F\u3002",
  advancedSettingsDefault: "\u9810\u8A2D\uFF08\u4F7F\u7528 provider \u884C\u70BA\uFF09",
  advancedSettingsCustom: "\u81EA\u5B9A\u7FA9\uFF08\u8986\u84CB provider \u9810\u8A2D\uFF09",
  disableThinkingName: "\u7981\u7528\u601D\u8003",
  disableThinkingDesc: "\u986F\u5F0F\u555F\u7528\u3002\u958B\u555F\u5F8C\uFF0C\u5916\u639B\u6703\u5411 provider \u50B3\u9001\u601D\u8003\u63A7\u5236\u6307\u4EE4\uFF0C\u4E26\u5728 provider \u62D2\u7D55\u6642\u56DE\u9000\u5230 3 \u5C64\u7D1A\u65B9\u8A00\u93C8\uFF08anthropic \u2192 openai \u2192 none\uFF09\u3002\u50C5\u7576\u4F60\u7684 provider \u628A\u601D\u8003\u5167\u5BB9\u6D29\u9732\u5230\u56DE\u7B54\u4E2D\u4E26\u9700\u8981\u6291\u5236\u6642\u624D\u4F7F\u7528\u3002\u5927\u591A\u6578 provider \u81EA\u8EAB\u80FD\u6B63\u78BA\u8655\u7406\u601D\u8003\uFF0C\u95DC\u9589\u5B83\uFF08\u9810\u8A2D\uFF09\u901A\u5E38\u80FD\u7372\u5F97\u66F4\u9AD8\u8CEA\u91CF\u3002",
  // Issue #137: 兼容性提示（简短；不列举 provider，避免维护负担）
  extractionTemperatureName: "\u63D0\u53D6\u6EAB\u5EA6",
  extractionTemperatureDesc: "\u7BC4\u570D 0\u20132\u3002\u8D8A\u4F4E\u5247 LLM \u8F38\u51FA\u8D8A\u78BA\u5B9A\u3001\u8D8A\u5FE0\u65BC\u539F\u6587\uFF1B\u8D8A\u9AD8\u5247\u8D8A\u6709\u5275\u610F\u3002\u7559\u7A7A\u8868\u793A\u4F7F\u7528\u63D0\u4F9B\u5546\u9810\u8A2D\u503C\u3002\u82E5\u63D0\u4F9B\u5546\u62D2\u7D55\u6B64\u503C\uFF0C\u5916\u639B\u6703\u81EA\u52D5\u525D\u96E2\u8A72\u6B04\u4F4D\u4E26\u986F\u793A\u4E00\u6B21\u6027\u901A\u77E5\u3002",
  chatTemperatureName: "\u67E5\u8A62\u6EAB\u5EA6",
  chatTemperatureDesc: "\u7BC4\u570D 0\u20132\u3002\u8207\u63D0\u53D6\u6EAB\u5EA6\u76F8\u540C\uFF0C\u4F46\u96BB\u5F71\u97FF\u804A\u5929/\u67E5\u8A62\u56DE\u8986\u3002\u7559\u7A7A\u8868\u793A\u4F7F\u7528\u63D0\u4F9B\u5546\u9810\u8A2D\u503C\u3002\u82E5\u63D0\u4F9B\u5546\u62D2\u7D55\u6B64\u503C\uFF0C\u5916\u639B\u6703\u81EA\u52D5\u525D\u96E2\u8A72\u6B04\u4F4D\u4E26\u986F\u793A\u4E00\u6B21\u6027\u901A\u77E5\u3002",
  repetitionPenaltyName: "\u91CD\u8907\u61F2\u7F70",
  repetitionPenaltyDesc: "\u7BC4\u570D 0\u20132\u3002\u8D8A\u9AD8\u5247\u8D8A\u80FD\u6E1B\u5C11\u91CD\u8907\u6A21\u5F0F\u3002\u7559\u7A7A\u8868\u793A\u4F7F\u7528\u63D0\u4F9B\u5546\u9810\u8A2D\u503C\u3002\u76F8\u5BB9\u6027\u63D0\u793A\uFF1A\u8A31\u591A\u96F2\u7AEF\u63D0\u4F9B\u5546\u4E0D\u63A5\u53D7\u6B64\u6B04\u4F4D\uFF0C\u5916\u639B\u6703\u5728 400 \u6642\u81EA\u52D5\u525D\u96E2\u4E26\u986F\u793A\u4E00\u6B21\u6027\u901A\u77E5\u3002",
  temperaturePlaceholder: "\u7559\u7A7A = \u63D0\u4F9B\u5546\u9810\u8A2D",
  lintDeadLinkSection: "\u65B7\u93C8\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintEmptyPageSection: "\u7A7A\u6D1E\u9801\u9762\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintOrphanSection: "\u5B64\u7ACB\u9801\u9762\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintContradictionSection: "\u77DB\u76FE\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09",
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}**\uFF08\u9801\u9762\u4E0D\u5B58\u5728\uFF09{dupFlag}",
  lintDeadLinkMore: "- ... \u5171 {count} \u8655\u65B7\u93C8",
  lintEmptyPageItem: "- [[{page}]] \u2014 \u5167\u5BB9\u4E0D\u8DB3 50 \u5B57\u5143",
  lintOrphanItem: "- [[{page}]] \u2014 \u7121\u5176\u4ED6 Wiki \u9801\u9762\u9023\u7D50\u81F3\u6B64{dupFlag}",
  lintContradictionAutoFixed: "\uFF08\u672C\u6B21\u81EA\u52D5\u4FEE\u5FA9 {count} \u500B\uFF09",
  lintDeadLinkAffectedByDup: " \uFF08\u26A0\uFE0F \u6D89\u53CA\u91CD\u8907\u9801\u9762\uFF09",
  lintOrphanIsDuplicate: " \uFF08\u26A0\uFE0F \u91CD\u8907\u9801\u9762\uFF09",
  lintNoIssuesFound: "\u672A\u6AA2\u6E2C\u5230\u91CD\u8907\u3001\u65B7\u93C8\u3001\u7A7A\u6D1E\u3001\u5B64\u7ACB\u9801\u9762\u6216\u7121\u4F86\u6E90\u5F15\u8B49\u3002",
  lintQuoteGroundingSection: "\u7121\u4F86\u6E90\u5F15\u8B49\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}\uFF1A"{quote}"',
  lintContradictionOpen: "\u672A\u89E3\u6C7A\u7684\u77DB\u76FE\uFF1A{count} \u500B",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "\u5F85\u8655\u7406",
  lintContradictionStatusPendingFix: "\u5F85\u4FEE\u5FA9",
  lintLLMAnalysisHeading: "## LLM \u5206\u6790",
  // 维护分析 Prompt
  lintAnalysisPrompt: '\u4F60\u662F\u4E00\u500B Wiki \u7DAD\u8B77\u52A9\u624B\u3002\u8ACB\u57FA\u65BC\u4EE5\u4E0B\u8CC7\u8A0A\u6AA2\u67E5 Wiki \u7684\u5065\u5EB7\u72C0\u6CC1\u3002\n\nWiki \u7D22\u5F15\uFF1A\n{index}\n\nWiki \u9801\u9762\u5167\u5BB9\u6A23\u672C\uFF08\u5171 {total} \u9801\uFF0C\u5C55\u793A {sample} \u9801\uFF09\uFF1A\n{contentSample}\n\n\u7A0B\u5F0F\u6AA2\u6E2C\u7D50\u679C\uFF08\u5DF2\u9A57\u8B49\uFF0C\u8ACB\u52FF\u91CD\u8907\u5831\u544A\uFF09\uFF1A\n{progReport}\n\n\u8ACB\u6AA2\u67E5\u4EE5\u4E0B\u65B9\u9762\uFF08\u8DF3\u904E\u7A0B\u5F0F\u5DF2\u6AA2\u6E2C\u7684\u65B7\u93C8/\u7A7A\u6D1E/\u5B64\u7ACB\uFF09\uFF1A\n1. **\u77DB\u76FE** \u2014 \u4E0D\u540C\u9801\u9762\u5C0D\u540C\u4E00\u4E8B\u5BE6\u7684\u8AAA\u6CD5\u662F\u5426\u77DB\u76FE\n2. **\u904E\u6642** \u2014 \u662F\u5426\u6709\u5BA3\u544A\u660E\u986F\u904E\u6642\n3. **\u7F3A\u5931** \u2014 \u54EA\u4E9B\u91CD\u8981\u6982\u5FF5\u7F3A\u5C11\u7368\u7ACB\u9801\u9762\n4. **\u7D50\u69CB** \u2014 \u9801\u9762\u7D50\u69CB\u662F\u5426\u5408\u7406\uFF0C\u4EA4\u53C9\u5F15\u7528\u662F\u5426\u5145\u5206\n\n\u8F38\u51FA\u683C\u5F0F\uFF1A\u4F7F\u7528 Markdown\uFF0C\u4EE5 "## LLM \u5206\u6790" \u958B\u982D\u3002\u6BCF\u500B\u767C\u73FE\u7528\u4E00\u884C "- [\u5177\u9AD4\u554F\u984C]"\u3002\u5982\u7121\u554F\u984C\u5247\u5BEB "\u2705 \u672A\u767C\u73FE\u660E\u986F\u554F\u984C\u3002"',
  // 维护修复进度
  lintFixProgress: "\u4FEE\u5FA9\u65B7\u93C8 {current}/{total}\uFF1A[[{target}]]",
  lintFixDeadComplete: "\u65B7\u93C8\u4FEE\u5FA9\u5B8C\u6210\u3002\u5DF2\u4FEE\u5FA9 {fixed}/{total} \u9805\u3002",
  lintFillProgress: "\u64F4\u5145\u7A7A\u6D1E {current}/{total}\uFF1A{page}",
  lintFillComplete: "\u9801\u9762\u64F4\u5145\u5B8C\u6210\u3002\u5DF2\u586B\u5145 {filled}/{total} \u9801\u3002",
  lintDeleteCompleted: "\u5DF2\u522A\u9664 {count} \u500B\u7A7A\u6D1E\u9801\u9762",
  lintDeleteFailed: "\u522A\u9664\u5931\u6557 {failed}/{total} \u500B\u7A7A\u767D stub\uFF08\u8A73\u60C5\u898B\u63A7\u5236\u6AAF\uFF09",
  lintFillFailed: "\u64F4\u5145\u5931\u6557\uFF1A{page} \u2014 {error}",
  lintLinkProgress: "\u9023\u7D50\u5B64\u7ACB {current}/{total}\uFF1A{page}",
  lintLinkComplete: "\u5B64\u7ACB\u9801\u9762\u9023\u7D50\u5B8C\u6210\u3002\u5DF2\u9023\u7D50 {linked} \u9801\u3002",
  lintFixNoAction: "\u672A\u57F7\u884C\u64CD\u4F5C\uFF08\u7121 client\uFF09",
  lintFixIndexUpdated: "Wiki \u7D22\u5F15\u548C\u65E5\u8A8C\u5DF2\u66F4\u65B0\u3002",
  lintPollutedFixed: "\u5DF2\u4FEE\u5FA9\u6C59\u67D3\u9801\u9762\uFF1A{fixed}/{total}\u3002\u7D22\u5F15\u5DF2\u91CD\u5EFA\u3002",
  regenerateIndexCompleted: "\u7D22\u5F15\u5DF2\u91CD\u5EFA",
  operationFailed: "\u5931\u6557\uFF1A",
  lintFixAllComplete: "\u6240\u6709\u4FEE\u5FA9\u5DF2\u5B8C\u6210\u3002\u8A73\u60C5\u898B\u65E5\u8A8C\u3002",
  // 维护报告弹窗
  lintModalActionsTitle: "\u4FEE\u5FA9\u5EFA\u8B70\uFF08\u9700\u6D88\u8017LLM Token\uFF09\uFF1A",
  lintLogReference: "\u5B8C\u6574\u5831\u544A\u5DF2\u5132\u5B58\u81F3 log.md",
  lintModalFixDeadLinks: "\u4FEE\u5FA9\u65B7\u93C8\uFF08{count}\uFF09",
  lintModalExpandEmpty: "\u64F4\u5145\u7A7A\u6D1E\u9801\u9762\uFF08{count}\uFF09",
  lintModalDeleteEmpty: "\u522A\u9664\u7A7A\u6D1E\u9801\u9762\uFF08{count}\uFF09",
  lintModalLinkOrphans: "\u9023\u7D50\u5B64\u7ACB\u9801\u9762\uFF08{count}\uFF09",
  lintModalAnalyzeSchema: "\u5206\u6790 Schema",
  lintModalMergeDuplicates: "\u5408\u4F75\u91CD\u8907\u9801\u9762\uFF08{count}\uFF09",
  lintModalFixAll: "\u4E00\u9375\u4FEE\u5FA9\u6240\u6709\u554F\u984C\uFF08{count}\uFF09",
  lintDuplicateSection: "\u91CD\u8907\u9801\u9762\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09",
  lintPollutedSection: "\u6C59\u67D3\u9801\u9762\uFF08\u7A0B\u5F0F\u6AA2\u6E2C\uFF09[\u5171 {count} \u500B]",
  lintPollutedItem: '- [[{page}]] \u2192 \u61C9\u70BA "{clean}"',
  lintSourcesNormalizedSection: "Sources \u5DF2\u6B78\u4E00\u5316\uFF08\u81EA\u52D5\u4FEE\u5FA9\uFF09[\u5171 {files} \u500B\u6A94\u6848 / {entries} \u8655]",
  lintSourcesNormalizedItem: "\u6E05\u7406\u4E86 {files} \u500B\u6A94\u6848\u4E2D\u7684 {entries} \u8655 sources \u689D\u76EE\uFF08\u5DF2\u79FB\u9664\u5916\u90E8\u8DEF\u5F91\u3001.md \u5B57\u5C3E\u3001\u5225\u540D\u7BA1\u9053\uFF0C\u4E26\u53BB\u91CD\uFF09\u3002",
  lintDuplicateItem: "- [[{target}]] \u8207 [[{source}]] \u2014 {reason}",
  lintMergeProgress: "\u5408\u4F75 {current}/{total}\uFF1A{source} \u2192 {target}",
  lintMergeComplete: "\u91CD\u8907\u9801\u9762\u5408\u4F75\u5B8C\u6210\u3002\u5DF2\u5408\u4F75 {merged}/{total} \u5C0D\u3002",
  // 摄入报告弹窗
  ingestReportTitle: "\u651D\u5165\u5831\u544A",
  ingestReportSourceFile: "\u539F\u59CB\u6A94",
  ingestReportCreated: "\u5DF2\u5EFA\u7ACB",
  ingestReportUpdated: "\u5DF2\u66F4\u65B0",
  ingestReportContradictions: "\u767C\u73FE\u77DB\u76FE",
  ingestReportFailedTitle: "\u672A\u80FD\u651D\u5165",
  ingestReportErrorDetail: "\u932F\u8AA4\u8A73\u60C5",
  ingestReportClose: "\u95DC\u9589",
  ingestReportCreatedPages: "\u5EFA\u7ACB\u9801\u9762\uFF1A{count}",
  ingestReportUpdatedPages: "\u66F4\u65B0\u9801\u9762\uFF1A{count}",
  ingestReportEntitiesCount: "{count} \u500B\u5BE6\u9AD4",
  ingestReportConceptsCount: "{count} \u500B\u6982\u5FF5",
  ingestReportContradictionsFound: "\u767C\u73FE\u77DB\u76FE\uFF1A{count}",
  ingestReportEntityType: "\u5BE6\u9AD4",
  ingestReportConceptType: "\u6982\u5FF5",
  timeMinutes: "\u5206",
  timeSeconds: "\u79D2",
  // 速率限制警告
  rateLimitDetected: "\u26A0\uFE0F \u6AA2\u6E2C\u5230\u901F\u7387\u9650\u5236\uFF1A{count} \u500B\u9801\u9762\u56E0 429 \u932F\u8AA4\u5931\u6557\u3002\u5EFA\u8B70\uFF1A(1) \u964D\u4F4E\u4F75\u767C\u5EA6\u81F3 {suggestedConcurrency} \u6216 1\uFF08\u5E8F\u5217\uFF09\uFF0C(2) \u589E\u5927\u6279\u6B21\u5EF6\u9072\u81F3 {suggestedDelay}ms\uFF0C(3) \u5207\u63DB\u5230\u901F\u7387\u9650\u5236\u66F4\u9AD8\u7684\u63D0\u4F9B\u5546\u3002",
  rateLimitDetectedShort: "\u26A0\uFE0F \u89F8\u767C\u901F\u7387\u9650\u5236 \u2014 \u5EFA\u8B70\u5728\u8A2D\u5B9A \u2192 \u651D\u5165\u52A0\u901F\u4E2D\u964D\u4F4E\u4F75\u767C\u5EA6\u6216\u589E\u5927\u6279\u6B21\u5EF6\u9072\u3002",
  // 长文本摄入提示
  sourceRejectedEmpty: '\u23ED\uFE0F "{filename}" \u6C92\u6709\u53EF\u63D0\u53D6\u7684\u5167\u5BB9\uFF0C\u5DF2\u8DF3\u904E\u3002\u7A7A\u7B46\u8A18\u6216\u50C5\u542B frontmatter \u7684\u7B46\u8A18\u4E0D\u6703\u751F\u6210 Wiki \u9801\u9762\u3002',
  sourceRejectedType: '\u23ED\uFE0F "{filename}" \u4E0D\u662F\u53D7\u652F\u63F4\u7684\u6A94\u6848\u578B\u5225\uFF0C\u5DF2\u8DF3\u904E\u3002\u50C5\u53EF\u63D0\u53D6\u6587\u5B57\u7B46\u8A18\uFF08\u5982 .md\u3001.txt\uFF09\u3002',
  sourceRejectedDuplicate: '\u23ED\uFE0F "{filename}" \u5DF2\u8DF3\u904E\u2014\u2014\u5176\u5167\u5BB9\u5DF2\u5B58\u5728\u65BC Wiki \u4E2D\u3002',
  ingestRejectedSummary: "\u5DF2\u8DF3\u904E {count} \u500B\u6A94\u6848\uFF08\u7A7A\u6A94\u6848\u3001\u91CD\u8907\u6216\u4E0D\u652F\u63F4\u7684\u578B\u5225\uFF09\u3002",
  reingestConfirmTitle: "\u91CD\u65B0\u63D0\u53D6\u8A72\u6A94\u6848\uFF1F",
  reingestConfirmBody: '"{filename}" \u7684\u5167\u5BB9\u5DF2\u5B58\u5728\u65BC Wiki \u4E2D\u3002\u4ECD\u8981\u91CD\u65B0\u63D0\u53D6\u55CE\uFF1F',
  reingestConfirmYes: "\u91CD\u65B0\u63D0\u53D6",
  reingestConfirmNo: "\u8DF3\u904E",
  longSourceNotice: '\u{1F4C4} "{filename}" \u5305\u542B {lines} \u884C\uFF08{size}\uFF09\u3002\u9577\u6587\u5B57\u9700\u8981\u591A\u8F2A\u8FED\u4EE3\u63D0\u53D6\uFF0CLLM \u5C07\u5B8C\u6574\u8B80\u53D6\u6587\u4EF6\u9032\u884C\u591A\u6B21\u5206\u6790\uFF0C\u8017\u6642\u53EF\u80FD\u8F03\u9577\uFF0C\u8ACB\u8010\u5FC3\u7B49\u5F85\u3002',
  longSourceNoticeShort: "\u{1F4C4} \u6AA2\u6E2C\u5230\u5927\u6A94\u6848\uFF08{lines} \u884C\uFF09\uFF0C\u651D\u5165\u53EF\u80FD\u9700\u8981\u8F03\u9577\u6642\u9593\u3002",
  // 摄入历史面板 (#122) — UI 文案
  historyButton: "\u64CD\u4F5C\u6B77\u53F2",
  historyButtonDesc: "\u6AA2\u8996 LLM-Wiki \u6700\u8FD1\u7684\u651D\u5165\u3001Lint \u5831\u544A\u548C\u5176\u4ED6\u8B8A\u66F4\u8A18\u9304",
  historyButtonOpen: "\u6AA2\u8996\u6B77\u53F2",
  historyModalTitle: "\u64CD\u4F5C\u6B77\u53F2",
  historyModalSubtitle: "LLM-Wiki \u6700\u8FD1\u7684\u8B8A\u66F4\u8A18\u9304",
  historyEmpty: "\u66AB\u7121\u64CD\u4F5C\u8A18\u9304\u3002\u651D\u5165\u7B46\u8A18\u6216\u57F7\u884C Lint \u4EE5\u586B\u5145\u6B64\u5217\u8868\u3002",
  historyReadError: "\u7121\u6CD5\u8B80\u53D6\u64CD\u4F5C\u65E5\u8A8C\uFF1A{error}",
  historyEntryKindIngest: "\u651D\u5165",
  historyEntryKindMaintenance: "\u7DAD\u8B77",
  historyEntryKindFix: "\u4FEE\u5FA9",
  historyEntryKindOther: "\u5176\u4ED6",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "\u4F86\u6E90\uFF1A{source}",
  historyEntrySourceUnknown: "\u672A\u547D\u540D\u651D\u5165",
  historyEntryCreatedLabel: "\u5DF2\u5EFA\u7ACB",
  historyEntryUpdatedLabel: "\u5DF2\u66F4\u65B0",
  historyEntryCreatedCount: "\u65B0\u5EFA {count} \u500B",
  historyEntryUpdatedCount: "\u66F4\u65B0 {count} \u500B",
  historyEntryNoChanges: "\u7121\u9801\u9762\u8B8A\u66F4",
  historyEntryContradictions: "\u26A0\uFE0F {count} \u8655\u77DB\u76FE",
  historyEntrySectionCreated: "\u5EFA\u7ACB\u7684\u9801\u9762",
  historyEntrySectionUpdated: "\u66F4\u65B0\u7684\u9801\u9762",
  historyEntrySectionContradictions: "\u77DB\u76FE",
  historyEntrySectionDetails: "\u8A73\u60C5",
  historyEntryOpenPage: "\u958B\u555F",
  historyEntryDetailsNoContradictions: "\u672A\u767C\u73FE\u77DB\u76FE",
  historySearchPlaceholder: "\u{1F50D} \u6309\u4F86\u6E90\u6A19\u984C\u6216\u9801\u9762\u8DEF\u5F91\u641C\u5C0B\u2026",
  historyFilterAll: "\u5168\u90E8",
  historyFilterIngest: "\u50C5\u651D\u5165",
  historyFilterMaintenance: "\u50C5\u7DAD\u8B77\u5831\u544A",
  historyFilterFix: "\u50C5\u4FEE\u5FA9",
  historyFilterContradictions: "\u26A0\uFE0F \u542B\u77DB\u76FE",
  historyRefreshButton: "\u27F3 \u91CD\u65B0\u6574\u7406",
  historyExpandDay: "\u9EDE\u9078\u5C55\u958B",
  historyCollapseDay: "\u9EDE\u9078\u6536\u8D77",
  historyShowMore: "\u986F\u793A\u66F4\u65E9\u7684 {count} \u689D",
  historyNoMatch: "\u6C92\u6709\u64CD\u4F5C\u5339\u914D\u60A8\u7684\u641C\u5C0B/\u7BE9\u9078\u689D\u4EF6\u3002",
  historyCloseButton: "\u95DC\u9589",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "\u5831\u544A\u767C\u73FE",
  historyKpiPages: "\u9801\u9762\u6578",
  historyKpiDeadLinks: "\u65B7\u93C8",
  historyKpiOrphans: "\u5B64\u7ACB",
  historyKpiEmpty: "\u7A7A\u6D1E",
  historyKpiDuplicates: "\u91CD\u8907",
  historyKpiTagViolations: "\u6A19\u7C64\u554F\u984C",
  historyKpiUnsourced: "\u7121\u4F86\u6E90",
  historyKpiDuration: "Lint \u8017\u6642",
  historyKpiDurationSec: "{seconds} \u79D2",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "\u65B7\u93C8 ({count})",
  historySectionTagViolations: "\u6A19\u7C64\u8D8A\u754C ({count})",
  historySectionOrphans: "\u5B64\u7ACB\u9801\u9762 ({count})",
  historySectionEmptyPages: "\u7A7A\u6D1E\u9801\u9762 ({count})",
  historySectionLlmAnalysis: "LLM \u5206\u6790 ({count})",
  historyDeadLinkSource: "\u4F86\u6E90",
  historyDeadLinkTarget: "\u7F3A\u5931",
  historyOpenInLog: "\u5728 log.md \u4E2D\u6AA2\u8996",
  historyShowMoreItems: "\u5C55\u958B\u5269\u9918 {count} \u9805",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 \u6301\u5E73",
  historyChipContradiction: "\u77DB\u76FE",
  historyChipOutdated: "\u904E\u6642",
  historyChipMissing: "\u7F3A\u5931",
  historyChipStructure: "\u7D50\u69CB",
  historySeverityHigh: "\u9AD8",
  historySeverityMedium: "\u4E2D",
  historySeverityLow: "\u4F4E",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "\u60A8\u7684 Wiki \u7576\u524D\u6709 {dead} \u500B\u65B7\u93C8\u3001{orphans} \u500B\u5B64\u7ACB\u9801\u9762\u3001{tags} \u500B\u6A19\u7C64\u554F\u984C\u3002\u6700\u8FD1\u4E00\u6B21 Lint \u8017\u6642 {duration}\u3002",
  historyGlobalInsightClean: "\u60A8\u7684 Wiki \u7576\u524D\u72C0\u614B\u826F\u597D\u2014\u2014\u6C92\u6709\u65B7\u93C8\u3001\u5B64\u7ACB\u9801\u9762\u6216\u6A19\u7C64\u554F\u984C\u3002",
  historyGlobalInsightNoData: "\u66AB\u7121\u7DAD\u8B77\u5831\u544A\u2014\u2014\u57F7\u884C Lint \u4EE5\u751F\u6210\u3002",
  historyModalHeaderTitle: "\u64CD\u4F5C\u6B77\u53F2",
  historyModalSubtitleWithCount: "LLM-Wiki \u6700\u8FD1\u7684\u8B8A\u66F4\u8A18\u9304 \xB7 \u5171 {count} \u689D",
  historyTimeRangeAll: "\u5168\u90E8\u6642\u9593",
  historyTimeRange1d: "\u8FD1 1 \u5929",
  historyTimeRange3d: "\u8FD1 3 \u5929",
  historyTimeRange1w: "\u8FD1 1 \u5468",
  historyTimeRange1m: "\u8FD1 1 \u500B\u6708",
  historyIngestTotal: "\u9801\u9762\u7E3D\u6578",
  historyIngestByType: "\u6309\u578B\u5225\u5206\u4F48",
  historyIngestSource: "\u4F86\u539F\u59CB\u6A94",
  historyIngestNoTimestamp: "\u7121\u6642\u9593\u6233",
  historyIngestFirstTime: "\u9996\u6B21\u651D\u5165",
  historyIngestLatestTime: "\u6700\u8FD1\u651D\u5165",
  historyTimeRangeCustom: "\u81EA\u5B9A\u7FA9\u7BC4\u570D",
  historyCustomRangeFrom: "\u8D77\u59CB",
  historyCustomRangeTo: "\u7D50\u675F",
  historyCustomRangeApply: "\u61C9\u7528",
  historyCustomRangeClear: "\u6E05\u9664"
};

// src/texts/ja.ts
var JA_TEXTS = {
  // Plugin Info
  pluginTitle: "Karpathy LLM Wiki \u8A2D\u5B9A",
  pluginIntro: "\u672C\u30D7\u30E9\u30B0\u30A4\u30F3\u306FObsidian\u7528\u306B{{link}}\u306E\u6982\u5FF5\u3092\u5B9F\u88C5\u3057\u3066\u3044\u307E\u3059\u3002\u30CE\u30FC\u30C8\u3092\u8AAD\u307F\u8FBC\u307F\u3001AI\u3067\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u3068\u6982\u5FF5\u3092\u62BD\u51FA\u3057\u3001Vault\u5185\u306B\u69CB\u9020\u5316\u3055\u308C\u305F\u76F8\u4E92\u30EA\u30F3\u30AFWiki\u3092\u69CB\u7BC9\u3057\u307E\u3059\u3002",
  karpathyLinkText: "Andrej Karpathy\u306ELLM Wiki",
  // Features Section
  featuresTitle: "\u4ED5\u7D44\u307F",
  workflow1Title: "1. \u53D6\u308A\u8FBC\u307F",
  workflow1Desc: "\u30CE\u30FC\u30C8\u3092\u9078\u629E \u2014 AI\u304C\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u3001\u6982\u5FF5\u3001\u95A2\u4FC2\u3092\u62BD\u51FA\u3057\u3066Wiki\u30DA\u30FC\u30B8\u306B\u307E\u3068\u3081\u307E\u3059\u3002",
  workflow2Title: "2. \u554F\u3044\u5408\u308F\u305B",
  workflow2Desc: "Wiki\u3068\u30C1\u30E3\u30C3\u30C8 \u2014 \u56DE\u7B54\u306F\u53D6\u308A\u8FBC\u3093\u3060\u77E5\u8B58\u306B\u57FA\u3065\u3044\u3066\u3044\u307E\u3059\u3002",
  workflow3Title: "3. \u7DAD\u6301\u7BA1\u7406",
  workflow3Desc: "\u81EA\u52D5\u751F\u6210\u306E\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u3001\u76F8\u4E92\u30EA\u30F3\u30AF\u3001Lint\u30C1\u30A7\u30C3\u30AF\u3067Wiki\u306E\u5065\u5168\u6027\u3092\u4FDD\u3061\u307E\u3059\u3002",
  // Language Switcher
  languageTitle: "\u30A4\u30F3\u30BF\u30FC\u30D5\u30A7\u30FC\u30B9\u8A00\u8A9E",
  languageDesc: "\u8A2D\u5B9A\u30D1\u30CD\u30EB\u306E\u8868\u793A\u8A00\u8A9E\u3092\u9078\u629E\u3057\u3066\u304F\u3060\u3055\u3044\u3002\u8A00\u8A9E\u5909\u66F4\u3092\u30B3\u30DE\u30F3\u30C9\u30D1\u30EC\u30C3\u30C8\u306B\u9069\u7528\u3059\u308B\u306B\u306F\u3001\u30D7\u30E9\u30B0\u30A4\u30F3\u3092\u518D\u8D77\u52D5\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "LLM Client \u306E\u72B6\u614B",
  llmWikiStatusSection: "LLM-Wiki \u72B6\u614B",
  statusInitialized: "\u521D\u671F\u5316\u6E08\u307F",
  statusReady: "LLM \u6E96\u5099\u5B8C\u4E86",
  statusNotReady: "LLM \u672A\u8A2D\u5B9A \u2014 \u4E0A\u8A18\u306E\u8A2D\u5B9A\u3068\u63A5\u7D9A\u30C6\u30B9\u30C8\u3092\u5B8C\u4E86\u3057\u3066\u304F\u3060\u3055\u3044",
  statusNotInitialized: "\u672A\u521D\u671F\u5316",
  currentProvider: "\u73FE\u5728\u306E\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC",
  // Provider Configuration
  providerSection: "LLM \u8A2D\u5B9A",
  providerName: "LLM \u30D7\u30ED\u30D0\u30A4\u30C0\u30FC",
  providerDesc: "\u5B9A\u7FA9\u6E08\u307F\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u307E\u305F\u306F\u30AB\u30B9\u30BF\u30E0OpenAI\u4E92\u63DB\u30B5\u30FC\u30D3\u30B9\u3092\u9078\u629E\u3002Coding Plan\u7B49\u306E\u30D0\u30F3\u30C9\u30EB\u306E\u5834\u5408\u3001\u30AB\u30B9\u30BF\u30E0OpenAI/Anthropic\u3092\u9078\u629E\u3057\u3001\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306EBase URL\u3068API Key\u3092\u624B\u52D5\u5165\u529B",
  // API Key
  apiKeyName: "API Key",
  apiKeyDesc: "\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u304B\u3089\u767A\u884C\u3055\u308C\u305FAPI Key\u3092\u5165\u529B\u3057\u3066\u304F\u3060\u3055\u3044",
  apiKeyPlaceholder: "API Key\u3092\u5165\u529B",
  // Base URL
  baseUrlName: "API Base URL",
  baseUrlDescCustom: "\u5FC5\u9808\uFF1A\u30AB\u30B9\u30BF\u30E0OpenAI\u4E92\u63DB\u30A8\u30F3\u30C9\u30DD\u30A4\u30F3\u30C8",
  baseUrlDescOverride: "\u4EFB\u610F\uFF1A\u5B9A\u7FA9\u6E08\u307FBase URL\u3092\u4E0A\u66F8\u304D",
  // Ollama Hint
  ollamaHint: "Ollama\u306F\u30ED\u30FC\u30AB\u30EB\u3067\u52D5\u4F5C\u3059\u308B\u305F\u3081\u3001API Key\u306F\u4E0D\u8981\u3067\u3059",
  lmstudioHint: "LM Studio\u306F\u30ED\u30FC\u30AB\u30EB\u3067\u52D5\u4F5C\u3057\u307E\u3059\u3002API Key\u306F\u4EFB\u610F\u3067\u3059",
  maxTokensPerCallName: "\u30B3\u30F3\u30C6\u30AD\u30B9\u30C8\u30A6\u30A3\u30F3\u30C9\u30A6",
  maxTokensPerCallDesc: "\u751F\u6210Token\u3092\u30E2\u30C7\u30EB\u306E\u30B3\u30F3\u30C6\u30AD\u30B9\u30C8\u30A6\u30A3\u30F3\u30C9\u30A6\u306B\u5236\u9650\u30020 = \u7121\u5236\u9650\uFF08\u30AF\u30E9\u30A6\u30C9\u30C7\u30D5\u30A9\u30EB\u30C8\uFF09\u3002",
  slugCaseName: "\u30D5\u30A1\u30A4\u30EB\u540D\u306E\u5927\u6587\u5B57\u5C0F\u6587\u5B57",
  slugCaseDesc: "\u751F\u6210\u3055\u308C\u308B Wiki \u30D5\u30A1\u30A4\u30EB\u540D\u3092\u5C0F\u6587\u5B57\u5316\u3059\u308B\u304B\u3069\u3046\u304B\u3092\u5236\u5FA1\u3057\u307E\u3059\u3002\u5927\u6587\u5B57\u5C0F\u6587\u5B57\u306B\u610F\u5473\u306E\u3042\u308B\u8A00\u8A9E\uFF08\u30C9\u30A4\u30C4\u8A9E\u306E\u540D\u8A5E\u306A\u3069\uFF09\u3067\u306F\u300C\u4FDD\u6301\u300D\u3092\u63A8\u5968\u3002",
  slugCaseLower: "\u5C0F\u6587\u5B57\uFF08\u30C7\u30D5\u30A9\u30EB\u30C8\uFF09",
  slugCasePreserve: "\u5927\u6587\u5B57\u5C0F\u6587\u5B57\u3092\u4FDD\u6301",
  // Model Selection
  modelSection: "\u30E2\u30C7\u30EB\u9078\u629E",
  fetchModelsName: "\u5229\u7528\u53EF\u80FD\u306A\u30E2\u30C7\u30EB\u3092\u53D6\u5F97",
  fetchModelsDesc: "\u30D7\u30ED\u30D0\u30A4\u30C0\u30FCAPI\u304B\u3089\u6700\u65B0\u306E\u30E2\u30C7\u30EB\u4E00\u89A7\u3092\u53D6\u5F97\u3057\u307E\u3059",
  fetchModelsButton: "\u30E2\u30C7\u30EB\u3092\u53D6\u5F97",
  fetchingModels: "\u53D6\u5F97\u4E2D...",
  fetchSuccess: "\u53D6\u5F97\u6210\u529F\uFF01{}\u4EF6\u306E\u30E2\u30C7\u30EB\u304C\u5229\u7528\u53EF\u80FD\u3067\u3059",
  fetchFailed: "\u53D6\u5F97\u306B\u5931\u6557\u3057\u305F\u304B\u30EA\u30B9\u30C8\u304C\u7A7A\u3067\u3059\u3002\u30E2\u30C7\u30EB\u540D\u3092\u624B\u52D5\u3067\u5165\u529B\u3057\u3066\u304F\u3060\u3055\u3044",
  fetchNotSupported: "\u3053\u306E\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306F\u30E2\u30C7\u30EB\u4E00\u89A7\u306E\u53D6\u5F97\u306B\u5BFE\u5FDC\u3057\u3066\u3044\u307E\u305B\u3093",
  fetchErrorAuth: "\u8A8D\u8A3C\u306B\u5931\u6557\u3057\u307E\u3057\u305F\uFF08HTTP 401/403\uFF09\u3002API \u30AD\u30FC\u3092\u78BA\u8A8D\u3059\u308B\u304B\u3001\u30E2\u30C7\u30EB ID \u3092\u76F4\u63A5\u5165\u529B\u3057\u3066\u300C\u30C6\u30B9\u30C8\u63A5\u7D9A\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u691C\u8A3C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  fetchErrorEndpoint: "\u30A8\u30F3\u30C9\u30DD\u30A4\u30F3\u30C8\u304C\u898B\u3064\u304B\u308A\u307E\u305B\u3093\uFF08HTTP 404\uFF09\u3002BaseURL \u3092\u78BA\u8A8D\u3059\u308B\u304B\u3001\u30E2\u30C7\u30EB ID \u3092\u76F4\u63A5\u5165\u529B\u3057\u3066\u300C\u30C6\u30B9\u30C8\u63A5\u7D9A\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u691C\u8A3C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  fetchErrorServer: "\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u30B5\u30FC\u30D0\u30FC\u30A8\u30E9\u30FC\uFF08HTTP 5xx\uFF09\u3002\u5F8C\u3067\u3082\u3046\u4E00\u5EA6\u304A\u8A66\u3057\u3044\u305F\u3060\u304F\u304B\u3001\u30E2\u30C7\u30EB ID \u3092\u76F4\u63A5\u5165\u529B\u3057\u3066\u300C\u30C6\u30B9\u30C8\u63A5\u7D9A\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u691C\u8A3C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  fetchErrorEmpty: "\u3053\u306E\u30A8\u30F3\u30C9\u30DD\u30A4\u30F3\u30C8\u306F\u30E2\u30C7\u30EB\u4E00\u89A7\u3092\u63D0\u4F9B\u3057\u3066\u3044\u307E\u305B\u3093\u3002\u30E2\u30C7\u30EB ID \u3092\u76F4\u63A5\u5165\u529B\u3057\u3066\u300C\u30C6\u30B9\u30C8\u63A5\u7D9A\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u691C\u8A3C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  fetchErrorNetwork: "\u30CD\u30C3\u30C8\u30EF\u30FC\u30AF\u30EA\u30AF\u30A8\u30B9\u30C8\u304C\u5931\u6557\u3057\u307E\u3057\u305F\u3002\u30A4\u30F3\u30BF\u30FC\u30CD\u30C3\u30C8\u63A5\u7D9A\u3001BaseURL\u3001\u307E\u305F\u306F\u30D7\u30ED\u30AD\u30B7\u8A2D\u5B9A\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044\u3002\u30E2\u30C7\u30EB ID \u3092\u76F4\u63A5\u5165\u529B\u3057\u3066\u300C\u30C6\u30B9\u30C8\u63A5\u7D9A\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u691C\u8A3C\u3059\u308B\u3053\u3068\u3082\u3067\u304D\u307E\u3059\u3002",
  selectModelName: "\u30E2\u30C7\u30EB\u3092\u9078\u629E",
  selectModelDesc: "{}\u4EF6\u306E\u5229\u7528\u53EF\u80FD\u306A\u30E2\u30C7\u30EB\u304B\u3089\u9078\u629E",
  customInputOption: "\u30AB\u30B9\u30BF\u30E0\u5165\u529B...",
  customInputHint: "\u4ED6\u306E\u30E2\u30C7\u30EB\u3092\u4F7F\u7528\u3059\u308B\u306B\u306F\u300C\u30AB\u30B9\u30BF\u30E0\u5165\u529B...\u300D\u3092\u9078\u629E\u3057\u3066\u304F\u3060\u3055\u3044",
  modelName: "\u30E2\u30C7\u30EB\u540D",
  modelDescCustom: "\u30AB\u30B9\u30BF\u30E0\u30E2\u30C7\u30EB\u3092\u4F7F\u7528\u4E2D\uFF08\u4E0A\u306E\u30DC\u30BF\u30F3\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u4E00\u89A7\u3092\u518D\u53D6\u5F97\uFF09",
  modelDescFetchFailed: "\u30E2\u30C7\u30EB\u4E00\u89A7\u306E\u53D6\u5F97\u306B\u5931\u6557\u3057\u307E\u3057\u305F\u3002API \u30AD\u30FC\u3068\u30A8\u30F3\u30C9\u30DD\u30A4\u30F3\u30C8 URL \u3092\u78BA\u8A8D\u3059\u308B\u304B\u3001\u30E2\u30C7\u30EB ID \u3092\u76F4\u63A5\u5165\u529B\u3057\u3066\u300C\u30C6\u30B9\u30C8\u63A5\u7D9A\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066\u691C\u8A3C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  modelInputPlaceholder: "\u30E2\u30C7\u30EB ID \u3092\u5165\u529B\u3057\u3066\u30C6\u30B9\u30C8\u63A5\u7D9A",
  switchToDropdown: "\u30C9\u30ED\u30C3\u30D7\u30C0\u30A6\u30F3\u9078\u629E\u306B\u5207\u308A\u66FF\u3048",
  useDropdownButton: "\u30C9\u30ED\u30C3\u30D7\u30C0\u30A6\u30F3\u3092\u4F7F\u7528",
  // Test & Save
  testConnectionName: "\u63A5\u7D9A\u30C6\u30B9\u30C8",
  testConnectionDesc: "\u8A2D\u5B9A\u304C\u6B63\u5E38\u306BLLM API\u3092\u547C\u3073\u51FA\u305B\u308B\u304B\u78BA\u8A8D\u3057\u307E\u3059",
  testButton: "\u63A5\u7D9A\u30C6\u30B9\u30C8",
  testing: "\u30C6\u30B9\u30C8\u4E2D...",
  saveSettingsName: "\u8A2D\u5B9A\u3092\u4FDD\u5B58",
  saveSettingsDesc: "\u73FE\u5728\u306E\u8A2D\u5B9A\u3092\u4FDD\u5B58\u3057\u307E\u3059",
  saveButton: "\u8A2D\u5B9A\u3092\u4FDD\u5B58",
  testConnectionSuccessful: "\u63A5\u7D9A\u6210\u529F",
  testConnectionFailed: "\u63A5\u7D9A\u5931\u6557",
  testConnectionProvider: "\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\uFF1A",
  errorUnknown: "\u4E0D\u660E\u306A\u30A8\u30E9\u30FC",
  savedNotice: "\u8A2D\u5B9A\u3092\u4FDD\u5B58\u3057\u307E\u3057\u305F\uFF01",
  // Issue #137: LLM フォールバック通知
  fallbackThinkingDialect: "\u601D\u8003\u5236\u5FA1\uFF1A\u300C{dialect}\u300D\u65B9\u8A00\u306B\u5207\u308A\u66FF\u3048\u307E\u3057\u305F\uFF08\u3053\u306E\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306F\u5225\u306E\u601D\u8003\u5236\u5FA1\u30D5\u30A3\u30FC\u30EB\u30C9\u5F62\u5F0F\u3092\u4F7F\u7528\u3057\u307E\u3059\uFF09\u3002\u51FA\u529B\u306F\u5909\u308F\u308A\u307E\u305B\u3093\u3002",
  fallbackThinkingNone: "\u3053\u306E\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u3067\u306F\u601D\u8003\u5236\u5FA1\u3092\u5B8C\u5168\u306B\u7121\u52B9\u5316\u3057\u307E\u3057\u305F\u3002\u63A8\u8AD6\u30B3\u30F3\u30C6\u30F3\u30C4\u304C\u5F15\u304D\u7D9A\u304D\u8868\u793A\u3055\u308C\u308B\u5834\u5408\u304C\u3042\u308A\u307E\u3059\u3002\u305D\u306E\u5834\u5408\u306F\u5225\u306E\u30E2\u30C7\u30EB\u3092\u8A66\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  fallbackParamStripped: "\u30D1\u30E9\u30E1\u30FC\u30BF\u300C{field}\u300D\u306F\u3053\u306E\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u3067\u30B5\u30DD\u30FC\u30C8\u3055\u308C\u3066\u3044\u307E\u305B\u3093\u3002\u30EA\u30AF\u30A8\u30B9\u30C8\u304B\u3089\u524A\u9664\u3055\u308C\u307E\u3057\u305F\u3002\u52D5\u4F5C\u306F\u8A2D\u5B9A\u3068\u7570\u306A\u308B\u5834\u5408\u304C\u3042\u308A\u307E\u3059\u3002",
  // Wiki Folder
  wikiSection: "Wiki\u8A2D\u5B9A",
  wikiFolderName: "Wiki \u30D5\u30A9\u30EB\u30C0\u30FC",
  wikiFolderDesc: "\u751F\u6210\u3055\u308C\u305FWiki\u30DA\u30FC\u30B8\u306E\u4FDD\u5B58\u5148",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "\u307E\u305AAPI Key\u3092\u8A2D\u5B9A\u3057\u3066\u304F\u3060\u3055\u3044",
  llmNotReady: "LLM\u304C\u8A2D\u5B9A\u3055\u308C\u3066\u3044\u307E\u305B\u3093\u3002\u8A2D\u5B9A \u2192 Karpathy LLM Wiki \u3067\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u3092\u8A2D\u5B9A\u3057\u3001\u30E2\u30C7\u30EB\u3092\u53D6\u5F97\u3057\u3066\u63A5\u7D9A\u30C6\u30B9\u30C8\u306B\u5408\u683C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  errorFetchFailed: "\u5931\u6557\uFF1A{}",
  // Query Settings
  querySectionTitle: "Wiki \u554F\u3044\u5408\u308F\u305B\u8A2D\u5B9A",
  maxConversationHistoryName: "\u4F1A\u8A71\u5C65\u6B74\u306E\u6700\u5927\u6570",
  maxConversationHistoryDesc: "\u30C8\u30FC\u30AF\u30F3\u30AA\u30FC\u30D0\u30FC\u30D5\u30ED\u30FC\u3092\u9632\u3050\u305F\u3081\u4F1A\u8A71\u30E1\u30C3\u30BB\u30FC\u30B8\u6570\u3092\u5236\u9650\u3057\u307E\u3059",
  maxConversationHistoryHint: "\u63A8\u5968\uFF1A50\u30E9\u30A6\u30F3\u30C9\u4EE5\u5185",
  numberRangeValidation: "1\u301C50\u306E\u6570\u5024\u3092\u5165\u529B\u3057\u3066\u304F\u3060\u3055\u3044",
  numberRangeClamped: "\u5024\u304C\u7BC4\u56F2\u5916\u3067\u3059\uFF081-500\uFF09\u3001\u81EA\u52D5\u7684\u306B {} \u306B\u8A2D\u5B9A\u3055\u308C\u307E\u3057\u305F",
  // Query Modal UI
  queryModalTitle: "Query Wiki - \u5BFE\u8A71\u578B\u554F\u3044\u5408\u308F\u305B",
  queryModalPlaceholder: "\u8CEA\u554F\u3092\u5165\u529B...",
  queryModalSendButton: "\u9001\u4FE1",
  queryModalStopButton: "\u505C\u6B62",
  queryModalSaveButton: "Wiki\u306B\u4FDD\u5B58",
  queryModalClearButton: "\u5C65\u6B74\u3092\u30AF\u30EA\u30A2",
  queryModalHistoryCount: "\u4F1A\u8A71\u5C65\u6B74\uFF1A{}/{} \u30E9\u30A6\u30F3\u30C9",
  queryModalStreaming: "\u30B9\u30C8\u30EA\u30FC\u30DF\u30F3\u30B0\u4E2D...",
  queryModalFallbackStreaming: "\u30B9\u30C8\u30EA\u30FC\u30DF\u30F3\u30B0\u306B\u5BFE\u5FDC\u3057\u3066\u3044\u306A\u3044\u305F\u3081\u3001\u975E\u30B9\u30C8\u30EA\u30FC\u30DF\u30F3\u30B0\u306B\u5207\u308A\u66FF\u3048\u307E\u3057\u305F\u3002\u304A\u5F85\u3061\u304F\u3060\u3055\u3044...",
  queryPhaseSearching: "Wiki\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u3092\u5206\u6790\u3057\u3001\u95A2\u9023\u30DA\u30FC\u30B8\u3092\u9078\u5225\u4E2D...",
  queryPhaseFoundPages: "{count}\u4EF6\u898B\u3064\u304B\u308A\u307E\u3057\u305F\uFF1A{pages}",
  queryPhaseLoadingPages: "\u30DA\u30FC\u30B8\u30B3\u30F3\u30C6\u30F3\u30C4\u3092\u8AAD\u307F\u8FBC\u307F\u4E2D...",
  queryPhaseContextReady: "\u30B3\u30F3\u30C6\u30AD\u30B9\u30C8\u6E96\u5099\u5B8C\u4E86\u3002\u56DE\u7B54\u3092\u751F\u6210\u4E2D...",
  queryPhaseGenerating: "\u751F\u6210\u4E2D...\uFF08\u7D4C\u904E {time}\u79D2\uFF09",
  queryPhaseNonStreaming: "\u975E\u30B9\u30C8\u30EA\u30FC\u30DF\u30F3\u30B0\u30E2\u30FC\u30C9\u3001\u56DE\u7B54\u751F\u6210\u4E2D...\uFF08\u7D4C\u904E {time}\u79D2\uFF09",
  queryModalErrorPrefix: "\u30A8\u30E9\u30FC\uFF1A",
  queryModalHint: "\u554F\u3044\u5408\u308F\u305B\u306FWiki\u306E\u5185\u5BB9\u306B\u57FA\u3065\u304D\u307E\u3059\u3002\u300CWiki\u306B\u4FDD\u5B58\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3059\u308B\u3068\u3001\u4FA1\u5024\u306E\u3042\u308B\u4F1A\u8A71\u3092Wiki\u30DA\u30FC\u30B8\u3068\u3057\u3066\u62BD\u51FA\u3067\u304D\u307E\u3059\u3002",
  // v1.20.0: Query Wiki 思考ブロック折り畳み見出し
  queryThinkingSummary: "\u601D\u8003\u30D7\u30ED\u30BB\u30B9",
  queryThinkingSteps: "\u30B9\u30C6\u30C3\u30D7",
  // Error Messages
  errorLLMClientNotInit: "LLM Client\u304C\u521D\u671F\u5316\u3055\u308C\u3066\u3044\u307E\u305B\u3093\u3002\u8A2D\u5B9A\u3092\u4FDD\u5B58\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  errorIngestFailed: "\u53D6\u308A\u8FBC\u307F\u5931\u6557\uFF1A",
  errorQueryFailed: "\u554F\u3044\u5408\u308F\u305B\u5931\u6557\uFF1A",
  fileWriteFailed: "\u30D5\u30A1\u30A4\u30EB\u3092\u4F5C\u6210\u307E\u305F\u306F\u66F4\u65B0\u3067\u304D\u307E\u305B\u3093\uFF1A{path}",
  // Success Messages
  ingestSuccess: "\u53D6\u308A\u8FBC\u307F\u6210\u529F\uFF1A{}\u30DA\u30FC\u30B8\u4F5C\u6210\u3001{}\u30DA\u30FC\u30B8\u66F4\u65B0",
  querySuccess: "\u554F\u3044\u5408\u308F\u305B\u5B8C\u4E86",
  lintSuccess: "Wiki Lint\u5B8C\u4E86",
  lintReadingPages: "{count}\u4EF6\u306EWiki\u30DA\u30FC\u30B8\u3092\u8AAD\u307F\u8FBC\u307F\u4E2D...",
  lintReadingPagesProgress: "Wiki\u30DA\u30FC\u30B8\u8AAD\u307F\u8FBC\u307F\u4E2D\uFF1A{current}/{total}...",
  lintScanningLinks: "\u30EA\u30F3\u30AF\u5207\u308C\u3092\u30B9\u30AD\u30E3\u30F3\u4E2D...",
  lintScanningLinksProgress: "\u30EA\u30F3\u30AF\u5207\u308C\u30B9\u30AD\u30E3\u30F3\u4E2D\uFF1A{current}/{total}...",
  lintCheckingDuplicates: "\u91CD\u8907\u30DA\u30FC\u30B8\u3092\u78BA\u8A8D\u4E2D...",
  lintCheckingDuplicatesProgress: "\u91CD\u8907\u30DA\u30FC\u30B8\u3092\u691C\u8A3C\u4E2D\uFF1A\u30D0\u30C3\u30C1 {current} ...",
  lintFixingPolluted: "\u6C5A\u67D3\u30DA\u30FC\u30B8\u3092\u4FEE\u6B63\u4E2D {current}/{total}\uFF1A{title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} \u6C5A\u67D3\u30DA\u30FC\u30B8\u3092\u4FEE\u6B63\uFF08{count}\uFF09",
  lintDuplicateCheckFailed: "\u91CD\u8907\u691C\u51FA\u306B\u5931\u6557\u3057\u307E\u3057\u305F \u2014 \u30B3\u30F3\u30BD\u30FC\u30EB\u3067\u8A73\u7D30\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044",
  lintDuplicateCheckFailedDetail: "\u91CD\u8907\u30C1\u30A7\u30C3\u30AF\u304C{step}\u3067\u5931\u6557\uFF1A{error}",
  lintMergeItemFailed: "\u30DE\u30FC\u30B8\u5931\u6557\uFF1A{source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "\u5225\u540D\u672A\u8A2D\u5B9A\uFF1A{count}\u4EF6\u306E\u30DA\u30FC\u30B8\u306B\u5225\u540D\u304C\u3042\u308A\u307E\u305B\u3093",
  lintAliasesSection: "\u30A8\u30A4\u30EA\u30A2\u30B9\u6B20\u843D\u30DA\u30FC\u30B8 [{count} \u4EF6]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "\u5225\u540D\u3092\u88DC\u5B8C\uFF08{count}\u4EF6\uFF09",
  lintAliasesFilling: "\u5225\u540D\u751F\u6210\u4E2D {current}/{total}\uFF1A{page}",
  lintAliasesFilled: "\u5225\u540D\u88DC\u5B8C\u5B8C\u4E86\u3002{filled}/{total}\u30DA\u30FC\u30B8\u306B\u5165\u529B\u3057\u307E\u3057\u305F\u3002",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "\u8A9E\u5F59\u5916\u30BF\u30B0\uFF08\u691C\u51FA\uFF09[{count} \u4EF6]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "\u5225\u540D\u751F\u6210\u306B\u5931\u6557\uFF1A{page} \u2014 {error}",
  lintFixItemFailed: "\u4FEE\u5FA9\u5931\u6557\uFF1A[[{target}]] \u2014 {error}",
  lintLinkItemFailed: "\u30EA\u30F3\u30AF\u8A2D\u5B9A\u5931\u6557\uFF1A{page} \u2014 {error}",
  lintRetrying: "\u30A8\u30E9\u30FC\u5F8C\u518D\u8A66\u884C\u4E2D\uFF08{attempt}/{max}\uFF09...",
  lintAnalyzingLLM: "LLM\u304CWiki\u306E\u5065\u5168\u6027\u3092\u5206\u6790\u4E2D...",
  saveToWikiSuccess: "\u4F1A\u8A71\u3092Wiki\u306B\u4FDD\u5B58\u3057\u307E\u3057\u305F\uFF01",
  saveSummary: "{entities} \u30A8\u30F3\u30C6\u30A3\u30C6\u30A3, {concepts} \u30B3\u30F3\u30BB\u30D7\u30C8, {pages} \u30DA\u30FC\u30B8",
  aliasAdded: "\u30DA\u30FC\u30B8\u300C{page}\u300D\u306B\u5225\u540D\u300C{alias}\u300D\u3092\u8FFD\u52A0\u3057\u307E\u3057\u305F",
  // Status Messages
  ingestingSources: "\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u3092\u53D6\u308A\u8FBC\u307F\u4E2D...",
  queryingWiki: "Wiki\u306B\u554F\u3044\u5408\u308F\u305B\u4E2D...",
  lintingWiki: "Wiki\u3092Lint\u4E2D...",
  savingToWiki: "\u4F1A\u8A71\u3092Wiki\u306B\u4FDD\u5B58\u4E2D...",
  clearingHistory: "\u4F1A\u8A71\u5C65\u6B74\u3092\u30AF\u30EA\u30A2\u4E2D...",
  // Buttons
  ingestButton: "\u53D6\u308A\u8FBC\u307F",
  queryButton: "\u554F\u3044\u5408\u308F\u305B",
  lintButton: "Lint",
  cancelButton: "\u30AD\u30E3\u30F3\u30BB\u30EB",
  // Links
  karpathyGistLink: "Karpathy\u306ELLM Wiki Gist",
  obsidianPluginAPI: "Obsidian Plugin API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // Other
  availableModelsLoading: "\u5229\u7528\u53EF\u80FD\u306A\u30E2\u30C7\u30EB\u3092\u8AAD\u307F\u8FBC\u307F\u4E2D...",
  noModelsAvailable: "\u5229\u7528\u53EF\u80FD\u306A\u30E2\u30C7\u30EB\u304C\u3042\u308A\u307E\u305B\u3093",
  // LLM Language Hint
  llmLanguageHint: "\u65E5\u672C\u8A9E\u3067\u56DE\u7B54\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  // Schema Configuration
  schemaSection: "\u30B9\u30AD\u30FC\u30DE\u8A2D\u5B9A",
  enableSchemaName: "\u30B9\u30AD\u30FC\u30DE\u3092\u6709\u52B9\u5316",
  enableSchemaDesc: "schema/config.md\u3092\u751F\u6210\u3057\u3001\u3059\u3079\u3066\u306ELLM\u30D7\u30ED\u30F3\u30D7\u30C8\u306B\u6CE8\u5165\u3057\u3066\u69CB\u9020\u5316\u3055\u308C\u305FWiki\u51FA\u529B\u3092\u5B9F\u73FE\u3057\u307E\u3059",
  viewSchemaButton: "\u30B9\u30AD\u30FC\u30DE\u306E\u95B2\u89A7 / \u7DE8\u96C6",
  regenerateSchemaButton: "\u30C7\u30D5\u30A9\u30EB\u30C8\u30B9\u30AD\u30FC\u30DE\u3092\u518D\u751F\u6210",
  schemaRegeneratedNotice: "\u30C7\u30D5\u30A9\u30EB\u30C8\u30B9\u30AD\u30FC\u30DE\u3092\u518D\u751F\u6210\u3057\u307E\u3057\u305F\u3002",
  schemaRegenerateFailed: "\u30B9\u30AD\u30FC\u30DE\u306E\u751F\u6210\u306B\u5931\u6557\u3057\u307E\u3057\u305F",
  // Wiki 初期化状態
  wikiInitStatusReady: "Wiki \u521D\u671F\u5316\u6E08\u307F",
  wikiInitStatusNotReady: "Wiki \u672A\u521D\u671F\u5316 \u2014 \u521D\u56DE\u53D6\u308A\u8FBC\u307F\u6642\u306B\u81EA\u52D5\u4F5C\u6210\u3055\u308C\u307E\u3059",
  // Wiki Output Language
  wikiLanguageName: "Wiki \u51FA\u529B\u8A00\u8A9E",
  wikiLanguageDesc: "\u751F\u6210\u3055\u308C\u308B\u3059\u3079\u3066\u306EWiki\u30DA\u30FC\u30B8\u3001\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u3001\u30ED\u30B0\u3001\u554F\u3044\u5408\u308F\u305B\u306E\u5FDC\u7B54\u304C\u3053\u306E\u8A00\u8A9E\u3067\u4F5C\u6210\u3055\u308C\u307E\u3059",
  customWikiLanguageOption: "\u30AB\u30B9\u30BF\u30E0...",
  customWikiLanguageHint: "\u8A00\u8A9E\u540D\u3092\u5165\u529B\u3057\u3066\u304F\u3060\u3055\u3044\uFF08\u4F8B\uFF1AItalian\u3001Arabic\uFF09\u3002LLM\u306B\u51FA\u529B\u8A00\u8A9E\u306E\u6307\u793A\u3068\u3057\u3066\u6E21\u3055\u308C\u307E\u3059\u3002",
  customWikiLanguagePlaceholder: "\u4F8B\uFF1AItalian",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "\u77E5\u8B58\u62BD\u51FA",
  extractionGranularityName: "\u62BD\u51FA\u7C92\u5EA6",
  extractionGranularityDesc: "\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u3054\u3068\u306E\u62BD\u51FA\u6570\u3092\u5236\u5FA1\u3002\u9AD8\u3044\u307B\u3069\u30DA\u30FC\u30B8/API\u6D88\u8CBB\u304C\u5897\u52A0\u3002\n\u7CBE\u7EC6\uFF1A\u6DF1\u5EA6\u5206\u6790\uFF08100\u500B\uFF09\u3002\u6A19\u6E96\uFF1A\u65E5\u5E38\u30CE\u30FC\u30C8\uFF0850\u500B\uFF09\u3002\u7C97\u3081\uFF1A\u6982\u8981\u628A\u63E1\uFF0810\u500B\uFF09\u3002\u6700\u5C0F\uFF1A100\u4EF6\u4EE5\u4E0A\u4E00\u62EC\u51E6\u7406\uFF085\u500B\uFF09\u3002\u30AB\u30B9\u30BF\u30E0\uFF1A\u72EC\u81EA\u8A2D\u5B9A\uFF08\u6700\u5927300\u500B\uFF09\u3002\n\u63D0\u793A\uFF1A\u30D5\u30A1\u30A4\u30EB\u6570\u304C\u591A\u3044\u5834\u5408\u306F\u6700\u5C0F/\u7C97\u3081\u3092\u4F7F\u7528\u3057\u6642\u9593\u3068\u30B3\u30B9\u30C8\u3092\u7BC0\u7D04\u3002",
  extractionGranularityFine: "\u7CBE\u7EC6 \u2014 \u6DF1\u5C64\u5206\u6790\uFF08\u2264100\u4EF6\uFF09",
  extractionGranularityStandard: "\u6A19\u6E96 \u2014 \u65E5\u5E38\u30CE\u30FC\u30C8\uFF08\u226450\u4EF6\uFF09",
  extractionGranularityCoarse: "\u7C97\u3081 \u2014 \u6982\u8981\uFF08\u226410\u4EF6\uFF09",
  extractionGranularityMinimal: "\u6700\u5C0F \u2014 100+\u30D5\u30A1\u30A4\u30EB\u4E00\u62EC\u51E6\u7406\uFF08\u22645\u4EF6\uFF09",
  extractionGranularityCustom: "\u30AB\u30B9\u30BF\u30E0 \u2014 \u4E0A\u9650\u3092\u72EC\u81EA\u8A2D\u5B9A\uFF081~500\uFF09",
  customEntityLimitName: "\u30AB\u30B9\u30BF\u30E0\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u4E0A\u9650",
  customEntityLimitDesc: "\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u3054\u3068\u306E\u6700\u5927\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u62BD\u51FA\u6570\uFF081-500\uFF09",
  customConceptLimitName: "\u30AB\u30B9\u30BF\u30E0\u6982\u5FF5\u4E0A\u9650",
  customConceptLimitDesc: "\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u3054\u3068\u306E\u6700\u5927\u6982\u5FF5\u62BD\u51FA\u6570\uFF081-500\uFF09",
  // Issue #85 v2: タグ語彙 (chip 入力 UI, Wiki 設定内に埋め込み)
  tagVocabularyInlineDesc: 'entity/concept \u30D5\u30ED\u30F3\u30C8\u30DE\u30BF\u30FC\u306E\u30BF\u30B0\u3092\u7BA1\u7406\u8A9E\u5F59\u3067\u5236\u5FA1\u3057\u307E\u3059\u3002"/" \u3092\u542B\u3080\u30CD\u30B9\u30C8\u30BF\u30B0\u306F\u4FDD\u6301\u3055\u308C\u307E\u3059\u3002',
  tagVocabularyModeName: "\u30BF\u30B0\u8A9E\u5F59\u8A2D\u5B9A",
  tagVocabularyModeDescDefault: "\u30C7\u30D5\u30A9\u30EB\u30C8\u306F\u7D44\u307F\u8FBC\u307F\u30BF\u30B0\u3092\u4F7F\u7528: {}\u3002\u300C\u30AB\u30B9\u30BF\u30E0\u300D\u306B\u5207\u308A\u66FF\u3048\u308B\u3068\u72EC\u81EA\u306E\u8A9E\u5F59\u3092\u5B9A\u7FA9\u3067\u304D\u307E\u3059\u3002",
  tagVocabularyModeDescCustom: "\u30AB\u30B9\u30BF\u30E0: \u4E0B\u3067 chip \u5165\u529B\u3092\u4F7F\u3063\u3066\u72EC\u81EA\u306E entity/concept \u30BF\u30B0\u3092\u5B9A\u7FA9\u3057\u307E\u3059\u3002Enter \u307E\u305F\u306F\u30AB\u30F3\u30DE\u3067\u8FFD\u52A0\u3001\xD7 \u3067\u524A\u9664\u3002",
  tagVocabularyModeDefault: "\u30C7\u30D5\u30A9\u30EB\u30C8 (\u7D44\u307F\u8FBC\u307F\u30B5\u30D6\u30BF\u30A4\u30D7\u30BF\u30B0)",
  tagVocabularyModeCustom: "\u30AB\u30B9\u30BF\u30E0 (\u30E6\u30FC\u30B6\u30FC\u5B9A\u7FA9)",
  customEntityTagsName: "\u30AB\u30B9\u30BF\u30E0\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u30BF\u30B0",
  customEntityTagsDesc: 'Enter \u307E\u305F\u306F\u30AB\u30F3\u30DE\u3067 chip \u8FFD\u52A0\u3001\xD7 \u3067\u524A\u9664\u3002"/" \u3092\u542B\u3080\u30CD\u30B9\u30C8\u30BF\u30B0\u306F\u4FDD\u6301\u3055\u308C\u307E\u3059\u3002',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "\u30AB\u30B9\u30BF\u30E0\u6982\u5FF5\u30BF\u30B0",
  customConceptTagsDesc: 'Enter \u307E\u305F\u306F\u30AB\u30F3\u30DE\u3067 chip \u8FFD\u52A0\u3001\xD7 \u3067\u524A\u9664\u3002"/" \u3092\u542B\u3080\u30CD\u30B9\u30C8\u30BF\u30B0\u306F\u4FDD\u6301\u3055\u308C\u307E\u3059\u3002',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "\u91CD\u8907\u30BF\u30B0\u3092\u30B9\u30AD\u30C3\u30D7\u3057\u307E\u3057\u305F",
  // Ingestion Acceleration
  accelerationSectionTitle: "\u53D6\u308A\u8FBC\u307F\u9AD8\u901F\u5316",
  pageGenerationConcurrencyName: "LLM \u4E26\u5217\u5EA6",
  pageGenerationConcurrencyDesc: "\u53D6\u308A\u8FBC\u307F\u304A\u3088\u3073Lint\u64CD\u4F5C\u4E2D\u306E\u4E26\u5217LLM\u547C\u3073\u51FA\u3057\u6570\u3002\u5024\u3092\u9AD8\u304F\u3059\u308B\u3068\u51E6\u7406\u304C\u9AD8\u901F\u5316\u3055\u308C\u307E\u3059\u304C\u3001API\u30B3\u30B9\u30C8\u306E\u5897\u52A0\u3084\u30EC\u30FC\u30C8\u30EA\u30DF\u30C3\u30C8\u306E\u30C8\u30EA\u30AC\u30FC\u306B\u6CE8\u610F\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  concurrencyValueSingular: "\u73FE\u5728\u306E\u4E26\u5217\u5EA6\uFF1A{}\uFF08\u76F4\u5217 \u2014 \u6700\u3082\u5B89\u5168\uFF09",
  concurrencyValuePlural: "\u73FE\u5728\u306E\u4E26\u5217\u5EA6\uFF1A{}\uFF08\u4E26\u5217\uFF09",
  batchDelayName: "\u30D0\u30C3\u30C1\u9045\u5EF6\uFF08ms\uFF09",
  batchDelayDesc: "\u4E26\u5217\u30D0\u30C3\u30C1\u9593\u306E\u9045\u5EF6\u306B\u3088\u308AAPI\u30EC\u30FC\u30C8\u30EA\u30DF\u30C3\u30C8\u3092\u56DE\u907F\u3057\u307E\u3059\uFF08100\u301C2000ms\uFF09\u3002\u73FE\u5728\u5024\uFF1A{}ms\u3002429\u30A8\u30E9\u30FC\u304C\u767A\u751F\u3059\u308B\u5834\u5408\u306F\u5024\u3092\u5927\u304D\u304F\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  // Auto Maintenance
  autoMaintainSection: "\u81EA\u52D5\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9",
  autoMaintainBetaBadge: "\u30D9\u30FC\u30BF\u7248 \u2014 \u5B9F\u9A13\u6A5F\u80FD\u3002\u554F\u984C\u304C\u767A\u751F\u3059\u308B\u53EF\u80FD\u6027\u304C\u3042\u308A\u307E\u3059\u3002\u4E0A\u7D1A\u30E6\u30FC\u30B6\u30FC\u306E\u307F\u63A8\u5968\u3002",
  autoWatchName: "\u76E3\u8996\u30D5\u30A9\u30EB\u30C0\u30FC",
  autoWatchDesc: "\u76E3\u8996\u30D5\u30A9\u30EB\u30C0\u30FC\u5185\u3067\u65B0\u898F\u307E\u305F\u306F\u5909\u66F4\u3055\u308C\u305F.md\u30D5\u30A1\u30A4\u30EB\u3092\u81EA\u52D5\u691C\u51FA\u3057\u3001\u901A\u77E5\u307E\u305F\u306F\u81EA\u52D5\u53D6\u308A\u8FBC\u307F\u3092\u884C\u3044\u307E\u3059",
  watchedFoldersName: "\u76E3\u8996\u5BFE\u8C61\u30D5\u30A9\u30EB\u30C0\u30FC",
  watchedFoldersDesc: "\u65B0\u30B3\u30F3\u30C6\u30F3\u30C4\u3092\u76E3\u8996\u3059\u308B\u30D5\u30A9\u30EB\u30C0\u30FC\u3002\u300C\u30D5\u30A9\u30EB\u30C0\u30FC\u3092\u8FFD\u52A0\u300D\u3092\u30AF\u30EA\u30C3\u30AF\u3057\u3066Vault\u304B\u3089\u9078\u629E\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  addWatchedFolderButton: "\u30D5\u30A9\u30EB\u30C0\u30FC\u3092\u8FFD\u52A0",
  removeWatchedFolderButton: "\u524A\u9664",
  webClipperPresetName: "Clippings\u3092\u76E3\u8996\uFF08Web Clipper\uFF09",
  webClipperPresetDesc: "Obsidian Web Clipper\u304C\u4F7F\u7528\u3059\u308BClippings/\u30D5\u30A9\u30EB\u30C0\u30FC\u3092\u76E3\u8996\u30EA\u30B9\u30C8\u306B\u8FFD\u52A0\u3057\u307E\u3059\u3002\u30A6\u30A7\u30D6\u30AF\u30EA\u30C3\u30D7\u304CWiki\u306B\u81EA\u52D5\u53D6\u308A\u8FBC\u307F\u3055\u308C\u307E\u3059\u3002",
  noWatchedFoldersHint: "\u76E3\u8996\u30D5\u30A9\u30EB\u30C0\u30FC\u304C\u8A2D\u5B9A\u3055\u308C\u3066\u3044\u307E\u305B\u3093\u3002\u30D5\u30A9\u30EB\u30C0\u30FC\u3092\u8FFD\u52A0\u3059\u308B\u304BClippings\u30D7\u30EA\u30BB\u30C3\u30C8\u3092\u6709\u52B9\u306B\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  autoWatchModeName: "\u76E3\u8996\u30E2\u30FC\u30C9",
  autoWatchModeDesc: "\u300C\u901A\u77E5\u306E\u307F\u300D\u306F\u30D7\u30ED\u30F3\u30D7\u30C8\u3092\u8868\u793A\u3057\u307E\u3059\u3002\u300C\u81EA\u52D5\u53D6\u308A\u8FBC\u307F\u300D\u306F\u30B5\u30A4\u30EC\u30F3\u30C8\u306B\u51E6\u7406\u3057\u307E\u3059\u3002",
  watchModeNotify: "\u901A\u77E5\u306E\u307F",
  watchModeAuto: "\u81EA\u52D5\u53D6\u308A\u8FBC\u307F",
  autoWatchDebounceName: "\u30C1\u30E3\u30BF\u30EA\u30F3\u30B0\u9632\u6B62\u9045\u5EF6\uFF08\u79D2\uFF09",
  autoWatchDebounceDesc: "\u30D5\u30A1\u30A4\u30EB\u5909\u66F4\u5F8C\u306B\u53D6\u308A\u8FBC\u307F\u3092\u30C8\u30EA\u30AC\u30FC\u3059\u308B\u307E\u3067\u306E\u5F85\u6A5F\u6642\u9593\uFF081\u301C60\u79D2\uFF09",
  periodicLintName: "\u5B9A\u671F Lint",
  periodicLintDesc: "\u30B9\u30B1\u30B8\u30E5\u30FC\u30EB\u306B\u5F93\u3063\u3066LLM Lint\u3092\u5B9F\u884C\u3002\u524D\u56DE\u30C1\u30A7\u30C3\u30AF\u4EE5\u964D\u306B\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u306E\u5909\u66F4\u304C\u3042\u308B\u5834\u5408\u306E\u307F\u5B9F\u884C\u3057\u307E\u3059",
  periodicLintOff: "\u30AA\u30D5",
  periodicLintHourly: "\u6BCE\u6642",
  periodicLintDaily: "\u6BCE\u65E5",
  periodicLintWeekly: "\u6BCE\u9031",
  autoSmartFixName: "\u81EA\u52D5\u30B9\u30DE\u30FC\u30C8\u4FEE\u6B63",
  autoSmartFixDesc: "Lint \u5B9F\u884C\u6642\u306B\u3001\u30EC\u30DD\u30FC\u30C8\u30E2\u30FC\u30C0\u30EB\u3092\u8868\u793A\u305B\u305A\u81EA\u52D5\u7684\u306B\u3059\u3079\u3066\u306E\u4FEE\u6B63\uFF08\u30B9\u30DE\u30FC\u30C8\u4E00\u62EC\u4FEE\u6B63\uFF09\u3092\u9069\u7528\u3057\u307E\u3059\u3002\u4FEE\u6B63\u5B8C\u4E86\u5F8C\u3082\u30B5\u30DE\u30EA\u30FC\u306F\u8868\u793A\u3055\u308C\u307E\u3059\u3002",
  autoSmartFixNotice: "\u81EA\u52D5\u30B9\u30DE\u30FC\u30C8\u4FEE\u6B63\uFF1A\u3059\u3079\u3066\u306E\u4FEE\u6B63\u3092\u9069\u7528\u4E2D...",
  startupCheckName: "\u8D77\u52D5\u6642\u306B\u30AF\u30A4\u30C3\u30AF\u4FEE\u6B63\u3092\u5B9F\u884C",
  startupCheckDesc: "\u30D7\u30E9\u30B0\u30A4\u30F3\u8AAD\u307F\u8FBC\u307F\u6642\u306B\u4F4E\u30EC\u30D9\u30EB\u66F8\u5F0F\u306E\u554F\u984C\uFF08sources \u30D5\u30A3\u30FC\u30EB\u30C9\u3001\u4E8C\u91CD wikilink\uFF09\u3092\u81EA\u52D5\u4FEE\u6B63\u3057\u3001Wiki \u30C7\u30A3\u30EC\u30AF\u30C8\u30EA\u69CB\u9020\u3092\u691C\u8A3C\u3057\u307E\u3059\u3002\u30C7\u30D5\u30A9\u30EB\u30C8\u3067\u6709\u52B9\u3002",
  suggestSchemaCommand: "\u30B9\u30AD\u30FC\u30DE\u66F4\u65B0\u3092\u63D0\u6848",
  autoMaintainCostWarning: "\u26A0\uFE0F \u30B3\u30B9\u30C8\u306E\u304A\u77E5\u3089\u305B\uFF1A\u81EA\u52D5\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9\u6A5F\u80FD\u306FAPI\u30C8\u30FC\u30AF\u30F3\u3092\u6D88\u8CBB\u3057\u307E\u3059\u3002\u300C\u81EA\u52D5\u53D6\u308A\u8FBC\u307F\u300D\u306F\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u306E\u5909\u66F4\u3054\u3068\u306BLLM\u547C\u3073\u51FA\u3057\u3092\u30C8\u30EA\u30AC\u30FC\u3057\u307E\u3059\u3002\u300C\u5B9A\u671FLint\u300D\u306F\u30B9\u30B1\u30B8\u30E5\u30FC\u30EB\u306B\u5F93\u3063\u3066LLM\u30D8\u30EB\u30B9\u30C1\u30A7\u30C3\u30AF\u3092\u5B9F\u884C\u3057\u307E\u3059\uFF08\u30BD\u30FC\u30B9\u306E\u5909\u66F4\u304C\u691C\u51FA\u3055\u308C\u305F\u5834\u5408\u306E\u307F\uFF09\u3002\u60F3\u5B9A\u5916\u306E\u8AB2\u91D1\u3092\u907F\u3051\u308B\u305F\u3081\u3001\u614E\u91CD\u306B\u8A2D\u5B9A\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  // Notices
  startupCheckSummary: "Wiki\u306F{pages}\u30DA\u30FC\u30B8\uFF08{entities}\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u3001{concepts}\u6982\u5FF5\u3001{sources}\u30BD\u30FC\u30B9\uFF09",
  watcherActiveNotice: "Wiki: \u30D5\u30A1\u30A4\u30EB\u76E3\u8996\u6709\u52B9 \u2014 \u76E3\u8996\u30D5\u30A9\u30EB\u30C0\u30FC\u3092\u30E2\u30CB\u30BF\u30EA\u30F3\u30B0\u4E2D",
  watchIngestNotice: "Wiki: sources/ \u306B{count}\u4EF6\u306E\u30D5\u30A1\u30A4\u30EB\u5909\u66F4\u3042\u308A\u3002\u300C\u30BD\u30FC\u30B9\u306E\u53D6\u308A\u8FBC\u307F\u300D\u3092\u5B9F\u884C\u3057\u3066\u51E6\u7406\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  autoIngestRunning: "{count}\u4EF6\u306E\u5909\u66F4\u30D5\u30A1\u30A4\u30EB\u3092\u81EA\u52D5\u53D6\u308A\u8FBC\u307F\u4E2D...",
  autoIngestComplete: "\u81EA\u52D5\u53D6\u308A\u8FBC\u307F\u5B8C\u4E86\uFF1A\u6210\u529F {success}\u4EF6\u3001\u5931\u6557 {fail}\u4EF6",
  scheduledLintRunning: "\u30B9\u30B1\u30B8\u30E5\u30FC\u30EB\u3055\u308C\u305FWiki Lint\u3092\u5B9F\u884C\u4E2D...",
  wikiLintStats: "Wiki Lint: {pages}\u30DA\u30FC\u30B8\uFF08{entities}\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u3001{concepts}\u6982\u5FF5\u3001{sources}\u30BD\u30FC\u30B9\uFF09",
  wikiHealthStats: "Wiki\u5065\u5168\u6027: {pages}\u30DA\u30FC\u30B8\uFF08{entities}\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3\u3001{concepts}\u6982\u5FF5\u3001{sources}\u30BD\u30FC\u30B9\uFF09{indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Wiki \u30AF\u30A4\u30C3\u30AF\u4FEE\u6B63\u5B8C\u4E86",
  startupCheckStructureLabel: "Wiki \u69CB\u9020",
  startupCheckStructureOk: "\u5B8C\u4E86",
  startupCheckStructureMissing: "\u4E0D\u5B8C\u5168 \u2014 \u521D\u56DE\u53D6\u308A\u8FBC\u307F\u6642\u306B\u81EA\u52D5\u4F5C\u6210\u3055\u308C\u307E\u3059",
  startupCheckSourcesLabel: "Sources \u6B63\u898F\u5316",
  startupCheckSourcesClean: "\u65E2\u306B\u6B63\u898F\u5316\u6E08\u307F",
  startupCheckSourcesCleaned: "{files} \u30D5\u30A1\u30A4\u30EB\u3001{entries} \u30A8\u30F3\u30C8\u30EA\u3092\u4FEE\u6B63",
  startupCheckIncompleteClean: "\u672A\u5B8C\u6210\u30DA\u30FC\u30B8\uFF1A\u306A\u3057",
  startupCheckIncompleteArchived: "\u672A\u5B8C\u6210\u30DA\u30FC\u30B8\uFF1A{count} \u4EF6\u3092\u30A2\u30FC\u30AB\u30A4\u30D6\uFF08.trash \u304B\u3089\u5FA9\u5143\u53EF\u80FD\uFF09",
  startupCheckDisableHint: "\u7121\u52B9\u5316\u3059\u308B\u306B\u306F \u8A2D\u5B9A \u2192 \u81EA\u52D5\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9 \u2192 \u8D77\u52D5\u6642\u306B\u30AF\u30A4\u30C3\u30AF\u4FEE\u6B63\u3092\u5B9F\u884C",
  lintWikiStart: "Wiki Lint\u3092\u958B\u59CB\u4E2D...",
  lintWikiComplete: "Wiki Lint\u5B8C\u4E86",
  lintWikiFailed: "Wiki Lint\u5931\u6557",
  analyzingSchema: "Wiki\u3092\u5206\u6790\u3057\u3001\u30B9\u30AD\u30FC\u30DE\u306E\u63D0\u6848\u3092\u751F\u6210\u4E2D...",
  schemaSuggestionGenerated: "\u30B9\u30AD\u30FC\u30DE\u306E\u63D0\u6848\u3092\u751F\u6210\u3057\u307E\u3057\u305F\u3002wiki/schema/suggestions.md\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044",
  noSchemaUpdateNeeded: "\u30B9\u30AD\u30FC\u30DE\u306E\u66F4\u65B0\u306F\u4E0D\u8981\u3067\u3059\u3002",
  schemaSuggestionFailed: "\u30B9\u30AD\u30FC\u30DE\u306E\u63D0\u6848\u306B\u5931\u6557\u3057\u307E\u3057\u305F",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Schema \u66F4\u65B0\u30D7\u30EC\u30D3\u30E5\u30FC",
  schemaDiffSummary: "+{add} / -{del} \u884C",
  schemaDiffRegenerateLabel: "\u63D0\u6848\u3092\u4FEE\u6B63\uFF08\u4EFB\u610F\uFF09\uFF1A",
  schemaDiffRegenerateBtn: "\u518D\u751F\u6210",
  schemaDiffOpenFileBtn: "\u30D5\u30A1\u30A4\u30EB\u3092\u958B\u304F",
  schemaDiffApplyBtn: "\u9069\u7528",
  schemaDiffApplied: "Schema \u3092\u66F4\u65B0\u3057\u307E\u3057\u305F\u3002\u30D0\u30C3\u30AF\u30A2\u30C3\u30D7\uFF1A{path}",
  schemaDiffEmptyTitle: "LLM \u306F\u5909\u66F4\u4E0D\u8981\u3068\u5224\u65AD",
  schemaDiffFailed: "Schema \u306E\u9069\u7528\u306B\u5931\u6557\uFF1A{reason}",
  schemaDiffRestoreHint: '{path} \u306B\u30D0\u30C3\u30AF\u30A2\u30C3\u30D7\u3057\u307E\u3057\u305F\u3002\u5FA9\u5143\u3059\u308B\u306B\u306F\u3001\u30D5\u30A1\u30A4\u30EB\u30A8\u30AF\u30B9\u30D7\u30ED\u30FC\u30E9\u30FC\u3067\u305D\u306E\u30D5\u30A1\u30A4\u30EB\u3092 "wiki/schema/config.md" \u306B\u30EA\u30CD\u30FC\u30E0\u3057\u3066\u304F\u3060\u3055\u3044\u3002\u6700\u8FD13\u4EF6\u306E\u30D0\u30C3\u30AF\u30A2\u30C3\u30D7\u304C\u4FDD\u6301\u3055\u308C\u307E\u3059\u3002',
  schemaRegenerateNoBody: "\u518D\u751F\u6210\u306F\u6210\u529F\u3057\u307E\u3057\u305F\u304C\u3001LLM \u306F\u65B0\u3057\u3044 Schema \u3092\u8FD4\u3057\u307E\u305B\u3093\u3067\u3057\u305F\u3002",
  schemaNotFoundNotice: "\u30B9\u30AD\u30FC\u30DE\u30D5\u30A1\u30A4\u30EB\u304C\u898B\u3064\u304B\u308A\u307E\u305B\u3093\u3002\u30B9\u30AD\u30FC\u30DE\u3092\u6709\u52B9\u5316\u3057\u3066\u4F5C\u6210\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  selectFolderNoMdFiles: "\u30D5\u30A9\u30EB\u30C0\u30FC {path} \u306BMarkdown\u30D5\u30A1\u30A4\u30EB\u304C\u3042\u308A\u307E\u305B\u3093",
  batchIngestSkipNotice: "\u53D6\u308A\u8FBC\u307F\u6E08\u307F\u306E{skipped}/{total}\u30D5\u30A1\u30A4\u30EB\u3092\u30B9\u30AD\u30C3\u30D7\u3002{new}\u4EF6\u306E\u65B0\u30D5\u30A1\u30A4\u30EB\u3092\u53D6\u308A\u8FBC\u307F\u4E2D...",
  batchIngestAllIngested: "\u3053\u306E\u30D5\u30A9\u30EB\u30C0\u30FC\u5185\u306E{total}\u4EF6\u306F\u3059\u3079\u3066\u53D6\u308A\u8FBC\u307F\u6E08\u307F\u3067\u3059\u3002",
  batchIngestStarting: "\u300C{folder}\u300D\u304B\u3089{count}\u4EF6\u3092\u53D6\u308A\u8FBC\u307F\u4E2D \u2014 \u6570\u5206\u304B\u304B\u308B\u5834\u5408\u304C\u3042\u308A\u307E\u3059\u3002\u5B8C\u4E86\u5F8C\u306B\u30EC\u30DD\u30FC\u30C8\u304C\u8868\u793A\u3055\u308C\u307E\u3059\u3002",
  batchIngestComplete: "\u30D0\u30C3\u30C1\u53D6\u308A\u8FBC\u307F\u5B8C\u4E86\uFF1A\u6210\u529F {success}/{total}\u4EF6\u3001\u5931\u6557 {fail}\u4EF6",
  batchIngestFailedFiles: "\u5931\u6557\u3057\u305F\u30D5\u30A1\u30A4\u30EB\uFF1A",
  historyTruncated: "\u5C65\u6B74\u3092\u76F4\u8FD1\u306E{max}\u30E9\u30A6\u30F3\u30C9\u306B\u5207\u308A\u6368\u3066\u307E\u3057\u305F",
  historyCleared: "\u5C65\u6B74\u3092\u30AF\u30EA\u30A2\u3057\u307E\u3057\u305F",
  // User Feedback Loop
  reviewedPagePreserved: "\u30E6\u30FC\u30B6\u30FC\u30EC\u30D3\u30E5\u30FC\u6E08\u307F\u306E\u30B3\u30F3\u30C6\u30F3\u30C4\u3092\u4FDD\u6301\uFF1A{}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "Wiki\u306B\u4FDD\u5B58\u3057\u307E\u3059\u304B\uFF1F",
  querySuggestSaveDesc: "\u3053\u306E\u4F1A\u8A71\u306B\u306F\u4FA1\u5024\u306E\u3042\u308B\u77E5\u8B58\u304C\u542B\u307E\u308C\u3066\u3044\u307E\u3059\u3002Wiki\u306B\u4FDD\u5B58\u3057\u307E\u3059\u304B\uFF1F",
  querySuggestSaveYes: "\u4FDD\u5B58",
  querySuggestSaveNo: "\u9589\u3058\u308B",
  // Ingestion Report
  ingestReportElapsedTime: "\u7D4C\u904E\u6642\u9593",
  ingestReportSkippedFiles: "\u30B9\u30AD\u30C3\u30D7\u6E08\u307F\uFF08\u53D6\u308A\u8FBC\u307F\u6E08\u307F\uFF09",
  ingestReportRejectedFiles: "\u30B9\u30AD\u30C3\u30D7",
  rejectionReasonEmpty: "\u7A7A",
  rejectionReasonType: "\u975E\u5BFE\u5FDC\u306E\u5F62\u5F0F",
  rejectionReasonDuplicate: "\u91CD\u8907\u30B3\u30F3\u30C6\u30F3\u30C4",
  ingestReportFailedGuidance: "\u3053\u308C\u3089\u306E\u9805\u76EE\u306F\u81EA\u52D5\u4F5C\u6210\u3067\u304D\u307E\u305B\u3093\u3067\u3057\u305F\u3002\u5BFE\u5FDC\u3059\u308B\u30DA\u30FC\u30B8\u3092\u624B\u52D5\u3067\u4F5C\u6210\u3059\u308B\u304B\u3001\u62BD\u51FA\u7C92\u5EA6\u3092\u4E0B\u3052\u3066\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB\u3092\u518D\u53D6\u308A\u8FBC\u307F\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  ingestReportCollisions: "\u30AF\u30ED\u30B9\u30BF\u30A4\u30D7\u885D\u7A81\uFF08\u30A8\u30A4\u30EA\u30A2\u30B9\u3068\u3057\u3066\u7D71\u5408\uFF09",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "\u5358\u4E00\u30BD\u30FC\u30B9\u306E\u53D6\u308A\u8FBC\u307F",
  cmdIngestFolder: "\u30D5\u30A9\u30EB\u30C0\u30FC\u304B\u3089\u306E\u53D6\u308A\u8FBC\u307F",
  cmdQueryWiki: "Wiki\u306B\u554F\u3044\u5408\u308F\u305B",
  cmdLintWiki: "Wiki\u306ELint",
  cmdRegenerateIndex: "\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u306E\u518D\u751F\u6210",
  cmdSuggestSchema: "\u30B9\u30AD\u30FC\u30DE\u66F4\u65B0\u306E\u63D0\u6848",
  cmdCancelIngestion: "\u73FE\u5728\u306E\u53D6\u308A\u8FBC\u307F\u3092\u30AD\u30E3\u30F3\u30BB\u30EB",
  cmdIngestActiveFile: "\u73FE\u5728\u306E\u30D5\u30A1\u30A4\u30EB\u3092\u53D6\u308A\u8FBC\u3080",
  cmdViewHistory: "\u53D6\u308A\u8FBC\u307F\u5C65\u6B74\u3092\u8868\u793A",
  noActiveFile: "\u73FE\u5728\u958B\u3044\u3066\u3044\u308B\u30D5\u30A1\u30A4\u30EB\u306F\u3042\u308A\u307E\u305B\u3093",
  mdOnlyFile: "Markdown\u30D5\u30A1\u30A4\u30EB\u306E\u307F\u53D6\u308A\u8FBC\u307F\u53EF\u80FD\u3067\u3059",
  // Ingestion status bar
  ingestionStatusBar: "\u53D6\u308A\u8FBC\u307F\u4E2D... \u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB",
  lintStatusBar: "\u30C1\u30A7\u30C3\u30AF\u4E2D... \u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB",
  ingestStatusAnalyzing: "\u53D6\u308A\u8FBC\u307F\u4E2D\u2026 (\u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB)",
  lintStatusReading: "\u30C1\u30A7\u30C3\u30AF\u4E2D\u2026 (\u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB)",
  lintStatusDuplicates: "\u30C1\u30A7\u30C3\u30AF\u4E2D\u2026 (\u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB)",
  lintStatusScanningLinks: "\u30C1\u30A7\u30C3\u30AF\u4E2D\u2026 (\u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB)",
  lintStatusAnalyzing: "\u30C1\u30A7\u30C3\u30AF\u4E2D\u2026 (\u30AF\u30EA\u30C3\u30AF\u3067\u30AD\u30E3\u30F3\u30BB\u30EB)",
  ingestionCancelling: "\u30AD\u30E3\u30F3\u30BB\u30EB\u4E2D \u2014 \u73FE\u5728\u306E\u30D0\u30C3\u30C1\u5B8C\u4E86\u5F8C\u306B\u505C\u6B62\u3057\u307E\u3059",
  ingestionCancelled: "\u53D6\u308A\u8FBC\u307F\u304C\u30AD\u30E3\u30F3\u30BB\u30EB\u3055\u308C\u307E\u3057\u305F",
  crossTypeCollisionNotice: "{count}\u4EF6\u304C\u30AF\u30ED\u30B9\u30BF\u30A4\u30D7\u5225\u540D\u3068\u3057\u3066\u7D71\u5408\uFF08\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3 \u2194 \u30B3\u30F3\u30BB\u30D7\u30C8\u91CD\u8907\u3092\u9632\u6B62\uFF09",
  // Lint Report
  lintReportTitle: "Wiki Lint\u30EC\u30DD\u30FC\u30C8",
  lintReportSummary: "Wiki \u72B6\u614B\u306E\u6982\u8981: \u5408\u8A08 {total} \u30DA\u30FC\u30B8\u3001{aliasesMissing} \u30DA\u30FC\u30B8\u306B\u30A8\u30A4\u30EA\u30A2\u30B9\u6B20\u843D\u3001\u91CD\u8907 {duplicates}\u3001\u30C7\u30C3\u30C9\u30EA\u30F3\u30AF {deadLinks}\uFF08\u3046\u3061 {deadLinkFromDup} \u306F\u91CD\u8907\u95A2\u9023\uFF09\u3001\u5B64\u7ACB {orphans}\uFF08\u3046\u3061 {orphanFromDup} \u306F\u91CD\u8907\uFF09\u3001\u7A7A\u30DA\u30FC\u30B8 {emptyPages}\u3001\u6839\u62E0\u306E\u306A\u3044\u5F15\u7528 {ungroundedQuotes}\u3001\u8A9E\u5F59\u5916\u30BF\u30B0 {tagViolations}\u3002Lint \u5B9F\u884C\u6642\u9593: {elapsedSeconds}\u79D2",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "\u9AD8\u5EA6\u306A\u30D1\u30E9\u30E1\u30FC\u30BF\u8A2D\u5B9A",
  advancedSettingsModeDesc: "\u30C7\u30D5\u30A9\u30EB\u30C8\u3067\u306F\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u81EA\u8EAB\u306E\u52D5\u4F5C\u3092\u4F7F\u7528\u3057\u3001\u601D\u8003\u5236\u5FA1\u30FB\u6E29\u5EA6\u30FB\u53CD\u5FA9\u30DA\u30CA\u30EB\u30C6\u30A3\u306E\u30D5\u30A3\u30FC\u30EB\u30C9\u306F\u9001\u4FE1\u3055\u308C\u307E\u305B\u3093\u3002\u307B\u3068\u3093\u3069\u306E\u30E6\u30FC\u30B6\u30FC\u306B\u9069\u3057\u3066\u3044\u307E\u3059\u3002\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306E\u30C7\u30D5\u30A9\u30EB\u30C8\u3092\u660E\u793A\u7684\u306B\u4E0A\u66F8\u304D\u3059\u308B\u5FC5\u8981\u304C\u3042\u308B\u5834\u5408\uFF08\u7279\u5B9A\u306E\u601D\u8003\u5236\u5FA1\u65B9\u8A00\u3092\u5F37\u5236\u3059\u308B\u3001\u30C7\u30D5\u30A9\u30EB\u30C8\u4EE5\u5916\u306E\u6E29\u5EA6\u3092\u8A2D\u5B9A\u3059\u308B\u306A\u3069\uFF09\u306E\u307F\u3001\u30AB\u30B9\u30BF\u30E0\u306B\u5207\u308A\u66FF\u3048\u3066\u304F\u3060\u3055\u3044\u3002",
  advancedSettingsDefault: "\u30C7\u30D5\u30A9\u30EB\u30C8\uFF08\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306E\u52D5\u4F5C\uFF09",
  advancedSettingsCustom: "\u30AB\u30B9\u30BF\u30E0\uFF08\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306E\u30C7\u30D5\u30A9\u30EB\u30C8\u3092\u4E0A\u66F8\u304D\uFF09",
  disableThinkingName: "\u601D\u8003\u3092\u7121\u52B9\u306B\u3059\u308B",
  disableThinkingDesc: "\u30AA\u30D7\u30C8\u30A4\u30F3\u3067\u3059\u3002\u6709\u52B9\u306B\u3059\u308B\u3068\u3001\u30D7\u30E9\u30B0\u30A4\u30F3\u306F\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306B\u601D\u8003\u5236\u5FA1\u30C7\u30A3\u30EC\u30AF\u30C6\u30A3\u30D6\u3092\u9001\u4FE1\u3057\u3001\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u304C\u62D2\u5426\u3057\u305F\u5834\u5408\u306F3\u5C64\u306E\u30C0\u30A4\u30A2\u30EC\u30AF\u30C8\u30D5\u30A9\u30FC\u30EB\u30D0\u30C3\u30AF\u30C1\u30A7\u30FC\u30F3\uFF08anthropic \u2192 openai \u2192 none\uFF09\u3092\u5B9F\u884C\u3057\u307E\u3059\u3002\u3053\u308C\u306F\u3001\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u304C\u63A8\u8AD6\u5185\u5BB9\u3092\u56DE\u7B54\u306B\u6F0F\u3089\u3057\u3001\u305D\u308C\u3092\u6291\u5236\u3059\u308B\u5FC5\u8981\u304C\u3042\u308B\u5834\u5408\u306B\u306E\u307F\u4F7F\u7528\u3057\u3066\u304F\u3060\u3055\u3044\u3002\u307B\u3068\u3093\u3069\u306E\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306F\u63A8\u8AD6\u3092\u6B63\u3057\u304F\u51E6\u7406\u3059\u308B\u305F\u3081\u3001\u3053\u308C\u3092\u7121\u52B9\u306E\u307E\u307E\u306B\u3057\u3066\u304A\u304F\uFF08\u30C7\u30D5\u30A9\u30EB\u30C8\uFF09\u65B9\u304C\u54C1\u8CEA\u304C\u9AD8\u304F\u306A\u308A\u307E\u3059\u3002",
  // Issue #137: 互換性に関する注意事項（簡潔；プロバイダ一覧は省略）
  extractionTemperatureName: "\u62BD\u51FA\u6E29\u5EA6",
  extractionTemperatureDesc: "\u7BC4\u56F2 0\u20132\u3002\u4F4E\u3044\u307B\u3069LLM\u306E\u51FA\u529B\u306F\u78BA\u5B9A\u7684\u3067\u5FE0\u5B9F\u306B\u3002\u9AD8\u3044\u307B\u3069\u5275\u9020\u7684\u306B\u3002\u7A7A\u6B04 = \u30D7\u30ED\u30D0\u30A4\u30C0\u306E\u30C7\u30D5\u30A9\u30EB\u30C8\u3002\u30D7\u30ED\u30D0\u30A4\u30C0\u304C\u3053\u306E\u5024\u3092\u62D2\u5426\u3057\u305F\u5834\u5408\u3001\u30D7\u30E9\u30B0\u30A4\u30F3\u306F\u81EA\u52D5\u7684\u306B\u30D5\u30A3\u30FC\u30EB\u30C9\u3092\u524A\u9664\u3057\u3001\u901A\u77E5\u3092\u4E00\u5EA6\u3060\u3051\u8868\u793A\u3057\u307E\u3059\u3002",
  chatTemperatureName: "\u30AF\u30A8\u30EA\u6E29\u5EA6",
  chatTemperatureDesc: "\u7BC4\u56F2 0\u20132\u3002\u62BD\u51FA\u6E29\u5EA6\u3068\u540C\u3058\u3067\u3059\u304C\u3001\u30C1\u30E3\u30C3\u30C8/\u30AF\u30A8\u30EA\u306E\u5FDC\u7B54\u306B\u306E\u307F\u5F71\u97FF\u3057\u307E\u3059\u3002\u7A7A\u6B04 = \u30D7\u30ED\u30D0\u30A4\u30C0\u306E\u30C7\u30D5\u30A9\u30EB\u30C8\u3002\u30D7\u30ED\u30D0\u30A4\u30C0\u304C\u3053\u306E\u5024\u3092\u62D2\u5426\u3057\u305F\u5834\u5408\u3001\u30D7\u30E9\u30B0\u30A4\u30F3\u306F\u81EA\u52D5\u7684\u306B\u30D5\u30A3\u30FC\u30EB\u30C9\u3092\u524A\u9664\u3057\u3001\u901A\u77E5\u3092\u4E00\u5EA6\u3060\u3051\u8868\u793A\u3057\u307E\u3059\u3002",
  repetitionPenaltyName: "\u7E70\u308A\u8FD4\u3057\u30DA\u30CA\u30EB\u30C6\u30A3",
  repetitionPenaltyDesc: "\u7BC4\u56F2 0\u20132\u3002\u9AD8\u3044\u307B\u3069\u7E70\u308A\u8FD4\u3057\u30D1\u30BF\u30FC\u30F3\u3092\u4F4E\u6E1B\u3057\u307E\u3059\u3002\u7A7A\u6B04 = \u30D7\u30ED\u30D0\u30A4\u30C0\u306E\u30C7\u30D5\u30A9\u30EB\u30C8\u3002\u4E92\u63DB\u6027\u306B\u95A2\u3059\u308B\u6CE8\u610F\uFF1A\u591A\u304F\u306E\u30AF\u30E9\u30A6\u30C9\u30D7\u30ED\u30D0\u30A4\u30C0\u306F\u3053\u306E\u30D5\u30A3\u30FC\u30EB\u30C9\u3092\u53D7\u3051\u4ED8\u3051\u307E\u305B\u3093\u3002\u30D7\u30E9\u30B0\u30A4\u30F3\u306F HTTP 400 \u6642\u306B\u81EA\u52D5\u7684\u306B\u524A\u9664\u3057\u3001\u901A\u77E5\u3092\u4E00\u5EA6\u3060\u3051\u8868\u793A\u3057\u307E\u3059\u3002",
  temperaturePlaceholder: "\u7A7A\u6B04 = \u30D7\u30ED\u30D0\u30A4\u30C0\u306E\u30C7\u30D5\u30A9\u30EB\u30C8",
  lintDeadLinkSection: "\u30C7\u30C3\u30C9\u30EA\u30F3\u30AF\uFF08\u691C\u51FA\uFF09[{count} \u4EF6]",
  lintEmptyPageSection: "\u7A7A\u30DA\u30FC\u30B8\uFF08\u691C\u51FA\uFF09[{count} \u4EF6]",
  lintOrphanSection: "\u5B64\u7ACB\u30DA\u30FC\u30B8\uFF08\u691C\u51FA\uFF09[{count} \u4EF6]",
  lintContradictionSection: "\u77DB\u76FE\uFF08\u691C\u51FA\u6E08\u307F\uFF09",
  lintDuplicateSection: "\u91CD\u8907\u30DA\u30FC\u30B8\uFF08\u691C\u51FA\u6E08\u307F\uFF09",
  lintNoIssuesFound: "\u91CD\u8907\u3001\u30EA\u30F3\u30AF\u5207\u308C\u3001\u7A7A\u30DA\u30FC\u30B8\u3001\u5B64\u7ACB\u30DA\u30FC\u30B8\u3001\u307E\u305F\u306F\u6839\u62E0\u306E\u306A\u3044\u5F15\u7528\u306F\u691C\u51FA\u3055\u308C\u307E\u305B\u3093\u3067\u3057\u305F\u3002",
  lintQuoteGroundingSection: "\u6839\u62E0\u306E\u306A\u3044\u5F15\u7528\uFF08\u691C\u51FA\uFF09[{count} \u4EF6]",
  lintQuoteGroundingItem: "- [[{page}]]{sourceHint}: \u300C{quote}\u300D",
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}**\uFF08\u30DA\u30FC\u30B8\u304C\u5B58\u5728\u3057\u307E\u305B\u3093\uFF09{dupFlag}",
  lintDeadLinkMore: "- ... \u4ED6{count}\u4EF6\u306E\u30EA\u30F3\u30AF\u5207\u308C",
  lintEmptyPageItem: "- [[{page}]] \u2014 \u5B9F\u8CEA\u7684\u306A\u30B3\u30F3\u30C6\u30F3\u30C4\u304C50\u6587\u5B57\u672A\u6E80",
  lintOrphanItem: "- [[{page}]] \u2014 \u4ED6\u306EWiki\u30DA\u30FC\u30B8\u304B\u3089\u30EA\u30F3\u30AF\u3055\u308C\u3066\u3044\u307E\u305B\u3093{dupFlag}",
  lintPollutedSection: "\u6C5A\u67D3\u30DA\u30FC\u30B8\uFF08\u691C\u51FA\uFF09[{count} \u4EF6]",
  lintPollutedItem: '- [[{page}]] \u2192 "{clean}"\u306B\u4FEE\u6B63\u5FC5\u8981',
  lintSourcesNormalizedSection: "Sources \u6B63\u898F\u5316\uFF08\u81EA\u52D5\u4FEE\u6B63\uFF09[{files} \u30D5\u30A1\u30A4\u30EB / {entries} \u4EF6]",
  lintSourcesNormalizedItem: "{files} \u30D5\u30A1\u30A4\u30EB\u5185\u306E {entries} \u4EF6\u306E sources \u30A8\u30F3\u30C8\u30EA\u3092\u4FEE\u6B63\u3057\u307E\u3057\u305F\uFF08\u5916\u90E8\u30D1\u30B9\u3001.md \u62E1\u5F35\u5B50\u3001\u30A8\u30A4\u30EA\u30A2\u30B9\u30D1\u30A4\u30D7\u3092\u524A\u9664\u3057\u91CD\u8907\u6392\u9664\uFF09\u3002",
  lintDuplicateItem: "- [[{target}]] \u3068 [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " \uFF08\u26A0\uFE0F \u91CD\u8907\u30DA\u30FC\u30B8\u304C\u95A2\u9023\uFF09",
  lintOrphanIsDuplicate: " \uFF08\u26A0\uFE0F \u91CD\u8907\u30DA\u30FC\u30B8\uFF09",
  lintContradictionOpen: "\u672A\u89E3\u6C7A\u306E\u77DB\u76FE\uFF1A{count}\u4EF6",
  lintContradictionAutoFixed: "\uFF08\u4ECA\u56DE{count}\u4EF6\u3092\u81EA\u52D5\u4FEE\u5FA9\uFF09",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "\u691C\u51FA\u6E08\u307F",
  lintContradictionStatusPendingFix: "\u4FEE\u5FA9\u5F85\u3061",
  lintLLMAnalysisHeading: "## LLM\u5206\u6790",
  // Lint Analysis Prompt
  lintAnalysisPrompt: "\u3042\u306A\u305F\u306FWiki\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9\u306E\u52A9\u624B\u3067\u3059\u3002\u4EE5\u4E0B\u306E\u60C5\u5831\u306B\u57FA\u3065\u3044\u3066Wiki\u306E\u5065\u5168\u6027\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044\u3002\n\nWiki\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\uFF1A\n{index}\n\nWiki\u30DA\u30FC\u30B8\u5185\u5BB9\u30B5\u30F3\u30D7\u30EB\uFF08\u5408\u8A08{total}\u30DA\u30FC\u30B8\u3001{sample}\u30DA\u30FC\u30B8\u3092\u8868\u793A\uFF09\uFF1A\n{contentSample}\n\n\u30D7\u30ED\u30B0\u30E9\u30E0\u306B\u3088\u308B\u30C1\u30A7\u30C3\u30AF\u7D50\u679C\uFF08\u691C\u8A3C\u6E08\u307F\u3001\u91CD\u8907\u5831\u544A\u4E0D\u8981\uFF09\uFF1A\n{progReport}\n\n\u4EE5\u4E0B\u306E\u9805\u76EE\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044\uFF08\u30D7\u30ED\u30B0\u30E9\u30E0\u306B\u3088\u308A\u691C\u51FA\u6E08\u307F\u306E\u30EA\u30F3\u30AF\u5207\u308C\u30FB\u7A7A\u30DA\u30FC\u30B8\u30FB\u5B64\u7ACB\u30DA\u30FC\u30B8\u306F\u9664\u5916\uFF09\uFF1A\n1. **\u77DB\u76FE** \u2014 \u7570\u306A\u308B\u30DA\u30FC\u30B8\u9593\u3067\u540C\u4E00\u4E8B\u5B9F\u306B\u95A2\u3059\u308B\u8A18\u8FF0\u304C\u77DB\u76FE\u3057\u3066\u3044\u306A\u3044\u304B\n2. **\u9673\u8150\u5316** \u2014 \u660E\u3089\u304B\u306B\u53E4\u304F\u306A\u3063\u305F\u8A18\u8FF0\u304C\u306A\u3044\u304B\n3. **\u4E0D\u8DB3** \u2014 \u91CD\u8981\u306A\u6982\u5FF5\u306B\u72EC\u7ACB\u3057\u305F\u30DA\u30FC\u30B8\u304C\u306A\u3044\u3082\u306E\u306F\u306A\u3044\u304B\n4. **\u69CB\u9020** \u2014 \u30DA\u30FC\u30B8\u69CB\u9020\u304C\u59A5\u5F53\u3067\u3001\u76F8\u4E92\u53C2\u7167\u304C\u5341\u5206\u304B\n\n\u51FA\u529B\u5F62\u5F0F\uFF1AMarkdown\u3092\u4F7F\u7528\u3057\u3001\u300C## LLM\u5206\u6790\u300D\u3067\u59CB\u3081\u3066\u304F\u3060\u3055\u3044\u3002\u5404\u767A\u898B\u306F\u4E00\u884C\u3067\u300C- [\u5177\u4F53\u7684\u306A\u554F\u984C]\u300D\u3068\u8A18\u8F09\u3002\u554F\u984C\u304C\u306A\u3044\u5834\u5408\u306F\u300C\u660E\u3089\u304B\u306A\u554F\u984C\u306F\u898B\u3064\u304B\u308A\u307E\u305B\u3093\u3067\u3057\u305F\u3002\u300D\u3068\u8A18\u8F09\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  // Lint Fix Progress
  lintFixProgress: "\u4FEE\u5FA9\u4E2D {current}/{total}\uFF1A[[{target}]]",
  lintFixDeadComplete: "\u30EA\u30F3\u30AF\u5207\u308C\u4FEE\u5FA9\u5B8C\u4E86\u3002{fixed}/{total}\u4EF6\u3092\u4FEE\u5FA9\u3057\u307E\u3057\u305F\u3002",
  lintFillProgress: "\u62E1\u5145\u4E2D {current}/{total}\uFF1A{page}",
  lintFillComplete: "\u30DA\u30FC\u30B8\u62E1\u5145\u5B8C\u4E86\u3002{filled}/{total}\u30DA\u30FC\u30B8\u3092\u5165\u529B\u3057\u307E\u3057\u305F\u3002",
  lintDeleteCompleted: "{count}\u4EF6\u306E\u7A7A\u30B9\u30BF\u30D6\u3092\u524A\u9664\u3057\u307E\u3057\u305F",
  lintDeleteFailed: "\u7A7A stub \u306E\u524A\u9664\u306B {failed}/{total} \u4EF6\u5931\u6557\uFF08\u8A73\u7D30\u306F\u30B3\u30F3\u30BD\u30FC\u30EB\uFF09",
  lintFillFailed: "\u62E1\u5145\u306B\u5931\u6557\uFF1A{page} \u2014 {error}",
  lintLinkProgress: "\u30EA\u30F3\u30AF\u8A2D\u5B9A\u4E2D {current}/{total}\uFF1A{page}",
  lintLinkComplete: "\u5B64\u7ACB\u30DA\u30FC\u30B8\u3078\u306E\u30EA\u30F3\u30AF\u5B8C\u4E86\u3002{linked}\u30DA\u30FC\u30B8\u306B\u30EA\u30F3\u30AF\u3092\u8A2D\u5B9A\u3057\u307E\u3057\u305F\u3002",
  lintFixNoAction: "\u64CD\u4F5C\u306A\u3057\uFF08\u30AF\u30E9\u30A4\u30A2\u30F3\u30C8\u672A\u63A5\u7D9A\uFF09",
  lintFixIndexUpdated: "Wiki\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u3068\u30ED\u30B0\u3092\u66F4\u65B0\u3057\u307E\u3057\u305F\u3002",
  lintPollutedFixed: "\u6C5A\u67D3\u30DA\u30FC\u30B8\u4FEE\u6B63\u6E08\u307F\uFF1A{fixed}/{total}\u3002\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u518D\u69CB\u7BC9\u6E08\u307F\u3002",
  regenerateIndexCompleted: "\u30A4\u30F3\u30C7\u30C3\u30AF\u30B9\u518D\u69CB\u7BC9\u6E08\u307F",
  operationFailed: "\u5931\u6557\uFF1A",
  lintFixAllComplete: "\u3059\u3079\u3066\u306E\u4FEE\u5FA9\u304C\u5B8C\u4E86\u3057\u307E\u3057\u305F\u3002\u8A73\u7D30\u306F\u30ED\u30B0\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  // Lint Report Modal
  lintModalActionsTitle: "\u4FEE\u5FA9\u63D0\u6848\uFF08LLM\u30C8\u30FC\u30AF\u30F3\u3092\u6D88\u8CBB\uFF09\uFF1A",
  lintLogReference: "\u5B8C\u5168\u306A\u30EC\u30DD\u30FC\u30C8\u304C log.md \u306B\u4FDD\u5B58\u3055\u308C\u307E\u3057\u305F",
  lintModalFixDeadLinks: "\u30EA\u30F3\u30AF\u5207\u308C\u3092\u4FEE\u5FA9\uFF08{count}\u4EF6\uFF09",
  lintModalExpandEmpty: "\u7A7A\u30DA\u30FC\u30B8\u3092\u62E1\u5145\uFF08{count}\u4EF6\uFF09",
  lintModalDeleteEmpty: "\u7A7A\u306E\u30B9\u30BF\u30D6\u3092\u524A\u9664\uFF08{count}\u4EF6\uFF09",
  lintModalLinkOrphans: "\u5B64\u7ACB\u30DA\u30FC\u30B8\u306B\u30EA\u30F3\u30AF\uFF08{count}\u4EF6\uFF09",
  lintModalAnalyzeSchema: "\u30B9\u30AD\u30FC\u30DE\u3092\u5206\u6790",
  lintModalMergeDuplicates: "\u91CD\u8907\u3092\u30DE\u30FC\u30B8\uFF08{count}\u4EF6\uFF09",
  lintModalFixAll: "\u4E00\u62EC\u30B9\u30DE\u30FC\u30C8\u4FEE\u5FA9\uFF08{count}\u4EF6\u306E\u554F\u984C\uFF09",
  lintMergeProgress: "\u30DE\u30FC\u30B8\u4E2D {current}/{total}\uFF1A{source} \u2192 {target}",
  lintMergeComplete: "\u91CD\u8907\u30DE\u30FC\u30B8\u5B8C\u4E86\u3002{merged}/{total}\u7D44\u3092\u30DE\u30FC\u30B8\u3057\u307E\u3057\u305F\u3002",
  // Ingest Report Modal
  ingestReportTitle: "\u53D6\u308A\u8FBC\u307F\u30EC\u30DD\u30FC\u30C8",
  ingestReportSourceFile: "\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB",
  ingestReportCreated: "\u4F5C\u6210",
  ingestReportUpdated: "\u66F4\u65B0",
  ingestReportContradictions: "\u77DB\u76FE\u3092\u767A\u898B",
  ingestReportFailedTitle: "\u53D6\u308A\u8FBC\u307F\u5931\u6557",
  ingestReportErrorDetail: "\u30A8\u30E9\u30FC\u8A73\u7D30",
  ingestReportClose: "\u9589\u3058\u308B",
  ingestReportCreatedPages: "\u4F5C\u6210\u30DA\u30FC\u30B8\uFF1A{count}",
  ingestReportUpdatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8\uFF1A{count}",
  ingestReportEntitiesCount: "{count}\u4EF6\u306E\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3",
  ingestReportConceptsCount: "{count}\u4EF6\u306E\u6982\u5FF5",
  ingestReportContradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B\uFF1A{count}\u4EF6",
  ingestReportEntityType: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3",
  ingestReportConceptType: "\u6982\u5FF5",
  timeMinutes: "\u5206",
  timeSeconds: "\u79D2",
  // レートリミット警告
  rateLimitDetected: "\u26A0\uFE0F \u30EC\u30FC\u30C8\u30EA\u30DF\u30C3\u30C8\u3092\u691C\u51FA\uFF1A{count}\u30DA\u30FC\u30B8\u304C429\u30A8\u30E9\u30FC\u3067\u5931\u6557\u3057\u307E\u3057\u305F\u3002\u5BFE\u51E6\u6CD5\uFF1A(1) \u4E26\u5217\u5EA6\u3092{suggestedConcurrency}\u307E\u305F\u306F1\uFF08\u76F4\u5217\uFF09\u306B\u4E0B\u3052\u308B\u3001(2) \u30D0\u30C3\u30C1\u9045\u5EF6\u3092{suggestedDelay}ms\u306B\u5897\u3084\u3059\u3001(3) \u3088\u308A\u9AD8\u3044\u30EC\u30FC\u30C8\u30EA\u30DF\u30C3\u30C8\u3092\u6301\u3064\u30D7\u30ED\u30D0\u30A4\u30C0\u30FC\u306B\u5207\u308A\u66FF\u3048\u308B\u3002",
  rateLimitDetectedShort: "\u26A0\uFE0F \u30EC\u30FC\u30C8\u30EA\u30DF\u30C3\u30C8\u767A\u751F \u2014 \u8A2D\u5B9A\u2192\u53D6\u308A\u8FBC\u307F\u52A0\u901F\u3067\u4E26\u5217\u5EA6\u3092\u4E0B\u3052\u308B\u304B\u30D0\u30C3\u30C1\u9045\u5EF6\u3092\u5897\u3084\u3059\u3053\u3068\u3092\u63A8\u5968\u3002",
  sourceRejectedEmpty: "\u23ED\uFE0F \u300C{filename}\u300D\u306B\u306F\u53D6\u308A\u8FBC\u3080\u5185\u5BB9\u304C\u306A\u3044\u305F\u3081\u30B9\u30AD\u30C3\u30D7\u3057\u307E\u3057\u305F\u3002\u7A7A\u306E\u30CE\u30FC\u30C8\u3084\u30D5\u30ED\u30F3\u30C8\u30DE\u30BF\u30FC\u306E\u307F\u306E\u30CE\u30FC\u30C8\u306FWiki\u30DA\u30FC\u30B8\u3092\u751F\u6210\u3057\u307E\u305B\u3093\u3002",
  sourceRejectedType: "\u23ED\uFE0F \u300C{filename}\u300D\u306F\u30B5\u30DD\u30FC\u30C8\u3055\u308C\u3066\u3044\u306A\u3044\u30D5\u30A1\u30A4\u30EB\u5F62\u5F0F\u306E\u305F\u3081\u30B9\u30AD\u30C3\u30D7\u3057\u307E\u3057\u305F\u3002\u53D6\u308A\u8FBC\u3081\u308B\u306E\u306F\u30C6\u30AD\u30B9\u30C8\u30CE\u30FC\u30C8\uFF08.md\u3001.txt \u306A\u3069\uFF09\u306E\u307F\u3067\u3059\u3002",
  sourceRejectedDuplicate: "\u23ED\uFE0F \u300C{filename}\u300D\u3092\u30B9\u30AD\u30C3\u30D7\u3057\u307E\u3057\u305F \u2014 \u5185\u5BB9\u306F\u3059\u3067\u306BWiki\u306B\u5B58\u5728\u3057\u307E\u3059\u3002",
  ingestRejectedSummary: "{count} \u500B\u306E\u30D5\u30A1\u30A4\u30EB\u3092\u30B9\u30AD\u30C3\u30D7\u3057\u307E\u3057\u305F\uFF08\u7A7A\u30FB\u91CD\u8907\u30FB\u975E\u5BFE\u5FDC\u5F62\u5F0F\uFF09\u3002",
  reingestConfirmTitle: "\u3053\u306E\u30D5\u30A1\u30A4\u30EB\u3092\u518D\u53D6\u308A\u8FBC\u307F\u3057\u307E\u3059\u304B\uFF1F",
  reingestConfirmBody: "\u300C{filename}\u300D\u306E\u5185\u5BB9\u306F\u3059\u3067\u306BWiki\u306B\u5B58\u5728\u3057\u307E\u3059\u3002\u305D\u308C\u3067\u3082\u518D\u53D6\u308A\u8FBC\u307F\u3057\u307E\u3059\u304B\uFF1F",
  reingestConfirmYes: "\u518D\u53D6\u308A\u8FBC\u307F",
  reingestConfirmNo: "\u30B9\u30AD\u30C3\u30D7",
  longSourceNotice: '"\u{1F4C4} \u300C{filename}\u300D\u306F{lines}\u884C\uFF08{size}\uFF09\u3067\u3059\u3002\u9577\u6587\u306F\u8907\u6570\u30D1\u30B9\u306E\u53CD\u5FA9\u62BD\u51FA\u304C\u5FC5\u8981\u3067\u3001LLM\u304C\u5168\u6587\u66F8\u3092\u5206\u6790\u3059\u308B\u305F\u3081\u6570\u5206\u304B\u304B\u308B\u5834\u5408\u304C\u3042\u308A\u307E\u3059\u3002\u3057\u3070\u3089\u304F\u304A\u5F85\u3061\u304F\u3060\u3055\u3044\u3002"',
  longSourceNoticeShort: '"\u{1F4C4} \u5927\u304D\u306A\u30D5\u30A1\u30A4\u30EB\uFF08{lines}\u884C\uFF09\u3092\u691C\u51FA\u3057\u307E\u3057\u305F\u3002\u53D6\u308A\u8FBC\u307F\u306B\u6642\u9593\u304C\u304B\u304B\u308B\u3053\u3068\u304C\u3042\u308A\u307E\u3059\u3002"',
  // 取り込み履歴パネル (#122) — UI テキスト
  historyButton: "\u64CD\u4F5C\u5C65\u6B74",
  historyButtonDesc: "LLM-Wiki \u306E\u6700\u8FD1\u306E\u53D6\u308A\u8FBC\u307F\u30FBLint \u30EC\u30DD\u30FC\u30C8\u30FB\u305D\u306E\u4ED6\u306E\u5909\u66F4\u3092\u8868\u793A",
  historyButtonOpen: "\u5C65\u6B74\u3092\u8868\u793A",
  historyModalTitle: "\u64CD\u4F5C\u5C65\u6B74",
  historyModalSubtitle: "LLM-Wiki \u306E\u6700\u8FD1\u306E\u5909\u66F4",
  historyEmpty: "\u307E\u3060\u64CD\u4F5C\u8A18\u9332\u304C\u3042\u308A\u307E\u305B\u3093\u3002\u30CE\u30FC\u30C8\u3092\u53D6\u308A\u8FBC\u3080\u304B Lint \u3092\u5B9F\u884C\u3057\u3066\u8A18\u9332\u3092\u4F5C\u6210\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  historyReadError: "\u64CD\u4F5C\u30ED\u30B0\u3092\u8AAD\u307F\u8FBC\u3081\u307E\u305B\u3093\uFF1A{error}",
  historyEntryKindIngest: "\u53D6\u308A\u8FBC\u307F",
  historyEntryKindMaintenance: "\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9",
  historyEntryKindFix: "\u4FEE\u6B63",
  historyEntryKindOther: "\u305D\u306E\u4ED6",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "\u30BD\u30FC\u30B9\uFF1A{source}",
  historyEntrySourceUnknown: "\u7121\u984C\u306E\u53D6\u308A\u8FBC\u307F",
  historyEntryCreatedLabel: "\u4F5C\u6210",
  historyEntryUpdatedLabel: "\u66F4\u65B0",
  historyEntryCreatedCount: "{count} \u4EF6\u4F5C\u6210",
  historyEntryUpdatedCount: "{count} \u4EF6\u66F4\u65B0",
  historyEntryNoChanges: "\u30DA\u30FC\u30B8\u5909\u66F4\u306A\u3057",
  historyEntryContradictions: "\u26A0\uFE0F \u77DB\u76FE {count} \u4EF6",
  historyEntrySectionCreated: "\u4F5C\u6210\u3057\u305F\u30DA\u30FC\u30B8",
  historyEntrySectionUpdated: "\u66F4\u65B0\u3057\u305F\u30DA\u30FC\u30B8",
  historyEntrySectionContradictions: "\u77DB\u76FE",
  historyEntrySectionDetails: "\u8A73\u7D30",
  historyEntryOpenPage: "\u958B\u304F",
  historyEntryDetailsNoContradictions: "\u77DB\u76FE\u306F\u898B\u3064\u304B\u308A\u307E\u305B\u3093\u3067\u3057\u305F",
  historySearchPlaceholder: "\u{1F50D} \u30BD\u30FC\u30B9\u540D\u307E\u305F\u306F\u30DA\u30FC\u30B8\u30D1\u30B9\u3067\u691C\u7D22\u2026",
  historyFilterAll: "\u3059\u3079\u3066",
  historyFilterIngest: "\u53D6\u308A\u8FBC\u307F\u306E\u307F",
  historyFilterMaintenance: "\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9\u306E\u307F",
  historyFilterFix: "\u4FEE\u6B63\u306E\u307F",
  historyFilterContradictions: "\u26A0\uFE0F \u77DB\u76FE\u3042\u308A",
  historyRefreshButton: "\u27F3 \u66F4\u65B0",
  historyExpandDay: "\u30AF\u30EA\u30C3\u30AF\u3067\u5C55\u958B",
  historyCollapseDay: "\u30AF\u30EA\u30C3\u30AF\u3067\u6298\u308A\u305F\u305F\u3080",
  historyShowMore: "\u53E4\u3044 {count} \u4EF6\u3092\u8868\u793A",
  historyNoMatch: "\u691C\u7D22/\u30D5\u30A3\u30EB\u30BF\u306B\u4E00\u81F4\u3059\u308B\u64CD\u4F5C\u304C\u3042\u308A\u307E\u305B\u3093\u3002",
  historyCloseButton: "\u9589\u3058\u308B",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "\u30EC\u30DD\u30FC\u30C8\u6240\u898B",
  historyKpiPages: "\u30DA\u30FC\u30B8\u6570",
  historyKpiDeadLinks: "\u30C7\u30C3\u30C9\u30EA\u30F3\u30AF",
  historyKpiOrphans: "\u5B64\u7ACB",
  historyKpiEmpty: "\u7A7A\u30DA\u30FC\u30B8",
  historyKpiDuplicates: "\u91CD\u8907",
  historyKpiTagViolations: "\u30BF\u30B0\u9055\u53CD",
  historyKpiUnsourced: "\u672A\u51FA\u5178",
  historyKpiDuration: "Lint \u6642\u9593",
  historyKpiDurationSec: "{seconds} \u79D2",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "\u30C7\u30C3\u30C9\u30EA\u30F3\u30AF ({count})",
  historySectionTagViolations: "\u30BF\u30B0\u9055\u53CD ({count})",
  historySectionOrphans: "\u5B64\u7ACB\u30DA\u30FC\u30B8 ({count})",
  historySectionEmptyPages: "\u7A7A\u30DA\u30FC\u30B8 ({count})",
  historySectionLlmAnalysis: "LLM \u5206\u6790 ({count})",
  historyDeadLinkSource: "\u53C2\u7167\u5143",
  historyDeadLinkTarget: "\u6B20\u843D",
  historyOpenInLog: "log.md \u3067\u8868\u793A",
  historyShowMoreItems: "\u6B8B\u308A {count} \u4EF6\u3092\u5C55\u958B",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 \u5909\u5316\u306A\u3057",
  historyChipContradiction: "\u77DB\u76FE",
  historyChipOutdated: "\u53E4\u3044",
  historyChipMissing: "\u6B20\u843D",
  historyChipStructure: "\u69CB\u9020",
  historySeverityHigh: "\u9AD8",
  historySeverityMedium: "\u4E2D",
  historySeverityLow: "\u4F4E",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Wiki \u306B\u306F {dead} \u4EF6\u306E\u30C7\u30C3\u30C9\u30EA\u30F3\u30AF\u3001{orphans} \u4EF6\u306E\u5B64\u7ACB\u30DA\u30FC\u30B8\u3001{tags} \u4EF6\u306E\u30BF\u30B0\u9055\u53CD\u304C\u3042\u308A\u307E\u3059\u3002\u524D\u56DE\u306E Lint \u306F {duration} \u304B\u304B\u308A\u307E\u3057\u305F\u3002",
  historyGlobalInsightClean: "Wiki \u306F\u6B63\u5E38\u3067\u3059 \u2014 \u30C7\u30C3\u30C9\u30EA\u30F3\u30AF\u3001\u5B64\u7ACB\u30DA\u30FC\u30B8\u3001\u30BF\u30B0\u9055\u53CD\u306F\u691C\u51FA\u3055\u308C\u3066\u3044\u307E\u305B\u3093\u3002",
  historyGlobalInsightNoData: "\u30E1\u30F3\u30C6\u30CA\u30F3\u30B9\u30EC\u30DD\u30FC\u30C8\u304C\u307E\u3060\u3042\u308A\u307E\u305B\u3093 \u2014 Lint \u3092\u5B9F\u884C\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
  historyModalHeaderTitle: "\u64CD\u4F5C\u5C65\u6B74",
  historyModalSubtitleWithCount: "LLM-Wiki \u306E\u6700\u8FD1\u306E\u5909\u66F4 \xB7 \u5168 {count} \u4EF6",
  historyTimeRangeAll: "\u5168\u671F\u9593",
  historyTimeRange1d: "\u904E\u53BB 1 \u65E5",
  historyTimeRange3d: "\u904E\u53BB 3 \u65E5",
  historyTimeRange1w: "\u904E\u53BB 1 \u9031\u9593",
  historyTimeRange1m: "\u904E\u53BB 1 \u304B\u6708",
  historyIngestTotal: "\u30DA\u30FC\u30B8\u7DCF\u6570",
  historyIngestByType: "\u30BF\u30A4\u30D7\u5225",
  historyIngestSource: "\u30BD\u30FC\u30B9\u30D5\u30A1\u30A4\u30EB",
  historyIngestNoTimestamp: "\u30BF\u30A4\u30E0\u30B9\u30BF\u30F3\u30D7\u306A\u3057",
  historyIngestFirstTime: "\u6700\u521D\u306E\u53D6\u308A\u8FBC\u307F",
  historyIngestLatestTime: "\u6700\u65B0\u306E\u53D6\u308A\u8FBC\u307F",
  historyTimeRangeCustom: "\u30AB\u30B9\u30BF\u30E0\u7BC4\u56F2",
  historyCustomRangeFrom: "\u958B\u59CB",
  historyCustomRangeTo: "\u7D42\u4E86",
  historyCustomRangeApply: "\u9069\u7528",
  historyCustomRangeClear: "\u30AF\u30EA\u30A2"
};

// src/texts/ko.ts
var KO_TEXTS = {
  // Plugin Info
  pluginTitle: "Karpathy LLM Wiki \uC124\uC815",
  pluginIntro: "\uC774 \uD50C\uB7EC\uADF8\uC778\uC740 Obsidian\uC5D0 {{link}} \uAC1C\uB150\uC744 \uAD6C\uD604\uD569\uB2C8\uB2E4. \uB178\uD2B8\uB97C \uC77D\uACE0 AI\uB85C \uC5D4\uD2F0\uD2F0\uC640 \uCEE8\uC149\uC744 \uCD94\uCD9C\uD558\uC5EC \uBCFC\uD2B8\uC5D0 \uAD6C\uC870\uD654\uB41C \uC704\uD0A4\uB97C \uAD6C\uCD95\uD569\uB2C8\uB2E4.",
  karpathyLinkText: "Andrej Karpathy\uC758 LLM Wiki",
  // Features Section
  featuresTitle: "\uC791\uB3D9 \uBC29\uC2DD",
  workflow1Title: "1. \uC218\uC9D1",
  workflow1Desc: "\uB178\uD2B8\uB97C \uC120\uD0DD\uD558\uBA74 AI\uAC00 \uC5D4\uD2F0\uD2F0, \uCEE8\uC149, \uAD00\uACC4\uB97C \uC704\uD0A4 \uD398\uC774\uC9C0\uB85C \uCD94\uCD9C\uD569\uB2C8\uB2E4.",
  workflow2Title: "2. \uC9C8\uC758",
  workflow2Desc: "\uC704\uD0A4\uC640 \uB300\uD654\uD558\uC138\uC694 \u2014 \uB2F5\uBCC0\uC740 \uC218\uC9D1\uB41C \uC9C0\uC2DD\uC5D0 \uAE30\uBC18\uD569\uB2C8\uB2E4.",
  workflow3Title: "3. \uC720\uC9C0",
  workflow3Desc: "\uC790\uB3D9 \uC0DD\uC131\uB41C \uC778\uB371\uC2A4, \uC0C1\uD638 \uB9C1\uD06C, \uB9B0\uD2B8 \uAC80\uC0AC\uB85C \uC704\uD0A4\uB97C \uAC74\uAC15\uD558\uAC8C \uC720\uC9C0\uD569\uB2C8\uB2E4.",
  // Language Switcher
  languageTitle: "\uC778\uD130\uD398\uC774\uC2A4 \uC5B8\uC5B4",
  languageDesc: "\uC124\uC815 \uD328\uB110\uC5D0\uC11C \uC0AC\uC6A9\uD560 \uC5B8\uC5B4\uB97C \uC120\uD0DD\uD558\uC138\uC694. \uC5B8\uC5B4 \uBCC0\uACBD\uC744 \uBA85\uB839 \uD314\uB808\uD2B8\uC5D0 \uC801\uC6A9\uD558\uB824\uBA74 \uD50C\uB7EC\uADF8\uC778\uC744 \uC7AC\uC2DC\uC791\uD558\uC138\uC694.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "LLM \uD074\uB77C\uC774\uC5B8\uD2B8 \uC0C1\uD0DC",
  llmWikiStatusSection: "LLM-Wiki \uC0C1\uD0DC",
  statusInitialized: "\uCD08\uAE30\uD654\uB428",
  statusReady: "LLM \uC900\uBE44 \uC644\uB8CC",
  statusNotReady: "LLM \uBBF8\uAD6C\uC131 \u2014 \uC704 \uC124\uC815\uC744 \uC644\uB8CC\uD558\uACE0 \uC5F0\uACB0 \uD14C\uC2A4\uD2B8\uB97C \uD1B5\uACFC\uD558\uC138\uC694",
  statusNotInitialized: "\uCD08\uAE30\uD654\uB418\uC9C0 \uC54A\uC74C",
  currentProvider: "\uD604\uC7AC Provider",
  // Provider Configuration
  providerSection: "LLM \uC124\uC815",
  providerName: "LLM Provider",
  providerDesc: "\uC0AC\uC804 \uC815\uC758\uB41C Provider \uB610\uB294 \uC0AC\uC6A9\uC790 \uC815\uC758 OpenAI \uD638\uD658 \uC11C\uBE44\uC2A4 \uC120\uD0DD. Coding Plan \uB4F1\uC758 \uBC88\uB4E4 \uACBD\uC6B0, \uC0AC\uC6A9\uC790 \uC815\uC758 OpenAI/Anthropic \uC120\uD0DD \uD6C4 Provider\uC758 Base URL\uACFC API Key \uC218\uB3D9 \uC785\uB825",
  // API Key
  apiKeyName: "API \uD0A4",
  apiKeyDesc: "Provider\uC758 API \uD0A4\uB97C \uC785\uB825\uD558\uC138\uC694",
  apiKeyPlaceholder: "API \uD0A4 \uC785\uB825",
  // Base URL
  baseUrlName: "API Base URL",
  baseUrlDescCustom: "\uD544\uC218: \uC0AC\uC6A9\uC790 \uC815\uC758 OpenAI \uD638\uD658 \uC5D4\uB4DC\uD3EC\uC778\uD2B8",
  baseUrlDescOverride: "\uC120\uD0DD: preset Base URL \uC7AC\uC815\uC758",
  // Ollama Hint
  ollamaHint: "Ollama\uB294 \uB85C\uCEEC\uC5D0\uC11C \uC2E4\uD589\uB418\uBA70 API \uD0A4\uAC00 \uD544\uC694\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4",
  lmstudioHint: "LM Studio\uB294 \uB85C\uCEEC\uC5D0\uC11C \uC2E4\uD589\uB429\uB2C8\uB2E4. API \uD0A4\uB294 \uC120\uD0DD \uC0AC\uD56D\uC785\uB2C8\uB2E4",
  maxTokensPerCallName: "\uCEE8\uD14D\uC2A4\uD2B8 \uCC3D",
  maxTokensPerCallDesc: "\uCEE8\uD14D\uC2A4\uD2B8 \uCC3D\uC5D0 \uB9DE\uAC8C \uD1A0\uD070 \uC81C\uD55C. 0 = \uC81C\uD55C \uC5C6\uC74C(\uD074\uB77C\uC6B0\uB4DC \uAE30\uBCF8\uAC12).",
  slugCaseName: "\uD30C\uC77C\uBA85 \uB300\uC18C\uBB38\uC790",
  slugCaseDesc: '\uC0DD\uC131\uB418\uB294 Wiki \uD30C\uC77C\uBA85\uC744 \uC18C\uBB38\uC790\uB85C \uBCC0\uD658\uD560\uC9C0 \uC81C\uC5B4\uD569\uB2C8\uB2E4. \uB300\uC18C\uBB38\uC790\uAC00 \uC758\uBBF8 \uC788\uB294 \uC5B8\uC5B4(\uC608: \uB3C5\uC77C\uC5B4 \uBA85\uC0AC)\uB294 "\uC720\uC9C0" \uAD8C\uC7A5.',
  slugCaseLower: "\uC18C\uBB38\uC790(\uAE30\uBCF8\uAC12)",
  slugCasePreserve: "\uB300\uC18C\uBB38\uC790 \uC720\uC9C0",
  // Model Selection
  modelSection: "\uBAA8\uB378 \uC120\uD0DD",
  fetchModelsName: "\uC0AC\uC6A9 \uAC00\uB2A5\uD55C \uBAA8\uB378 \uAC00\uC838\uC624\uAE30",
  fetchModelsDesc: "Provider API\uC5D0\uC11C \uCD5C\uC2E0 \uBAA8\uB378 \uBAA9\uB85D\uC744 \uAC00\uC838\uC635\uB2C8\uB2E4",
  fetchModelsButton: "\uBAA8\uB378 \uAC00\uC838\uC624\uAE30",
  fetchingModels: "\uAC00\uC838\uC624\uB294 \uC911...",
  fetchSuccess: "\uC131\uACF5! {}\uAC1C\uC758 \uBAA8\uB378 \uC0AC\uC6A9 \uAC00\uB2A5",
  fetchFailed: "\uAC00\uC838\uC624\uAE30 \uC2E4\uD328 \uB610\uB294 \uBE48 \uBAA9\uB85D. \uBAA8\uB378 \uC774\uB984\uC744 \uC218\uB3D9\uC73C\uB85C \uC785\uB825\uD558\uC138\uC694",
  fetchNotSupported: "Provider\uAC00 \uBAA8\uB378 \uBAA9\uB85D \uC870\uD68C\uB97C \uC9C0\uC6D0\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4",
  fetchErrorAuth: "\uC778\uC99D \uC2E4\uD328 (HTTP 401/403). API \uD0A4\uB97C \uD655\uC778\uD558\uAC70\uB098 \uBAA8\uB378 ID\uB97C \uC9C1\uC811 \uC785\uB825\uD55C \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0\uC744 \uD074\uB9AD\uD558\uC5EC \uAC80\uC99D\uD558\uC138\uC694.",
  fetchErrorEndpoint: "\uC5D4\uB4DC\uD3EC\uC778\uD2B8\uB97C \uCC3E\uC744 \uC218 \uC5C6\uC74C (HTTP 404). BaseURL\uC744 \uD655\uC778\uD558\uAC70\uB098 \uBAA8\uB378 ID\uB97C \uC9C1\uC811 \uC785\uB825\uD55C \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0\uC744 \uD074\uB9AD\uD558\uC5EC \uAC80\uC99D\uD558\uC138\uC694.",
  fetchErrorServer: "Provider \uC11C\uBC84 \uC624\uB958 (HTTP 5xx). \uB098\uC911\uC5D0 \uB2E4\uC2DC \uC2DC\uB3C4\uD558\uAC70\uB098 \uBAA8\uB378 ID\uB97C \uC9C1\uC811 \uC785\uB825\uD55C \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0\uC744 \uD074\uB9AD\uD558\uC5EC \uAC80\uC99D\uD558\uC138\uC694.",
  fetchErrorEmpty: "\uC774 \uC5D4\uB4DC\uD3EC\uC778\uD2B8\uB294 \uBAA8\uB378 \uBAA9\uB85D\uC744 \uC81C\uACF5\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4. \uBAA8\uB378 ID\uB97C \uC9C1\uC811 \uC785\uB825\uD55C \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0\uC744 \uD074\uB9AD\uD558\uC5EC \uAC80\uC99D\uD558\uC138\uC694.",
  fetchErrorNetwork: "\uB124\uD2B8\uC6CC\uD06C \uC694\uCCAD \uC2E4\uD328. \uC778\uD130\uB137 \uC5F0\uACB0, BaseURL \uB610\uB294 \uD504\uB85D\uC2DC \uC124\uC815\uC744 \uD655\uC778\uD558\uC138\uC694. \uBAA8\uB378 ID\uB97C \uC9C1\uC811 \uC785\uB825\uD55C \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0\uC744 \uD074\uB9AD\uD558\uC5EC \uAC80\uC99D\uD560 \uC218\uB3C4 \uC788\uC2B5\uB2C8\uB2E4.",
  selectModelName: "\uBAA8\uB378 \uC120\uD0DD",
  selectModelDesc: "{}\uAC1C\uC758 \uC0AC\uC6A9 \uAC00\uB2A5\uD55C \uBAA8\uB378 \uC911\uC5D0\uC11C \uC120\uD0DD",
  customInputOption: "\uC9C1\uC811 \uC785\uB825...",
  customInputHint: '\uB2E4\uB978 \uBAA8\uB378\uC744 \uC0AC\uC6A9\uD558\uB824\uBA74 "\uC9C1\uC811 \uC785\uB825..."\uC744 \uC120\uD0DD\uD558\uC138\uC694',
  modelName: "\uBAA8\uB378 \uC774\uB984",
  modelDescCustom: "\uC0AC\uC6A9\uC790 \uC815\uC758 \uBAA8\uB378 \uC0AC\uC6A9 (\uC704 \uBC84\uD2BC\uC73C\uB85C \uBAA9\uB85D \uB2E4\uC2DC \uAC00\uC838\uC624\uAE30)",
  modelDescFetchFailed: "\uBAA8\uB378 \uBAA9\uB85D \uAC00\uC838\uC624\uAE30 \uC2E4\uD328. API \uD0A4\uC640 \uC5D4\uB4DC\uD3EC\uC778\uD2B8 URL\uC744 \uD655\uC778\uD558\uAC70\uB098 \uBAA8\uB378 ID\uB97C \uC9C1\uC811 \uC785\uB825\uD55C \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0\uC744 \uD074\uB9AD\uD558\uC5EC \uAC80\uC99D\uD558\uC138\uC694.",
  modelInputPlaceholder: "\uBAA8\uB378 ID \uC785\uB825 \uD6C4 \uD14C\uC2A4\uD2B8 \uC5F0\uACB0",
  switchToDropdown: "\uB4DC\uB86D\uB2E4\uC6B4 \uC120\uD0DD\uC73C\uB85C \uC804\uD658",
  useDropdownButton: "\uB4DC\uB86D\uB2E4\uC6B4 \uC0AC\uC6A9",
  // Test & Save
  testConnectionName: "\uC5F0\uACB0 \uD14C\uC2A4\uD2B8",
  testConnectionDesc: "\uC124\uC815\uC774 LLM API\uB97C \uC131\uACF5\uC801\uC73C\uB85C \uD638\uCD9C\uD560 \uC218 \uC788\uB294\uC9C0 \uD655\uC778\uD569\uB2C8\uB2E4",
  testButton: "\uC5F0\uACB0 \uD14C\uC2A4\uD2B8",
  testing: "\uD14C\uC2A4\uD2B8 \uC911...",
  saveSettingsName: "\uC124\uC815 \uC800\uC7A5",
  saveSettingsDesc: "\uD604\uC7AC \uC124\uC815\uC744 \uC800\uC7A5\uD569\uB2C8\uB2E4",
  saveButton: "\uC124\uC815 \uC800\uC7A5",
  testConnectionSuccessful: "\uC5F0\uACB0 \uC131\uACF5",
  testConnectionFailed: "\uC5F0\uACB0 \uC2E4\uD328",
  testConnectionProvider: "Provider: ",
  errorUnknown: "\uC54C \uC218 \uC5C6\uB294 \uC624\uB958",
  savedNotice: "\uC124\uC815\uC774 \uC800\uC7A5\uB418\uC5C8\uC2B5\uB2C8\uB2E4!",
  // Issue #137: LLM 폴백 알림
  fallbackThinkingDialect: '\uC0AC\uACE0 \uC81C\uC5B4: "{dialect}" \uBC29\uC5B8\uC73C\uB85C \uC804\uD658\uB428(\uC774 \uACF5\uAE09\uC790\uB294 \uB2E4\uB978 \uC0AC\uACE0 \uC81C\uC5B4 \uD544\uB4DC \uD615\uC2DD\uC744 \uC0AC\uC6A9\uD568). \uCD9C\uB825\uC740 \uBCC0\uACBD\uB418\uC9C0 \uC54A\uC74C.',
  fallbackThinkingNone: "\uC774 \uACF5\uAE09\uC790\uC5D0 \uB300\uD574 \uC0AC\uACE0 \uC81C\uC5B4\uAC00 \uC644\uC804\uD788 \uBE44\uD65C\uC131\uD654\uB418\uC5C8\uC2B5\uB2C8\uB2E4. \uCD94\uB860 \uCF58\uD150\uCE20\uAC00 \uC5EC\uC804\uD788 \uB098\uD0C0\uB0A0 \uC218 \uC788\uC2B5\uB2C8\uB2E4; \uC774 \uACBD\uC6B0 \uB2E4\uB978 \uBAA8\uB378\uC744 \uC2DC\uB3C4\uD558\uC138\uC694.",
  fallbackParamStripped: '\uB9E4\uAC1C\uBCC0\uC218 "{field}"\uB294(\uC740) \uC774 \uACF5\uAE09\uC790\uC5D0\uC11C \uC9C0\uC6D0\uB418\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4. \uC694\uCCAD\uC5D0\uC11C \uC81C\uAC70\uB418\uC5C8\uC2B5\uB2C8\uB2E4; \uB3D9\uC791\uC740 \uAD6C\uC131\uACFC \uB2E4\uB97C \uC218 \uC788\uC2B5\uB2C8\uB2E4.',
  // Wiki Folder
  wikiSection: "Wiki \uC124\uC815",
  wikiFolderName: "\uC704\uD0A4 \uD3F4\uB354",
  wikiFolderDesc: "\uC0DD\uC131\uB41C \uC704\uD0A4 \uD398\uC774\uC9C0\uC758 \uC800\uC7A5 \uC704\uCE58",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "API \uD0A4\uB97C \uBA3C\uC800 \uC124\uC815\uD558\uC138\uC694",
  llmNotReady: "LLM\uC774 \uAD6C\uC131\uB418\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4. \uC124\uC815 \u2192 Karpathy LLM Wiki\uC5D0\uC11C \uC81C\uACF5\uC790\uB97C \uAD6C\uC131\uD558\uACE0 \uBAA8\uB378\uC744 \uAC00\uC838\uC628 \uD6C4 \uC5F0\uACB0 \uD14C\uC2A4\uD2B8\uB97C \uD1B5\uACFC\uD558\uC138\uC694.",
  errorFetchFailed: "\uC2E4\uD328: {}",
  // Query Settings
  querySectionTitle: "\uC704\uD0A4 \uC9C8\uC758 \uC124\uC815",
  maxConversationHistoryName: "\uCD5C\uB300 \uB300\uD654 \uAE30\uB85D",
  maxConversationHistoryDesc: "\uD1A0\uD070 \uCD08\uACFC\uB97C \uBC29\uC9C0\uD558\uAE30 \uC704\uD574 \uB300\uD654 \uBA54\uC2DC\uC9C0\uB97C \uC81C\uD55C\uD569\uB2C8\uB2E4",
  maxConversationHistoryHint: "\uCD94\uCC9C: 50 \uB77C\uC6B4\uB4DC\uB97C \uCD08\uACFC\uD558\uC9C0 \uC54A\uAE30",
  numberRangeValidation: "1-50 \uC0AC\uC774\uC758 \uC22B\uC790\uB97C \uC785\uB825\uD558\uC138\uC694",
  numberRangeClamped: "\uAC12\uC774 \uBC94\uC704\uB97C \uCD08\uACFC\uD588\uC2B5\uB2C8\uB2E4 (1-500), \uC790\uB3D9\uC73C\uB85C {}\uC73C\uB85C \uC124\uC815\uB418\uC5C8\uC2B5\uB2C8\uB2E4",
  // Query Modal UI
  queryModalTitle: "\uC704\uD0A4 \uC9C8\uC758 - \uB300\uD654\uD615 \uC9C8\uC758",
  queryModalPlaceholder: "\uC9C8\uBB38\uC744 \uC785\uB825\uD558\uC138\uC694...",
  queryModalSendButton: "\uC804\uC1A1",
  queryModalStopButton: "\uC911\uC9C0",
  queryModalSaveButton: "\uC704\uD0A4\uC5D0 \uC800\uC7A5",
  queryModalClearButton: "\uAE30\uB85D \uC9C0\uC6B0\uAE30",
  queryModalHistoryCount: "\uB300\uD654 \uAE30\uB85D: {}/{} \uB77C\uC6B4\uB4DC",
  queryModalStreaming: "\uC2A4\uD2B8\uB9AC\uBC0D \uC911...",
  queryModalFallbackStreaming: "\uC2A4\uD2B8\uB9AC\uBC0D\uC744 \uC9C0\uC6D0\uD558\uC9C0 \uC54A\uC544 \uB17C\uC2A4\uD2B8\uB9AC\uBC0D\uC73C\uB85C \uC804\uD658\uB418\uC5C8\uC2B5\uB2C8\uB2E4. \uC7A0\uC2DC \uAE30\uB2E4\uB824\uC8FC\uC138\uC694...",
  queryPhaseSearching: "\uC704\uD0A4 \uC778\uB371\uC2A4 \uBD84\uC11D \uC911, \uAD00\uB828 \uD398\uC774\uC9C0 \uC120\uD0DD \uC911...",
  queryPhaseFoundPages: "{count}\uAC1C\uC758 \uD398\uC774\uC9C0 \uBC1C\uACAC: {pages}",
  queryPhaseLoadingPages: "\uD398\uC774\uC9C0 \uCF58\uD150\uCE20 \uB85C\uB529 \uC911...",
  queryPhaseContextReady: "\uCEE8\uD14D\uC2A4\uD2B8 \uC900\uBE44 \uC644\uB8CC. \uB2F5\uBCC0 \uC0DD\uC131 \uC911...",
  queryPhaseGenerating: "\uC0DD\uC131 \uC911... (\uACBD\uACFC {time}\uCD08)",
  queryPhaseNonStreaming: "\uB17C\uC2A4\uD2B8\uB9AC\uBC0D \uBAA8\uB4DC, \uC751\uB2F5 \uC0DD\uC131 \uC911... (\uACBD\uACFC {time}\uCD08)",
  queryModalErrorPrefix: "\uC624\uB958: ",
  queryModalHint: '\uC704\uD0A4 \uCF58\uD150\uCE20 \uAE30\uBC18 \uC9C8\uC758\uC785\uB2C8\uB2E4. "\uC704\uD0A4\uC5D0 \uC800\uC7A5"\uC744 \uD074\uB9AD\uD558\uBA74 \uAC00\uCE58 \uC788\uB294 \uB300\uD654\uB97C \uC704\uD0A4 \uD398\uC774\uC9C0\uB85C \uCD94\uCD9C\uD560 \uC218 \uC788\uC2B5\uB2C8\uB2E4.',
  // v1.20.0: Query Wiki 사고 블록 접기 요약
  queryThinkingSummary: "\uC0AC\uACE0 \uACFC\uC815",
  queryThinkingSteps: "\uB2E8\uACC4",
  // Error Messages
  errorLLMClientNotInit: "LLM \uD074\uB77C\uC774\uC5B8\uD2B8\uAC00 \uCD08\uAE30\uD654\uB418\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4. \uC124\uC815\uC744 \uC800\uC7A5\uD558\uC138\uC694.",
  errorIngestFailed: "\uC218\uC9D1 \uC2E4\uD328: ",
  errorQueryFailed: "\uC9C8\uC758 \uC2E4\uD328: ",
  fileWriteFailed: "\uD30C\uC77C\uC744 \uB9CC\uB4E4\uAC70\uB098 \uC5C5\uB370\uC774\uD2B8\uD560 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4: {path}",
  // Success Messages
  ingestSuccess: "\uC218\uC9D1 \uC644\uB8CC: {} \uD398\uC774\uC9C0 \uC0DD\uC131, {} \uD398\uC774\uC9C0 \uC5C5\uB370\uC774\uD2B8",
  querySuccess: "\uC9C8\uC758 \uC644\uB8CC",
  lintSuccess: "\uC704\uD0A4 \uB9B0\uD2B8 \uC644\uB8CC",
  lintReadingPages: "{count}\uAC1C\uC758 \uC704\uD0A4 \uD398\uC774\uC9C0 \uC77D\uB294 \uC911...",
  lintReadingPagesProgress: "\uC704\uD0A4 \uD398\uC774\uC9C0 \uC77D\uB294 \uC911: {current}/{total}...",
  lintScanningLinks: "\uAE68\uC9C4 \uB9C1\uD06C \uAC80\uC0AC \uC911...",
  lintScanningLinksProgress: "\uAE68\uC9C4 \uB9C1\uD06C \uAC80\uC0AC \uC911: {current}/{total}...",
  lintCheckingDuplicates: "\uC911\uBCF5 \uD398\uC774\uC9C0 \uD655\uC778 \uC911...",
  lintCheckingDuplicatesProgress: "\uC911\uBCF5 \uD398\uC774\uC9C0 \uD655\uC778 \uC911: \uBC30\uCE58 {current} ...",
  lintFixingPolluted: "\uC624\uC5FC \uD398\uC774\uC9C0 \uC218\uC815 \uC911 {current}/{total}: {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} \uC624\uC5FC \uD398\uC774\uC9C0 \uC218\uC815 ({count})",
  lintDuplicateCheckFailed: "\uC911\uBCF5 \uAC10\uC9C0 \uC2E4\uD328 \u2014 \uC790\uC138\uD55C \uB0B4\uC6A9\uC740 \uCF58\uC194\uC744 \uD655\uC778\uD558\uC138\uC694",
  lintDuplicateCheckFailedDetail: "\uC911\uBCF5 \uD655\uC778 \uC2E4\uD328 ({step}): {error}",
  lintMergeItemFailed: "\uBCD1\uD569 \uC2E4\uD328: {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "\uBCC4\uCE6D \uB204\uB77D: {count}\uAC1C\uC758 \uD398\uC774\uC9C0\uC5D0 \uBCC4\uCE6D\uC774 \uC5C6\uC2B5\uB2C8\uB2E4",
  lintAliasesSection: "\uBCC4\uCE6D \uB204\uB77D \uD398\uC774\uC9C0 [{count}\uAC1C]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "\uBCC4\uCE6D \uC644\uC131 ({count})",
  lintAliasesFilling: "\uBCC4\uCE6D \uC0DD\uC131 {current}/{total}: {page}",
  lintAliasesFilled: "\uBCC4\uCE6D \uC644\uC131 \uC644\uB8CC. {filled}/{total} \uD398\uC774\uC9C0 \uCC44\uC6C0.",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "\uC5B4\uD718 \uC678 \uD0DC\uADF8 (\uAC10\uC9C0\uB428) [{count}\uAC1C]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "\uBCC4\uCE6D \uC0DD\uC131 \uC2E4\uD328: {page} \u2014 {error}",
  lintFixItemFailed: "\uC218\uC815 \uC2E4\uD328: [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "\uB9C1\uD06C \uC2E4\uD328: {page} \u2014 {error}",
  lintRetrying: "\uC624\uB958 \uD6C4 \uC7AC\uC2DC\uB3C4 ({attempt}/{max})...",
  lintAnalyzingLLM: "LLM\uC774 \uC704\uD0A4 \uAC74\uAC15 \uC0C1\uD0DC \uBD84\uC11D \uC911...",
  saveToWikiSuccess: "\uB300\uD654\uAC00 \uC704\uD0A4\uC5D0 \uC800\uC7A5\uB418\uC5C8\uC2B5\uB2C8\uB2E4!",
  saveSummary: "{entities} \uAC1C\uCCB4, {concepts} \uAC1C\uB150, {pages} \uD398\uC774\uC9C0",
  aliasAdded: "\uD398\uC774\uC9C0 '{page}'\uC5D0 \uBCC4\uCE6D '{alias}' \uCD94\uAC00\uB428",
  // Status Messages
  ingestingSources: "\uC18C\uC2A4 \uC218\uC9D1 \uC911...",
  queryingWiki: "\uC704\uD0A4 \uC9C8\uC758 \uC911...",
  lintingWiki: "\uC704\uD0A4 \uB9B0\uD2B8 \uC911...",
  savingToWiki: "\uB300\uD654\uB97C \uC704\uD0A4\uC5D0 \uC800\uC7A5 \uC911...",
  clearingHistory: "\uB300\uD654 \uAE30\uB85D \uC9C0\uC6B0\uB294 \uC911...",
  // Buttons
  ingestButton: "\uC218\uC9D1",
  queryButton: "\uC9C8\uC758",
  lintButton: "\uB9B0\uD2B8",
  cancelButton: "\uCDE8\uC18C",
  // Links
  karpathyGistLink: "Karpathy\uC758 LLM Wiki Gist",
  obsidianPluginAPI: "Obsidian \uD50C\uB7EC\uADF8\uC778 API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // Other
  availableModelsLoading: "\uC0AC\uC6A9 \uAC00\uB2A5\uD55C \uBAA8\uB378 \uB85C\uB529 \uC911...",
  noModelsAvailable: "\uC0AC\uC6A9 \uAC00\uB2A5\uD55C \uBAA8\uB378 \uC5C6\uC74C",
  // LLM Language Hint
  llmLanguageHint: "Please answer in Korean.",
  // Schema Configuration
  schemaSection: "\uC2A4\uD0A4\uB9C8 \uC124\uC815",
  enableSchemaName: "\uC2A4\uD0A4\uB9C8 \uD65C\uC131\uD654",
  enableSchemaDesc: "\uC2A4\uD0A4\uB9C8/config.md\uB97C \uC0DD\uC131\uD558\uC5EC \uBAA8\uB4E0 LLM \uD504\uB86C\uD504\uD2B8\uC5D0 \uC8FC\uC785, \uAD6C\uC870\uD654\uB41C \uC704\uD0A4 \uCD9C\uB825 \uC9C0\uC6D0",
  viewSchemaButton: "\uC2A4\uD0A4\uB9C8 \uBCF4\uAE30 / \uD3B8\uC9D1",
  regenerateSchemaButton: "\uAE30\uBCF8 \uC2A4\uD0A4\uB9C8 \uC7AC\uC0DD\uC131",
  schemaRegeneratedNotice: "\uAE30\uBCF8 \uC2A4\uD0A4\uB9C8\uAC00 \uC7AC\uC0DD\uC131\uB418\uC5C8\uC2B5\uB2C8\uB2E4.",
  schemaRegenerateFailed: "\uC2A4\uD0A4\uB9C8 \uC0DD\uC131 \uC2E4\uD328",
  // Wiki 초기화 상태
  wikiInitStatusReady: "Wiki \uCD08\uAE30\uD654 \uC644\uB8CC",
  wikiInitStatusNotReady: "Wiki \uCD08\uAE30\uD654\uB418\uC9C0 \uC54A\uC74C \u2014 \uCCAB \uBC88\uC9F8 \uC218\uC9D1 \uC2DC \uC790\uB3D9 \uC0DD\uC131",
  // Wiki Output Language
  wikiLanguageName: "\uC704\uD0A4 \uCD9C\uB825 \uC5B8\uC5B4",
  wikiLanguageDesc: "\uC0DD\uC131\uB41C \uBAA8\uB4E0 \uC704\uD0A4 \uD398\uC774\uC9C0, \uC778\uB371\uC2A4, \uB85C\uADF8, \uC9C8\uC758 \uC751\uB2F5\uC5D0 \uC774 \uC5B8\uC5B4\uAC00 \uC0AC\uC6A9\uB429\uB2C8\uB2E4",
  customWikiLanguageOption: "\uC0AC\uC6A9\uC790 \uC815\uC758...",
  customWikiLanguageHint: "\uC5B8\uC5B4 \uC774\uB984\uC744 \uC785\uB825\uD558\uC138\uC694 (\uC608: Italian, Arabic). LLM\uC5D0 \uCD9C\uB825 \uC5B8\uC5B4 \uC9C0\uC2DC\uBB38\uC73C\uB85C \uC804\uB2EC\uB429\uB2C8\uB2E4.",
  customWikiLanguagePlaceholder: "\uC608: Italian",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "\uCD94\uCD9C",
  extractionGranularityName: "\uCD94\uCD9C \uC138\uBD84\uD654",
  extractionGranularityDesc: "\uC18C\uC2A4 \uD30C\uC77C\uB2F9 \uCD94\uCD9C \uC218 \uC81C\uC5B4. \uB192\uC744\uC218\uB85D \uD398\uC774\uC9C0/API \uC18C\uBE44 \uC99D\uAC00.\n\uC138\uBC00: \uC2EC\uCE35 \uBD84\uC11D. \uD45C\uC900: \uC77C\uC0C1 \uB178\uD2B8. \uAD75\uC74C: \uBE60\uB978 \uAC1C\uC694. \uCD5C\uC18C: 100\uAC1C \uC774\uC0C1 \uC77C\uAD04 \uCC98\uB9AC. \uC0AC\uC6A9\uC790 \uC9C0\uC815: \uC790\uCCB4 \uC124\uC815 (\uCD5C\uB300 300\uAC1C).\n\uD301: \uD30C\uC77C\uC774 \uB9CE\uC740 \uD3F4\uB354\uB294 \uCD5C\uC18C/\uAD75\uC74C \uC0AC\uC6A9\uC73C\uB85C \uC2DC\uAC04 \uBC0F \uBE44\uC6A9 \uC808\uAC10.",
  extractionGranularityFine: "\uC138\uBC00 \u2014 \uC2EC\uCE35 \uBD84\uC11D (\u2264100\uAC1C)",
  extractionGranularityStandard: "\uD45C\uC900 \u2014 \uC77C\uC0C1 \uB178\uD2B8 (\u226450\uAC1C)",
  extractionGranularityCoarse: "\uAD75\uC74C \u2014 \uBE60\uB978 \uAC1C\uC694 (\u226410\uAC1C)",
  extractionGranularityMinimal: "\uCD5C\uC18C \u2014 100+ \uD30C\uC77C \uC77C\uAD04 \uCC98\uB9AC (\u22645\uAC1C)",
  extractionGranularityCustom: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 \u2014 \uC790\uCCB4 \uC0C1\uD55C \uC124\uC815 (1~500)",
  customEntityLimitName: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 \uC5D4\uD2F0\uD2F0 \uC0C1\uD55C",
  customEntityLimitDesc: "\uC18C\uC2A4 \uD30C\uC77C\uB2F9 \uCD5C\uB300 \uC5D4\uD2F0\uD2F0 \uCD94\uCD9C \uC218 (1-500)",
  customConceptLimitName: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 \uCEE8\uC149 \uC0C1\uD55C",
  customConceptLimitDesc: "\uC18C\uC2A4 \uD30C\uC77C\uB2F9 \uCD5C\uB300 \uCEE8\uC149 \uCD94\uCD9C \uC218 (1-500)",
  // Issue #85 v2: 태그 어휘 (chip 입력 UI, Wiki 설정에 임베드)
  tagVocabularyInlineDesc: 'entity/concept frontmatter \uD0DC\uADF8\uC758 \uAD00\uB9AC \uC5B4\uD718\uC785\uB2C8\uB2E4. "/"\uAC00 \uC788\uB294 \uC911\uCCA9 \uD0DC\uADF8\uB294 \uBCF4\uC874\uB429\uB2C8\uB2E4.',
  tagVocabularyModeName: "\uD0DC\uADF8 \uC5B4\uD718 \uC124\uC815",
  tagVocabularyModeDescDefault: '\uAE30\uBCF8\uAC12\uC740 \uB0B4\uC7A5 \uD0DC\uADF8 \uC0AC\uC6A9: {}. "\uC0AC\uC6A9\uC790 \uC9C0\uC815"\uC73C\uB85C \uC804\uD658\uD558\uBA74 \uC790\uCCB4 \uC5B4\uD718\uB97C \uC815\uC758\uD560 \uC218 \uC788\uC2B5\uB2C8\uB2E4.',
  tagVocabularyModeDescCustom: "\uC0AC\uC6A9\uC790 \uC9C0\uC815: \uC544\uB798\uC5D0\uC11C chip \uC785\uB825\uC744 \uC0AC\uC6A9\uD574 \uC790\uCCB4 entity/concept \uD0DC\uADF8\uB97C \uC815\uC758\uD558\uC138\uC694. Enter \uB610\uB294 \uC27C\uD45C\uB85C \uCD94\uAC00, \xD7\uB85C \uC0AD\uC81C.",
  tagVocabularyModeDefault: "\uAE30\uBCF8 (\uB0B4\uC7A5 \uC11C\uBE0C\uD0C0\uC785 \uD0DC\uADF8)",
  tagVocabularyModeCustom: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 (\uC0AC\uC6A9\uC790 \uC815\uC758)",
  customEntityTagsName: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 \uC5D4\uD2F0\uD2F0 \uD0DC\uADF8",
  customEntityTagsDesc: 'Enter \uB610\uB294 \uC27C\uD45C\uB85C chip \uCD94\uAC00, \xD7\uB85C \uC0AD\uC81C. "/"\uAC00 \uC788\uB294 \uC911\uCCA9 \uD0DC\uADF8\uB294 \uBCF4\uC874\uB429\uB2C8\uB2E4.',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 \uCEE8\uC149 \uD0DC\uADF8",
  customConceptTagsDesc: 'Enter \uB610\uB294 \uC27C\uD45C\uB85C chip \uCD94\uAC00, \xD7\uB85C \uC0AD\uC81C. "/"\uAC00 \uC788\uB294 \uC911\uCCA9 \uD0DC\uADF8\uB294 \uBCF4\uC874\uB429\uB2C8\uB2E4.',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "\uC911\uBCF5 \uD0DC\uADF8 \uAC74\uB108\uB700",
  // Ingestion Acceleration
  accelerationSectionTitle: "\uC218\uC9D1 \uAC00\uC18D\uD654",
  pageGenerationConcurrencyName: "LLM \uB3D9\uC2DC\uC131",
  pageGenerationConcurrencyDesc: "\uC218\uC9D1 \uBC0F Lint \uC791\uC5C5 \uC911 \uBCD1\uB82C LLM \uD638\uCD9C \uC218. \uAC12\uC774 \uB192\uC744\uC218\uB85D \uCC98\uB9AC\uAC00 \uBE68\uB77C\uC9C0\uC9C0\uB9CC API \uBE44\uC6A9\uC774 \uC99D\uAC00\uD558\uACE0 \uC18D\uB3C4 \uC81C\uD55C\uC774 \uBC1C\uC0DD\uD560 \uC218 \uC788\uC2B5\uB2C8\uB2E4.",
  concurrencyValueSingular: "\uD604\uC7AC \uB3D9\uC2DC\uC131: {} (\uC9C1\uB82C \u2014 \uAC00\uC7A5 \uC548\uC804)",
  concurrencyValuePlural: "\uD604\uC7AC \uB3D9\uC2DC\uC131: {} (\uBCD1\uB82C)",
  batchDelayName: "\uBC30\uCE58 \uC9C0\uC5F0 (ms)",
  batchDelayDesc: "API \uC18D\uB3C4 \uC81C\uD55C \uBC29\uC9C0\uB97C \uC704\uD55C \uBCD1\uB82C \uBC30\uCE58 \uAC04 \uC9C0\uC5F0 (100-2000ms). \uD604\uC7AC\uAC12: {}ms. 429 \uC624\uB958\uAC00 \uBC1C\uC0DD\uD558\uBA74 \uAC12\uC744 \uB192\uC774\uC138\uC694.",
  // Auto Maintenance
  autoMaintainSection: "\uC790\uB3D9 \uC720\uC9C0\uBCF4\uC218",
  autoMaintainBetaBadge: "BETA \u2014 \uC2E4\uD5D8\uC801 \uAE30\uB2A5. \uBB38\uC81C\uAC00 \uC788\uC744 \uC218 \uC788\uC2B5\uB2C8\uB2E4. \uACE0\uAE09 \uC0AC\uC6A9\uC790\uC5D0\uAC8C\uB9CC \uAD8C\uC7A5.",
  autoWatchName: "\uD3F4\uB354 \uAC10\uC2DC",
  autoWatchDesc: "\uAC10\uC2DC \uD3F4\uB354\uC5D0\uC11C \uC0C8\uB86D\uAC70\uB098 \uBCC0\uACBD\uB41C .md \uD30C\uC77C\uC744 \uC790\uB3D9\uC73C\uB85C \uAC10\uC9C0\uD558\uC5EC \uC54C\uB9BC \uB610\uB294 \uC790\uB3D9 \uC218\uC9D1",
  watchedFoldersName: "\uAC10\uC2DC \uD3F4\uB354",
  watchedFoldersDesc: '\uC0C8 \uCF58\uD150\uCE20\uB97C \uAC10\uC2DC\uD560 \uD3F4\uB354. "\uD3F4\uB354 \uCD94\uAC00"\uB97C \uD074\uB9AD\uD558\uC5EC \uBCFC\uD2B8\uC5D0\uC11C \uC120\uD0DD\uD558\uC138\uC694.',
  addWatchedFolderButton: "\uD3F4\uB354 \uCD94\uAC00",
  removeWatchedFolderButton: "\uC81C\uAC70",
  webClipperPresetName: "\uD074\uB9AC\uD551 \uAC10\uC2DC (Web Clipper)",
  webClipperPresetDesc: "Obsidian Web Clipper\uAC00 \uC0AC\uC6A9\uD558\uB294 Clippings/ \uD3F4\uB354\uB97C \uAC10\uC2DC \uBAA9\uB85D\uC5D0 \uCD94\uAC00\uD569\uB2C8\uB2E4. \uC6F9 \uD074\uB9BD\uC774 \uC790\uB3D9\uC73C\uB85C \uC704\uD0A4\uC5D0 \uC218\uC9D1\uB429\uB2C8\uB2E4.",
  noWatchedFoldersHint: "\uC124\uC815\uB41C \uD3F4\uB354\uAC00 \uC5C6\uC2B5\uB2C8\uB2E4. \uD3F4\uB354\uB97C \uCD94\uAC00\uD558\uAC70\uB098 Clippings \uD504\uB9AC\uC14B\uC744 \uD65C\uC131\uD654\uD558\uC138\uC694.",
  autoWatchModeName: "\uAC10\uC2DC \uBAA8\uB4DC",
  autoWatchModeDesc: '"\uC54C\uB9BC\uB9CC"\uC740 \uD504\uB86C\uD504\uD2B8\uB97C \uD45C\uC2DC\uD569\uB2C8\uB2E4. "\uC790\uB3D9 \uC218\uC9D1"\uC740 \uC790\uB3D9\uC73C\uB85C \uCC98\uB9AC\uD569\uB2C8\uB2E4.',
  watchModeNotify: "\uC54C\uB9BC\uB9CC",
  watchModeAuto: "\uC790\uB3D9 \uC218\uC9D1",
  autoWatchDebounceName: "\uB514\uBC14\uC6B4\uC2A4 \uC9C0\uC5F0 (\uCD08)",
  autoWatchDebounceDesc: "\uD30C\uC77C \uBCC0\uACBD \uD6C4 \uC218\uC9D1 \uD2B8\uB9AC\uAC70\uAE4C\uC9C0 \uB300\uAE30 \uC2DC\uAC04 (1-60\uCD08)",
  periodicLintName: "\uC815\uAE30 \uB9B0\uD2B8",
  periodicLintDesc: "\uC77C\uC815\uC5D0 \uB530\uB77C LLM \uB9B0\uD2B8 \uC2E4\uD589, \uB9C8\uC9C0\uB9C9 \uD655\uC778 \uC774\uD6C4 \uC18C\uC2A4 \uD30C\uC77C\uC774 \uBCC0\uACBD\uB41C \uACBD\uC6B0\uC5D0\uB9CC",
  periodicLintOff: "\uB044\uAE30",
  periodicLintHourly: "\uB9E4\uC2DC\uAC04",
  periodicLintDaily: "\uB9E4\uC77C",
  periodicLintWeekly: "\uB9E4\uC8FC",
  autoSmartFixName: "\uC790\uB3D9 \uC2A4\uB9C8\uD2B8 \uC218\uC815",
  autoSmartFixDesc: "Lint \uC2E4\uD589 \uC2DC \uBCF4\uACE0\uC11C \uBAA8\uB2EC \uC5C6\uC774 \uBAA8\uB4E0 \uC218\uC815(\uC2A4\uB9C8\uD2B8 \uC77C\uAD04 \uC218\uC815)\uC744 \uC790\uB3D9\uC73C\uB85C \uC801\uC6A9\uD569\uB2C8\uB2E4. \uC218\uC815 \uC644\uB8CC \uD6C4 \uC694\uC57D\uC740 \uACC4\uC18D \uD45C\uC2DC\uB429\uB2C8\uB2E4.",
  autoSmartFixNotice: "\uC790\uB3D9 \uC2A4\uB9C8\uD2B8 \uC218\uC815: \uBAA8\uB4E0 \uC218\uC815 \uC801\uC6A9 \uC911...",
  startupCheckName: "\uC2DC\uC791 \uC2DC \uBE60\uB978 \uC218\uC815 \uC2E4\uD589",
  startupCheckDesc: "\uD50C\uB7EC\uADF8\uC778 \uB85C\uB529 \uC2DC\u4F4E\u7EA7 \uD615\uC2DD \uBB38\uC81C(sources \uD544\uB4DC, \uC774\uC911 wikilink)\uB97C \uC790\uB3D9 \uC218\uC815\uD558\uACE0 Wiki \uD3F4\uB354 \uAD6C\uC870\uB97C \uAC80\uC99D\uD569\uB2C8\uB2E4. \uAE30\uBCF8 \uD65C\uC131\uD654.",
  suggestSchemaCommand: "\uC2A4\uD0A4\uB9C8 \uC5C5\uB370\uC774\uD2B8 \uC81C\uC548",
  autoMaintainCostWarning: '\u26A0\uFE0F \uBE44\uC6A9 \uC54C\uB9BC: \uC790\uB3D9 \uC720\uC9C0\uBCF4\uC218 \uAE30\uB2A5\uC740 API \uD1A0\uD070\uC744 \uC18C\uBE44\uD569\uB2C8\uB2E4. "\uC790\uB3D9 \uC218\uC9D1"\uC740 \uC18C\uC2A4 \uD30C\uC77C \uBCC0\uACBD \uC2DC\uB9C8\uB2E4 LLM \uD638\uCD9C\uC744 \uD2B8\uB9AC\uAC70\uD569\uB2C8\uB2E4. "\uC815\uAE30 \uB9B0\uD2B8"\uB294 \uC77C\uC815\uC5D0 \uB530\uB77C LLM \uAC74\uAC15 \uAC80\uC0AC\uB97C \uC2E4\uD589\uD569\uB2C8\uB2E4 (\uC18C\uC2A4 \uBCC0\uACBD \uAC10\uC9C0 \uC2DC\uC5D0\uB9CC). \uC608\uC0C1\uCE58 \uBABB\uD55C \uC694\uAE08\uC744 \uD53C\uD558\uAE30 \uC704\uD574 \uC2E0\uC911\uD558\uAC8C \uC124\uC815\uD558\uC138\uC694.',
  // Notices
  startupCheckSummary: "\uC704\uD0A4\uC5D0 {pages}\uAC1C\uC758 \uD398\uC774\uC9C0\uAC00 \uC788\uC2B5\uB2C8\uB2E4 ({entities} \uC5D4\uD2F0\uD2F0, {concepts} \uCEE8\uC149, {sources} \uC18C\uC2A4)",
  watcherActiveNotice: "Wiki: \uD30C\uC77C \uAC10\uC2DC \uD65C\uC131\uD654 \u2014 \uAC10\uC2DC \uD3F4\uB354 \uBAA8\uB2C8\uD130\uB9C1 \uC911",
  watchIngestNotice: 'Wiki: sources/\uC5D0\uC11C {count}\uAC1C\uC758 \uD30C\uC77C\uC774 \uBCC0\uACBD\uB418\uC5C8\uC2B5\uB2C8\uB2E4. "\uC18C\uC2A4 \uC218\uC9D1"\uC744 \uC2E4\uD589\uD558\uC5EC \uCC98\uB9AC\uD558\uC138\uC694.',
  autoIngestRunning: "{count}\uAC1C\uC758 \uBCC0\uACBD \uD30C\uC77C \uC790\uB3D9 \uC218\uC9D1 \uC911...",
  autoIngestComplete: "\uC790\uB3D9 \uC218\uC9D1 \uC644\uB8CC: {success} \uC131\uACF5, {fail} \uC2E4\uD328",
  scheduledLintRunning: "\uC77C\uC815\uB41C \uC704\uD0A4 \uB9B0\uD2B8 \uC2E4\uD589 \uC911...",
  wikiLintStats: "\uC704\uD0A4 \uB9B0\uD2B8: {pages}\uAC1C\uC758 \uD398\uC774\uC9C0 ({entities} \uC5D4\uD2F0\uD2F0, {concepts} \uCEE8\uC149, {sources} \uC18C\uC2A4)",
  wikiHealthStats: "\uC704\uD0A4 \uAC74\uAC15: {pages}\uAC1C\uC758 \uD398\uC774\uC9C0 ({entities} \uC5D4\uD2F0\uD2F0, {concepts} \uCEE8\uC149, {sources} \uC18C\uC2A4){indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Wiki \uBE60\uB978 \uC218\uC815 \uC644\uB8CC",
  startupCheckStructureLabel: "Wiki\uAD6C\uC870",
  startupCheckStructureOk: "\uC644\uB8CC",
  startupCheckStructureMissing: "\uBD88\uC644\uC804 \u2014 \uCCAB \uC218\uC9D1 \uC2DC \uC790\uB3D9 \uC0DD\uC131",
  startupCheckSourcesLabel: "Sources \uC815\uADDC\uD654",
  startupCheckSourcesClean: "\uC774\uBBF8 \uC815\uADDC\uD654\uB428",
  startupCheckSourcesCleaned: "{files}\uAC1C \uD30C\uC77C, {entries}\uAC1C \uD56D\uBAA9 \uC815\uB9AC",
  startupCheckIncompleteClean: "\uBBF8\uC644\uC131 \uD398\uC774\uC9C0: \uC5C6\uC74C",
  startupCheckIncompleteArchived: "\uBBF8\uC644\uC131 \uD398\uC774\uC9C0: {count}\uAC1C \uC544\uCE74\uC774\uBE0C (.trash\uC5D0\uC11C \uBCF5\uC6D0 \uAC00\uB2A5)",
  startupCheckDisableHint: "\uBE44\uD65C\uC131\uD654\uD558\uB824\uBA74 \uC124\uC815 \u2192 \uC790\uB3D9 \uC720\uC9C0\uBCF4\uC218 \u2192 \uC2DC\uC791 \uC2DC \uBE60\uB978 \uC218\uC815 \uC2E4\uD589",
  lintWikiStart: "\uC704\uD0A4 \uB9B0\uD2B8 \uC2DC\uC791 \uC911...",
  lintWikiComplete: "\uC704\uD0A4 \uB9B0\uD2B8 \uC644\uB8CC",
  lintWikiFailed: "\uC704\uD0A4 \uB9B0\uD2B8 \uC2E4\uD328",
  analyzingSchema: "\uC704\uD0A4 \uBD84\uC11D \uBC0F \uC2A4\uD0A4\uB9C8 \uC81C\uC548 \uC0DD\uC131 \uC911...",
  schemaSuggestionGenerated: "\uC2A4\uD0A4\uB9C8 \uC81C\uC548\uC774 \uC0DD\uC131\uB418\uC5C8\uC2B5\uB2C8\uB2E4. wiki/schema/suggestions.md\uB97C \uD655\uC778\uD558\uC138\uC694",
  noSchemaUpdateNeeded: "\uC2A4\uD0A4\uB9C8 \uC5C5\uB370\uC774\uD2B8\uAC00 \uD544\uC694\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4.",
  schemaSuggestionFailed: "\uC2A4\uD0A4\uB9C8 \uC81C\uC548 \uC2E4\uD328",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Schema \uC5C5\uB370\uC774\uD2B8 \uBBF8\uB9AC\uBCF4\uAE30",
  schemaDiffSummary: "+{add} / -{del} \uC904",
  schemaDiffRegenerateLabel: "\uC81C\uC548 \uC218\uC815 (\uC120\uD0DD):",
  schemaDiffRegenerateBtn: "\uC7AC\uC0DD\uC131",
  schemaDiffOpenFileBtn: "\uD30C\uC77C \uC5F4\uAE30",
  schemaDiffApplyBtn: "\uC801\uC6A9",
  schemaDiffApplied: "Schema \uC5C5\uB370\uC774\uD2B8\uB428. \uBC31\uC5C5: {path}",
  schemaDiffEmptyTitle: "LLM\uC774 \uBCC0\uACBD \uBD88\uD544\uC694\uB77C\uACE0 \uD310\uB2E8",
  schemaDiffFailed: "Schema \uC801\uC6A9 \uC2E4\uD328: {reason}",
  schemaDiffRestoreHint: '{path}\uC5D0 \uBC31\uC5C5\uB418\uC5C8\uC2B5\uB2C8\uB2E4. \uBCF5\uC6D0\uD558\uB824\uBA74 \uD30C\uC77C \uD0D0\uC0C9\uAE30\uC5D0\uC11C \uD574\uB2F9 \uD30C\uC77C\uC744 "wiki/schema/config.md"\uB85C \uC774\uB984\uC744 \uBC14\uAFB8\uC138\uC694. \uCD5C\uADFC \uBC31\uC5C5 3\uAC1C\uAC00 \uC720\uC9C0\uB429\uB2C8\uB2E4.',
  schemaRegenerateNoBody: "\uC7AC\uC0DD\uC131\uC740 \uC131\uACF5\uD588\uC9C0\uB9CC LLM\uC774 \uC0C8 Schema\uB97C \uBC18\uD658\uD558\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4.",
  schemaNotFoundNotice: "\uC2A4\uD0A4\uB9C8 \uD30C\uC77C\uC744 \uCC3E\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4. \uC2A4\uD0A4\uB9C8\uB97C \uD65C\uC131\uD654\uD558\uC5EC \uC0DD\uC131\uD558\uC138\uC694.",
  selectFolderNoMdFiles: "\uD3F4\uB354\uC5D0 Markdown \uD30C\uC77C\uC774 \uC5C6\uC2B5\uB2C8\uB2E4: {path}",
  batchIngestSkipNotice: "\uC774\uBBF8 \uC218\uC9D1\uB41C {skipped}/{total} \uD30C\uC77C \uAC74\uB108\uB701\uB2C8\uB2E4. {new}\uAC1C\uC758 \uC0C8 \uD30C\uC77C \uC218\uC9D1 \uC911...",
  batchIngestAllIngested: "\uC774 \uD3F4\uB354\uC758 {total}\uAC1C \uD30C\uC77C\uC740 \uBAA8\uB450 \uC774\uBBF8 \uC218\uC9D1\uB418\uC5C8\uC2B5\uB2C8\uB2E4.",
  batchIngestStarting: '"{folder}"\uC5D0\uC11C {count}\uAC1C\uC758 \uD30C\uC77C \uC218\uC9D1 \uC911 \u2014 \uBA87 \uBD84 \uC815\uB3C4 \uC18C\uC694\uB420 \uC218 \uC788\uC2B5\uB2C8\uB2E4. \uC644\uB8CC \uC2DC \uBCF4\uACE0\uC11C\uAC00 \uD45C\uC2DC\uB429\uB2C8\uB2E4.',
  batchIngestComplete: "\uBC30\uCE58 \uC218\uC9D1 \uC644\uB8CC: {success}/{total} \uC131\uACF5, {fail} \uC2E4\uD328",
  batchIngestFailedFiles: "\uC2E4\uD328\uD55C \uD30C\uC77C:",
  historyTruncated: "\uCD5C\uADFC {max} \uB77C\uC6B4\uB4DC\uB85C \uAE30\uB85D\uC774 \uC798\uB838\uC2B5\uB2C8\uB2E4",
  historyCleared: "\uAE30\uB85D\uC774 \uC9C0\uC6CC\uC84C\uC2B5\uB2C8\uB2E4",
  // User Feedback Loop
  reviewedPagePreserved: "\uC0AC\uC6A9\uC790 \uAC80\uD1A0 \uCF58\uD150\uCE20 \uBCF4\uC874: {}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "\uC704\uD0A4\uC5D0 \uC800\uC7A5\uD558\uC2DC\uACA0\uC2B5\uB2C8\uAE4C?",
  querySuggestSaveDesc: "\uC774 \uB300\uD654\uC5D0\uB294 \uAC00\uCE58 \uC788\uB294 \uC9C0\uC2DD\uC774 \uD3EC\uD568\uB418\uC5B4 \uC788\uC2B5\uB2C8\uB2E4. \uC704\uD0A4\uC5D0 \uC800\uC7A5\uD558\uC2DC\uACA0\uC2B5\uB2C8\uAE4C?",
  querySuggestSaveYes: "\uC800\uC7A5",
  querySuggestSaveNo: "\uCDE8\uC18C",
  // Ingestion Report
  ingestReportElapsedTime: "\uACBD\uACFC \uC2DC\uAC04",
  ingestReportSkippedFiles: "\uAC74\uB108\uB700 (\uC774\uBBF8 \uC218\uC9D1\uB428)",
  ingestReportRejectedFiles: "\uAC74\uB108\uB700",
  rejectionReasonEmpty: "\uBE44\uC5B4 \uC788\uC74C",
  rejectionReasonType: "\uC9C0\uC6D0\uB418\uC9C0 \uC54A\uB294 \uD615\uC2DD",
  rejectionReasonDuplicate: "\uC911\uBCF5 \uCF58\uD150\uCE20",
  sourceRejectedEmpty: "\u23ED\uFE0F \u300C{filename}\u300D\uC5D0 \uAC00\uC838\uC62C \uB0B4\uC6A9\uC774 \uC5C6\uC5B4 \uAC74\uB108\uB6F0\uC5C8\uC2B5\uB2C8\uB2E4. \uBE44\uC5B4 \uC788\uAC70\uB098 \uD504\uB860\uD2B8\uB9E4\uD130\uB9CC \uC788\uB294 \uB178\uD2B8\uB294 \uC704\uD0A4 \uD398\uC774\uC9C0\uB97C \uB9CC\uB4E4\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4.",
  sourceRejectedType: "\u23ED\uFE0F \u300C{filename}\u300D\uC740(\uB294) \uC9C0\uC6D0\uB418\uC9C0 \uC54A\uB294 \uD30C\uC77C \uD615\uC2DD\uC774\uB77C \uAC74\uB108\uB6F0\uC5C8\uC2B5\uB2C8\uB2E4. \uD14D\uC2A4\uD2B8 \uB178\uD2B8(.md, .txt \uB4F1)\uB9CC \uAC00\uC838\uC62C \uC218 \uC788\uC2B5\uB2C8\uB2E4.",
  sourceRejectedDuplicate: "\u23ED\uFE0F \u300C{filename}\u300D\uC744(\uB97C) \uAC74\uB108\uB6F0\uC5C8\uC2B5\uB2C8\uB2E4 \u2014 \uB0B4\uC6A9\uC774 \uC774\uBBF8 \uC704\uD0A4\uC5D0 \uC788\uC2B5\uB2C8\uB2E4.",
  ingestRejectedSummary: "{count}\uAC1C \uD30C\uC77C\uC744 \uAC74\uB108\uB6F0\uC5C8\uC2B5\uB2C8\uB2E4(\uBE44\uC5B4 \uC788\uC74C, \uC911\uBCF5 \uB610\uB294 \uBBF8\uC9C0\uC6D0 \uD615\uC2DD).",
  reingestConfirmTitle: "\uC774 \uD30C\uC77C\uC744 \uB2E4\uC2DC \uAC00\uC838\uC62C\uAE4C\uC694?",
  reingestConfirmBody: "\u300C{filename}\u300D\uC758 \uB0B4\uC6A9\uC774 \uC774\uBBF8 \uC704\uD0A4\uC5D0 \uC788\uC2B5\uB2C8\uB2E4. \uADF8\uB798\uB3C4 \uB2E4\uC2DC \uAC00\uC838\uC62C\uAE4C\uC694?",
  reingestConfirmYes: "\uB2E4\uC2DC \uAC00\uC838\uC624\uAE30",
  reingestConfirmNo: "\uAC74\uB108\uB6F0\uAE30",
  ingestReportFailedGuidance: "\uC774 \uD56D\uBAA9\uC740 \uC790\uB3D9\uC73C\uB85C \uC0DD\uC131\uB418\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4. \uD574\uB2F9\uD558\uB294 \uD398\uC774\uC9C0\uB97C \uC218\uB3D9\uC73C\uB85C \uC0DD\uC131\uD558\uAC70\uB098 \uCD94\uCD9C \uC138\uBD84\uD654\uB97C \uB0AE\uCD94\uACE0 \uC18C\uC2A4 \uD30C\uC77C\uC744 \uB2E4\uC2DC \uC218\uC9D1\uD560 \uC218 \uC788\uC2B5\uB2C8\uB2E4.",
  ingestReportCollisions: "\uD06C\uB85C\uC2A4\uD0C0\uC785 \uCDA9\uB3CC (\uBCC4\uCE6D\uC73C\uB85C \uBCD1\uD569\uB428)",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "\uB2E8\uC77C \uC18C\uC2A4 \uC218\uC9D1",
  cmdIngestFolder: "\uD3F4\uB354\uC5D0\uC11C \uC218\uC9D1",
  cmdQueryWiki: "\uC704\uD0A4 \uC9C8\uC758",
  cmdLintWiki: "\uC704\uD0A4 \uB9B0\uD2B8",
  cmdRegenerateIndex: "\uC778\uB371\uC2A4 \uC7AC\uC0DD\uC131",
  cmdSuggestSchema: "\uC2A4\uD0A4\uB9C8 \uC5C5\uB370\uC774\uD2B8 \uC81C\uC548",
  cmdCancelIngestion: "\uD604\uC7AC \uC218\uC9D1 \uCDE8\uC18C",
  cmdIngestActiveFile: "\uD604\uC7AC \uD30C\uC77C \uC218\uC9D1",
  cmdViewHistory: "\uC218\uC9D1 \uC774\uB825 \uBCF4\uAE30",
  noActiveFile: "\uC5F4\uB824 \uC788\uB294 \uD30C\uC77C\uC774 \uC5C6\uC2B5\uB2C8\uB2E4",
  mdOnlyFile: "Markdown \uD30C\uC77C\uB9CC \uC218\uC9D1\uD560 \uC218 \uC788\uC2B5\uB2C8\uB2E4",
  // Ingestion status bar
  ingestionStatusBar: "\uC218\uC9D1 \uC911... \uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C",
  lintStatusBar: "\uC810\uAC80 \uC911... \uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C",
  ingestStatusAnalyzing: "\uC218\uC9D1 \uC911\u2026 (\uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C)",
  lintStatusReading: "\uC810\uAC80 \uC911\u2026 (\uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C)",
  lintStatusDuplicates: "\uC810\uAC80 \uC911\u2026 (\uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C)",
  lintStatusScanningLinks: "\uC810\uAC80 \uC911\u2026 (\uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C)",
  lintStatusAnalyzing: "\uC810\uAC80 \uC911\u2026 (\uD074\uB9AD\uD558\uC5EC \uCDE8\uC18C)",
  ingestionCancelling: "\uCDE8\uC18C \uC911 \u2014 \uD604\uC7AC \uBC30\uCE58 \uC644\uB8CC \uD6C4 \uC911\uC9C0\uB429\uB2C8\uB2E4",
  ingestionCancelled: "\uC218\uC9D1\uC774 \uCDE8\uC18C\uB418\uC5C8\uC2B5\uB2C8\uB2E4",
  crossTypeCollisionNotice: "{count}\uAC1C \uD56D\uBAA9\uC774 \uD06C\uB85C\uC2A4\uD0C0\uC785 \uBCC4\uCE6D\uC73C\uB85C \uBCD1\uD569\uB428\uFF08\uC5D4\uD2F0\uD2F0 \u2194 \uCEE8\uC149 \uC911\uBCF5 \uBC29\uC9C0\uFF09",
  // Lint Report
  lintReportTitle: "\uC704\uD0A4 \uB9B0\uD2B8 \uBCF4\uACE0\uC11C",
  lintReportSummary: "Wiki \uC0C1\uD0DC \uAC1C\uC694: \uCD1D {total}\uD398\uC774\uC9C0, {aliasesMissing}\uD398\uC774\uC9C0 \uBCC4\uCE6D \uB204\uB77D, \uC911\uBCF5 {duplicates}\uAC1C, \uB370\uB4DC \uB9C1\uD06C {deadLinks}\uAC1C ({deadLinkFromDup}\uAC1C \uC911\uBCF5 \uAD00\uB828), \uACE0\uC544 {orphans}\uAC1C ({orphanFromDup}\uAC1C \uC911\uBCF5), \uBE48 \uD398\uC774\uC9C0 {emptyPages}\uAC1C, \uCD9C\uCC98 \uC5C6\uB294 \uC778\uC6A9 {ungroundedQuotes}\uAC1C, \uC5B4\uD718 \uC678 \uD0DC\uADF8 {tagViolations}\uAC1C. Lint \uC18C\uC694: {elapsedSeconds}\uCD08",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "\uACE0\uAE09 \uB9E4\uAC1C\uBCC0\uC218 \uC124\uC815",
  advancedSettingsModeDesc: "\uAE30\uBCF8\uAC12\uC740 \uACF5\uAE09\uC790\uC758 \uC790\uCCB4 \uB3D9\uC791\uC744 \uC0AC\uC6A9\uD558\uBA70, \uC0AC\uACE0 \uC81C\uC5B4, \uC628\uB3C4, \uBC18\uBCF5 \uD398\uB110\uD2F0 \uD544\uB4DC\uB97C \uC804\uC1A1\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4. \uB300\uBD80\uBD84\uC758 \uC0AC\uC6A9\uC790\uC5D0\uAC8C \uC801\uD569\uD569\uB2C8\uB2E4. \uACF5\uAE09\uC790 \uAE30\uBCF8\uAC12\uC744 \uBA85\uC2DC\uC801\uC73C\uB85C \uC7AC\uC815\uC758\uD574\uC57C \uD558\uB294 \uACBD\uC6B0(\uC608: \uD2B9\uC815 \uC0AC\uACE0 \uC81C\uC5B4 \uBC29\uC5B8 \uAC15\uC81C, \uAE30\uBCF8\uAC12\uC774 \uC544\uB2CC \uC628\uB3C4 \uC124\uC815 \uB4F1)\uC5D0\uB9CC \uC0AC\uC6A9\uC790 \uC815\uC758\uB85C \uC804\uD658\uD558\uC138\uC694.",
  advancedSettingsDefault: "\uAE30\uBCF8\uAC12 (\uACF5\uAE09\uC790 \uB3D9\uC791 \uC0AC\uC6A9)",
  advancedSettingsCustom: "\uC0AC\uC6A9\uC790 \uC815\uC758 (\uACF5\uAE09\uC790 \uAE30\uBCF8\uAC12 \uC7AC\uC815\uC758)",
  disableThinkingName: "\uC0DD\uAC01 \uBE44\uD65C\uC131\uD654",
  disableThinkingDesc: "\uC635\uD2B8\uC778\uC785\uB2C8\uB2E4. \uD65C\uC131\uD654\uD558\uBA74 \uD50C\uB7EC\uADF8\uC778\uC740 \uC0AC\uACE0 \uC81C\uC5B4 \uC9C0\uC2DC\uBB38\uC744 \uACF5\uAE09\uC790\uC5D0\uAC8C \uBCF4\uB0B4\uACE0, \uACF5\uAE09\uC790\uAC00 \uAC70\uBD80\uD558\uBA74 3\uB2E8\uACC4 \uBC29\uC5B8 \uD3F4\uBC31 \uCCB4\uC778(anthropic \u2192 openai \u2192 none)\uC744 \uC2E4\uD589\uD569\uB2C8\uB2E4. \uACF5\uAE09\uC790\uAC00 \uCD94\uB860 \uB0B4\uC6A9\uC744 \uB2F5\uBCC0\uC5D0 \uB204\uC124\uD558\uACE0 \uC774\uB97C \uC5B5\uC81C\uD574\uC57C \uD560 \uB54C\uB9CC \uC0AC\uC6A9\uD558\uC138\uC694. \uB300\uBD80\uBD84\uC758 \uACF5\uAE09\uC790\uB294 \uCD94\uB860\uC744 \uC62C\uBC14\uB974\uAC8C \uCC98\uB9AC\uD558\uBBC0\uB85C, \uC774\uB97C \uBE44\uD65C\uC131\uD654(\uAE30\uBCF8\uAC12)\uB85C \uB450\uB294 \uAC83\uC774 \uB354 \uB098\uC740 \uD488\uC9C8\uC744 \uC81C\uACF5\uD569\uB2C8\uB2E4.",
  // Issue #137: 호환성 안내 (간결하게; 공급자 목록 생략)
  extractionTemperatureName: "\uCD94\uCD9C \uC628\uB3C4",
  extractionTemperatureDesc: "\uBC94\uC704 0\u20132. \uB0AE\uC744\uC218\uB85D LLM \uCD9C\uB825\uC774 \uACB0\uC815\uC801\uC774\uACE0 \uC6D0\uBCF8\uC5D0 \uCDA9\uC2E4\uD574\uC9D1\uB2C8\uB2E4. \uB192\uC744\uC218\uB85D \uCC3D\uC758\uC801\uC785\uB2C8\uB2E4. \uBE44\uC6CC \uB450\uBA74 \uC81C\uACF5\uC790 \uAE30\uBCF8\uAC12\uC774 \uC0AC\uC6A9\uB429\uB2C8\uB2E4. \uC81C\uACF5\uC790\uAC00 \uC774 \uAC12\uC744 \uAC70\uBD80\uD558\uBA74 \uD50C\uB7EC\uADF8\uC778\uC774 \uC790\uB3D9\uC73C\uB85C \uD544\uB4DC\uB97C \uC81C\uAC70\uD558\uACE0 \uD55C \uBC88 \uC54C\uB9BC\uC744 \uD45C\uC2DC\uD569\uB2C8\uB2E4.",
  chatTemperatureName: "\uCFFC\uB9AC \uC628\uB3C4",
  chatTemperatureDesc: "\uBC94\uC704 0\u20132. \uCD94\uCD9C \uC628\uB3C4\uC640 \uB3D9\uC77C\uD558\uC9C0\uB9CC \uCC44\uD305/\uCFFC\uB9AC \uC751\uB2F5\uC5D0\uB9CC \uC601\uD5A5\uC744 \uBBF8\uCE69\uB2C8\uB2E4. \uBE44\uC6CC \uB450\uBA74 \uC81C\uACF5\uC790 \uAE30\uBCF8\uAC12\uC774 \uC0AC\uC6A9\uB429\uB2C8\uB2E4. \uC81C\uACF5\uC790\uAC00 \uC774 \uAC12\uC744 \uAC70\uBD80\uD558\uBA74 \uD50C\uB7EC\uADF8\uC778\uC774 \uC790\uB3D9\uC73C\uB85C \uD544\uB4DC\uB97C \uC81C\uAC70\uD558\uACE0 \uD55C \uBC88 \uC54C\uB9BC\uC744 \uD45C\uC2DC\uD569\uB2C8\uB2E4.",
  repetitionPenaltyName: "\uBC18\uBCF5 \uD328\uB110\uD2F0",
  repetitionPenaltyDesc: "\uBC94\uC704 0\u20132. \uB192\uC744\uC218\uB85D \uBC18\uBCF5 \uD328\uD134\uC774 \uC904\uC5B4\uB4ED\uB2C8\uB2E4. \uBE44\uC6CC \uB450\uBA74 \uC81C\uACF5\uC790 \uAE30\uBCF8\uAC12\uC774 \uC0AC\uC6A9\uB429\uB2C8\uB2E4. \uD638\uD658\uC131 \uC548\uB0B4: \uB9CE\uC740 \uD074\uB77C\uC6B0\uB4DC \uACF5\uAE09\uC790\uB294 \uC774 \uD544\uB4DC\uB97C \uD5C8\uC6A9\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4. \uD50C\uB7EC\uADF8\uC778\uC740 HTTP 400 \uC2DC \uC790\uB3D9\uC73C\uB85C \uC81C\uAC70\uD558\uACE0 \uD55C \uBC88 \uC54C\uB9BC\uC744 \uD45C\uC2DC\uD569\uB2C8\uB2E4.",
  temperaturePlaceholder: "\uBE44\uC6C0 = \uC81C\uACF5\uC790 \uAE30\uBCF8\uAC12",
  lintDeadLinkSection: "\uB370\uB4DC \uB9C1\uD06C (\uAC10\uC9C0\uB428) [{count}\uAC1C]",
  lintEmptyPageSection: "\uBE48 \uD398\uC774\uC9C0 (\uAC10\uC9C0\uB428) [{count}\uAC1C]",
  lintOrphanSection: "\uACE0\uC544 \uD398\uC774\uC9C0 (\uAC10\uC9C0\uB428) [{count}\uAC1C]",
  lintContradictionSection: "\uBAA8\uC21C (\uAC10\uC9C0\uB428)",
  lintDuplicateSection: "\uC911\uBCF5 \uD398\uC774\uC9C0 (\uAC10\uC9C0\uB428)",
  lintNoIssuesFound: "\uC911\uBCF5, \uAE68\uC9C4 \uB9C1\uD06C, \uBE48 \uD398\uC774\uC9C0, \uACE0\uC544 \uD398\uC774\uC9C0 \uB610\uB294 \uCD9C\uCC98 \uC5C6\uB294 \uC778\uC6A9\uC774 \uAC10\uC9C0\uB418\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4.",
  lintQuoteGroundingSection: "\uCD9C\uCC98 \uC5C6\uB294 \uC778\uC6A9 (\uAC10\uC9C0\uB428) [{count}\uAC1C]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}: "{quote}"',
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (\uD398\uC774\uC9C0\uAC00 \uC874\uC7AC\uD558\uC9C0 \uC54A\uC74C){dupFlag}",
  lintDeadLinkMore: "- ... {count}\uAC1C\uC758 \uAE68\uC9C4 \uB9C1\uD06C \uCD94\uAC00",
  lintEmptyPageItem: "- [[{page}]] \u2014 \uC2E4\uC9C8\uC801\uC778 \uCF58\uD150\uCE20\uAC00 50\uC790 \uBBF8\uB9CC",
  lintOrphanItem: "- [[{page}]] \u2014 \uB2E4\uB978 \uC704\uD0A4 \uD398\uC774\uC9C0\uC5D0\uC11C \uB9C1\uD06C\uD558\uC9C0 \uC54A\uC74C{dupFlag}",
  lintPollutedSection: "\uC624\uC5FC \uD398\uC774\uC9C0 (\uAC10\uC9C0\uB428) [{count}\uAC1C]",
  lintPollutedItem: '- [[{page}]] \u2192 "{clean}"(\uC73C)\uB85C \uC218\uC815 \uD544\uC694',
  lintSourcesNormalizedSection: "Sources \uC815\uADDC\uD654 (\uC790\uB3D9 \uC218\uC815) [{files}\uAC1C \uD30C\uC77C / {entries}\uAC1C \uD56D\uBAA9]",
  lintSourcesNormalizedItem: "{files}\uAC1C \uD30C\uC77C\uC5D0\uC11C {entries}\uAC1C\uC758 sources \uD56D\uBAA9\uC744 \uC815\uB9AC\uD588\uC2B5\uB2C8\uB2E4 (\uC678\uBD80 \uACBD\uB85C, .md \uD655\uC7A5\uC790, \uBCC4\uCE6D \uD30C\uC774\uD504 \uC81C\uAC70 \uBC0F \uC911\uBCF5 \uC81C\uAC70).",
  lintDuplicateItem: "- [[{target}]] \uBC0F [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F \uC911\uBCF5 \uD398\uC774\uC9C0 \uAD00\uB828)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F \uC911\uBCF5 \uD398\uC774\uC9C0)",
  lintContradictionOpen: "\uD574\uACB0\uB418\uC9C0 \uC54A\uC740 \uBAA8\uC21C: {count}\uAC1C",
  lintContradictionAutoFixed: "(\uC774\uBC88 \uC2E4\uD589\uC5D0\uC11C {count}\uAC1C \uC790\uB3D9 \uC218\uC815)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "\uAC10\uC9C0\uB428",
  lintContradictionStatusPendingFix: "\uC218\uC815 \uB300\uAE30 \uC911",
  lintLLMAnalysisHeading: "## LLM \uBD84\uC11D",
  // Lint Analysis Prompt
  lintAnalysisPrompt: '\uB2F9\uC2E0\uC740 \uC704\uD0A4 \uC720\uC9C0\uBCF4\uC218 \uBCF4\uC870 \uB3C4\uAD6C\uC785\uB2C8\uB2E4. \uB2E4\uC74C \uC815\uBCF4\uB97C \uBC14\uD0D5\uC73C\uB85C \uC704\uD0A4 \uAC74\uAC15 \uC0C1\uD0DC\uB97C \uD655\uC778\uD558\uC138\uC694.\n\n\uC704\uD0A4 \uC778\uB371\uC2A4:\n{index}\n\n\uC704\uD0A4 \uD398\uC774\uC9C0 \uCF58\uD150\uCE20 \uC0D8\uD50C (\uCD1D {total} \uD398\uC774\uC9C0, {sample} \uD398\uC774\uC9C0 \uD45C\uC2DC):\n{contentSample}\n\n\uD504\uB85C\uADF8\uB7A8 \uAC80\uC0AC \uACB0\uACFC (\uC774\uBBF8 \uD655\uC778\uB428, \uBC18\uBCF5\uD558\uC9C0 \uB9C8\uC138\uC694):\n{progReport}\n\n\uB2E4\uC74C \uD56D\uBAA9\uC744 \uD655\uC778\uD558\uC138\uC694 (\uD504\uB85C\uADF8\uB7A8 \uAC80\uC0AC\uC5D0\uC11C \uC774\uBBF8 \uAC10\uC9C0\uB41C \uAE68\uC9C4 \uB9C1\uD06C/\uBE48 \uD398\uC774\uC9C0/\uACE0\uC544 \uD398\uC774\uC9C0\uB294 \uBC18\uBCF5\uD558\uC9C0 \uB9C8\uC138\uC694):\n1. **\uBAA8\uC21C** \u2014 \uC11C\uB85C \uB2E4\uB978 \uD398\uC774\uC9C0\uAC00 \uB3D9\uC77C\uD55C \uC0AC\uC2E4\uC5D0 \uB300\uD574 \uBAA8\uC21C\uB418\uB294\uC9C0\n2. **\uB178\uD6C4\uD654** \u2014 \uBA85\uBC31\uD788 \uC624\uB798\uB41C \uC8FC\uC7A5\uC774 \uC788\uB294\uC9C0\n3. **\uB204\uB77D** \u2014 \uC911\uC694\uD55C \uCEE8\uC149 \uC911 \uB3C5\uB9BD \uD398\uC774\uC9C0\uAC00 \uC5C6\uB294 \uAC83\n4. **\uAD6C\uC870** \u2014 \uD398\uC774\uC9C0 \uAD6C\uC870\uAC00 \uC801\uC808\uD55C\uC9C0 \uBC0F \uC0C1\uD638 \uCC38\uC870\uAC00 \uCDA9\uBD84\uD55C\uC9C0\n\n\uCD9C\uB825 \uD615\uC2DD: Markdown \uC0AC\uC6A9, "## LLM \uBD84\uC11D"\uC73C\uB85C \uC2DC\uC791. \uAC01 \uBC1C\uACAC \uC0AC\uD56D\uC744 \uD55C \uC904 "- [\uAD6C\uCCB4\uC801 \uC774\uC288]"\uB85C \uC791\uC131. \uC774\uC288\uAC00 \uC5C6\uC73C\uBA74 "\uBA85\uD655\uD55C \uC774\uC288 \uC5C6\uC74C."\uC744 \uC791\uC131\uD558\uC138\uC694.',
  // Lint Fix Progress
  lintFixProgress: "\uC218\uC815 \uC911 {current}/{total}: [[{target}]]",
  lintFixDeadComplete: "\uAE68\uC9C4 \uB9C1\uD06C \uC218\uC815 \uC644\uB8CC. {fixed}/{total} \uD56D\uBAA9 \uC218\uC815.",
  lintFillProgress: "\uD655\uC7A5 \uC911 {current}/{total}: {page}",
  lintFillComplete: "\uD398\uC774\uC9C0 \uD655\uC7A5 \uC644\uB8CC. {filled}/{total} \uD398\uC774\uC9C0 \uCC44\uC6C0.",
  lintDeleteCompleted: "{count}\uAC1C\uC758 \uBE48 \uC2A4\uD141 \uC0AD\uC81C\uB428",
  lintDeleteFailed: "\uBE48 stub \uC0AD\uC81C \uC2E4\uD328 {failed}/{total}\uAC1C (\uC790\uC138\uD55C \uB0B4\uC6A9\uC740 \uCF58\uC194 \uCC38\uC870)",
  lintFillFailed: "\uD655\uC7A5 \uC2E4\uD328: {page} \u2014 {error}",
  lintLinkProgress: "\uB9C1\uD06C \uC911 {current}/{total}: {page}",
  lintLinkComplete: "\uACE0\uC544 \uB9C1\uD06C \uC644\uB8CC. {linked} \uD398\uC774\uC9C0 \uB9C1\uD06C\uB428.",
  lintFixNoAction: "\uC870\uCE58 \uC5C6\uC74C (\uD074\uB77C\uC774\uC5B8\uD2B8 \uC5C6\uC74C)",
  lintFixIndexUpdated: "\uC704\uD0A4 \uC778\uB371\uC2A4 \uBC0F \uB85C\uADF8\uAC00 \uC5C5\uB370\uC774\uD2B8\uB418\uC5C8\uC2B5\uB2C8\uB2E4.",
  lintPollutedFixed: "\uC624\uC5FC\uB41C \uD398\uC774\uC9C0 \uC218\uC815 \uC644\uB8CC: {fixed}/{total}. \uC778\uB371\uC2A4 \uC7AC\uC0DD\uC131\uB428.",
  regenerateIndexCompleted: "\uC778\uB371\uC2A4 \uC7AC\uC0DD\uC131\uB428",
  operationFailed: "\uC2E4\uD328: ",
  lintFixAllComplete: "\uBAA8\uB4E0 \uC218\uC815\uC774 \uC644\uB8CC\uB418\uC5C8\uC2B5\uB2C8\uB2E4. \uC790\uC138\uD55C \uB0B4\uC6A9\uC740 \uB85C\uADF8\uB97C \uD655\uC778\uD558\uC138\uC694.",
  // Lint Report Modal
  lintModalActionsTitle: "\uC218\uC815 \uC81C\uC548 (LLM \uD1A0\uD070 \uD544\uC694):",
  lintLogReference: "\uC804\uCCB4 \uBCF4\uACE0\uC11C\uAC00 log.md\uC5D0 \uC800\uC7A5\uB418\uC5C8\uC2B5\uB2C8\uB2E4",
  lintModalFixDeadLinks: "\uAE68\uC9C4 \uB9C1\uD06C \uC218\uC815 ({count})",
  lintModalExpandEmpty: "\uBE48 \uD398\uC774\uC9C0 \uD655\uC7A5 ({count})",
  lintModalDeleteEmpty: "\uBE48 \uC2A4\uD141 \uC0AD\uC81C ({count})",
  lintModalLinkOrphans: "\uACE0\uC544 \uD398\uC774\uC9C0 \uB9C1\uD06C ({count})",
  lintModalAnalyzeSchema: "\uC2A4\uD0A4\uB9C8 \uBD84\uC11D",
  lintModalMergeDuplicates: "\uC911\uBCF5 \uBCD1\uD569 ({count})",
  lintModalFixAll: "\uC2A4\uB9C8\uD2B8 \uC804\uCCB4 \uC218\uC815 ({count} \uC774\uC288)",
  lintMergeProgress: "\uBCD1\uD569 \uC911 {current}/{total}: {source} \u2192 {target}",
  lintMergeComplete: "\uC911\uBCF5 \uBCD1\uD569 \uC644\uB8CC. {merged}/{total} \uC30D \uBCD1\uD569.",
  // Ingest Report Modal
  ingestReportTitle: "\uC218\uC9D1 \uBCF4\uACE0\uC11C",
  ingestReportSourceFile: "\uC18C\uC2A4 \uD30C\uC77C",
  ingestReportCreated: "\uC0DD\uC131\uB428",
  ingestReportUpdated: "\uC5C5\uB370\uC774\uD2B8\uB428",
  ingestReportContradictions: "\uBAA8\uC21C \uBC1C\uACAC",
  ingestReportFailedTitle: "\uC218\uC9D1 \uC2E4\uD328",
  ingestReportErrorDetail: "\uC624\uB958 \uC0C1\uC138",
  ingestReportClose: "\uB2EB\uAE30",
  ingestReportCreatedPages: "\uC0DD\uC131\uB41C \uD398\uC774\uC9C0: {count}",
  ingestReportUpdatedPages: "\uC5C5\uB370\uC774\uD2B8\uB41C \uD398\uC774\uC9C0: {count}",
  ingestReportEntitiesCount: "{count} \uC5D4\uD2F0\uD2F0",
  ingestReportConceptsCount: "{count} \uCEE8\uC149",
  ingestReportContradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC: {count}",
  ingestReportEntityType: "\uC5D4\uD2F0\uD2F0",
  ingestReportConceptType: "\uCEE8\uC149",
  timeMinutes: "\uBD84",
  timeSeconds: "\uCD08",
  // 속도 제한 경고
  rateLimitDetected: "\u26A0\uFE0F \uC18D\uB3C4 \uC81C\uD55C \uAC10\uC9C0: {count}\uAC1C \uD398\uC774\uC9C0\uAC00 429 \uC624\uB958\uB85C \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4. \uAD8C\uC7A5 \uC870\uCE58: (1) \uB3D9\uC2DC\uC131\uC744 {suggestedConcurrency} \uB610\uB294 1(\uC9C1\uB82C)\uB85C \uB0AE\uCD94\uAE30, (2) \uBC30\uCE58 \uC9C0\uC5F0\uC744 {suggestedDelay}ms\uB85C \uB298\uB9AC\uAE30, (3) \uB354 \uB192\uC740 \uC18D\uB3C4 \uC81C\uD55C\uC744 \uAC00\uC9C4 \uC81C\uACF5\uC5C5\uCCB4\uB85C \uC804\uD658.",
  rateLimitDetectedShort: "\u26A0\uFE0F \uC18D\uB3C4 \uC81C\uD55C \uBC1C\uC0DD \u2014 \uC124\uC815 \u2192 \uC218\uC9D1 \uAC00\uC18D\uC5D0\uC11C \uB3D9\uC2DC\uC131\uC744 \uB0AE\uCD94\uAC70\uB098 \uBC30\uCE58 \uC9C0\uC5F0\uC744 \uB298\uB9AC\uB294 \uAC83\uC774 \uC88B\uC2B5\uB2C8\uB2E4.",
  longSourceNotice: '\u{1F4C4} "{filename}" \uD30C\uC77C\uC740 {lines}\uC904({size})\uC785\uB2C8\uB2E4. \uAE34 \uD14D\uC2A4\uD2B8\uB294 \uBC18\uBCF5 \uC77C\uAD04 \uCD94\uCD9C\uC774 \uD544\uC694\uD558\uBA70 LLM\uC774 \uC804\uCCB4 \uBB38\uC11C\uB97C \uC5EC\uB7EC \uBC88 \uBD84\uC11D\uD558\uBBC0\uB85C \uBA87 \uBD84\uC774 \uAC78\uB9B4 \uC218 \uC788\uC2B5\uB2C8\uB2E4. \uC7A0\uC2DC\uB9CC \uAE30\uB2E4\uB824 \uC8FC\uC138\uC694.',
  longSourceNoticeShort: "\u{1F4C4} \uB300\uC6A9\uB7C9 \uD30C\uC77C \uAC10\uC9C0({lines}\uC904). \uC218\uC9D1\uC5D0 \uB2E4\uC18C \uC2DC\uAC04\uC774 \uAC78\uB9B4 \uC218 \uC788\uC2B5\uB2C8\uB2E4.",
  // 수집 이력 패널 (#122) — UI 텍스트
  historyButton: "\uC791\uC5C5 \uAE30\uB85D",
  historyButtonDesc: "LLM-Wiki\uC758 \uCD5C\uADFC \uC218\uC9D1, Lint \uBCF4\uACE0\uC11C \uBC0F \uAE30\uD0C0 \uBCC0\uACBD \uC0AC\uD56D \uBCF4\uAE30",
  historyButtonOpen: "\uAE30\uB85D \uBCF4\uAE30",
  historyModalTitle: "\uC791\uC5C5 \uAE30\uB85D",
  historyModalSubtitle: "LLM-Wiki\uC758 \uCD5C\uADFC \uBCC0\uACBD \uC0AC\uD56D",
  historyEmpty: "\uAE30\uB85D\uB41C \uC791\uC5C5\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uB178\uD2B8\uB97C \uC218\uC9D1\uD558\uAC70\uB098 Lint\uB97C \uC2E4\uD589\uD558\uC5EC \uC774 \uBAA9\uB85D\uC744 \uCC44\uC6B0\uC138\uC694.",
  historyReadError: "\uC791\uC5C5 \uB85C\uADF8\uB97C \uC77D\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4: {error}",
  historyEntryKindIngest: "\uC218\uC9D1",
  historyEntryKindMaintenance: "\uC720\uC9C0\uBCF4\uC218",
  historyEntryKindFix: "\uC218\uC815",
  historyEntryKindOther: "\uAE30\uD0C0",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "\uC18C\uC2A4: {source}",
  historyEntrySourceUnknown: "\uC774\uB984 \uC5C6\uB294 \uC218\uC9D1",
  historyEntryCreatedLabel: "\uC0DD\uC131\uB428",
  historyEntryUpdatedLabel: "\uC5C5\uB370\uC774\uD2B8\uB428",
  historyEntryCreatedCount: "{count}\uAC1C \uC0DD\uC131",
  historyEntryUpdatedCount: "{count}\uAC1C \uC5C5\uB370\uC774\uD2B8",
  historyEntryNoChanges: "\uD398\uC774\uC9C0 \uBCC0\uACBD \uC5C6\uC74C",
  historyEntryContradictions: "\u26A0\uFE0F \uBAA8\uC21C {count}\uAC74",
  historyEntrySectionCreated: "\uC0DD\uC131\uB41C \uD398\uC774\uC9C0",
  historyEntrySectionUpdated: "\uC5C5\uB370\uC774\uD2B8\uB41C \uD398\uC774\uC9C0",
  historyEntrySectionContradictions: "\uBAA8\uC21C",
  historyEntrySectionDetails: "\uC138\uBD80 \uC815\uBCF4",
  historyEntryOpenPage: "\uC5F4\uAE30",
  historyEntryDetailsNoContradictions: "\uBAA8\uC21C\uC774 \uBC1C\uACAC\uB418\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4",
  historySearchPlaceholder: "\u{1F50D} \uC18C\uC2A4 \uC81C\uBAA9 \uB610\uB294 \uD398\uC774\uC9C0 \uACBD\uB85C\uB85C \uAC80\uC0C9\u2026",
  historyFilterAll: "\uBAA8\uB450",
  historyFilterIngest: "\uC218\uC9D1\uB9CC",
  historyFilterMaintenance: "\uC720\uC9C0\uBCF4\uC218\uB9CC",
  historyFilterFix: "\uC218\uC815\uB9CC",
  historyFilterContradictions: "\u26A0\uFE0F \uBAA8\uC21C \uD3EC\uD568",
  historyRefreshButton: "\u27F3 \uC0C8\uB85C \uACE0\uCE68",
  historyExpandDay: "\uD074\uB9AD\uD558\uC5EC \uD3BC\uCE58\uAE30",
  historyCollapseDay: "\uD074\uB9AD\uD558\uC5EC \uC811\uAE30",
  historyShowMore: "\uC774\uC804 \uD56D\uBAA9 {count}\uAC1C \uBCF4\uAE30",
  historyNoMatch: "\uAC80\uC0C9/\uD544\uD130\uC5D0 \uC77C\uCE58\uD558\uB294 \uC791\uC5C5\uC774 \uC5C6\uC2B5\uB2C8\uB2E4.",
  historyCloseButton: "\uB2EB\uAE30",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "\uBCF4\uACE0\uC11C \uACB0\uACFC",
  historyKpiPages: "\uD398\uC774\uC9C0",
  historyKpiDeadLinks: "\uB04A\uAE34 \uB9C1\uD06C",
  historyKpiOrphans: "\uACE0\uC544",
  historyKpiEmpty: "\uBE48 \uD398\uC774\uC9C0",
  historyKpiDuplicates: "\uC911\uBCF5",
  historyKpiTagViolations: "\uD0DC\uADF8 \uBB38\uC81C",
  historyKpiUnsourced: "\uCD9C\uCC98 \uC5C6\uC74C",
  historyKpiDuration: "Lint \uC2DC\uAC04",
  historyKpiDurationSec: "{seconds}\uCD08",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "\uB04A\uAE34 \uB9C1\uD06C ({count})",
  historySectionTagViolations: "\uD0DC\uADF8 \uC704\uBC18 ({count})",
  historySectionOrphans: "\uACE0\uC544 \uD398\uC774\uC9C0 ({count})",
  historySectionEmptyPages: "\uBE48 \uD398\uC774\uC9C0 ({count})",
  historySectionLlmAnalysis: "LLM \uBD84\uC11D ({count})",
  historyDeadLinkSource: "\uCD9C\uCC98",
  historyDeadLinkTarget: "\uB204\uB77D",
  historyOpenInLog: "log.md\uC5D0\uC11C \uBCF4\uAE30",
  historyShowMoreItems: "\uB098\uBA38\uC9C0 {count}\uAC1C \uD3BC\uCE58\uAE30",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 \uB3D9\uC77C",
  historyChipContradiction: "\uBAA8\uC21C",
  historyChipOutdated: "\uC624\uB798\uB428",
  historyChipMissing: "\uB204\uB77D",
  historyChipStructure: "\uAD6C\uC870",
  historySeverityHigh: "\uB192\uC74C",
  historySeverityMedium: "\uC911\uAC04",
  historySeverityLow: "\uB0AE\uC74C",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Wiki\uC5D0 \uB04A\uAE34 \uB9C1\uD06C {dead}\uAC1C, \uACE0\uC544 \uD398\uC774\uC9C0 {orphans}\uAC1C, \uD0DC\uADF8 \uBB38\uC81C {tags}\uAC1C\uAC00 \uC788\uC2B5\uB2C8\uB2E4. \uCD5C\uADFC Lint\uB294 {duration} \uC18C\uC694.",
  historyGlobalInsightClean: "Wiki \uC0C1\uD0DC\uAC00 \uC591\uD638\uD569\uB2C8\uB2E4 \u2014 \uB04A\uAE34 \uB9C1\uD06C, \uACE0\uC544 \uD398\uC774\uC9C0, \uD0DC\uADF8 \uBB38\uC81C\uAC00 \uAC10\uC9C0\uB418\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4.",
  historyGlobalInsightNoData: "\uC720\uC9C0\uBCF4\uC218 \uBCF4\uACE0\uC11C\uAC00 \uC544\uC9C1 \uC5C6\uC2B5\uB2C8\uB2E4 \u2014 Lint\uB97C \uC2E4\uD589\uD558\uC138\uC694.",
  historyModalHeaderTitle: "\uC791\uC5C5 \uAE30\uB85D",
  historyModalSubtitleWithCount: "LLM-Wiki\uC758 \uCD5C\uADFC \uBCC0\uACBD \xB7 \uCD1D {count}\uAC1C",
  historyTimeRangeAll: "\uC804\uCCB4 \uAE30\uAC04",
  historyTimeRange1d: "\uCD5C\uADFC 1\uC77C",
  historyTimeRange3d: "\uCD5C\uADFC 3\uC77C",
  historyTimeRange1w: "\uCD5C\uADFC 1\uC8FC",
  historyTimeRange1m: "\uCD5C\uADFC 1\uAC1C\uC6D4",
  historyIngestTotal: "\uCD1D \uD398\uC774\uC9C0",
  historyIngestByType: "\uC720\uD615\uBCC4",
  historyIngestSource: "\uC18C\uC2A4 \uD30C\uC77C",
  historyIngestNoTimestamp: "\uD0C0\uC784\uC2A4\uD0EC\uD504 \uC5C6\uC74C",
  historyIngestFirstTime: "\uCCAB \uC218\uC9D1",
  historyIngestLatestTime: "\uCD5C\uADFC \uC218\uC9D1",
  historyTimeRangeCustom: "\uC0AC\uC6A9\uC790 \uC9C0\uC815 \uBC94\uC704",
  historyCustomRangeFrom: "\uC2DC\uC791",
  historyCustomRangeTo: "\uC885\uB8CC",
  historyCustomRangeApply: "\uC801\uC6A9",
  historyCustomRangeClear: "\uC9C0\uC6B0\uAE30"
};

// src/texts/de.ts
var DE_TEXTS = {
  // Plugin Info
  pluginTitle: "Karpathy LLM-Wiki-Einstellungen",
  pluginIntro: "Dieses Plugin setzt das {{link}}-Konzept f\xFCr Obsidian um. Es liest deine Notizen, nutzt KI, um Entit\xE4ten, Konzepte und Zusammenh\xE4nge zu extrahieren, und erstellt ein strukturiertes, vernetztes Wiki in deinem Vault.",
  karpathyLinkText: "Andrej Karpathys LLM-Wiki",
  // Features Section
  featuresTitle: "Funktionsweise",
  workflow1Title: "1. Aufnehmen",
  workflow1Desc: "W\xE4hle eine Notiz \u2014 KI extrahiert Entit\xE4ten, Konzepte und Zusammenh\xE4nge in Wiki-Seiten.",
  workflow2Title: "2. Anfragen",
  workflow2Desc: "Chatte mit deinem Wiki \u2014 Antworten basieren auf deinem aufgenommenen Wissen.",
  workflow3Title: "3. Pflegen",
  workflow3Desc: "Automatisch generierte Indizes, Querverweise und Lint-Pr\xFCfungen halten das Wiki gesund.",
  // Language Switcher
  languageTitle: "Oberfl\xE4chensprache",
  languageDesc: "W\xE4hle deine bevorzugte Sprache f\xFCr das Einstellungsfenster. Starte das Plugin neu, um die Sprach\xE4nderungen auf die Befehlspalette anzuwenden.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "LLM-Client-Status",
  llmWikiStatusSection: "LLM-Wiki-Status",
  statusInitialized: "Initialisiert",
  statusReady: "LLM bereit",
  statusNotReady: "LLM nicht konfiguriert \u2014 Einrichtung oben abschlie\xDFen und Verbindungstest bestehen",
  statusNotInitialized: "Nicht initialisiert",
  currentProvider: "Aktueller Anbieter",
  // Provider Configuration
  providerSection: "LLM-Konfiguration",
  providerName: "LLM-Anbieter",
  providerDesc: "Vordefinierten Anbieter oder benutzerdefinierten OpenAI-kompatiblen Dienst w\xE4hlen. F\xFCr Coding Plan oder \xE4hnliche Bundles: Custom OpenAI/Anthropic w\xE4hlen, Base URL und API Key des Providers manuell eingeben",
  // API Key
  apiKeyName: "API-Schl\xFCssel",
  apiKeyDesc: "API-Schl\xFCssel vom Anbieter eingeben",
  apiKeyPlaceholder: "API-Schl\xFCssel eingeben",
  // Base URL
  baseUrlName: "API-Basis-URL",
  baseUrlDescCustom: "Erforderlich: Benutzerdefinierte OpenAI-kompatible Endpunkt",
  baseUrlDescOverride: "Optional: Voreingestellte Basis-URL \xFCberschreiben",
  // Ollama Hint
  ollamaHint: "Ollama l\xE4uft lokal, kein API-Schl\xFCssel erforderlich",
  lmstudioHint: "LM Studio l\xE4uft lokal, API-Key ist optional",
  maxTokensPerCallName: "Kontextfenster",
  maxTokensPerCallDesc: "Tokens an das Kontextfenster des Modells anpassen. 0 = kein Limit (Cloud).",
  slugCaseName: "Dateinamen-Schreibweise",
  slugCaseDesc: "Legt fest, ob generierte Wiki-Dateinamen kleingeschrieben werden. 'Beibehalten' empfohlen fuer Sprachen, in denen Gross-/Kleinschreibung bedeutungsrelevant ist (z. B. deutsche Substantive).",
  slugCaseLower: "Kleinschreibung (Standard)",
  slugCasePreserve: "Schreibweise beibehalten",
  // Model Selection
  modelSection: "Modellauswahl",
  fetchModelsName: "Verf\xFCgbare Modelle abrufen",
  fetchModelsDesc: "Aktuelle Modellliste von der Anbieter-API abrufen",
  fetchModelsButton: "Modelle abrufen",
  fetchingModels: "Wird abgerufen...",
  fetchSuccess: "Erfolg! {} Modelle verf\xFCgbar",
  fetchFailed: "Abruf fehlgeschlagen oder leere Liste, bitte Modellnamen manuell eingeben",
  fetchNotSupported: "Anbieter unterst\xFCtzt keine Modelllistenabfrage",
  fetchErrorAuth: "Authentifizierung fehlgeschlagen (HTTP 401/403). \xDCberpr\xFCfen Sie den API-Schl\xFCssel, oder geben Sie eine Modell-ID ein und klicken Sie auf Verbindung testen.",
  fetchErrorEndpoint: "Endpunkt nicht gefunden (HTTP 404). \xDCberpr\xFCfen Sie die Endpunkt-URL, oder geben Sie eine Modell-ID ein und klicken Sie auf Verbindung testen.",
  fetchErrorServer: "Anbieter-Serverfehler (HTTP 5xx). Versuchen Sie es sp\xE4ter erneut, oder geben Sie eine Modell-ID ein und klicken Sie auf Verbindung testen.",
  fetchErrorEmpty: "Dieser Endpunkt stellt keine Modellliste bereit. Geben Sie eine Modell-ID ein und klicken Sie auf Verbindung testen.",
  fetchErrorNetwork: "Netzwerkanfrage fehlgeschlagen. \xDCberpr\xFCfen Sie Internetverbindung, Endpunkt-URL oder Proxy-Einstellungen. Sie k\xF6nnen auch eine Modell-ID eingeben und auf Verbindung testen klicken.",
  selectModelName: "Modell ausw\xE4hlen",
  selectModelDesc: "Aus {} verf\xFCgbaren Modellen w\xE4hlen",
  customInputOption: "Benutzerdefinierte Eingabe...",
  customInputHint: 'Um andere Modelle zu verwenden, w\xE4hle "Benutzerdefinierte Eingabe..."',
  modelName: "Modellname",
  modelDescCustom: "Benutzerdefiniertes Modell wird verwendet (Schaltfl\xE4che oben klicken, um Liste erneut abzurufen)",
  modelDescFetchFailed: "Abruf der Modellliste fehlgeschlagen. \xDCberpr\xFCfen Sie API-Schl\xFCssel und Endpunkt-URL, oder geben Sie eine Modell-ID ein und klicken Sie auf Verbindung testen.",
  modelInputPlaceholder: "Modell-ID eingeben, dann Verbindung testen",
  switchToDropdown: "Zur Dropdown-Auswahl wechseln",
  useDropdownButton: "Dropdown verwenden",
  // Test & Save
  testConnectionName: "Verbindung testen",
  testConnectionDesc: "Konfiguration \xFCberpr\xFCfen und LLM-API-Aufruf validieren",
  testButton: "Verbindung testen",
  testing: "Wird getestet...",
  saveSettingsName: "Einstellungen speichern",
  saveSettingsDesc: "Aktuelle Konfiguration speichern",
  saveButton: "Einstellungen speichern",
  testConnectionSuccessful: "Verbindung erfolgreich",
  testConnectionFailed: "Verbindung fehlgeschlagen",
  testConnectionProvider: "Anbieter: ",
  errorUnknown: "Unbekannter Fehler",
  savedNotice: "Einstellungen gespeichert!",
  // Issue #137: LLM-Fallback-Hinweise
  fallbackThinkingDialect: 'Denksteuerung: Zum Dialekt "{dialect}" gewechselt (dieser Anbieter verwendet ein anderes Format f\xFCr die Denksteuerung). Ausgabe unver\xE4ndert.',
  fallbackThinkingNone: "Denksteuerung f\xFCr diesen Anbieter vollst\xE4ndig deaktiviert. Es k\xF6nnen weiterhin Reasoning-Inhalte erscheinen; falls ja, versuchen Sie ein anderes Modell.",
  fallbackParamStripped: 'Parameter "{field}" wird von diesem Anbieter nicht unterst\xFCtzt. Aus der Anfrage entfernt; Verhalten kann von der Konfiguration abweichen.',
  // Wiki Folder
  wikiSection: "Wiki-Konfiguration",
  wikiFolderName: "Wiki-Ordner",
  wikiFolderDesc: "Speicherort f\xFCr generierte Wiki-Seiten",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "Bitte zun\xE4chst API-Schl\xFCssel konfigurieren",
  llmNotReady: "LLM ist nicht konfiguriert. Gehen Sie zu Einstellungen \u2192 Karpathy LLM Wiki, um Ihren Anbieter zu konfigurieren und den Verbindungstest zu bestehen.",
  errorFetchFailed: "Fehlgeschlagen: {}",
  // Query Settings
  querySectionTitle: "Wiki-Anfrage-Konfiguration",
  maxConversationHistoryName: "Maximaler Konversationsverlauf",
  maxConversationHistoryDesc: "Konversationsnachrichten begrenzen, um Token-\xDCberlauf zu vermeiden",
  maxConversationHistoryHint: "Empfehlung: 50 Runden nicht \xFCberschreiten",
  numberRangeValidation: "Bitte eine Zahl zwischen 1 und 50 eingeben",
  numberRangeClamped: "Wert au\xDFerhalb des Bereichs (1-500), automatisch auf {} gesetzt",
  // Query Modal UI
  queryModalTitle: "Wiki-Anfrage \u2014 Konversationelle Abfrage",
  queryModalPlaceholder: "Frage eingeben...",
  queryModalSendButton: "Senden",
  queryModalStopButton: "Stopp",
  queryModalSaveButton: "Im Wiki speichern",
  queryModalClearButton: "Verlauf l\xF6schen",
  queryModalHistoryCount: "Konversationsverlauf: {}/{} Runden",
  queryModalStreaming: "Streaming...",
  queryModalFallbackStreaming: "Streaming nicht unterst\xFCtzt, Wechsel zu Nicht-Streaming. Bitte warten...",
  queryPhaseSearching: "Wiki-Index wird analysiert, relevante Seiten werden ausgew\xE4hlt...",
  queryPhaseFoundPages: "{count} Seite(n) gefunden: {pages}",
  queryPhaseLoadingPages: "Seiteninhalt wird geladen...",
  queryPhaseContextReady: "Kontext bereit. Antwort wird generiert...",
  queryPhaseGenerating: "Generierung... (verstrichen {time}s)",
  queryPhaseNonStreaming: "Nicht-Streaming-Modus, Antwort wird generiert... (verstrichen {time}s)",
  queryModalErrorPrefix: "Fehler: ",
  queryModalHint: 'Anfragen basieren auf Wiki-Inhalten. Klicke auf "Im Wiki speichern", um wertvolle Konversationen als Wiki-Seiten zu extrahieren.',
  // v1.20.0: Query Wiki Denkblock-Einklapp-Zusammenfassung
  queryThinkingSummary: "Denkprozess",
  queryThinkingSteps: "Schritte",
  // Error Messages
  errorLLMClientNotInit: "LLM-Client nicht initialisiert. Bitte Einstellungen speichern.",
  errorIngestFailed: "Aufnahme fehlgeschlagen: ",
  errorQueryFailed: "Anfrage fehlgeschlagen: ",
  fileWriteFailed: "Datei konnte nicht erstellt oder aktualisiert werden: {path}",
  // Success Messages
  ingestSuccess: "Aufnahme erfolgreich: {} Seiten erstellt, {} Seiten aktualisiert",
  querySuccess: "Anfrage abgeschlossen",
  lintSuccess: "Wiki-Lint abgeschlossen",
  lintReadingPages: "{count} Wiki-Seiten werden gelesen...",
  lintReadingPagesProgress: "Wiki-Seiten werden gelesen: {current}/{total}...",
  lintScanningLinks: "Defekte Links werden gepr\xFCft...",
  lintScanningLinksProgress: "Defekte Links werden gepr\xFCft: {current}/{total}...",
  lintCheckingDuplicates: "Auf doppelte Seiten wird gepr\xFCft...",
  lintCheckingDuplicatesProgress: "Duplikate werden \xFCberpr\xFCft: Stapel {current} ...",
  lintFixingPolluted: "Bereinigte Seite wird repariert {current}/{total}: {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} Bereinigte Seiten reparieren ({count})",
  lintDuplicateCheckFailed: "Duplikaterkennung fehlgeschlagen \u2014 Details in der Konsole",
  lintDuplicateCheckFailedDetail: "Duplikatpr\xFCfung fehlgeschlagen bei {step}: {error}",
  lintMergeItemFailed: "Zusammenf\xFChrung fehlgeschlagen: {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "Fehlende Aliase: {count} Seite(n) ohne Aliase",
  lintAliasesSection: "Seiten ohne Aliase [{count}]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "Aliase vervollst\xE4ndigen ({count})",
  lintAliasesFilling: "Aliase werden generiert {current}/{total}: {page}",
  lintAliasesFilled: "Alias-Vervollst\xE4ndigung abgeschlossen. {filled}/{total} Seiten erg\xE4nzt.",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "Tags au\xDFerhalb des Vokabulars (erkannt) [{count}]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "Alias-Generierung fehlgeschlagen: {page} \u2014 {error}",
  lintFixItemFailed: "Behebung fehlgeschlagen: [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "Verlinkung fehlgeschlagen: {page} \u2014 {error}",
  lintRetrying: "Wiederholung ({attempt}/{max}) nach Fehler...",
  lintAnalyzingLLM: "LLM analysiert Wiki-Gesundheit...",
  saveToWikiSuccess: "Konversation im Wiki gespeichert!",
  saveSummary: "{entities} Entit\xE4ten, {concepts} Konzepte, {pages} Seiten",
  aliasAdded: "Alias '{alias}' zur Seite '{page}' hinzugef\xFCgt",
  // Status Messages
  ingestingSources: "Quellen werden aufgenommen...",
  queryingWiki: "Wiki wird angefragt...",
  lintingWiki: "Wiki wird gepr\xFCft...",
  savingToWiki: "Konversation wird im Wiki gespeichert...",
  clearingHistory: "Konversationsverlauf wird gel\xF6scht...",
  // Buttons
  ingestButton: "Aufnehmen",
  queryButton: "Anfragen",
  lintButton: "Pr\xFCfen",
  cancelButton: "Abbrechen",
  // Links
  karpathyGistLink: "Karpathys LLM-Wiki Gist",
  obsidianPluginAPI: "Obsidian-Plugin-API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // Other
  availableModelsLoading: "Verf\xFCgbare Modelle werden geladen...",
  noModelsAvailable: "Keine Modelle verf\xFCgbar",
  // LLM Language Hint
  llmLanguageHint: "Bitte antworten Sie auf Deutsch.",
  // Schema Configuration
  schemaSection: "Schema-Konfiguration",
  enableSchemaName: "Schema aktivieren",
  enableSchemaDesc: "Schema/config.md generieren und in alle LLM-Prompts einf\xFCgen, um strukturierte Wiki-Ausgabe zu erm\xF6glichen",
  viewSchemaButton: "Schema ansehen / bearbeiten",
  regenerateSchemaButton: "Standard-Schema neu generieren",
  schemaRegeneratedNotice: "Standard-Schema neu generiert.",
  schemaRegenerateFailed: "Schema-Generierung fehlgeschlagen",
  // Wiki-Initialisierungsstatus
  wikiInitStatusReady: "Wiki initialisiert",
  wikiInitStatusNotReady: "Wiki nicht initialisiert \u2014 wird beim ersten Ingest automatisch erstellt",
  // Wiki Output Language
  wikiLanguageName: "Wiki-Ausgabesprache",
  wikiLanguageDesc: "Alle generierten Wiki-Seiten, Indizes, Protokolle und Anfrageantworten verwenden diese Sprache",
  customWikiLanguageOption: "Benutzerdefiniert...",
  customWikiLanguageHint: "Sprachnamen eingeben (z. B. Italienisch, Arabisch). Wird als Ausgabesprachen-Anweisung an das LLM \xFCbergeben.",
  customWikiLanguagePlaceholder: "z. B. Italienisch",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "Extraktion",
  extractionGranularityName: "Extraktions-Granularit\xE4t",
  extractionGranularityDesc: "Steuert Extraktionen pro Quelldatei. H\xF6her = mehr Seiten/API-Verbrauch.\nFein: Tiefenanalyse. Standard: T\xE4gliche Notizen. Grob: Schnelle \xDCbersicht. Minimal: 100+ Dateien stapeln. Benutzerdefiniert: Eigene Limits (max 500).\nTipp: Verwenden Sie Minimal/Grob f\xFCr Ordner mit vielen Dateien zur Zeit- und Kosteneinsparung.",
  extractionGranularityFine: "Fein \u2014 Tiefenanalyse (\u2264100 Elemente)",
  extractionGranularityStandard: "Standard \u2014 T\xE4gliche Notizen (\u226450 Elemente)",
  extractionGranularityCoarse: "Grob \u2014 Schnell\xFCbersicht (\u226410 Elemente)",
  extractionGranularityMinimal: "Minimal \u2014 Stapelverarbeitung 100+ Dateien (\u22645 Elemente)",
  extractionGranularityCustom: "Benutzerdefiniert \u2014 Eigene Limits festlegen (1~500)",
  customEntityLimitName: "Benutzerdefiniertes Entit\xE4tslimit",
  customEntityLimitDesc: "Maximale Anzahl zu extrahierender Entit\xE4ten pro Quelldatei (1-500)",
  customConceptLimitName: "Benutzerdefiniertes Konzeptlimit",
  customConceptLimitDesc: "Maximale Anzahl zu extrahierender Konzepte pro Quelldatei (1-500)",
  // Issue #85 v2: Tag-Vokabular (Chip-Eingabe, in Wiki-Konfiguration eingebettet)
  tagVocabularyInlineDesc: 'Kontrolliertes Vokabular f\xFCr entity- und concept-Frontmatter-Tags. Verschachtelte Obsidian-Tags mit \u201E/" bleiben erhalten.',
  tagVocabularyModeName: "Tag-Vokabular-Konfiguration",
  tagVocabularyModeDescDefault: 'Standard verwendet eingebaute Tags: {}. Wechseln Sie zu \u201EBenutzerdefiniert", um Ihr eigenes Vokabular zu definieren.',
  tagVocabularyModeDescCustom: "Benutzerdefiniert: Definieren Sie unten Ihre eigenen entity- und concept-Tags per Chip-Eingabe \u2014 Enter oder Komma zum Hinzuf\xFCgen, \xD7 zum Entfernen.",
  tagVocabularyModeDefault: "Standard (eingebaute Subtyp-Tags)",
  tagVocabularyModeCustom: "Benutzerdefiniert (eigene)",
  customEntityTagsName: "Benutzerdefinierte Entit\xE4ts-Tags",
  customEntityTagsDesc: 'Enter oder Komma zum Hinzuf\xFCgen eines Chips, \xD7 zum Entfernen. Verschachtelte Tags mit \u201E/" bleiben erhalten.',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "Benutzerdefinierte Konzept-Tags",
  customConceptTagsDesc: 'Enter oder Komma zum Hinzuf\xFCgen eines Chips, \xD7 zum Entfernen. Verschachtelte Tags mit \u201E/" bleiben erhalten.',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "Doppeltes Tag \xFCbersprungen",
  // Ingestion Acceleration
  accelerationSectionTitle: "Aufnahmen-Beschleunigung",
  pageGenerationConcurrencyName: "LLM-Parallelit\xE4t",
  pageGenerationConcurrencyDesc: "Anzahl paralleler LLM-Aufrufe w\xE4hrend Aufnahme und Lint-Operationen. H\xF6here Werte beschleunigen die Verarbeitung, erh\xF6hen aber API-Kosten und k\xF6nnen Ratenbegrenzungen ausl\xF6sen.",
  concurrencyValueSingular: "Aktuelle Parallelit\xE4t: {} (seriell \u2014 am sichersten)",
  concurrencyValuePlural: "Aktuelle Parallelit\xE4t: {} (parallel)",
  batchDelayName: "Batch-Verz\xF6gerung (ms)",
  batchDelayDesc: "Verz\xF6gerung zwischen parallelen Stapeln zur Vermeidung von API-Ratenbegrenzungen (100\u20132000 ms). Aktuell: {}ms. Erh\xF6hen bei 429-Fehlern.",
  // Auto Maintenance
  autoMaintainSection: "Automatische Wartung",
  autoMaintainBetaBadge: "BETA \u2014 Experimentelle Funktion. Kann Probleme aufweisen. Nur f\xFCr erfahrene Benutzer empfohlen.",
  autoWatchName: "Ordner beobachten",
  autoWatchDesc: "Neue oder ge\xE4nderte .md-Dateien in den beobachteten Ordnern automatisch erkennen und benachrichtigen oder automatisch aufnehmen",
  watchedFoldersName: "Beobachtete Ordner",
  watchedFoldersDesc: 'Ordner, die auf neue Inhalte \xFCberwacht werden. Klicke auf "Ordner hinzuf\xFCgen", um aus deinem Vault auszuw\xE4hlen.',
  addWatchedFolderButton: "Ordner hinzuf\xFCgen",
  removeWatchedFolderButton: "Entfernen",
  webClipperPresetName: "Clippings beobachten (Web Clipper)",
  webClipperPresetDesc: "Den Clippings/-Ordner von Obsidian Web Clipper zur Beobachtungsliste hinzuf\xFCgen. Webclips werden automatisch ins Wiki aufgenommen.",
  noWatchedFoldersHint: "Keine Ordner konfiguriert. F\xFCge einen Ordner hinzu oder aktiviere die Clippings-Voreinstellung.",
  autoWatchModeName: "Beobachtungsmodus",
  autoWatchModeDesc: '"Nur benachrichtigen" zeigt eine Eingabeaufforderung. "Automatisch aufnehmen" verarbeitet still.',
  watchModeNotify: "Nur benachrichtigen",
  watchModeAuto: "Automatisch aufnehmen",
  autoWatchDebounceName: "Entprell-Verz\xF6gerung (Sekunden)",
  autoWatchDebounceDesc: "Wartezeit vor der Aufnahmeausl\xF6sung nach einer Datei\xE4nderung (1\u201360 Sekunden)",
  periodicLintName: "Periodische Pr\xFCfung",
  periodicLintDesc: "LLM-Pr\xFCfung zeitgesteuert ausf\xFChren, nur wenn sich Quelldateien seit der letzten Pr\xFCfung ge\xE4ndert haben",
  periodicLintOff: "Aus",
  periodicLintHourly: "St\xFCndlich",
  periodicLintDaily: "T\xE4glich",
  periodicLintWeekly: "W\xF6chentlich",
  autoSmartFixName: "Automatische Smart-Korrektur",
  autoSmartFixDesc: "Wenn Lint ausgef\xFChrt wird, alle Korrekturen (Smart Fix All) automatisch anwenden, ohne das Berichts-Modal anzuzeigen. Die Zusammenfassung wird nach Abschluss weiterhin angezeigt.",
  autoSmartFixNotice: "Automatische Smart-Korrektur: Alle Korrekturen werden angewendet...",
  startupCheckName: "Schnellkorrekturen beim Start ausf\xFChren",
  startupCheckDesc: "Beim Laden des Plugins\u4F4E\u7EA7 Formatprobleme (sources-Feld, doppelt verschachtelte Wikilinks) automatisch korrigieren und Wiki-Ordnerstruktur \xFCberpr\xFCfen. Standardm\xE4\xDFig aktiviert.",
  suggestSchemaCommand: "Schema-Aktualisierungen vorschlagen",
  autoMaintainCostWarning: '\u26A0\uFE0F Kostenhinweis: Funktionen zur automatischen Wartung verbrauchen API-Tokens. "Automatisch aufnehmen" l\xF6st bei jeder Quelldatei-\xC4nderung LLM-Aufrufe aus. "Periodische Pr\xFCfung" f\xFChrt zeitgesteuerte LLM-Gesundheitspr\xFCfungen durch (nur bei erkannten Quell\xE4nderungen). Sorgf\xE4ltig konfigurieren, um unerwartete Kosten zu vermeiden.',
  // Notices
  startupCheckSummary: "Wiki enth\xE4lt {pages} Seiten ({entities} Entit\xE4ten, {concepts} Konzepte, {sources} Quellen)",
  watcherActiveNotice: "Wiki: Datei-Beobachtung aktiv \u2014 beobachtete Ordner werden \xFCberwacht",
  watchIngestNotice: 'Wiki: {count} Datei(en) in sources/ ge\xE4ndert. "Quellen aufnehmen" ausf\xFChren, um zu verarbeiten.',
  autoIngestRunning: "Automatische Aufnahme von {count} ge\xE4nderter(n) Datei(en)...",
  autoIngestComplete: "Automatische Aufnahme abgeschlossen: {success} erfolgreich, {fail} fehlgeschlagen",
  scheduledLintRunning: "Geplante Wiki-Pr\xFCfung wird ausgef\xFChrt...",
  wikiLintStats: "Wiki-Pr\xFCfung: {pages} Seiten ({entities} Entit\xE4ten, {concepts} Konzepte, {sources} Quellen)",
  wikiHealthStats: "Wiki-Gesundheit: {pages} Seiten ({entities} Entit\xE4ten, {concepts} Konzepte, {sources} Quellen){indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Wiki-Schnellkorrekturen abgeschlossen",
  startupCheckStructureLabel: "Wiki-Struktur",
  startupCheckStructureOk: "vollst\xE4ndig",
  startupCheckStructureMissing: "unvollst\xE4ndig \u2014 wird beim ersten Ingest automatisch erstellt",
  startupCheckSourcesLabel: "Sources normalisiert",
  startupCheckSourcesClean: "bereits sauber",
  startupCheckSourcesCleaned: "{files} Datei(en), {entries} Eintrag/Eintr\xE4ge bereinigt",
  startupCheckIncompleteClean: "unvollst\xE4ndige Seiten: keine",
  startupCheckIncompleteArchived: "unvollst\xE4ndige Seiten: {count} archiviert (wiederherstellbar aus .trash)",
  startupCheckDisableHint: "Zum Deaktivieren: Einstellungen \u2192 Auto-Wartung \u2192 Schnellkorrekturen beim Start ausf\xFChren",
  lintWikiStart: "Wiki-Pr\xFCfung wird gestartet...",
  lintWikiComplete: "Wiki-Pr\xFCfung abgeschlossen",
  lintWikiFailed: "Wiki-Pr\xFCfung fehlgeschlagen",
  analyzingSchema: "Wiki wird analysiert und Schema-Vorschl\xE4ge werden generiert...",
  schemaSuggestionGenerated: "Schema-Vorschl\xE4ge generiert, siehe wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "Keine Schema-Aktualisierungen erforderlich.",
  schemaSuggestionFailed: "Schema-Vorschlag fehlgeschlagen",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Schema-Update-Vorschau",
  schemaDiffSummary: "+{add} / -{del} Zeilen",
  schemaDiffRegenerateLabel: "Vorschlag verfeinern (optional):",
  schemaDiffRegenerateBtn: "Neu generieren",
  schemaDiffOpenFileBtn: "Datei \xF6ffnen",
  schemaDiffApplyBtn: "Anwenden",
  schemaDiffApplied: "Schema aktualisiert. Sicherung: {path}",
  schemaDiffEmptyTitle: "LLM empfiehlt keine \xC4nderungen",
  schemaDiffFailed: "Schema-Anwendung fehlgeschlagen: {reason}",
  schemaDiffRestoreHint: 'Backup gespeichert unter {path}. Zum Wiederherstellen benennen Sie diese Datei im Datei-Explorer in "wiki/schema/config.md" um. Die neuesten 3 Backups werden aufbewahrt.',
  schemaRegenerateNoBody: "Neugenerierung erfolgreich, aber LLM hat keinen neuen Body zur\xFCckgegeben.",
  schemaNotFoundNotice: "Schema-Datei nicht gefunden. Schema aktivieren, um sie zu erstellen.",
  selectFolderNoMdFiles: "Keine Markdown-Dateien im Ordner: {path}",
  batchIngestSkipNotice: "{skipped}/{total} bereits aufgenommene Dateien werden \xFCbersprungen. {new} neue Datei(en) werden aufgenommen...",
  batchIngestAllIngested: "Alle {total} Dateien in diesem Ordner wurden bereits aufgenommen.",
  batchIngestStarting: '{count} Datei(en) aus "{folder}" werden aufgenommen \u2014 dies kann mehrere Minuten dauern. Ein Bericht erscheint bei Abschluss.',
  batchIngestComplete: "Batch-Aufnahme abgeschlossen: {success}/{total} erfolgreich, {fail} fehlgeschlagen",
  batchIngestFailedFiles: "Fehlgeschlagene Dateien:",
  historyTruncated: "Verlauf auf letzte {max} Runden gek\xFCrzt",
  historyCleared: "Verlauf gel\xF6scht",
  // User Feedback Loop
  reviewedPagePreserved: "Benutzergepr\xFCfte Inhalte werden beibehalten f\xFCr: {}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "Im Wiki speichern?",
  querySuggestSaveDesc: "Diese Konversation enth\xE4lt wertvolles Wissen. Im Wiki speichern?",
  querySuggestSaveYes: "Speichern",
  querySuggestSaveNo: "Verwerfen",
  // Ingestion Report
  ingestReportElapsedTime: "Verstrichene Zeit",
  ingestReportSkippedFiles: "\xDCbersprungen (bereits aufgenommen)",
  ingestReportRejectedFiles: "\xDCbersprungen",
  rejectionReasonEmpty: "leer",
  rejectionReasonType: "nicht unterst\xFCtzter Typ",
  rejectionReasonDuplicate: "doppelter Inhalt",
  sourceRejectedEmpty: '\u23ED\uFE0F \u201E{filename}" hat keinen Inhalt zum Einlesen \u2014 \xFCbersprungen. Leere oder nur aus Frontmatter bestehende Notizen erzeugen keine Wiki-Seiten.',
  sourceRejectedType: '\u23ED\uFE0F \u201E{filename}" ist kein unterst\xFCtzter Dateityp \u2014 \xFCbersprungen. Nur Textnotizen (z. B. .md, .txt) k\xF6nnen eingelesen werden.',
  sourceRejectedDuplicate: '\u23ED\uFE0F \u201E{filename}" \xFCbersprungen \u2014 der Inhalt ist bereits im Wiki vorhanden.',
  ingestRejectedSummary: "{count} Datei(en) \xFCbersprungen (leer, doppelt oder nicht unterst\xFCtzter Typ).",
  reingestConfirmTitle: "Diese Datei erneut einlesen?",
  reingestConfirmBody: 'Der Inhalt von \u201E{filename}" ist bereits im Wiki. Trotzdem erneut einlesen?',
  reingestConfirmYes: "Erneut einlesen",
  reingestConfirmNo: "\xDCberspringen",
  ingestReportFailedGuidance: "Diese Elemente konnten nicht automatisch erstellt werden. Du kannst die entsprechenden Seiten manuell erstellen oder die Extraktions-Granularit\xE4t senken und die Quelldatei erneut aufnehmen.",
  ingestReportCollisions: "Cross-Type-Kollisionen (als Aliase zusammengef\xFChrt)",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "Einzelne Quelle aufnehmen",
  cmdIngestFolder: "Aus Ordner aufnehmen",
  cmdQueryWiki: "Wiki anfragen",
  cmdLintWiki: "Wiki pr\xFCfen",
  cmdRegenerateIndex: "Index neu generieren",
  cmdSuggestSchema: "Schema-Aktualisierungen vorschlagen",
  cmdCancelIngestion: "Aktuelle Aufnahme abbrechen",
  cmdIngestActiveFile: "Aktuelle Datei aufnehmen",
  cmdViewHistory: "Ingestionsverlauf anzeigen",
  noActiveFile: "Keine Datei ge\xF6ffnet",
  mdOnlyFile: "Nur Markdown-Dateien k\xF6nnen aufgenommen werden",
  // Ingestion status bar
  ingestionStatusBar: "Aufnahme l\xE4uft... zum Abbrechen klicken",
  lintStatusBar: "Pr\xFCfung l\xE4uft... zum Abbrechen klicken",
  ingestStatusAnalyzing: "Aufnahme l\xE4uft... (zum Abbrechen klicken)",
  lintStatusReading: "Pr\xFCfung l\xE4uft... (zum Abbrechen klicken)",
  lintStatusDuplicates: "Pr\xFCfung l\xE4uft... (zum Abbrechen klicken)",
  lintStatusScanningLinks: "Pr\xFCfung l\xE4uft... (zum Abbrechen klicken)",
  lintStatusAnalyzing: "Pr\xFCfung l\xE4uft... (zum Abbrechen klicken)",
  ingestionCancelling: "Wird abgebrochen \u2014 Stopp nach aktuellem Batch",
  ingestionCancelled: "Aufnahme abgebrochen",
  crossTypeCollisionNotice: "{count} Eintr\xE4ge als Cross-Type-Alias zusammengef\xFChrt (Entity \u2194 Concept Duplikate verhindert)",
  // Lint Report
  lintReportTitle: "Wiki-Pr\xFCfbericht",
  lintReportSummary: "Wiki-Status\xFCbersicht: {total} Seiten gesamt, {aliasesMissing} Seiten ohne Aliase, {duplicates} Duplikate, {deadLinks} defekte Links ({deadLinkFromDup} betreffen Duplikate), {orphans} verwaiste Seiten ({orphanFromDup} sind Duplikate), {emptyPages} leere Seiten, {ungroundedQuotes} unbelegte Zitate, {tagViolations} Tags au\xDFerhalb des Vokabulars. Lint-Dauer: {elapsedSeconds}s",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "Erweiterte Parametereinstellungen",
  advancedSettingsModeDesc: "Im Standardmodus wird das Standardverhalten Ihres Providers verwendet \u2014 es werden keine Felder f\xFCr Denksteuerung, Temperatur oder Wiederholungsstrafe gesendet. Dies funktioniert f\xFCr die meisten Benutzer. Wechseln Sie nur dann zu \u201EBenutzerdefiniert\u201C, wenn Sie die Provider-Standards explizit \xFCberschreiben m\xFCssen (z. B. einen bestimmten Denksteuerungsdialekt erzwingen, eine andere als die Standardtemperatur festlegen usw.).",
  advancedSettingsDefault: "Standard (Provider-Verhalten verwenden)",
  advancedSettingsCustom: "Benutzerdefiniert (Provider-Standards \xFCberschreiben)",
  disableThinkingName: "Thinking deaktivieren",
  disableThinkingDesc: "Opt-in. Wenn aktiviert, sendet das Plugin eine Denksteuerungsanweisung an den Provider und durchl\xE4uft eine 3-stufige Dialekt-Fallback-Kette (anthropic \u2192 openai \u2192 none), falls der Provider sie ablehnt. Verwenden Sie dies nur, wenn Ihr Provider Denkinhalte in die Antwort einstreut und Sie diese unterdr\xFCcken m\xFCssen. Die meisten Provider verarbeiten Denkprozesse korrekt von selbst \u2014 sie deaktiviert zu lassen (Standard) bietet bessere Qualit\xE4t.",
  // Issue #137: Kompatibilitätshinweise (kurz gehalten; keine Provider-Liste)
  extractionTemperatureName: "Extraktionstemperatur",
  extractionTemperatureDesc: "Bereich 0\u20132. Niedrigere Werte machen die LLM-Ausgabe deterministischer und originalgetreuer. H\xF6here Werte machen sie kreativer. Leer lassen = Provider-Standard. Lehnt Ihr Anbieter diesen Wert ab, entfernt das Plugin das Feld automatisch und zeigt einen einmaligen Hinweis.",
  chatTemperatureName: "Abfragetemperatur",
  chatTemperatureDesc: "Bereich 0\u20132. Wie Extraktionstemperatur, betrifft aber nur Chat-/Query-Antworten. Leer lassen = Provider-Standard. Lehnt Ihr Anbieter diesen Wert ab, entfernt das Plugin das Feld automatisch und zeigt einen einmaligen Hinweis.",
  repetitionPenaltyName: "Wiederholungsstrafe",
  repetitionPenaltyDesc: "Bereich 0\u20132. H\xF6here Werte reduzieren Wiederholungen. Leer lassen = Provider-Standard. Kompatibilit\xE4tshinweis: Viele Cloud-Anbieter akzeptieren dieses Feld nicht. Das Plugin entfernt es bei HTTP 400 automatisch und zeigt einen einmaligen Hinweis.",
  temperaturePlaceholder: "leer = Provider-Standard",
  lintDeadLinkSection: "Defekte Links (erkannt) [{count}]",
  lintEmptyPageSection: "Leere Seiten (erkannt) [{count}]",
  lintOrphanSection: "Verwaiste Seiten (erkannt) [{count}]",
  lintContradictionSection: "Widerspr\xFCche (erkannt)",
  lintDuplicateSection: "Doppelte Seiten (erkannt)",
  lintNoIssuesFound: "Keine Duplikate, defekten Links, leeren Seiten, verwaisten Seiten oder unbelegten Zitate erkannt.",
  lintQuoteGroundingSection: "Unbelegte Zitate (erkannt) [{count}]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}: "{quote}"',
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (Seite existiert nicht){dupFlag}",
  lintDeadLinkMore: "- ... {count} weitere defekte Links",
  lintEmptyPageItem: "- [[{page}]] \u2014 weniger als 50 Zeichen substanziellen Inhalts",
  lintOrphanItem: "- [[{page}]] \u2014 keine anderen Wiki-Seiten verweisen hierher{dupFlag}",
  lintPollutedSection: "Verschmutzte Seiten (erkannt) [{count}]",
  lintPollutedItem: '- [[{page}]] \u2192 sollte "{clean}" sein',
  lintSourcesNormalizedSection: "Sources normalisiert (automatisch repariert) [{files} Dateien / {entries} Eintr\xE4ge]",
  lintSourcesNormalizedItem: "{entries} verschmutzte sources-Eintr\xE4ge in {files} Datei(en) bereinigt (externe Pfade, .md-Erweiterungen, Alias-Pipes entfernt und dedupliziert).",
  lintDuplicateItem: "- [[{target}]] und [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F betrifft doppelte Seite)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F doppelte Seite)",
  lintContradictionOpen: "Offene Widerspr\xFCche: {count}",
  lintContradictionAutoFixed: "({count} in diesem Durchlauf automatisch behoben)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "Erkannt",
  lintContradictionStatusPendingFix: "Behebung ausstehend",
  lintLLMAnalysisHeading: "## LLM-Analyse",
  // Lint Analysis Prompt
  lintAnalysisPrompt: 'Du bist ein Wiki-Wartungsassistent. \xDCberpr\xFCfe die Wiki-Gesundheit anhand der folgenden Informationen.\n\nWiki-Index:\n{index}\n\nWiki-Seiteninhalt-Stichprobe ({total} Seiten gesamt, {sample} Seiten angezeigt):\n{contentSample}\n\nErgebnisse der programmatischen Pr\xFCfung (bereits verifiziert, nicht wiederholen):\n{progReport}\n\nPr\xFCfe die folgenden Aspekte (defekte Links/leere/verwaiste Seiten, die bereits durch programmatische Pr\xFCfungen erkannt wurden, \xFCberspringen):\n1. **Widerspr\xFCche** \u2014 ob verschiedene Seiten sich bei denselben Fakten widersprechen\n2. **Veraltung** \u2014 ob Behauptungen offensichtlich veraltet sind\n3. **Fehlend** \u2014 welche wichtigen Konzepte keine eigenst\xE4ndigen Seiten haben\n4. **Struktur** \u2014 ob die Seitenstruktur angemessen und Querverweise ausreichend sind\n\nAusgabeformat: Verwende Markdown, beginnend mit "## LLM-Analyse". Jede Feststellung in einer Zeile "- [konkretes Problem]". Wenn keine Probleme: "Keine offensichtlichen Probleme gefunden."',
  // Lint Fix Progress
  lintFixProgress: "Behebung {current}/{total}: [[{target}]]",
  lintFixDeadComplete: "Behebung defekter Links abgeschlossen. {fixed}/{total} Elemente behoben.",
  lintFillProgress: "Erweiterung {current}/{total}: {page}",
  lintFillComplete: "Seitenerweiterung abgeschlossen. {filled}/{total} Seiten gef\xFCllt.",
  lintDeleteCompleted: "{count} leere Stubs gel\xF6scht",
  lintDeleteFailed: "Fehler beim L\xF6schen von {failed}/{total} leeren Stubs (Details in der Konsole)",
  lintFillFailed: "Erweiterung fehlgeschlagen: {page} \u2014 {error}",
  lintLinkProgress: "Verlinkung {current}/{total}: {page}",
  lintLinkComplete: "Verwaiste-Seiten-Verlinkung abgeschlossen. {linked} Seiten verlinkt.",
  lintFixNoAction: "Keine Ma\xDFnahme ergriffen (kein Client)",
  lintFixIndexUpdated: "Wiki-Index und Protokoll aktualisiert.",
  lintPollutedFixed: "Besch\xE4digte Seiten behoben: {fixed}/{total}. Index neu generiert.",
  regenerateIndexCompleted: "Index neu generiert",
  operationFailed: "Fehlgeschlagen: ",
  lintFixAllComplete: "Alle Behebungen abgeschlossen. Details im Protokoll.",
  // Lint Report Modal
  lintModalActionsTitle: "Behebungsvorschl\xE4ge (ben\xF6tigt LLM-Tokens):",
  lintLogReference: "Vollst\xE4ndiger Bericht in log.md gespeichert",
  lintModalFixDeadLinks: "Defekte Links beheben ({count})",
  lintModalExpandEmpty: "Leere Seiten erweitern ({count})",
  lintModalDeleteEmpty: "Leere Stubs l\xF6schen ({count})",
  lintModalLinkOrphans: "Verwaiste Seiten verlinken ({count})",
  lintModalAnalyzeSchema: "Schema analysieren",
  lintModalMergeDuplicates: "Duplikate zusammenf\xFChren ({count})",
  lintModalFixAll: "Intelligente Gesamtbehebung ({count} Probleme)",
  lintMergeProgress: "Zusammenf\xFChrung {current}/{total}: {source} \u2192 {target}",
  lintMergeComplete: "Duplikat-Zusammenf\xFChrung abgeschlossen. {merged}/{total} Paare zusammengef\xFChrt.",
  // Ingest Report Modal
  ingestReportTitle: "Aufnahmebericht",
  ingestReportSourceFile: "Quelldatei",
  ingestReportCreated: "Erstellt",
  ingestReportUpdated: "Aktualisiert",
  ingestReportContradictions: "Widerspr\xFCche gefunden",
  ingestReportFailedTitle: "Aufnahme fehlgeschlagen",
  ingestReportErrorDetail: "Fehlerdetail",
  ingestReportClose: "Schlie\xDFen",
  ingestReportCreatedPages: "Erstellte Seiten: {count}",
  ingestReportUpdatedPages: "Aktualisierte Seiten: {count}",
  ingestReportEntitiesCount: "{count} Entit\xE4ten",
  ingestReportConceptsCount: "{count} Konzepte",
  ingestReportContradictionsFound: "Gefundene Widerspr\xFCche: {count}",
  ingestReportEntityType: "Entit\xE4t",
  ingestReportConceptType: "Konzept",
  timeMinutes: "Min",
  timeSeconds: "Sek",
  // Ratenbegrenzungs-Warnungen
  rateLimitDetected: "\u26A0\uFE0F Ratenbegrenzung erkannt: {count} Seite(n) mit 429-Fehlern fehlgeschlagen. Empfehlungen: (1) Parallelit\xE4t auf {suggestedConcurrency} oder 1 (seriell) reduzieren, (2) Batch-Verz\xF6gerung auf {suggestedDelay}ms erh\xF6hen, (3) Zu einem Anbieter mit h\xF6heren Ratenbegrenzungen wechseln.",
  rateLimitDetectedShort: "\u26A0\uFE0F Ratenbegrenzung erreicht \u2014 Reduzieren Sie die Parallelit\xE4t oder erh\xF6hen Sie die Batch-Verz\xF6gerung in Einstellungen \u2192 Aufnahmebeschleunigung.",
  longSourceNotice: '\u{1F4C4} "{filename}" hat {lines} Zeilen ({size}). Lange Texte erfordern iterative Stapel-Extraktion \u2014 das LLM liest das gesamte Dokument in mehreren Durchl\xE4ufen. Dies kann einige Minuten dauern. Bitte haben Sie Geduld.',
  longSourceNoticeShort: "\u{1F4C4} Gro\xDFe Datei erkannt ({lines} Zeilen). Die Ingestion kann einige Zeit dauern.",
  // Ingestionsverlauf-Panel (#122) — UI-Texte
  historyButton: "Operationsverlauf",
  historyButtonDesc: "Letzte Ingestionen, Lint-Berichte und andere \xC4nderungen Ihres Wikis anzeigen",
  historyButtonOpen: "Verlauf anzeigen",
  historyModalTitle: "Operationsverlauf",
  historyModalSubtitle: "Letzte \xC4nderungen Ihres LLM-Wikis",
  historyEmpty: "Noch keine Operationen aufgezeichnet. Ingestiere eine Notiz oder f\xFChre Lint aus, um diese Liste zu f\xFCllen.",
  historyReadError: "Operationsprotokoll konnte nicht gelesen werden: {error}",
  historyEntryKindIngest: "Ingestion",
  historyEntryKindMaintenance: "Wartung",
  historyEntryKindFix: "Korrektur",
  historyEntryKindOther: "Sonstige",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "Quelle: {source}",
  historyEntrySourceUnknown: "Unbenannter Ingest",
  historyEntryCreatedLabel: "Erstellt",
  historyEntryUpdatedLabel: "Aktualisiert",
  historyEntryCreatedCount: "{count} erstellt",
  historyEntryUpdatedCount: "{count} aktualisiert",
  historyEntryNoChanges: "Keine Seiten\xE4nderungen",
  historyEntryContradictions: "\u26A0\uFE0F {count} Widerspruch(e)",
  historyEntrySectionCreated: "Erstellte Seiten",
  historyEntrySectionUpdated: "Aktualisierte Seiten",
  historyEntrySectionContradictions: "Widerspr\xFCche",
  historyEntrySectionDetails: "Details",
  historyEntryOpenPage: "\xD6ffnen",
  historyEntryDetailsNoContradictions: "Keine Widerspr\xFCche gefunden",
  historySearchPlaceholder: "\u{1F50D} Nach Quelltitel oder Seitenpfad suchen\u2026",
  historyFilterAll: "Alle",
  historyFilterIngest: "Nur Ingest",
  historyFilterMaintenance: "Nur Wartung",
  historyFilterFix: "Nur Korrektur",
  historyFilterContradictions: "\u26A0\uFE0F Mit Widerspr\xFCchen",
  historyRefreshButton: "\u27F3 Aktualisieren",
  historyExpandDay: "Zum Erweitern klicken",
  historyCollapseDay: "Zum Einklappen klicken",
  historyShowMore: "{count} \xE4ltere Eintr\xE4ge anzeigen",
  historyNoMatch: "Keine Operationen entsprechen Ihrer Suche/Ihrem Filter.",
  historyCloseButton: "Schlie\xDFen",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "Berichts-Befunde",
  historyKpiPages: "Seiten",
  historyKpiDeadLinks: "Tote Links",
  historyKpiOrphans: "Verwaist",
  historyKpiEmpty: "Leer",
  historyKpiDuplicates: "Duplikate",
  historyKpiTagViolations: "Tag-Probleme",
  historyKpiUnsourced: "Unbelegt",
  historyKpiDuration: "Lint-Zeit",
  historyKpiDurationSec: "{seconds}s",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "Tote Links ({count})",
  historySectionTagViolations: "Tag-Verst\xF6\xDFe ({count})",
  historySectionOrphans: "Verwaiste Seiten ({count})",
  historySectionEmptyPages: "Leere Seiten ({count})",
  historySectionLlmAnalysis: "LLM-Analyse ({count})",
  historyDeadLinkSource: "Quelle",
  historyDeadLinkTarget: "Fehlt",
  historyOpenInLog: "In log.md \xF6ffnen",
  historyShowMoreItems: "{count} weitere ausklappen",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 unver\xE4ndert",
  historyChipContradiction: "Widerspruch",
  historyChipOutdated: "Veraltet",
  historyChipMissing: "Fehlt",
  historyChipStructure: "Struktur",
  historySeverityHigh: "Hoch",
  historySeverityMedium: "Mittel",
  historySeverityLow: "Niedrig",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Ihr Wiki hat {dead} tote Links, {orphans} verwaiste Seiten und {tags} Tag-Probleme. Letzte Lint-Dauer: {duration}.",
  historyGlobalInsightClean: "Ihr Wiki ist sauber \u2014 keine toten Links, verwaisten Seiten oder Tag-Probleme erkannt.",
  historyGlobalInsightNoData: "Noch keine Wartungsberichte \u2014 f\xFChren Sie Lint aus.",
  historyModalHeaderTitle: "Operationsverlauf",
  historyModalSubtitleWithCount: "Letzte \xC4nderungen Ihres LLM-Wikis \xB7 {count} Eintr\xE4ge",
  historyTimeRangeAll: "Gesamter Zeitraum",
  historyTimeRange1d: "Letzter Tag",
  historyTimeRange3d: "Letzte 3 Tage",
  historyTimeRange1w: "Letzte Woche",
  historyTimeRange1m: "Letzter Monat",
  historyIngestTotal: "Seiten gesamt",
  historyIngestByType: "Nach Typ",
  historyIngestSource: "Quelldatei",
  historyIngestNoTimestamp: "Kein Zeitstempel",
  historyIngestFirstTime: "Erste Ingestion",
  historyIngestLatestTime: "Letzte Ingestion",
  historyTimeRangeCustom: "Benutzerdef. Zeitraum",
  historyCustomRangeFrom: "Von",
  historyCustomRangeTo: "Bis",
  historyCustomRangeApply: "Anwenden",
  historyCustomRangeClear: "Zur\xFCcksetzen"
};

// src/texts/fr.ts
var FR_TEXTS = {
  // Plugin Info
  pluginTitle: "Param\xE8tres de Karpathy LLM Wiki",
  pluginIntro: "Ce plugin impl\xE9mente le concept de {{link}} pour Obsidian. Il lit vos notes, utilise l'IA pour extraire des entit\xE9s et des concepts, et construit un wiki structur\xE9 et interconnect\xE9 dans votre coffre.",
  karpathyLinkText: "LLM Wiki d'Andrej Karpathy",
  // Features Section
  featuresTitle: "Fonctionnement",
  workflow1Title: "1. Importer",
  workflow1Desc: "S\xE9lectionnez une note \u2014 l'IA extrait les entit\xE9s, concepts et relations dans des pages wiki.",
  workflow2Title: "2. Interroger",
  workflow2Desc: "Discutez avec votre wiki \u2014 les r\xE9ponses s'appuient sur vos connaissances import\xE9es.",
  workflow3Title: "3. Maintenir",
  workflow3Desc: "L'index, les liens crois\xE9s et les v\xE9rifications automatiques assurent la sant\xE9 du wiki.",
  // Language Switcher
  languageTitle: "Langue de l'interface",
  languageDesc: "S\xE9lectionnez la langue du panneau des param\xE8tres. Red\xE9marrez le plugin pour appliquer les changements de langue \xE0 la palette de commandes.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "\xC9tat du client LLM",
  llmWikiStatusSection: "\xC9tat LLM-Wiki",
  statusInitialized: "Initialis\xE9",
  statusReady: "LLM pr\xEAt",
  statusNotReady: "LLM non configur\xE9 \u2014 terminez la configuration ci-dessus et r\xE9ussissez le test de connexion",
  statusNotInitialized: "Non initialis\xE9",
  currentProvider: "Fournisseur actuel",
  // Provider Configuration
  providerSection: "Configuration LLM",
  providerName: "Fournisseur LLM",
  providerDesc: "S\xE9lectionnez un fournisseur pr\xE9d\xE9fini ou un service compatible OpenAI personnalis\xE9. Pour Coding Plan ou bundles similaires: choisissez Custom OpenAI/Anthropic, saisissez manuellement le Base URL et API Key du fournisseur",
  // API Key
  apiKeyName: "Cl\xE9 API",
  apiKeyDesc: "Saisissez votre cl\xE9 API fournie par le fournisseur",
  apiKeyPlaceholder: "Saisir la cl\xE9 API",
  // Base URL
  baseUrlName: "URL de base de l'API",
  baseUrlDescCustom: "Obligatoire : point de terminaison compatible OpenAI personnalis\xE9",
  baseUrlDescOverride: "Facultatif : remplacer l'URL de base pr\xE9d\xE9finie",
  // Ollama Hint
  ollamaHint: "Ollama fonctionne localement, aucune cl\xE9 API n'est requise",
  lmstudioHint: "LM Studio fonctionne localement, la cl\xE9 API est facultative",
  maxTokensPerCallName: "Fen\xEAtre de contexte",
  maxTokensPerCallDesc: "Limiter les tokens \xE0 la fen\xEAtre de contexte du mod\xE8le. 0 = aucune limite (cloud).",
  slugCaseName: "Casse des noms de fichiers",
  slugCaseDesc: "Contr\xF4le si les noms de fichiers Wiki g\xE9n\xE9r\xE9s sont mis en minuscules. 'Conserver' recommand\xE9 pour les langues o\xF9 la casse est significative (ex. noms allemands).",
  slugCaseLower: "Minuscules (d\xE9faut)",
  slugCasePreserve: "Conserver la casse",
  // Model Selection
  modelSection: "S\xE9lection du mod\xE8le",
  fetchModelsName: "R\xE9cup\xE9rer les mod\xE8les disponibles",
  fetchModelsDesc: "Obtenir la derni\xE8re liste de mod\xE8les depuis l'API du fournisseur",
  fetchModelsButton: "R\xE9cup\xE9rer les mod\xE8les",
  fetchingModels: "R\xE9cup\xE9ration...",
  fetchSuccess: "Succ\xE8s ! {} mod\xE8les disponibles",
  fetchFailed: "\xC9chec ou liste vide, veuillez saisir le nom du mod\xE8le manuellement",
  fetchNotSupported: "Le fournisseur ne prend pas en charge la requ\xEAte de liste de mod\xE8les",
  fetchErrorAuth: "\xC9chec d'authentification (HTTP 401/403). V\xE9rifiez la cl\xE9 API, ou entrez un ID de mod\xE8le puis cliquez sur Tester la connexion.",
  fetchErrorEndpoint: "Point d'acc\xE8s introuvable (HTTP 404). V\xE9rifiez l'URL du point d'acc\xE8s, ou entrez un ID de mod\xE8le puis cliquez sur Tester la connexion.",
  fetchErrorServer: "Erreur du serveur du fournisseur (HTTP 5xx). R\xE9essayez plus tard, ou entrez un ID de mod\xE8le puis cliquez sur Tester la connexion.",
  fetchErrorEmpty: "Ce point d'acc\xE8s n'expose pas de liste de mod\xE8les. Entrez un ID de mod\xE8le puis cliquez sur Tester la connexion.",
  fetchErrorNetwork: "\xC9chec de la requ\xEAte r\xE9seau. V\xE9rifiez votre connexion Internet, l'URL du point d'acc\xE8s ou les param\xE8tres du proxy. Vous pouvez aussi entrer un ID de mod\xE8le puis cliquer sur Tester la connexion.",
  selectModelName: "S\xE9lectionner un mod\xE8le",
  selectModelDesc: "Choisir parmi {} mod\xE8les disponibles",
  customInputOption: "Saisie personnalis\xE9e...",
  customInputHint: `Pour utiliser d'autres mod\xE8les, s\xE9lectionnez "Saisie personnalis\xE9e..."`,
  modelName: "Nom du mod\xE8le",
  modelDescCustom: "Mod\xE8le personnalis\xE9 utilis\xE9 (cliquez ci-dessus pour r\xE9cup\xE9rer la liste)",
  modelDescFetchFailed: "\xC9chec de r\xE9cup\xE9ration de la liste des mod\xE8les. V\xE9rifiez la cl\xE9 API et l'URL du point d'acc\xE8s, ou entrez un ID de mod\xE8le puis cliquez sur Tester la connexion.",
  modelInputPlaceholder: "Saisir l'ID du mod\xE8le, puis Tester la connexion",
  switchToDropdown: "Passer \xE0 la s\xE9lection par liste d\xE9roulante",
  useDropdownButton: "Utiliser la liste d\xE9roulante",
  // Test & Save
  testConnectionName: "Tester la connexion",
  testConnectionDesc: "V\xE9rifier que la configuration peut appeler l'API LLM avec succ\xE8s",
  testButton: "Tester la connexion",
  testing: "Test en cours...",
  saveSettingsName: "Enregistrer les param\xE8tres",
  saveSettingsDesc: "Enregistrer la configuration actuelle",
  saveButton: "Enregistrer",
  testConnectionSuccessful: "Connexion r\xE9ussie",
  testConnectionFailed: "Connexion \xE9chou\xE9e",
  testConnectionProvider: "Fournisseur : ",
  errorUnknown: "Erreur inconnue",
  savedNotice: "Param\xE8tres enregistr\xE9s !",
  // Issue #137 : Notifications de repli LLM
  fallbackThinkingDialect: "Contr\xF4le de r\xE9flexion : bascul\xE9 vers le dialecte \xAB {dialect} \xBB (ce fournisseur utilise un format diff\xE9rent). Le r\xE9sultat est inchang\xE9.",
  fallbackThinkingNone: "Contr\xF4le de r\xE9flexion enti\xE8rement d\xE9sactiv\xE9 pour ce fournisseur. Du contenu de raisonnement peut encore appara\xEEtre ; le cas \xE9ch\xE9ant, essayez un autre mod\xE8le.",
  fallbackParamStripped: "Le param\xE8tre \xAB {field} \xBB n'est pas pris en charge par ce fournisseur. Supprim\xE9 de la requ\xEAte ; le comportement peut diff\xE9rer de la configuration.",
  // Wiki Folder
  wikiSection: "Configuration du Wiki",
  wikiFolderName: "Dossier wiki",
  wikiFolderDesc: "Emplacement des pages wiki g\xE9n\xE9r\xE9es",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "Veuillez d'abord configurer la cl\xE9 API",
  llmNotReady: "LLM non configur\xE9. Allez dans Param\xE8tres \u2192 Karpathy LLM Wiki pour configurer votre fournisseur et r\xE9ussir le test de connexion.",
  errorFetchFailed: "\xC9chec : {}",
  // Query Settings
  querySectionTitle: "Configuration de l'interrogation du wiki",
  maxConversationHistoryName: "Historique maximal de conversation",
  maxConversationHistoryDesc: "Limiter les messages de conversation pour \xE9viter le d\xE9passement de tokens",
  maxConversationHistoryHint: "Recommand\xE9 : ne pas d\xE9passer 50 tours",
  numberRangeValidation: "Veuillez saisir un nombre entre 1 et 50",
  numberRangeClamped: "Valeur hors plage (1-500), automatiquement r\xE9gl\xE9e sur {}",
  // Query Modal UI
  queryModalTitle: "Interroger le wiki \u2014 Requ\xEAte conversationnelle",
  queryModalPlaceholder: "Saisir votre question...",
  queryModalSendButton: "Envoyer",
  queryModalStopButton: "Arr\xEAter",
  queryModalSaveButton: "Enregistrer dans le wiki",
  queryModalClearButton: "Effacer l'historique",
  queryModalHistoryCount: "Historique de conversation : {}/{} tours",
  queryModalStreaming: "G\xE9n\xE9ration en continu...",
  queryModalFallbackStreaming: "La g\xE9n\xE9ration en continu n'est pas prise en charge, passage en mode non continu. Veuillez patienter...",
  queryPhaseSearching: "Analyse de l'index wiki, recherche de pages pertinentes...",
  queryPhaseFoundPages: "{count} page(s) trouv\xE9e(s) : {pages}",
  queryPhaseLoadingPages: "Chargement du contenu des pages...",
  queryPhaseContextReady: "Contexte pr\xEAt. G\xE9n\xE9ration de la r\xE9ponse...",
  queryPhaseGenerating: "G\xE9n\xE9ration en cours... (\xE9coul\xE9 {time}s)",
  queryPhaseNonStreaming: "Mode non continu, g\xE9n\xE9ration de la r\xE9ponse... (\xE9coul\xE9 {time}s)",
  queryModalErrorPrefix: "Erreur : ",
  queryModalHint: "Les requ\xEAtes s'appuient sur le contenu du wiki. Cliquez sur \xAB Enregistrer dans le wiki \xBB pour extraire les conversations utiles en pages wiki.",
  // v1.20.0: Résumé du bloc de réflexion pliable de Query Wiki
  queryThinkingSummary: "Processus de r\xE9flexion",
  queryThinkingSteps: "\xE9tapes",
  // Error Messages
  errorLLMClientNotInit: "Client LLM non initialis\xE9. Veuillez enregistrer les param\xE8tres.",
  errorIngestFailed: "\xC9chec de l'import : ",
  errorQueryFailed: "\xC9chec de la requ\xEAte : ",
  fileWriteFailed: "Impossible de cr\xE9er ou de mettre \xE0 jour le fichier : {path}",
  // Success Messages
  ingestSuccess: "Import r\xE9ussi : {} pages cr\xE9\xE9es, {} pages mises \xE0 jour",
  querySuccess: "Requ\xEAte termin\xE9e",
  lintSuccess: "V\xE9rification du wiki termin\xE9e",
  lintReadingPages: "Lecture de {count} pages wiki...",
  lintReadingPagesProgress: "Lecture des pages wiki : {current}/{total}...",
  lintScanningLinks: "Analyse des liens cass\xE9s...",
  lintScanningLinksProgress: "Analyse des liens cass\xE9s : {current}/{total}...",
  lintCheckingDuplicates: "V\xE9rification des pages dupliqu\xE9es...",
  lintCheckingDuplicatesProgress: "V\xE9rification des doublons : lot {current} ...",
  lintFixingPolluted: "R\xE9paration de page pollu\xE9e {current}/{total} : {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} R\xE9parer les pages pollu\xE9es ({count})",
  lintDuplicateCheckFailed: "\xC9chec de la d\xE9tection des doublons \u2014 consultez la console pour les d\xE9tails",
  lintDuplicateCheckFailedDetail: "\xC9chec de la v\xE9rification des doublons \xE0 l'\xE9tape {step} : {error}",
  lintMergeItemFailed: "\xC9chec de la fusion : {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "Alias manquants : {count} page(s) sans alias",
  lintAliasesSection: "Pages sans alias [{count}]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "Compl\xE9ter les alias ({count})",
  lintAliasesFilling: "G\xE9n\xE9ration d'alias {current}/{total} : {page}",
  lintAliasesFilled: "Compl\xE9tion des alias termin\xE9e. {filled}/{total} pages remplies.",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "Tags hors vocabulaire (d\xE9tect\xE9s) [{count}]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "\xC9chec de la g\xE9n\xE9ration d'alias : {page} \u2014 {error}",
  lintFixItemFailed: "\xC9chec de la r\xE9paration : [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "\xC9chec du lien : {page} \u2014 {error}",
  lintRetrying: "Nouvelle tentative ({attempt}/{max}) apr\xE8s erreur...",
  lintAnalyzingLLM: "L'IA analyse la sant\xE9 du wiki...",
  saveToWikiSuccess: "Conversation enregistr\xE9e dans le wiki !",
  saveSummary: "{entities} entit\xE9s, {concepts} concepts, {pages} pages",
  aliasAdded: "Alias '{alias}' ajout\xE9 \xE0 la page '{page}'",
  // Status Messages
  ingestingSources: "Import des sources en cours...",
  queryingWiki: "Interrogation du wiki...",
  lintingWiki: "V\xE9rification du wiki...",
  savingToWiki: "Enregistrement de la conversation dans le wiki...",
  clearingHistory: "Effacement de l'historique de conversation...",
  // Buttons
  ingestButton: "Importer",
  queryButton: "Interroger",
  lintButton: "V\xE9rifier",
  cancelButton: "Annuler",
  // Links
  karpathyGistLink: "Gist LLM Wiki de Karpathy",
  obsidianPluginAPI: "API des plugins Obsidian",
  anthropicSDK: "SDK Anthropic",
  openaiSDK: "SDK OpenAI",
  // Other
  availableModelsLoading: "Chargement des mod\xE8les disponibles...",
  noModelsAvailable: "Aucun mod\xE8le disponible",
  // LLM Language Hint
  llmLanguageHint: "Veuillez r\xE9pondre en fran\xE7ais.",
  // Schema Configuration
  schemaSection: "Configuration du sch\xE9ma",
  enableSchemaName: "Activer le sch\xE9ma",
  enableSchemaDesc: "G\xE9n\xE9rer et injecter schema/config.md dans toutes les invites LLM pour une sortie wiki structur\xE9e",
  viewSchemaButton: "Voir / Modifier le sch\xE9ma",
  regenerateSchemaButton: "R\xE9g\xE9n\xE9rer le sch\xE9ma par d\xE9faut",
  schemaRegeneratedNotice: "Sch\xE9ma par d\xE9faut r\xE9g\xE9n\xE9r\xE9.",
  schemaRegenerateFailed: "\xC9chec de la g\xE9n\xE9ration du sch\xE9ma",
  // Statut d\'initialisation du Wiki
  wikiInitStatusReady: "Wiki initialis\xE9",
  wikiInitStatusNotReady: "Wiki non initialis\xE9 \u2014 cr\xE9ation automatique lors du premier ingest",
  // Wiki Output Language
  wikiLanguageName: "Langue de sortie du wiki",
  wikiLanguageDesc: "Toutes les pages wiki, l'index, le journal et les r\xE9ponses de requ\xEAte utilisent cette langue",
  customWikiLanguageOption: "Personnalis\xE9...",
  customWikiLanguageHint: "Saisissez un nom de langue (p. ex. italien, arabe). Il sera transmis au LLM comme directive de langue de sortie.",
  customWikiLanguagePlaceholder: "p. ex. Italien",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "Extraction",
  extractionGranularityName: "Granularit\xE9 de l'extraction",
  extractionGranularityDesc: "Contr\xF4le les extractions par fichier source. Plus \xE9lev\xE9 = plus de pages/tokens API.\nFine: analyse approfondie. Standard: notes quotidiennes. Grossi\xE8re: vue d'ensemble rapide. Minimale: traitement 100+ fichiers. Personnalis\xE9e: limites propres (max 500).\nConseil: Utilisez Minimale/Grossi\xE8re pour les dossiers avec nombreux fichiers pour gagner du temps et r\xE9duire les co\xFBts.",
  extractionGranularityFine: "Fine \u2014 analyse approfondie (\u2264100 \xE9l\xE9ments)",
  extractionGranularityStandard: "Standard \u2014 notes quotidiennes (\u226450 \xE9l\xE9ments)",
  extractionGranularityCoarse: "Grossi\xE8re \u2014 aper\xE7u rapide (\u226410 \xE9l\xE9ments)",
  extractionGranularityMinimal: "Minimale \u2014 traitement par lot 100+ fichiers (\u22645 \xE9l\xE9ments)",
  extractionGranularityCustom: "Personnalis\xE9e \u2014 D\xE9finir vos propres limites (1~500)",
  customEntityLimitName: "Limite personnalis\xE9e d'entit\xE9s",
  customEntityLimitDesc: "Nombre maximum d'entit\xE9s \xE0 extraire par fichier source (1-500)",
  customConceptLimitName: "Limite personnalis\xE9e de concepts",
  customConceptLimitDesc: "Nombre maximum de concepts \xE0 extraire par fichier source (1-500)",
  // Issue #85 v2 : Vocabulaire des tags (saisie par chip, intégré à la configuration Wiki)
  tagVocabularyInlineDesc: "Vocabulaire contr\xF4l\xE9 pour les tags frontmatter entity et concept. Les tags imbriqu\xE9s Obsidian avec \xAB / \xBB sont conserv\xE9s.",
  tagVocabularyModeName: "Configuration du vocabulaire des tags",
  tagVocabularyModeDescDefault: "Par d\xE9faut utilise les tags int\xE9gr\xE9s : {}. Basculez sur \xAB Personnalis\xE9 \xBB pour d\xE9finir votre propre vocabulaire.",
  tagVocabularyModeDescCustom: "Personnalis\xE9 : d\xE9finissez ci-dessous vos propres tags entity et concept via la saisie par chip \u2014 Entr\xE9e ou virgule pour ajouter, \xD7 pour retirer.",
  tagVocabularyModeDefault: "Par d\xE9faut (sous-types int\xE9gr\xE9s)",
  tagVocabularyModeCustom: "Personnalis\xE9 (d\xE9fini par l'utilisateur)",
  customEntityTagsName: "Tags d'entit\xE9 personnalis\xE9s",
  customEntityTagsDesc: "Entr\xE9e ou virgule pour ajouter un chip, \xD7 pour retirer. Les tags imbriqu\xE9s \xAB / \xBB sont conserv\xE9s.",
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "Tags de concept personnalis\xE9s",
  customConceptTagsDesc: "Entr\xE9e ou virgule pour ajouter un chip, \xD7 pour retirer. Les tags imbriqu\xE9s \xAB / \xBB sont conserv\xE9s.",
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "Tag en double ignor\xE9",
  // Ingestion Acceleration
  accelerationSectionTitle: "Acc\xE9l\xE9ration de l'import",
  pageGenerationConcurrencyName: "Parall\xE9lisme LLM",
  pageGenerationConcurrencyDesc: "Nombre d'appels LLM en parall\xE8le pendant l'ingest et les op\xE9rations de lint. Des valeurs plus \xE9lev\xE9es acc\xE9l\xE8rent le traitement mais augmentent les co\xFBts API et peuvent d\xE9clencher des limites de d\xE9bit.",
  concurrencyValueSingular: "Concurrence actuelle : {} (s\xE9quentiel \u2014 le plus s\xFBr)",
  concurrencyValuePlural: "Concurrence actuelle : {} (parall\xE8le)",
  batchDelayName: "D\xE9lai entre lots (ms)",
  batchDelayDesc: "D\xE9lai entre lots parall\xE8les pour \xE9viter la limitation de d\xE9bit de l'API (100-2000ms). Actuel : {}ms. Augmentez si vous voyez des erreurs 429.",
  // Auto Maintenance
  autoMaintainSection: "Maintenance automatique",
  autoMaintainBetaBadge: "B\xCATA \u2014 Fonctionnalit\xE9 exp\xE9rimentale. Peut pr\xE9senter des probl\xE8mes. Recommand\xE9 uniquement pour les utilisateurs avanc\xE9s.",
  autoWatchName: "Surveiller des dossiers",
  autoWatchDesc: "D\xE9tecter automatiquement les nouveaux fichiers .md ou les modifications dans les dossiers surveill\xE9s et notifier ou importer automatiquement",
  watchedFoldersName: "Dossiers surveill\xE9s",
  watchedFoldersDesc: "Dossiers \xE0 surveiller pour les nouveaux contenus. Cliquez sur \xAB Ajouter un dossier \xBB pour s\xE9lectionner depuis votre coffre.",
  addWatchedFolderButton: "Ajouter un dossier",
  removeWatchedFolderButton: "Retirer",
  webClipperPresetName: "Surveiller les captures (Web Clipper)",
  webClipperPresetDesc: "Ajouter le dossier Clippings/ utilis\xE9 par Obsidian Web Clipper \xE0 la liste de surveillance. Vos captures web seront import\xE9es automatiquement dans le wiki.",
  noWatchedFoldersHint: "Aucun dossier configur\xE9. Ajoutez un dossier ou activez le pr\xE9r\xE9glage Clippings.",
  autoWatchModeName: "Mode de surveillance",
  autoWatchModeDesc: '"Notifier seulement" affiche une invite. "Import automatique" traite silencieusement.',
  watchModeNotify: "Notifier seulement",
  watchModeAuto: "Import automatique",
  autoWatchDebounceName: "D\xE9lai d'anti-rebond (secondes)",
  autoWatchDebounceDesc: "Temps d'attente avant de d\xE9clencher l'import apr\xE8s une modification de fichier (1-60 secondes)",
  periodicLintName: "V\xE9rification p\xE9riodique",
  periodicLintDesc: "Ex\xE9cuter une v\xE9rification LLM planifi\xE9e, uniquement lorsque les fichiers sources ont chang\xE9 depuis la derni\xE8re v\xE9rification",
  periodicLintOff: "D\xE9sactiv\xE9",
  periodicLintHourly: "Toutes les heures",
  periodicLintDaily: "Quotidien",
  periodicLintWeekly: "Hebdomadaire",
  autoSmartFixName: "Correction intelligente automatique",
  autoSmartFixDesc: "Lors de l'ex\xE9cution du lint, applique automatiquement toutes les corrections (Smart Fix All) sans afficher le modal de rapport. Le r\xE9sum\xE9 des corrections s'affiche toujours \xE0 la fin.",
  autoSmartFixNotice: "Correction intelligente automatique : application de toutes les corrections...",
  startupCheckName: "Ex\xE9cuter les corrections rapides au d\xE9marrage",
  startupCheckDesc: "Corrige automatiquement les probl\xE8mes de format de bas niveau (champ sources, wikilinks doublement imbriqu\xE9s) au chargement du plugin. V\xE9rifie la structure du dossier Wiki. Activ\xE9 par d\xE9faut.",
  suggestSchemaCommand: "Sugg\xE9rer des mises \xE0 jour du sch\xE9ma",
  autoMaintainCostWarning: "\u26A0\uFE0F Avertissement sur les co\xFBts : Les fonctionnalit\xE9s de maintenance automatique consomment des tokens API. L'\xAB Import automatique \xBB d\xE9clenche des appels LLM \xE0 chaque modification de fichier source. La \xAB V\xE9rification p\xE9riodique \xBB ex\xE9cute des contr\xF4les de sant\xE9 LLM selon un planning (uniquement lorsque des modifications de source sont d\xE9tect\xE9es). Configurez avec soin pour \xE9viter des frais impr\xE9vus.",
  // Notices
  startupCheckSummary: "Le wiki comporte {pages} pages ({entities} entit\xE9s, {concepts} concepts, {sources} sources)",
  watcherActiveNotice: "Wiki : surveillance de fichiers active \u2014 dossiers surveill\xE9s en cours de suivi",
  watchIngestNotice: "Wiki : {count} fichier(s) modifi\xE9(s) dans sources/. Ex\xE9cutez \xAB Importer les sources \xBB pour traiter.",
  autoIngestRunning: "Import automatique de {count} fichier(s) modifi\xE9(s) en cours...",
  autoIngestComplete: "Import automatique termin\xE9 : {success} r\xE9ussi(s), {fail} \xE9chou\xE9(s)",
  scheduledLintRunning: "Ex\xE9cution de la v\xE9rification p\xE9riodique du wiki...",
  wikiLintStats: "V\xE9rification wiki : {pages} pages ({entities} entit\xE9s, {concepts} concepts, {sources} sources)",
  wikiHealthStats: "Sant\xE9 du wiki : {pages} pages ({entities} entit\xE9s, {concepts} concepts, {sources} sources){indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Corrections rapides Wiki termin\xE9es",
  startupCheckStructureLabel: "Structure Wiki",
  startupCheckStructureOk: "compl\xE8te",
  startupCheckStructureMissing: "incompl\xE8te \u2014 sera cr\xE9\xE9e automatiquement au premier ingest",
  startupCheckSourcesLabel: "Sources normalis\xE9s",
  startupCheckSourcesClean: "d\xE9j\xE0 propre",
  startupCheckSourcesCleaned: "{files} fichier(s), {entries} entr\xE9e(s) nettoy\xE9e(s)",
  startupCheckIncompleteClean: "pages incompl\xE8tes : aucune",
  startupCheckIncompleteArchived: "pages incompl\xE8tes : {count} archiv\xE9e(s) (r\xE9cup\xE9rable depuis .trash)",
  startupCheckDisableHint: "Pour d\xE9sactiver : Param\xE8tres \u2192 Maintenance automatique \u2192 Ex\xE9cuter les corrections rapides au d\xE9marrage",
  lintWikiStart: "D\xE9marrage de la v\xE9rification du wiki...",
  lintWikiComplete: "V\xE9rification du wiki termin\xE9e",
  lintWikiFailed: "\xC9chec de la v\xE9rification du wiki",
  analyzingSchema: "Analyse du wiki et g\xE9n\xE9ration des suggestions de sch\xE9ma...",
  schemaSuggestionGenerated: "Suggestions de sch\xE9ma g\xE9n\xE9r\xE9es, consultez wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "Aucune mise \xE0 jour du sch\xE9ma n\xE9cessaire.",
  schemaSuggestionFailed: "\xC9chec de la suggestion de sch\xE9ma",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Aper\xE7u de mise \xE0 jour du sch\xE9ma",
  schemaDiffSummary: "+{add} / -{del} lignes",
  schemaDiffRegenerateLabel: "Affiner la suggestion (optionnel) :",
  schemaDiffRegenerateBtn: "R\xE9g\xE9n\xE9rer",
  schemaDiffOpenFileBtn: "Ouvrir le fichier",
  schemaDiffApplyBtn: "Appliquer",
  schemaDiffApplied: "Sch\xE9ma mis \xE0 jour. Sauvegarde : {path}",
  schemaDiffEmptyTitle: "LLM ne recommande aucune modification",
  schemaDiffFailed: "\xC9chec de l'application du sch\xE9ma : {reason}",
  schemaDiffRestoreHint: `Sauvegarde enregistr\xE9e dans {path}. Pour restaurer, renommez ce fichier en "wiki/schema/config.md" dans l'explorateur de fichiers. Les 3 sauvegardes les plus r\xE9centes sont conserv\xE9es.`,
  schemaRegenerateNoBody: "R\xE9g\xE9n\xE9ration r\xE9ussie mais le LLM n'a pas renvoy\xE9 de nouveau sch\xE9ma.",
  schemaNotFoundNotice: "Fichier de sch\xE9ma introuvable. Activez le sch\xE9ma pour le cr\xE9er.",
  selectFolderNoMdFiles: "Aucun fichier Markdown dans le dossier : {path}",
  batchIngestSkipNotice: "{skipped}/{total} fichiers d\xE9j\xE0 import\xE9s ignor\xE9s. Import de {new} nouveau(x) fichier(s)...",
  batchIngestAllIngested: "Tous les {total} fichiers de ce dossier ont d\xE9j\xE0 \xE9t\xE9 import\xE9s.",
  batchIngestStarting: "Import de {count} fichier(s) depuis \xAB {folder} \xBB \u2014 cela peut prendre plusieurs minutes. Un rapport appara\xEEtra une fois termin\xE9.",
  batchIngestComplete: "Import par lot termin\xE9 : {success}/{total} r\xE9ussi(s), {fail} \xE9chou\xE9(s)",
  batchIngestFailedFiles: "Fichiers ayant \xE9chou\xE9 :",
  historyTruncated: "Historique tronqu\xE9 aux {max} derniers tours",
  historyCleared: "Historique effac\xE9",
  // User Feedback Loop
  reviewedPagePreserved: "Pr\xE9servation du contenu r\xE9vis\xE9 par l'utilisateur pour : {}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "Enregistrer dans le wiki ?",
  querySuggestSaveDesc: "Cette conversation contient des connaissances utiles. Voulez-vous les enregistrer dans votre wiki ?",
  querySuggestSaveYes: "Enregistrer",
  querySuggestSaveNo: "Ignorer",
  // Ingestion Report
  ingestReportElapsedTime: "Temps \xE9coul\xE9",
  ingestReportSkippedFiles: "Ignor\xE9s (d\xE9j\xE0 import\xE9s)",
  ingestReportRejectedFiles: "Ignor\xE9s",
  rejectionReasonEmpty: "vide",
  rejectionReasonType: "type non pris en charge",
  rejectionReasonDuplicate: "contenu en double",
  ingestReportFailedGuidance: "Ces \xE9l\xE9ments n'ont pas pu \xEAtre cr\xE9\xE9s automatiquement. Vous pouvez cr\xE9er manuellement les pages correspondantes, ou r\xE9duire la granularit\xE9 d'extraction et r\xE9importer le fichier source.",
  ingestReportCollisions: "Collisions cross-type (fusionn\xE9es comme alias)",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "Importer une source unique",
  cmdIngestFolder: "Importer depuis un dossier",
  cmdQueryWiki: "Interroger le wiki",
  cmdLintWiki: "V\xE9rifier le wiki",
  cmdRegenerateIndex: "R\xE9g\xE9n\xE9rer l'index",
  cmdSuggestSchema: "Sugg\xE9rer des mises \xE0 jour du sch\xE9ma",
  cmdCancelIngestion: "Annuler l'ingestion en cours",
  cmdIngestActiveFile: "Ing\xE9rer le fichier actif",
  cmdViewHistory: "Voir l'historique d'ingestion",
  noActiveFile: "Aucun fichier ouvert",
  mdOnlyFile: "Seuls les fichiers Markdown peuvent \xEAtre ing\xE9r\xE9s",
  // Ingestion status bar
  ingestionStatusBar: "Ingestion en cours... cliquer pour annuler",
  lintStatusBar: "V\xE9rification en cours... cliquer pour annuler",
  ingestStatusAnalyzing: "Ingestion\u2026 (cliquer pour annuler)",
  lintStatusReading: "V\xE9rification\u2026 (cliquer pour annuler)",
  lintStatusDuplicates: "V\xE9rification\u2026 (cliquer pour annuler)",
  lintStatusScanningLinks: "V\xE9rification\u2026 (cliquer pour annuler)",
  lintStatusAnalyzing: "V\xE9rification\u2026 (cliquer pour annuler)",
  ingestionCancelling: "Annulation \u2014 arr\xEAt apr\xE8s le lot en cours",
  ingestionCancelled: "Ingestion annul\xE9e",
  crossTypeCollisionNotice: "{count} \xE9l\xE9ments fusionn\xE9s comme alias cross-type (doublons entit\xE9 \u2194 concept \xE9vit\xE9s)",
  // Lint Report
  lintReportTitle: "Rapport de v\xE9rification du wiki",
  lintReportSummary: "Aper\xE7u de l'\xE9tat du wiki : {total} pages au total, {aliasesMissing} pages sans alias, {duplicates} pages dupliqu\xE9es, {deadLinks} liens cass\xE9s ({deadLinkFromDup} impliquent des doublons), {orphans} pages orphelines ({orphanFromDup} sont des doublons), {emptyPages} pages vides, {ungroundedQuotes} citations non fond\xE9es, {tagViolations} tags hors vocabulaire. Dur\xE9e du lint : {elapsedSeconds}s",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "Param\xE8tres avanc\xE9s",
  advancedSettingsModeDesc: "Le mode par d\xE9faut utilise le comportement natif de votre fournisseur \u2014 aucun champ de contr\xF4le du raisonnement, de temp\xE9rature ou de p\xE9nalit\xE9 de r\xE9p\xE9tition n'est envoy\xE9. Cela convient \xE0 la plupart des utilisateurs. Ne passez en mode Personnalis\xE9 que si vous devez explicitement remplacer les param\xE8tres par d\xE9faut du fournisseur (par exemple, forcer un dialecte de raisonnement sp\xE9cifique, d\xE9finir une temp\xE9rature autre que celle par d\xE9faut, etc.).",
  advancedSettingsDefault: "Par d\xE9faut (comportement du fournisseur)",
  advancedSettingsCustom: "Personnalis\xE9 (remplacer les param\xE8tres par d\xE9faut du fournisseur)",
  disableThinkingName: "D\xE9sactiver le raisonnement",
  disableThinkingDesc: "Optionnel. Lorsqu'il est activ\xE9, le plugin envoie une directive de contr\xF4le du raisonnement au fournisseur et parcourt une cha\xEEne de repli dialectal en 3 niveaux (anthropic \u2192 openai \u2192 none) si le fournisseur la refuse. Utilisez-le uniquement si votre fournisseur laisse le raisonnement s'infiltrer dans la r\xE9ponse et que vous devez le supprimer. La plupart des fournisseurs g\xE8rent correctement le raisonnement d'eux-m\xEAmes \u2014 laisser cette option d\xE9sactiv\xE9e (par d\xE9faut) offre une meilleure qualit\xE9.",
  // Issue #137 : Notes de compatibilité (courtes ; pas de liste de fournisseurs)
  extractionTemperatureName: "Temp\xE9rature d'extraction",
  extractionTemperatureDesc: "Plage 0\u20132. Plus la valeur est basse, plus la sortie LLM est d\xE9terministe et fid\xE8le. Plus elle est haute, plus elle est cr\xE9ative. Laissez vide pour utiliser la valeur par d\xE9faut du fournisseur. Si votre fournisseur refuse cette valeur, le plugin supprime le champ automatiquement et affiche un avis unique.",
  chatTemperatureName: "Temp\xE9rature de requ\xEAte",
  chatTemperatureDesc: "Plage 0\u20132. Comme la temp\xE9rature d'extraction, mais n'affecte que les r\xE9ponses de chat/requ\xEAte. Laissez vide pour utiliser la valeur par d\xE9faut du fournisseur. Si votre fournisseur refuse cette valeur, le plugin supprime le champ automatiquement et affiche un avis unique.",
  repetitionPenaltyName: "P\xE9nalit\xE9 de r\xE9p\xE9tition",
  repetitionPenaltyDesc: "Plage 0\u20132. Plus la valeur est haute, plus les r\xE9p\xE9titions sont r\xE9duites. Laissez vide pour utiliser la valeur par d\xE9faut du fournisseur. Note de compatibilit\xE9 : de nombreux fournisseurs cloud n'acceptent pas ce champ. Le plugin le supprime automatiquement sur HTTP 400 et affiche un avis unique.",
  temperaturePlaceholder: "vide = valeur par d\xE9faut du fournisseur",
  lintDeadLinkSection: "Liens cass\xE9s (d\xE9tect\xE9s) [{count}]",
  lintEmptyPageSection: "Pages vides (d\xE9tect\xE9es) [{count}]",
  lintOrphanSection: "Pages orphelines (d\xE9tect\xE9es) [{count}]",
  lintContradictionSection: "Contradictions (d\xE9tect\xE9es)",
  lintDuplicateSection: "Pages dupliqu\xE9es (d\xE9tect\xE9es)",
  lintNoIssuesFound: "Aucun doublon, lien cass\xE9, page vide, page orpheline ou citation non fond\xE9e d\xE9tect\xE9.",
  lintQuoteGroundingSection: "Citations non fond\xE9es (d\xE9tect\xE9es) [{count}]",
  lintQuoteGroundingItem: "- [[{page}]]{sourceHint}: \xAB {quote} \xBB",
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (la page n'existe pas){dupFlag}",
  lintDeadLinkMore: "- ... {count} liens cass\xE9s suppl\xE9mentaires",
  lintEmptyPageItem: "- [[{page}]] \u2014 moins de 50 caract\xE8res de contenu substantiel",
  lintOrphanItem: "- [[{page}]] \u2014 aucune autre page wiki ne pointe vers ici{dupFlag}",
  lintPollutedSection: "Pages pollu\xE9es (d\xE9tect\xE9es) [{count}]",
  lintPollutedItem: '- [[{page}]] \u2192 devrait \xEAtre "{clean}"',
  lintSourcesNormalizedSection: "Sources normalis\xE9es (auto-corrig\xE9es) [{files} fichiers / {entries} entr\xE9es]",
  lintSourcesNormalizedItem: "Nettoy\xE9 {entries} entr\xE9es de sources pollu\xE9es dans {files} fichier(s) (chemins externes, extensions .md, pipes d'alias supprim\xE9s et d\xE9duplication).",
  lintDuplicateItem: "- [[{target}]] et [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F implique une page dupliqu\xE9e)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F page dupliqu\xE9e)",
  lintContradictionOpen: "Contradictions non r\xE9solues : {count}",
  lintContradictionAutoFixed: "({count} corrig\xE9(s) automatiquement cette ex\xE9cution)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "D\xE9tect\xE9",
  lintContradictionStatusPendingFix: "Correction en attente",
  lintLLMAnalysisHeading: "## Analyse LLM",
  // Lint Analysis Prompt
  lintAnalysisPrompt: "Vous \xEAtes un assistant de maintenance wiki. V\xE9rifiez la sant\xE9 du wiki sur la base des informations suivantes.\n\nIndex wiki :\n{index}\n\n\xC9chantillon de contenu des pages wiki ({total} pages au total, {sample} pages affich\xE9es) :\n{contentSample}\n\nR\xE9sultats des v\xE9rifications programmatiques (d\xE9j\xE0 v\xE9rifi\xE9s, ne pas r\xE9p\xE9ter) :\n{progReport}\n\nV\xE9rifiez les aspects suivants (ignorez les liens cass\xE9s/pages vides/pages orphelines d\xE9j\xE0 d\xE9tect\xE9s par les v\xE9rifications programmatiques) :\n1. **Contradictions** \u2014 des pages diff\xE9rentes se contredisent-elles sur les m\xEAmes faits\n2. **Obsolescence** \u2014 certaines affirmations sont-elles clairement d\xE9pass\xE9es\n3. **Manquants** \u2014 quels concepts importants n'ont pas de page d\xE9di\xE9e\n4. **Structure** \u2014 la structure des pages est-elle raisonnable et les r\xE9f\xE9rences crois\xE9es sont-elles ad\xE9quates\n\nFormat de sortie : utilisez Markdown, en commen\xE7ant par \xAB ## Analyse LLM \xBB. Chaque constatation sur une ligne \xAB - [probl\xE8me sp\xE9cifique] \xBB. S'il n'y a pas de probl\xE8mes, \xE9crivez \xAB Aucun probl\xE8me \xE9vident trouv\xE9. \xBB",
  // Lint Fix Progress
  lintFixProgress: "R\xE9paration {current}/{total} : [[{target}]]",
  lintFixDeadComplete: "R\xE9paration des liens cass\xE9s termin\xE9e. {fixed}/{total} \xE9l\xE9ments r\xE9par\xE9s.",
  lintFillProgress: "Expansion {current}/{total} : {page}",
  lintFillComplete: "Expansion des pages termin\xE9e. {filled}/{total} pages remplies.",
  lintDeleteCompleted: "{count} stubs vides supprim\xE9s",
  lintDeleteFailed: "\xC9chec de suppression de {failed}/{total} stubs vides (voir la console)",
  lintFillFailed: "\xC9chec de l'expansion : {page} \u2014 {error}",
  lintLinkProgress: "Lien {current}/{total} : {page}",
  lintLinkComplete: "Liens des pages orphelines termin\xE9s. {linked} pages li\xE9es.",
  lintFixNoAction: "Aucune action entreprise (pas de client)",
  lintFixIndexUpdated: "L'index et le journal wiki ont \xE9t\xE9 mis \xE0 jour.",
  lintPollutedFixed: "Pages pollu\xE9es corrig\xE9es : {fixed}/{total}. Index r\xE9g\xE9n\xE9r\xE9.",
  regenerateIndexCompleted: "Index r\xE9g\xE9n\xE9r\xE9",
  operationFailed: "\xC9chec : ",
  lintFixAllComplete: "Toutes les r\xE9parations sont termin\xE9es. Consultez le journal pour les d\xE9tails.",
  // Lint Report Modal
  lintModalActionsTitle: "Suggestions de r\xE9paration (n\xE9cessite des tokens LLM) :",
  lintLogReference: "Rapport complet enregistr\xE9 dans log.md",
  lintModalFixDeadLinks: "R\xE9parer les liens cass\xE9s ({count})",
  lintModalExpandEmpty: "\xC9tendre les pages vides ({count})",
  lintModalDeleteEmpty: "Supprimer les stubs vides ({count})",
  lintModalLinkOrphans: "Lier les pages orphelines ({count})",
  lintModalAnalyzeSchema: "Analyser le sch\xE9ma",
  lintModalMergeDuplicates: "Fusionner les doublons ({count})",
  lintModalFixAll: "R\xE9paration intelligente de tout ({count} probl\xE8mes)",
  lintMergeProgress: "Fusion {current}/{total} : {source} \u2192 {target}",
  lintMergeComplete: "Fusion des doublons termin\xE9e. {merged}/{total} paires fusionn\xE9es.",
  // Ingest Report Modal
  ingestReportTitle: "Rapport d'import",
  ingestReportSourceFile: "Fichier source",
  ingestReportCreated: "Cr\xE9\xE9es",
  ingestReportUpdated: "Mises \xE0 jour",
  ingestReportContradictions: "Contradictions trouv\xE9es",
  ingestReportFailedTitle: "\xC9chec de l'import",
  ingestReportErrorDetail: "D\xE9tail de l'erreur",
  ingestReportClose: "Fermer",
  ingestReportCreatedPages: "Pages cr\xE9\xE9es : {count}",
  ingestReportUpdatedPages: "Pages mises \xE0 jour : {count}",
  ingestReportEntitiesCount: "{count} entit\xE9s",
  ingestReportConceptsCount: "{count} concepts",
  ingestReportContradictionsFound: "Contradictions trouv\xE9es : {count}",
  ingestReportEntityType: "Entit\xE9",
  ingestReportConceptType: "Concept",
  timeMinutes: "min",
  timeSeconds: "s",
  // Avertissements de limite de débit
  rateLimitDetected: "\u26A0\uFE0F Limite de d\xE9bit d\xE9tect\xE9e : {count} page(s) ont \xE9chou\xE9 avec des erreurs 429. Suggestions : (1) R\xE9duire la concurrence \xE0 {suggestedConcurrency} ou 1 (s\xE9quentiel), (2) Augmenter le d\xE9lai entre lots \xE0 {suggestedDelay}ms, (3) Passer \xE0 un fournisseur avec des limites de d\xE9bit plus \xE9lev\xE9es.",
  rateLimitDetectedShort: "\u26A0\uFE0F Limite de d\xE9bit atteinte \u2014 envisagez de r\xE9duire la concurrence ou d'augmenter le d\xE9lai entre lots dans Param\xE8tres \u2192 Acc\xE9l\xE9ration d'import.",
  sourceRejectedEmpty: "\u23ED\uFE0F \xAB {filename} \xBB n'a aucun contenu \xE0 ing\xE9rer \u2014 ignor\xE9. Les notes vides ou ne contenant que des m\xE9tadonn\xE9es ne cr\xE9ent pas de pages wiki.",
  sourceRejectedType: "\u23ED\uFE0F \xAB {filename} \xBB n'est pas un type de fichier pris en charge \u2014 ignor\xE9. Seules les notes texte (.md, .txt, etc.) peuvent \xEAtre ing\xE9r\xE9es.",
  sourceRejectedDuplicate: "\u23ED\uFE0F \xAB {filename} \xBB ignor\xE9 \u2014 son contenu est d\xE9j\xE0 dans le wiki.",
  ingestRejectedSummary: "{count} fichier(s) ignor\xE9(s) (vide, doublon ou type non pris en charge).",
  reingestConfirmTitle: "R\xE9-ing\xE9rer ce fichier ?",
  reingestConfirmBody: "Le contenu de \xAB {filename} \xBB est d\xE9j\xE0 dans le wiki. Le r\xE9-ing\xE9rer quand m\xEAme ?",
  reingestConfirmYes: "R\xE9-ing\xE9rer",
  reingestConfirmNo: "Ignorer",
  longSourceNotice: '\u{1F4C4} "{filename}" contient {lines} lignes ({size}). Les textes longs n\xE9cessitent une extraction it\xE9rative par lots \u2014 le LLM lit le document complet en plusieurs passes. Cela peut prendre plusieurs minutes. Veuillez patienter.',
  longSourceNoticeShort: "\u{1F4C4} Fichier volumineux d\xE9tect\xE9 ({lines} lignes). Ingestion peut prendre du temps.",
  // Panneau Historique d'ingestion (#122) — textes UI
  historyButton: "Historique des op\xE9rations",
  historyButtonDesc: "Afficher les ingestions r\xE9centes, les rapports Lint et autres modifications du Wiki",
  historyButtonOpen: "Voir l'historique",
  historyModalTitle: "Historique des op\xE9rations",
  historyModalSubtitle: "Modifications r\xE9centes de votre LLM-Wiki",
  historyEmpty: "Aucune op\xE9ration enregistr\xE9e. Ing\xE9rez une note ou ex\xE9cutez Lint pour remplir cette liste.",
  historyReadError: "Impossible de lire le journal d'op\xE9rations : {error}",
  historyEntryKindIngest: "Ingestion",
  historyEntryKindMaintenance: "Maintenance",
  historyEntryKindFix: "Correction",
  historyEntryKindOther: "Autre",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "Source : {source}",
  historyEntrySourceUnknown: "Ingestion sans titre",
  historyEntryCreatedLabel: "Cr\xE9\xE9es",
  historyEntryUpdatedLabel: "Mises \xE0 jour",
  historyEntryCreatedCount: "{count} cr\xE9\xE9es",
  historyEntryUpdatedCount: "{count} mises \xE0 jour",
  historyEntryNoChanges: "Aucun changement de page",
  historyEntryContradictions: "\u26A0\uFE0F {count} contradiction(s)",
  historyEntrySectionCreated: "Pages cr\xE9\xE9es",
  historyEntrySectionUpdated: "Pages mises \xE0 jour",
  historyEntrySectionContradictions: "Contradictions",
  historyEntrySectionDetails: "D\xE9tails",
  historyEntryOpenPage: "Ouvrir",
  historyEntryDetailsNoContradictions: "Aucune contradiction trouv\xE9e",
  historySearchPlaceholder: "\u{1F50D} Rechercher par titre de source ou chemin de page\u2026",
  historyFilterAll: "Toutes",
  historyFilterIngest: "Ingestions seules",
  historyFilterMaintenance: "Maintenance seule",
  historyFilterFix: "Corrections seules",
  historyFilterContradictions: "\u26A0\uFE0F Avec contradictions",
  historyRefreshButton: "\u27F3 Actualiser",
  historyExpandDay: "Cliquer pour d\xE9plier",
  historyCollapseDay: "Cliquer pour replier",
  historyShowMore: "Afficher les {count} entr\xE9es plus anciennes",
  historyNoMatch: "Aucune op\xE9ration ne correspond \xE0 votre recherche/filtre.",
  historyCloseButton: "Fermer",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "R\xE9sultats du rapport",
  historyKpiPages: "Pages",
  historyKpiDeadLinks: "Liens morts",
  historyKpiOrphans: "Orphelins",
  historyKpiEmpty: "Vides",
  historyKpiDuplicates: "Doublons",
  historyKpiTagViolations: "Tags",
  historyKpiUnsourced: "Sans source",
  historyKpiDuration: "Dur\xE9e Lint",
  historyKpiDurationSec: "{seconds}s",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "Liens morts ({count})",
  historySectionTagViolations: "Tags ({count})",
  historySectionOrphans: "Pages orphelines ({count})",
  historySectionEmptyPages: "Pages vides ({count})",
  historySectionLlmAnalysis: "Analyse LLM ({count})",
  historyDeadLinkSource: "Source",
  historyDeadLinkTarget: "Manquant",
  historyOpenInLog: "Ouvrir dans log.md",
  historyShowMoreItems: "Afficher {count} de plus",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 stable",
  historyChipContradiction: "Contradiction",
  historyChipOutdated: "Obsol\xE8te",
  historyChipMissing: "Manquant",
  historyChipStructure: "Structure",
  historySeverityHigh: "Haute",
  historySeverityMedium: "Moyenne",
  historySeverityLow: "Basse",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Votre Wiki contient {dead} lien(s) mort(s), {orphans} page(s) orpheline(s) et {tags} probl\xE8me(s) de tag. Derni\xE8re Lint : {duration}.",
  historyGlobalInsightClean: "Votre Wiki est sain \u2014 aucun lien mort, page orpheline ou probl\xE8me de tag d\xE9tect\xE9.",
  historyGlobalInsightNoData: "Aucun rapport de maintenance \u2014 ex\xE9cutez Lint.",
  historyModalHeaderTitle: "Historique des op\xE9rations",
  historyModalSubtitleWithCount: "Modifications r\xE9centes de votre LLM-Wiki \xB7 {count} entr\xE9es",
  historyTimeRangeAll: "Toute la p\xE9riode",
  historyTimeRange1d: "Dernier jour",
  historyTimeRange3d: "3 derniers jours",
  historyTimeRange1w: "Semaine derni\xE8re",
  historyTimeRange1m: "Mois dernier",
  historyIngestTotal: "Pages totales",
  historyIngestByType: "Par type",
  historyIngestSource: "Fichier source",
  historyIngestNoTimestamp: "Pas d'horodatage",
  historyIngestFirstTime: "Premi\xE8re ingestion",
  historyIngestLatestTime: "Derni\xE8re ingestion",
  historyTimeRangeCustom: "P\xE9riode personnalis\xE9e",
  historyCustomRangeFrom: "Du",
  historyCustomRangeTo: "Au",
  historyCustomRangeApply: "Appliquer",
  historyCustomRangeClear: "Effacer"
};

// src/texts/es.ts
var ES_TEXTS = {
  // Plugin Info
  pluginTitle: "Karpathy LLM Wiki Configuraci\xF3n",
  pluginIntro: "Este plugin implementa el concepto de {{link}} para Obsidian. Lee tus notas, usa IA para extraer entidades, conceptos y relaciones, y construye una Wiki estructurada e interconectada en tu vault.",
  karpathyLinkText: "Wiki LLM de Andrej Karpathy",
  // Features Section
  featuresTitle: "C\xF3mo funciona",
  workflow1Title: "1. Ingesti\xF3n",
  workflow1Desc: "Selecciona una nota \u2014 la IA extrae entidades, conceptos y relaciones en p\xE1ginas Wiki.",
  workflow2Title: "2. Consulta",
  workflow2Desc: "Chatea con tu Wiki \u2014 las respuestas se fundamentan en tu conocimiento ingerido.",
  workflow3Title: "3. Mantenimiento",
  workflow3Desc: "\xCDndice autogenerado, enlaces cruzados y verificaciones lint mantienen la Wiki saludable.",
  // Language Switcher
  languageTitle: "Idioma de la interfaz",
  languageDesc: "Selecciona tu idioma preferido para el panel de configuraci\xF3n. Reinicia el plugin para aplicar los cambios de idioma a la paleta de comandos.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "Estado del cliente LLM",
  llmWikiStatusSection: "Estado de LLM-Wiki",
  statusInitialized: "Inicializado",
  statusReady: "LLM listo",
  statusNotReady: "LLM no configurado \u2014 complete la configuraci\xF3n y pase la prueba de conexi\xF3n",
  statusNotInitialized: "No inicializado",
  currentProvider: "Proveedor actual",
  // Provider Configuration
  providerSection: "Configuraci\xF3n LLM",
  providerName: "Proveedor LLM",
  providerDesc: "Selecciona un proveedor predefinido o un servicio compatible con OpenAI personalizado. Para Coding Plan o bundles similares: elige Custom OpenAI/Anthropic, ingresa manualmente el Base URL y API Key del proveedor",
  // API Key
  apiKeyName: "API Key",
  apiKeyDesc: "Introduce tu API Key del proveedor",
  apiKeyPlaceholder: "Introduce la API Key",
  // Base URL
  baseUrlName: "API Base URL",
  baseUrlDescCustom: "Requerido: endpoint personalizado compatible con OpenAI",
  baseUrlDescOverride: "Opcional: sobrescribe la Base URL preconfigurada",
  // Ollama Hint
  ollamaHint: "Ollama se ejecuta localmente, no requiere API Key",
  lmstudioHint: "LM Studio se ejecuta localmente, API Key es opcional",
  maxTokensPerCallName: "Ventana de contexto",
  maxTokensPerCallDesc: "Limitar tokens a la ventana de contexto del modelo. 0 = sin l\xEDmite (cloud).",
  slugCaseName: "May\xFAsculas en nombres de archivo",
  slugCaseDesc: "Controla si los nombres de archivo Wiki generados se ponen en min\xFAsculas. Se recomienda 'Conservar' para idiomas donde las may\xFAsculas son significativas (p. ej. sustantivos alemanes).",
  slugCaseLower: "Min\xFAsculas (predeterminado)",
  slugCasePreserve: "Conservar may\xFAsculas",
  // Model Selection
  modelSection: "Selecci\xF3n del modelo",
  fetchModelsName: "Obtener modelos disponibles",
  fetchModelsDesc: "Obtiene la lista de modelos m\xE1s reciente desde la API del proveedor",
  fetchModelsButton: "Obtener modelos",
  fetchingModels: "Obteniendo...",
  fetchSuccess: "\xA1Correcto! {} modelos disponibles",
  fetchFailed: "Error o lista vac\xEDa. Introduce el nombre del modelo manualmente",
  fetchNotSupported: "El proveedor no admite consultas de lista de modelos",
  fetchErrorAuth: "Autenticaci\xF3n fallida (HTTP 401/403). Verifica la clave API, o introduce un ID de modelo y haz clic en Probar conexi\xF3n.",
  fetchErrorEndpoint: "Endpoint no encontrado (HTTP 404). Verifica la URL del endpoint, o introduce un ID de modelo y haz clic en Probar conexi\xF3n.",
  fetchErrorServer: "Error del servidor del proveedor (HTTP 5xx). Int\xE9ntalo m\xE1s tarde, o introduce un ID de modelo y haz clic en Probar conexi\xF3n.",
  fetchErrorEmpty: "Este endpoint no expone lista de modelos. Introduce un ID de modelo y haz clic en Probar conexi\xF3n.",
  fetchErrorNetwork: "Fall\xF3 la solicitud de red. Verifica tu conexi\xF3n a internet, la URL del endpoint o la configuraci\xF3n del proxy. Tambi\xE9n puedes introducir un ID de modelo y hacer clic en Probar conexi\xF3n.",
  selectModelName: "Seleccionar modelo",
  selectModelDesc: "Elige entre {} modelos disponibles",
  customInputOption: "Entrada personalizada...",
  customInputHint: 'Para usar otros modelos, selecciona "Entrada personalizada..."',
  modelName: "Nombre del modelo",
  modelDescCustom: "Usando modelo personalizado (pulsa el bot\xF3n de arriba para volver a obtener la lista)",
  modelDescFetchFailed: "Error al obtener la lista de modelos. Verifica la clave API y la URL del endpoint, o introduce un ID de modelo y haz clic en Probar conexi\xF3n.",
  modelInputPlaceholder: "Introduce el ID del modelo y prueba la conexi\xF3n",
  switchToDropdown: "Cambiar a selecci\xF3n con men\xFA desplegable",
  useDropdownButton: "Usar men\xFA desplegable",
  // Test & Save
  testConnectionName: "Probar conexi\xF3n",
  testConnectionDesc: "Valida que la configuraci\xF3n puede llamar correctamente a la API LLM",
  testButton: "Probar conexi\xF3n",
  testing: "Probando...",
  saveSettingsName: "Guardar configuraci\xF3n",
  saveSettingsDesc: "Guarda la configuraci\xF3n actual",
  saveButton: "Guardar configuraci\xF3n",
  testConnectionSuccessful: "Conexi\xF3n exitosa",
  testConnectionFailed: "Conexi\xF3n fallida",
  testConnectionProvider: "Proveedor: ",
  errorUnknown: "Error desconocido",
  savedNotice: "\xA1Configuraci\xF3n guardada!",
  // Issue #137: Avisos de fallback de LLM
  fallbackThinkingDialect: 'Control de pensamiento: cambiado al dialecto "{dialect}" (este proveedor usa un formato diferente). La salida no cambia.',
  fallbackThinkingNone: "Control de pensamiento completamente deshabilitado para este proveedor. Puede seguir apareciendo contenido de razonamiento; si ocurre, pruebe con otro modelo.",
  fallbackParamStripped: 'El par\xE1metro "{field}" no es compatible con este proveedor. Se ha eliminado de la solicitud; el comportamiento puede diferir de la configuraci\xF3n.',
  // Wiki Folder
  wikiSection: "Configuraci\xF3n del Wiki",
  wikiFolderName: "Carpeta Wiki",
  wikiFolderDesc: "Ubicaci\xF3n para las p\xE1ginas Wiki generadas",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "Configura primero la API Key",
  llmNotReady: "LLM no est\xE1 configurado. Vaya a Configuraci\xF3n \u2192 Karpathy LLM Wiki para configurar su proveedor y pasar la prueba de conexi\xF3n.",
  errorFetchFailed: "Error: {}",
  // Query Settings
  querySectionTitle: "Configuraci\xF3n de consulta Wiki",
  maxConversationHistoryName: "M\xE1ximo de historial de conversaci\xF3n",
  maxConversationHistoryDesc: "Limita los mensajes de conversaci\xF3n para evitar desbordamiento de tokens",
  maxConversationHistoryHint: "Recomendado: no superar 50 rondas",
  numberRangeValidation: "Introduce un n\xFAmero entre 1 y 50",
  numberRangeClamped: "Valor fuera del rango (1-500), autom\xE1ticamente establecido a {}",
  // Query Modal UI
  queryModalTitle: "Consultar Wiki - Consulta conversacional",
  queryModalPlaceholder: "Introduce una pregunta...",
  queryModalSendButton: "Enviar",
  queryModalStopButton: "Detener",
  queryModalSaveButton: "Guardar en Wiki",
  queryModalClearButton: "Limpiar historial",
  queryModalHistoryCount: "Historial de conversaci\xF3n: {}/{} rondas",
  queryModalStreaming: "Transmisi\xF3n en curso...",
  queryModalFallbackStreaming: "Transmisi\xF3n no disponible, cambiado a modo sin transmisi\xF3n. Espera...",
  queryPhaseSearching: "Analizando \xEDndice Wiki, seleccionando p\xE1ginas relevantes...",
  queryPhaseFoundPages: "Se encontraron {count} p\xE1gina(s): {pages}",
  queryPhaseLoadingPages: "Cargando contenido de las p\xE1ginas...",
  queryPhaseContextReady: "Contexto listo. Generando respuesta...",
  queryPhaseGenerating: "Generando... (transcurrido {time}s)",
  queryPhaseNonStreaming: "Modo sin transmisi\xF3n, generando respuesta... (transcurrido {time}s)",
  queryModalErrorPrefix: "Error: ",
  queryModalHint: 'Las consultas se basan en el contenido de la Wiki. Pulsa "Guardar en Wiki" para extraer conversaciones valiosas como p\xE1ginas Wiki.',
  // v1.20.0: Resumen del bloque de pensamiento plegable de Query Wiki
  queryThinkingSummary: "Proceso de pensamiento",
  queryThinkingSteps: "pasos",
  // Error Messages
  errorLLMClientNotInit: "Cliente LLM no inicializado. Guarda la configuraci\xF3n.",
  errorIngestFailed: "La ingesti\xF3n fall\xF3: ",
  errorQueryFailed: "La consulta fall\xF3: ",
  fileWriteFailed: "No se pudo crear ni actualizar el archivo: {path}",
  // Success Messages
  ingestSuccess: "Ingesti\xF3n completada: {} p\xE1ginas creadas, {} p\xE1ginas actualizadas",
  querySuccess: "Consulta completada",
  lintSuccess: "Verificaci\xF3n lint de Wiki completada",
  lintReadingPages: "Leyendo {count} p\xE1ginas Wiki...",
  lintReadingPagesProgress: "Leyendo p\xE1ginas Wiki: {current}/{total}...",
  lintScanningLinks: "Escaneando enlaces rotos...",
  lintScanningLinksProgress: "Escaneando enlaces rotos: {current}/{total}...",
  lintCheckingDuplicates: "Buscando p\xE1ginas duplicadas...",
  lintCheckingDuplicatesProgress: "Verificando duplicados: lote {current} ...",
  lintFixingPolluted: "Reparando p\xE1gina contaminada {current}/{total}: {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} Reparar p\xE1ginas contaminadas ({count})",
  lintDuplicateCheckFailed: "Fall\xF3 la detecci\xF3n de duplicados \u2014 consulta la consola para m\xE1s detalles",
  lintDuplicateCheckFailedDetail: "Fall\xF3 la comprobaci\xF3n de duplicados en {step}: {error}",
  lintMergeItemFailed: "Fall\xF3 la fusi\xF3n: {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "Alias faltantes: {count} p\xE1gina(s) sin alias",
  lintAliasesSection: "P\xE1ginas sin alias [{count}]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "Completar alias ({count})",
  lintAliasesFilling: "Generando alias {current}/{total}: {page}",
  lintAliasesFilled: "Completado de alias finalizado. Rellenadas {filled}/{total} p\xE1ginas.",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "Etiquetas fuera de vocabulario (detectadas) [{count}]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "Fall\xF3 la generaci\xF3n de alias: {page} \u2014 {error}",
  lintFixItemFailed: "Fall\xF3 la correcci\xF3n: [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "Fall\xF3 el enlace: {page} \u2014 {error}",
  lintRetrying: "Reintentando ({attempt}/{max}) tras error...",
  lintAnalyzingLLM: "La IA est\xE1 analizando el estado de la Wiki...",
  saveToWikiSuccess: "\xA1Conversaci\xF3n guardada en la Wiki!",
  saveSummary: "{entities} entidades, {concepts} conceptos, {pages} p\xE1ginas",
  aliasAdded: "A\xF1adido alias '{alias}' a la p\xE1gina '{page}'",
  // Status Messages
  ingestingSources: "Ingestando fuentes...",
  queryingWiki: "Consultando Wiki...",
  lintingWiki: "Verificando Wiki...",
  savingToWiki: "Guardando conversaci\xF3n en Wiki...",
  clearingHistory: "Limpiando historial de conversaci\xF3n...",
  // Buttons
  ingestButton: "Ingestar",
  queryButton: "Consultar",
  lintButton: "Verificar",
  cancelButton: "Cancelar",
  // Links
  karpathyGistLink: "Gist de Karpathy LLM Wiki",
  obsidianPluginAPI: "API de plugins de Obsidian",
  anthropicSDK: "SDK de Anthropic",
  openaiSDK: "SDK de OpenAI",
  // Other
  availableModelsLoading: "Cargando modelos disponibles...",
  noModelsAvailable: "No hay modelos disponibles",
  // LLM Language Hint
  llmLanguageHint: "Please answer in Spanish.",
  // Schema Configuration
  schemaSection: "Configuraci\xF3n del esquema",
  enableSchemaName: "Activar esquema",
  enableSchemaDesc: "Genera e inyecta schema/config.md en todos los prompts LLM para salida Wiki estructurada",
  viewSchemaButton: "Ver / editar esquema",
  regenerateSchemaButton: "Regenerar esquema predeterminado",
  schemaRegeneratedNotice: "Esquema predeterminado regenerado.",
  schemaRegenerateFailed: "Error al generar el esquema",
  // Estado de inicialización del Wiki
  wikiInitStatusReady: "Wiki inicializado",
  wikiInitStatusNotReady: "Wiki no inicializado \u2014 se crear\xE1 autom\xE1ticamente en la primera ingesti\xF3n",
  // Wiki Output Language
  wikiLanguageName: "Idioma de salida de la Wiki",
  wikiLanguageDesc: "Todas las p\xE1ginas wiki generadas, \xEDndice, registro y respuestas de consulta usan este idioma",
  customWikiLanguageOption: "Personalizado...",
  customWikiLanguageHint: "Introduce un idioma (p. ej. italiano, \xE1rabe). Se pasar\xE1 al LLM como directiva de idioma de salida.",
  customWikiLanguagePlaceholder: "p. ej. italiano",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "Extracci\xF3n",
  extractionGranularityName: "Granularidad de extracci\xF3n",
  extractionGranularityDesc: "Controla extracciones por archivo fuente. Mayor = m\xE1s p\xE1ginas/tokens API.\nFina: an\xE1lisis profundo. Est\xE1ndar: notas diarias. Gruesa: visi\xF3n r\xE1pida. M\xEDnima: procesamiento 100+ archivos. Personalizada: l\xEDmites propios (hasta 300).\nConsejo: Use M\xEDnima/Gruesa para carpetas con muchos archivos para ahorrar tiempo y costo.",
  extractionGranularityFine: "Fina \u2014 an\xE1lisis profundo (\u2264100 elementos)",
  extractionGranularityStandard: "Est\xE1ndar \u2014 notas diarias (\u226450 elementos)",
  extractionGranularityCoarse: "Gruesa \u2014 visi\xF3n r\xE1pida (\u226410 elementos)",
  extractionGranularityMinimal: "M\xEDnima \u2014 procesamiento por lotes 100+ archivos (\u22645 elementos)",
  extractionGranularityCustom: "Personalizado \u2014 Establecer tus propios l\xEDmites (1~500)",
  customEntityLimitName: "L\xEDmite personalizado de entidades",
  customEntityLimitDesc: "N\xFAmero m\xE1ximo de entidades a extraer por archivo fuente (1-500)",
  customConceptLimitName: "L\xEDmite personalizado de conceptos",
  customConceptLimitDesc: "N\xFAmero m\xE1ximo de conceptos a extraer por archivo fuente (1-500)",
  // Issue #85 v2: Vocabulario de etiquetas (entrada por chips, integrado en Configuración Wiki)
  tagVocabularyInlineDesc: "Vocabulario controlado para etiquetas frontmatter de entity y concept. Las etiquetas anidadas de Obsidian con \xAB/\xBB se conservan.",
  tagVocabularyModeName: "Configuraci\xF3n del vocabulario de etiquetas",
  tagVocabularyModeDescDefault: "Por defecto usa etiquetas integradas: {}. Cambie a \xABPersonalizado\xBB para definir su propio vocabulario.",
  tagVocabularyModeDescCustom: "Personalizado: defina abajo sus propias etiquetas de entity y concept con la entrada por chips \u2014 Enter o coma para a\xF1adir, \xD7 para eliminar.",
  tagVocabularyModeDefault: "Por defecto (subtipos integrados)",
  tagVocabularyModeCustom: "Personalizado (definido por el usuario)",
  customEntityTagsName: "Etiquetas de entidad personalizadas",
  customEntityTagsDesc: "Enter o coma para a\xF1adir un chip, \xD7 para eliminar. Las etiquetas anidadas con \xAB/\xBB se conservan.",
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "Etiquetas de concepto personalizadas",
  customConceptTagsDesc: "Enter o coma para a\xF1adir un chip, \xD7 para eliminar. Las etiquetas anidadas con \xAB/\xBB se conservan.",
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "Etiqueta duplicada omitida",
  // Ingestion Acceleration
  accelerationSectionTitle: "Aceleraci\xF3n de ingesti\xF3n",
  pageGenerationConcurrencyName: "Concurrencia LLM",
  pageGenerationConcurrencyDesc: "N\xFAmero de llamadas LLM en paralelo durante ingesti\xF3n y operaciones lint. Valores m\xE1s altos aceleran el procesamiento pero incrementan costes de API y pueden alcanzar l\xEDmites de tasa.",
  concurrencyValueSingular: "Concurrencia actual: {} (serial \u2014 m\xE1s seguro)",
  concurrencyValuePlural: "Concurrencia actual: {} (paralelo)",
  batchDelayName: "Retraso entre lotes (ms)",
  batchDelayDesc: "Retraso entre lotes paralelos para evitar l\xEDmites de tasa de la API (100-2000ms). Actual: {}ms. Aumenta si ves errores 429.",
  // Auto Maintenance
  autoMaintainSection: "Mantenimiento autom\xE1tico",
  autoMaintainBetaBadge: "BETA \u2014 Funci\xF3n experimental. Puede tener problemas. Recomendado solo para usuarios avanzados.",
  autoWatchName: "Monitorizar carpetas",
  autoWatchDesc: "Detecta autom\xE1ticamente archivos .md nuevos o modificados en las carpetas monitorizadas y notifica o ingesta autom\xE1ticamente",
  watchedFoldersName: "Carpetas monitorizadas",
  watchedFoldersDesc: 'Carpetas a monitorizar para contenido nuevo. Pulsa "A\xF1adir carpeta" para seleccionar desde tu vault.',
  addWatchedFolderButton: "A\xF1adir carpeta",
  removeWatchedFolderButton: "Eliminar",
  webClipperPresetName: "Monitorizar recortes (Web Clipper)",
  webClipperPresetDesc: "A\xF1ade la carpeta Clippings/ usada por Obsidian Web Clipper a la lista de monitorizaci\xF3n. Tus recortes web se ingerir\xE1n autom\xE1ticamente en la Wiki.",
  noWatchedFoldersHint: "Ninguna carpeta configurada. A\xF1ade una carpeta o activa el preset Clippings.",
  autoWatchModeName: "Modo de monitorizaci\xF3n",
  autoWatchModeDesc: '"Solo notificar" muestra un aviso. "Ingesti\xF3n autom\xE1tica" procesa sin intervenci\xF3n.',
  watchModeNotify: "Solo notificar",
  watchModeAuto: "Ingesti\xF3n autom\xE1tica",
  autoWatchDebounceName: "Retraso de debounce (segundos)",
  autoWatchDebounceDesc: "Tiempo de espera antes de disparar la ingesti\xF3n tras un cambio en un archivo (1-60 segundos)",
  periodicLintName: "Verificaci\xF3n lint peri\xF3dica",
  periodicLintDesc: "Ejecuta lint LLM de forma programada, solo cuando los archivos fuente han cambiado desde la \xFAltima comprobaci\xF3n",
  periodicLintOff: "Desactivado",
  periodicLintHourly: "Cada hora",
  periodicLintDaily: "Diario",
  periodicLintWeekly: "Semanal",
  autoSmartFixName: "Correcci\xF3n inteligente autom\xE1tica",
  autoSmartFixDesc: "Cuando se ejecute el lint, aplica autom\xE1ticamente todas las correcciones (Smart Fix All) sin mostrar el modal de informe. El resumen de correcciones se sigue mostrando al finalizar.",
  autoSmartFixNotice: "Correcci\xF3n inteligente autom\xE1tica: aplicando todas las correcciones...",
  startupCheckName: "Ejecutar correcciones r\xE1pidas al inicio",
  startupCheckDesc: "Corrige autom\xE1ticamente problemas de formato de bajo nivel (campo sources, wikilinks doblemente anidados) al cargar el plugin. Verifica la estructura de carpetas de la Wiki. Activado por defecto.",
  suggestSchemaCommand: "Sugerir actualizaciones del esquema",
  autoMaintainCostWarning: '\u26A0\uFE0F Aviso de coste: Las funciones de mantenimiento autom\xE1tico consumen tokens de API. "Ingesti\xF3n autom\xE1tica" dispara llamadas LLM en cada cambio de archivo fuente. "Verificaci\xF3n lint peri\xF3dica" ejecuta comprobaciones de salud programadas (solo cuando se detectan cambios). Configura con cuidado para evitar cargos inesperados.',
  // Notices
  startupCheckSummary: "La Wiki tiene {pages} p\xE1ginas ({entities} entidades, {concepts} conceptos, {sources} fuentes)",
  watcherActiveNotice: "Wiki: monitor de archivos activo \u2014 supervisando carpetas monitorizadas",
  watchIngestNotice: 'Wiki: {count} archivo(s) modificado(s) en sources/. Ejecuta "Ingestar fuentes" para procesar.',
  autoIngestRunning: "Ingestando autom\xE1ticamente {count} archivo(s) modificado(s)...",
  autoIngestComplete: "Ingesti\xF3n autom\xE1tica completada: {success} correctas, {fail} fallidas",
  scheduledLintRunning: "Ejecutando verificaci\xF3n lint programada de la Wiki...",
  wikiLintStats: "Lint de Wiki: {pages} p\xE1ginas ({entities} entidades, {concepts} conceptos, {sources} fuentes)",
  wikiHealthStats: "Estado de la Wiki: {pages} p\xE1ginas ({entities} entidades, {concepts} conceptos, {sources} fuentes){indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Correcciones r\xE1pidas Wiki completadas",
  startupCheckStructureLabel: "Estructura Wiki",
  startupCheckStructureOk: "completa",
  startupCheckStructureMissing: "incompleta \u2014 se crear\xE1 autom\xE1ticamente en la primera ingesti\xF3n",
  startupCheckSourcesLabel: "Sources normalizados",
  startupCheckSourcesClean: "ya limpio",
  startupCheckSourcesCleaned: "{files} archivo(s), {entries} entrada(s) limpiada(s)",
  startupCheckIncompleteClean: "p\xE1ginas incompletas: ninguna",
  startupCheckIncompleteArchived: "p\xE1ginas incompletas: {count} archivada(s) (recuperable desde .trash)",
  startupCheckDisableHint: "Para desactivar: Configuraci\xF3n \u2192 Mantenimiento autom\xE1tico \u2192 Ejecutar correcciones r\xE1pidas al inicio",
  lintWikiStart: "Iniciando verificaci\xF3n lint de la Wiki...",
  lintWikiComplete: "Verificaci\xF3n lint de la Wiki completada",
  lintWikiFailed: "Verificaci\xF3n lint de la Wiki fallida",
  analyzingSchema: "Analizando la Wiki y generando sugerencias de esquema...",
  schemaSuggestionGenerated: "Sugerencias de esquema generadas, consulta wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "No se necesitan actualizaciones del esquema.",
  schemaSuggestionFailed: "Fall\xF3 la sugerencia de esquema",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Vista previa de actualizaci\xF3n del esquema",
  schemaDiffSummary: "+{add} / -{del} l\xEDneas",
  schemaDiffRegenerateLabel: "Refinar la sugerencia (opcional):",
  schemaDiffRegenerateBtn: "Regenerar",
  schemaDiffOpenFileBtn: "Abrir archivo",
  schemaDiffApplyBtn: "Aplicar",
  schemaDiffApplied: "Esquema actualizado. Respaldo: {path}",
  schemaDiffEmptyTitle: "LLM no recomienda cambios",
  schemaDiffFailed: "Error al aplicar el esquema: {reason}",
  schemaDiffRestoreHint: 'Copia de seguridad guardada en {path}. Para restaurar, renombra ese archivo a "wiki/schema/config.md" en el explorador de archivos. Se conservan las 3 copias de seguridad m\xE1s recientes.',
  schemaRegenerateNoBody: "Regeneraci\xF3n exitosa pero el LLM no devolvi\xF3 un nuevo esquema.",
  schemaNotFoundNotice: "Archivo de esquema no encontrado. Activa el esquema para crearlo.",
  selectFolderNoMdFiles: "No hay archivos Markdown en la carpeta: {path}",
  batchIngestSkipNotice: "Omitiendo {skipped}/{total} archivos ya ingeridos. Ingestando {new} archivos nuevos...",
  batchIngestAllIngested: "Los {total} archivos de esta carpeta ya han sido ingeridos.",
  batchIngestStarting: 'Ingestando {count} archivo(s) de "{folder}" \u2014 esto puede tardar varios minutos. Aparecer\xE1 un informe al finalizar.',
  batchIngestComplete: "Ingesti\xF3n por lotes completada: {success}/{total} correctas, {fail} fallidas",
  batchIngestFailedFiles: "Archivos fallidos:",
  historyTruncated: "Historial reducido a las \xFAltimas {max} rondas",
  historyCleared: "Historial limpiado",
  // User Feedback Loop
  reviewedPagePreserved: "Conservando contenido revisado por el usuario para: {}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "\xBFGuardar en la Wiki?",
  querySuggestSaveDesc: "Esta conversaci\xF3n contiene conocimiento valioso. \xBFGuardarlo en tu Wiki?",
  querySuggestSaveYes: "Guardar",
  querySuggestSaveNo: "Descartar",
  // Ingestion Report
  ingestReportElapsedTime: "Tiempo transcurrido",
  ingestReportSkippedFiles: "Omitidos (ya ingeridos)",
  ingestReportRejectedFiles: "Omitidos",
  rejectionReasonEmpty: "vac\xEDo",
  rejectionReasonType: "tipo no compatible",
  rejectionReasonDuplicate: "contenido duplicado",
  sourceRejectedEmpty: '\u23ED\uFE0F "{filename}" no tiene contenido para ingerir \u2014 omitido. Las notas vac\xEDas o solo con frontmatter no crean p\xE1ginas wiki.',
  sourceRejectedType: '\u23ED\uFE0F "{filename}" no es un tipo de archivo compatible \u2014 omitido. Solo se pueden ingerir notas de texto (.md, .txt, etc.).',
  sourceRejectedDuplicate: '\u23ED\uFE0F "{filename}" omitido \u2014 su contenido ya est\xE1 en el wiki.',
  ingestRejectedSummary: "{count} archivo(s) omitido(s) (vac\xEDo, duplicado o tipo no compatible).",
  reingestConfirmTitle: "\xBFVolver a ingerir este archivo?",
  reingestConfirmBody: 'El contenido de "{filename}" ya est\xE1 en el wiki. \xBFVolver a ingerirlo de todos modos?',
  reingestConfirmYes: "Volver a ingerir",
  reingestConfirmNo: "Omitir",
  ingestReportFailedGuidance: "Estos elementos no pudieron crearse autom\xE1ticamente. Puedes crear manualmente las p\xE1ginas correspondientes, o reducir la granularidad de extracci\xF3n y volver a ingerir el archivo fuente.",
  ingestReportCollisions: "Colisiones cross-type (fusionadas como alias)",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "Ingestar fuente individual",
  cmdIngestFolder: "Ingestar desde carpeta",
  cmdQueryWiki: "Consultar wiki",
  cmdLintWiki: "Verificar wiki",
  cmdRegenerateIndex: "Regenerar \xEDndice",
  cmdSuggestSchema: "Sugerir actualizaciones del esquema",
  cmdCancelIngestion: "Cancelar ingesta actual",
  cmdIngestActiveFile: "Ingerir archivo actual",
  cmdViewHistory: "Ver historial de ingesta",
  noActiveFile: "No hay ning\xFAn archivo abierto",
  mdOnlyFile: "Solo se pueden ingerir archivos Markdown",
  // Ingestion status bar
  ingestionStatusBar: "Ingiriendo... clic para cancelar",
  lintStatusBar: "Verificando... clic para cancelar",
  ingestStatusAnalyzing: "Ingiriendo\u2026 (clic para cancelar)",
  lintStatusReading: "Verificando\u2026 (clic para cancelar)",
  lintStatusDuplicates: "Verificando\u2026 (clic para cancelar)",
  lintStatusScanningLinks: "Verificando\u2026 (clic para cancelar)",
  lintStatusAnalyzing: "Verificando\u2026 (clic para cancelar)",
  ingestionCancelling: "Cancelando \u2014 se detendr\xE1 tras el lote actual",
  ingestionCancelled: "Ingesta cancelada",
  crossTypeCollisionNotice: "{count} elementos fusionados como alias cross-type (duplicados entidad \u2194 concepto evitados)",
  // Lint Report
  lintReportTitle: "Informe de verificaci\xF3n lint de la Wiki",
  lintReportSummary: "Resumen de estado del wiki: {total} p\xE1ginas en total, {aliasesMissing} p\xE1ginas sin alias, {duplicates} duplicados, {deadLinks} enlaces rotos ({deadLinkFromDup} implican duplicados), {orphans} p\xE1ginas hu\xE9rfanas ({orphanFromDup} son duplicados), {emptyPages} p\xE1ginas vac\xEDas, {ungroundedQuotes} citas sin fuente, {tagViolations} etiquetas fuera de vocabulario. Lint: {elapsedSeconds}s",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "Configuraci\xF3n de par\xE1metros avanzados",
  advancedSettingsModeDesc: "El modo predeterminado usa el comportamiento nativo de tu proveedor \u2014 no se env\xEDa ning\xFAn campo de control de pensamiento, temperatura o penalizaci\xF3n por repetici\xF3n. Esto funciona para la mayor\xEDa de los usuarios. Solo cambia a Personalizado si necesitas anular expl\xEDcitamente los valores predeterminados del proveedor (por ejemplo, forzar un dialecto de control de pensamiento espec\xEDfico, establecer una temperatura distinta a la predeterminada, etc.).",
  advancedSettingsDefault: "Predeterminado (usar comportamiento del proveedor)",
  advancedSettingsCustom: "Personalizado (anular valores predeterminados del proveedor)",
  disableThinkingName: "Deshabilitar pensamiento",
  disableThinkingDesc: "Opcional. Cuando est\xE1 activado, el complemento env\xEDa una directiva de control de pensamiento al proveedor y recorre una cadena de respaldo de dialecto de 3 niveles (anthropic \u2192 openai \u2192 none) si el proveedor la rechaza. Usa esto solo si tu proveedor filtra contenido de razonamiento en la respuesta y necesitas suprimirlo. La mayor\xEDa de los proveedores manejan el razonamiento correctamente por s\xED mismos \u2014 dejarlo desactivado (predeterminado) ofrece mejor calidad.",
  // Issue #137: Notas de compatibilidad (breves; sin lista de proveedores)
  extractionTemperatureName: "Temperatura de extracci\xF3n",
  extractionTemperatureDesc: "Rango 0\u20132. Cuanto m\xE1s bajo, m\xE1s determinista y fiel es la salida del LLM. Cuanto m\xE1s alto, m\xE1s creativo. D\xE9jalo vac\xEDo para usar el valor por defecto del proveedor. Si su proveedor rechaza este valor, el plugin elimina el campo autom\xE1ticamente y muestra un aviso \xFAnico.",
  chatTemperatureName: "Temperatura de consulta",
  chatTemperatureDesc: "Rango 0\u20132. Igual que la temperatura de extracci\xF3n, pero solo afecta las respuestas de chat/consulta. D\xE9jalo vac\xEDo para usar el valor por defecto del proveedor. Si su proveedor rechaza este valor, el plugin elimina el campo autom\xE1ticamente y muestra un aviso \xFAnico.",
  repetitionPenaltyName: "Penalizaci\xF3n por repetici\xF3n",
  repetitionPenaltyDesc: "Rango 0\u20132. Cuanto m\xE1s alto, m\xE1s se reducen los patrones repetitivos. D\xE9jalo vac\xEDo para usar el valor por defecto del proveedor. Nota de compatibilidad: muchos proveedores en la nube no aceptan este campo. El plugin lo elimina autom\xE1ticamente en HTTP 400 y muestra un aviso \xFAnico.",
  temperaturePlaceholder: "vac\xEDo = valor por defecto del proveedor",
  lintDeadLinkSection: "Enlaces rotos (detectados) [{count}]",
  lintEmptyPageSection: "P\xE1ginas vac\xEDas (detectadas) [{count}]",
  lintOrphanSection: "P\xE1ginas hu\xE9rfanas (detectadas) [{count}]",
  lintContradictionSection: "Contradicciones (detectadas)",
  lintDuplicateSection: "P\xE1ginas duplicadas (detectadas)",
  lintNoIssuesFound: "No se detectaron duplicados, enlaces rotos, p\xE1ginas vac\xEDas, p\xE1ginas hu\xE9rfanas ni citas sin fuente.",
  lintQuoteGroundingSection: "Citas sin fuente (detectadas) [{count}]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}: "{quote}"',
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (la p\xE1gina no existe){dupFlag}",
  lintDeadLinkMore: "- ... {count} enlaces rotos m\xE1s",
  lintEmptyPageItem: "- [[{page}]] \u2014 menos de 50 caracteres de contenido sustantivo",
  lintOrphanItem: "- [[{page}]] \u2014 ninguna otra p\xE1gina Wiki enlaza aqu\xED{dupFlag}",
  lintPollutedSection: "P\xE1ginas contaminadas (detectadas) [{count}]",
  lintPollutedItem: '- [[{page}]] \u2192 deber\xEDa ser "{clean}"',
  lintSourcesNormalizedSection: "Sources normalizadas (auto-corregidas) [{files} archivos / {entries} entradas]",
  lintSourcesNormalizedItem: "Limpiadas {entries} entradas de sources contaminadas en {files} archivo(s) (rutas externas, extensiones .md, pipes de alias eliminados y deduplicados).",
  lintDuplicateItem: "- [[{target}]] y [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F implica p\xE1gina duplicada)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F p\xE1gina duplicada)",
  lintContradictionOpen: "Contradicciones abiertas: {count}",
  lintContradictionAutoFixed: "({count} autocorregidas en esta ejecuci\xF3n)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "Detectada",
  lintContradictionStatusPendingFix: "Pendiente de correcci\xF3n",
  lintLLMAnalysisHeading: "## An\xE1lisis LLM",
  // Lint Analysis Prompt
  lintAnalysisPrompt: 'Eres un asistente de mantenimiento de Wiki. Comprueba el estado de la Wiki bas\xE1ndote en la siguiente informaci\xF3n.\n\n\xCDndice Wiki:\n{index}\n\nMuestra de contenido de p\xE1ginas Wiki ({total} p\xE1ginas en total, mostrando {sample} p\xE1ginas):\n{contentSample}\n\nResultados de la comprobaci\xF3n program\xE1tica (ya verificados, no repetir):\n{progReport}\n\nComprueba los siguientes aspectos (omite enlaces rotos/p\xE1ginas vac\xEDas/hu\xE9rfanas ya detectados por las comprobaciones program\xE1ticas):\n1. **Contradicciones** \u2014 si diferentes p\xE1ginas se contradicen entre s\xED sobre los mismos hechos\n2. **Obsolescencia** \u2014 si alguna afirmaci\xF3n est\xE1 claramente desactualizada\n3. **Faltantes** \u2014 qu\xE9 conceptos importantes carecen de p\xE1ginas independientes\n4. **Estructura** \u2014 si la estructura de las p\xE1ginas es razonable y si los enlaces cruzados son adecuados\n\nFormato de salida: usa Markdown, comenzando con "## An\xE1lisis LLM". Cada hallazgo en una l\xEDnea "- [problema espec\xEDfico]". Si no hay problemas, escribe "No se encontraron problemas evidentes."',
  // Lint Fix Progress
  lintFixProgress: "Corrigiendo {current}/{total}: [[{target}]]",
  lintFixDeadComplete: "Correcci\xF3n de enlaces rotos completada. Corregidos {fixed}/{total} elementos.",
  lintFillProgress: "Expandiendo {current}/{total}: {page}",
  lintFillComplete: "Expansi\xF3n de p\xE1ginas completada. Rellenadas {filled}/{total} p\xE1ginas.",
  lintDeleteCompleted: "{count} stubs vac\xEDos eliminados",
  lintDeleteFailed: "Error al eliminar {failed}/{total} stubs vac\xEDos (ver consola)",
  lintFillFailed: "Fall\xF3 la expansi\xF3n: {page} \u2014 {error}",
  lintLinkProgress: "Enlazando {current}/{total}: {page}",
  lintLinkComplete: "Enlace de hu\xE9rfanas completado. Enlazadas {linked} p\xE1ginas.",
  lintFixNoAction: "No se tom\xF3 ninguna acci\xF3n (sin cliente)",
  lintFixIndexUpdated: "\xCDndice y registro de la Wiki actualizados.",
  lintPollutedFixed: "P\xE1ginas contaminadas corregidas: {fixed}/{total}. \xCDndice regenerado.",
  regenerateIndexCompleted: "\xCDndice regenerado",
  operationFailed: "Error: ",
  lintFixAllComplete: "Todas las correcciones completadas. Consulta el registro para m\xE1s detalles.",
  // Lint Report Modal
  lintModalActionsTitle: "Sugerencias de correcci\xF3n (requiere tokens LLM):",
  lintLogReference: "Informe completo guardado en log.md",
  lintModalFixDeadLinks: "Corregir enlaces rotos ({count})",
  lintModalExpandEmpty: "Expandir p\xE1ginas vac\xEDas ({count})",
  lintModalDeleteEmpty: "Eliminar stubs vac\xEDos ({count})",
  lintModalLinkOrphans: "Enlazar p\xE1ginas hu\xE9rfanas ({count})",
  lintModalAnalyzeSchema: "Analizar esquema",
  lintModalMergeDuplicates: "Fusionar duplicados ({count})",
  lintModalFixAll: "Correcci\xF3n inteligente global ({count} problemas)",
  lintMergeProgress: "Fusionando {current}/{total}: {source} \u2192 {target}",
  lintMergeComplete: "Fusi\xF3n de duplicados completada. Fusionados {merged}/{total} pares.",
  // Ingest Report Modal
  ingestReportTitle: "Informe de ingesti\xF3n",
  ingestReportSourceFile: "Archivo fuente",
  ingestReportCreated: "Creadas",
  ingestReportUpdated: "Actualizadas",
  ingestReportContradictions: "Contradicciones encontradas",
  ingestReportFailedTitle: "Ingesti\xF3n fallida",
  ingestReportErrorDetail: "Detalle del error",
  ingestReportClose: "Cerrar",
  ingestReportCreatedPages: "P\xE1ginas creadas: {count}",
  ingestReportUpdatedPages: "P\xE1ginas actualizadas: {count}",
  ingestReportEntitiesCount: "{count} entidades",
  ingestReportConceptsCount: "{count} conceptos",
  ingestReportContradictionsFound: "Contradicciones encontradas: {count}",
  ingestReportEntityType: "Entidad",
  ingestReportConceptType: "Concepto",
  timeMinutes: "min",
  timeSeconds: "seg",
  // Advertencias de límite de tasa
  rateLimitDetected: "\u26A0\uFE0F L\xEDmite de tasa detectado: {count} p\xE1gina(s) fallaron con errores 429. Sugerencias: (1) Reducir concurrencia a {suggestedConcurrency} o 1 (serial), (2) Aumentar retraso entre lotes a {suggestedDelay}ms, (3) Cambiar a un proveedor con l\xEDmites de tasa m\xE1s altos.",
  rateLimitDetectedShort: "\u26A0\uFE0F L\xEDmite de tasa alcanzado \u2014 considere reducir la concurrencia o aumentar el retraso entre lotes en Configuraci\xF3n \u2192 Aceleraci\xF3n de ingesti\xF3n.",
  longSourceNotice: '\u{1F4C4} "{filename}" tiene {lines} l\xEDneas ({size}). Los textos largos requieren extracci\xF3n iterativa por lotes \u2014 el LLM lee el documento completo en m\xFAltiples pasadas. Esto puede tomar varios minutos. Por favor, ten paciencia.',
  longSourceNoticeShort: "\u{1F4C4} Archivo grande detectado ({lines} l\xEDneas). La ingesti\xF3n puede tardar un poco.",
  // Panel de Historial de Ingesta (#122) — textos UI
  historyButton: "Historial de operaciones",
  historyButtonDesc: "Ver ingestas recientes, informes de Lint y otros cambios de tu Wiki",
  historyButtonOpen: "Ver historial",
  historyModalTitle: "Historial de operaciones",
  historyModalSubtitle: "Cambios recientes de tu LLM-Wiki",
  historyEmpty: "A\xFAn no hay operaciones registradas. Ingesta una nota o ejecuta Lint para completar esta lista.",
  historyReadError: "No se pudo leer el registro de operaciones: {error}",
  historyEntryKindIngest: "Ingesta",
  historyEntryKindMaintenance: "Mantenimiento",
  historyEntryKindFix: "Correcci\xF3n",
  historyEntryKindOther: "Otro",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "Origen: {source}",
  historyEntrySourceUnknown: "Ingesta sin t\xEDtulo",
  historyEntryCreatedLabel: "Creadas",
  historyEntryUpdatedLabel: "Actualizadas",
  historyEntryCreatedCount: "{count} creadas",
  historyEntryUpdatedCount: "{count} actualizadas",
  historyEntryNoChanges: "Sin cambios en p\xE1ginas",
  historyEntryContradictions: "\u26A0\uFE0F {count} contradicci\xF3n(es)",
  historyEntrySectionCreated: "P\xE1ginas creadas",
  historyEntrySectionUpdated: "P\xE1ginas actualizadas",
  historyEntrySectionContradictions: "Contradicciones",
  historyEntrySectionDetails: "Detalles",
  historyEntryOpenPage: "Abrir",
  historyEntryDetailsNoContradictions: "No se encontraron contradicciones",
  historySearchPlaceholder: "\u{1F50D} Buscar por t\xEDtulo de origen o ruta de p\xE1gina\u2026",
  historyFilterAll: "Todas",
  historyFilterIngest: "Solo ingesta",
  historyFilterMaintenance: "Solo mantenimiento",
  historyFilterFix: "Solo correcciones",
  historyFilterContradictions: "\u26A0\uFE0F Con contradicciones",
  historyRefreshButton: "\u27F3 Actualizar",
  historyExpandDay: "Clic para expandir",
  historyCollapseDay: "Clic para contraer",
  historyShowMore: "Mostrar {count} entradas anteriores",
  historyNoMatch: "Ninguna operaci\xF3n coincide con tu b\xFAsqueda/filtro.",
  historyCloseButton: "Cerrar",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "Hallazgos del informe",
  historyKpiPages: "P\xE1ginas",
  historyKpiDeadLinks: "Enlaces rotos",
  historyKpiOrphans: "Hu\xE9rfanas",
  historyKpiEmpty: "Vac\xEDas",
  historyKpiDuplicates: "Duplicados",
  historyKpiTagViolations: "Etiquetas",
  historyKpiUnsourced: "Sin fuente",
  historyKpiDuration: "Duraci\xF3n Lint",
  historyKpiDurationSec: "{seconds}s",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "Enlaces rotos ({count})",
  historySectionTagViolations: "Etiquetas ({count})",
  historySectionOrphans: "P\xE1ginas hu\xE9rfanas ({count})",
  historySectionEmptyPages: "P\xE1ginas vac\xEDas ({count})",
  historySectionLlmAnalysis: "An\xE1lisis LLM ({count})",
  historyDeadLinkSource: "Origen",
  historyDeadLinkTarget: "Falta",
  historyOpenInLog: "Abrir en log.md",
  historyShowMoreItems: "Mostrar {count} m\xE1s",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 igual",
  historyChipContradiction: "Contradicci\xF3n",
  historyChipOutdated: "Obsoleto",
  historyChipMissing: "Falta",
  historyChipStructure: "Estructura",
  historySeverityHigh: "Alta",
  historySeverityMedium: "Media",
  historySeverityLow: "Baja",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Tu Wiki tiene {dead} enlace(s) roto(s), {orphans} p\xE1gina(s) hu\xE9rfana(s) y {tags} problema(s) de etiqueta. \xDAltima Lint: {duration}.",
  historyGlobalInsightClean: "Tu Wiki est\xE1 limpio \u2014 sin enlaces rotos, p\xE1ginas hu\xE9rfanas ni problemas de etiqueta.",
  historyGlobalInsightNoData: "Sin informes de mantenimiento a\xFAn \u2014 ejecuta Lint.",
  historyModalHeaderTitle: "Historial de operaciones",
  historyModalSubtitleWithCount: "Cambios recientes de tu LLM-Wiki \xB7 {count} entradas",
  historyTimeRangeAll: "Todo el tiempo",
  historyTimeRange1d: "\xDAltimo d\xEDa",
  historyTimeRange3d: "\xDAltimos 3 d\xEDas",
  historyTimeRange1w: "\xDAltima semana",
  historyTimeRange1m: "\xDAltimo mes",
  historyIngestTotal: "P\xE1ginas totales",
  historyIngestByType: "Por tipo",
  historyIngestSource: "Archivo de origen",
  historyIngestNoTimestamp: "Sin marca de tiempo",
  historyIngestFirstTime: "Primera ingesta",
  historyIngestLatestTime: "\xDAltima ingesta",
  historyTimeRangeCustom: "Rango personalizado",
  historyCustomRangeFrom: "Desde",
  historyCustomRangeTo: "Hasta",
  historyCustomRangeApply: "Aplicar",
  historyCustomRangeClear: "Limpiar"
};

// src/texts/pt.ts
var PT_TEXTS = {
  // Plugin Info
  pluginTitle: "Configura\xE7\xF5es do Karpathy LLM Wiki",
  pluginIntro: "Este plug-in implementa o conceito de {{link}} para o Obsidian. Ele l\xEA suas notas, usa IA para extrair entidades, conceitos e rela\xE7\xF5es, e constr\xF3i uma Wiki estruturada e interligada no seu vault.",
  karpathyLinkText: "LLM Wiki de Andrej Karpathy",
  // Features Section
  featuresTitle: "Como funciona",
  workflow1Title: "1. Ingerir",
  workflow1Desc: "Selecione uma nota \u2014 a IA extrai entidades, conceitos e rela\xE7\xF5es para p\xE1ginas da Wiki.",
  workflow2Title: "2. Consultar",
  workflow2Desc: "Converse com sua Wiki \u2014 as respostas s\xE3o baseadas no seu conhecimento ingerido.",
  workflow3Title: "3. Manter",
  workflow3Desc: "\xCDndice gerado automaticamente, links cruzados e verifica\xE7\xF5es de qualidade mant\xEAm a Wiki saud\xE1vel.",
  // Language Switcher
  languageTitle: "Idioma da interface",
  languageDesc: "Selecione seu idioma preferido para o painel de configura\xE7\xF5es. Reinicie o plugin para aplicar as altera\xE7\xF5es de idioma \xE0 paleta de comandos.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Status
  statusTitle: "Status do LLM Client",
  llmWikiStatusSection: "Status do LLM-Wiki",
  statusInitialized: "Inicializado",
  statusReady: "LLM pronto",
  statusNotReady: "LLM n\xE3o configurado \u2014 complete a configura\xE7\xE3o acima e passe no teste de conex\xE3o",
  statusNotInitialized: "N\xE3o inicializado",
  currentProvider: "Provedor atual",
  // Provider Configuration
  providerSection: "Configura\xE7\xE3o LLM",
  providerName: "Provedor LLM",
  providerDesc: "Selecione um provedor predefinido ou servi\xE7o personalizado compat\xEDvel com OpenAI. Para Coding Plan ou bundles similares: escolha Custom OpenAI/Anthropic, insira manualmente o Base URL e API Key do provedor",
  // API Key
  apiKeyName: "API Key",
  apiKeyDesc: "Insira sua API Key do provedor",
  apiKeyPlaceholder: "Insira a API Key",
  // Base URL
  baseUrlName: "URL base da API",
  baseUrlDescCustom: "Obrigat\xF3rio: endpoint personalizado compat\xEDvel com OpenAI",
  baseUrlDescOverride: "Opcional: substituir URL base predefinida",
  // Ollama Hint
  ollamaHint: "O Ollama roda localmente, n\xE3o requer API Key",
  lmstudioHint: "O LM Studio roda localmente, API Key \xE9 opcional",
  maxTokensPerCallName: "Janela de contexto",
  maxTokensPerCallDesc: "Limitar tokens \xE0 janela de contexto do modelo. 0 = sem limite (cloud).",
  slugCaseName: "Capitaliza\xE7\xE3o de nomes de arquivo",
  slugCaseDesc: "Controla se os nomes de arquivo Wiki gerados s\xE3o convertidos para min\xFAsculas. Recomenda-se 'Preservar' para idiomas onde mai\xFAsculas s\xE3o significativas (ex. substantivos alem\xE3es).",
  slugCaseLower: "Min\xFAsculas (padr\xE3o)",
  slugCasePreserve: "Preservar capitaliza\xE7\xE3o",
  // Model Selection
  modelSection: "Sele\xE7\xE3o de modelo",
  fetchModelsName: "Buscar modelos dispon\xEDveis",
  fetchModelsDesc: "Obter a lista mais recente de modelos da API do provedor",
  fetchModelsButton: "Buscar modelos",
  fetchingModels: "Buscando...",
  fetchSuccess: "Sucesso! {} modelos dispon\xEDveis",
  fetchFailed: "Falha ou lista vazia. Insira o nome do modelo manualmente",
  fetchNotSupported: "O provedor n\xE3o suporta consulta \xE0 lista de modelos",
  fetchErrorAuth: "Falha de autentica\xE7\xE3o (HTTP 401/403). Verifique a chave API, ou insira um ID de modelo e clique em Testar conex\xE3o.",
  fetchErrorEndpoint: "Endpoint n\xE3o encontrado (HTTP 404). Verifique a URL do endpoint, ou insira um ID de modelo e clique em Testar conex\xE3o.",
  fetchErrorServer: "Erro do servidor do provedor (HTTP 5xx). Tente novamente mais tarde, ou insira um ID de modelo e clique em Testar conex\xE3o.",
  fetchErrorEmpty: "Este endpoint n\xE3o exp\xF5e lista de modelos. Insira um ID de modelo e clique em Testar conex\xE3o.",
  fetchErrorNetwork: "Falha na requisi\xE7\xE3o de rede. Verifique sua conex\xE3o com a internet, URL do endpoint ou configura\xE7\xF5es de proxy. Voc\xEA tamb\xE9m pode inserir um ID de modelo e clicar em Testar conex\xE3o.",
  selectModelName: "Selecionar modelo",
  selectModelDesc: "Escolha entre {} modelos dispon\xEDveis",
  customInputOption: "Entrada personalizada...",
  customInputHint: 'Para usar outros modelos, selecione "Entrada personalizada..."',
  modelName: "Nome do modelo",
  modelDescCustom: "Usando modelo personalizado (clique no bot\xE3o acima para buscar a lista novamente)",
  modelDescFetchFailed: "Falha ao buscar lista de modelos. Verifique a chave API e URL do endpoint, ou insira um ID de modelo e clique em Testar conex\xE3o.",
  modelInputPlaceholder: "Insira o ID do modelo e teste a conex\xE3o",
  switchToDropdown: "Alternar para sele\xE7\xE3o por lista",
  useDropdownButton: "Usar lista suspensa",
  // Test & Save
  testConnectionName: "Testar conex\xE3o",
  testConnectionDesc: "Validar se a configura\xE7\xE3o consegue chamar a API LLM com sucesso",
  testButton: "Testar conex\xE3o",
  testing: "Testando...",
  saveSettingsName: "Salvar configura\xE7\xF5es",
  saveSettingsDesc: "Salvar a configura\xE7\xE3o atual",
  saveButton: "Salvar configura\xE7\xF5es",
  testConnectionSuccessful: "Conex\xE3o bem-sucedida",
  testConnectionFailed: "Conex\xE3o falhou",
  testConnectionProvider: "Provedor: ",
  errorUnknown: "Erro desconhecido",
  savedNotice: "Configura\xE7\xF5es salvas!",
  // Issue #137: Avisos de fallback de LLM
  fallbackThinkingDialect: 'Controle de pensamento: alternado para o dialeto "{dialect}" (este provedor usa um formato diferente). A sa\xEDda permanece inalterada.',
  fallbackThinkingNone: "Controle de pensamento totalmente desativado para este provedor. Conte\xFAdo de racioc\xEDnio ainda pode aparecer; se isso acontecer, tente outro modelo.",
  fallbackParamStripped: 'O par\xE2metro "{field}" n\xE3o \xE9 compat\xEDvel com este provedor. Removido da requisi\xE7\xE3o; o comportamento pode diferir da configura\xE7\xE3o.',
  // Wiki Folder
  wikiSection: "Configura\xE7\xE3o do Wiki",
  wikiFolderName: "Pasta Wiki",
  wikiFolderDesc: "Local para as p\xE1ginas geradas da Wiki",
  wikiFolderPlaceholder: "wiki",
  // Errors
  errorNoApiKey: "Configure a API Key primeiro",
  llmNotReady: "LLM n\xE3o est\xE1 configurado. V\xE1 para Configura\xE7\xF5es \u2192 Karpathy LLM Wiki para configurar seu provedor e passar no teste de conex\xE3o.",
  errorFetchFailed: "Falha: {}",
  // Query Settings
  querySectionTitle: "Configura\xE7\xE3o de consulta Wiki",
  maxConversationHistoryName: "Hist\xF3rico m\xE1ximo de conversa",
  maxConversationHistoryDesc: "Limitar mensagens da conversa para evitar excesso de tokens",
  maxConversationHistoryHint: "Recomendado: n\xE3o ultrapassar 50 rodadas",
  numberRangeValidation: "Insira um n\xFAmero entre 1 e 50",
  numberRangeClamped: "Valor fora do intervalo (1-500), automaticamente definido como {}",
  // Query Modal UI
  queryModalTitle: "Consultar Wiki \u2014 Consulta conversacional",
  queryModalPlaceholder: "Insira sua pergunta...",
  queryModalSendButton: "Enviar",
  queryModalStopButton: "Parar",
  queryModalSaveButton: "Salvar na Wiki",
  queryModalClearButton: "Limpar hist\xF3rico",
  queryModalHistoryCount: "Hist\xF3rico de conversa: {}/{} rodadas",
  queryModalStreaming: "Transmiss\xE3o em andamento...",
  queryModalFallbackStreaming: "Transmiss\xE3o n\xE3o suportada, alternando para modo n\xE3o transmissivo. Aguarde...",
  queryPhaseSearching: "Analisando \xEDndice da Wiki, selecionando p\xE1ginas relevantes...",
  queryPhaseFoundPages: "{count} p\xE1gina(s) encontrada(s): {pages}",
  queryPhaseLoadingPages: "Carregando conte\xFAdo das p\xE1ginas...",
  queryPhaseContextReady: "Contexto pronto. Gerando resposta...",
  queryPhaseGenerating: "Gerando... ({time}s decorrido)",
  queryPhaseNonStreaming: "Modo n\xE3o transmissivo, gerando resposta... ({time}s decorrido)",
  queryModalErrorPrefix: "Erro: ",
  queryModalHint: 'Consultas baseadas no conte\xFAdo da Wiki. Clique em "Salvar na Wiki" para extrair conversas valiosas como p\xE1ginas.',
  // v1.20.0: Resumo do bloco de raciocínio colapsável do Query Wiki
  queryThinkingSummary: "Processo de racioc\xEDnio",
  queryThinkingSteps: "passos",
  // Error Messages
  errorLLMClientNotInit: "LLM Client n\xE3o inicializado. Salve as configura\xE7\xF5es.",
  errorIngestFailed: "Falha na ingest\xE3o: ",
  errorQueryFailed: "Falha na consulta: ",
  fileWriteFailed: "N\xE3o foi poss\xEDvel criar ou atualizar o ficheiro: {path}",
  // Success Messages
  ingestSuccess: "Ingest\xE3o bem-sucedida: {} p\xE1ginas criadas, {} p\xE1ginas atualizadas",
  querySuccess: "Consulta conclu\xEDda",
  lintSuccess: "Verifica\xE7\xE3o da Wiki conclu\xEDda",
  lintReadingPages: "Lendo {count} p\xE1ginas da Wiki...",
  lintReadingPagesProgress: "Lendo p\xE1ginas da Wiki: {current}/{total}...",
  lintScanningLinks: "Verificando links quebrados...",
  lintScanningLinksProgress: "Verificando links quebrados: {current}/{total}...",
  lintCheckingDuplicates: "Verificando p\xE1ginas duplicadas...",
  lintCheckingDuplicatesProgress: "Verificando duplicados: lote {current} ...",
  lintFixingPolluted: "Reparando p\xE1gina contaminada {current}/{total}: {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} Reparar p\xE1ginas contaminadas ({count})",
  lintDuplicateCheckFailed: "Detec\xE7\xE3o de duplicatas falhou \u2014 consulte o console para detalhes",
  lintDuplicateCheckFailedDetail: "Verifica\xE7\xE3o de duplicatas falhou em {step}: {error}",
  lintMergeItemFailed: "Falha na mesclagem: {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "Aliases ausentes: {count} p\xE1gina(s) sem aliases",
  lintAliasesSection: "P\xE1ginas sem aliases [{count}]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "Completar aliases ({count})",
  lintAliasesFilling: "Gerando aliases {current}/{total}: {page}",
  lintAliasesFilled: "Completar aliases conclu\xEDdo. Preenchidas {filled}/{total} p\xE1ginas.",
  lintTagViolationFiring: "Retagging {current}/{total}: {path}",
  lintTagViolationFailed: "Retag failed for {path}: {error}",
  lintTagViolationFixed: "Retag complete. Fixed {fixed}/{total} page(s).",
  lintTagViolationFixedNone: "Retag complete. No pages needed fixing (LLM kept current tags).",
  lintTagViolationSection: "Tags fora do vocabul\xE1rio (detectadas) [{count}]",
  lintTagViolationItem: "- [[{path}]] \u2014 invalid: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Retag {count} page(s) with LLM",
  lintAliasesFillFailed: "Falha ao gerar aliases: {page} \u2014 {error}",
  lintFixItemFailed: "Falha na corre\xE7\xE3o: [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "Falha ao linkar: {page} \u2014 {error}",
  lintRetrying: "Tentando novamente ({attempt}/{max}) ap\xF3s erro...",
  lintAnalyzingLLM: "LLM analisando sa\xFAde da Wiki...",
  saveToWikiSuccess: "Conversa salva na Wiki!",
  saveSummary: "{entities} entidades, {concepts} conceitos, {pages} p\xE1ginas",
  aliasAdded: "Alias '{alias}' adicionado \xE0 p\xE1gina '{page}'",
  // Status Messages
  ingestingSources: "Ingerindo fontes...",
  queryingWiki: "Consultando Wiki...",
  lintingWiki: "Verificando Wiki...",
  savingToWiki: "Salvando conversa na Wiki...",
  clearingHistory: "Limpando hist\xF3rico de conversa...",
  // Buttons
  ingestButton: "Ingerir",
  queryButton: "Consultar",
  lintButton: "Verificar",
  cancelButton: "Cancelar",
  // Links
  karpathyGistLink: "Gist do LLM Wiki do Karpathy",
  obsidianPluginAPI: "API de plug-ins do Obsidian",
  anthropicSDK: "SDK da Anthropic",
  openaiSDK: "SDK da OpenAI",
  // Other
  availableModelsLoading: "Carregando modelos dispon\xEDveis...",
  noModelsAvailable: "Nenhum modelo dispon\xEDvel",
  // LLM Language Hint
  llmLanguageHint: "Please answer in Portuguese.",
  // Schema Configuration
  schemaSection: "Configura\xE7\xE3o do Schema",
  enableSchemaName: "Ativar Schema",
  enableSchemaDesc: "Gerar e injetar schema/config.md em todos os prompts LLM para sa\xEDda estruturada da Wiki",
  viewSchemaButton: "Ver / editar Schema",
  regenerateSchemaButton: "Regenerar Schema padr\xE3o",
  schemaRegeneratedNotice: "Schema padr\xE3o regenerado.",
  schemaRegenerateFailed: "Falha na gera\xE7\xE3o do Schema",
  // Status de inicialização do Wiki
  wikiInitStatusReady: "Wiki inicializado",
  wikiInitStatusNotReady: "Wiki n\xE3o inicializado \u2014 ser\xE1 criado automaticamente na primeira ingest\xE3o",
  // Wiki Output Language
  wikiLanguageName: "Idioma de sa\xEDda da Wiki",
  wikiLanguageDesc: "Todas as p\xE1ginas da Wiki, \xEDndice, log e respostas de consulta gerados usam este idioma",
  customWikiLanguageOption: "Personalizado...",
  customWikiLanguageHint: "Insira um nome de idioma (ex.: Italiano, \xC1rabe). Ele ser\xE1 passado ao LLM como diretiva de idioma de sa\xEDda.",
  customWikiLanguagePlaceholder: "Ex.: Italiano",
  // Index & Log Labels (per language)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Extraction Settings
  extractionSectionTitle: "Extra\xE7\xE3o",
  extractionGranularityName: "Granularidade da extra\xE7\xE3o",
  extractionGranularityDesc: "Controla extra\xE7\xF5es por arquivo fonte. Maior = mais p\xE1ginas/tokens API.\nFina: an\xE1lise profunda. Padr\xE3o: notas di\xE1rias. Grosseira: vis\xE3o r\xE1pida. M\xEDnima: processamento 100+ arquivos. Personalizada: limites pr\xF3prios (at\xE9 500).\nDica: Use M\xEDnima/Grosseira para pastas com muitos arquivos para economizar tempo e custo.",
  extractionGranularityFine: "Fina \u2014 an\xE1lise profunda (\u2264100 itens)",
  extractionGranularityStandard: "Padr\xE3o \u2014 notas di\xE1rias (\u226450 itens)",
  extractionGranularityCoarse: "Grosseira \u2014 vis\xE3o r\xE1pida (\u226410 itens)",
  extractionGranularityMinimal: "M\xEDnima \u2014 processamento em lote 100+ arquivos (\u22645 itens)",
  extractionGranularityCustom: "Personalizada \u2014 Definir seus pr\xF3prios limites (1~500)",
  customEntityLimitName: "Limite personalizado de entidades",
  customEntityLimitDesc: "N\xFAmero m\xE1ximo de entidades a extrair por arquivo fonte (1-500)",
  customConceptLimitName: "Limite personalizado de conceitos",
  customConceptLimitDesc: "N\xFAmero m\xE1ximo de conceitos a extrair por arquivo fonte (1-500)",
  // Issue #85 v2: Vocabulário de tags (entrada por chips, integrado em Configuração Wiki)
  tagVocabularyInlineDesc: 'Vocabul\xE1rio controlado para tags frontmatter de entity e concept. Tags aninhadas do Obsidian com "/" s\xE3o preservadas.',
  tagVocabularyModeName: "Configura\xE7\xE3o do vocabul\xE1rio de tags",
  tagVocabularyModeDescDefault: 'Padr\xE3o usa tags embutidas: {}. Mude para "Personalizado" para definir seu pr\xF3prio vocabul\xE1rio.',
  tagVocabularyModeDescCustom: "Personalizado: defina abaixo suas pr\xF3prias tags de entity e concept pela entrada por chips \u2014 Enter ou v\xEDrgula para adicionar, \xD7 para remover.",
  tagVocabularyModeDefault: "Padr\xE3o (subtipos embutidos)",
  tagVocabularyModeCustom: "Personalizado (definido pelo usu\xE1rio)",
  customEntityTagsName: "Tags de entidade personalizadas",
  customEntityTagsDesc: 'Enter ou v\xEDrgula para adicionar um chip, \xD7 para remover. Tags aninhadas com "/" s\xE3o preservadas.',
  customEntityTagsPlaceholder: "person, organization, project, place",
  customConceptTagsName: "Tags de conceito personalizadas",
  customConceptTagsDesc: 'Enter ou v\xEDrgula para adicionar um chip, \xD7 para remover. Tags aninhadas com "/" s\xE3o preservadas.',
  customConceptTagsPlaceholder: "theory, method, field, phenomenon, term",
  chipDuplicateHint: "Tag duplicada ignorada",
  // Ingestion Acceleration
  accelerationSectionTitle: "Acelera\xE7\xE3o da ingest\xE3o",
  pageGenerationConcurrencyName: "Paralelismo LLM",
  pageGenerationConcurrencyDesc: "N\xFAmero de chamadas LLM em paralelo durante ingest\xE3o e opera\xE7\xF5es de lint. Valores mais altos aceleram o processamento, mas aumentam custos de API e podem acionar limites de taxa.",
  concurrencyValueSingular: "Concorr\xEAncia atual: {} (serial \u2014 mais seguro)",
  concurrencyValuePlural: "Concorr\xEAncia atual: {} (paralelo)",
  batchDelayName: "Atraso entre lotes (ms)",
  batchDelayDesc: "Atraso entre lotes paralelos para evitar limita\xE7\xE3o de taxa da API (100-2000 ms). Atual: {}ms. Aumente se observar erros 429.",
  // Auto Maintenance
  autoMaintainSection: "Manuten\xE7\xE3o autom\xE1tica",
  autoMaintainBetaBadge: "BETA \u2014 Recurso experimental. Pode apresentar problemas. Recomendado apenas para usu\xE1rios avan\xE7ados.",
  autoWatchName: "Pastas monitoradas",
  autoWatchDesc: "Detectar automaticamente arquivos .md novos ou alterados nas pastas monitoradas e notificar ou ingerir automaticamente",
  watchedFoldersName: "Pastas monitoradas",
  watchedFoldersDesc: 'Pastas a monitorar para novo conte\xFAdo. Clique em "Adicionar pasta" para selecionar no seu vault.',
  addWatchedFolderButton: "Adicionar pasta",
  removeWatchedFolderButton: "Remover",
  webClipperPresetName: "Monitorar Clippings (Web Clipper)",
  webClipperPresetDesc: "Adicionar a pasta Clippings/ usada pelo Obsidian Web Clipper \xE0 lista de monitoramento. Seus recortes web ser\xE3o ingeridos automaticamente na Wiki.",
  noWatchedFoldersHint: "Nenhuma pasta configurada. Adicione uma pasta ou ative o preset Clippings.",
  autoWatchModeName: "Modo de monitoramento",
  autoWatchModeDesc: '"Somente notificar" exibe um aviso. "Ingerir automaticamente" processa silenciosamente.',
  watchModeNotify: "Somente notificar",
  watchModeAuto: "Ingerir automaticamente",
  autoWatchDebounceName: "Atraso de debounce (segundos)",
  autoWatchDebounceDesc: "Tempo de espera antes de acionar a ingest\xE3o ap\xF3s altera\xE7\xE3o de arquivo (1-60 segundos)",
  periodicLintName: "Verifica\xE7\xE3o peri\xF3dica",
  periodicLintDesc: "Executar verifica\xE7\xE3o LLM agendada, somente quando arquivos fonte foram alterados desde a \xFAltima verifica\xE7\xE3o",
  periodicLintOff: "Desativado",
  periodicLintHourly: "A cada hora",
  periodicLintDaily: "Diariamente",
  periodicLintWeekly: "Semanalmente",
  autoSmartFixName: "Corre\xE7\xE3o inteligente autom\xE1tica",
  autoSmartFixDesc: "Quando o lint for executado, aplica automaticamente todas as corre\xE7\xF5es (Smart Fix All) sem exibir o modal de relat\xF3rio. O resumo das corre\xE7\xF5es ainda \xE9 exibido ao concluir.",
  autoSmartFixNotice: "Corre\xE7\xE3o inteligente autom\xE1tica: aplicando todas as corre\xE7\xF5es...",
  startupCheckName: "Executar corre\xE7\xF5es r\xE1pidas na inicializa\xE7\xE3o",
  startupCheckDesc: "Corrige automaticamente problemas de formato de baixo n\xEDvel (campo sources, wikilinks duplamente aninhados) ao carregar o plug-in. Verifica a estrutura de pastas do Wiki. Ativado por padr\xE3o.",
  suggestSchemaCommand: "Sugerir atualiza\xE7\xF5es de Schema",
  autoMaintainCostWarning: '\u26A0\uFE0F Aviso de custo: Recursos de manuten\xE7\xE3o autom\xE1tica consomem tokens de API. "Ingerir automaticamente" aciona chamadas LLM em cada altera\xE7\xE3o de arquivo fonte. "Verifica\xE7\xE3o peri\xF3dica" executa verifica\xE7\xF5es de sa\xFAde LLM agendadas (somente quando altera\xE7\xF5es s\xE3o detectadas). Configure com cuidado para evitar cobran\xE7as inesperadas.',
  // Notices
  startupCheckSummary: "A Wiki tem {pages} p\xE1ginas ({entities} entidades, {concepts} conceitos, {sources} fontes)",
  watcherActiveNotice: "Wiki: monitor de arquivos ativo \u2014 monitorando pastas selecionadas",
  watchIngestNotice: 'Wiki: {count} arquivo(s) alterado(s) em sources/. Execute "Ingerir fontes" para processar.',
  autoIngestRunning: "Ingerindo automaticamente {count} arquivo(s) alterado(s)...",
  autoIngestComplete: "Ingest\xE3o autom\xE1tica conclu\xEDda: {success} bem-sucedido(s), {fail} falha(s)",
  scheduledLintRunning: "Executando verifica\xE7\xE3o agendada da Wiki...",
  wikiLintStats: "Verifica\xE7\xE3o da Wiki: {pages} p\xE1ginas ({entities} entidades, {concepts} conceitos, {sources} fontes)",
  wikiHealthStats: "Sa\xFAde da Wiki: {pages} p\xE1ginas ({entities} entidades, {concepts} conceitos, {sources} fontes){indexStatus}",
  // Startup quick fixes detail (Issue #81)
  startupCheckTitle: "Corre\xE7\xF5es r\xE1pidas Wiki conclu\xEDdas",
  startupCheckStructureLabel: "Estrutura Wiki",
  startupCheckStructureOk: "completa",
  startupCheckStructureMissing: "incompleta \u2014 ser\xE1 criada automaticamente na primeira ingest\xE3o",
  startupCheckSourcesLabel: "Sources normalizados",
  startupCheckSourcesClean: "j\xE1 limpo",
  startupCheckSourcesCleaned: "{files} arquivo(s), {entries} entrada(s) limpa(s)",
  startupCheckIncompleteClean: "p\xE1ginas incompletas: nenhuma",
  startupCheckIncompleteArchived: "p\xE1ginas incompletas: {count} arquivada(s) (recuper\xE1vel de .trash)",
  startupCheckDisableHint: "Para desativar: Configura\xE7\xF5es \u2192 Manuten\xE7\xE3o autom\xE1tica \u2192 Executar corre\xE7\xF5es r\xE1pidas na inicializa\xE7\xE3o",
  lintWikiStart: "Iniciando verifica\xE7\xE3o da Wiki...",
  lintWikiComplete: "Verifica\xE7\xE3o da Wiki conclu\xEDda",
  lintWikiFailed: "Verifica\xE7\xE3o da Wiki falhou",
  analyzingSchema: "Analisando Wiki e gerando sugest\xF5es de Schema...",
  schemaSuggestionGenerated: "Sugest\xF5es de Schema geradas. Consulte wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "Nenhuma atualiza\xE7\xE3o de Schema necess\xE1ria.",
  schemaSuggestionFailed: "Falha ao gerar sugest\xF5es de Schema",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Pr\xE9-visualiza\xE7\xE3o de atualiza\xE7\xE3o do Schema",
  schemaDiffSummary: "+{add} / -{del} linhas",
  schemaDiffRegenerateLabel: "Refinar a sugest\xE3o (opcional):",
  schemaDiffRegenerateBtn: "Regenerar",
  schemaDiffOpenFileBtn: "Abrir arquivo",
  schemaDiffApplyBtn: "Aplicar",
  schemaDiffApplied: "Schema atualizado. Backup: {path}",
  schemaDiffEmptyTitle: "LLM n\xE3o recomenda altera\xE7\xF5es",
  schemaDiffFailed: "Falha ao aplicar o Schema: {reason}",
  schemaDiffRestoreHint: 'Backup salvo em {path}. Para restaurar, renomeie esse arquivo para "wiki/schema/config.md" no explorador de arquivos. Os 3 backups mais recentes s\xE3o mantidos.',
  schemaRegenerateNoBody: "Regenera\xE7\xE3o bem-sucedida mas o LLM n\xE3o retornou um novo Schema.",
  schemaNotFoundNotice: "Arquivo de Schema n\xE3o encontrado. Ative o Schema para cri\xE1-lo.",
  selectFolderNoMdFiles: "Nenhum arquivo Markdown na pasta: {path}",
  batchIngestSkipNotice: "Ignorando {skipped}/{total} arquivos j\xE1 ingeridos. Ingerindo {new} novos arquivos...",
  batchIngestAllIngested: "Todos os {total} arquivos desta pasta j\xE1 foram ingeridos.",
  batchIngestStarting: 'Ingerindo {count} arquivo(s) de "{folder}" \u2014 isso pode levar alguns minutos. Um relat\xF3rio aparecer\xE1 ao concluir.',
  batchIngestComplete: "Ingest\xE3o em lote conclu\xEDda: {success}/{total} bem-sucedido(s), {fail} falha(s)",
  batchIngestFailedFiles: "Arquivos com falha:",
  historyTruncated: "Hist\xF3rico truncado para as \xFAltimas {max} rodadas",
  historyCleared: "Hist\xF3rico limpo",
  // User Feedback Loop
  reviewedPagePreserved: "Preservando conte\xFAdo revisado pelo usu\xE1rio para: {}",
  // Query-to-Wiki feedback
  querySuggestSaveTitle: "Salvar na Wiki?",
  querySuggestSaveDesc: "Esta conversa cont\xE9m conhecimento valioso. Deseja salvar na sua Wiki?",
  querySuggestSaveYes: "Salvar",
  querySuggestSaveNo: "Dispensar",
  // Ingestion Report
  ingestReportElapsedTime: "Tempo decorrido",
  ingestReportSkippedFiles: "Ignorados (j\xE1 ingeridos)",
  ingestReportRejectedFiles: "Ignorados",
  rejectionReasonEmpty: "vazio",
  rejectionReasonType: "tipo n\xE3o suportado",
  rejectionReasonDuplicate: "conte\xFAdo duplicado",
  sourceRejectedEmpty: '\u23ED\uFE0F "{filename}" n\xE3o tem conte\xFAdo para ingerir \u2014 ignorado. Notas vazias ou apenas com frontmatter n\xE3o criam p\xE1ginas wiki.',
  sourceRejectedType: '\u23ED\uFE0F "{filename}" n\xE3o \xE9 um tipo de arquivo compat\xEDvel \u2014 ignorado. Apenas notas de texto (.md, .txt, etc.) podem ser ingeridas.',
  sourceRejectedDuplicate: '\u23ED\uFE0F "{filename}" ignorado \u2014 seu conte\xFAdo j\xE1 est\xE1 no wiki.',
  ingestRejectedSummary: "{count} arquivo(s) ignorado(s) (vazio, duplicado ou tipo n\xE3o compat\xEDvel).",
  reingestConfirmTitle: "Reingerir este arquivo?",
  reingestConfirmBody: 'O conte\xFAdo de "{filename}" j\xE1 est\xE1 no wiki. Reingerir mesmo assim?',
  reingestConfirmYes: "Reingerir",
  reingestConfirmNo: "Ignorar",
  ingestReportFailedGuidance: "Estes itens n\xE3o puderam ser criados automaticamente. Voc\xEA pode criar as p\xE1ginas manualmente ou reduzir a granularidade da extra\xE7\xE3o e reingerir o arquivo fonte.",
  ingestReportCollisions: "Colis\xF5es cross-type (fundidas como alias)",
  // Command Names (sentence case per Obsidian Bot rule 1)
  cmdIngestSource: "Ingerir fonte individual",
  cmdIngestFolder: "Ingerir de pasta",
  cmdQueryWiki: "Consultar Wiki",
  cmdLintWiki: "Verificar Wiki",
  cmdRegenerateIndex: "Regenerar \xEDndice",
  cmdSuggestSchema: "Sugerir atualiza\xE7\xF5es de Schema",
  cmdCancelIngestion: "Cancelar ingest\xE3o atual",
  cmdIngestActiveFile: "Ingerir arquivo atual",
  cmdViewHistory: "Ver hist\xF3rico de ingest\xE3o",
  noActiveFile: "Nenhum arquivo aberto",
  mdOnlyFile: "Apenas arquivos Markdown podem ser ingeridos",
  // Ingestion status bar
  ingestionStatusBar: "Ingerindo... clique para cancelar",
  lintStatusBar: "Verificando... clique para cancelar",
  ingestStatusAnalyzing: "Ingerindo\u2026 (clique para cancelar)",
  lintStatusReading: "Verificando\u2026 (clique para cancelar)",
  lintStatusDuplicates: "Verificando\u2026 (clique para cancelar)",
  lintStatusScanningLinks: "Verificando\u2026 (clique para cancelar)",
  lintStatusAnalyzing: "Verificando\u2026 (clique para cancelar)",
  ingestionCancelling: "Cancelando \u2014 ir\xE1 parar ap\xF3s o lote atual",
  ingestionCancelled: "Ingest\xE3o cancelada",
  crossTypeCollisionNotice: "{count} elementos fundidos como alias cross-type (duplicatas entidade \u2194 conceito evitadas)",
  // Lint Report
  lintReportTitle: "Relat\xF3rio de verifica\xE7\xE3o da Wiki",
  lintReportSummary: "Vis\xE3o geral do wiki: {total} p\xE1ginas no total, {aliasesMissing} sem aliases, {duplicates} duplicados, {deadLinks} links quebrados ({deadLinkFromDup} envolvem duplicados), {orphans} \xF3rf\xE3s ({orphanFromDup} s\xE3o duplicados), {emptyPages} vazias, {ungroundedQuotes} cita\xE7\xF5es sem fonte, {tagViolations} tags fora do vocabul\xE1rio. Lint: {elapsedSeconds}s",
  // Advanced LLM Settings (Issues #99 / #128)
  advancedSettingsModeName: "Configura\xE7\xE3o de par\xE2metros avan\xE7ados",
  advancedSettingsModeDesc: "O modo padr\xE3o usa o comportamento nativo do seu provedor \u2014 nenhum campo de controle de racioc\xEDnio, temperatura ou penalidade de repeti\xE7\xE3o \xE9 enviado. Isso funciona para a maioria dos usu\xE1rios. S\xF3 mude para Personalizado se precisar substituir explicitamente os padr\xF5es do provedor (por exemplo, for\xE7ar um dialeto de controle de racioc\xEDnio espec\xEDfico, definir uma temperatura diferente da padr\xE3o, etc.).",
  advancedSettingsDefault: "Padr\xE3o (usar comportamento do provedor)",
  advancedSettingsCustom: "Personalizado (substituir padr\xF5es do provedor)",
  disableThinkingName: "Desabilitar pensamento",
  disableThinkingDesc: "Opt-in. Quando ativado, o plugin envia uma diretiva de controle de racioc\xEDnio ao provedor e percorre uma cadeia de fallback de dialeto de 3 n\xEDveis (anthropic \u2192 openai \u2192 none) se o provedor a rejeitar. Use isso apenas se o seu provedor vazar conte\xFAdo de racioc\xEDnio na resposta e voc\xEA precisar suprimi-lo. A maioria dos provedores trata o racioc\xEDnio corretamente por conta pr\xF3pria \u2014 deixar isso desativado (padr\xE3o) oferece melhor qualidade.",
  // Issue #137: Avisos de compatibilidade (breves; sem lista de provedores)
  extractionTemperatureName: "Temperatura de extra\xE7\xE3o",
  extractionTemperatureDesc: "Intervalo 0\u20132. Valores mais baixos tornam a sa\xEDda do LLM mais determin\xEDstica e fiel. Valores mais altos tornam mais criativa. Deixe vazio para usar o padr\xE3o do provedor. Se o provedor rejeitar este valor, o plugin remove o campo automaticamente e mostra um aviso \xFAnico.",
  chatTemperatureName: "Temperatura de consulta",
  chatTemperatureDesc: "Intervalo 0\u20132. Igual \xE0 temperatura de extra\xE7\xE3o, mas afeta apenas respostas de chat/consulta. Deixe vazio para usar o padr\xE3o do provedor. Se o provedor rejeitar este valor, o plugin remove o campo automaticamente e mostra um aviso \xFAnico.",
  repetitionPenaltyName: "Penalidade de repeti\xE7\xE3o",
  repetitionPenaltyDesc: "Intervalo 0\u20132. Valores mais altos reduzem padr\xF5es repetitivos. Deixe vazio para usar o padr\xE3o do provedor. Aviso de compatibilidade: muitos provedores em nuvem n\xE3o aceitam este campo. O plugin remove-o automaticamente em HTTP 400 e mostra um aviso \xFAnico.",
  temperaturePlaceholder: "vazio = padr\xE3o do provedor",
  lintDeadLinkSection: "Links quebrados (detectados) [{count}]",
  lintEmptyPageSection: "P\xE1ginas vazias (detectadas) [{count}]",
  lintOrphanSection: "P\xE1ginas \xF3rf\xE3s (detectadas) [{count}]",
  lintContradictionSection: "Contradi\xE7\xF5es (detectadas)",
  lintDuplicateSection: "P\xE1ginas duplicadas (detectadas)",
  lintNoIssuesFound: "Nenhuma duplicata, link quebrado, p\xE1gina vazia, p\xE1gina \xF3rf\xE3 ou cita\xE7\xE3o sem fonte detectada.",
  lintQuoteGroundingSection: "Cita\xE7\xF5es sem fonte (detectadas) [{count}]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}: "{quote}"',
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (p\xE1gina inexistente){dupFlag}",
  lintDeadLinkMore: "- ... mais {count} links quebrados",
  lintEmptyPageItem: "- [[{page}]] \u2014 menos de 50 caracteres de conte\xFAdo substantivo",
  lintOrphanItem: "- [[{page}]] \u2014 nenhuma outra p\xE1gina da Wiki linka aqui{dupFlag}",
  lintPollutedSection: "P\xE1ginas polu\xEDdas (detectadas) [{count}]",
  lintPollutedItem: '- [[{page}]] \u2192 deveria ser "{clean}"',
  lintSourcesNormalizedSection: "Sources normalizadas (auto-corrigidas) [{files} arquivos / {entries} entradas]",
  lintSourcesNormalizedItem: "Limpou {entries} entradas de sources polu\xEDdas em {files} arquivo(s) (caminhos externos, extens\xF5es .md, pipes de alias removidos e deduplicados).",
  lintDuplicateItem: "- [[{target}]] e [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F envolve p\xE1gina duplicada)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F p\xE1gina duplicada)",
  lintContradictionOpen: "Contradi\xE7\xF5es abertas: {count}",
  lintContradictionAutoFixed: "({count} auto-corrigido(s) nesta execu\xE7\xE3o)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "Detectado",
  lintContradictionStatusPendingFix: "Aguardando corre\xE7\xE3o",
  lintLLMAnalysisHeading: "## An\xE1lise LLM",
  // Lint Analysis Prompt
  lintAnalysisPrompt: 'Voc\xEA \xE9 um assistente de manuten\xE7\xE3o de Wiki. Verifique a sa\xFAde da Wiki com base nas seguintes informa\xE7\xF5es.\n\n\xCDndice da Wiki:\n{index}\n\nAmostra de conte\xFAdo das p\xE1ginas ({total} p\xE1ginas no total, mostrando {sample} p\xE1ginas):\n{contentSample}\n\nResultados da verifica\xE7\xE3o program\xE1tica (j\xE1 verificados, n\xE3o repetir):\n{progReport}\n\nVerifique os seguintes aspectos (pule links quebrados/p\xE1ginas vazias/\xF3rf\xE3s j\xE1 detectados pelas verifica\xE7\xF5es program\xE1ticas):\n1. **Contradi\xE7\xF5es** \u2014 se p\xE1ginas diferentes se contradizem sobre os mesmos fatos\n2. **Desatualiza\xE7\xE3o** \u2014 se alguma declara\xE7\xE3o est\xE1 claramente desatualizada\n3. **Aus\xEAncia** \u2014 quais conceitos importantes n\xE3o possuem p\xE1ginas independentes\n4. **Estrutura** \u2014 se a estrutura das p\xE1ginas \xE9 razo\xE1vel e as refer\xEAncias cruzadas s\xE3o adequadas\n\nFormato de sa\xEDda: Use Markdown, come\xE7ando com "## An\xE1lise LLM". Cada descoberta em uma linha "- [problema espec\xEDfico]". Se n\xE3o houver problemas, escreva "Nenhum problema \xF3bvio encontrado."',
  // Lint Fix Progress
  lintFixProgress: "Corrigindo {current}/{total}: [[{target}]]",
  lintFixDeadComplete: "Corre\xE7\xE3o de links quebrados conclu\xEDda. {fixed}/{total} itens corrigidos.",
  lintFillProgress: "Expandindo {current}/{total}: {page}",
  lintFillComplete: "Expans\xE3o de p\xE1ginas conclu\xEDda. {filled}/{total} p\xE1ginas preenchidas.",
  lintDeleteCompleted: "{count} stubs vazios exclu\xEDdos",
  lintDeleteFailed: "Falha ao excluir {failed}/{total} stubs vazios (ver console)",
  lintFillFailed: "Falha ao expandir: {page} \u2014 {error}",
  lintLinkProgress: "Linkando {current}/{total}: {page}",
  lintLinkComplete: "Links de p\xE1ginas \xF3rf\xE3s conclu\xEDdos. {linked} p\xE1ginas linkadas.",
  lintFixNoAction: "Nenhuma a\xE7\xE3o realizada (sem client)",
  lintFixIndexUpdated: "\xCDndice e log da Wiki atualizados.",
  lintPollutedFixed: "P\xE1ginas polu\xEDdas corrigidas: {fixed}/{total}. \xCDndice regenerado.",
  regenerateIndexCompleted: "\xCDndice regenerado",
  operationFailed: "Falha: ",
  lintFixAllComplete: "Todas as corre\xE7\xF5es conclu\xEDdas. Consulte o log para detalhes.",
  // Lint Report Modal
  lintModalActionsTitle: "Sugest\xF5es de corre\xE7\xE3o (requer tokens LLM):",
  lintLogReference: "Relat\xF3rio completo salvo em log.md",
  lintModalFixDeadLinks: "Corrigir links quebrados ({count})",
  lintModalExpandEmpty: "Expandir p\xE1ginas vazias ({count})",
  lintModalDeleteEmpty: "Excluir stubs vazios ({count})",
  lintModalLinkOrphans: "Linkar p\xE1ginas \xF3rf\xE3s ({count})",
  lintModalAnalyzeSchema: "Analisar Schema",
  lintModalMergeDuplicates: "Mesclar duplicatas ({count})",
  lintModalFixAll: "Corre\xE7\xE3o inteligente de tudo ({count} problemas)",
  lintMergeProgress: "Mesclando {current}/{total}: {source} \u2192 {target}",
  lintMergeComplete: "Mesclagem de duplicatas conclu\xEDda. {merged}/{total} pares mesclados.",
  // Ingest Report Modal
  ingestReportTitle: "Relat\xF3rio de ingest\xE3o",
  ingestReportSourceFile: "Arquivo fonte",
  ingestReportCreated: "Criadas",
  ingestReportUpdated: "Atualizadas",
  ingestReportContradictions: "Contradi\xE7\xF5es encontradas",
  ingestReportFailedTitle: "Falha ao ingerir",
  ingestReportErrorDetail: "Detalhe do erro",
  ingestReportClose: "Fechar",
  ingestReportCreatedPages: "P\xE1ginas criadas: {count}",
  ingestReportUpdatedPages: "P\xE1ginas atualizadas: {count}",
  ingestReportEntitiesCount: "{count} entidades",
  ingestReportConceptsCount: "{count} conceitos",
  ingestReportContradictionsFound: "Contradi\xE7\xF5es encontradas: {count}",
  ingestReportEntityType: "Entidade",
  ingestReportConceptType: "Conceito",
  timeMinutes: "min",
  timeSeconds: "s",
  // Avisos de limite de taxa
  rateLimitDetected: "\u26A0\uFE0F Limite de taxa detectado: {count} p\xE1gina(s) falharam com erros 429. Sugest\xF5es: (1) Reduzir concorr\xEAncia para {suggestedConcurrency} ou 1 (serial), (2) Aumentar atraso entre lotes para {suggestedDelay}ms, (3) Mudar para um provedor com limites de taxa mais altos.",
  rateLimitDetectedShort: "\u26A0\uFE0F Limite de taxa atingido \u2014 considere reduzir a concorr\xEAncia ou aumentar o atraso entre lotes em Configura\xE7\xF5es \u2192 Acelera\xE7\xE3o de ingest\xE3o.",
  longSourceNotice: '\u{1F4C4} "{filename}" tem {lines} linhas ({size}). Textos longos requerem extra\xE7\xE3o iterativa em lotes \u2014 o LLM l\xEA o documento completo em m\xFAltiplas passagens. Isso pode levar v\xE1rios minutos. Por favor, tenha paci\xEAncia.',
  longSourceNoticeShort: "\u{1F4C4} Arquivo grande detectado ({lines} linhas). A ingest\xE3o pode demorar um pouco.",
  // Painel de Histórico de Ingestão (#122) — textos da UI
  historyButton: "Hist\xF3rico de opera\xE7\xF5es",
  historyButtonDesc: "Ver ingest\xF5es recentes, relat\xF3rios de Lint e outras altera\xE7\xF5es do seu Wiki",
  historyButtonOpen: "Ver hist\xF3rico",
  historyModalTitle: "Hist\xF3rico de opera\xE7\xF5es",
  historyModalSubtitle: "Altera\xE7\xF5es recentes do seu LLM-Wiki",
  historyEmpty: "Nenhuma opera\xE7\xE3o registrada ainda. Ingira uma nota ou execute Lint para preencher esta lista.",
  historyReadError: "N\xE3o foi poss\xEDvel ler o registro de opera\xE7\xF5es: {error}",
  historyEntryKindIngest: "Ingest\xE3o",
  historyEntryKindMaintenance: "Manuten\xE7\xE3o",
  historyEntryKindFix: "Corre\xE7\xE3o",
  historyEntryKindOther: "Outro",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "Origem: {source}",
  historyEntrySourceUnknown: "Ingest\xE3o sem t\xEDtulo",
  historyEntryCreatedLabel: "Criadas",
  historyEntryUpdatedLabel: "Atualizadas",
  historyEntryCreatedCount: "{count} criadas",
  historyEntryUpdatedCount: "{count} atualizadas",
  historyEntryNoChanges: "Sem altera\xE7\xF5es em p\xE1ginas",
  historyEntryContradictions: "\u26A0\uFE0F {count} contradi\xE7\xE3o(\xF5es)",
  historyEntrySectionCreated: "P\xE1ginas criadas",
  historyEntrySectionUpdated: "P\xE1ginas atualizadas",
  historyEntrySectionContradictions: "Contradi\xE7\xF5es",
  historyEntrySectionDetails: "Detalhes",
  historyEntryOpenPage: "Abrir",
  historyEntryDetailsNoContradictions: "Nenhuma contradi\xE7\xE3o encontrada",
  historySearchPlaceholder: "\u{1F50D} Buscar por t\xEDtulo de origem ou caminho de p\xE1gina\u2026",
  historyFilterAll: "Todas",
  historyFilterIngest: "Apenas ingest\xE3o",
  historyFilterMaintenance: "Apenas manuten\xE7\xE3o",
  historyFilterFix: "Apenas corre\xE7\xF5es",
  historyFilterContradictions: "\u26A0\uFE0F Com contradi\xE7\xF5es",
  historyRefreshButton: "\u27F3 Atualizar",
  historyExpandDay: "Clique para expandir",
  historyCollapseDay: "Clique para recolher",
  historyShowMore: "Mostrar {count} entradas mais antigas",
  historyNoMatch: "Nenhuma opera\xE7\xE3o corresponde \xE0 sua busca/filtro.",
  historyCloseButton: "Fechar",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "Achados do relat\xF3rio",
  historyKpiPages: "P\xE1ginas",
  historyKpiDeadLinks: "Links mortos",
  historyKpiOrphans: "\xD3rf\xE3s",
  historyKpiEmpty: "Vazias",
  historyKpiDuplicates: "Duplicadas",
  historyKpiTagViolations: "Tags",
  historyKpiUnsourced: "Sem fonte",
  historyKpiDuration: "Tempo Lint",
  historyKpiDurationSec: "{seconds}s",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "Links mortos ({count})",
  historySectionTagViolations: "Tags ({count})",
  historySectionOrphans: "P\xE1ginas \xF3rf\xE3s ({count})",
  historySectionEmptyPages: "P\xE1ginas vazias ({count})",
  historySectionLlmAnalysis: "An\xE1lise LLM ({count})",
  historyDeadLinkSource: "Origem",
  historyDeadLinkTarget: "Faltando",
  historyOpenInLog: "Abrir em log.md",
  historyShowMoreItems: "Mostrar mais {count}",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 igual",
  historyChipContradiction: "Contradi\xE7\xE3o",
  historyChipOutdated: "Desatualizado",
  historyChipMissing: "Faltando",
  historyChipStructure: "Estrutura",
  historySeverityHigh: "Alta",
  historySeverityMedium: "M\xE9dia",
  historySeverityLow: "Baixa",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Seu Wiki tem {dead} link(s) morto(s), {orphans} p\xE1gina(s) \xF3rf\xE3(s) e {tags} problema(s) de tag. \xDAltima Lint: {duration}.",
  historyGlobalInsightClean: "Seu Wiki est\xE1 limpo \u2014 sem links mortos, p\xE1ginas \xF3rf\xE3s ou problemas de tag.",
  historyGlobalInsightNoData: "Sem relat\xF3rios de manuten\xE7\xE3o ainda \u2014 execute Lint.",
  historyModalHeaderTitle: "Hist\xF3rico de opera\xE7\xF5es",
  historyModalSubtitleWithCount: "Altera\xE7\xF5es recentes do seu LLM-Wiki \xB7 {count} entradas",
  historyTimeRangeAll: "Todo o per\xEDodo",
  historyTimeRange1d: "\xDAltimo dia",
  historyTimeRange3d: "\xDAltimos 3 dias",
  historyTimeRange1w: "\xDAltima semana",
  historyTimeRange1m: "\xDAltimo m\xEAs",
  historyIngestTotal: "Total de p\xE1ginas",
  historyIngestByType: "Por tipo",
  historyIngestSource: "Arquivo de origem",
  historyIngestNoTimestamp: "Sem carimbo de tempo",
  historyIngestFirstTime: "Primeira ingest\xE3o",
  historyIngestLatestTime: "\xDAltima ingest\xE3o",
  historyTimeRangeCustom: "Intervalo personalizado",
  historyCustomRangeFrom: "De",
  historyCustomRangeTo: "At\xE9",
  historyCustomRangeApply: "Aplicar",
  historyCustomRangeClear: "Limpar"
};

// src/texts/it.ts
var IT_TEXTS = {
  // Informazioni Plugin
  pluginTitle: "Impostazioni Karpathy LLM Wiki",
  pluginIntro: "Questo plugin implementa il concetto di {{link}} per Obsidian. Legge le tue note, usa l'IA per estrarre entit\xE0 e concetti, e costruisce una Wiki strutturata e interconnessa nel tuo vault.",
  karpathyLinkText: "LLM Wiki di Andrej Karpathy",
  // Sezione Funzionalità
  featuresTitle: "Come funziona",
  workflow1Title: "1. Acquisisci",
  workflow1Desc: "Seleziona una nota \u2014 l'IA estrae entit\xE0, concetti e relazioni in pagine Wiki.",
  workflow2Title: "2. Interroga",
  workflow2Desc: "Conversa con la tua Wiki \u2014 le risposte si basano sulla conoscenza acquisita.",
  workflow3Title: "3. Mantieni",
  workflow3Desc: "Indice auto-generato, collegamenti incrociati e controlli Lint mantengono la Wiki in salute.",
  // Selettore Lingua
  languageTitle: "Lingua interfaccia",
  languageDesc: "Seleziona la lingua preferita per il pannello delle impostazioni. Riavvia il plugin per applicare le modifiche di lingua alla palette dei comandi.",
  languageEn: "English",
  languageZh: "\u4E2D\u6587",
  languageZhHant: "\u7E41\u9AD4\u4E2D\u6587",
  languageJa: "\u65E5\u672C\u8A9E",
  languageKo: "\uD55C\uAD6D\uC5B4",
  languageDe: "Deutsch",
  languageFr: "Fran\xE7ais",
  languageEs: "Espa\xF1ol",
  languagePt: "Portugu\xEAs",
  languageIt: "Italiano",
  // Stato
  statusTitle: "Stato client LLM",
  llmWikiStatusSection: "Stato LLM-Wiki",
  statusInitialized: "Inizializzato",
  statusNotInitialized: "Non inizializzato",
  statusReady: "LLM pronto",
  statusNotReady: "LLM non configurato \u2014 completa la configurazione e supera il test di connessione",
  currentProvider: "Provider attuale",
  // Configurazione Provider
  providerSection: "Configurazione LLM",
  providerName: "Provider LLM",
  providerDesc: "Seleziona un provider predefinito o un servizio personalizzato compatibile con OpenAI. Per Coding Plan o pacchetti simili, scegli Custom OpenAI/Anthropic e inserisci manualmente l'URL di base e la chiave API del provider",
  // Chiave API
  apiKeyName: "Chiave API",
  apiKeyDesc: "Inserisci la chiave API del tuo provider",
  apiKeyPlaceholder: "Inserisci la chiave API",
  // URL di base
  baseUrlName: "URL di base API",
  baseUrlDescCustom: "Obbligatorio: endpoint personalizzato compatibile con OpenAI",
  baseUrlDescOverride: "Facoltativo: sovrascrivi l'URL di base predefinito",
  // Suggerimento Ollama
  ollamaHint: "Ollama gira in locale, nessuna chiave API richiesta",
  // Suggerimento LM Studio
  lmstudioHint: "LM Studio gira in locale, la chiave API \xE8 facoltativa",
  // Limite esecuzione LLM
  maxTokensPerCallName: "Finestra di contesto",
  maxTokensPerCallDesc: "Limita i token di generazione per adattarli alla finestra di contesto del tuo modello. 0 = nessun limite (predefinito cloud).",
  // Issue #111: maiuscole/minuscole slug
  slugCaseName: "Maiuscole/minuscole nome file",
  slugCaseDesc: 'Controlla se i nomi dei file wiki generati vengono convertiti in minuscolo. "Preserva" \xE8 consigliato per le lingue in cui il minuscolo cambia il significato (es. i sostantivi tedeschi).',
  slugCaseLower: "Minuscolo (predefinito)",
  slugCasePreserve: "Preserva maiuscole/minuscole",
  // Selezione Modello
  modelSection: "Selezione modello",
  fetchModelsName: "Recupera modelli disponibili",
  fetchModelsDesc: "Ottieni la lista pi\xF9 recente di modelli dalla API del provider",
  fetchModelsButton: "Recupera modelli",
  fetchingModels: "Recupero in corso...",
  fetchSuccess: "Riuscito! {} modelli disponibili",
  fetchFailed: "Operazione fallita o lista vuota, inserisci manualmente il nome del modello",
  fetchNotSupported: "Il provider non supporta la query della lista modelli",
  fetchErrorAuth: "Autenticazione fallita (HTTP 401/403). Verifica la tua chiave API, oppure inserisci un ID modello qui sotto e clicca Testa connessione per validare.",
  fetchErrorEndpoint: "Endpoint non trovato (HTTP 404). Verifica l'URL di base, oppure inserisci un ID modello e clicca Testa connessione per validare.",
  fetchErrorServer: "Errore del server del provider (HTTP 5xx). Riprova pi\xF9 tardi, oppure inserisci un ID modello e clicca Testa connessione per validare.",
  fetchErrorEmpty: "Il provider non ha un endpoint per la lista modelli. Inserisci un ID modello qui sotto e clicca Testa connessione per validare.",
  fetchErrorNetwork: "Richiesta di rete fallita. Controlla la connessione internet, l'URL di base o le impostazioni del proxy. Puoi anche inserire un ID modello qui sotto e cliccare Testa connessione per validare.",
  selectModelName: "Seleziona modello",
  selectModelDesc: "Scegli tra {} modelli disponibili",
  customInputOption: "Inserimento personalizzato...",
  customInputHint: 'Per usare altri modelli, seleziona "Inserimento personalizzato..."',
  modelName: "Nome modello",
  modelDescCustom: "Uso di modello personalizzato (clicca il pulsante sopra per ri-recuperare la lista)",
  modelDescFetchFailed: "Recupero della lista modelli fallito. Verifica la tua chiave API e l'URL dell'endpoint, oppure inserisci un ID modello qui sotto e clicca Testa connessione per validare.",
  modelInputPlaceholder: "Inserisci ID modello, poi Testa connessione",
  switchToDropdown: "Passa alla selezione a tendina",
  useDropdownButton: "Usa menu a tendina",
  // Test e Salvataggio
  testConnectionName: "Testa connessione",
  testConnectionDesc: "Verifica che la configurazione possa chiamare con successo la API LLM",
  testButton: "Testa connessione",
  testing: "Test in corso...",
  saveSettingsName: "Salva impostazioni",
  saveSettingsDesc: "Salva la configurazione attuale",
  saveButton: "Salva impostazioni",
  savedNotice: "Impostazioni salvate!",
  // Test Connessione
  testConnectionSuccessful: "Connessione riuscita",
  testConnectionFailed: "Connessione fallita",
  testConnectionProvider: "Provider: ",
  errorUnknown: "Errore sconosciuto",
  // Issue #137: avvisi di fallback LLM (mostrati quando avviene un
  // fallback del dialetto di ragionamento o uno strip di parametri durante una richiesta).
  fallbackThinkingDialect: `Controllo ragionamento: passato al dialetto "{dialect}" (questo provider usa un formato di controllo del ragionamento diverso). L'output rimane invariato.`,
  fallbackThinkingNone: "Controllo del ragionamento completamente disabilitato per questo provider. Il contenuto del ragionamento potrebbe comunque apparire; in tal caso, prova un modello diverso.",
  fallbackParamStripped: 'Parametro "{field}" non supportato da questo provider. Rimosso dalla richiesta; il comportamento potrebbe differire dalla configurazione.',
  // Stato Inizializzazione Wiki
  wikiInitStatusReady: "Wiki inizializzata",
  wikiInitStatusNotReady: "Wiki non inizializzata \u2014 verr\xE0 creata automaticamente alla prima acquisizione",
  // Cartella Wiki
  wikiSection: "Configurazione Wiki",
  wikiFolderName: "Cartella Wiki",
  wikiFolderDesc: "Posizione delle pagine Wiki generate",
  wikiFolderPlaceholder: "wiki",
  // Errori
  errorNoApiKey: "Configura prima la chiave API",
  llmNotReady: "LLM non configurato. Vai su Impostazioni \u2192 Karpathy LLM Wiki per configurare il tuo provider, recuperare i modelli disponibili e superare il test di connessione.",
  errorFetchFailed: "Fallito: {}",
  // Impostazioni Query
  querySectionTitle: "Configurazione query Wiki",
  maxConversationHistoryName: "Cronologia conversazione massima",
  maxConversationHistoryDesc: "Limita i messaggi della conversazione per evitare il superamento dei token",
  maxConversationHistoryHint: "Consigliato: non superare 50 turni",
  numberRangeValidation: "Inserisci un numero tra 1 e 50",
  numberRangeClamped: "Il valore supera il range (1-500), impostato automaticamente a {}",
  // UI Modale Query
  queryModalTitle: "Query Wiki - Query conversazionale",
  queryModalPlaceholder: "Inserisci una domanda...",
  queryModalSendButton: "Invia",
  queryModalStopButton: "Ferma",
  queryModalSaveButton: "Salva nella Wiki",
  queryModalClearButton: "Cancella cronologia",
  queryModalHistoryCount: "Cronologia conversazione: {}/{} turni",
  queryModalStreaming: "Streaming...",
  queryModalFallbackStreaming: "Streaming non supportato, passato a non-streaming. Attendere...",
  queryPhaseSearching: "Analisi dell'indice Wiki, selezione delle pagine pertinenti...",
  queryPhaseFoundPages: "Trovate {count} pagina/e: {pages}",
  queryPhaseLoadingPages: "Caricamento del contenuto della pagina...",
  queryPhaseContextReady: "Contesto pronto. Generazione della risposta...",
  queryPhaseGenerating: "Generazione... (trascorsi {time}s)",
  queryPhaseNonStreaming: "Modalit\xE0 non-streaming, generazione della risposta... (trascorsi {time}s)",
  queryModalErrorPrefix: "Errore: ",
  queryModalHint: 'Le query si basano sul contenuto della Wiki. Clicca "Salva nella Wiki" per estrarre le conversazioni preziose come pagine Wiki.',
  // v1.20.0: riepilogo collassabile del blocco di ragionamento in Query Wiki
  queryThinkingSummary: "Processo di ragionamento",
  queryThinkingSteps: "passaggi",
  // Messaggi di errore
  errorLLMClientNotInit: "Client LLM non inizializzato. Salva le impostazioni.",
  errorIngestFailed: "Acquisizione fallita: ",
  errorQueryFailed: "Query fallita: ",
  fileWriteFailed: "Impossibile creare o aggiornare il file: {path}",
  // Messaggi di successo
  ingestSuccess: "Acquisizione riuscita: {} pagine create, {} pagine aggiornate",
  querySuccess: "Query completata",
  lintSuccess: "Lint della Wiki completato",
  lintReadingPages: "Lettura di {count} pagine Wiki...",
  lintReadingPagesProgress: "Lettura pagine Wiki: {current}/{total}...",
  lintScanningLinks: "Scansione dei collegamenti interrotti...",
  lintScanningLinksProgress: "Scansione collegamenti interrotti: {current}/{total}...",
  lintCheckingDuplicates: "Controllo delle pagine duplicate...",
  lintCheckingDuplicatesProgress: "Verifica duplicati: batch {current} ...",
  lintFixingPolluted: "Correzione pagina inquinata {current}/{total}: {title} \u2192 {newTitle}",
  lintModalFixPolluted: "\u{1F9F9} Correggi pagine inquinate ({count})",
  lintDuplicateCheckFailed: "Rilevamento duplicati fallito \u2014 vedi la console per i dettagli",
  lintDuplicateCheckFailedDetail: "Controllo duplicati fallito a {step}: {error}",
  lintMergeItemFailed: "Unione fallita: {source} \u2192 {target} \u2014 {error}",
  lintAliasesMissing: "Alias mancanti: {count} pagina/e senza alias",
  lintAliasesSection: "Pagine senza alias [{count}]",
  lintAliasesItem: "- [[{page}]]",
  lintAliasesCompleteBtn: "Completa alias ({count})",
  lintAliasesFilling: "Generazione alias {current}/{total}: {page}",
  lintAliasesFilled: "Completamento alias terminato. Compilate {filled}/{total} pagine.",
  lintAliasesFillFailed: "Generazione alias fallita: {page} \u2014 {error}",
  // Issue #85 v7: notifiche di ri-tagging per violazione tag
  lintTagViolationFiring: "Ri-tagging {current}/{total}: {path}",
  lintTagViolationFailed: "Ri-tagging fallito per {path}: {error}",
  lintTagViolationFixed: "Ri-tagging completato. Corrette {fixed}/{total} pagina/e.",
  lintTagViolationFixedNone: "Ri-tagging completato. Nessuna pagina necessitava correzioni (l'LLM ha mantenuto i tag attuali).",
  lintTagViolationSection: "Pagine con tag fuori vocabolario [{count}]",
  lintTagViolationItem: "- [[{path}]] \u2014 non validi: {tags}",
  lintTagViolationRetagBtn: "\u{1F3F7}\uFE0F Ri-tagga {count} pagina/e con LLM",
  lintFixItemFailed: "Correzione fallita: [[{target}]] \u2014 {error}",
  lintLinkItemFailed: "Collegamento fallito: {page} \u2014 {error}",
  lintRetrying: "Nuovo tentativo ({attempt}/{max}) dopo errore...",
  lintAnalyzingLLM: "LLM sta analizzando la salute della Wiki...",
  saveToWikiSuccess: "Conversazione salvata nella Wiki!",
  saveSummary: "{entities} entit\xE0, {concepts} concetti, {pages} pagine",
  aliasAdded: "Aggiunto alias '{alias}' alla pagina '{page}'",
  // Messaggi di stato
  ingestingSources: "Acquisizione delle sorgenti...",
  queryingWiki: "Interrogazione della Wiki...",
  lintingWiki: "Lint della Wiki...",
  savingToWiki: "Salvataggio della conversazione nella Wiki...",
  clearingHistory: "Cancellazione della cronologia conversazione...",
  // Pulsanti
  ingestButton: "Acquisisci",
  queryButton: "Interroga",
  lintButton: "Lint",
  cancelButton: "Annulla",
  // Collegamenti
  karpathyGistLink: "Gist della LLM Wiki di Karpathy",
  obsidianPluginAPI: "Obsidian Plugin API",
  anthropicSDK: "Anthropic SDK",
  openaiSDK: "OpenAI SDK",
  // Altro
  availableModelsLoading: "Caricamento dei modelli disponibili...",
  noModelsAvailable: "Nessun modello disponibile",
  // Suggerimento Lingua LLM
  llmLanguageHint: "Rispondi in italiano.",
  // Configurazione Schema
  schemaSection: "Configurazione Schema",
  enableSchemaName: "Abilita Schema",
  enableSchemaDesc: "Genera e inietta schema/config.md in tutti i prompt LLM per un output Wiki strutturato",
  viewSchemaButton: "Visualizza / Modifica Schema",
  regenerateSchemaButton: "Rigenera Schema predefinito",
  schemaRegeneratedNotice: "Schema predefinito rigenerato.",
  schemaRegenerateFailed: "Generazione dello Schema fallita",
  // Lingua di output Wiki
  wikiLanguageName: "Lingua di output Wiki",
  wikiLanguageDesc: "Tutte le pagine wiki generate, l'indice, il log e le risposte alle query usano questa lingua",
  customWikiLanguageOption: "Personalizzata...",
  customWikiLanguageHint: "Inserisci il nome di una lingua (es. Italiano, Arabo). Verr\xE0 passato all'LLM come direttiva di lingua di output.",
  customWikiLanguagePlaceholder: "es. Italiano",
  // Etichette Indice e Log (per lingua)
  indexLabels: {
    en: { subtitle: "Auto-generated knowledge base directory", entities: "Entities", concepts: "Concepts", sources: "Sources" },
    zh: { subtitle: "\u81EA\u52A8\u751F\u6210\u7684\u77E5\u8BC6\u5E93\u76EE\u5F55", entities: "\u5B9E\u4F53", concepts: "\u6982\u5FF5", sources: "\u6765\u6E90" },
    ja: { subtitle: "\u81EA\u52D5\u751F\u6210\u30CA\u30EC\u30C3\u30B8\u30D9\u30FC\u30B9\u30C7\u30A3\u30EC\u30AF\u30C8\u30EA", entities: "\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3", concepts: "\u6982\u5FF5", sources: "\u30BD\u30FC\u30B9" },
    ko: { subtitle: "\uC790\uB3D9 \uC0DD\uC131 \uC9C0\uC2DD \uBCA0\uC774\uC2A4 \uB514\uB809\uD1A0\uB9AC", entities: "\uC5D4\uD2F0\uD2F0", concepts: "\uCEE8\uC149", sources: "\uC18C\uC2A4" },
    de: { subtitle: "Automatisch generiertes Wissensdatenbank-Verzeichnis", entities: "Entit\xE4ten", concepts: "Konzepte", sources: "Quellen" },
    fr: { subtitle: "R\xE9pertoire de base de connaissances g\xE9n\xE9r\xE9 automatiquement", entities: "Entit\xE9s", concepts: "Concepts", sources: "Sources" },
    es: { subtitle: "Directorio de base de conocimiento generado autom\xE1ticamente", entities: "Entidades", concepts: "Conceptos", sources: "Fuentes" },
    pt: { subtitle: "Diret\xF3rio de base de conhecimento gerado automaticamente", entities: "Entidades", concepts: "Conceitos", sources: "Fontes" }
  },
  logLabels: {
    en: { createdPages: "Created pages", updatedPages: "Updated pages", contradictionsFound: "Contradictions found" },
    zh: { createdPages: "\u521B\u5EFA\u9875\u9762", updatedPages: "\u66F4\u65B0\u9875\u9762", contradictionsFound: "\u53D1\u73B0\u77DB\u76FE" },
    ja: { createdPages: "\u4F5C\u6210\u30DA\u30FC\u30B8", updatedPages: "\u66F4\u65B0\u30DA\u30FC\u30B8", contradictionsFound: "\u77DB\u76FE\u3092\u767A\u898B" },
    ko: { createdPages: "\uC0DD\uC131 \uD398\uC774\uC9C0", updatedPages: "\uC5C5\uB370\uC774\uD2B8 \uD398\uC774\uC9C0", contradictionsFound: "\uBAA8\uC21C \uBC1C\uACAC" },
    de: { createdPages: "Erstellte Seiten", updatedPages: "Aktualisierte Seiten", contradictionsFound: "Widerspr\xFCche gefunden" },
    fr: { createdPages: "Pages cr\xE9\xE9es", updatedPages: "Pages mises \xE0 jour", contradictionsFound: "Contradictions trouv\xE9es" },
    es: { createdPages: "P\xE1ginas creadas", updatedPages: "P\xE1ginas actualizadas", contradictionsFound: "Contradicciones encontradas" },
    pt: { createdPages: "P\xE1ginas criadas", updatedPages: "P\xE1ginas atualizadas", contradictionsFound: "Contradi\xE7\xF5es encontradas" }
  },
  // Impostazioni Estrazione
  extractionSectionTitle: "Estrazione",
  extractionGranularityName: "Granularit\xE0 estrazione",
  extractionGranularityDesc: "Controlla le entit\xE0/concetti estratti per file sorgente. Pi\xF9 alto = pi\xF9 pagine, pi\xF9 token API.\nFine: analisi approfondita. Standard: note quotidiane. Grossolana: panoramica rapida. Minima: batch di oltre 100 file. Personalizzata: imposta i tuoi limiti (fino a 500).\nSuggerimento: usa Minima/Grossolana per cartelle con molti file per risparmiare tempo e costi.",
  extractionGranularityFine: "Fine \u2014 analisi approfondita (\u2264100 elementi)",
  extractionGranularityStandard: "Standard \u2014 note quotidiane (\u226450 elementi)",
  extractionGranularityCoarse: "Grossolana \u2014 panoramica rapida (\u226410 elementi)",
  extractionGranularityMinimal: "Minima \u2014 batch di oltre 100 file (\u22645 elementi)",
  extractionGranularityCustom: "Personalizzata \u2014 imposta i tuoi limiti (1~500)",
  customEntityLimitName: "Limite entit\xE0 personalizzato",
  customEntityLimitDesc: "Numero massimo di entit\xE0 da estrarre per file sorgente (1-500)",
  customConceptLimitName: "Limite concetti personalizzato",
  customConceptLimitDesc: "Numero massimo di concetti da estrarre per file sorgente (1-500)",
  // Issue #85 v2: Vocabolario tag (UX a chip, integrato in Configurazione Wiki)
  tagVocabularyInlineDesc: 'Vocabolario controllato per i tag del frontmatter di entit\xE0 e concetti. I tag annidati di Obsidian con "/" vengono preservati.',
  tagVocabularyModeName: "Configurazione vocabolario tag",
  tagVocabularyModeDescDefault: "Il predefinito usa i tag integrati: {}. Passa a Personalizzato per definire i tuoi.",
  tagVocabularyModeDescCustom: "Personalizzato: definisci qui sotto i tuoi tag per entit\xE0 e concetti. Usa l'input a chip \u2014 Invio o virgola per aggiungere, \xD7 per rimuovere.",
  tagVocabularyModeDefault: "Predefinito (tag di sottotipo integrati)",
  tagVocabularyModeCustom: "Personalizzato (definito dall'utente)",
  customEntityTagsName: "Tag entit\xE0 personalizzati",
  customEntityTagsDesc: 'Premi Invio o virgola per aggiungere una chip. Clicca \xD7 per rimuovere. I tag annidati con "/" vengono preservati.',
  customEntityTagsPlaceholder: "persona, organizzazione, progetto, luogo",
  customConceptTagsName: "Tag concetti personalizzati",
  customConceptTagsDesc: 'Premi Invio o virgola per aggiungere una chip. Clicca \xD7 per rimuovere. I tag annidati con "/" vengono preservati.',
  customConceptTagsPlaceholder: "teoria, metodo, campo, fenomeno, termine",
  chipDuplicateHint: "Tag duplicato ignorato",
  // Accelerazione Acquisizione
  accelerationSectionTitle: "Accelerazione acquisizione",
  pageGenerationConcurrencyName: "Concorrenza LLM",
  pageGenerationConcurrencyDesc: "Numero di chiamate LLM in parallelo durante le operazioni di acquisizione e lint. Valori pi\xF9 alti accelerano l'elaborazione ma aumentano i costi API e possono attivare limiti di frequenza.",
  concurrencyValueSingular: "Concorrenza attuale: {} (seriale \u2014 pi\xF9 sicura)",
  concurrencyValuePlural: "Concorrenza attuale: {} (parallela)",
  batchDelayName: "Ritardo batch (ms)",
  batchDelayDesc: "Ritardo tra batch paralleli per prevenire il rate limiting delle API (100-2000ms). Attuale: {}ms. Aumentalo se vedi errori 429.",
  // Manutenzione Automatica
  autoMaintainSection: "Manutenzione automatica",
  autoMaintainBetaBadge: "BETA \u2014 Funzionalit\xE0 sperimentale. Potrebbe presentare problemi. Consigliata solo per utenti esperti.",
  autoWatchName: "Monitora cartelle",
  autoWatchDesc: "Rileva automaticamente i file .md nuovi o modificati nelle cartelle monitorate e notifica o acquisisce automaticamente",
  watchedFoldersName: "Cartelle monitorate",
  watchedFoldersDesc: 'Cartelle da monitorare per nuovi contenuti. Clicca "Aggiungi cartella" per selezionarla dal tuo vault.',
  addWatchedFolderButton: "Aggiungi cartella",
  removeWatchedFolderButton: "Rimuovi",
  webClipperPresetName: "Monitora Clippings (Web Clipper)",
  webClipperPresetDesc: "Aggiungi alla lista di monitoraggio la cartella Clippings/ usata da Obsidian Web Clipper. I tuoi ritagli web verranno acquisiti automaticamente nella Wiki.",
  noWatchedFoldersHint: "Nessuna cartella configurata. Aggiungi una cartella o abilita il preset Clippings.",
  autoWatchModeName: "Modalit\xE0 monitoraggio",
  autoWatchModeDesc: '"Solo notifica" mostra un avviso. "Acquisizione automatica" elabora in silenzio.',
  watchModeNotify: "Solo notifica",
  watchModeAuto: "Acquisizione automatica",
  autoWatchDebounceName: "Ritardo debounce (secondi)",
  autoWatchDebounceDesc: "Tempo di attesa prima di avviare l'acquisizione dopo una modifica di file (1-60 secondi)",
  periodicLintName: "Lint periodico",
  periodicLintDesc: "Esegui il lint LLM su pianificazione, solo quando i file sorgente sono cambiati dall'ultimo controllo",
  periodicLintOff: "Disattivato",
  periodicLintHourly: "Ogni ora",
  periodicLintDaily: "Giornaliero",
  periodicLintWeekly: "Settimanale",
  autoSmartFixName: "Correzione intelligente automatica",
  autoSmartFixDesc: "Quando viene eseguito il lint, applica automaticamente tutte le correzioni (Correzione intelligente totale) senza mostrare la modale del report. Il riepilogo delle correzioni viene comunque mostrato al completamento.",
  autoSmartFixNotice: "Correzione intelligente automatica: applicazione di tutte le correzioni...",
  startupCheckName: "Esegui correzioni rapide all'avvio",
  startupCheckDesc: "Corregge automaticamente problemi di formato di basso livello (sorgenti, collegamenti doppiamente annidati) al caricamento del plugin. Verifica la struttura della cartella Wiki. Predefinito ATTIVO.",
  suggestSchemaCommand: "Suggerisci aggiornamenti Schema",
  autoMaintainCostWarning: '\u26A0\uFE0F Avviso costi: le funzionalit\xE0 di manutenzione automatica consumano token API. "Acquisizione automatica" attiva chiamate LLM a ogni modifica di file sorgente. "Lint periodico" esegue controlli di salute LLM su pianificazione (solo quando vengono rilevate modifiche alle sorgenti). Configura con attenzione per evitare addebiti imprevisti.',
  // Avvisi
  startupCheckSummary: "La Wiki ha {pages} pagine ({entities} entit\xE0, {concepts} concetti, {sources} sorgenti)",
  watcherActiveNotice: "Wiki: monitoraggio file attivo \u2014 sorveglianza delle cartelle monitorate",
  watchIngestNotice: 'Wiki: {count} file modificati in sources/. Esegui "Acquisisci sorgenti" per elaborarli.',
  // Dettagli correzioni rapide all'avvio (Issue #81)
  startupCheckTitle: "Correzioni rapide della Wiki completate",
  startupCheckStructureLabel: "Struttura Wiki",
  startupCheckStructureOk: "completa",
  startupCheckStructureMissing: "incompleta \u2014 verr\xE0 creata automaticamente alla prima acquisizione",
  startupCheckSourcesLabel: "Sorgenti normalizzate",
  startupCheckSourcesClean: "gi\xE0 pulite",
  startupCheckSourcesCleaned: "pulite {files} file, {entries} voce/i",
  startupCheckIncompleteClean: "pagine incomplete: nessuna",
  startupCheckIncompleteArchived: "pagine incomplete: {count} archiviate (recuperabili da .trash)",
  startupCheckDisableHint: "Per disabilitare, vai su Impostazioni \u2192 Manutenzione automatica \u2192 Esegui correzioni rapide all'avvio",
  autoIngestRunning: "Acquisizione automatica di {count} file modificato/i...",
  autoIngestComplete: "Acquisizione automatica completata: {success} riusciti, {fail} falliti",
  scheduledLintRunning: "Esecuzione del lint Wiki pianificato...",
  wikiLintStats: "Lint Wiki: {pages} pagine ({entities} entit\xE0, {concepts} concetti, {sources} sorgenti)",
  wikiHealthStats: "Salute Wiki: {pages} pagine ({entities} entit\xE0, {concepts} concetti, {sources} sorgenti){indexStatus}",
  lintWikiStart: "Avvio del lint Wiki...",
  lintWikiComplete: "Lint Wiki completato",
  lintWikiFailed: "Lint Wiki fallito",
  analyzingSchema: "Analisi della Wiki e generazione di suggerimenti per lo schema...",
  schemaSuggestionGenerated: "Suggerimenti schema generati, vedi wiki/schema/suggestions.md",
  noSchemaUpdateNeeded: "Nessun aggiornamento dello schema necessario.",
  schemaSuggestionFailed: "Suggerimento dello schema fallito",
  // v1.22.0 #97: Schema diff Modal — IDE-style preview + Apply/Regenerate
  schemaDiffTitle: "Anteprima aggiornamento schema",
  schemaDiffSummary: "+{add} / -{del} righe",
  schemaDiffRegenerateLabel: "Affina il suggerimento (opzionale):",
  schemaDiffRegenerateBtn: "Rigenera",
  schemaDiffOpenFileBtn: "Apri file",
  schemaDiffApplyBtn: "Applica",
  schemaDiffApplied: "Schema aggiornato. Backup: {path}",
  schemaDiffEmptyTitle: "LLM non consiglia modifiche",
  schemaDiffFailed: "Applicazione schema non riuscita: {reason}",
  schemaDiffRestoreHint: `Backup salvato in {path}. Per ripristinare, rinomina quel file in "wiki/schema/config.md" nell'esplora file. Vengono conservati i 3 backup pi\xF9 recenti.`,
  schemaRegenerateNoBody: "Rigenerazione riuscita ma l'LLM non ha restituito un nuovo schema.",
  schemaNotFoundNotice: "File schema non trovato. Abilita lo schema per crearlo.",
  selectFolderNoMdFiles: "Nessun file Markdown nella cartella: {path}",
  batchIngestSkipNotice: "Saltati {skipped}/{total} file gi\xE0 acquisiti. Acquisizione di {new} nuovi file...",
  batchIngestAllIngested: "Tutti i {total} file in questa cartella sono gi\xE0 stati acquisiti.",
  batchIngestStarting: 'Acquisizione di {count} file da "{folder}" \u2014 potrebbe richiedere alcuni minuti. Un report apparir\xE0 al completamento.',
  batchIngestComplete: "Acquisizione batch completata: {success}/{total} riusciti, {fail} falliti",
  batchIngestFailedFiles: "File falliti:",
  historyTruncated: "Cronologia troncata agli ultimi {max} turni",
  historyCleared: "Cronologia cancellata",
  // Ciclo di feedback utente
  reviewedPagePreserved: "Conservazione del contenuto revisionato dall'utente per: {}",
  // Feedback da Query a Wiki
  querySuggestSaveTitle: "Salvare nella Wiki?",
  querySuggestSaveDesc: "Questa conversazione contiene conoscenza preziosa. Salvarla nella tua Wiki?",
  querySuggestSaveYes: "Salva",
  querySuggestSaveNo: "Ignora",
  // Report Acquisizione
  ingestReportElapsedTime: "Tempo trascorso",
  ingestReportSkippedFiles: "Saltati (gi\xE0 acquisiti)",
  ingestReportRejectedFiles: "Saltati",
  rejectionReasonEmpty: "vuoto",
  rejectionReasonType: "tipo non supportato",
  rejectionReasonDuplicate: "contenuto duplicato",
  sourceRejectedEmpty: '\u23ED\uFE0F "{filename}" non ha contenuto da acquisire \u2014 saltato. Le note vuote o con solo frontmatter non creano pagine wiki.',
  sourceRejectedType: '\u23ED\uFE0F "{filename}" non \xE8 un tipo di file supportato \u2014 saltato. Possono essere acquisite solo note di testo (.md, .txt, ecc.).',
  sourceRejectedDuplicate: '\u23ED\uFE0F "{filename}" saltato \u2014 il suo contenuto \xE8 gi\xE0 nel wiki.',
  ingestRejectedSummary: "{count} file saltati (vuoti, duplicati o di tipo non supportato).",
  reingestConfirmTitle: "Riacquisire questo file?",
  reingestConfirmBody: 'Il contenuto di "{filename}" \xE8 gi\xE0 nel wiki. Riacquisirlo comunque?',
  reingestConfirmYes: "Riacquisisci",
  reingestConfirmNo: "Salta",
  ingestReportFailedGuidance: "Questi elementi non sono stati creati automaticamente. Puoi creare manualmente le pagine corrispondenti, oppure abbassare la granularit\xE0 di estrazione e ri-acquisire il file sorgente.",
  ingestReportCollisions: "Collisioni inter-tipo (unite come alias)",
  // Nomi Comandi (sentence case secondo la regola 1 dell'Obsidian Bot)
  cmdIngestSource: "Acquisisci singola sorgente",
  cmdIngestFolder: "Acquisisci da cartella",
  cmdQueryWiki: "Interroga wiki",
  cmdLintWiki: "Lint wiki",
  cmdRegenerateIndex: "Rigenera indice",
  cmdSuggestSchema: "Suggerisci aggiornamenti schema",
  cmdCancelIngestion: "Annulla acquisizione corrente",
  cmdIngestActiveFile: "Acquisisci file corrente",
  cmdViewHistory: "Visualizza cronologia acquisizioni",
  noActiveFile: "Nessun file \xE8 attualmente aperto",
  mdOnlyFile: "Solo i file Markdown possono essere acquisiti",
  // Barra di stato acquisizione
  ingestionStatusBar: "Acquisizione... clicca per annullare",
  lintStatusBar: "Lint... clicca per annullare",
  ingestStatusAnalyzing: "Acquisizione\u2026 (clicca per annullare)",
  lintStatusReading: "Lint\u2026 (clicca per annullare)",
  lintStatusDuplicates: "Lint\u2026 (clicca per annullare)",
  lintStatusScanningLinks: "Lint\u2026 (clicca per annullare)",
  lintStatusAnalyzing: "Lint\u2026 (clicca per annullare)",
  ingestionCancelling: "Annullamento \u2014 si fermer\xE0 al completamento del batch corrente",
  ingestionCancelled: "Acquisizione annullata",
  crossTypeCollisionNotice: "{count} elementi uniti come alias inter-tipo (duplicati entit\xE0 \u2194 concetto prevenuti)",
  // Report Lint
  lintReportTitle: "Report lint Wiki",
  lintReportSummary: "Panoramica stato Wiki: {total} pagine totali, {aliasesMissing} pagine senza alias, {duplicates} pagine duplicate, {deadLinks} collegamenti interrotti ({deadLinkFromDup} coinvolgono duplicati), {orphans} pagine orfane ({orphanFromDup} sono duplicati), {emptyPages} pagine vuote, {ungroundedQuotes} citazioni non fondate, {tagViolations} tag fuori vocabolario. Lint completato in: {elapsedSeconds}s",
  // Impostazioni LLM avanzate (v1.20.0: predefinito = nessun override specifico del provider)
  advancedSettingsModeName: "Impostazioni parametri avanzate",
  advancedSettingsModeDesc: "Il predefinito usa il comportamento del tuo provider \u2014 non vengono inviati campi di controllo del ragionamento, temperatura o penalit\xE0 di ripetizione. Questo funziona per la maggior parte degli utenti. Passa a Personalizzato solo se hai bisogno di sovrascrivere esplicitamente i valori predefiniti del provider (es. forzare uno specifico dialetto di controllo del ragionamento, impostare una temperatura non predefinita, ecc.).",
  advancedSettingsDefault: "Predefinito (usa comportamento del provider)",
  advancedSettingsCustom: "Personalizzato (sovrascrivi i valori predefiniti del provider)",
  disableThinkingName: "Disabilita ragionamento",
  disableThinkingDesc: "Opt-in. Quando abilitato, il plugin invia una direttiva di controllo del ragionamento al provider e percorre una catena di fallback a 3 livelli (anthropic \u2192 openai \u2192 none) se il provider la rifiuta. Usalo solo se il tuo provider fa trapelare il contenuto del ragionamento nella risposta e devi sopprimerlo. La maggior parte dei provider gestisce il ragionamento correttamente da s\xE9 \u2014 lasciarlo disattivato offre qualit\xE0 migliore.",
  // Issue #137: suggerimenti di compatibilità per le impostazioni avanzate (tenuti brevi; nessuna
  // lista di provider per evitare oneri di manutenzione quando i provider cambiano).
  extractionTemperatureName: "Temperatura estrazione",
  extractionTemperatureDesc: "Range 0\u20132. Valori pi\xF9 bassi rendono l'output dell'LLM pi\xF9 deterministico e fedele. Valori pi\xF9 alti lo rendono pi\xF9 creativo. Lascia vuoto per usare il valore predefinito del tuo provider. Se il tuo provider rifiuta questo valore, il plugin rimuove automaticamente il campo e mostra un avviso una tantum.",
  chatTemperatureName: "Temperatura query",
  chatTemperatureDesc: "Range 0\u20132. Come la Temperatura estrazione, ma influisce solo sulle risposte di chat/query. Lascia vuoto per usare il valore predefinito del tuo provider. Se il tuo provider rifiuta questo valore, il plugin rimuove automaticamente il campo e mostra un avviso una tantum.",
  repetitionPenaltyName: "Penalit\xE0 di ripetizione",
  repetitionPenaltyDesc: "Range 0\u20132. Valori pi\xF9 alti riducono i pattern ripetitivi. Lascia vuoto per usare il valore predefinito del tuo provider. Nota di compatibilit\xE0: molti provider cloud non accettano questo campo. Il plugin lo rimuove automaticamente su errore 400 e mostra un avviso una tantum.",
  temperaturePlaceholder: "vuoto = predefinito provider",
  lintDeadLinkSection: "Collegamenti interrotti (rilevati) [{count}]",
  lintEmptyPageSection: "Pagine vuote (rilevate) [{count}]",
  lintOrphanSection: "Pagine orfane (rilevate) [{count}]",
  lintContradictionSection: "Contraddizioni (rilevate)",
  lintDuplicateSection: "Pagine duplicate (rilevate)",
  lintPollutedSection: "Pagine inquinate (rilevate) [{count}]",
  lintPollutedItem: '- [[{page}]] \u2192 dovrebbe essere "{clean}"',
  lintSourcesNormalizedSection: "Sorgenti normalizzate (corrette automaticamente) [{files} file / {entries} voci]",
  lintSourcesNormalizedItem: "Pulite {entries} voci sorgenti inquinate in {files} file (percorsi esterni, estensioni .md, pipe degli alias rimossi e deduplicati).",
  lintNoIssuesFound: "Nessun duplicato, collegamento interrotto, pagina vuota, pagina orfana o citazione non fondata rilevato.",
  lintQuoteGroundingSection: "Citazioni non fondate (rilevate) [{count}]",
  lintQuoteGroundingItem: '- [[{page}]]{sourceHint}: "{quote}"',
  lintDeadLinkItem: "- [[{source}]] \u2192 **{target}** (la pagina non esiste){dupFlag}",
  lintDeadLinkMore: "- ... {count} altri collegamenti interrotti",
  lintEmptyPageItem: "- [[{page}]] \u2014 meno di 50 caratteri di contenuto sostanziale",
  lintOrphanItem: "- [[{page}]] \u2014 nessun'altra pagina Wiki collega qui{dupFlag}",
  lintDuplicateItem: "- [[{target}]] e [[{source}]] \u2014 {reason}",
  lintDeadLinkAffectedByDup: " (\u26A0\uFE0F coinvolge una pagina duplicata)",
  lintOrphanIsDuplicate: " (\u26A0\uFE0F pagina duplicata)",
  lintContradictionOpen: "Contraddizioni aperte: {count}",
  lintContradictionAutoFixed: "({count} corrette automaticamente in questa esecuzione)",
  lintContradictionItem: "- [{status}] [[{page}]] \u2014 {claim}",
  lintContradictionStatusDetected: "Rilevata",
  lintContradictionStatusPendingFix: "Correzione in attesa",
  lintLLMAnalysisHeading: "## Analisi LLM",
  // Prompt di analisi Lint
  lintAnalysisPrompt: 'Sei un assistente per la manutenzione della Wiki. Controlla la salute della Wiki in base alle seguenti informazioni.\n\nIndice Wiki:\n{index}\n\nCampione di contenuto delle pagine Wiki ({total} pagine totali, mostrate {sample} pagine):\n{contentSample}\n\nRisultati dei controlli programmatici (gi\xE0 verificati, non ripetere):\n{progReport}\n\nControlla i seguenti aspetti (salta collegamenti interrotti/vuoti/orfani gi\xE0 rilevati dai controlli programmatici):\n1. **Contraddizioni** \u2014 se pagine diverse si contraddicono sugli stessi fatti\n2. **Obsolescenza** \u2014 se qualche affermazione \xE8 chiaramente datata\n3. **Mancanti** \u2014 quali concetti importanti mancano di pagine autonome\n4. **Struttura** \u2014 se la struttura delle pagine \xE8 ragionevole e i riferimenti incrociati sono adeguati\n\nFormato di output: usa Markdown, iniziando con "## Analisi LLM". Ogni risultato su una riga "- [problema specifico]". Se non ci sono problemi, scrivi "Nessun problema evidente trovato."',
  // Avanzamento Correzioni Lint
  lintFixProgress: "Correzione {current}/{total}: [[{target}]]",
  lintFixDeadComplete: "Correzione collegamenti interrotti completata. Corretti {fixed}/{total} elementi.",
  lintFillProgress: "Espansione {current}/{total}: {page}",
  lintFillComplete: "Espansione delle pagine completata. Compilate {filled}/{total} pagine.",
  lintDeleteCompleted: "Eliminati {count} stub vuoti",
  lintDeleteFailed: "Eliminazione fallita di {failed}/{total} stub vuoti (vedi la console per i dettagli)",
  lintFillFailed: "Espansione fallita: {page} \u2014 {error}",
  lintLinkProgress: "Collegamento {current}/{total}: {page}",
  lintLinkComplete: "Collegamento delle orfane completato. Collegate {linked} pagine.",
  lintFixNoAction: "Nessuna azione intrapresa (nessun client)",
  lintFixIndexUpdated: "Indice e log della Wiki aggiornati.",
  lintFixAllComplete: "Tutte le correzioni completate. Vedi il log per i dettagli.",
  lintPollutedFixed: "Pagine inquinate corrette: {fixed}/{total}. Indice rigenerato.",
  regenerateIndexCompleted: "Indice rigenerato",
  operationFailed: "Fallito: ",
  // Modale Report Lint
  lintModalActionsTitle: "Suggerimenti di correzione (richiede token LLM):",
  lintLogReference: "Report completo salvato in log.md",
  lintModalFixDeadLinks: "Correggi collegamenti interrotti ({count})",
  lintModalExpandEmpty: "Espandi pagine vuote ({count})",
  lintModalDeleteEmpty: "Elimina stub vuoti ({count})",
  lintModalLinkOrphans: "Collega pagine orfane ({count})",
  lintModalAnalyzeSchema: "Analizza schema",
  lintModalMergeDuplicates: "Unisci duplicati ({count})",
  lintModalFixAll: "Correzione intelligente totale ({count} problemi)",
  lintMergeProgress: "Unione {current}/{total}: {source} \u2192 {target}",
  lintMergeComplete: "Unione duplicati completata. Unite {merged}/{total} coppie.",
  // Modale Report Acquisizione
  ingestReportTitle: "Report acquisizione",
  ingestReportSourceFile: "File sorgente",
  ingestReportCreated: "Create",
  ingestReportUpdated: "Aggiornate",
  ingestReportContradictions: "Contraddizioni trovate",
  ingestReportFailedTitle: "Acquisizione fallita",
  ingestReportErrorDetail: "Dettaglio errore",
  ingestReportClose: "Chiudi",
  ingestReportCreatedPages: "Pagine create: {count}",
  ingestReportUpdatedPages: "Pagine aggiornate: {count}",
  ingestReportEntitiesCount: "{count} entit\xE0",
  ingestReportConceptsCount: "{count} concetti",
  ingestReportContradictionsFound: "Contraddizioni trovate: {count}",
  ingestReportEntityType: "Entit\xE0",
  ingestReportConceptType: "Concetto",
  timeMinutes: "min",
  timeSeconds: "sec",
  // Avvisi Limite di Frequenza
  rateLimitDetected: "\u26A0\uFE0F Limite di frequenza rilevato: {count} pagina/e fallita/e con errori 429. Prova: (1) Abbassa la concorrenza a {suggestedConcurrency} o 1 (seriale), (2) Aumenta il ritardo batch a {suggestedDelay}ms, (3) Passa a un provider con limiti di frequenza pi\xF9 alti.",
  rateLimitDetectedShort: "\u26A0\uFE0F Limite di frequenza raggiunto \u2014 considera di abbassare la concorrenza o aumentare il ritardo batch in Impostazioni \u2192 Accelerazione acquisizione.",
  // Avviso sorgente lunga
  longSourceNotice: `\u{1F4C4} "{filename}" ha {lines} righe ({size}). I testi lunghi richiedono un'estrazione iterativa a batch \u2014 l'LLM legge l'intero documento in pi\xF9 passaggi. Questo potrebbe richiedere alcuni minuti. Abbi pazienza.`,
  longSourceNoticeShort: "\u{1F4C4} File di grandi dimensioni rilevato ({lines} righe). L'acquisizione potrebbe richiedere del tempo.",
  // Pannello Cronologia Acquisizioni (#122) — testi UI
  historyButton: "Cronologia operazioni",
  historyButtonDesc: "Visualizza acquisizioni recenti, report Lint e altre modifiche del tuo Wiki",
  historyButtonOpen: "Visualizza cronologia",
  historyModalTitle: "Cronologia operazioni",
  historyModalSubtitle: "Modifiche recenti del tuo LLM-Wiki",
  historyEmpty: "Nessuna operazione registrata. Acquisisci una nota o esegui Lint per popolare questo elenco.",
  historyReadError: "Impossibile leggere il registro operazioni: {error}",
  historyEntryKindIngest: "Acquisizione",
  historyEntryKindMaintenance: "Manutenzione",
  historyEntryKindFix: "Correzione",
  historyEntryKindOther: "Altro",
  historyEntryTime: "{date} \xB7 {time}",
  historyEntryTimeNoTime: "{date}",
  historyEntrySource: "Sorgente: {source}",
  historyEntrySourceUnknown: "Acquisizione senza titolo",
  historyEntryCreatedLabel: "Create",
  historyEntryUpdatedLabel: "Aggiornate",
  historyEntryCreatedCount: "{count} create",
  historyEntryUpdatedCount: "{count} aggiornate",
  historyEntryNoChanges: "Nessuna modifica alle pagine",
  historyEntryContradictions: "\u26A0\uFE0F {count} contraddizione/i",
  historyEntrySectionCreated: "Pagine create",
  historyEntrySectionUpdated: "Pagine aggiornate",
  historyEntrySectionContradictions: "Contraddizioni",
  historyEntrySectionDetails: "Dettagli",
  historyEntryOpenPage: "Apri",
  historyEntryDetailsNoContradictions: "Nessuna contraddizione trovata",
  historySearchPlaceholder: "\u{1F50D} Cerca per titolo sorgente o percorso pagina\u2026",
  historyFilterAll: "Tutte",
  historyFilterIngest: "Solo acquisizioni",
  historyFilterMaintenance: "Solo manutenzione",
  historyFilterFix: "Solo correzioni",
  historyFilterContradictions: "\u26A0\uFE0F Con contraddizioni",
  historyRefreshButton: "\u27F3 Aggiorna",
  historyExpandDay: "Clicca per espandere",
  historyCollapseDay: "Clicca per comprimere",
  historyShowMore: "Mostra {count} voci pi\xF9 vecchie",
  historyNoMatch: "Nessuna operazione corrisponde alla tua ricerca/filtro.",
  historyCloseButton: "Chiudi",
  historyLimit: 50,
  historyBadgeIngestShort: "\u{1F4E5}",
  historyBadgeMaintenanceShort: "\u{1F50D}",
  historyBadgeFixShort: "\u{1F527}",
  historyBadgeOtherShort: "\u{1F4CC}",
  historyEntrySectionReport: "Risultati del rapporto",
  historyKpiPages: "Pagine",
  historyKpiDeadLinks: "Link morti",
  historyKpiOrphans: "Orfane",
  historyKpiEmpty: "Vuote",
  historyKpiDuplicates: "Duplicate",
  historyKpiTagViolations: "Tag",
  historyKpiUnsourced: "Senza fonte",
  historyKpiDuration: "Durata Lint",
  historyKpiDurationSec: "{seconds}s",
  // Operation History v3 — rich section rendering
  historySectionDeadLinks: "Link morti ({count})",
  historySectionTagViolations: "Tag ({count})",
  historySectionOrphans: "Pagine orfane ({count})",
  historySectionEmptyPages: "Pagine vuote ({count})",
  historySectionLlmAnalysis: "Analisi LLM ({count})",
  historyDeadLinkSource: "Origine",
  historyDeadLinkTarget: "Mancante",
  historyOpenInLog: "Apri in log.md",
  historyShowMoreItems: "Mostra altri {count}",
  historyTrendUp: "\u2197 {delta}",
  historyTrendDown: "\u2198 {delta}",
  historyTrendSame: "\u2192 stabile",
  historyChipContradiction: "Contraddizione",
  historyChipOutdated: "Obsoleto",
  historyChipMissing: "Mancante",
  historyChipStructure: "Struttura",
  historySeverityHigh: "Alta",
  historySeverityMedium: "Media",
  historySeverityLow: "Bassa",
  historyPageTypeEntity: "\u{1F4E6} entity",
  historyPageTypeConcept: "\u{1F4A1} concept",
  historyPageTypeSource: "\u{1F4C4} source",
  historyGlobalInsight: "Il tuo Wiki ha {dead} link morto/i, {orphans} pagina/e orfana/e e {tags} problema/i di tag. Ultima Lint: {duration}.",
  historyGlobalInsightClean: "Il tuo Wiki \xE8 pulito \u2014 nessun link morto, pagina orfana o problema di tag.",
  historyGlobalInsightNoData: "Nessun rapporto di manutenzione \u2014 esegui Lint.",
  historyModalHeaderTitle: "Cronologia operazioni",
  historyModalSubtitleWithCount: "Modifiche recenti del tuo LLM-Wiki \xB7 {count} voci",
  historyTimeRangeAll: "Tutto il periodo",
  historyTimeRange1d: "Ultimo giorno",
  historyTimeRange3d: "Ultimi 3 giorni",
  historyTimeRange1w: "Ultima settimana",
  historyTimeRange1m: "Ultimo mese",
  historyIngestTotal: "Pagine totali",
  historyIngestByType: "Per tipo",
  historyIngestSource: "File sorgente",
  historyIngestNoTimestamp: "Nessun timestamp",
  historyIngestFirstTime: "Prima acquisizione",
  historyIngestLatestTime: "Ultima acquisizione",
  historyTimeRangeCustom: "Intervallo personalizzato",
  historyCustomRangeFrom: "Da",
  historyCustomRangeTo: "A",
  historyCustomRangeApply: "Applica",
  historyCustomRangeClear: "Pulisci"
};

// src/texts.ts
var TEXTS = {
  en: EN_TEXTS,
  zh: ZH_TEXTS,
  "zh-Hant": ZH_HANT_TEXTS,
  ja: JA_TEXTS,
  ko: KO_TEXTS,
  de: DE_TEXTS,
  fr: FR_TEXTS,
  es: ES_TEXTS,
  pt: PT_TEXTS,
  it: IT_TEXTS
};

// src/core/i18n.ts
function getText(language, key, replacements) {
  const texts = TEXTS[language] || TEXTS.en;
  let text = texts[key];
  if (!text) {
    text = TEXTS.en[key];
  }
  if (replacements) {
    for (const [k, v] of Object.entries(replacements)) {
      text = text.replace(`{${k}}`, v);
    }
  }
  return text;
}

// src/core/sse-parser.ts
function parseSSEEvents(responseText, format) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i;
  const normalized = responseText.replace(/\r\n/g, "\n");
  const events = normalized.split("\n\n");
  const deltas = [];
  for (const event of events) {
    if (!event.trim()) continue;
    const dataLine = event.split("\n").find((line) => line.startsWith("data:"));
    if (!dataLine) continue;
    if (format === "openai") {
      const dataContent = dataLine.substring(5).trim();
      if (dataContent === "[DONE]") break;
    }
    try {
      const jsonStart = dataLine.indexOf("{");
      if (jsonStart === -1) continue;
      const parsed = JSON.parse(dataLine.substring(jsonStart));
      if (format === "anthropic") {
        const anth = parsed;
        if (anth.type === "content_block_delta" && ((_a = anth.delta) == null ? void 0 : _a.type) === "text_delta" && anth.delta.text) {
          deltas.push({ text: anth.delta.text });
        }
      } else {
        const oai = parsed;
        const content = (_d = (_c = (_b = oai.choices) == null ? void 0 : _b[0]) == null ? void 0 : _c.delta) == null ? void 0 : _d.content;
        const reasoning = (_g = (_f = (_e = oai.choices) == null ? void 0 : _e[0]) == null ? void 0 : _f.delta) == null ? void 0 : _g.reasoning_content;
        const finish = (_i = (_h = oai.choices) == null ? void 0 : _h[0]) == null ? void 0 : _i.finish_reason;
        if (content || reasoning) {
          deltas.push({ text: content != null ? content : "", reasoning: reasoning != null ? reasoning : void 0 });
        }
        if (finish) deltas.push({ text: "", finishReason: finish });
      }
    } catch (e) {
    }
  }
  return deltas;
}

// src/core/truncation-retry.ts
var DEFAULT_MAX_CAP = MAX_TOKENS_BATCH;
async function withTruncationRetry(opts) {
  var _a, _b, _c;
  const initialResponse = await opts.initialFn();
  let text = opts.extractText(initialResponse);
  if (!opts.isTruncated(initialResponse)) {
    return text;
  }
  (_a = opts.onTruncatedResponse) == null ? void 0 : _a.call(opts, initialResponse);
  const cap = (_b = opts.maxCap) != null ? _b : DEFAULT_MAX_CAP;
  const currentMax = opts.getMaxTokens();
  const retryMaxTokens = Math.min(currentMax * 2, cap);
  const stopReason = (_c = opts.getStopReason) == null ? void 0 : _c.call(opts, initialResponse);
  const reasonSuffix = stopReason ? ` (${stopReason})` : "";
  console.warn(
    `${opts.label} response truncated at ${currentMax} tokens${reasonSuffix}. Retrying with ${retryMaxTokens} tokens.`
  );
  const retryResponse = await opts.retryFn(retryMaxTokens);
  text = opts.extractText(retryResponse);
  return text;
}

// src/core/markdown.ts
function cleanMarkdownResponse(response) {
  console.debug("cleanMarkdownResponse input length:", response.length);
  let cleaned = response.trim();
  cleaned = cleaned.replace(/<think\b[^>]*>[\s\S]*?<\/think>/gi, "");
  cleaned = cleaned.replace(/<thinking\b[^>]*>[\s\S]*?<\/thinking>/gi, "");
  if (cleaned.indexOf("\n---\n") === -1) {
    const headerMatch = cleaned.match(/\n#{1,2} \S/);
    if (headerMatch) {
      const cutIdx = cleaned.indexOf(headerMatch[0]);
      if (cutIdx > 0) {
        cleaned = cleaned.slice(cutIdx + 1).replace(/^\s+/, "");
      }
    }
  }
  const codeBlockPatterns = [
    /^```(?:markdown|md)?\s*\n([\s\S]*?)\n```$/gm,
    /^```(?:markdown|md)?\s*([\s\S]*?)```$/gm,
    /^```(?:markdown|md)?\s*\n([\s\S]*)$/gm,
    /^```(?:markdown|md)?\s*([\s\S]*)$/gm
  ];
  for (const pattern of codeBlockPatterns) {
    const match = cleaned.match(pattern);
    if (match) {
      cleaned = cleaned.replace(pattern, "$1").trim();
      console.debug("code block wrapping detected, removed");
      break;
    }
  }
  if (cleaned.startsWith("```")) {
    cleaned = cleaned.replace(/^```(?:markdown|md)?\s*\n?/, "");
    console.debug("removed opening code block marker");
  }
  if (cleaned.endsWith("```")) {
    cleaned = cleaned.replace(/\n?```$/, "");
    console.debug("removed closing code block marker");
  }
  console.debug("cleanMarkdownResponse output length:", cleaned.length);
  console.debug("first 50 chars:", cleaned.substring(0, 50));
  if (!cleaned.startsWith("---")) {
    const fmEnd = cleaned.indexOf("\n---\n");
    if (fmEnd !== -1) {
      const beforeFm = cleaned.substring(0, fmEnd);
      const looksLikeFrontmatter = beforeFm.includes(":") && !beforeFm.startsWith("#") && !beforeFm.startsWith("```") && beforeFm.split("\n").filter((l) => l.trim()).every((l) => l.includes(":") || l.trim() === "");
      if (looksLikeFrontmatter) {
        cleaned = "---\n" + cleaned;
        console.debug("added missing opening ---");
      } else {
        cleaned = cleaned.substring(fmEnd + 1);
        console.debug("removed preamble text before frontmatter");
      }
    }
  }
  return cleaned.trim();
}
function extractThinkingBlocks(content) {
  if (!content) {
    return { thinkingBlocks: [], visibleContent: "" };
  }
  const blockRegex = /<think(?:ing)?\b[^>]*>[\s\S]*?<\/think(?:ing)?>\s*/gi;
  const thinkingBlocks = [];
  let visibleContent = content.replace(blockRegex, (_match) => {
    return "";
  });
  const innerRegex = /<think(?:ing)?\b[^>]*>([\s\S]*?)<\/think(?:ing)?>/gi;
  let m;
  while ((m = innerRegex.exec(content)) !== null) {
    thinkingBlocks.push(unescapeThinkingTag(m[1].trim()));
  }
  return { thinkingBlocks, visibleContent: visibleContent.trimStart() };
}
function wrapReasoningContent(reasoning, text) {
  if (!reasoning) return text;
  const safeReasoning = reasoning.replace(/<\/think/gi, "<\\/think");
  return "<think>" + safeReasoning + "</think>\n\n" + text;
}
function unescapeThinkingTag(s) {
  return s.replace(/<\\\/think/gi, "</think");
}

// src/llm-client.ts
var fallbackNoticesThisRequest = [];
function flushFallbackNotices() {
  if (fallbackNoticesThisRequest.length === 0) return;
  const summary = fallbackNoticesThisRequest.map((n) => n.message).join(" / ");
  fallbackNoticesThisRequest.length = 0;
  try {
    new import_obsidian.Notice(summary, 5e3);
  } catch (e) {
  }
}
var RETRYABLE = /status 5\d{2}|status 429|overload|network|fetch|econnrefused|etimedout|timeout|abort/i;
var IS_400 = /status 400|HTTP 400|Bad Request/i;
var isThinkingControlError = (e) => {
  const msg = e instanceof Error ? e.message : "";
  if (!IS_400.test(msg)) return false;
  return /unknown field|unsupported field|invalid parameter|not supported|reasoning_effort|thinking/i.test(msg);
};
function errMsg(err) {
  return err instanceof Error ? err.message : String(err);
}
function parseUnknownFields(err) {
  var _a, _b, _c;
  const errWithBody = err;
  let candidateText = "";
  if (errWithBody.json && typeof errWithBody.json === "object") {
    const jsonObj = errWithBody.json;
    if ((_a = jsonObj.error) == null ? void 0 : _a.message) candidateText = jsonObj.error.message;
  }
  if (!candidateText) {
    candidateText = (_c = (_b = errWithBody.text) != null ? _b : errWithBody.message) != null ? _c : "";
  }
  if (!candidateText) return [];
  const fields = /* @__PURE__ */ new Set();
  for (const m of candidateText.matchAll(/Unknown name "([^"]+)"/g)) {
    fields.add(m[1]);
  }
  for (const m of candidateText.matchAll(/\b([A-Za-z_][A-Za-z0-9_]*)\s+must be in the range/g)) {
    fields.add(m[1]);
  }
  for (const m of candidateText.matchAll(/Unknown field:?\s*([A-Za-z_][A-Za-z0-9_]*)/g)) {
    fields.add(m[1]);
  }
  for (const m of candidateText.matchAll(/invalid parameter\s+['"]?([A-Za-z_][A-Za-z0-9_]*)/gi)) {
    fields.add(m[1]);
  }
  return [...fields];
}
function logFallback(category, detail) {
  console.debug(`[LLMClient Fallback] ${category}: ${detail}`);
}
async function withRetry(fn, maxAttempts = MAX_RETRIES, label = "API") {
  let lastError;
  for (let attempt = 0; attempt < maxAttempts; attempt++) {
    try {
      return await fn();
    } catch (error) {
      lastError = error;
      const msg = errMsg(error);
      if (RETRYABLE.test(msg) && attempt < maxAttempts - 1) {
        const delay = Math.pow(2, attempt) * RETRY_BASE_DELAY_MS + Math.random() * RETRY_BASE_DELAY_MS;
        console.warn(`${label} error on attempt ${attempt + 1}, retrying in ${Math.round(delay)}ms: ${msg}`);
        await new Promise((resolve) => window.setTimeout(resolve, delay));
        continue;
      }
      throw error;
    }
  }
  throw lastError;
}
var AnthropicCompatibleClient = class {
  constructor(apiKey, baseUrl) {
    // v1.20.1: Cache prefill-not-supported (#141, #147).
    this.prefillingNotSupported = false;
    this.apiKey = apiKey;
    this.baseUrl = baseUrl.replace(/\/v1\/?$/, "").replace(/\/+$/, "") + "/v1";
    this.apiVersion = "2023-06-01";
  }
  extractText(content) {
    const textBlock = content.find((c) => c.type === "text");
    return (textBlock == null ? void 0 : textBlock.text) || "";
  }
  async createMessage(params) {
    var _a, _b;
    const shouldPrefill = ((_a = params.response_format) == null ? void 0 : _a.type) === "json_object" && !this.prefillingNotSupported;
    const body = {
      model: params.model,
      max_tokens: params.max_tokens,
      messages: shouldPrefill ? [...params.messages, { role: "assistant", content: "{" }] : params.messages
    };
    if (params.system) body.system = params.system;
    if (params.temperature !== void 0) body.temperature = params.temperature;
    if (params.repetition_penalty !== void 0) body.repetition_penalty = params.repetition_penalty;
    if (params.chat_template_kwargs !== void 0) body.chat_template_kwargs = params.chat_template_kwargs;
    if (params.enableThinking === false) {
      body.thinking = { type: "disabled" };
    }
    const anthropicDoRequest = async (requestBody) => withRetry(async () => {
      var _a2, _b2, _c, _d, _e, _f;
      const response = await (0, import_obsidian.requestUrl)({
        url: this.baseUrl + "/messages",
        method: "POST",
        headers: {
          "x-api-key": this.apiKey,
          "Anthropic-Version": this.apiVersion,
          "Content-Type": "application/json"
        },
        body: JSON.stringify(requestBody)
      });
      if (response.status >= 400) {
        const errBody = (_b2 = (_a2 = response.text) == null ? void 0 : _a2.slice(0, 2e3)) != null ? _b2 : JSON.stringify(response).slice(0, 1e3);
        console.error(
          "[AnthropicCompat DEBUG] HTTP",
          response.status,
          "on",
          this.baseUrl + "/messages",
          "| model:",
          requestBody.model,
          "| system_len:",
          typeof requestBody.system === "string" ? requestBody.system.length : "n/a",
          "| messages:",
          Array.isArray(requestBody.messages) ? requestBody.messages.length : "n/a",
          "| last_msg_role:",
          Array.isArray(requestBody.messages) && requestBody.messages.length > 0 ? requestBody.messages[requestBody.messages.length - 1].role : "n/a",
          "| last_msg_content_prefix:",
          Array.isArray(requestBody.messages) && requestBody.messages.length > 0 ? String((_c = requestBody.messages[requestBody.messages.length - 1].content) != null ? _c : "").slice(0, 60) : "n/a",
          "| response:",
          errBody
        );
      }
      const data = response.json;
      if (data.error) throw new Error(`status ${response.status}: ${data.error.message}`);
      console.debug("Anthropic API response:", {
        stop_reason: data.stop_reason,
        content_length: ((_d = data.content) == null ? void 0 : _d.length) || 0,
        content_types: ((_e = data.content) == null ? void 0 : _e.map((c) => c.type)) || []
      });
      const initialData = data;
      const text = await withTruncationRetry({
        initialFn: async () => initialData,
        retryFn: async (retryTokens) => {
          const retryResponse = await (0, import_obsidian.requestUrl)({
            url: this.baseUrl + "/messages",
            method: "POST",
            headers: {
              "x-api-key": this.apiKey,
              "Anthropic-Version": this.apiVersion,
              "Content-Type": "application/json"
            },
            body: JSON.stringify({ ...body, max_tokens: retryTokens })
          });
          const retryData = retryResponse.json;
          if (retryData.error) throw new Error(`status ${retryResponse.status}: ${retryData.error.message}`);
          return retryData;
        },
        isTruncated: (r) => r.stop_reason === "max_tokens",
        extractText: (r) => this.extractText(r.content || []),
        getMaxTokens: () => params.max_tokens,
        getStopReason: (r) => r.stop_reason,
        maxCap: params.maxTokensPerCall || MAX_TOKENS_BATCH,
        label: "Anthropic-compatible API"
      });
      console.debug("Extracted text length:", text.length);
      if (((_f = params.response_format) == null ? void 0 : _f.type) === "json_object" && text.length > 0 && text[0] !== "{") {
        return "{" + text;
      }
      return text;
    }, 3, "Anthropic-compatible API");
    try {
      return await anthropicDoRequest(body);
    } catch (e) {
      const errMsg2 = e instanceof Error ? e.message : String(e);
      if (shouldPrefill && IS_400.test(errMsg2)) {
        this.prefillingNotSupported = true;
        console.debug("[AnthropicCompat] 400 with prefill \u2014 retrying without prefill");
        const noPrefillBody = {
          model: params.model,
          max_tokens: params.max_tokens,
          messages: [...params.messages]
        };
        if (params.system) noPrefillBody.system = params.system;
        if (params.temperature !== void 0) noPrefillBody.temperature = params.temperature;
        if (params.repetition_penalty !== void 0) noPrefillBody.repetition_penalty = params.repetition_penalty;
        if (params.chat_template_kwargs !== void 0) noPrefillBody.chat_template_kwargs = params.chat_template_kwargs;
        if (params.enableThinking === false) noPrefillBody.thinking = { type: "disabled" };
        return await anthropicDoRequest(noPrefillBody);
      }
      if (params.enableThinking === false && isThinkingControlError(e)) {
        this.thinkingControlSupported = false;
        console.debug(`[AnthropicCompat] thinking.type='disabled' not supported by ${this.baseUrl}, falling back`);
        const fallbackBody = {
          model: params.model,
          max_tokens: params.max_tokens,
          messages: [...params.messages]
        };
        if (params.system) fallbackBody.system = params.system;
        if (((_b = params.response_format) == null ? void 0 : _b.type) === "json_object") {
          fallbackBody.messages = [...fallbackBody.messages, { role: "assistant", content: "{" }];
        }
        return await anthropicDoRequest(fallbackBody);
      }
      throw e;
    }
  }
  async createMessageStream(params) {
    var _a;
    const messages = params.system ? params.messages : [
      ...params.messages,
      {
        role: "user",
        content: "Please respond in the same language as the user's question. If the user asks in Chinese, reply in Chinese. If the user asks in English, reply in English. Keep the response language consistent with the user's input language."
      }
    ];
    const body = {
      model: params.model,
      max_tokens: params.max_tokens,
      messages,
      stream: true
    };
    if (params.system) body.system = params.system;
    if (params.temperature !== void 0) body.temperature = params.temperature;
    if (params.repetition_penalty !== void 0) body.repetition_penalty = params.repetition_penalty;
    if (params.enableThinking === false) {
      body.thinking = { type: "disabled" };
    }
    console.debug(
      "[AnthropicCompat SSE] sending stream request, model:",
      params.model,
      "max_tokens:",
      params.max_tokens,
      "system length:",
      ((_a = params.system) == null ? void 0 : _a.length) || 0,
      "messages count:",
      messages.length
    );
    let response;
    try {
      response = await (0, import_obsidian.requestUrl)({
        url: this.baseUrl + "/messages",
        method: "POST",
        headers: {
          "x-api-key": this.apiKey,
          "Anthropic-Version": this.apiVersion,
          "Content-Type": "application/json"
        },
        body: JSON.stringify(body)
      });
    } catch (err) {
      console.error("[AnthropicCompat SSE] requestUrl request failed:", err);
      throw err;
    }
    const responseText = response.text;
    console.debug(
      "[AnthropicCompat SSE] response received, length:",
      responseText.length,
      "first 200 chars:",
      responseText.substring(0, 200)
    );
    const deltas = parseSSEEvents(responseText, "anthropic");
    let fullText = "";
    for (const delta of deltas) {
      if (delta.text) {
        fullText += delta.text;
        params.onChunk(delta.text);
      }
    }
    if (!fullText) {
      console.debug("[AnthropicCompat SSE] SSE parsing empty, trying non-streaming JSON fallback");
      try {
        const data = JSON.parse(responseText);
        if (data.error) throw new Error(data.error.message);
        fullText = this.extractText(data.content || []);
        if (fullText) {
          console.debug("[AnthropicCompat SSE] non-streaming fallback successful, length:", fullText.length);
          params.onChunk(fullText);
        }
      } catch (parseErr) {
        console.debug("[AnthropicCompat SSE] non-streaming JSON parse also failed:", parseErr);
      }
    }
    if (!fullText) {
      throw new Error(
        "Anthropic-compatible endpoint returned neither SSE events nor a standard JSON response. The provider may not support the Messages API streaming format. Response preview: " + responseText.substring(0, 300)
      );
    }
    console.debug("[AnthropicCompat SSE] success, response length:", fullText.length);
    return fullText;
  }
  listModels() {
    return Promise.resolve([]);
  }
};
var AnthropicClient = class {
  constructor(apiKey, baseUrl) {
    this.apiVersion = "2023-06-01";
    // v1.20.1: Cache prefill-not-supported per client instance.
    // Newer Claude models (Opus 4.8, 4.7, 4.6, Sonnet 4.6, Fable 5,
    // Mythos 5, Mythos Preview) reject assistant prefill messages.
    // Once detected, skip prefill on all subsequent requests to avoid
    // the 400 round-trip.
    this.prefillingNotSupported = false;
    this.apiKey = apiKey;
    const normalized = (baseUrl || "https://api.anthropic.com").replace(/\/v1\/?$/, "").replace(/\/+$/, "");
    this.baseUrl = normalized + "/v1";
    this.apiVersion = "2023-06-01";
  }
  async createMessage(params) {
    var _a;
    const shouldPrefill = ((_a = params.response_format) == null ? void 0 : _a.type) === "json_object" && !this.prefillingNotSupported;
    const messages = shouldPrefill ? [...params.messages, { role: "assistant", content: "{" }] : [...params.messages];
    if (params.cacheBreakpoint && params.cacheBreakpoint > 0 && messages.length > 0) {
      const first = messages[0];
      if (first.role === "user" && typeof first.content === "string" && params.cacheBreakpoint < first.content.length) {
        const text = first.content;
        first.content = [
          { type: "text", text: text.substring(0, params.cacheBreakpoint), cache_control: { type: "ephemeral" } },
          { type: "text", text: text.substring(params.cacheBreakpoint) }
        ];
      }
    }
    const body = {
      model: params.model,
      max_tokens: params.max_tokens,
      messages
    };
    if (params.system) body.system = params.system;
    if (params.temperature !== void 0) body.temperature = params.temperature;
    if (params.repetition_penalty !== void 0) body.repetition_penalty = params.repetition_penalty;
    if (params.chat_template_kwargs !== void 0) body.chat_template_kwargs = params.chat_template_kwargs;
    if (params.enableThinking === false) {
      body.thinking = { type: "disabled" };
    }
    const anthropicDoRequest = async (requestBody) => withRetry(async () => {
      var _a2, _b, _c, _d;
      const response = await (0, import_obsidian.requestUrl)({
        url: `${this.baseUrl}/messages`,
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "x-api-key": this.apiKey,
          "Anthropic-Version": this.apiVersion
        },
        body: JSON.stringify(requestBody)
      });
      if (response.status >= 400) {
        const errBody = (_b = (_a2 = response.text) == null ? void 0 : _a2.slice(0, 2e3)) != null ? _b : JSON.stringify(response).slice(0, 1e3);
        console.error(
          "[AnthropicClient DEBUG] HTTP",
          response.status,
          "on",
          this.baseUrl + "/messages",
          "| model:",
          requestBody.model,
          "| system_len:",
          typeof requestBody.system === "string" ? requestBody.system.length : "n/a",
          "| messages:",
          Array.isArray(requestBody.messages) ? requestBody.messages.length : "n/a",
          "| last_msg_role:",
          Array.isArray(requestBody.messages) && requestBody.messages.length > 0 ? requestBody.messages[requestBody.messages.length - 1].role : "n/a",
          "| last_msg_content_prefix:",
          Array.isArray(requestBody.messages) && requestBody.messages.length > 0 ? String((_c = requestBody.messages[requestBody.messages.length - 1].content) != null ? _c : "").slice(0, 60) : "n/a",
          "| response:",
          errBody
        );
      }
      const data = response.json;
      if (data.error) throw new Error(`status ${response.status}: ${data.error.message}`);
      const text = await withTruncationRetry({
        initialFn: async () => data,
        retryFn: async (retryTokens) => {
          const retryResponse = await (0, import_obsidian.requestUrl)({
            url: `${this.baseUrl}/messages`,
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              "x-api-key": this.apiKey,
              "Anthropic-Version": this.apiVersion
            },
            body: JSON.stringify({ ...requestBody, max_tokens: retryTokens })
          });
          const retryData = retryResponse.json;
          if (retryData.error) throw new Error(`status ${retryResponse.status}: ${retryData.error.message}`);
          return retryData;
        },
        isTruncated: (r) => r.stop_reason === "max_tokens",
        extractText: (r) => {
          var _a3;
          const block = (_a3 = r.content) == null ? void 0 : _a3.find((c) => c.type === "text");
          return block && "text" in block ? block.text || "" : "";
        },
        getMaxTokens: () => params.max_tokens,
        getStopReason: (r) => r.stop_reason,
        maxCap: params.maxTokensPerCall || MAX_TOKENS_BATCH,
        label: "Anthropic API"
      });
      if (((_d = params.response_format) == null ? void 0 : _d.type) === "json_object" && text.length > 0 && text[0] !== "{") {
        return "{" + text;
      }
      return text;
    }, 3, "Anthropic API");
    try {
      return await anthropicDoRequest(body);
    } catch (e) {
      const errMsg2 = e instanceof Error ? e.message : String(e);
      if (shouldPrefill && IS_400.test(errMsg2)) {
        this.prefillingNotSupported = true;
        console.debug("[AnthropicClient] 400 with prefill \u2014 retrying without prefill");
        const noPrefillBody = {
          model: params.model,
          max_tokens: params.max_tokens,
          messages: [...params.messages]
        };
        if (params.system) noPrefillBody.system = params.system;
        if (params.temperature !== void 0) noPrefillBody.temperature = params.temperature;
        if (params.repetition_penalty !== void 0) noPrefillBody.repetition_penalty = params.repetition_penalty;
        if (params.chat_template_kwargs !== void 0) noPrefillBody.chat_template_kwargs = params.chat_template_kwargs;
        if (params.enableThinking === false) noPrefillBody.thinking = { type: "disabled" };
        return await anthropicDoRequest(noPrefillBody);
      }
      if (params.enableThinking === false && isThinkingControlError(e)) {
        this.thinkingControlSupported = false;
        console.debug(`[AnthropicClient] thinking.type='disabled' not supported, falling back`);
        const fallbackBody = {
          model: params.model,
          max_tokens: params.max_tokens,
          messages: [...params.messages]
        };
        if (params.system) fallbackBody.system = params.system;
        if (params.temperature !== void 0) fallbackBody.temperature = params.temperature;
        if (params.repetition_penalty !== void 0) fallbackBody.repetition_penalty = params.repetition_penalty;
        return await anthropicDoRequest(fallbackBody);
      }
      throw e;
    }
  }
  async createMessageStream(params) {
    const messagesWithLanguageHint = params.system ? params.messages : [
      ...params.messages,
      {
        role: "user",
        content: "Please respond in the same language as the user's question. If the user asks in Chinese, reply in Chinese. If the user asks in English, reply in English. Keep the response language consistent with the user's input language."
      }
    ];
    const streamBody = {
      model: params.model,
      max_tokens: params.max_tokens,
      messages: messagesWithLanguageHint,
      stream: true
    };
    if (params.system) streamBody.system = params.system;
    if (params.temperature !== void 0) streamBody.temperature = params.temperature;
    if (params.repetition_penalty !== void 0) streamBody.repetition_penalty = params.repetition_penalty;
    if (params.enableThinking === false) {
      streamBody.thinking = { type: "disabled" };
    }
    const responseText = await (0, import_obsidian.requestUrl)({
      url: `${this.baseUrl}/messages`,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-api-key": this.apiKey,
        "Anthropic-Version": this.apiVersion
      },
      body: JSON.stringify(streamBody)
    }).then((r) => r.text);
    const deltas = parseSSEEvents(responseText, "anthropic");
    let fullResponse = "";
    for (const d of deltas) {
      if (d.text) {
        fullResponse += d.text;
        params.onChunk(d.text);
      }
    }
    return fullResponse;
  }
  async listModels() {
    var _a;
    const response = await (0, import_obsidian.requestUrl)({
      url: `${this.baseUrl}/models`,
      headers: {
        "x-api-key": this.apiKey,
        "Anthropic-Version": "2023-06-01"
      }
    });
    const data = response.json;
    if (!((_a = data.data) == null ? void 0 : _a.length)) return [];
    return data.data.map((m) => m.id).filter((id) => !id.includes(":") && !id.includes("/")).sort();
  }
};
var _OpenAICompatibleClient = class _OpenAICompatibleClient {
  constructor(apiKey, baseUrl) {
    // #137: cross-request cache of field names this baseUrl has rejected.
    // Populated lazily from 400 error bodies; pre-stripped on next request
    // so we don't pay the probe round-trip again. Replaces ad-hoc
    // chat_template_kwargs injection for the OpenAI dialect fallback.
    this.unsupportedFields = /* @__PURE__ */ new Set();
    // #137: language tag for localized fallback notices. Wired by
    // createLLMClient so that queueFallbackNotice can resolve templates via
    // getText() instead of hard-coding TEXTS.en (which would discard the
    // per-locale translations just added to the 8 text files).
    this.language = "en";
    this.apiKey = apiKey;
    this.baseUrl = baseUrl || "https://api.openai.com/v1";
  }
  getHeaders() {
    const headers = {
      "Content-Type": "application/json"
    };
    if (this.apiKey) {
      headers["Authorization"] = `Bearer ${this.apiKey}`;
    }
    return headers;
  }
  detectReasoningOnlyResponse(response) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const content = (_b = (_a = response.choices[0]) == null ? void 0 : _a.message) == null ? void 0 : _b.content;
    if (content && content.trim().length > 0) return;
    const finishReason = (_c = response.choices[0]) == null ? void 0 : _c.finish_reason;
    if (finishReason !== "length") return;
    const reasoningTokens = (_f = (_e = (_d = response.usage) == null ? void 0 : _d.completion_tokens_details) == null ? void 0 : _e.reasoning_tokens) != null ? _f : 0;
    const completionTokens = (_h = (_g = response.usage) == null ? void 0 : _g.completion_tokens) != null ? _h : 0;
    if (completionTokens > 0 && reasoningTokens / completionTokens >= 0.5) {
      throw new Error(
        'The model returned empty content after using most tokens for reasoning. This happens with some thinking-capable models when the reasoning toggle does not match the runtime. Try disabling thinking in Settings \u2192 LLM Wiki \u2192 LLM Configuration \u2192 Advanced \u2192 "Enable thinking" (turn off), or enable "Use chat-template kwarg to disable thinking" if the first toggle does not help.'
      );
    }
  }
  async createMessage(params) {
    const messages = params.system ? [{ role: "system", content: params.system }, ...params.messages] : params.messages;
    const body = this.buildRequestBody(params, messages, false);
    console.debug("[OPENAI-DEBUG] buildRequestBody result reasoning_effort:", body.reasoning_effort, "thinking:", body.thinking !== void 0, "dialect:", this.thinkingControlDialect, "params.enableThinking:", params.enableThinking);
    const doRequest = (bodyToUse) => withRetry(async () => {
      var _a, _b, _c, _d, _e, _f, _g;
      let response;
      try {
        response = await (0, import_obsidian.requestUrl)({
          url: this.baseUrl + "/chat/completions",
          method: "POST",
          headers: this.getHeaders(),
          body: JSON.stringify(bodyToUse)
        });
      } catch (err) {
        const status = err.status;
        const json = err.json;
        const text = err.text;
        const errMessage = (_a = err.message) != null ? _a : "";
        const is400 = status === 400 || IS_400.test(errMessage);
        if (is400) {
          try {
            const errObj = err;
            console.debug("[DEBUG-400] full error keys:", Object.keys(errObj));
            console.debug("[DEBUG-400] status:", status, "message:", errMessage);
            console.debug("[DEBUG-400] err.json:", json, "err.text:", text);
          } catch (e) {
          }
          try {
            void window.fetch(this.baseUrl + "/chat/completions", {
              method: "POST",
              headers: this.getHeaders(),
              body: JSON.stringify(bodyToUse)
            }).then(async (resp) => {
              const respBody = await resp.text();
              console.debug("[DEBUG-400] Gemini raw error response (re-fetch):", resp.status, respBody.substring(0, 2e3));
            }).catch((fetchErr) => {
              console.debug("[DEBUG-400] Re-fetch also failed:", String(fetchErr));
            });
          } catch (e) {
          }
          const unknown = parseUnknownFields(err);
          if (unknown.length > 0) {
            for (const f of unknown) {
              if (!_OpenAICompatibleClient.PROTECTED_FIELDS.has(f)) this.unsupportedFields.add(f);
            }
            logFallback("field-strip", `fields rejected by ${this.baseUrl}: ${unknown.join(", ")}`);
          }
          console.debug("[OpenAICompat Debug] 400 error body:", JSON.stringify(json) || text || "no body");
          console.debug("[OpenAICompat Debug] Request body size:", JSON.stringify(bodyToUse).length);
          console.debug("[OpenAICompat Debug] Model:", params.model, "| max_tokens:", params.max_tokens);
        }
        throw err;
      }
      const data = response.json;
      if (data.error) throw new Error(`status ${response.status}: ${data.error.message}`);
      const initialChoices = data.choices;
      const reasoning = ((_d = (_c = (_b = data.choices) == null ? void 0 : _b[0]) == null ? void 0 : _c.message) == null ? void 0 : _d.reasoning_content) || "";
      const content = ((_g = (_f = (_e = data.choices) == null ? void 0 : _e[0]) == null ? void 0 : _f.message) == null ? void 0 : _g.content) || "";
      const initialText = wrapReasoningContent(reasoning, content);
      return withTruncationRetry({
        initialFn: async () => {
          this.confirmDialectOnSuccess(bodyToUse);
          return { choices: initialChoices != null ? initialChoices : [], initialText, usage: data.usage };
        },
        retryFn: async (retryTokens) => {
          var _a2, _b2, _c2, _d2;
          const retryTokenKey = "max_completion_tokens" in bodyToUse ? "max_completion_tokens" : "max_tokens";
          const retryResponse = await (0, import_obsidian.requestUrl)({
            url: this.baseUrl + "/chat/completions",
            method: "POST",
            headers: this.getHeaders(),
            body: JSON.stringify({ ...bodyToUse, [retryTokenKey]: retryTokens })
          });
          const retryData = retryResponse.json;
          if (retryData.error) throw new Error(`status ${retryResponse.status}: ${retryData.error.message}`);
          return { choices: (_a2 = retryData.choices) != null ? _a2 : [], initialText, usage: retryData.usage, retryReasoning: ((_d2 = (_c2 = (_b2 = retryData.choices) == null ? void 0 : _b2[0]) == null ? void 0 : _c2.message) == null ? void 0 : _d2.reasoning_content) || "" };
        },
        isTruncated: (r) => {
          var _a2;
          return ((_a2 = r.choices[0]) == null ? void 0 : _a2.finish_reason) === "length";
        },
        extractText: (r) => {
          var _a2, _b2;
          const content2 = ((_b2 = (_a2 = r.choices[0]) == null ? void 0 : _a2.message) == null ? void 0 : _b2.content) || "";
          const retryReasoning = r.retryReasoning || "";
          return retryReasoning ? wrapReasoningContent(retryReasoning, content2) : content2 || r.initialText;
        },
        getMaxTokens: () => params.max_tokens,
        getStopReason: (r) => {
          var _a2;
          return (_a2 = r.choices[0]) == null ? void 0 : _a2.finish_reason;
        },
        maxCap: params.maxTokensPerCall || MAX_TOKENS_BATCH,
        label: "OpenAI-compatible API",
        onTruncatedResponse: (r) => this.detectReasoningOnlyResponse(r)
      });
    }, 3, "OpenAI-compatible API");
    try {
      return await doRequest(body);
    } catch (e) {
      if (params.enableThinking === false && isThinkingControlError(e)) {
        return await this.applyThinkingDialectFallback(body, params, messages, doRequest);
      }
      const stripped = this.retryBodyWithStrippedFields(body);
      if (stripped !== null) {
        logFallback("field-strip-retry", `retrying without: ${[...this.unsupportedFields].join(", ")}`);
        this.queueFallbackNotice("paramStripped", [...this.unsupportedFields].join(", "));
        flushFallbackNotices();
        return await doRequest(stripped);
      }
      throw e;
    }
  }
  // Issue #137: build the request body with dialect-aware thinking control
  // and pre-strip any fields this baseUrl has previously rejected.
  //
  //   dialect=anthropic → thinking.type='disabled'
  //   dialect=openai    → reasoning_effort='none'
  //   dialect=none      → no thinking control field
  //   dialect=undefined → assume anthropic (probe will correct on 400)
  //
  // Note: chat_template_kwargs is no longer injected here. v1.19.0 used it
  // as the fallback path, but #137 showed that Gemini rejects it as well.
  // Local backends that accept it (Ollama MLX) still receive it from the
  // wrapper (see llm-client-wrapper.ts injection).
  buildRequestBody(params, messages, streaming) {
    var _a;
    const isGpt5 = params.model === "gpt-5" || params.model.startsWith("gpt-5-");
    const tokenKey = isGpt5 ? "max_completion_tokens" : "max_tokens";
    const body = {
      model: params.model,
      [tokenKey]: params.max_tokens,
      messages
    };
    if (streaming) body.stream = true;
    if (params.temperature !== void 0 && !this.unsupportedFields.has("temperature")) {
      body.temperature = params.temperature;
    }
    if (params.repetition_penalty !== void 0 && !this.unsupportedFields.has("repetition_penalty")) {
      body.repetition_penalty = params.repetition_penalty;
    }
    if (params.chat_template_kwargs !== void 0 && !this.unsupportedFields.has("chat_template_kwargs")) {
      body.chat_template_kwargs = params.chat_template_kwargs;
    }
    if (params.enableThinking === false) {
      const dialect = (_a = this.thinkingControlDialect) != null ? _a : "anthropic";
      if (dialect === "anthropic") {
        body.thinking = { type: "disabled" };
      } else if (dialect === "openai") {
        body.reasoning_effort = "none";
      }
    }
    return body;
  }
  // #137: invoked by doRequest on successful response when thinking control
  // was sent. Confirms the dialect cache so subsequent calls skip the probe.
  confirmDialectOnSuccess(body) {
    if (body.thinking !== void 0 && this.thinkingControlDialect !== "anthropic") {
      this.thinkingControlDialect = "anthropic";
    } else if (body.reasoning_effort !== void 0 && this.thinkingControlDialect !== "openai") {
      this.thinkingControlDialect = "openai";
    }
  }
  // #137: shared field-strip retry helper. Returns a new body with
  // unsupportedFields removed, or null if nothing was actually stripped
  // (so the caller knows to fall through to the original error path).
  // Used by both the non-stream and stream 400 catch blocks.
  retryBodyWithStrippedFields(body) {
    if (this.unsupportedFields.size === 0) return null;
    const retryBody = { ...body };
    let changed = false;
    for (const f of this.unsupportedFields) {
      if (f in retryBody) {
        delete retryBody[f];
        changed = true;
      }
    }
    return changed ? retryBody : null;
  }
  // Issue #137: 3-tier dialect fallback chain.
  //   Tier 1: anthropic → openai (thinking.type → reasoning_effort)
  //   Tier 2: openai → none (drop thinking field entirely)
  //
  // Each tier reuses buildRequestBody with a forced dialect override so
  // the retry inherits the unsupportedFields pre-strip (a previous Tier-1
  // failure may have populated unsupportedFields via the catch in
  // doRequest). Building the retry body by hand would skip that strip
  // and risk re-sending fields the backend just rejected.
  async applyThinkingDialectFallback(body, params, messages, doRequest) {
    const sentThinking = body.thinking !== void 0;
    const sentReasoning = body.reasoning_effort !== void 0;
    const startedAtDialect = this.thinkingControlDialect;
    const reachedTier2 = sentThinking && (startedAtDialect === void 0 || startedAtDialect === "anthropic");
    if (reachedTier2) {
      this.thinkingControlDialect = "openai";
      logFallback("thinking-dialect", `anthropic \u2192 openai (reasoning_effort) for ${this.baseUrl}`);
      this.queueFallbackNotice("thinkingDialect", "openai (reasoning_effort)");
      try {
        return await doRequest(this.buildRequestBody(params, messages, false));
      } catch (e2) {
        if (!isThinkingControlError(e2)) throw e2;
      }
    }
    if (reachedTier2 || sentReasoning) {
      this.thinkingControlDialect = "none";
      logFallback("thinking-dialect", `openai \u2192 none (no thinking control) for ${this.baseUrl}`);
      this.queueFallbackNotice("thinkingNone");
      return await doRequest(this.buildRequestBody(params, messages, false));
    }
    throw new Error(
      `Thinking control rejected by ${this.baseUrl} for all known dialects (anthropic, openai, none). The provider may not support disabling thinking. Try a different model or disable "Enable thinking" in Advanced settings.`
    );
  }
  // #137: queue a localized fallback notice for the next flushFallbackNotices() call.
  queueFallbackNotice(kind, detail) {
    let msg = "";
    if (kind === "thinkingDialect") {
      msg = getText(this.language, "fallbackThinkingDialect").replace("{dialect}", detail != null ? detail : "openai");
    } else if (kind === "thinkingNone") {
      msg = getText(this.language, "fallbackThinkingNone");
    } else if (kind === "paramStripped") {
      msg = getText(this.language, "fallbackParamStripped").replace("{field}", detail != null ? detail : "");
    }
    if (msg) fallbackNoticesThisRequest.push({ level: "warn", message: msg });
  }
  async createMessageStream(params) {
    const messages = params.system ? [{ role: "system", content: params.system }, ...params.messages] : [
      ...params.messages,
      {
        role: "user",
        content: "Please respond in the same language as the user's question. If the user asks in Chinese, reply in Chinese. If the user asks in English, reply in English. Keep the response language consistent with the user's input language."
      }
    ];
    const body = this.buildRequestBody(
      params,
      messages,
      /* streaming */
      true
    );
    const doRequest = (bodyToUse) => withRetry(async () => {
      var _a, _b, _c, _d, _e, _f, _g;
      let response;
      try {
        response = await (0, import_obsidian.requestUrl)({
          url: this.baseUrl + "/chat/completions",
          method: "POST",
          headers: this.getHeaders(),
          body: JSON.stringify(bodyToUse)
        });
      } catch (err) {
        const status = err.status;
        const errMessage = (_a = err.message) != null ? _a : "";
        const is400 = status === 400 || IS_400.test(errMessage);
        if (is400) {
          const unknown = parseUnknownFields(err);
          if (unknown.length > 0) {
            for (const f of unknown) {
              if (!_OpenAICompatibleClient.PROTECTED_FIELDS.has(f)) this.unsupportedFields.add(f);
            }
            logFallback("field-strip", `fields rejected by ${this.baseUrl}: ${unknown.join(", ")}`);
          }
        }
        throw err;
      }
      const responseText = response.text;
      const deltas = parseSSEEvents(responseText, "openai");
      let fullText = "";
      let reasoningContent = "";
      for (const delta of deltas) {
        if (delta.reasoning) {
          reasoningContent += delta.reasoning;
        }
        if (delta.text) {
          fullText += delta.text;
          params.onChunk(delta.text);
        }
      }
      if (reasoningContent) {
        fullText = wrapReasoningContent(reasoningContent, fullText);
      }
      if (!fullText) {
        console.debug("[OpenAICompat SSE] SSE parsing empty, trying non-streaming JSON fallback");
        try {
          const data = JSON.parse(responseText);
          if (data.error) throw new Error(data.error.message);
          const text = ((_d = (_c = (_b = data.choices) == null ? void 0 : _b[0]) == null ? void 0 : _c.message) == null ? void 0 : _d.content) || "";
          const reasoning = ((_g = (_f = (_e = data.choices) == null ? void 0 : _e[0]) == null ? void 0 : _f.message) == null ? void 0 : _g.reasoning_content) || "";
          if (text || reasoning) {
            console.debug("[OpenAICompat SSE] Non-streaming fallback successful, length:", text.length);
            fullText = wrapReasoningContent(reasoning, text);
            if (text) params.onChunk(text);
          }
        } catch (parseErr) {
          console.debug("[OpenAICompat SSE] Non-streaming JSON parse also failed:", parseErr);
        }
      }
      if (!fullText) {
        throw new Error(
          "OpenAI-compatible endpoint returned neither SSE events nor a standard JSON response. The provider may not support streaming. Response preview: " + responseText.substring(0, 300)
        );
      }
      return fullText;
    }, 3, "OpenAI-compatible stream");
    try {
      return await doRequest(body);
    } catch (e) {
      if (params.enableThinking === false && isThinkingControlError(e)) {
        return await this.applyThinkingDialectFallback(body, params, messages, doRequest);
      }
      if (e.message && IS_400.test(e.message)) {
        const stripped = this.retryBodyWithStrippedFields(body);
        if (stripped !== null) {
          logFallback("field-strip-retry", `stream retrying without: ${[...this.unsupportedFields].join(", ")}`);
          this.queueFallbackNotice("paramStripped", [...this.unsupportedFields].join(", "));
          flushFallbackNotices();
          return await doRequest(stripped);
        }
      }
      throw e;
    }
  }
  async listModels() {
    try {
      const response = await (0, import_obsidian.requestUrl)({
        url: this.baseUrl + "/models",
        method: "GET",
        headers: this.getHeaders()
      });
      const data = response.json;
      if (data.error) throw new Error(`status ${response.status}: ${data.error.message}`);
      const modelIds = (data.data || []).map((m) => m.id).filter((id) => !id.includes(":") && !id.includes("/")).sort();
      return modelIds.slice(0, 100);
    } catch (error) {
      console.error("Failed to fetch model list:", error);
      return [];
    }
  }
};
// v1.20.0: fields that must never be stripped even if a 400 error
// mentions them (e.g., "Unknown name 'model'"). Stripping these
// would break all subsequent requests.
_OpenAICompatibleClient.PROTECTED_FIELDS = /* @__PURE__ */ new Set(["model", "messages", "stream"]);
var OpenAICompatibleClient = _OpenAICompatibleClient;

// src/core/token-cap.ts
function capMaxTokens(requested, settings) {
  var _a;
  const ceiling = (_a = settings.maxTokensPerCall) != null ? _a : 0;
  if (ceiling <= 0) return requested;
  return Math.min(requested, ceiling);
}

// src/llm-client-wrapper.ts
function wrapWithAdvancedSettings(client, settings) {
  const capTokens = settings.maxTokensPerCall > 0;
  const originalCreate = client.createMessage.bind(client);
  client.createMessage = async (params) => {
    return originalCreate({
      ...params,
      ...capTokens ? { max_tokens: capMaxTokens(params.max_tokens, { maxTokensPerCall: settings.maxTokensPerCall }), maxTokensPerCall: settings.maxTokensPerCall } : {},
      ...params.temperature === void 0 && settings.extractionTemperature !== void 0 ? { temperature: settings.extractionTemperature } : {},
      ...params.repetition_penalty === void 0 && settings.repetitionPenalty !== void 0 ? { repetition_penalty: settings.repetitionPenalty } : {}
    });
  };
  return client;
}

// src/schema/analyze.ts
var import_obsidian2 = require("obsidian");
async function runSchemaAnalyze(ctx) {
  var _a;
  if (!ctx.requireLLMReady()) return;
  if (!ctx.llmClient) {
    new import_obsidian2.Notice(TEXTS[ctx.settings.language].errorNoApiKey);
    return;
  }
  const signal = ctx.wikiEngine.startLintOperation();
  const thinkingNotice = new import_obsidian2.Notice(TEXTS[ctx.settings.language].analyzingSchema, 0);
  try {
    const result = await ctx.schemaManager.suggestSchemaUpdate((_a = ctx.lintAnalysisContext) != null ? _a : "Wiki lint analysis");
    thinkingNotice.hide();
    if (signal.aborted) return;
    if (result && ctx.openSchemaDiffModal) {
      await ctx.openSchemaDiffModal(result);
      return;
    }
    if (result == null ? void 0 : result.changes_needed) {
      new import_obsidian2.Notice(TEXTS[ctx.settings.language].schemaSuggestionGenerated, NOTICE_ERROR);
    } else if (result) {
      new import_obsidian2.Notice(TEXTS[ctx.settings.language].noSchemaUpdateNeeded, NOTICE_NORMAL);
    }
  } catch (error) {
    thinkingNotice.hide();
    console.error("Schema suggestion failed:", error);
    if (signal.aborted) return;
    const errMsg2 = error instanceof Error ? error.message : String(error);
    new import_obsidian2.Notice(
      TEXTS[ctx.settings.language].schemaSuggestionFailed + ": " + errMsg2,
      NOTICE_ERROR
    );
  } finally {
    ctx.wikiEngine.endLintOperation();
  }
}

// src/core/slug.ts
function slugify(text, preserveCase = false) {
  console.debug("slugify input:", text, "length:", text == null ? void 0 : text.length);
  if (!text || text.trim().length === 0) {
    console.warn("slugify: input text is empty");
    return "untitled";
  }
  return computeSlug(text, preserveCase);
}
function computeSlug(text, preserveCase = false) {
  if (!text || text.trim().length === 0) return "untitled";
  const trimmed = text.trim();
  const afterRemoveInvalid = trimmed.replace(/[\x00-\x1f]/g, "").replace(/[/\\:*?"<>|,()'!?、，。；：！？（）【】《》]/g, "");
  if (afterRemoveInvalid.length === 0) return "untitled-" + Date.now();
  const afterSpaceToDash = afterRemoveInvalid.replace(/[\s.]+/g, "-");
  const afterMergeDash = afterSpaceToDash.replace(/-+/g, "-");
  const finalSlug = afterMergeDash.replace(/^-|-$/g, "").trim();
  if (finalSlug.length === 0) return "untitled-" + Date.now();
  return preserveCase ? finalSlug : finalSlug.toLowerCase();
}
function filterRedundantAliases(pagePath, candidateAliases) {
  const fileName = pagePath.split("/").pop() || "";
  const fileKey = fileName.replace(/\.md$/i, "").trim().toLowerCase();
  const seen = /* @__PURE__ */ new Set();
  return candidateAliases.filter((alias) => {
    if (!alias || alias.trim().length === 0) return false;
    const key = alias.trim().toLowerCase();
    if (key === fileKey) return false;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}

// src/core/tag-vocab.ts
function getActiveEntityTags(settings) {
  var _a;
  const custom = ((_a = settings.customEntityTags) != null ? _a : "").trim();
  if (settings.tagVocabularyMode === "custom" && custom.length > 0) {
    const userTags = custom.split(",").map((t) => t.trim()).filter((t) => t.length > 0);
    return Array.from(new Set(userTags));
  }
  return [...VALID_ENTITY_TAGS];
}
function getActiveConceptTags(settings) {
  var _a;
  const custom = ((_a = settings.customConceptTags) != null ? _a : "").trim();
  if (settings.tagVocabularyMode === "custom" && custom.length > 0) {
    const userTags = custom.split(",").map((t) => t.trim()).filter((t) => t.length > 0);
    return Array.from(new Set(userTags));
  }
  return [...VALID_CONCEPT_TAGS];
}
function getActiveSourceTags(settings) {
  return [...VALID_SOURCE_TAGS];
}
function normalizeVocabularyCsv(csv) {
  if (!csv) return "";
  const seen = /* @__PURE__ */ new Set();
  const result = [];
  for (const raw of csv.split(",")) {
    const trimmed = raw.trim();
    if (!trimmed) continue;
    const key = trimmed.toLowerCase();
    if (seen.has(key)) continue;
    seen.add(key);
    result.push(trimmed);
  }
  return result.join(", ");
}

// src/core/frontmatter.ts
function parseFrontmatter(content) {
  var _a;
  const match = content.match(/^---\n([\s\S]*?)\n---/);
  if (!match) return null;
  const result = {};
  const fmText = match[1];
  const lines = fmText.split("\n");
  let currentKey = null;
  let arrayValues = [];
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const trimmed = line.trim();
    if (!trimmed) {
      if (currentKey && arrayValues.length > 0) {
        result[currentKey] = arrayValues;
        arrayValues = [];
        currentKey = null;
      }
      continue;
    }
    if (trimmed.startsWith("- ") && currentKey) {
      const value2 = trimmed.substring(2).trim();
      arrayValues.push(value2.replace(/^["']|["']$/g, ""));
      continue;
    }
    if (currentKey && arrayValues.length > 0 && !trimmed.startsWith("- ")) {
      result[currentKey] = arrayValues;
      arrayValues = [];
      currentKey = null;
    }
    const colonIdx = line.indexOf(":");
    if (colonIdx === -1) continue;
    const key = line.substring(0, colonIdx).trim();
    const value = line.substring(colonIdx + 1).trim();
    const nextLine = (_a = lines[i + 1]) == null ? void 0 : _a.trim();
    if (nextLine && nextLine.startsWith("- ")) {
      currentKey = key;
      arrayValues = [];
      continue;
    }
    if (key === "reviewed") {
      result.reviewed = value === "true";
    } else if (key === "type") {
      result.type = value;
    } else if (key === "created") {
      result.created = value;
    } else if (key === "updated") {
      result.updated = value;
    } else if (value.startsWith("[") && value.endsWith("]")) {
      try {
        result[key] = value.slice(1, -1).split(",").map((v) => v.trim().replace(/^["']|["']$/g, "")).filter((v) => v);
      } catch (e) {
        result[key] = value;
      }
    } else {
      result[key] = value.replace(/^["']|["']$/g, "");
    }
  }
  if (currentKey && arrayValues.length > 0) {
    result[currentKey] = arrayValues;
  }
  const ARRAY_FIELDS = ["aliases", "sources", "tags"];
  for (const field of ARRAY_FIELDS) {
    const val = result[field];
    if (typeof val === "string") {
      result[field] = [val];
    } else if (!Array.isArray(val)) {
      delete result[field];
    }
  }
  return result;
}
function yamlStringify(value) {
  if (Array.isArray(value)) {
    if (value.length === 0) return "[]";
    return "\n" + value.map((v) => `  - "${v}"`).join("\n");
  }
  if (typeof value === "string") {
    if (/[":[\]{}\n]/.test(value)) {
      return `"${value.replace(/"/g, '\\"')}"`;
    }
    return value;
  }
  if (typeof value === "boolean") {
    return String(value);
  }
  if (typeof value === "number") {
    return String(value);
  }
  if (value === null || value === void 0) {
    return "";
  }
  return "";
}
function extractBody(content) {
  if (!content.startsWith("---")) return content;
  const endIdx = content.indexOf("\n---", 3);
  if (endIdx === -1) return content;
  return content.substring(endIdx + 4).trim();
}
function isBlankSource(content) {
  return extractBody(content).trim().length === 0;
}
function upsertFrontmatterField(content, key, value) {
  const line = `${key}: ${value}`;
  const escapedKey = key.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const keyRe = new RegExp(`^${escapedKey}:.*$`, "m");
  if (content.startsWith("---")) {
    const endIdx = content.indexOf("\n---", 3);
    if (endIdx !== -1) {
      const fmBlock = content.substring(0, endIdx);
      const rest = content.substring(endIdx);
      if (keyRe.test(fmBlock)) {
        return fmBlock.replace(keyRe, line) + rest;
      }
      return `${fmBlock}
${line}${rest}`;
    }
  }
  return `---
${line}
---

${content}`;
}
function mergeFrontmatter(existingContent, newSourcePath) {
  const fm = parseFrontmatter(existingContent);
  const body = extractBody(existingContent);
  if (!fm) {
    return {
      frontmatter: "",
      body: existingContent,
      wasMerged: false
    };
  }
  const normalizeSourcePath2 = (s) => {
    const trimmed = s.trim();
    if (trimmed.startsWith("[[") && trimmed.endsWith("]]")) {
      return trimmed.slice(2, -2).trim();
    }
    return trimmed;
  };
  const existingSources = Array.isArray(fm.sources) ? fm.sources : [];
  const sourceSet = /* @__PURE__ */ new Set();
  for (const s of existingSources) {
    sourceSet.add(normalizeSourcePath2(String(s)));
  }
  sourceSet.add(newSourcePath);
  const mergedSources = Array.from(sourceSet).map((s) => `[[${s}]]`);
  const created = fm.created || (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  const updated = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  const lines = ["---"];
  if (fm.type) lines.push(`type: ${fm.type}`);
  lines.push(`created: ${created}`);
  lines.push(`updated: ${updated}`);
  if (mergedSources.length > 0) {
    lines.push(`sources:${yamlStringify(mergedSources)}`);
  }
  if (Array.isArray(fm.tags) && fm.tags.length > 0) {
    lines.push(`tags:${yamlStringify(fm.tags)}`);
  } else {
    lines.push("tags:");
  }
  if (fm.reviewed) {
    lines.push("reviewed: true");
  }
  if (Array.isArray(fm.aliases) && fm.aliases.length > 0) {
    const dedupedAliases = fm.aliases.filter((v, i, a) => a.indexOf(v) === i && v);
    if (dedupedAliases.length > 0) {
      lines.push(`aliases:${yamlStringify(dedupedAliases)}`);
    }
  }
  lines.push("---");
  return {
    frontmatter: lines.join("\n"),
    body,
    wasMerged: true
  };
}
function enforceFrontmatterConstraints(content, pageType, settings) {
  if (!content.startsWith("---")) return content;
  const fmEnd = content.indexOf("\n---\n", 3);
  if (fmEnd === -1) return content;
  const fmText = content.substring(3, fmEnd);
  if (/^reviewed:\s*true\s*$/m.test(fmText)) {
    const today2 = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
    return content.replace(/^created:\s*\d{4}-\d{2}-\d{2}\s*$/m, `created: ${today2}`).replace(/^updated:\s*\d{4}-\d{2}-\d{2}\s*$/m, `updated: ${today2}`);
  }
  let body = content.substring(fmEnd + 5);
  const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  const lines = fmText.split("\n");
  const newLines = [];
  let typeLine = "";
  let collectedTags = [];
  let foundType = false;
  let foundTags = false;
  let foundAliases = false;
  let collectedAliases = [];
  let createdValue = "";
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i].trim();
    if (!line) continue;
    if (line.startsWith("created:")) {
      createdValue = line.substring(8).trim();
      continue;
    }
    if (line.startsWith("updated:")) {
      continue;
    }
    if (line.startsWith("type:")) {
      foundType = true;
      const currentType = line.substring(5).trim();
      typeLine = `type: ${pageType}`;
      if ((pageType === "entity" || pageType === "concept") && currentType && currentType !== "entity" && currentType !== "concept" && currentType !== pageType) {
        collectedTags.push(currentType);
      }
    } else if (line.startsWith("tags:")) {
      foundTags = true;
      const tagsValue = line.substring(5).trim();
      if (tagsValue.startsWith("[") && tagsValue.endsWith("]")) {
        const inner = tagsValue.slice(1, -1).trim();
        if (inner) {
          collectedTags.push(...inner.split(",").map((t) => t.trim().replace(/^["']|["']$/g, "")));
        }
      }
      let j = i + 1;
      while (j < lines.length && lines[j].trim().startsWith("- ")) {
        const tagVal = lines[j].trim().substring(2).trim().replace(/^["']|["']$/g, "");
        if (tagVal) collectedTags.push(tagVal);
        j++;
      }
      if (j > i + 1) i = j - 1;
    } else if (line.startsWith("aliases:")) {
      foundAliases = true;
      const aliasesValue = line.substring(8).trim();
      if (aliasesValue.startsWith("[") && aliasesValue.endsWith("]")) {
        const inner = aliasesValue.slice(1, -1).trim();
        if (inner) {
          collectedAliases.push(...inner.split(",").map((t) => t.trim().replace(/^["']|["']$/g, "")));
        }
      }
      let j = i + 1;
      while (j < lines.length && lines[j].trim().startsWith("- ")) {
        const aliasVal = lines[j].trim().substring(2).trim().replace(/^["']|["']$/g, "");
        if (aliasVal) collectedAliases.push(aliasVal);
        j++;
      }
      if (j > i + 1) i = j - 1;
    } else if (!line.startsWith("- ")) {
      newLines.push(line);
    }
  }
  const result = ["---"];
  if (foundType) {
    result.push(typeLine);
  }
  result.push(`created: ${createdValue || today}`);
  result.push(`updated: ${today}`);
  for (const line of newLines) {
    if (!line.startsWith("type:") && !line.startsWith("tags:") && !line.startsWith("aliases:") && !line.startsWith("created:") && !line.startsWith("updated:")) {
      result.push(line);
    }
  }
  if (foundTags || collectedTags.length > 0) {
    const validSubtypes = pageType === "entity" ? settings ? getActiveEntityTags(settings) : VALID_ENTITY_TAGS : pageType === "concept" ? settings ? getActiveConceptTags(settings) : VALID_CONCEPT_TAGS : pageType === "source" ? settings ? getActiveSourceTags(settings) : VALID_SOURCE_TAGS : [];
    const dedupedTags = [];
    const outOfVocab = [];
    for (const tag of collectedTags) {
      if (!tag || tag === pageType) continue;
      if (dedupedTags.includes(tag)) continue;
      dedupedTags.push(tag);
      if (!validSubtypes.includes(tag)) outOfVocab.push(tag);
    }
    if (outOfVocab.length > 0) {
      console.debug(
        `[enforceFrontmatterConstraints] ${pageType} page retained ${outOfVocab.length} tag(s) not in active vocabulary (${validSubtypes.length} entries):`,
        outOfVocab
      );
    }
    if (dedupedTags.length > 0) {
      result.push(`tags: [${dedupedTags.join(", ")}]`);
    } else {
      result.push("tags:");
    }
  }
  if (foundAliases || collectedAliases.length > 0) {
    const validAliases = collectedAliases.filter((v, i, a) => a.indexOf(v) === i && v);
    if (validAliases.length > 0) {
      result.push(`aliases:${yamlStringify(validAliases)}`);
    }
  }
  result.push("---");
  return result.join("\n") + "\n\n" + body;
}

// src/core/settings-migrations.ts
function applySettingsMigrations(savedData) {
  const applied = [];
  const settings = Object.assign({}, DEFAULT_SETTINGS, savedData || {});
  if (savedData && savedData.disableThinking === true && !savedData._migrated_v1_20_0_thinking) {
    settings.disableThinking = false;
    settings.advancedSettingsMode = "default";
    settings._migrated_v1_20_0_thinking = true;
    applied.push("v1.20.0-thinking");
  }
  return { settings, applied };
}

// src/core/status-bar.ts
function buildIngestStatusBarText(label, filename, batch) {
  const name = (filename == null ? void 0 : filename.trim()) || "";
  const prefix = batch ? `[${batch.current}/${batch.total}] ` : "";
  const body = name ? `${name} \xB7 ${label}` : label;
  return `${prefix}${body}`;
}

// src/ui/settings.ts
var import_obsidian5 = require("obsidian");

// src/ui/modals.ts
var import_obsidian3 = require("obsidian");
var FileSuggestModal = class extends import_obsidian3.FuzzySuggestModal {
  constructor(app, wikiFolder, onSelect) {
    super(app);
    this.wikiFolder = wikiFolder;
    this.onSelect = onSelect;
  }
  getItems() {
    return this.app.vault.getMarkdownFiles().filter((f) => !f.path.startsWith(this.wikiFolder) && !f.path.startsWith(this.app.vault.configDir));
  }
  getItemText(file) {
    return file.path;
  }
  onChooseItem(file) {
    this.onSelect(file);
  }
};
var FolderSuggestModal = class extends import_obsidian3.FuzzySuggestModal {
  constructor(app, wikiFolder, onSelect) {
    super(app);
    this.wikiFolder = wikiFolder;
    this.onSelect = onSelect;
  }
  getItems() {
    const folders = [];
    const root = this.app.vault.getRoot();
    const collect = (folder) => {
      if (!folder.path.startsWith(this.app.vault.configDir) && !folder.path.startsWith(this.wikiFolder)) {
        folders.push(folder);
      }
      for (const child of folder.children) {
        if (child instanceof import_obsidian3.TFolder) {
          collect(child);
        }
      }
    };
    collect(root);
    return folders;
  }
  getItemText(folder) {
    return folder.path;
  }
  onChooseItem(folder) {
    this.onSelect(folder);
  }
};
var LintReportModal = class extends import_obsidian3.Modal {
  constructor(app, report, fixCallbacks, counts, language = "en") {
    super(app);
    this.renderComponent = null;
    this.report = report;
    this.fixCallbacks = fixCallbacks;
    this.counts = counts;
    this.language = language;
  }
  onOpen() {
    const { contentEl } = this;
    this.renderComponent = new import_obsidian3.Component();
    this.renderComponent.load();
    const t = TEXTS[this.language] || TEXTS.en;
    const reportDiv = contentEl.createDiv({
      attr: { style: "max-height: 50vh; overflow-y: auto; padding: 8px 0;" }
    });
    void import_obsidian3.MarkdownRenderer.render(this.app, this.report, reportDiv, "", this.renderComponent);
    if (t.lintLogReference) {
      contentEl.createEl("p", {
        text: `\u{1F4CB} ${t.lintLogReference}`,
        attr: { style: "font-size: 0.85em; color: var(--text-muted); margin: 4px 0 0 0;" }
      });
    }
    const actionSection = contentEl.createDiv({
      attr: { style: "margin-top: 16px; border-top: 1px solid var(--background-modifier-border); padding-top: 12px;" }
    });
    actionSection.createEl("p", {
      text: t.lintModalActionsTitle,
      attr: { style: "font-weight: bold; margin-bottom: 10px;" }
    });
    if (this.counts.pagesMissingAliases > 0 && this.fixCallbacks.onCompleteAliases) {
      const row = actionSection.createDiv({ attr: { style: "margin-bottom: 10px;" } });
      const btn = row.createEl("button", {
        text: t.lintAliasesCompleteBtn.replace("{count}", String(this.counts.pagesMissingAliases)),
        cls: "mod-cta",
        attr: { style: "font-weight: bold;" }
      });
      btn.addEventListener("click", () => {
        var _a, _b;
        (_b = (_a = this.fixCallbacks).onCompleteAliases) == null ? void 0 : _b.call(_a);
        this.close();
      });
    }
    if (this.counts.tagViolations > 0 && this.fixCallbacks.onRetagViolations) {
      const row = actionSection.createDiv({ attr: { style: "margin-bottom: 10px;" } });
      const btn = row.createEl("button", {
        text: t.lintTagViolationRetagBtn.replace("{count}", String(this.counts.tagViolations)),
        cls: "mod-cta",
        attr: { style: "font-weight: bold;" }
      });
      btn.addEventListener("click", () => {
        var _a, _b;
        (_b = (_a = this.fixCallbacks).onRetagViolations) == null ? void 0 : _b.call(_a);
        this.close();
      });
    }
    if (this.counts.pollutedPages > 0 && this.fixCallbacks.onFixPollutedPages) {
      const row = actionSection.createDiv({ attr: { style: "margin-bottom: 10px;" } });
      const btn = row.createEl("button", {
        text: t.lintModalFixPolluted.replace("{count}", String(this.counts.pollutedPages)),
        cls: "mod-cta",
        attr: { style: "font-weight: bold;" }
      });
      btn.addEventListener("click", () => {
        var _a, _b;
        (_b = (_a = this.fixCallbacks).onFixPollutedPages) == null ? void 0 : _b.call(_a);
        this.close();
      });
    }
    const fixableItems = [
      { count: this.counts.duplicates, cb: this.fixCallbacks.onMergeDuplicates, text: t.lintModalMergeDuplicates },
      { count: this.counts.deadLinks, cb: this.fixCallbacks.onFixDeadLinks, text: t.lintModalFixDeadLinks },
      { count: this.counts.orphans, cb: this.fixCallbacks.onLinkOrphans, text: t.lintModalLinkOrphans },
      { count: this.counts.emptyPages, cb: this.fixCallbacks.onFillEmptyPages, text: t.lintModalExpandEmpty },
      { count: this.counts.emptyPages, cb: this.fixCallbacks.onDeleteEmptyStubs, text: t.lintModalDeleteEmpty }
    ].filter((item) => item.count > 0 && item.cb);
    if (fixableItems.length > 0) {
      const fixRow = actionSection.createDiv({
        attr: { style: "display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 12px;" }
      });
      for (const item of fixableItems) {
        const btn = fixRow.createEl("button", {
          text: item.text.replace("{count}", String(item.count)),
          cls: "mod-cta"
        });
        btn.addEventListener("click", () => {
          var _a;
          (_a = item.cb) == null ? void 0 : _a.call(item);
          this.close();
        });
      }
    }
    const totalFixable = this.counts.deadLinks + this.counts.emptyPages + this.counts.orphans + this.counts.duplicates + this.counts.pagesMissingAliases;
    if (totalFixable > 0 && this.fixCallbacks.onFixAll) {
      const row = actionSection.createDiv({ attr: { style: "margin-bottom: 10px;" } });
      const btn = row.createEl("button", {
        text: t.lintModalFixAll.replace("{count}", String(totalFixable)),
        attr: { style: "font-weight: bold;" }
      });
      btn.addEventListener("click", () => {
        var _a, _b;
        (_b = (_a = this.fixCallbacks).onFixAll) == null ? void 0 : _b.call(_a);
        this.close();
      });
    }
    if (this.fixCallbacks.onAnalyzeSchema) {
      const row = actionSection.createDiv({ attr: { style: "margin-top: 8px;" } });
      row.createEl("button", {
        text: t.lintModalAnalyzeSchema
      }).addEventListener("click", () => {
        var _a, _b;
        (_b = (_a = this.fixCallbacks).onAnalyzeSchema) == null ? void 0 : _b.call(_a);
        this.close();
      });
    }
  }
  onClose() {
    var _a;
    (_a = this.renderComponent) == null ? void 0 : _a.unload();
    this.contentEl.empty();
  }
};
var IngestReportModal = class extends import_obsidian3.Modal {
  constructor(app, report, language = "en") {
    super(app);
    this.report = report;
    this.language = language;
  }
  t(key) {
    return getText(this.language, key) || key;
  }
  /** Map a gate rejection reason to its short localized label key. Mirrors WikiEngine.rejectionNoticeKey. */
  reasonLabelKey(reason) {
    if (reason === "incompatible-type") return "rejectionReasonType";
    if (reason === "duplicate") return "rejectionReasonDuplicate";
    return "rejectionReasonEmpty";
  }
  onOpen() {
    const { sourceFile, createdPages, updatedPages, entitiesCreated, conceptsCreated, failedItems, contradictionsFound, success, errorMessage, collisions, elapsedSeconds, skippedFiles, totalFilesInFolder, rejectedFiles } = this.report;
    const statusEmoji = success ? "\u2705" : "\u26A0\uFE0F";
    this.contentEl.createEl("h2", { text: `${statusEmoji} ${this.t("ingestReportTitle")}` });
    this.contentEl.createEl("p", { text: `${this.t("ingestReportSourceFile")}\uFF1A${sourceFile}` });
    if (skippedFiles !== void 0 && skippedFiles > 0) {
      this.contentEl.createEl("p", {
        text: `${this.t("ingestReportSkippedFiles")}: ${skippedFiles}/${totalFilesInFolder || skippedFiles}`,
        attr: { style: "color: var(--text-muted); font-size: 13px;" }
      });
    }
    if (elapsedSeconds !== void 0) {
      const minutes = Math.floor(elapsedSeconds / 60);
      const seconds = elapsedSeconds % 60;
      const timeStr = minutes > 0 ? `${minutes} ${this.t("timeMinutes")} ${seconds} ${this.t("timeSeconds")}` : `${seconds} ${this.t("timeSeconds")}`;
      this.contentEl.createEl("p", { text: `${this.t("ingestReportElapsedTime")}\uFF1A${timeStr}` });
    }
    const statsEl = this.contentEl.createDiv({ attr: { style: "margin: 12px 0;" } });
    const createdText = this.t("ingestReportCreatedPages").replace("{count}", String(createdPages.length));
    const breakdown = entitiesCreated > 0 || conceptsCreated > 0 ? ` (${this.t("ingestReportEntitiesCount").replace("{count}", String(entitiesCreated))} + ${this.t("ingestReportConceptsCount").replace("{count}", String(conceptsCreated))})` : "";
    statsEl.createEl("p", { text: createdText + breakdown });
    statsEl.createEl("p", { text: this.t("ingestReportUpdatedPages").replace("{count}", String(updatedPages.length)) });
    if (contradictionsFound > 0) {
      statsEl.createEl("p", { text: this.t("ingestReportContradictionsFound").replace("{count}", String(contradictionsFound)) });
    }
    if (collisions && collisions.length > 0) {
      this.contentEl.createEl("h3", { text: "\u{1F500} " + this.t("ingestReportCollisions") + ` (${collisions.length})` });
      const list = this.contentEl.createEl("ul");
      for (const c of collisions) {
        const sourceTypeLabel = c.sourceType === "entity" ? this.t("ingestReportEntityType") : this.t("ingestReportConceptType");
        const targetTypeLabel = c.targetType === "entity" ? this.t("ingestReportEntityType") : this.t("ingestReportConceptType");
        list.createEl("li", { text: `"${c.name}" (${sourceTypeLabel}) \u2192 ${targetTypeLabel}` });
      }
    }
    if (createdPages.length > 0) {
      this.contentEl.createEl("h3", { text: this.t("ingestReportCreated") });
      const list = this.contentEl.createEl("ul");
      for (const page of createdPages) {
        list.createEl("li", { text: page });
      }
    }
    if (updatedPages.length > 0) {
      this.contentEl.createEl("h3", { text: this.t("ingestReportUpdated") });
      const list = this.contentEl.createEl("ul");
      for (const page of updatedPages) {
        list.createEl("li", { text: page });
      }
    }
    if (failedItems.length > 0) {
      this.contentEl.createEl("h3", { text: "\u26A0\uFE0F " + this.t("ingestReportFailedTitle") });
      const list = this.contentEl.createEl("ul");
      for (const item of failedItems) {
        const typeLabel = item.type === "entity" ? this.t("ingestReportEntityType") : this.t("ingestReportConceptType");
        list.createEl("li", { text: `[${typeLabel}] ${item.name} \u2014 ${item.reason}` });
      }
      this.contentEl.createEl("p", {
        text: this.t("ingestReportFailedGuidance"),
        attr: { style: "color: var(--text-muted); margin-top: 8px; font-size: 13px;" }
      });
    }
    if (rejectedFiles && rejectedFiles.length > 0) {
      this.contentEl.createEl("h3", { text: "\u23ED\uFE0F " + this.t("ingestReportRejectedFiles") + ` (${rejectedFiles.length})` });
      const list = this.contentEl.createEl("ul");
      for (const r of rejectedFiles) {
        const name = r.path.split("/").pop() || r.path;
        list.createEl("li", { text: `${name} \u2014 ${this.t(this.reasonLabelKey(r.reason))}` });
      }
    }
    if (errorMessage) {
      this.contentEl.createEl("p", {
        text: `${this.t("ingestReportErrorDetail")}\uFF1A${errorMessage}`,
        attr: { style: "color: var(--text-error); margin-top: 12px;" }
      });
    }
    const btnRow = this.contentEl.createDiv({ attr: { style: "margin-top: 16px; text-align: right;" } });
    btnRow.createEl("button", { text: this.t("ingestReportClose") }).addEventListener("click", () => this.close());
  }
  onClose() {
    this.contentEl.empty();
  }
};
var ConfirmModal = class extends import_obsidian3.Modal {
  constructor(app, opts) {
    super(app);
    this.opts = opts;
    this.decided = false;
  }
  onOpen() {
    this.contentEl.createEl("h2", { text: this.opts.title });
    this.contentEl.createEl("p", { text: this.opts.body });
    const btnRow = this.contentEl.createDiv({ attr: { style: "margin-top: 16px; text-align: right;" } });
    btnRow.createEl("button", { text: this.opts.cancelText }).addEventListener("click", () => this.decide(false));
    btnRow.createEl("button", { text: this.opts.confirmText, cls: "mod-cta", attr: { style: "margin-left: 8px;" } }).addEventListener("click", () => this.decide(true));
  }
  decide(confirmed) {
    this.decided = true;
    this.opts.onChoice(confirmed);
    this.close();
  }
  onClose() {
    this.contentEl.empty();
    if (!this.decided) {
      this.decided = true;
      this.opts.onChoice(false);
    }
  }
};
var FixReportModal = class extends import_obsidian3.Modal {
  constructor(app, phases, language) {
    super(app);
    this.phases = phases;
    this.language = language;
  }
  onOpen() {
    const tk = (k) => getText(this.language, k) || k;
    const titleText = tk("lintFixAllComplete");
    this.contentEl.createEl("h2", { text: titleText });
    const list = this.contentEl.createEl("ul", {
      attr: { style: "margin: 12px 0; line-height: 1.8;" }
    });
    for (const phase of this.phases) {
      const itemText = phase.detail ? `${phase.label}: ${phase.detail}` : phase.label;
      list.createEl("li", { text: itemText });
    }
    const indexNote = tk("lintFixIndexUpdated");
    if (indexNote) {
      this.contentEl.createEl("p", {
        text: indexNote,
        attr: { style: "color: var(--text-muted); font-size: 13px; margin-top: 8px;" }
      });
    }
    const btnRow = this.contentEl.createDiv({ attr: { style: "margin-top: 16px; text-align: right;" } });
    const closeText = tk("ingestReportClose");
    btnRow.createEl("button", { text: closeText }).addEventListener("click", () => this.close());
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/ui/history-modal.ts
var import_obsidian4 = require("obsidian");

// src/core/log-parser.ts
var H2_RE = /^## \[([0-9]{4}-[0-9]{2}-[0-9]{2})(?: ([0-9]{2}:[0-9]{2}))?\] (.+)$/;
var CREATED_RE = /^\*\*(?:Created pages|创建页面|作成ページ|생성 페이지|Erstellte Seiten|Pages créées|Páginas criadas|Páginas criadas|Pagine create)\*\*[：:]\s*(.*)$/;
var UPDATED_RE = /^\*\*(?:Updated pages|更新页面|更新ページ|업데이트 페이지|Aktualisierte Seiten|Pages mises à jour|Páginas actualizadas|Páginas atualizadas|Pagine aggiornate)\*\*[：:]\s*(.*)$/;
var BLOCKQUOTE_RE = /^>\s?(.*)$/;
var H3_SECTION_RE = /^###\s+(.+)$/;
var BULLET_RE = /^\s*[-*+]\s+(.+)$/;
var WIKI_LINK_RE = /\[\[([^\]|#]+?)(?:#\^?[^\]|]+)?(?:\|([^\]]+))?\]\]/g;
function classifyOperation(h2Text, bodyLines) {
  const text = h2Text.toLowerCase();
  if (text.includes("\u7EF4\u62A4") || text.includes("maintenance") || text.includes("wartung") || text.includes("mantenimiento") || text.includes("manuten\xE7\xE3o") || text.includes("manutenzione") || text.includes("maintenance")) {
    return "maintenance";
  }
  if (text.startsWith("ingest") || text.includes("ingest |")) {
    return "ingest";
  }
  if (text.includes("lint") || text.includes("alias") || text.includes("fix")) {
    return "fix";
  }
  if (bodyLines.some((l) => BLOCKQUOTE_RE.test(l) && /\d+\s*(个|page|pages)/i.test(l))) {
    return "maintenance";
  }
  return "other";
}
function parseKpiSummary(line) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
  const kpi = {};
  let matched = false;
  const grab = (re) => {
    const m = line.match(re);
    return m ? Number(m[1]) : void 0;
  };
  const total = (_a = grab(/共\s*(\d+)\s*个页面/)) != null ? _a : grab(/(\d+)\s*pages?\b/i);
  if (total !== void 0) {
    kpi.totalPages = total;
    matched = true;
  }
  const missing = (_c = (_b = grab(/(\d+)\s*个缺失别名/)) != null ? _b : grab(/(\d+)\s*missing\s*alias(?:es)?/i)) != null ? _c : grab(/缺失别名\s*(\d+)/);
  if (missing !== void 0) {
    kpi.missingAliases = missing;
    matched = true;
  }
  const dup = (_d = grab(/重复\s*(\d+)/)) != null ? _d : grab(/(\d+)\s*duplicates?/i);
  if (dup !== void 0) {
    kpi.duplicates = dup;
    matched = true;
  }
  const dead = (_e = grab(/断链\s*(\d+)/)) != null ? _e : grab(/(\d+)\s*dead\s*links?/i);
  if (dead !== void 0) {
    kpi.deadLinks = dead;
    matched = true;
  }
  const orphan = (_f = grab(/孤立\s*(\d+)/)) != null ? _f : grab(/(\d+)\s*orphans?/i);
  if (orphan !== void 0) {
    kpi.orphans = orphan;
    matched = true;
  }
  const empty = (_g = grab(/空洞\s*(\d+)/)) != null ? _g : grab(/(\d+)\s*empty\s*(?:pages?|stubs?)/i);
  if (empty !== void 0) {
    kpi.emptyPages = empty;
    matched = true;
  }
  const unsourced = (_h = grab(/无来源引证\s*(\d+)/)) != null ? _h : grab(/(\d+)\s*unsourced/i);
  if (unsourced !== void 0) {
    kpi.unsourced = unsourced;
    matched = true;
  }
  const tag = (_i = grab(/标签越界\s*(\d+)/)) != null ? _i : grab(/(\d+)\s*tag\s*violations?/i);
  if (tag !== void 0) {
    kpi.tagViolations = tag;
    matched = true;
  }
  const dur = (_k = (_j = grab(/耗时\s*(\d+)\s*秒/)) != null ? _j : grab(/(\d+)\s*seconds?/i)) != null ? _k : grab(/(\d+)\s*s\s/i);
  if (dur !== void 0) {
    kpi.durationSeconds = dur;
    matched = true;
  }
  return matched ? kpi : void 0;
}
function extractWikiLinks(line) {
  var _a;
  const matches = [];
  WIKI_LINK_RE.lastIndex = 0;
  let m;
  while ((m = WIKI_LINK_RE.exec(line)) !== null) {
    matches.push({ path: m[1].trim(), alias: (_a = m[2]) == null ? void 0 : _a.trim() });
  }
  return matches;
}
function stripWikiLinks(line) {
  return line.replace(WIKI_LINK_RE, "").replace(/\s{2,}/g, " ");
}
var BULLET_PREFIX_RE = /^\s*[-*+]\s+/;
function parseDetailRows(details) {
  if (!details) return [];
  const rows = [];
  for (const line of details.split("\n")) {
    const trimmed = line.trim();
    if (trimmed.length === 0) continue;
    const isBullet = BULLET_PREFIX_RE.test(trimmed);
    const links = extractWikiLinks(trimmed);
    const textWithoutLinks = stripWikiLinks(trimmed).trim();
    rows.push({ isBullet, links, textWithoutLinks, raw: trimmed });
  }
  return rows;
}
function parseLogEntries(content) {
  if (!content || content.trim().length === 0) return [];
  const lines = content.split("\n");
  const entries = [];
  let i = 0;
  while (i < lines.length) {
    const m = H2_RE.exec(lines[i]);
    if (!m) {
      i++;
      continue;
    }
    const date = m[1];
    const time = m[2];
    const rest = m[3].trim();
    let ingestMetrics;
    let restWithoutMetrics = rest;
    const SUFFIX_SEP = " \xB7 ";
    while (true) {
      const idx = restWithoutMetrics.lastIndexOf(SUFFIX_SEP);
      if (idx <= 0) break;
      const tail = restWithoutMetrics.substring(idx + SUFFIX_SEP.length);
      const parsed = parseIngestMetrics(tail);
      if (!parsed) break;
      ingestMetrics = { ...ingestMetrics, ...parsed };
      restWithoutMetrics = restWithoutMetrics.substring(0, idx);
    }
    const sep = restWithoutMetrics.indexOf("|");
    let operation;
    let sourceTitle;
    if (sep >= 0) {
      operation = restWithoutMetrics.substring(0, sep).trim();
      sourceTitle = restWithoutMetrics.substring(sep + 1).trim();
    } else {
      operation = restWithoutMetrics;
    }
    i++;
    const bodyLines = [];
    while (i < lines.length && !H2_RE.test(lines[i])) {
      bodyLines.push(lines[i]);
      i++;
    }
    const kind = classifyOperation(operation, bodyLines);
    const entry = buildEntry(kind, date, time, operation, sourceTitle, bodyLines, ingestMetrics);
    entries.push(entry);
  }
  return entries;
}
function buildEntry(kind, date, time, operation, sourceTitle, bodyLines, ingestMetrics) {
  var _a;
  const entry = {
    kind,
    date,
    time,
    operation,
    sourceTitle,
    createdPages: [],
    updatedPages: [],
    sections: [],
    details: []
  };
  if (ingestMetrics) entry.ingestMetrics = ingestMetrics;
  let firstBulletIdx = -1;
  for (let j = 0; j < bodyLines.length; j++) {
    const line = bodyLines[j];
    const created = CREATED_RE.exec(line);
    if (created) {
      entry.createdPages = extractWikiLinks(created[1]).map((l) => l.path);
      continue;
    }
    const updated = UPDATED_RE.exec(line);
    if (updated) {
      entry.updatedPages = extractWikiLinks(updated[1]).map((l) => l.path);
      continue;
    }
    if (firstBulletIdx === -1 && BULLET_RE.test(line)) {
      firstBulletIdx = j;
    }
  }
  if (kind === "maintenance") {
    for (const line of bodyLines) {
      const bq = BLOCKQUOTE_RE.exec(line);
      if (bq) {
        const kpi = parseKpiSummary(bq[1]);
        if (kpi) {
          entry.kpi = kpi;
          break;
        }
      }
    }
    let currentSection = null;
    let ungrouped = [];
    const flush = () => {
      if (currentSection) {
        entry.sections.push(currentSection);
        currentSection = null;
      }
    };
    for (const line of bodyLines) {
      const h3 = H3_SECTION_RE.exec(line);
      if (h3) {
        flush();
        const heading = h3[1].trim();
        currentSection = {
          kind: classifySectionKind(heading, []),
          heading,
          items: []
        };
        const count = parseCountFromHeading(heading);
        if (count !== void 0) currentSection.count = count;
        continue;
      }
      if (currentSection) {
        const bullet = BULLET_RE.exec(line);
        if (bullet) {
          currentSection.items.push(parseSectionItem(currentSection.kind, bullet[1]));
        } else if (line.trim().length > 0 && !BLOCKQUOTE_RE.test(line) && !CREATED_RE.test(line) && !UPDATED_RE.test(line)) {
          currentSection.items.push(parseSectionItem(currentSection.kind, line.trim()));
        }
      } else {
        ungrouped.push(line);
      }
    }
    flush();
    if (ungrouped.length > 0) {
      const topBullets = ungrouped.map((l) => {
        var _a2;
        return (_a2 = BULLET_RE.exec(l)) == null ? void 0 : _a2[1];
      }).filter((s) => Boolean(s));
      if (topBullets.length > 0) {
        const topKind = classifySectionKind("", topBullets);
        entry.sections.unshift({
          kind: topKind,
          heading: topKind === "deadLinks" ? "Dead links" : "",
          items: topBullets.map((b) => parseSectionItem(topKind, b))
        });
      }
    }
    for (let i = 0; i < entry.sections.length - 1; i++) {
      const cur = entry.sections[i];
      const next = entry.sections[i + 1];
      if (cur.heading && cur.heading === next.heading) {
        cur.items.push(...next.items);
        if (typeof next.count === "number") {
          cur.count = ((_a = cur.count) != null ? _a : 0) + next.count;
        }
        entry.sections.splice(i + 1, 1);
        i--;
      }
    }
    return entry;
  }
  const detailsBody = bodyLines.filter((l) => !CREATED_RE.test(l) && !UPDATED_RE.test(l) && l.trim().length > 0).join("\n");
  entry.details = parseDetailRows(detailsBody);
  entry.rawDetails = detailsBody || void 0;
  return entry;
}
var SECTION_KIND_PATTERNS = [
  ["llmAnalysis", /LLM\s*(分析|Analysis)|LLM\s*Analysis/i],
  ["tagViolation", /标签越界|Tag\s*violations?|tag\s*issues?/i],
  ["orphan", /孤立页面?|Orphans?|Orphaned/i],
  ["empty", /空洞页面?|Empty\s*(?:pages?|stubs?)/i],
  ["duplicate", /重复|Duplicates?/i],
  ["deadLinks", /断链|Dead\s*links?/i]
];
function classifySectionKind(heading, bullets) {
  for (const [kind, re] of SECTION_KIND_PATTERNS) {
    if (re.test(heading)) return kind;
  }
  if (bullets.length > 0) {
    const deadLinkHits = bullets.filter((b) => DEAD_LINK_BULLET_RE.test(b)).length;
    if (deadLinkHits / bullets.length >= 0.8) return "deadLinks";
  }
  return "unknown";
}
function parseCountFromHeading(heading) {
  var _a;
  const m = (_a = heading.match(/共\s*(\d+)\s*个/)) != null ? _a : heading.match(/\[(\d+)\s*(?:items?|个|pages?)\]/i);
  return m ? Number(m[1]) : void 0;
}
var DEAD_LINK_BULLET_RE = /\[\[[^\]]+\]\][^[]*→\s*\*\*[^*]+\*\*/;
var DEAD_LINK_TARGET_RE = /→\s*\*\*([^*]+)\*\*/;
var LLM_TYPE_RE = /^\[([^\]：:]{1,6})[\]】：:]/;
function parseSectionItem(kind, bulletBody) {
  const links = extractWikiLinks(bulletBody);
  const text = stripWikiLinks(bulletBody).trim();
  const item = {
    links,
    text,
    raw: bulletBody,
    severity: defaultSeverityForKind(kind)
  };
  if (kind === "deadLinks") {
    const m = DEAD_LINK_TARGET_RE.exec(bulletBody);
    if (m) item.targetPage = m[1].trim();
    item.severity = "high";
  } else if (kind === "tagViolation") {
    item.severity = "medium";
  } else if (kind === "orphan" || kind === "empty") {
    item.severity = "low";
  } else if (kind === "llmAnalysis") {
    const m = LLM_TYPE_RE.exec(bulletBody);
    if (m) {
      item.llmType = m[1].trim();
      item.severity = llmSeverityFor(item.llmType);
    }
  }
  return item;
}
function defaultSeverityForKind(kind) {
  switch (kind) {
    case "deadLinks":
      return "high";
    case "tagViolation":
      return "medium";
    case "orphan":
      return "low";
    case "empty":
      return "low";
    case "llmAnalysis":
      return "medium";
    default:
      return "none";
  }
}
function parseIngestMetrics(tail) {
  var _a;
  const tokens = tail.split(/\s·\s/).map((s) => s.trim()).filter(Boolean);
  if (tokens.length === 0) return void 0;
  const m = {};
  let recognized = false;
  for (const tok of tokens) {
    const durM = tok.match(/^(?:(\d+(?:\.\d+)?)m)?(?:(\d+(?:\.\d+)?)s)?$/i);
    if (durM && (durM[1] || durM[2])) {
      const minutes = durM[1] ? Number(durM[1]) : 0;
      const seconds = durM[2] ? Number(durM[2]) : 0;
      m.durationSec = ((_a = m.durationSec) != null ? _a : 0) + Math.round(minutes * 60 + seconds);
      recognized = true;
      continue;
    }
    const sizeMatch = tok.match(/^(\d+(?:\.\d+)?)\s*(B|KB|MB|GB)$/i);
    if (sizeMatch) {
      const num = Number(sizeMatch[1]);
      const unit = sizeMatch[2].toUpperCase();
      const bytes = unit === "B" ? num : unit === "KB" ? num * 1024 : unit === "MB" ? num * 1024 * 1024 : num * 1024 * 1024 * 1024;
      m.sourceBytes = Math.round(bytes);
      recognized = true;
      continue;
    }
    m.model = tok;
    recognized = true;
  }
  return recognized ? m : void 0;
}
function llmSeverityFor(type) {
  if (type.includes("\u77DB\u76FE")) return "high";
  if (type.includes("\u7F3A\u5931")) return "medium";
  if (type.includes("\u8FC7\u65F6")) return "medium";
  if (type.includes("\u7ED3\u6784")) return "low";
  return "none";
}

// src/ui/history-modal.ts
var HISTORY_LIMIT = 50;
function maintenanceInsight(e, t) {
  if (!e.kpi) {
    return {
      primary: e.operation,
      severity: "none"
    };
  }
  const k = e.kpi;
  const parts = [];
  if (typeof k.deadLinks === "number" && k.deadLinks > 0) {
    parts.push(`${k.deadLinks} ${t.historyKpiDeadLinks}`);
  }
  if (typeof k.orphans === "number" && k.orphans > 0) {
    parts.push(`${k.orphans} ${t.historyKpiOrphans}`);
  }
  if (typeof k.emptyPages === "number" && k.emptyPages > 0) {
    parts.push(`${k.emptyPages} ${t.historyKpiEmpty}`);
  }
  if (typeof k.tagViolations === "number" && k.tagViolations > 0) {
    parts.push(`${k.tagViolations} ${t.historyKpiTagViolations}`);
  }
  if (typeof k.duplicates === "number" && k.duplicates > 0) {
    parts.push(`${k.duplicates} ${t.historyKpiDuplicates}`);
  }
  if (parts.length === 0) {
    if (typeof k.durationSeconds === "number") {
      parts.push(`\u2713 ${t.historyKpiDurationSec.replace("{seconds}", String(k.durationSeconds))}`);
    }
    return {
      primary: parts.join(" \xB7 ") || t.historyKpiDuration,
      severity: "none"
    };
  }
  const severity = k.deadLinks && k.deadLinks > 10 ? "high" : k.deadLinks || k.tagViolations || k.emptyPages ? "medium" : "low";
  return { primary: parts.join(" \xB7 "), severity };
}
function fixInsight(e, _t) {
  const itemCount = e.details.length;
  if (itemCount === 0) {
    return { primary: e.operation, severity: "none" };
  }
  return {
    primary: `${itemCount} item${itemCount > 1 ? "s" : ""} fixed`,
    severity: "low"
  };
}
function ingestInsight(e, t) {
  var _a;
  const total = e.createdPages.length + e.updatedPages.length;
  const parts = [];
  if (e.createdPages.length > 0) {
    parts.push(t.historyEntryCreatedCount.replace("{count}", String(e.createdPages.length)));
  }
  if (e.updatedPages.length > 0) {
    parts.push(t.historyEntryUpdatedCount.replace("{count}", String(e.updatedPages.length)));
  }
  if (typeof ((_a = e.ingestMetrics) == null ? void 0 : _a.durationSec) === "number") {
    parts.push(`${e.ingestMetrics.durationSec}s`);
  }
  return {
    primary: parts.join(" \xB7 ") || t.historyEntryNoChanges,
    severity: total === 0 ? "low" : "none"
  };
}
function otherInsight(e) {
  return { primary: e.operation, severity: "none" };
}
function computeKpiDeltas(entries, currentIdx) {
  let prev;
  for (let j = currentIdx - 1; j >= 0; j--) {
    if (entries[j].kind === "maintenance") {
      prev = entries[j];
      break;
    }
  }
  const current = entries[currentIdx];
  if (!current.kpi) return void 0;
  const mk = (key) => {
    var _a, _b;
    const cur = (_a = current.kpi) == null ? void 0 : _a[key];
    if (typeof cur !== "number") return void 0;
    const pv = (_b = prev == null ? void 0 : prev.kpi) == null ? void 0 : _b[key];
    return {
      current: cur,
      prev: typeof pv === "number" ? pv : void 0,
      delta: typeof pv === "number" ? cur - pv : 0
    };
  };
  return {
    totalPages: mk("totalPages"),
    deadLinks: mk("deadLinks"),
    orphans: mk("orphans"),
    durationSeconds: mk("durationSeconds")
  };
}
function matchesSearch(e, search) {
  if (!search) return true;
  const q = search.toLowerCase();
  if (e.sourceTitle && e.sourceTitle.toLowerCase().includes(q)) return true;
  if (e.operation.toLowerCase().includes(q)) return true;
  for (const p of e.createdPages) if (p.toLowerCase().includes(q)) return true;
  for (const p of e.updatedPages) if (p.toLowerCase().includes(q)) return true;
  for (const sec of e.sections) {
    if (sec.heading.toLowerCase().includes(q)) return true;
    for (const it of sec.items) {
      if (it.text.toLowerCase().includes(q)) return true;
      if (it.raw.toLowerCase().includes(q)) return true;
    }
  }
  if (e.rawDetails && e.rawDetails.toLowerCase().includes(q)) return true;
  return false;
}
function matchesFilter(e, filter) {
  switch (filter) {
    case "all":
      return true;
    case "ingest":
      return e.kind === "ingest";
    case "maintenance":
      return e.kind === "maintenance";
    case "fix":
      return e.kind === "fix";
    case "contradictions":
      return e.kind === "ingest";
  }
}
function matchesTimeRange(e, range, nowMs, customFrom, customTo) {
  if (range === "all") return true;
  if (range === "custom") {
    if (!customFrom && !customTo) return true;
    const entryMs2 = Date.parse(`${e.date}T00:00:00Z`);
    const fromMs = customFrom ? Date.parse(`${customFrom}T00:00:00Z`) : 0;
    const toMs = customTo ? Date.parse(`${customTo}T23:59:59Z`) : Infinity;
    return entryMs2 >= fromMs && entryMs2 <= toMs;
  }
  const dayMs = 24 * 60 * 60 * 1e3;
  const days = range === "1d" ? 1 : range === "3d" ? 3 : range === "1w" ? 7 : 30;
  const cutoffMs = nowMs - days * dayMs;
  const entryMs = Date.parse(`${e.date}T23:59:59Z`);
  return entryMs >= cutoffMs;
}
function badgeForKind(kind, t) {
  switch (kind) {
    case "ingest":
      return t.historyBadgeIngestShort;
    case "maintenance":
      return t.historyBadgeMaintenanceShort;
    case "fix":
      return t.historyBadgeFixShort;
    case "other":
      return t.historyBadgeOtherShort;
  }
}
function kindLabelFor(kind, t) {
  switch (kind) {
    case "ingest":
      return t.historyEntryKindIngest;
    case "maintenance":
      return t.historyEntryKindMaintenance;
    case "fix":
      return t.historyEntryKindFix;
    case "other":
      return t.historyEntryKindOther;
  }
}
function computeGlobalInsight(entries, t) {
  const latestMaint = entries.find((e) => e.kind === "maintenance" && e.kpi);
  if (!latestMaint || !latestMaint.kpi) {
    return { kind: "noData", primary: t.historyGlobalInsightNoData };
  }
  const k = latestMaint.kpi;
  const dead = typeof k.deadLinks === "number" ? k.deadLinks : 0;
  const orphans = typeof k.orphans === "number" ? k.orphans : 0;
  const tags = typeof k.tagViolations === "number" ? k.tagViolations : 0;
  const dur = typeof k.durationSeconds === "number" ? t.historyKpiDurationSec.replace("{seconds}", String(k.durationSeconds)) : "\u2014";
  if (dead === 0 && orphans === 0 && tags === 0) {
    return { kind: "clean", primary: t.historyGlobalInsightClean };
  }
  const primary = t.historyGlobalInsight.replace("{dead}", String(dead)).replace("{orphans}", String(orphans)).replace("{tags}", String(tags)).replace("{duration}", dur);
  return { kind: "withData", primary };
}
function renderHistoryEntries(entries, t, params) {
  var _a, _b;
  if (entries.length === 0) {
    return { kind: "empty" };
  }
  const nowMs = (_a = params.nowMs) != null ? _a : Date.now();
  const range = (_b = params.timeRange) != null ? _b : "all";
  const filtered = entries.filter(
    (e) => matchesFilter(e, params.filter) && matchesSearch(e, params.search) && matchesTimeRange(e, range, nowMs, params.customFrom, params.customTo)
  );
  if (filtered.length === 0) {
    return { kind: "noMatch" };
  }
  const limit = t.historyLimit || HISTORY_LIMIT;
  const visible = filtered.slice(0, limit);
  const overflow = filtered.length - visible.length;
  const groupMap = /* @__PURE__ */ new Map();
  for (const e of visible) {
    const list = groupMap.get(e.date);
    if (list) list.push(e);
    else groupMap.set(e.date, [e]);
  }
  const idxByEntry = new Map(entries.map((e, i) => [e, i]));
  const groups = [...groupMap.entries()].sort(([a], [b]) => b.localeCompare(a)).map(([date, dayEntries]) => ({
    date,
    entries: dayEntries.map((e) => {
      var _a2;
      const insight = e.kind === "maintenance" ? maintenanceInsight(e, t) : e.kind === "fix" ? fixInsight(e, t) : e.kind === "ingest" ? ingestInsight(e, t) : otherInsight(e);
      return {
        kind: e.kind,
        date: e.date,
        time: e.time,
        operation: e.operation,
        sourceTitle: e.sourceTitle,
        insight,
        createdPages: e.createdPages,
        updatedPages: e.updatedPages,
        kpi: e.kpi,
        kpiDeltas: e.kind === "maintenance" ? computeKpiDeltas(entries, (_a2 = idxByEntry.get(e)) != null ? _a2 : 0) : void 0,
        sections: e.sections,
        details: e.details,
        rawDetails: e.rawDetails,
        ingestMetrics: e.ingestMetrics,
        badge: badgeForKind(e.kind, t),
        kindLabel: kindLabelFor(e.kind, t)
      };
    })
  }));
  return {
    kind: "groups",
    groups,
    overflow,
    totalFiltered: filtered.length,
    globalInsight: computeGlobalInsight(filtered, t)
  };
}
var HistoryModal = class extends import_obsidian4.Modal {
  constructor(app, opts) {
    super(app);
    this.renderComponent = null;
    this.language = opts.language;
    this.logPath = `${opts.wikiFolder}/log.md`;
  }
  onOpen() {
    const { contentEl } = this;
    this.renderComponent = new import_obsidian4.Component();
    this.renderComponent.load();
    void this.renderContent(contentEl);
  }
  onClose() {
    var _a;
    (_a = this.renderComponent) == null ? void 0 : _a.unload();
    this.contentEl.empty();
  }
  async renderContent(container) {
    const locale = this.language in TEXTS ? this.language : "en";
    const t = TEXTS[locale];
    container.createEl("h2", {
      text: t.historyModalHeaderTitle,
      attr: { style: "margin: 0 0 4px 0;" }
    });
    const subtitle = container.createEl("p", {
      text: t.historyModalSubtitle,
      attr: { style: "font-size: 0.85em; color: var(--text-muted); margin: 0 0 12px 0;" }
    });
    let rawContent;
    try {
      rawContent = await this.app.vault.adapter.read(this.logPath);
    } catch (e) {
      container.createEl("p", {
        text: t.historyReadError.replace("{error}", "file not found"),
        attr: { style: "color: var(--text-warning); padding: 12px;" }
      });
      container.createEl("p", {
        text: t.historyGlobalInsightNoData,
        attr: { style: "color: var(--text-muted); text-align: center; padding: 24px 0;" }
      });
      this.renderCloseFooter(container, t);
      return;
    }
    const entries = parseLogEntries(rawContent);
    let currentSearch = "";
    let currentFilter = "all";
    let currentRange = "all";
    let customFrom;
    let customTo;
    const controls = container.createDiv({
      attr: { style: "display: flex; gap: 8px; align-items: center; margin-bottom: 12px; flex-wrap: wrap;" }
    });
    const searchInput = controls.createEl("input", {
      type: "text",
      placeholder: t.historySearchPlaceholder,
      attr: { style: "flex: 1; min-width: 200px; padding: 6px 10px; border-radius: 4px; border: 1px solid var(--background-modifier-border);" }
    });
    const filterSelect = controls.createEl("select", {
      attr: { style: "padding: 6px 10px; border-radius: 4px; border: 1px solid var(--background-modifier-border);" }
    });
    filterSelect.createEl("option", { value: "all", text: t.historyFilterAll });
    filterSelect.createEl("option", { value: "ingest", text: t.historyFilterIngest });
    filterSelect.createEl("option", { value: "maintenance", text: t.historyFilterMaintenance });
    filterSelect.createEl("option", { value: "fix", text: t.historyFilterFix });
    filterSelect.createEl("option", { value: "contradictions", text: t.historyFilterContradictions });
    const rangeSelect = controls.createEl("select", {
      attr: { style: "padding: 6px 10px; border-radius: 4px; border: 1px solid var(--background-modifier-border);" }
    });
    rangeSelect.createEl("option", { value: "all", text: t.historyTimeRangeAll });
    rangeSelect.createEl("option", { value: "1d", text: t.historyTimeRange1d });
    rangeSelect.createEl("option", { value: "3d", text: t.historyTimeRange3d });
    rangeSelect.createEl("option", { value: "1w", text: t.historyTimeRange1w });
    rangeSelect.createEl("option", { value: "1m", text: t.historyTimeRange1m });
    rangeSelect.createEl("option", { value: "custom", text: t.historyTimeRangeCustom });
    const customRangeContainer = controls.createDiv({
      cls: "llm-wiki-custom-range"
    });
    customRangeContainer.createEl("span", {
      text: t.historyCustomRangeFrom,
      attr: { style: "font-size: 0.85em; color: var(--text-muted);" }
    });
    const fromDate = customRangeContainer.createEl("input", {
      type: "date",
      cls: "llm-wiki-date-input"
    });
    customRangeContainer.createEl("span", {
      text: t.historyCustomRangeTo,
      attr: { style: "font-size: 0.85em; color: var(--text-muted);" }
    });
    const toDate = customRangeContainer.createEl("input", {
      type: "date",
      cls: "llm-wiki-date-input"
    });
    const applyBtn = customRangeContainer.createEl("button", {
      text: t.historyCustomRangeApply,
      attr: { style: "padding: 4px 12px; border-radius: 4px; cursor: pointer;" }
    });
    const clearBtn = customRangeContainer.createEl("button", {
      text: t.historyCustomRangeClear,
      attr: { style: "padding: 4px 12px; border-radius: 4px; cursor: pointer;" }
    });
    const refreshBtn = controls.createEl("button", {
      text: t.historyRefreshButton,
      attr: { style: "padding: 6px 12px; border-radius: 4px; cursor: pointer;" }
    });
    const resultsContainer = container.createDiv();
    const render = () => {
      resultsContainer.empty();
      const result = renderHistoryEntries(entries, t, {
        search: currentSearch,
        filter: currentFilter,
        timeRange: currentRange,
        customFrom,
        customTo
      });
      subtitle.textContent = t.historyModalSubtitleWithCount.replace(
        "{count}",
        String(entries.length)
      );
      if (result.kind === "empty") {
        resultsContainer.createEl("p", {
          text: t.historyEmpty,
          attr: { style: "color: var(--text-muted); text-align: center; padding: 24px 0;" }
        });
        return;
      }
      if (result.kind === "noMatch") {
        resultsContainer.createEl("p", {
          text: t.historyNoMatch,
          attr: { style: "color: var(--text-muted); text-align: center; padding: 24px 0;" }
        });
        return;
      }
      if (result.globalInsight) {
        this.renderGlobalInsight(resultsContainer, result.globalInsight, t);
      }
      for (const group of result.groups) {
        const dayEl = resultsContainer.createDiv({
          attr: { style: "margin-bottom: 16px;" }
        });
        dayEl.createEl("strong", {
          text: group.date,
          attr: { style: "display: block; font-size: 0.9em; color: var(--text-muted); margin-bottom: 6px;" }
        });
        for (const entry of group.entries) {
          this.renderEntry(dayEl, entry, t);
        }
      }
      if (result.overflow > 0) {
        const showMoreBtn = resultsContainer.createEl("button", {
          text: t.historyShowMore.replace("{count}", String(result.overflow)),
          attr: { style: "margin-top: 8px; padding: 6px 12px; border-radius: 4px; cursor: pointer;" }
        });
        showMoreBtn.addEventListener("click", () => {
        });
      }
    };
    searchInput.addEventListener("input", () => {
      currentSearch = searchInput.value.trim();
      render();
    });
    filterSelect.addEventListener("change", () => {
      currentFilter = filterSelect.value;
      render();
    });
    rangeSelect.addEventListener("change", () => {
      currentRange = rangeSelect.value;
      if (currentRange === "custom") {
        customRangeContainer.classList.add("llm-wiki-custom-range-visible");
      } else {
        customRangeContainer.classList.remove("llm-wiki-custom-range-visible");
      }
      render();
    });
    applyBtn.addEventListener("click", () => {
      if (fromDate.value || toDate.value) {
        customFrom = fromDate.value || void 0;
        customTo = toDate.value || void 0;
        render();
      }
    });
    clearBtn.addEventListener("click", () => {
      fromDate.value = "";
      toDate.value = "";
      customFrom = void 0;
      customTo = void 0;
      render();
    });
    refreshBtn.addEventListener("click", () => {
      void (async () => {
        try {
          const updatedContent = await this.app.vault.adapter.read(this.logPath);
          const newEntries = parseLogEntries(updatedContent);
          entries.length = 0;
          entries.push(...newEntries);
          render();
        } catch (e) {
        }
      })();
    });
    render();
    this.renderCloseFooter(container, t);
  }
  // ── Global insight card ──
  renderGlobalInsight(parent, insight, t) {
    const isClean = insight.kind === "clean";
    const isNoData = insight.kind === "noData";
    const bg = "var(--background-secondary)";
    const borderColor = isClean ? "var(--text-success)" : isNoData ? "var(--text-faint)" : "var(--text-warning)";
    const icon = isClean ? "\u2705" : isNoData ? "\u2139\uFE0F" : "\u26A0\uFE0F";
    parent.createDiv({
      attr: {
        style: `padding: 12px 14px; margin-bottom: 16px; border-radius: 6px; background: ${bg}; border-left: 4px solid ${borderColor}; font-size: 0.95em;`
      }
    }).createEl("div", {
      text: `${icon}  ${insight.primary}`
    });
    void t;
  }
  // ── Per-entry rendering ──
  renderEntry(parent, entry, t) {
    const borderColor = entry.kind === "ingest" ? "var(--text-accent)" : entry.kind === "maintenance" ? "var(--text-warning)" : entry.kind === "fix" ? "var(--text-success)" : "var(--text-faint)";
    const entryEl = parent.createDiv({
      attr: {
        style: `padding: 8px 12px; border-left: 3px solid ${borderColor}; margin-bottom: 6px; border-radius: 0 4px 4px 0; background: var(--background-secondary);`
      }
    });
    const row1 = entryEl.createDiv({
      attr: { style: "display: flex; gap: 8px; align-items: baseline; margin-bottom: 2px;" }
    });
    row1.createEl("span", {
      text: entry.badge,
      attr: { style: "font-size: 1em;" }
    });
    row1.createEl("span", {
      text: entry.kindLabel,
      attr: { style: "font-size: 0.75em; color: var(--text-muted); font-weight: 500;" }
    });
    if (entry.time) {
      row1.createEl("span", {
        text: entry.time,
        attr: { style: "color: var(--text-muted); font-size: 0.8em;" }
      });
    }
    if (entry.kind === "ingest" && entry.sourceTitle) {
      row1.createEl("span", {
        text: entry.sourceTitle,
        attr: { style: "font-weight: 500;" }
      });
    }
    const insightColor = entry.insight.severity === "high" ? "var(--text-warning)" : entry.insight.severity === "medium" ? "var(--text-normal)" : "var(--text-muted)";
    entryEl.createEl("div", {
      text: entry.insight.primary,
      attr: {
        style: `font-size: 0.95em; color: ${insightColor}; ` + (entry.insight.severity === "high" ? "font-weight: 600;" : "")
      }
    });
    if (entry.insight.delta) {
      entryEl.createEl("div", {
        text: entry.insight.delta,
        attr: { style: "font-size: 0.8em; color: var(--text-faint); margin-top: 2px;" }
      });
    }
    const detailsEl = entryEl.createEl("details", {
      attr: { style: "margin-top: 6px;" }
    });
    detailsEl.createEl("summary", {
      text: t.historyEntrySectionDetails,
      attr: { style: "cursor: pointer; font-size: 0.8em; color: var(--text-faint); user-select: none;" }
    });
    const detailsBody = detailsEl.createDiv({
      attr: { style: "padding: 8px 0 0 0; font-size: 0.85em;" }
    });
    if (entry.kind === "maintenance") {
      this.renderMaintenanceDetails(detailsBody, entry, t);
    } else if (entry.kind === "ingest") {
      this.renderIngestDetails(detailsBody, entry, t);
    } else if (entry.kind === "fix") {
      this.renderFixDetails(detailsBody, entry, t);
    } else {
      detailsBody.createEl("div", {
        text: entry.rawDetails || entry.operation,
        attr: { style: "white-space: pre-wrap; color: var(--text-muted); margin-left: 8px;" }
      });
    }
    this.renderOpenInLogLink(detailsBody, t);
  }
  // ── Maintenance details (the rich viz layer) ──
  renderMaintenanceDetails(body, entry, t) {
    if (entry.kpi) {
      this.renderCriticalKpiCards(body, entry.kpi, entry.kpiDeltas, t);
    }
    if (entry.sections.length > 0) {
      body.createEl("div", {
        text: t.historyEntrySectionReport,
        attr: {
          style: "font-weight: 600; margin-top: 12px; margin-bottom: 6px; color: var(--text-normal); font-size: 0.9em;"
        }
      });
      for (const section of entry.sections) {
        this.renderReportSection(body, section, t);
      }
    }
    if (!entry.kpi && entry.sections.length === 0 && entry.rawDetails) {
      body.createEl("div", {
        text: entry.rawDetails,
        attr: { style: "white-space: pre-wrap; color: var(--text-muted); margin-left: 8px;" }
      });
    }
  }
  /**
   * Render the 8 KPI cards. Each card shows its delta vs the previous
   * maintenance entry in a small arrow chip. 8 cards in a 4×2 grid is
   * information-dense but still scannable — each cell is wide enough for
   * a single short label, and the user can spot outliers at a glance.
   */
  renderCriticalKpiCards(body, kpi, deltas, t) {
    const grid = body.createDiv({
      attr: {
        style: "display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 6px; margin-bottom: 8px;"
      }
    });
    const cards = [];
    const deadLinks = kpi.deadLinks;
    if (typeof deadLinks === "number") {
      const sev = deadLinks > 10 ? "high" : deadLinks > 0 ? "medium" : "none";
      cards.push({
        label: t.historyKpiDeadLinks,
        value: deadLinks,
        severity: sev,
        deltaChip: this.deltaChip(deltas == null ? void 0 : deltas.deadLinks, t)
      });
    }
    const orphans = kpi.orphans;
    if (typeof orphans === "number") {
      cards.push({
        label: t.historyKpiOrphans,
        value: orphans,
        severity: orphans > 0 ? "low" : "none",
        deltaChip: this.deltaChip(deltas == null ? void 0 : deltas.orphans, t)
      });
    }
    const emptyPages = kpi.emptyPages;
    if (typeof emptyPages === "number") {
      cards.push({
        label: t.historyKpiEmpty,
        value: emptyPages,
        severity: emptyPages > 0 ? "low" : "none",
        deltaChip: void 0
      });
    }
    const duplicates = kpi.duplicates;
    if (typeof duplicates === "number") {
      cards.push({
        label: t.historyKpiDuplicates,
        value: duplicates,
        severity: duplicates > 0 ? "medium" : "none",
        deltaChip: void 0
      });
    }
    const tagViolations = kpi.tagViolations;
    if (typeof tagViolations === "number") {
      cards.push({
        label: t.historyKpiTagViolations,
        value: tagViolations,
        severity: tagViolations > 0 ? "medium" : "none",
        deltaChip: void 0
      });
    }
    const unsourced = kpi.unsourced;
    if (typeof unsourced === "number") {
      cards.push({
        label: t.historyKpiUnsourced,
        value: unsourced,
        severity: unsourced > 0 ? "low" : "none",
        deltaChip: void 0
      });
    }
    if (typeof kpi.durationSeconds === "number") {
      cards.push({
        label: t.historyKpiDuration,
        value: t.historyKpiDurationSec.replace("{seconds}", String(kpi.durationSeconds)),
        severity: "none",
        deltaChip: this.deltaChip(deltas == null ? void 0 : deltas.durationSeconds, t)
      });
    }
    if (typeof kpi.totalPages === "number") {
      cards.push({
        label: t.historyKpiPages,
        value: kpi.totalPages,
        severity: "none",
        deltaChip: this.deltaChip(deltas == null ? void 0 : deltas.totalPages, t)
      });
    }
    for (const card of cards) {
      const valueColor = card.severity === "high" ? "var(--text-error)" : card.severity === "medium" ? "var(--text-warning)" : card.severity === "low" ? "var(--text-accent)" : "var(--text-normal)";
      const cardEl = grid.createDiv({
        attr: {
          style: "padding: 8px 10px; border-radius: 4px; background: var(--background-primary); border: 1px solid var(--background-modifier-border); position: relative;"
        }
      });
      cardEl.createEl("div", {
        text: String(card.value),
        attr: {
          style: `font-size: 1.3em; font-weight: 700; line-height: 1.2; color: ${valueColor};`
        }
      });
      cardEl.createEl("div", {
        text: card.label,
        attr: { style: "font-size: 0.7em; color: var(--text-muted); margin-top: 2px;" }
      });
      if (card.deltaChip) {
        cardEl.createEl("div", {
          text: card.deltaChip,
          attr: {
            style: "position: absolute; top: 4px; right: 6px; font-size: 0.7em; font-weight: 600; color: var(--text-muted);"
          }
        });
      }
    }
  }
  /** Build the delta chip string ('↗ +3', '↘ -1', '→ same'). Returns undefined if no previous entry. */
  deltaChip(d, t) {
    if (!d || d.prev === void 0) return void 0;
    if (d.delta === 0) return t.historyTrendSame;
    if (d.delta > 0) return t.historyTrendUp.replace("{delta}", `+${d.delta}`);
    return t.historyTrendDown.replace("{delta}", String(d.delta));
  }
  // ── Per-section rich rendering ──
  /**
   * Dispatch on section.kind and render appropriately. This is where the
   * "信息可视化" happens — each section type gets its own visual treatment.
   */
  renderReportSection(body, section, t) {
    switch (section.kind) {
      case "deadLinks":
        this.renderDeadLinkSection(body, section, t);
        break;
      case "tagViolation":
        this.renderTagViolationSection(body, section, t);
        break;
      case "orphan":
        this.renderSimpleListSection(body, section, t.historySectionOrphans, t);
        break;
      case "empty":
        this.renderSimpleListSection(body, section, t.historySectionEmptyPages, t);
        break;
      case "llmAnalysis":
        this.renderLlmAnalysisSection(body, section, t);
        break;
      default:
        this.renderSimpleListSection(body, section, section.heading, t);
        break;
    }
  }
  /** Dead links rendered as a 2-column table (Source → Missing). */
  renderDeadLinkSection(body, section, t) {
    const label = t.historySectionDeadLinks.replace("{count}", String(section.items.length));
    this.renderSectionTitle(body, label, "high");
    if (section.items.length === 0) {
      body.createEl("div", {
        text: "\u2713",
        attr: { style: "color: var(--text-success); margin-left: 12px;" }
      });
      return;
    }
    const collapsed = section.items.length > 10;
    if (collapsed) {
      const details = body.createEl("details", { attr: { style: "margin-left: 4px;" } });
      details.createEl("summary", {
        text: t.historyShowMoreItems.replace("{count}", String(section.items.length)),
        attr: { style: "cursor: pointer; color: var(--text-accent); user-select: none;" }
      });
      const inner = details.createDiv({ attr: { style: "margin-top: 4px;" } });
      this.renderDeadLinkTable(inner, section.items, t);
    } else {
      this.renderDeadLinkTable(body, section.items, t);
    }
  }
  renderDeadLinkTable(body, items, t) {
    var _a;
    const table = body.createEl("table", {
      attr: {
        style: "width: 100%; border-collapse: collapse; margin-top: 4px; font-size: 0.85em;"
      }
    });
    const thead = table.createEl("thead");
    const headRow = thead.createEl("tr");
    headRow.createEl("th", {
      text: t.historyDeadLinkSource,
      attr: { style: "text-align: left; padding: 4px 8px; color: var(--text-muted); font-weight: 500; font-size: 0.85em; border-bottom: 1px solid var(--background-modifier-border);" }
    });
    headRow.createEl("th", {
      text: "\u2192",
      attr: { style: "width: 24px; border-bottom: 1px solid var(--background-modifier-border);" }
    });
    headRow.createEl("th", {
      text: t.historyDeadLinkTarget,
      attr: { style: "text-align: left; padding: 4px 8px; color: var(--text-muted); font-weight: 500; font-size: 0.85em; border-bottom: 1px solid var(--background-modifier-border);" }
    });
    const tbody = table.createEl("tbody");
    for (const item of items) {
      const row = tbody.createEl("tr");
      row.createEl("td", {
        attr: { style: "padding: 3px 8px; border-bottom: 1px solid var(--background-modifier-border);" }
      });
      const sourceCell = row.cells[0];
      if (item.links[0]) {
        const sourceLink = sourceCell.createEl("a", {
          text: item.links[0].path,
          href: "#",
          attr: { style: "color: var(--text-accent); cursor: pointer; text-decoration: none;" }
        });
        sourceLink.addEventListener("click", (ev) => {
          ev.preventDefault();
          void this.app.workspace.openLinkText(item.links[0].path, this.logPath);
        });
      } else {
        sourceCell.textContent = item.raw.split("\u2192")[0].trim();
      }
      row.createEl("td", {
        text: "\u2192",
        attr: { style: "padding: 3px 8px; color: var(--text-muted); text-align: center; border-bottom: 1px solid var(--background-modifier-border);" }
      });
      row.createEl("td", {
        text: (_a = item.targetPage) != null ? _a : item.raw,
        attr: {
          style: "padding: 3px 8px; color: var(--text-error); border-bottom: 1px solid var(--background-modifier-border);"
        }
      });
    }
  }
  /** Tag violations rendered as a tight list with chip + warning color. */
  renderTagViolationSection(body, section, t) {
    const label = t.historySectionTagViolations.replace("{count}", String(section.items.length));
    this.renderSectionTitle(body, label, "medium");
    const list = body.createEl("ul", { attr: { style: "margin: 0; padding-left: 20px; list-style: none;" } });
    for (const item of section.items) {
      const li = list.createEl("li", {
        attr: { style: "margin: 2px 0; color: var(--text-warning);" }
      });
      if (item.links[0]) {
        const link = li.createEl("a", {
          text: item.links[0].path,
          href: "#",
          attr: { style: "color: var(--text-accent); cursor: pointer; text-decoration: none;" }
        });
        link.addEventListener("click", (ev) => {
          ev.preventDefault();
          void this.app.workspace.openLinkText(item.links[0].path, this.logPath);
        });
      }
      li.appendText(" \u2014 " + item.text);
    }
  }
  /** Orphan / empty sections: simple chip + one-line list. */
  renderSimpleListSection(body, section, labelText, t) {
    void t;
    const finalLabel = section.heading || labelText.replace("{count}", String(section.items.length));
    this.renderSectionTitle(body, finalLabel, "low");
    const list = body.createEl("ul", { attr: { style: "margin: 0; padding-left: 20px; list-style: none;" } });
    for (const item of section.items) {
      const li = list.createEl("li", { attr: { style: "margin: 2px 0;" } });
      if (item.links[0]) {
        const link = li.createEl("a", {
          text: item.links[0].path,
          href: "#",
          attr: { style: "color: var(--text-accent); cursor: pointer; text-decoration: none;" }
        });
        link.addEventListener("click", (ev) => {
          ev.preventDefault();
          void this.app.workspace.openLinkText(item.links[0].path, this.logPath);
        });
      }
      li.appendText(" \u2014 " + item.text);
    }
  }
  /**
   * LLM analysis: group items by type chip (矛盾/过时/缺失/结构), each in its
   * own collapsible group. This is the most info-dense section so we collapse
   * by default when >5 items.
   */
  renderLlmAnalysisSection(body, section, t) {
    var _a;
    const label = t.historySectionLlmAnalysis.replace("{count}", String(section.items.length));
    this.renderSectionTitle(body, label, "medium");
    const groups = /* @__PURE__ */ new Map();
    for (const item of section.items) {
      const key = (_a = item.llmType) != null ? _a : "other";
      const list = groups.get(key);
      if (list) list.push(item);
      else groups.set(key, [item]);
    }
    const chipLabel = (type) => {
      if (type.includes("\u77DB\u76FE") || type.toLowerCase().includes("contradiction")) {
        return { label: t.historyChipContradiction, color: "var(--text-error)" };
      }
      if (type.includes("\u8FC7\u65F6") || type.toLowerCase().includes("outdated")) {
        return { label: t.historyChipOutdated, color: "var(--text-warning)" };
      }
      if (type.includes("\u7F3A\u5931") || type.toLowerCase().includes("missing")) {
        return { label: t.historyChipMissing, color: "var(--text-accent)" };
      }
      if (type.includes("\u7ED3\u6784") || type.toLowerCase().includes("structure")) {
        return { label: t.historyChipStructure, color: "var(--text-muted)" };
      }
      return { label: type, color: "var(--text-muted)" };
    };
    const sortedTypes = [...groups.entries()].sort(([a], [b]) => a.localeCompare(b));
    for (const [type, items] of sortedTypes) {
      const { label: chipText, color } = chipLabel(type);
      const groupEl = body.createDiv({
        attr: { style: "margin: 6px 0 0 0; padding-left: 4px; border-left: 2px solid " + color + ";" }
      });
      const header = groupEl.createDiv({
        attr: { style: "display: flex; gap: 6px; align-items: center;" }
      });
      header.createEl("span", {
        text: chipText,
        attr: {
          style: `font-size: 0.7em; font-weight: 600; padding: 2px 8px; border-radius: 10px; background: ${color}; color: var(--text-on-accent);`
        }
      });
      header.createEl("span", {
        text: String(items.length),
        attr: { style: "font-size: 0.85em; color: var(--text-muted);" }
      });
      const itemsContainer = groupEl.createDiv({ attr: { style: "margin-top: 4px;" } });
      if (items.length > 5) {
        const det = itemsContainer.createEl("details");
        det.createEl("summary", {
          text: t.historyShowMoreItems.replace("{count}", String(items.length)),
          attr: { style: "cursor: pointer; color: var(--text-accent); user-select: none;" }
        });
        const inner = det.createDiv({ attr: { style: "margin-top: 4px; padding-left: 8px;" } });
        this.renderLlmItems(inner, items);
      } else {
        this.renderLlmItems(itemsContainer, items);
      }
    }
  }
  renderLlmItems(parent, items) {
    const list = parent.createEl("ul", {
      attr: { style: "margin: 0; padding-left: 16px; list-style: disc;" }
    });
    for (const item of items) {
      const li = list.createEl("li", {
        attr: { style: "margin: 3px 0; color: var(--text-muted); line-height: 1.4;" }
      });
      this.renderLineWithLinks(li, item.raw);
    }
  }
  // ── Ingest details ──
  /**
   * Ingest: group Created/Updated pages by page type (entity/concept/source)
   * with count chips. This replaces 30-line flat lists with a 3-row summary.
   */
  renderIngestDetails(body, entry, t) {
    if (entry.ingestMetrics && (typeof entry.ingestMetrics.durationSec === "number" || entry.ingestMetrics.model || typeof entry.ingestMetrics.sourceBytes === "number")) {
      this.renderIngestMetricCards(body, entry, t);
    }
    if (entry.time) {
      body.createEl("div", {
        text: `${t.historyIngestLatestTime}: ${entry.date} ${entry.time}`,
        attr: { style: "color: var(--text-muted); font-size: 0.8em; margin: 6px 0 4px 0;" }
      });
    } else {
      body.createEl("div", {
        text: `${t.historyIngestLatestTime}: ${entry.date} \xB7 ${t.historyIngestNoTimestamp}`,
        attr: { style: "color: var(--text-faint); font-size: 0.8em; margin: 6px 0 4px 0;" }
      });
    }
    if (entry.sourceTitle) {
      body.createEl("div", {
        text: `${t.historyIngestSource}: ${entry.sourceTitle}`,
        attr: { style: "color: var(--text-muted); font-size: 0.85em; margin-bottom: 6px;" }
      });
    }
    if (entry.createdPages.length > 0) {
      this.renderPageTypeGroup(body, entry.createdPages, t.historyEntrySectionCreated, t);
    }
    if (entry.updatedPages.length > 0) {
      this.renderPageTypeGroup(body, entry.updatedPages, t.historyEntrySectionUpdated, t);
    }
    if (entry.createdPages.length === 0 && entry.updatedPages.length === 0) {
      body.createEl("div", {
        text: t.historyEntryNoChanges,
        attr: { style: "color: var(--text-faint); margin-left: 8px;" }
      });
    }
  }
  /** Render ingest metric cards: total pages, duration, model, source size. */
  renderIngestMetricCards(body, entry, t) {
    var _a, _b, _c;
    const grid = body.createDiv({
      attr: {
        style: "display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 6px; margin: 4px 0 8px 0;"
      }
    });
    const cards = [];
    const total = entry.createdPages.length + entry.updatedPages.length;
    cards.push({ label: t.historyIngestTotal, value: String(total) });
    if (typeof ((_a = entry.ingestMetrics) == null ? void 0 : _a.durationSec) === "number") {
      cards.push({ label: t.historyKpiDuration, value: `${entry.ingestMetrics.durationSec}s` });
    }
    if ((_b = entry.ingestMetrics) == null ? void 0 : _b.model) {
      cards.push({ label: "model", value: entry.ingestMetrics.model });
    }
    if (typeof ((_c = entry.ingestMetrics) == null ? void 0 : _c.sourceBytes) === "number") {
      cards.push({ label: t.historyIngestSource, value: this.formatBytesShort(entry.ingestMetrics.sourceBytes) });
    }
    for (const c of cards) {
      const card = grid.createDiv({
        attr: {
          style: "padding: 6px 8px; border-radius: 4px; background: var(--background-primary); border: 1px solid var(--background-modifier-border);"
        }
      });
      card.createEl("div", {
        text: c.value,
        attr: {
          style: "font-size: 1em; font-weight: 600; line-height: 1.2; color: var(--text-normal); overflow: hidden; text-overflow: ellipsis; white-space: nowrap;"
        }
      });
      card.createEl("div", {
        text: c.label,
        attr: { style: "font-size: 0.7em; color: var(--text-muted); margin-top: 2px;" }
      });
    }
  }
  formatBytesShort(n) {
    if (n >= 1024 * 1024) return `${(n / 1024 / 1024).toFixed(1)}MB`;
    if (n >= 1024) return `${(n / 1024).toFixed(1)}KB`;
    return `${n}B`;
  }
  renderPageTypeGroup(body, pages, sectionLabel, t) {
    body.createEl("div", {
      text: `${sectionLabel} (${pages.length})`,
      attr: {
        style: "font-weight: 600; margin-top: 8px; margin-bottom: 4px; font-size: 0.9em;"
      }
    });
    const byType = /* @__PURE__ */ new Map();
    for (const p of pages) {
      const type = p.startsWith("entities/") ? "entity" : p.startsWith("concepts/") ? "concept" : p.startsWith("sources/") ? "source" : "other";
      const list = byType.get(type);
      if (list) list.push(p);
      else byType.set(type, [p]);
    }
    const order = ["entity", "concept", "source", "other"];
    for (const type of order) {
      const list = byType.get(type);
      if (!list || list.length === 0) continue;
      const labelText = type === "entity" ? t.historyPageTypeEntity : type === "concept" ? t.historyPageTypeConcept : type === "source" ? t.historyPageTypeSource : "other";
      const row = body.createDiv({
        attr: { style: "margin: 4px 0; display: flex; gap: 6px; align-items: flex-start;" }
      });
      row.createEl("span", {
        text: `${labelText} \xD7${list.length}`,
        attr: {
          style: "flex-shrink: 0; padding: 2px 8px; border-radius: 10px; background: var(--background-modifier-border); font-size: 0.7em; font-weight: 600; color: var(--text-muted);"
        }
      });
      const linksContainer = row.createDiv({
        attr: { style: "display: flex; flex-wrap: wrap; gap: 4px 10px;" }
      });
      for (const page of list) {
        const link = linksContainer.createEl("a", {
          text: page.replace(/^(entities|concepts|sources)\//, ""),
          href: "#",
          attr: { style: "color: var(--text-accent); cursor: pointer; text-decoration: none; font-size: 0.85em;" }
        });
        link.addEventListener("click", (ev) => {
          ev.preventDefault();
          void this.app.workspace.openLinkText(page, this.logPath);
        });
      }
    }
  }
  // ── Fix details ──
  renderFixDetails(body, entry, t) {
    body.createEl("div", {
      text: `${t.historyEntrySectionDetails} (${entry.details.length})`,
      attr: { style: "font-weight: 600; margin-bottom: 4px; font-size: 0.9em;" }
    });
    if (entry.details.length === 0 && entry.rawDetails) {
      body.createEl("div", {
        text: entry.rawDetails,
        attr: { style: "white-space: pre-wrap; color: var(--text-muted); margin-left: 8px;" }
      });
      return;
    }
    const list = body.createEl("ul", { attr: { style: "margin: 0; padding-left: 20px; list-style: disc;" } });
    for (const row of entry.details) {
      const li = list.createEl("li", { attr: { style: "margin: 2px 0; color: var(--text-muted);" } });
      this.renderLineWithLinks(li, row.raw);
    }
  }
  // ── Helpers ──
  renderSectionTitle(body, label, severity) {
    const color = severity === "high" ? "var(--text-error)" : severity === "medium" ? "var(--text-warning)" : severity === "low" ? "var(--text-accent)" : "var(--text-muted)";
    body.createEl("div", {
      text: label,
      attr: {
        style: `font-weight: 600; margin-top: 10px; margin-bottom: 4px; color: ${color}; font-size: 0.85em;`
      }
    });
  }
  renderOpenInLogLink(body, t) {
    const footer = body.createDiv({
      attr: {
        style: "margin-top: 12px; padding-top: 8px; border-top: 1px solid var(--background-modifier-border); display: flex; justify-content: flex-end;"
      }
    });
    const link = footer.createEl("a", {
      text: t.historyOpenInLog,
      href: "#",
      attr: {
        style: "font-size: 0.8em; color: var(--text-accent); cursor: pointer; text-decoration: none;"
      }
    });
    link.addEventListener("click", (ev) => {
      ev.preventDefault();
      void this.app.workspace.openLinkText(this.logPath, "");
    });
  }
  /** Render a raw line with `[[path]]` and `[[path|alias]]` markers replaced by clickable spans. */
  renderLineWithLinks(parent, rawLine) {
    var _a, _b;
    let cursor = 0;
    const re = /\[\[([^\]|#]+?)(?:#\^?[^\]|]+)?(?:\|([^\]]+))?\]\]/g;
    let m;
    while ((m = re.exec(rawLine)) !== null) {
      if (m.index > cursor) parent.appendText(rawLine.substring(cursor, m.index));
      const path = m[1].trim();
      const alias = (_b = (_a = m[2]) == null ? void 0 : _a.trim()) != null ? _b : path;
      const link = parent.createEl("a", {
        text: alias,
        href: "#",
        attr: { style: "color: var(--text-accent); cursor: pointer; text-decoration: none;" }
      });
      link.addEventListener("click", (ev) => {
        ev.preventDefault();
        void this.app.workspace.openLinkText(path, this.logPath);
      });
      cursor = m.index + m[0].length;
    }
    if (cursor < rawLine.length) parent.appendText(rawLine.substring(cursor));
  }
  renderCloseFooter(container, t) {
    const footer = container.createDiv({
      attr: {
        style: "display: flex; justify-content: flex-end; margin-top: 16px; padding-top: 12px; border-top: 1px solid var(--background-modifier-border);"
      }
    });
    const closeBtn = footer.createEl("button", {
      text: t.historyCloseButton,
      attr: { style: "padding: 6px 16px; border-radius: 4px; cursor: pointer;" }
    });
    closeBtn.addEventListener("click", () => this.close());
  }
};

// src/ui/settings-helpers.ts
function classifyFetchError(msg) {
  if (msg === "empty model list") return "Empty";
  if (/\b(401|403)\b/.test(msg) || /\bunauthor\w*|\bforbidden\b|\binvalid[_\s-]?(api[_\s-]?)?key\b|\binvalid[_\s-]?token\b|\bauth(entication|orization)?[_\s-]?fail/i.test(msg)) {
    return "Auth";
  }
  if (/\b(404|405|410|421)\b/.test(msg) || /\bnot[_\s-]?found\b|\bmethod[_\s-]?not[_\s-]?allowed\b/i.test(msg)) {
    return "Endpoint";
  }
  if (/\b5\d\d\b/.test(msg) || /\bserver[_\s-]?error\b|\bbad[_\s-]?gateway\b|\bservice[_\s-]?unavailable\b|\brate[_\s-]?limit/i.test(msg)) {
    return "Server";
  }
  return "Network";
}

// src/ui/tag-chip-input.ts
var DUPLICATE_FLASH_MS = 800;
function doc() {
  return activeDocument;
}
function createChild(parent, tag, opts = {}) {
  const el = doc().createElement(tag);
  if (opts.cls) el.className = opts.cls;
  if (opts.text !== void 0) el.textContent = opts.text;
  if (opts.attr) {
    for (const [k, v] of Object.entries(opts.attr)) el.setAttribute(k, v);
  }
  parent.appendChild(el);
  return el;
}
var TagChipInputComponent = class {
  constructor(opts) {
    this.disabled = false;
    this.tags = [];
    /** Tracks whether the user has ever edited (used to suppress the
     *  initial-load onChange from firing when fallback defaults are
     *  materialized into real chips). */
    this.userEdited = false;
    this.opts = opts;
    this.controlEl = opts.controlEl;
    this.controlEl.replaceChildren();
    this.controlEl.classList.add("llm-wiki-tag-container");
    this.controlEl.setAttribute("role", "group");
    this.controlEl.setAttribute("aria-label", opts.ariaLabel);
    this.inputEl = createChild(this.controlEl, "input", {
      cls: "llm-wiki-tag-input",
      attr: { type: "text", "aria-label": opts.ariaLabel }
    });
    if (opts.placeholder) this.inputEl.placeholder = opts.placeholder;
    this.liveRegionEl = createChild(this.controlEl, "div", {
      cls: "llm-wiki-tag-sr-live",
      attr: { "aria-live": "polite", "aria-atomic": "true" }
    });
    this.inputEl.addEventListener("keydown", (e) => this.handleKeydown(e));
    this.setValue(opts.initialTags || "");
  }
  // Make TS happy with the `then` chain pattern in the BaseComponent
  // interface; the real one just returns `this`.
  then(_cb) {
    return this;
  }
  setDisabled(disabled) {
    this.disabled = disabled;
    return this;
  }
  getValue() {
    return this.tags.join(", ");
  }
  getTags() {
    return [...this.tags];
  }
  setValue(csv) {
    var _a;
    const seen = /* @__PURE__ */ new Set();
    const next = [];
    const source = csv ? csv.split(",") : (_a = this.opts.defaultTags) != null ? _a : [];
    for (const raw of source) {
      const trimmed = raw.trim();
      if (!trimmed) continue;
      const key = trimmed.toLowerCase();
      if (seen.has(key)) continue;
      seen.add(key);
      next.push(trimmed);
    }
    this.tags = next;
    this.userEdited = !!csv;
    this.renderChips();
  }
  addTag(tag) {
    const trimmed = tag.trim();
    if (!trimmed) return false;
    const key = trimmed.toLowerCase();
    if (this.tags.some((t) => t.toLowerCase() === key)) {
      this.flashDuplicate(trimmed);
      return false;
    }
    this.tags.push(trimmed);
    this.appendChip(trimmed);
    this.emitChange();
    return true;
  }
  removeTag(tag) {
    var _a;
    const idx = this.tags.findIndex((t) => t.toLowerCase() === tag.toLowerCase());
    if (idx === -1) return false;
    this.tags.splice(idx, 1);
    const chips = this.controlEl.querySelectorAll(".llm-wiki-tag-chip");
    (_a = chips[idx]) == null ? void 0 : _a.remove();
    this.emitChange();
    return true;
  }
  handleKeydown(e) {
    if (e.isComposing) return;
    if (e.key === "Enter" || e.key === "," || e.key === ";") {
      const value = this.inputEl.value;
      if (!value.trim()) {
        e.preventDefault();
        return;
      }
      e.preventDefault();
      this.addTag(value);
      this.inputEl.value = "";
      return;
    }
    if (e.key === "Backspace" && this.inputEl.value === "" && this.tags.length > 0) {
      e.preventDefault();
      const last = this.tags[this.tags.length - 1];
      this.removeTag(last);
      return;
    }
    if (e.key === "Escape") {
      this.inputEl.blur();
    }
  }
  renderChips() {
    const existing = Array.from(this.controlEl.querySelectorAll(".llm-wiki-tag-chip"));
    for (const el of existing) el.remove();
    for (const tag of this.tags) this.appendChip(tag);
  }
  appendChip(tag) {
    const chip = createChild(this.controlEl, "div", {
      cls: "llm-wiki-tag-chip",
      attr: { "data-tag": tag }
    });
    createChild(chip, "span", { cls: "llm-wiki-tag-chip-label", text: tag });
    const remove = createChild(chip, "button", {
      cls: "llm-wiki-tag-chip-remove",
      attr: { "aria-label": `Remove ${tag}`, type: "button" },
      text: "\xD7"
    });
    remove.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.removeTag(tag);
    });
    this.controlEl.insertBefore(chip, this.inputEl);
  }
  flashDuplicate(tag) {
    var _a;
    this.controlEl.classList.add("llm-wiki-tag-container--duplicate");
    this.liveRegionEl.textContent = `${(_a = this.opts.duplicateHint) != null ? _a : "Duplicate"} (${tag})`;
    window.setTimeout(() => {
      this.controlEl.classList.remove("llm-wiki-tag-container--duplicate");
    }, DUPLICATE_FLASH_MS);
  }
  emitChange() {
    this.opts.onChange(this.getValue());
  }
};

// src/ui/settings.ts
var LLMWikiSettingTab = class extends import_obsidian5.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
    this.tempSettings = {
      ...plugin.settings,
      watchedFolders: [...plugin.settings.watchedFolders || []]
    };
  }
  // Issue #137: merge tempSettings → plugin.settings, preserving any
  // fields the form does not track but Test Connection mutates
  // (currently: thinkingControlCache). Called by hide() (auto-save) and
  // the explicit Save button. Adding a new probe-mutated field only
  // requires extending this one helper, not every save site.
  commitTempSettings() {
    this.plugin.settings = {
      ...this.tempSettings,
      watchedFolders: [...this.tempSettings.watchedFolders || []],
      thinkingControlCache: this.plugin.settings.thinkingControlCache
    };
  }
  // Auto-save when user navigates away from settings tab
  hide() {
    const hasChanges = JSON.stringify(this.tempSettings) !== JSON.stringify(this.plugin.settings);
    if (hasChanges) {
      this.commitTempSettings();
      void this.plugin.saveSettings();
      console.debug("Settings auto-saved on tab close");
    }
  }
  getText(key) {
    var _a, _b;
    const texts = TEXTS[this.tempSettings.language];
    return (_b = (_a = texts[key]) != null ? _a : TEXTS.en[key]) != null ? _b : key;
  }
  /**
   * Check if wiki folder structure exists (entities, concepts, sources, schema).
   * Uses IO inspection — no persistent flag.
   */
  isWikiInitialized() {
    const wikiFolder = this.tempSettings.wikiFolder || "wiki";
    return !!(this.app.vault.getAbstractFileByPath(`${wikiFolder}/entities`) && this.app.vault.getAbstractFileByPath(`${wikiFolder}/concepts`) && this.app.vault.getAbstractFileByPath(`${wikiFolder}/sources`) && this.app.vault.getAbstractFileByPath(`${wikiFolder}/schema`));
  }
  display() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian5.Setting(containerEl).setName(this.getText("languageTitle")).setDesc(this.getText("languageDesc")).addDropdown((dropdown) => {
      for (const [key, label] of Object.entries(WIKI_LANGUAGES)) {
        dropdown.addOption(key, label);
      }
      dropdown.setValue(this.tempSettings.language);
      dropdown.onChange((value) => {
        this.tempSettings.language = value;
        this.display();
      });
    }).addButton((button) => button.setButtonText(this.getText("saveButton")).setCta().onClick(() => {
      void (async () => {
        this.commitTempSettings();
        await this.plugin.saveSettings();
        new import_obsidian5.Notice(this.getText("savedNotice"), NOTICE_SHORT);
      })();
    }));
    new import_obsidian5.Setting(containerEl).setName(this.getText("wikiLanguageName")).setHeading();
    new import_obsidian5.Setting(containerEl).setName(this.getText("wikiLanguageName")).setDesc(this.getText("wikiLanguageDesc")).addDropdown((dropdown) => {
      for (const [key, label] of Object.entries(WIKI_LANGUAGES)) dropdown.addOption(key, label);
      dropdown.addOption("__custom__", this.getText("customWikiLanguageOption"));
      if (this.tempSettings.useCustomWikiLanguage) {
        dropdown.setValue("__custom__");
      } else {
        const currentLang = this.tempSettings.wikiLanguage;
        if (currentLang && WIKI_LANGUAGES[currentLang]) dropdown.setValue(currentLang);
        else if (currentLang) dropdown.setValue("__custom__");
        else dropdown.setValue("en");
      }
      dropdown.onChange((value) => {
        if (value === "__custom__") {
          this.tempSettings.useCustomWikiLanguage = true;
          if (WIKI_LANGUAGES[this.tempSettings.wikiLanguage || ""]) {
            this.tempSettings.wikiLanguage = "";
          }
        } else {
          this.tempSettings.wikiLanguage = value;
          this.tempSettings.useCustomWikiLanguage = false;
        }
        this.display();
      });
    });
    if (this.tempSettings.useCustomWikiLanguage) {
      new import_obsidian5.Setting(containerEl).setName(this.getText("wikiLanguageName") + " (Custom)").setDesc(this.getText("customWikiLanguageHint")).addText((text) => text.setPlaceholder(this.getText("customWikiLanguagePlaceholder")).setValue(this.tempSettings.wikiLanguage && !WIKI_LANGUAGES[this.tempSettings.wikiLanguage] ? this.tempSettings.wikiLanguage : "").onChange((value) => {
        this.tempSettings.wikiLanguage = value.trim();
      }));
    }
    const readyStatus = this.tempSettings.llmReady ? "\u2705 " + (this.getText("statusReady") || "Ready") : "\u26A0\uFE0F " + (this.getText("statusNotReady") || "Not configured \u2014 please complete setup below");
    const clientStatus = this.plugin.llmClient ? this.getText("statusInitialized") : this.getText("statusNotInitialized");
    const wikiInitCheck = this.isWikiInitialized();
    const wikiInitStatus = wikiInitCheck ? "\u2705 " + (this.getText("wikiInitStatusReady") || "Wiki initialized") : "\u26A0\uFE0F " + (this.getText("wikiInitStatusNotReady") || "Wiki not initialized \u2014 will auto-create on first ingestion");
    new import_obsidian5.Setting(containerEl).setName(this.getText("llmWikiStatusSection")).setDesc(`${readyStatus} | ${clientStatus}  \u2022  ${wikiInitStatus}`);
    new import_obsidian5.Setting(containerEl).setName(this.getText("providerSection")).setHeading();
    const providerConfig = PREDEFINED_PROVIDERS[this.tempSettings.provider];
    const isOllama = this.tempSettings.provider === "ollama";
    const isLmStudio = this.tempSettings.provider === "lmstudio";
    new import_obsidian5.Setting(containerEl).setName(this.getText("providerName")).setDesc(this.getText("providerDesc")).addDropdown((dropdown) => {
      Object.values(PREDEFINED_PROVIDERS).forEach((config) => {
        const lang = this.tempSettings.language;
        const displayName = lang === "zh" ? config.nameZh : config.nameEn;
        dropdown.addOption(config.id, displayName);
      });
      dropdown.setValue(this.tempSettings.provider);
      dropdown.onChange((value) => {
        this.tempSettings.provider = value;
        this.tempSettings.llmReady = false;
        this.tempSettings.availableModels = [];
        this.tempSettings.useCustomModel = false;
        this.tempSettings.model = "";
        const config = PREDEFINED_PROVIDERS[value];
        if (config && value !== "custom") this.tempSettings.baseUrl = config.baseUrl;
        this.display();
      });
    });
    if (!isOllama && !isLmStudio) {
      new import_obsidian5.Setting(containerEl).setName(this.getText("apiKeyName")).setDesc(this.getText("apiKeyDesc")).addText((text) => {
        text.setPlaceholder(this.getText("apiKeyPlaceholder")).setValue(this.tempSettings.apiKey).onChange((value) => {
          this.tempSettings.apiKey = value;
          this.tempSettings.llmReady = false;
        });
        text.inputEl.type = "password";
      });
    } else if (isLmStudio) {
      containerEl.createEl("p", {
        text: this.getText("lmstudioHint"),
        cls: "llm-wiki-ollama-hint"
      });
    } else {
      containerEl.createEl("p", {
        text: this.getText("ollamaHint"),
        cls: "llm-wiki-ollama-hint"
      });
    }
    if (this.tempSettings.provider === "custom" || this.tempSettings.provider === "anthropic-compatible" || providerConfig && this.tempSettings.baseUrl !== providerConfig.baseUrl) {
      new import_obsidian5.Setting(containerEl).setName(this.getText("baseUrlName")).setDesc(this.tempSettings.provider === "custom" || this.tempSettings.provider === "anthropic-compatible" ? this.getText("baseUrlDescCustom") : this.getText("baseUrlDescOverride")).addText((text) => text.setPlaceholder((providerConfig == null ? void 0 : providerConfig.baseUrl) || "https://api.example.com/v1").setValue(this.tempSettings.baseUrl).onChange((value) => {
        this.tempSettings.baseUrl = value;
        this.tempSettings.llmReady = false;
      }));
    }
    const concurrencyValue = (_a = this.tempSettings.pageGenerationConcurrency) != null ? _a : 3;
    const concurrencyDesc = concurrencyValue === 1 ? this.getText("concurrencyValueSingular").replace("{}", String(concurrencyValue)) : this.getText("concurrencyValuePlural").replace("{}", String(concurrencyValue));
    let concurrencySetting;
    new import_obsidian5.Setting(containerEl).setName(this.getText("pageGenerationConcurrencyName")).setDesc(this.getText("pageGenerationConcurrencyDesc") + " " + concurrencyDesc).addSlider((slider) => slider.setLimits(1, 5, 1).setValue(concurrencyValue).setDynamicTooltip().onChange((value) => {
      this.tempSettings.pageGenerationConcurrency = value;
      const desc = value === 1 ? this.getText("concurrencyValueSingular").replace("{}", String(value)) : this.getText("concurrencyValuePlural").replace("{}", String(value));
      concurrencySetting.setDesc(this.getText("pageGenerationConcurrencyDesc") + " " + desc);
    })).then((s) => {
      concurrencySetting = s;
    });
    const batchDelayValue = (_b = this.tempSettings.batchDelayMs) != null ? _b : 300;
    let batchDelaySetting;
    new import_obsidian5.Setting(containerEl).setName(this.getText("batchDelayName")).setDesc(this.getText("batchDelayDesc").replace("{}", String(batchDelayValue))).addSlider((slider) => slider.setLimits(100, 2e3, 50).setValue(batchDelayValue).setDynamicTooltip().onChange((value) => {
      this.tempSettings.batchDelayMs = value;
      batchDelaySetting.setDesc(this.getText("batchDelayDesc").replace("{}", String(value)));
    })).then((s) => {
      batchDelaySetting = s;
    });
    new import_obsidian5.Setting(containerEl).setName(this.getText("modelSection")).setHeading();
    new import_obsidian5.Setting(containerEl).setName(this.getText("fetchModelsName")).setDesc(this.getText("fetchModelsDesc")).addButton((button) => button.setButtonText(this.getText("fetchModelsButton")).onClick(async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2;
      button.setButtonText(this.getText("fetchingModels"));
      button.setDisabled(true);
      try {
        const apiKey = isOllama ? "ollama" : this.tempSettings.apiKey.trim();
        const baseUrl = ((_a2 = this.tempSettings.baseUrl) == null ? void 0 : _a2.trim()) || (providerConfig == null ? void 0 : providerConfig.baseUrl) || void 0;
        const getModelFilter = (provider) => {
          if (provider === "openrouter") {
            return (id) => !id.includes(":");
          } else if (provider === "ollama") {
            return (id) => !id.includes("/");
          } else {
            return (id) => !id.includes(":") && !id.includes("/");
          }
        };
        const modelFilter = getModelFilter(this.tempSettings.provider);
        if (this.tempSettings.provider === "anthropic" || this.tempSettings.provider === "anthropic-compatible") {
          if (this.tempSettings.provider === "anthropic-compatible" && ((_b2 = this.tempSettings.baseUrl) == null ? void 0 : _b2.trim())) {
            const rawBase = this.tempSettings.baseUrl.trim();
            const cleanBase = rawBase.replace(/\/v1\/?$/, "").replace(/\/+$/, "");
            const modelsUrl = cleanBase + "/v1/models";
            try {
              const response = await (0, import_obsidian5.requestUrl)({ url: modelsUrl, headers: { "x-api-key": apiKey, "Anthropic-Version": "2023-06-01" } });
              if (response.status >= 200 && response.status < 300) {
                const data = response.json;
                if ((_c2 = data.data) == null ? void 0 : _c2.length) {
                  this.tempSettings.availableModels = data.data.map((m) => m.id).filter(modelFilter).sort();
                } else throw new Error("empty model list");
              } else throw new Error(`HTTP ${response.status}`);
            } catch (e) {
              const altUrl = cleanBase + "/models";
              const altResponse = await (0, import_obsidian5.requestUrl)({ url: altUrl, headers: { "Authorization": `Bearer ${apiKey}` } });
              if (altResponse.status >= 200 && altResponse.status < 300) {
                const altData = altResponse.json;
                if ((_d2 = altData.data) == null ? void 0 : _d2.length) {
                  this.tempSettings.availableModels = altData.data.map((m) => m.id).filter(modelFilter).sort();
                } else throw new Error("empty model list");
              } else throw new Error(`HTTP ${altResponse.status}`);
            }
          } else {
            const response = await (0, import_obsidian5.requestUrl)({
              url: "https://api.anthropic.com/v1/models",
              headers: { "x-api-key": apiKey, "Anthropic-Version": "2023-06-01" }
            });
            if (response.status >= 200 && response.status < 300) {
              const data = response.json;
              if ((_e2 = data.data) == null ? void 0 : _e2.length) {
                this.tempSettings.availableModels = data.data.map((m) => m.id).filter(modelFilter).sort();
              } else throw new Error("empty model list");
            } else throw new Error(`HTTP ${response.status}`);
          }
        } else {
          const modelsUrl = (baseUrl || "https://api.openai.com/v1") + "/models";
          const response = await (0, import_obsidian5.requestUrl)({
            url: modelsUrl,
            method: "GET",
            headers: { "Authorization": `Bearer ${apiKey}` }
          });
          const data = response.json;
          if ((_f2 = data.data) == null ? void 0 : _f2.length) {
            this.tempSettings.availableModels = data.data.map((m) => m.id).filter(modelFilter).sort().slice(0, 100);
          } else throw new Error("empty model list");
        }
        if (this.tempSettings.availableModels.length > 0) {
          new import_obsidian5.Notice(this.getText("fetchSuccess").replace("{}", this.tempSettings.availableModels.length.toString()), NOTICE_NORMAL);
          if (!this.tempSettings.model || !this.tempSettings.availableModels.includes(this.tempSettings.model)) {
            this.tempSettings.model = this.tempSettings.availableModels[0];
          }
          this.tempSettings.useCustomModel = false;
        } else {
          new import_obsidian5.Notice(this.getText("fetchFailed"), NOTICE_NORMAL);
          this.tempSettings.useCustomModel = true;
        }
        this.display();
      } catch (error) {
        const errorMsg = error instanceof Error ? error.message : String(error);
        const category = classifyFetchError(errorMsg);
        new import_obsidian5.Notice(this.getText(`fetchError${category}`), NOTICE_ERROR);
        this.tempSettings.useCustomModel = true;
        this.tempSettings.availableModels = [];
        this.display();
      }
      button.setButtonText(this.getText("fetchModelsButton"));
      button.setDisabled(false);
    }));
    if (this.tempSettings.availableModels && this.tempSettings.availableModels.length > 0 && !this.tempSettings.useCustomModel) {
      new import_obsidian5.Setting(containerEl).setName(this.getText("selectModelName")).setDesc(this.getText("selectModelDesc").replace("{}", this.tempSettings.availableModels.length.toString())).addDropdown((dropdown) => {
        (this.tempSettings.availableModels || []).forEach((model) => {
          dropdown.addOption(model, model);
        });
        dropdown.addOption("__custom__", this.getText("customInputOption"));
        dropdown.setValue(this.tempSettings.model);
        dropdown.onChange((value) => {
          if (value === "__custom__") {
            this.tempSettings.useCustomModel = true;
            this.display();
          } else {
            this.tempSettings.model = value;
            this.tempSettings.useCustomModel = false;
          }
        });
      });
    } else {
      const useDropdown = ((_d = (_c = this.tempSettings.availableModels) == null ? void 0 : _c.length) != null ? _d : 0) > 0;
      new import_obsidian5.Setting(containerEl).setName(this.getText("modelName")).setDesc(((_e = this.tempSettings.availableModels) == null ? void 0 : _e.length) ? this.getText("modelDescCustom") : this.getText("modelDescFetchFailed")).addText((text) => text.setPlaceholder(this.getText("modelInputPlaceholder")).setValue(this.tempSettings.model).onChange((value) => {
        this.tempSettings.model = value;
      })).addExtraButton((button) => {
        if (useDropdown) {
          button.setIcon("list").setTooltip(this.getText("useDropdownButton")).onClick(() => {
            this.tempSettings.useCustomModel = false;
            this.display();
          });
        }
      });
    }
    const localLikeProviders = ["ollama", "lmstudio", "custom", "anthropic-compatible"];
    if (localLikeProviders.includes(this.tempSettings.provider)) {
      const tokenOptions = [0, 4096, 8192, 16384, 32768, 65536, 131072, 262144, 524288, 1048576];
      const tokenLabels = ["0 (No limit)", "4K", "8K", "16K", "32K", "64K", "128K", "256K", "512K", "1M"];
      const currentVal = (_f = this.tempSettings.maxTokensPerCall) != null ? _f : 0;
      new import_obsidian5.Setting(containerEl).setName(this.getText("maxTokensPerCallName")).setDesc(this.getText("maxTokensPerCallDesc")).addDropdown((dropdown) => {
        tokenOptions.forEach((val, idx) => {
          dropdown.addOption(String(val), tokenLabels[idx]);
        });
        dropdown.setValue(String(currentVal));
        dropdown.onChange((value) => {
          this.tempSettings.maxTokensPerCall = parseInt(value);
        });
      });
    }
    new import_obsidian5.Setting(containerEl).setName(this.getText("advancedSettingsModeName")).setDesc(this.getText("advancedSettingsModeDesc")).addDropdown((dropdown) => {
      dropdown.addOption("default", this.getText("advancedSettingsDefault")).addOption("custom", this.getText("advancedSettingsCustom")).setValue(this.tempSettings.advancedSettingsMode || "default").onChange((value) => {
        this.tempSettings.advancedSettingsMode = value;
        if (value === "default") {
          this.tempSettings.disableThinking = false;
          this.tempSettings.extractionTemperature = void 0;
          this.tempSettings.chatTemperature = void 0;
          this.tempSettings.repetitionPenalty = void 0;
        }
        this.display();
      });
    });
    if (this.tempSettings.advancedSettingsMode === "custom") {
      new import_obsidian5.Setting(containerEl).setName(this.getText("disableThinkingName")).setDesc(this.getText("disableThinkingDesc")).addToggle((toggle) => toggle.setValue(this.tempSettings.disableThinking === true).onChange((value) => {
        this.tempSettings.disableThinking = value;
      }));
      new import_obsidian5.Setting(containerEl).setName(this.getText("extractionTemperatureName")).setDesc(this.getText("extractionTemperatureDesc")).addText((text) => {
        var _a2, _b2;
        text.setPlaceholder(this.getText("temperaturePlaceholder")).setValue((_b2 = (_a2 = this.tempSettings.extractionTemperature) == null ? void 0 : _a2.toString()) != null ? _b2 : "").onChange((value) => {
          const trimmed = value.trim();
          if (trimmed === "") {
            this.tempSettings.extractionTemperature = void 0;
          } else {
            const parsed = parseFloat(trimmed);
            if (!isNaN(parsed)) {
              this.tempSettings.extractionTemperature = parsed;
            }
          }
        });
        text.inputEl.type = "number";
        text.inputEl.min = "0";
        text.inputEl.max = "2";
        text.inputEl.step = "0.05";
        text.inputEl.classList.add("llm-wiki-number-input");
      });
      new import_obsidian5.Setting(containerEl).setName(this.getText("chatTemperatureName")).setDesc(this.getText("chatTemperatureDesc")).addText((text) => {
        var _a2, _b2;
        text.setPlaceholder(this.getText("temperaturePlaceholder")).setValue((_b2 = (_a2 = this.tempSettings.chatTemperature) == null ? void 0 : _a2.toString()) != null ? _b2 : "").onChange((value) => {
          const trimmed = value.trim();
          if (trimmed === "") {
            this.tempSettings.chatTemperature = void 0;
          } else {
            const parsed = parseFloat(trimmed);
            if (!isNaN(parsed)) {
              this.tempSettings.chatTemperature = parsed;
            }
          }
        });
        text.inputEl.type = "number";
        text.inputEl.min = "0";
        text.inputEl.max = "2";
        text.inputEl.step = "0.05";
        text.inputEl.classList.add("llm-wiki-number-input");
      });
      new import_obsidian5.Setting(containerEl).setName(this.getText("repetitionPenaltyName")).setDesc(this.getText("repetitionPenaltyDesc")).addText((text) => {
        var _a2, _b2;
        text.setPlaceholder(this.getText("temperaturePlaceholder")).setValue((_b2 = (_a2 = this.tempSettings.repetitionPenalty) == null ? void 0 : _a2.toString()) != null ? _b2 : "").onChange((value) => {
          const trimmed = value.trim();
          if (trimmed === "") {
            this.tempSettings.repetitionPenalty = void 0;
          } else {
            const parsed = parseFloat(trimmed);
            if (!isNaN(parsed)) {
              this.tempSettings.repetitionPenalty = parsed;
            }
          }
        });
        text.inputEl.type = "number";
        text.inputEl.min = "0";
        text.inputEl.max = "2";
        text.inputEl.step = "0.05";
        text.inputEl.classList.add("llm-wiki-number-input");
      });
    }
    new import_obsidian5.Setting(containerEl).setName(this.getText("testConnectionName")).setDesc(this.getText("testConnectionDesc")).addButton((button) => button.setButtonText(this.getText("testButton")).onClick(async () => {
      var _a2, _b2;
      button.setButtonText(this.getText("testing"));
      button.setDisabled(true);
      const testSettings = { ...this.tempSettings };
      const oldSettings = this.plugin.settings;
      this.plugin.settings = testSettings;
      this.plugin.initializeLLMClient();
      (_a2 = this.plugin.wikiEngine) == null ? void 0 : _a2.updateSettings(testSettings);
      const result = await this.plugin.testLLMConnection();
      if (!result.success) {
        this.plugin.settings = oldSettings;
        this.plugin.initializeLLMClient();
        (_b2 = this.plugin.wikiEngine) == null ? void 0 : _b2.updateSettings(oldSettings);
        await this.plugin.saveSettings();
      } else {
        this.tempSettings.thinkingControlCache = this.plugin.settings.thinkingControlCache;
      }
      this.tempSettings.llmReady = result.success;
      button.setButtonText(this.getText("testButton"));
      button.setDisabled(false);
      this.display();
      new import_obsidian5.Notice(result.message, result.success ? NOTICE_NORMAL : NOTICE_ERROR);
    }));
    new import_obsidian5.Setting(containerEl).setName(this.getText("wikiSection")).setHeading();
    new import_obsidian5.Setting(containerEl).setName(this.getText("wikiFolderName")).setDesc(this.getText("wikiFolderDesc")).addText((text) => text.setPlaceholder(this.getText("wikiFolderPlaceholder")).setValue(this.tempSettings.wikiFolder).onChange((value) => {
      this.tempSettings.wikiFolder = value;
    }));
    new import_obsidian5.Setting(containerEl).setName(this.getText("slugCaseName")).setDesc(this.getText("slugCaseDesc")).addDropdown((dropdown) => {
      dropdown.addOption("lower", this.getText("slugCaseLower"));
      dropdown.addOption("preserve", this.getText("slugCasePreserve"));
      dropdown.setValue(this.tempSettings.slugCase || "lower");
      dropdown.onChange((value) => {
        this.tempSettings.slugCase = value;
      });
    });
    let customEntitySetting = null;
    let customConceptSetting = null;
    const updateCustomVisibility = (value) => {
      const isCustom = value === "custom";
      if (customEntitySetting) {
        customEntitySetting.settingEl.style.display = isCustom ? "flex" : "none";
      }
      if (customConceptSetting) {
        customConceptSetting.settingEl.style.display = isCustom ? "flex" : "none";
      }
    };
    new import_obsidian5.Setting(containerEl).setName(this.getText("extractionGranularityName")).setDesc(this.getText("extractionGranularityDesc")).addDropdown((dropdown) => {
      dropdown.addOption("fine", this.getText("extractionGranularityFine"));
      dropdown.addOption("standard", this.getText("extractionGranularityStandard"));
      dropdown.addOption("coarse", this.getText("extractionGranularityCoarse"));
      dropdown.addOption("minimal", this.getText("extractionGranularityMinimal"));
      dropdown.addOption("custom", this.getText("extractionGranularityCustom"));
      dropdown.setValue(this.tempSettings.extractionGranularity || "standard");
      dropdown.onChange((value) => {
        this.tempSettings.extractionGranularity = value;
        updateCustomVisibility(value);
      });
    });
    customEntitySetting = new import_obsidian5.Setting(containerEl).setName(this.getText("customEntityLimitName")).setDesc(this.getText("customEntityLimitDesc")).addText((text) => {
      var _a2;
      text.setPlaceholder("5").setValue(String((_a2 = this.tempSettings.customEntityLimit) != null ? _a2 : 5)).onChange((value) => {
        const parsed = parseInt(value);
        if (parsed > CUSTOM_LIMIT_MAX) {
          this.tempSettings.customEntityLimit = CUSTOM_LIMIT_MAX;
          text.setValue(String(CUSTOM_LIMIT_MAX));
          new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", String(CUSTOM_LIMIT_MAX)), NOTICE_SHORT);
        } else if (parsed < CUSTOM_LIMIT_MIN) {
          this.tempSettings.customEntityLimit = CUSTOM_LIMIT_MIN;
          text.setValue(String(CUSTOM_LIMIT_MIN));
          new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", String(CUSTOM_LIMIT_MIN)), NOTICE_SHORT);
        } else if (!isNaN(parsed)) {
          this.tempSettings.customEntityLimit = parsed;
        }
      });
      text.inputEl.type = "number";
      text.inputEl.min = String(CUSTOM_LIMIT_MIN);
      text.inputEl.max = String(CUSTOM_LIMIT_MAX);
      text.inputEl.classList.add("llm-wiki-number-input");
    });
    customEntitySetting.settingEl.style.display = this.tempSettings.extractionGranularity === "custom" ? "flex" : "none";
    customConceptSetting = new import_obsidian5.Setting(containerEl).setName(this.getText("customConceptLimitName")).setDesc(this.getText("customConceptLimitDesc")).addText((text) => {
      var _a2;
      text.setPlaceholder("5").setValue(String((_a2 = this.tempSettings.customConceptLimit) != null ? _a2 : 5)).onChange((value) => {
        const parsed = parseInt(value);
        if (parsed > CUSTOM_LIMIT_MAX) {
          this.tempSettings.customConceptLimit = CUSTOM_LIMIT_MAX;
          text.setValue(String(CUSTOM_LIMIT_MAX));
          new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", String(CUSTOM_LIMIT_MAX)), NOTICE_SHORT);
        } else if (parsed < CUSTOM_LIMIT_MIN) {
          this.tempSettings.customConceptLimit = CUSTOM_LIMIT_MIN;
          text.setValue(String(CUSTOM_LIMIT_MIN));
          new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", String(CUSTOM_LIMIT_MIN)), NOTICE_SHORT);
        } else if (!isNaN(parsed)) {
          this.tempSettings.customConceptLimit = parsed;
        }
      });
      text.inputEl.type = "number";
      text.inputEl.min = String(CUSTOM_LIMIT_MIN);
      text.inputEl.max = String(CUSTOM_LIMIT_MAX);
      text.inputEl.classList.add("llm-wiki-number-input");
    });
    customConceptSetting.settingEl.style.display = this.tempSettings.extractionGranularity === "custom" ? "flex" : "none";
    let customEntityTagsSetting = null;
    let customConceptTagsSetting = null;
    const updateTagVocabularyVisibility = (mode) => {
      const isCustom = mode === "custom";
      if (customEntityTagsSetting) {
        customEntityTagsSetting.settingEl.style.display = isCustom ? "flex" : "none";
      }
      if (customConceptTagsSetting) {
        customConceptTagsSetting.settingEl.style.display = isCustom ? "flex" : "none";
      }
    };
    const customEntities = ((_g = this.tempSettings.customEntityTags) != null ? _g : "").trim();
    const customConcepts = ((_h = this.tempSettings.customConceptTags) != null ? _h : "").trim();
    const hasCustomInput = customEntities.length > 0 || customConcepts.length > 0;
    const effectiveEntityTags = customEntities.length > 0 ? customEntities.split(",").map((t) => t.trim()).filter(Boolean) : VALID_ENTITY_TAGS;
    const effectiveConceptTags = customConcepts.length > 0 ? customConcepts.split(",").map((t) => t.trim()).filter(Boolean) : VALID_CONCEPT_TAGS;
    const effectiveListDesc = hasCustomInput ? `${effectiveEntityTags.join(", ")} (entities) / ${effectiveConceptTags.join(", ")} (concepts)${this.tempSettings.tagVocabularyMode === "default" ? " \u2014 custom values shown above (toggle to Custom to activate)" : ""}` : `${VALID_ENTITY_TAGS.join(", ")} (entities) / ${VALID_CONCEPT_TAGS.join(", ")} (concepts)`;
    const leadDesc = this.getText("tagVocabularyInlineDesc");
    const modeDesc = this.tempSettings.tagVocabularyMode === "custom" ? `${leadDesc}
${this.getText("tagVocabularyModeDescCustom")}` : `${leadDesc}
${this.getText("tagVocabularyModeDescDefault").replace("{}", effectiveListDesc)}`;
    new import_obsidian5.Setting(containerEl).setName(this.getText("tagVocabularyModeName")).setDesc(modeDesc).addDropdown((dropdown) => {
      dropdown.addOption("default", this.getText("tagVocabularyModeDefault")).addOption("custom", this.getText("tagVocabularyModeCustom")).setValue(this.tempSettings.tagVocabularyMode || "default").onChange((value) => {
        this.tempSettings.tagVocabularyMode = value;
        this.display();
      });
    });
    customEntityTagsSetting = new import_obsidian5.Setting(containerEl).setName(this.getText("customEntityTagsName")).setDesc(this.getText("customEntityTagsDesc")).addComponent((el) => new TagChipInputComponent({
      controlEl: el,
      initialTags: this.tempSettings.customEntityTags || "",
      placeholder: this.getText("customEntityTagsPlaceholder"),
      ariaLabel: this.getText("customEntityTagsName"),
      duplicateHint: this.getText("chipDuplicateHint"),
      defaultTags: VALID_ENTITY_TAGS,
      onChange: (csv) => {
        this.tempSettings.customEntityTags = csv;
      }
    }));
    customConceptTagsSetting = new import_obsidian5.Setting(containerEl).setName(this.getText("customConceptTagsName")).setDesc(this.getText("customConceptTagsDesc")).addComponent((el) => new TagChipInputComponent({
      controlEl: el,
      initialTags: this.tempSettings.customConceptTags || "",
      placeholder: this.getText("customConceptTagsPlaceholder"),
      ariaLabel: this.getText("customConceptTagsName"),
      duplicateHint: this.getText("chipDuplicateHint"),
      defaultTags: VALID_CONCEPT_TAGS,
      onChange: (csv) => {
        this.tempSettings.customConceptTags = csv;
      }
    }));
    updateTagVocabularyVisibility(this.tempSettings.tagVocabularyMode || "default");
    new import_obsidian5.Setting(containerEl).setName(this.getText("maxConversationHistoryName")).setDesc(this.getText("maxConversationHistoryDesc")).addText((text) => {
      text.setValue(this.tempSettings.maxConversationHistory.toString()).setPlaceholder("30").onChange((value) => {
        const parsed = parseInt(value);
        if (parsed > 50) {
          this.tempSettings.maxConversationHistory = 50;
          text.setValue("50");
          new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", "50"), NOTICE_SHORT);
        } else if (parsed < 1) {
          this.tempSettings.maxConversationHistory = 1;
          text.setValue("1");
          new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", "1"), NOTICE_SHORT);
        } else if (!isNaN(parsed)) {
          this.tempSettings.maxConversationHistory = parsed;
        }
      });
      text.inputEl.type = "number";
      text.inputEl.min = "1";
      text.inputEl.max = "50";
      text.inputEl.classList.add("llm-wiki-number-input");
    });
    new import_obsidian5.Setting(containerEl).setName(this.getText("schemaSection")).setDesc(this.getText("enableSchemaDesc")).addButton((button) => button.setButtonText(this.getText("viewSchemaButton")).onClick(() => {
      const schemaPath = `${this.tempSettings.wikiFolder}/schema/config.md`;
      const file = this.app.vault.getAbstractFileByPath(schemaPath);
      if (file instanceof import_obsidian5.TFile) void this.app.workspace.getLeaf().openFile(file);
      else new import_obsidian5.Notice(this.getText("schemaNotFoundNotice"), NOTICE_NORMAL);
    })).addButton((button) => button.setButtonText(this.getText("regenerateSchemaButton")).onClick(async () => {
      try {
        if (!this.isWikiInitialized()) {
          await this.plugin.wikiEngine.ensureWikiStructure();
        }
        await this.plugin.wikiEngine.regenerateDefaultSchema();
        new import_obsidian5.Notice(this.getText("schemaRegeneratedNotice"), NOTICE_SHORT);
      } catch (error) {
        const msg = error instanceof Error ? error.message : String(error);
        new import_obsidian5.Notice(`${this.getText("schemaRegenerateFailed") || "Schema generation failed"}: ${msg}`, NOTICE_ERROR);
      }
    }));
    new import_obsidian5.Setting(containerEl).setName(this.getText("historyButton")).setDesc(this.getText("historyButtonDesc")).addButton((button) => button.setButtonText(this.getText("historyButtonOpen")).onClick(() => {
      new HistoryModal(this.app, {
        language: this.tempSettings.language,
        wikiFolder: this.tempSettings.wikiFolder || "wiki"
      }).open();
    }));
    new import_obsidian5.Setting(containerEl).setName(this.getText("autoMaintainSection")).setHeading();
    new import_obsidian5.Setting(containerEl).setName(this.getText("startupCheckName")).setDesc(this.getText("startupCheckDesc")).addToggle((toggle) => toggle.setValue(this.tempSettings.startupCheck).onChange((value) => {
      this.tempSettings.startupCheck = value;
    }));
    const betaDiv = containerEl.createDiv({
      cls: "llm-wiki-blue-infobox"
    });
    betaDiv.setText("\u{1F9EA} " + this.getText("autoMaintainBetaBadge"));
    const warningDiv = containerEl.createDiv({
      cls: "llm-wiki-yellow-infobox"
    });
    warningDiv.setText(this.getText("autoMaintainCostWarning"));
    new import_obsidian5.Setting(containerEl).setName(this.getText("autoWatchName")).setDesc(this.getText("autoWatchDesc")).addToggle((toggle) => toggle.setValue(this.tempSettings.autoWatchSources).onChange((value) => {
      this.tempSettings.autoWatchSources = value;
      this.display();
    }));
    if (this.tempSettings.autoWatchSources) {
      if (!this.tempSettings.watchedFolders) this.tempSettings.watchedFolders = [];
      new import_obsidian5.Setting(containerEl).setName(this.getText("watchedFoldersName")).setDesc(this.getText("watchedFoldersDesc")).addButton((button) => button.setButtonText(this.getText("addWatchedFolderButton")).setCta().onClick(() => {
        new FolderSuggestModal(this.app, this.tempSettings.wikiFolder, (folder) => {
          const folderPath = folder.path.endsWith("/") ? folder.path : `${folder.path}/`;
          if (!this.tempSettings.watchedFolders.includes(folderPath)) {
            this.tempSettings.watchedFolders.push(folderPath);
            this.display();
          }
        }).open();
      }));
      if (this.tempSettings.watchedFolders.length === 0) {
        containerEl.createEl("p", {
          text: this.getText("noWatchedFoldersHint"),
          cls: "llm-wiki-section-hint"
        });
      }
      for (let i = 0; i < this.tempSettings.watchedFolders.length; i++) {
        const idx = i;
        new import_obsidian5.Setting(containerEl).setName(this.tempSettings.watchedFolders[i]).addButton((button) => button.setButtonText(this.getText("removeWatchedFolderButton")).onClick(() => {
          this.tempSettings.watchedFolders.splice(idx, 1);
          this.display();
        }));
      }
      const hasClippings = this.tempSettings.watchedFolders.includes("Clippings/");
      new import_obsidian5.Setting(containerEl).setName(this.getText("webClipperPresetName")).setDesc(this.getText("webClipperPresetDesc")).addToggle((toggle) => toggle.setValue(hasClippings).onChange((value) => {
        if (value && !this.tempSettings.watchedFolders.includes("Clippings/")) this.tempSettings.watchedFolders.push("Clippings/");
        else if (!value) {
          const ci = this.tempSettings.watchedFolders.indexOf("Clippings/");
          if (ci >= 0) this.tempSettings.watchedFolders.splice(ci, 1);
        }
        this.display();
      }));
      new import_obsidian5.Setting(containerEl).setName(this.getText("autoWatchModeName")).setDesc(this.getText("autoWatchModeDesc")).addDropdown((dropdown) => {
        dropdown.addOption("notify", this.getText("watchModeNotify"));
        dropdown.addOption("auto", this.getText("watchModeAuto"));
        dropdown.setValue(this.tempSettings.autoWatchMode);
        dropdown.onChange((value) => {
          this.tempSettings.autoWatchMode = value;
        });
      });
      new import_obsidian5.Setting(containerEl).setName(this.getText("autoWatchDebounceName")).setDesc(this.getText("autoWatchDebounceDesc")).addText((text) => {
        text.setValue(String(Math.round(this.tempSettings.autoWatchDebounceMs / 1e3))).onChange((value) => {
          const parsed = parseInt(value);
          if (parsed > 60) {
            this.tempSettings.autoWatchDebounceMs = 6e4;
            text.setValue("60");
            new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", "60"), NOTICE_SHORT);
          } else if (parsed < 1) {
            this.tempSettings.autoWatchDebounceMs = 1e3;
            text.setValue("1");
            new import_obsidian5.Notice(this.getText("numberRangeClamped").replace("{}", "1"), NOTICE_SHORT);
          } else if (!isNaN(parsed)) {
            this.tempSettings.autoWatchDebounceMs = parsed * 1e3;
          }
        });
        text.inputEl.type = "number";
        text.inputEl.min = "1";
        text.inputEl.max = "60";
        text.inputEl.classList.add("llm-wiki-number-input");
      });
    }
    new import_obsidian5.Setting(containerEl).setName(this.getText("periodicLintName")).setDesc(this.getText("periodicLintDesc")).addDropdown((dropdown) => {
      dropdown.addOption("off", this.getText("periodicLintOff"));
      dropdown.addOption("hourly", this.getText("periodicLintHourly"));
      dropdown.addOption("daily", this.getText("periodicLintDaily"));
      dropdown.addOption("weekly", this.getText("periodicLintWeekly"));
      dropdown.setValue(this.tempSettings.periodicLint);
      dropdown.onChange((value) => {
        this.tempSettings.periodicLint = value;
      });
    });
    new import_obsidian5.Setting(containerEl).setName(this.getText("autoSmartFixName")).setDesc(this.getText("autoSmartFixDesc")).addToggle((toggle) => toggle.setValue(this.tempSettings.autoSmartFix).onChange((value) => {
      this.tempSettings.autoSmartFix = value;
    }));
  }
};

// src/wiki/wiki-engine.ts
var import_obsidian7 = require("obsidian");

// src/wiki/prompts/ingestion.ts
var INGESTION_PROMPTS = {
  analyzeSource: `You are a Wiki knowledge base maintainer. Analyze the following source file and output structured JSON.

**Existing Wiki pages \u2014 use ONLY these exact paths when creating [[links]]:**
{{existing_slugs}}

**Source File Content:**
{{content}}

{{batch_context}}

**Extraction Scope:**
{{granularity_instruction}}

**Task Requirements:**
0. [FIRST ROUND ONLY] Write a 100-200 word source summary (field: summary) and extract the source title (field: source_title). These fields must NOT appear in later rounds.
1. In EVERY round (including the first), output both "entities" and "concepts" arrays. Use [] when a category has no items. Never omit either array.
2. Optionally generate 1-2 aliases per entity/concept \u2014 alternative names, acronyms, translations, or common phrasings. Aliases serve as seeds for page generation and help the model avoid duplicate extractions in later rounds. The aliases field is OPTIONAL in extraction; skip it when no natural alias exists.
3. Output at most {{batch_size}} items (entities + concepts total) this round
3. Write a detailed, informative summary for each item (target 4-6 sentences). Include concrete information: what the entity/concept is, its role/significance in the source, key factual details, and how it relates to other items. Provide enough substance that the summary alone can seed a quality Wiki page
4. For mentions_in_source: quote 2-4 verbatim sentences from the source where this entity/concept appears or is discussed. These quotes are critical \u2014 they provide the downstream page generator with source-grounded evidence. Include surrounding context, not just the name mention
5. For related_entities and related_concepts: identify entities/concepts mentioned in the same context as this item. These should be other items extracted from this same source file
6. Identify contradictions or conflicts with the existing Wiki (only output contradictions in the first round)
7. Identify related existing Wiki pages (only output related_pages in the first round)
8. Generate key points from the source file (only output key_points in the first round)

**Output Format (strict JSON, output only JSON, no explanatory text):**
{
  "source_title": "Source file title",
  "summary": "150-250 word source summary (first round only, omitted thereafter)",
  "entities": [
    {
      "name": "Entity name \u2014 MUST be in the source's original language, NEVER translate",
      "type": "person|organization|project|product|event|place|other",
      "aliases": ["Optional: 1-2 alternative names, abbreviations, or translations. Helps prevent duplicate extractions in later rounds.", "If provided, these will seed the page aliases."],
      "summary": "Detailed 4-6 sentence description with concrete facts: identity, role/significance, key attributes",
      "mentions_in_source": ["Verbatim sentence from source: '...'.", "Another verbatim quote: '...'."],
      "related_entities": ["Related entity names from this source"],
      "related_concepts": ["Related concept names from this source"]
    }
  ],
  "concepts": [
    {
      "name": "Concept name \u2014 MUST be in the source's original language, NEVER translate",
      "type": "theory|method|field|phenomenon|standard|term|other",
      "aliases": ["Optional: 1-2 alternative names, abbreviations, or translations. Helps prevent duplicate extractions in later rounds.", "If provided, these will seed the page aliases."],
      "summary": "Detailed 4-6 sentence description with concrete facts: definition, importance, relationships",
      "mentions_in_source": ["Verbatim sentence from source: '...'.", "Another verbatim quote: '...'."],
      "related_concepts": ["Related concept names from this source"],
      "related_entities": ["Related entity names from this source"]
    }
  ],
  "contradictions": [
    {
      "claim": "What the source file claims",
      "source_page": "Conflicting existing Wiki page [[page-name]]",
      "contradicted_by": "What that page claims",
      "resolution": "Suggested resolution"
    }
  ],
  "related_pages": ["Related existing Wiki page names \u2014 use ONLY the plain page name, NOT wiki-link format. Example: 'Machine Learning' not [[concepts/Machine Learning|Machine Learning]]"],
  "key_points": ["Key point 1", "Key point 2"]
}

**Entity Recognition Guide:**
- person: individual who is a significant SUBJECT of the source. Authors cited only as evidence sources ("Smith et al. found...") are NOT wiki-worthy entities
- organization: organization/institution (company, school, team, department, etc.)
- project: project/initiative/program
- product: product/tool/software/service. Publications only when they are the primary subject of analysis, not when cited as evidence sources
- event: event/conference/milestone/historical occurrence
- location: place/region/geographic concept
- other: observable, instantiable concrete things (a specific dataset, benchmark, physical instrument) that do not fit any category above. NOT for abstract ideas, paradigms, or techniques \u2014 those are concepts

**Classification Decision Tree (Entity vs. Concept) \u2014 apply in order, stop at first match:**
1. Named PERSON \u2192 entity (person)
2. Named ORGANIZATION, institution, company, team, lab \u2192 entity (organization)
3. Named PROJECT or named initiative \u2192 entity (project)
4. Named LOCATION, place, region \u2192 entity (location)
5. Named EVENT, conference, competition, release milestone \u2192 entity (event)
6. Named PRODUCT with its own vendor/release cycle (specific software package, hardware device, hosted service) \u2192 entity (product). Examples: PyTorch, GPT-4, BERT, TensorFlow. BUT if the source is not primarily ABOUT this product, extract its key ideas as concepts instead
7. Abstract THEORY, principle, hypothesis, cognitive/scientific model \u2192 concept (theory)
8. Procedural METHOD, algorithm, technique, protocol, training procedure \u2192 concept (method). Examples: gradient descent, RLHF, fine-tuning, chain-of-thought prompting, backpropagation
9. Broad TECHNOLOGY paradigm or architectural pattern \u2192 concept (technology). Examples: transformer architecture, deep learning, attention mechanism, retrieval-augmented generation
10. Any TERM, definition, or construct explaining how something works \u2192 concept (term)
11. A concrete named thing that does not fit rules 1\u20136 \u2192 entity (other). Reserve for observable/instantiable things only
12. If still uncertain \u2192 **prefer concept over entity**

**Key boundary**: Named AI models and named frameworks are entities (product). Architectural ideas and learning techniques are concepts (method/technology). When a source mentions a product only as a tool used for something else, extract its role/capabilities as a concept, not the product as an entity.

**Important Rules:**
- Output ONLY JSON, nothing else
- **CRITICAL: Entity and concept "name" MUST use the ORIGINAL language from the source file. NEVER translate names.** If the source says "Yinmin Zhong", the name MUST be "Yinmin Zhong", NOT "\u949F\u80E4\u654F". If the source says "Conditional Memory", the name MUST be "Conditional Memory", NOT "\u6761\u4EF6\u8BB0\u5FC6". If the source says "Cache-Compute Ratio", the name MUST be "Cache-Compute Ratio". Translation of names is FORBIDDEN. Summaries and descriptions may use the wiki language, but the name field is inviolable
- "mentions_in_source" MUST contain 2-4 verbatim quotes from the source text. Do NOT paraphrase \u2014 copy the actual sentences where the entity/concept appears. Include full sentences with context, not fragments
- Each entity and concept should have its own independent Wiki page
- Carefully compare against existing content when detecting contradictions
- related_pages should be pages that actually exist in the current Wiki
- Output must be valid JSON format
- Do NOT repeat any item already in the "extracted list". If no unextracted items remain in the source, return empty arrays [] for entities and concepts
- Apply the wiki-link test to every candidate: if an entity/concept would not be linked from other notes, do not extract it. Knowledge claims and findings are more valuable than evidence containers`,
  // Semantic entity resolution: when slug-based matching fails, use LLM to determine
  // whether a newly extracted entity/concept is semantically equivalent to an existing page.
  resolveEntityDedup: `You are an entity resolution engine. Given a newly extracted entity/concept and a list of existing wiki pages, determine if it is semantically equivalent to any existing page.

**New entity/concept:**
- Name: {{entity_name}}
- Type: {{entity_type}}
- Summary: {{entity_summary}}

**Existing {{page_type}} pages:**
{{existing_pages}}

**Task:** Determine whether the new entity/concept is semantically the SAME as any existing page. Consider:
- Translations between languages (e.g. "\u6E05\u534E\u5927\u5B66" = "Tsinghua University")
- Abbreviations and full names (e.g. "MIT" = "Massachusetts Institute of Technology")
- Alternative phrasings (e.g. "Supervised Learning" = "Supervised ML")
- Spelling variations

**Output JSON:**
- If it matches an existing page, output: {"match": true, "path": "{{wikiFolder}}/entities/existing-slug.md"}
- If no match exists, output: {"match": false, "path": null}

Do NOT create a new name \u2014 only match against the existing pages listed above.`
};

// src/wiki/prompts/generation.ts
var GENERATION_PROMPTS = {
  generateEntityPage: `You are a Wiki knowledge base maintainer. Create a Wiki page for the following entity.

**Entity Information:**
- Name: {{entity_name}}
- Type: {{entity_type}}
- Summary: {{entity_summary}}
- Mentions in source (VERBATIM \u2014 preserve original language): {{mentions}}
- Related entities: {{related_entities}}
- Related concepts: {{related_concepts}}
- Extraction aliases (seeds): {{extraction_aliases}}

**Existing Wiki Pages (use these exact full paths when referencing):**
{{existing_pages}}

**Existing Related Content in Wiki:**
{{related_content}}

{{merge_strategy}}

**Task Requirements:**
1. Create an entity page with basic and key information
2. When referencing other pages, copy the wiki-link format EXACTLY from the "Existing Wiki Pages" list. The LEFT side of | is the full path (entities/Page-Name), the RIGHT side is the DISPLAY NAME ONLY. NEVER duplicate folder prefixes like entities/ or concepts/ in the display name. Example: [[entities/Qwen|Qwen]] is CORRECT, [[entities/Qwen|entities/Qwen]] is WRONG
3. IMPORTANT: All related entities and concepts MUST be formatted as wiki-links using the [[path|display]] format \u2014 even if the target page does not yet exist in the Wiki. This allows the Lint system to detect dead links and create stub pages later. Never output a related entity/concept name as plain text.
4. If the entity already exists in the Wiki, use the merge strategy above for intelligent merging
4. Be objective, accurate, and concise
5. **Generate aliases for this page** \u2014 provide 1-3 alternative names. This field is REQUIRED:
   - Include acronyms, abbreviations, and same-language alternative names
   - English is universally acceptable as a "linker language" \u2014 when a term originates in English
     (e.g. "Transformer", "DNA", "API", "RoPE", "CUDA"), keep it as-is even in non-English wikis
   - **CRITICAL: do NOT invent translations for established technical terms.** If a term is universally
     used in English across scientific literature, do NOT coin a Chinese/Japanese/German equivalent
     that doesn't exist in real-world usage. Real-world convention always wins over linguistic purity.
   - **If no natural alias exists**, use the page title itself as the first alias. The aliases field MUST NOT be left empty \u2014 always provide at least one alias

   Examples:
   - \u7EF4\u751F\u7D20 B2 (Chinese wiki) \u2192 ["\u7EF4\u4ED6\u547D B2", "Vitamin B2", "VB2"]
   - Transformer (Chinese wiki) \u2192 ["Transformers", "BERT"]      \u2190 NO \u53D8\u6362\u5668 (no such usage in Chinese)
   - Rotary Position Embedding (Japanese wiki) \u2192 ["RoPE", "\u56DE\u8EE2\u4F4D\u7F6E\u57CB\u3081\u8FBC\u307F"]
   - Neural Network (Chinese wiki) \u2192 ["\u795E\u7ECF\u7F51\u7EDC", "NN"]
6. In "Mentions in Source" section: preserve the VERBATIM quotes in their ORIGINAL language. You may ADD a brief translation in parentheses if the wiki language differs, but the original text must be preserved exactly

**Output Format:**
---
type: entity  # MUST be exactly "entity" - do not change this value
created: {{date}}
updated: {{date}}
sources: ["[[{{source_file}}]]"]
tags: [{{entity_type}}]  # Use entity_type (e.g., product, person, organization) as a tag
aliases: ["Alternative name or translation"]  # REQUIRED: at least 1 alias, must NOT be empty
---

# {{entity_name}}

## {{section_basic_information}}
- Type: {{entity_type}}
- Source: [[{{source_file}}]]

## {{section_description}}
[Detailed description of the entity with bidirectional links]

## {{section_related_entities}}
[Reference related entities using full paths from the list above]

## {{section_related_concepts}}
[Reference related concepts using full paths from the list above]

## {{section_mentions_in_source}}
[Each verbatim quote as an academic-footnote style entry. The provided mentions in the input already include the source wiki-link \u2014 keep them as-is. If you need to add more quotes, use the same format:
- "Verbatim quote in original language (optional translation)" \u2014 [[source-name]]]

---`,
  generateConceptPage: `You are a Wiki knowledge base maintainer. Create a Wiki page for the following concept.

**Concept Information:**
- Name: {{concept_name}}
- Type: {{concept_type}}
- Summary: {{concept_summary}}
- Mentions in source (VERBATIM \u2014 preserve original language): {{mentions}}
- Related concepts: {{related_concepts}}
- Related entities: {{related_entities}}
- Extraction aliases (seeds): {{extraction_aliases}}

**Existing Wiki Pages (use these exact full paths when referencing):**
{{existing_pages}}

**Existing Related Content in Wiki:**
{{related_content}}

{{merge_strategy}}

**Task Requirements:**
1. Create a concept page including definition, characteristics, and applications
2. When referencing other pages, copy the wiki-link format EXACTLY from the "Existing Wiki Pages" list. The LEFT side of | is the full path (concepts/Page-Name), the RIGHT side is the DISPLAY NAME ONLY. NEVER duplicate folder prefixes like entities/ or concepts/ in the display name. Example: [[concepts/Attention|Attention]] is CORRECT, [[concepts/Attention|concepts/Attention]] is WRONG
3. IMPORTANT: All related entities and concepts MUST use [[wiki-link]] format even if the target page does not yet exist \u2014 this allows the Lint system to detect and fix them later. Never output a related entity/concept name as plain text.
4. If the concept already exists in the Wiki, use the merge strategy above for intelligent merging
4. Be objective, accurate, and concise
5. **Generate aliases for this page** \u2014 provide 1-3 alternative names. This field is REQUIRED:
   - Include acronyms, abbreviations, and same-language alternative names
   - English is universally acceptable as a "linker language" \u2014 when a term originates in English
     (e.g. "Transformer", "DNA", "API", "RoPE", "CUDA"), keep it as-is even in non-English wikis
   - **CRITICAL: do NOT invent translations for established technical terms.** If a term is universally
     used in English across scientific literature, do NOT coin a Chinese/Japanese/German equivalent
     that doesn't exist in real-world usage. Real-world convention always wins over linguistic purity.
   - **If no natural alias exists**, use the page title itself as the first alias. The aliases field MUST NOT be left empty \u2014 always provide at least one alias

   Examples:
   - \u7EF4\u751F\u7D20 B2 (Chinese wiki) \u2192 ["\u7EF4\u4ED6\u547D B2", "Vitamin B2", "VB2"]
   - Transformer (Chinese wiki) \u2192 ["Transformers", "BERT"]      \u2190 NO \u53D8\u6362\u5668 (no such usage in Chinese)
   - Rotary Position Embedding (Japanese wiki) \u2192 ["RoPE", "\u56DE\u8EE2\u4F4D\u7F6E\u57CB\u3081\u8FBC\u307F"]
   - Neural Network (Chinese wiki) \u2192 ["\u795E\u7ECF\u7F51\u7EDC", "NN"]
6. In "Mentions in Source" section: preserve the VERBATIM quotes in their ORIGINAL language. You may ADD a brief translation in parentheses if the wiki language differs, but the original text must be preserved exactly

**Output Format:**
---
type: concept  # MUST be exactly "concept" - do not change this value
created: {{date}}
updated: {{date}}
sources: ["[[{{source_file}}]]"]
tags: [{{concept_type}}]  # Use concept_type (e.g., theory, method, field) as a tag
aliases: ["Alternative name or translation"]  # REQUIRED: at least 1 alias, must NOT be empty
---

# {{concept_name}}

## {{section_definition}}
[Clear definition of the concept]

## {{section_key_characteristics}}
- Characteristic 1
- Characteristic 2

## {{section_applications}}
[Application scenarios for the concept]

## {{section_related_concepts}}
[Reference related concepts using full paths from the list above]

## {{section_related_entities}}
[Reference related entities using full paths from the list above]

## {{section_mentions_in_source}}
[Each verbatim quote as an academic-footnote style entry. The provided mentions in the input already include the source wiki-link \u2014 keep them as-is. If you need to add more quotes, use the same format:
- "Verbatim quote in original language (optional translation)" \u2014 [[source-name]]]

---`,
  generateSummaryPage: `You are a Wiki knowledge base maintainer. Create a summary page for the following source file.

**Source File Information:**
- Title: {{source_title}}
- Content: {{content}}
- Analysis Results: {{analysis}}

**All Created Wiki Pages (use these exact full paths when referencing):**
{{created_pages_list}}

**Task Requirements:**
1. Create a concise summary page
2. When referencing entities and concepts, use the exact full path format from the "All Created Wiki Pages" list above
3. {{constraints}}
4. Highlight key points
5. Be objective and accurate
6. **Generate aliases for this page** \u2014 provide 1-2 alternative names for the source. This field is REQUIRED:
   - Include alternative titles, abbreviations, or common alternative names for the source
   - English is universally acceptable as a "linker language" \u2014 when a term originates in English
     (e.g. "Transformer", "DNA", "API", "RoPE"), keep it as-is even in non-English wikis
   - **CRITICAL: do NOT invent translations for established technical terms.** Real-world usage
     always wins over linguistic purity. Only include translations that actually exist in the target language.
   - **If no natural alias exists**, use the source file name or the page title itself. The aliases field MUST NOT be left empty \u2014 always provide at least one alias

**Output Format:**
---
type: source
created: {{date}}
updated: {{date}}
source_file: "[[{{source_file}}]]"
tags: [{{tags}}]
aliases: ["Alternative title or translation"]  # REQUIRED: at least 1 alias, must NOT be empty
---

# {{source_title}} - Summary

## {{section_source}}
- Original file: [[{{source_file}}]]
- Ingested: {{date}}

## {{section_core_content}}
[100-200 word summary with bidirectional links]

## {{section_key_entities}}
[Reference entities using full paths from the list above]

## {{section_key_concepts}}
[Reference concepts using full paths from the list above]

## {{section_main_points}}
- Point 1
- Point 2

---`,
  // Variant used when the existing page has `reviewed: true` in frontmatter.
  preserveReviewedEntityPage: `You are a Wiki knowledge base maintainer. The following entity page has been manually reviewed by the user (reviewed: true).

**\u26A0\uFE0F Important: User-reviewed content must be fully preserved. Do NOT delete or rewrite.**

**Entity Information (from new source file):**
- Name: {{entity_name}}
- Type: {{entity_type}}
- Summary: {{entity_summary}}
- Mentions in source: {{mentions}}

**Existing Wiki Pages (use these exact full paths when referencing):**
{{existing_pages}}

**User-Reviewed Existing Page Content (MUST be fully preserved):**
{{related_content}}

**Task Requirements:**
1. **Fully preserve** all user-reviewed content \u2014 do not delete or rewrite any paragraph
2. Only add non-duplicate information from the new source at the end in a "New Information" section
3. If new information duplicates or contradicts existing content, do NOT add it; keep the user's version
4. The frontmatter MUST retain reviewed: true
5. When referencing other pages, copy the wiki-link format EXACTLY from the list above. NEVER duplicate folder prefixes in the display name. Example: [[entities/Qwen|Qwen]] is CORRECT, [[entities/Qwen|entities/Qwen]] is WRONG

**Output Format:**
---
type: entity
created: {{date}}
updated: {{date}}
sources: ["[[{{source_file}}]]"]
tags: [{{tags}}]
aliases: []
reviewed: true
---

[Fully preserve user-reviewed existing content here]

## {{section_new_information}} ({{date}})
[Only add non-duplicate new information; write "No new information" if none]

---`,
  // Variant used when the existing concept page has `reviewed: true` in frontmatter.
  preserveReviewedConceptPage: `You are a Wiki knowledge base maintainer. The following concept page has been manually reviewed by the user (reviewed: true).

**\u26A0\uFE0F Important: User-reviewed content must be fully preserved. Do NOT delete or rewrite.**

**Concept Information (from new source file):**
- Name: {{concept_name}}
- Type: {{concept_type}}
- Summary: {{concept_summary}}
- Mentions in source: {{mentions}}
- Related concepts: {{related_concepts}}

**Existing Wiki Pages (use these exact full paths when referencing):**
{{existing_pages}}

**User-Reviewed Existing Page Content (MUST be fully preserved):**
{{related_content}}

**Task Requirements:**
1. **Fully preserve** all user-reviewed content \u2014 do not delete or rewrite any paragraph
2. Only add non-duplicate information from the new source at the end in a "New Information" section
3. If new information duplicates or contradicts existing content, do NOT add it; keep the user's version
4. The frontmatter MUST retain reviewed: true
5. When referencing other pages, copy the wiki-link format EXACTLY from the list above. NEVER duplicate folder prefixes in the display name. Example: [[entities/Qwen|Qwen]] is CORRECT, [[entities/Qwen|entities/Qwen]] is WRONG

**Output Format:**
---
type: concept
created: {{date}}
updated: {{date}}
sources: ["[[{{source_file}}]]"]
tags: [{{tags}}]
aliases: []
reviewed: true
---

[Fully preserve user-reviewed existing content here]

## {{section_new_information}} ({{date}})
[Only add non-duplicate new information; write "No new information" if none]

---`,
  suggestSchemaUpdate: `You are a Wiki Schema advisor. Review the current schema and the latest ingestion analysis.

Current Schema:
{{schema_content}}

Analysis Context:
{{analysis_context}}

Task: Determine if the schema needs updating to better accommodate recent content.
Consider:
1. Are there new entity types that should be added to the classification rules?
2. Are there new concept types that should be added?
3. Should naming conventions be adjusted?
4. Should page templates be updated (missing sections, better structure)?
5. Should maintenance policies be revised (stale thresholds, severity levels)?

Output JSON format:
{
  "changes_needed": true,
  "new_schema_body": "The COMPLETE updated schema body in markdown, starting with the H1 title. Include all sections that should exist after the update (you may keep unchanged sections exactly as-is).",
  "suggestions": "Markdown description of suggested schema changes with reasoning (1-3 sentences)"
}

If no changes are needed:
{
  "changes_needed": false,
  "suggestions": "Short explanation of why no changes are needed"
}

CRITICAL:
- The "suggestions" field must be written in the user's UI language below (after this CRITICAL block). Do NOT default to English unless the user's UI language is English.
- new_schema_body is the COMPLETE schema after your changes \u2014 not a diff, not a patch. The apply path replaces the current body with new_schema_body verbatim.
- DO NOT include YAML frontmatter (--- ... ---) in new_schema_body. Start directly with the H1 title (e.g. "# Wiki Schema Configuration").
- DO NOT wrap new_schema_body in markdown code fences (\`\`\`). The parser strips them, but cleaner output reduces parse risk on small models.
- Preserve all existing sections that are still relevant. Only change what the analysis actually warrants.
- Output ONLY the JSON, no other text.

User UI language: {{user_language}}`
};

// src/wiki/prompts/merge.ts
var MERGE_PROMPTS = {
  // Multi-Source Knowledge Fusion: structured merge analysis
  mergeAnalysis: `You are a Wiki knowledge fusion analyzer. Compare existing Wiki page content with new source file information, and output a structured merge strategy.

**Page Name:** {{page_name}}
**Page Type:** entity or concept

**Existing Page Content:**
{{existing_content}}

**New Information from Source File:**
{{new_info}}

**Task:**
1. Compare each piece of new information against the existing content
2. Classify each piece of new information as:
   - "new" \u2014 completely new information not in the existing page
   - "duplicate" \u2014 duplicates existing content; no need to add
   - "complementary" \u2014 supplements existing content (additional detail on the same topic)
   - "contradictory" \u2014 contradicts existing content
3. For "complementary" information, specify which section of the existing page it should be inserted after
4. For "contradictory" information, document the specific contradiction

Output JSON format:
{
  "merge_items": [
    {
      "content": "Specific content of the new information",
      "classification": "new|duplicate|complementary|contradictory",
      "target_section": "Section name to insert after (only for new and complementary)",
      "reason": "Reason for classification (one sentence)"
    }
  ],
  "contradictions": [
    {
      "claim": "What the new information claims",
      "existing_claim": "Contradictory content in the existing page",
      "resolution": "Suggested resolution"
    }
  ],
  "merge_summary": "Merge strategy summary (one sentence)"
}

Rules:
- Output ONLY JSON, nothing else
- Existing content takes priority over new information (unless the new information is clearly more accurate)
- Do NOT delete or rewrite any part of the existing content`,
  mergeEntityPage: `You are a Wiki editor performing intelligent content integration. Merge new source information into an existing page following the schema-defined structure.

**Schema Rules (MUST follow this structure):**
- ## {{section_basic_information}}: Type, sources, key attributes
- ## {{section_description}}: Core definition and significance (3-6 sentences)
- ## {{section_related_entities}}: Links to related entities
- ## {{section_related_concepts}}: Links to related concepts
- ## {{section_mentions_in_source}}: Chronological list of mentions with VERBATIM quotes in original language

**Existing Page Content (the current version):**
{{existing_body}}

**New Information from Source "{{new_source}}":**
- Summary: {{entity_summary}}
- Mentions in source: {{mentions}}
- Related entities: {{related_entities}}
- Related concepts: {{related_concepts}}
- Key details: {{key_details}}

**Available Wiki Pages for linking (use exact [[path|name]] format):**
{{existing_pages}}

**Integration Requirements:**
1. STRUCTURE: Follow the schema sections exactly. If a section exists, update it; if missing, create it.
2. DESCRIPTION: Integrate new facts naturally. Do NOT duplicate existing information.
3. RELATED: Update "{{section_related_entities}}" and "{{section_related_concepts}}" sections with new relationships.
4. CONTRADICTIONS: If new info conflicts with existing, preserve BOTH with clear attribution.
5. MENTIONS: Append new mentions to "{{section_mentions_in_source}}". Preserve VERBATIM quotes in original language. Translation optional in parentheses.
6. LINKS: Use [[path|display]] format. LEFT side = full path, RIGHT side = display name ONLY. NEVER duplicate folder prefixes (entities/, concepts/) in display name. Verify paths exist.
7. STYLE: Match existing writing style.
8. NO REDUNDANCY: Do NOT restate existing facts.

**Output Format:**
Output ONLY the body content (no frontmatter):

## {{section_basic_information}}
[Type, sources, key attributes \u2014 updated]

## {{section_description}}
[Integrated description \u2014 merge existing + new, no duplication]

## {{section_related_entities}}
[Updated entity links]

## {{section_related_concepts}}
[Updated concept links]

## {{section_mentions_in_source}}
[ALL mentions \u2014 existing preserved with their source attribution blocks, new appended under a new source block:
> **Source: [[source-name]]**
> - "Verbatim quote in original language"]`,
  mergeConceptPage: `You are a Wiki editor performing intelligent content integration. Merge new source information into an existing concept page following the schema-defined structure.

**Schema Rules (MUST follow this structure):**
- ## {{section_basic_information}}: Type, sources, definition
- ## {{section_description}}: Detailed explanation with examples (3-6 sentences)
- ## {{section_related_concepts}}: Connected concepts using [[concepts/...]]
- ## {{section_related_entities}}: Connected entities using [[entities/...]]
- ## {{section_mentions_in_source}}: VERBATIM quotes in original language with source attribution

**Existing Page Content (the current version):**
{{existing_body}}

**New Information from Source "{{new_source}}":**
- Summary: {{concept_summary}}
- Mentions in source: {{mentions}}
- Related concepts: {{related_concepts}}
- Related entities: {{related_entities}}
- Key details: {{key_details}}

**Available Wiki Pages for linking (use exact [[path|name]] format):**
{{existing_pages}}

**Integration Requirements:**
1. STRUCTURE: Follow the schema sections exactly. Update existing, create missing.
2. DESCRIPTION: Integrate new understanding coherently with existing.
3. RELATED CONCEPTS: Update links \u2014 add new ones, preserve existing.
4. RELATED ENTITIES: Update links \u2014 add new ones from this source.
5. CONTRADICTIONS: If new info conflicts, preserve both with attribution.
6. MENTIONS: Append to "{{section_mentions_in_source}}". Preserve VERBATIM quotes in original language.
7. LINKS: Use [[path|display]] format. LEFT side = full path, RIGHT side = display name ONLY. NEVER duplicate folder prefixes (entities/, concepts/) in display name. Verify paths exist.
8. STYLE: Match existing writing style.
9. NO REDUNDANCY: Do NOT restate existing facts.

**Output Format:**
Output ONLY the body content (no frontmatter):

## {{section_basic_information}}
[Type, sources, definition \u2014 updated]

## {{section_description}}
[Integrated description \u2014 merge existing + new]

## {{section_related_concepts}}
[Updated concept links]

## {{section_related_entities}}
[Updated entity links]

## {{section_mentions_in_source}}
[ALL mentions \u2014 existing preserved with their source attribution blocks, new appended under a new source block:
> **Source: [[source-name]]**
> - "Verbatim quote in original language"]`,
  // Minimal append mode for reviewed pages
  appendToReviewedPage: `You are a Wiki editor adding new information to a user-reviewed page. The existing content is AUTHORITATIVE and must be preserved exactly.

**User-Reviewed Existing Content (MUST preserve completely):**
{{existing_body}}

**New Information from Source "{{new_source}}":**
- Summary: {{entity_summary}}
- Mentions: {{mentions}}
- Key details: {{key_details}}

**Task:**
1. Compare new information against existing content
2. If new info is COMPLETELY REDUNDANT with existing content \u2192 output "NO_NEW_CONTENT"
3. If new info adds genuinely new facts \u2192 append them in a "New Information ({{new_source}})" section at the end
4. DO NOT modify any existing content
5. DO NOT remove or rewrite any existing sections
6. {{constraints}}

**Output Format:**
If no new content: output exactly "NO_NEW_CONTENT"

If new content exists:
[existing content preserved exactly]

## New Information ({{new_source}})
[Only genuinely new facts, written to match existing style]`,
  // Update related page with incremental information from a new source
  updateRelatedPage: `Existing Wiki page: {{page_name}}

Existing content:
{{existing_body}}

The new source file ("{{source_basename}}") provides additional information about {{page_name}}:
{{new_info}}

Update the page by adding the new information without deleting existing content.
{{constraints}}
Use wiki-link syntax [[page-name]].
Output ONLY the updated page BODY content (without frontmatter), no other text.`
};

// src/wiki/prompts/fixes.ts
var FIX_PROMPTS = {
  fixDeadLink: `You are a Wiki dead link repair assistant. Analyze the dead link and repair it based on the situation.

Dead Link Source Page:
{{source_content}}

Dead Link Target (link text):
{{target_name}}

Existing Wiki Page List:
{{existing_pages}}

Task:
1. **FIRST, check aliases:** Look at the \`aliases:\` listed after each page. If the dead link target matches any page's title OR any page's aliases (case-insensitive), that page IS the correct target \u2014 use action=correct
2. If no alias match, search for semantic similarity (translations, abbreviations, alternative phrasings)
3. If a matching page is found: output the correct [[entities/page-name|Display Name]] or [[concepts/page-name|Display Name]] link. The display name after | must NOT include folder prefixes like entities/ or concepts/
4. If no match is found after checking BOTH aliases and semantic similarity: output a concise name suitable as a new page title

Output JSON format:
{"action": "correct|create_stub", "correct_link": "Corrected link (when action=correct)", "stub_title": "New page title (when action=create_stub)", "stub_type": "entity|concept", "reason": "Reason for judgment (one sentence)"}`,
  fillEmptyPage: `You are a Wiki page expansion assistant. Generate content for the following under-populated Wiki page.

Page Type (entities/concepts/sources): {{page_type}}

Existing Content:
{{existing_content}}

Wiki Index (background reference):
{{wiki_index}}

**Target Language Section Headers (use EXACTLY these headers):**
{{section_labels}}

Task:
1. Generate appropriate content (150-300 words) based on the page type and title
2. entities type: describe the entity's definition, relevant background, and relationships with other entities
3. concepts type: explain the concept's definition, application scenarios, and related concepts
4. sources type: summarize the source's core viewpoints and contributions
5. Use [[wiki-links]] to link to related pages. Copy the link format EXACTLY from the Wiki Index \u2014 the LEFT side is the full path (folder/page-name), the RIGHT side after | is the DISPLAY NAME only. NEVER duplicate folder prefixes like entities/ or concepts/ in the display name. Example: [[entities/Qwen|Qwen]] is correct, [[entities/Qwen|entities/Qwen]] is WRONG
6. **Related entities/concepts LIMIT:** Create at MOST {{max_entities}} related entity links and {{max_concepts}} related concept links. Focus on the most important connections only. Do NOT generate exhaustive lists
7. **IMPORTANT \u2014 Source Mentions:** Only create a "Mentions in Source" section if the existing content already contains verbatim source quotes. If no source quotes exist in the existing content, do NOT fabricate a "Mentions in Source" section \u2014 instead, note "*(No source content available for this page)*" in the description. NEVER invent fake citations or source references
8. Preserve any existing frontmatter fields exactly (type, created, sources, tags, reviewed). Do NOT remove or alter these fields
9. **Aliases are REQUIRED:** If the existing content has non-empty aliases, keep them. If aliases are missing or empty, you MUST generate 1-2 meaningful aliases following the fallback hierarchy: translation in Wiki language \u2192 alternative names \u2192 abbreviations. The aliases field MUST NOT be left empty
10. **Tags constraint:** entity pages MUST use tags from: [person, organization, project, product, event, place, other]. Concept pages MUST use tags from: [theory, method, field, phenomenon, standard, term, other]. Never invent new tags outside these lists

Output format: directly output the complete Markdown page content (do not output explanatory text)`,
  linkOrphanPage: `You are a Wiki link repair assistant. Establish backlinks to an orphan page from relevant pages.

Orphan Page:
{{orphan_content}}

Wiki Index:
{{wiki_index}}

Task:
1. Analyze the orphan page's topic
2. Select 1-3 most relevant existing pages from the Wiki index
3. For each relevant page, generate a suggested link text (one sentence description + wiki-link)

Output JSON format:
{"related_pages": [{"page_path": "{{wikiFolder}}/entities/xxx.md", "link_text": "One sentence describing this connection", "link_target": "[[entities/orphan-name]]"}], "reason": "Connection rationale"}`,
  resolveContradiction: `You are a Wiki contradiction resolution assistant. Generate a repaired page based on the contradiction record and affected page content.

Affected Page Content:
{{existing_content}}

Contradiction Record:
{{contradiction_content}}

Task:
1. Analyze both sides of the contradiction
2. Reconcile: preserve correct information, annotate suspected incorrect information
3. For factual contradictions, choose the more reliable or newer source
4. For perspective differences, preserve both viewpoints and note the different standpoints

Important Rules:
- Do NOT delete any existing content
- Add a "## {{section_resolved_contradictions}}" section at the end of the affected page, explaining the resolution approach and reasoning
- Keep the overall page structure intact; only adjust contradiction-related parts
- Output the complete repaired page content (not just the modified parts)
- Do NOT output any explanatory text; directly output Markdown-formatted page content`
};

// src/wiki/prompts/lint.ts
var LINT_PROMPTS = {
  // First-pass LLM title scan to find cross-lingual/translation/abbreviation candidates.
  lintTitleScanCandidates: `You are a Wiki duplicate scanner. Below is a list of ALL entity and concept wiki pages (path + title). Find pages that likely refer to the SAME underlying concept, even when named in different languages or formats.

**Pages ({{total}} total):**
{{title_list}}

**Common duplicate patterns to look for:**
- Translations: one Chinese title, one English title describing the same concept
- Abbreviations: "CoT" vs "Chain of Thought", "MoE" vs "Mixture of Experts"
- Alternate phrasings: same concept expressed with different wording
- Partial matches: "KV\u7F13\u5B58" vs "\u952E\u503C\u7F13\u5B58" (KV = Key-Value = \u952E\u503C)

**Rules:**
- Only report pairs you are HIGHLY CONFIDENT refer to the same thing
- Do NOT report pairs that are merely related concepts
- Max 30 pairs
- Prefer the page with the MORE descriptive/specific title as "target" (keep)

**You MUST output ONLY a valid JSON object, no other text:**
{"candidates": [["{{wikiFolder}}/concepts/target-page.md", "{{wikiFolder}}/concepts/source-page.md"], ...]}

If no candidates found: {"candidates": []}`,
  // Verify duplicate candidates with content analysis
  lintDuplicateDetection: `You are a Wiki duplicate detection verifier. Review the candidate pairs below and confirm which are TRUE semantic duplicates.

**Candidate pairs (from programmatic signals and title scan):**
{{candidates}}

**Total entity/concept pages in wiki:** {{total}}

**Rules:**
- Confirm a pair ONLY if both pages refer to the SAME underlying concept/entity
- TRUE duplicates: translations (e.g. "CoT" = "\u601D\u7EF4\u94FE"), abbreviations, spelling variants, same thing named differently
- NOT duplicates: related concepts, parent-child relationships, overlapping topics
- Only confirm pairs you are HIGHLY CONFIDENT about (95%+ certainty)
- Keep the candidate's target/source assignment (target = keep, source = merge into target)

**You MUST output ONLY a valid JSON object, no other text:**
{"duplicates": [{"target": "{{wikiFolder}}/entities/page1.md", "source": "{{wikiFolder}}/entities/page2.md", "reason": "Same concept: both refer to X because..."}]}

If none of the candidates are true duplicates: {"duplicates": []}`,
  // Merge two duplicate pages: intelligently fuse source into target.
  mergeDuplicatePages: `You are a Wiki editor merging two duplicate pages. Intelligently fuse the source page body into the target page body to create the best single version. Also extract any alternative names found in either page.

**Target page body (will be kept):**
{{target_content}}

**Source page body (will be merged then deleted):**
{{source_content}}

**IMPORTANT:** You are receiving ONLY the Markdown body content (frontmatter already stripped by the system). DO not include any frontmatter in your output. The system will handle frontmatter merging programmatically. Focus on merging the semantic content only.

**Fusion rules:**
1. Keep ALL information from the target page \u2014 it is the primary version
2. Read the source page carefully. Add ONLY information from source that is GENUINELY NEW (not already in target in different words)
3. If source has verbatim quotes (Mentions in Source) not in target, add them with the source's citation
4. If source links to related entities/concepts that target doesn't, add those links in the appropriate sections
5. If source describes applications, characteristics, or details not covered by target, weave them naturally into target's existing sections
6. If there are factual contradictions, preserve both sides with source attribution in a "## Contradictions" section if one doesn't exist
7. Do NOT duplicate information already present in target
8. Preserve the target page's overall structure and section ordering

**Aliases to extract (put in the "aliases" array):**
- Alternative names, translations, abbreviations found in EITHER page's title or content
- e.g. if source is "Mixture-of-Experts-MoE" and target is "\u6DF7\u5408\u4E13\u5BB6\u6A21\u578B", aliases might include: "MoE", "Mixture of Experts", "Sparse MoE"
- Include names that someone might search for when looking for this concept
- Do NOT include the target page's own title or filename as an alias

**You MUST output ONLY a valid JSON object, no other text:**
{
  "body": "# Page Title\\n\\n## Section\\n\\n...merged content...",
  "aliases": ["alias1", "alias2", "alias3"]
}

If source adds nothing new, still output the target body AND any aliases found.`,
  // Generate aliases for a page by analyzing its content and title.
  generateAliases: `You are a knowledge curator. Given a wiki page's title and body, suggest alternative names (aliases) someone might search for when looking for this concept.

**Page title:** {{title}}

**Page body:**
{{body}}

**What to generate:**
- **CRITICAL: do NOT invent translations for established technical terms.** Only suggest
  translations that genuinely exist in real-world usage (e.g. "Neural Network" \u2194 "\u795E\u7ECF\u7F51\u7EDC",
  not "Transformer" \u2194 "\u53D8\u6362\u5668"). Real-world convention always wins.
- English is universally acceptable as a "linker language" \u2014 established English technical terms
  (e.g. "Transformer", "DNA", "API", "RoPE", "CUDA") may be kept as-is across all wikis
- Abbreviations: e.g. "Chain of Thought" \u2192 "CoT", "Mixture of Experts" \u2192 "MoE"
- Spelling variants: "Mixture-of-Experts", "Mixture of Experts", "Sparse MoE"
- Common alternative names in the field
- Full forms of abbreviations (e.g. "NTP" \u2192 "Next Token Prediction")

**Rules:**
- 3-8 aliases is ideal
- Only include names that genuinely refer to THIS concept
- Do NOT include the page title itself as an alias
- Prefer commonly-used names in the field

**You MUST output ONLY a valid JSON object, no other text:**
{"aliases": ["alias1", "alias2", "alias3"]}

If no meaningful aliases found: {"aliases": []}`
};

// src/wiki/prompts/conversation.ts
var CONVERSATION_PROMPTS = {
  evaluateConversationValue: `You are a Wiki knowledge evaluation assistant. Determine whether the following conversation contains substantive knowledge worth saving to the Wiki.

Conversation Content:
{{conversation}}

Evaluation Criteria:
- Contains specific concept explanations, analysis, or factual information (not just casual chat)
- The conversation content can be distilled into structured Wiki entries
- The information has reference value and may be consulted again in the future

Output JSON format:
{"valuable": true/false, "reason": "Reason for judgment (one sentence)"}`,
  dedupCheck: `You are a Wiki knowledge deduplication assistant. Determine whether the conversation content is already covered by existing Wiki pages.

Existing Wiki Page Index:
{{wiki_index}}

Conversation Summary:
{{conversation_summary}}

Task:
1. Analyze the knowledge topics covered in the conversation
2. Determine whether these topics already exist in the above Wiki pages
3. If all topics are already covered (semantically identical or highly similar), mark as fully_redundant
4. If some topics are new, mark as partially_new and list the new topics

Output JSON format:
{"status": "fully_redundant|partially_new|entirely_new", "new_topics": ["new topic 1"], "redundant_topics": ["covered topic 1"], "reason": "Reason for judgment (one sentence)"}`
};

// src/prompts.ts
var PROMPTS = {
  ...INGESTION_PROMPTS,
  ...GENERATION_PROMPTS,
  ...MERGE_PROMPTS,
  ...FIX_PROMPTS,
  ...LINT_PROMPTS,
  ...CONVERSATION_PROMPTS
};

// src/core/source-slug.ts
var DEFAULT_MAX_LEN = 80;
var FINGERPRINT_LEN = 6;
function sourceFingerprint(fullPath) {
  let hash = 2166136261;
  for (let i = 0; i < fullPath.length; i++) {
    hash ^= fullPath.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16).padStart(8, "0").slice(0, FINGERPRINT_LEN);
}
function basenameNoExt(fullPath) {
  const last = fullPath.split("/").pop() || fullPath;
  return last.replace(/\.md$/i, "");
}
function sourceBaseSlug(fullPath, preserveCase = false) {
  return computeSlug(basenameNoExt(fullPath), preserveCase);
}
function resolveSourceSlug(fullPath, options = {}) {
  var _a, _b;
  const maxLen = (_a = options.maxLen) != null ? _a : DEFAULT_MAX_LEN;
  const preserveCase = (_b = options.preserveCase) != null ? _b : false;
  const fp = sourceFingerprint(fullPath);
  const tail = `_${fp}`;
  const base = sourceBaseSlug(fullPath, preserveCase);
  const trimmedBase = base.slice(0, Math.max(1, maxLen - tail.length));
  return `${trimmedBase}${tail}`;
}

// src/core/incomplete-page-cleaner.ts
function isIncomplete(content) {
  const fm = parseFrontmatter(content);
  if (!fm) return false;
  return fm.generation_complete === "false";
}
async function findIncompletePages(app, wikiFolder) {
  const prefix = `${wikiFolder}/`;
  const allFiles = app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(prefix));
  const incomplete = [];
  for (const f of allFiles) {
    try {
      const content = await app.vault.read(f);
      if (isIncomplete(content)) {
        incomplete.push(f);
      }
    } catch (e) {
      console.warn(`[incomplete-page-cleaner] failed to read ${f.path}:`, e);
    }
  }
  return incomplete;
}
async function cleanIncompletePages(app, files) {
  let cleaned = 0;
  for (const f of files) {
    try {
      await app.fileManager.trashFile(f);
      cleaned++;
      console.debug(`[incomplete-page-cleaner] trashed ${f.path}`);
    } catch (e) {
      console.warn(`[incomplete-page-cleaner] failed to trash ${f.path}:`, e);
    }
  }
  return cleaned;
}
function setGenerationComplete(content, complete) {
  const fm = parseFrontmatter(content);
  const value = complete ? "true" : "false";
  const existingRe = /^(\s*)generation_complete:\s*(?:true|false)\s*$/m;
  if (fm) {
    const endIdx = content.indexOf("\n---", 3);
    if (endIdx === -1) return content;
    const fmBlock = content.substring(0, endIdx);
    const rest = content.substring(endIdx);
    if (existingRe.test(fmBlock)) {
      const newFm = fmBlock.replace(existingRe, `$1generation_complete: ${value}`);
      return newFm + rest;
    }
    return fmBlock + `
generation_complete: ${value}` + rest;
  }
  return `---
generation_complete: ${value}
---

${content}`;
}

// src/core/source-requirements.ts
function checkNonEmpty(input) {
  return isBlankSource(input.content) ? { reason: "empty" } : null;
}
function checkCompatibleType(input) {
  return input.allowedExtensions.includes(input.extension.toLowerCase()) ? null : { reason: "incompatible-type", detail: input.extension };
}
var CONTENT_CHECKS = [
  checkNonEmpty,
  checkCompatibleType
];
function checkContentRequirements(input) {
  for (const check of CONTENT_CHECKS) {
    const rejection = check(input);
    if (rejection) return rejection;
  }
  return null;
}
function hashBody(body) {
  const norm = body.trim().replace(/\s+/g, " ");
  let hash = 2166136261;
  for (let i = 0; i < norm.length; i++) {
    hash ^= norm.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  const hex = (hash >>> 0).toString(16).padStart(8, "0");
  return `${norm.length.toString(16)}-${hex}`;
}

// src/core/rate-limit.ts
function detectRateLimitFailures(failedItems, currentConcurrency, currentBatchDelay) {
  const rateLimitFailures = failedItems.filter(
    (f) => /429|rate.?limit|too many requests|throttl/i.test(f.reason || "")
  );
  if (rateLimitFailures.length === 0) return null;
  return {
    count: rateLimitFailures.length,
    rateLimitNames: rateLimitFailures.map((f) => f.name || f.reason || "unknown"),
    suggestedConcurrency: Math.max(1, currentConcurrency - 1),
    suggestedDelay: currentBatchDelay < 100 ? 500 : Math.min(2e3, Math.round(currentBatchDelay * 2))
  };
}
function formatRateLimitNotice(info, language) {
  return getText(language, "rateLimitDetected").replace("{count}", String(info.count)).replace("{suggestedConcurrency}", String(info.suggestedConcurrency)).replace("{suggestedDelay}", String(info.suggestedDelay));
}

// src/core/arrays.ts
function extractSourceTags(content) {
  const fm = parseFrontmatter(content);
  if (!fm) return [];
  const raw = fm.tags;
  if (Array.isArray(raw)) {
    return raw.map((t) => String(t).trim()).filter((t) => t.length > 0);
  }
  return [];
}
function coerceToArray(value) {
  if (Array.isArray(value)) {
    return value;
  }
  return [];
}

// src/wiki/system-prompts.ts
function buildWikiLanguageDirective(settings) {
  const lang = settings.wikiLanguage || "en";
  const langName = WIKI_LANGUAGES[lang] || lang;
  return `IMPORTANT: You MUST write ALL content in ${langName}. Every page title, summary, description, and label must be in ${langName}. Do NOT output any content in other languages.`;
}
var SECTION_LABELS = {
  en: {
    basic_information: "Basic Information",
    description: "Description",
    related_content: "Related Content",
    mentions_in_source: "Mentions in Source",
    new_information: "New Information",
    definition: "Definition",
    key_characteristics: "Key Characteristics",
    applications: "Applications",
    related_concepts: "Related Concepts",
    related_entities: "Related Entities",
    source: "Source",
    core_content: "Core Content",
    key_entities: "Key Entities",
    key_concepts: "Key Concepts",
    main_points: "Main Points",
    resolved_contradictions: "Resolved Contradictions",
    new_claim: "New Claim",
    existing_knowledge: "Existing Knowledge",
    resolution_suggestion: "Resolution Suggestion",
    source_page: "Source Page",
    related_pages: "Related Pages",
    updated: "Updated"
  },
  zh: {
    basic_information: "\u57FA\u672C\u4FE1\u606F",
    description: "\u63CF\u8FF0",
    related_content: "\u76F8\u5173\u5185\u5BB9",
    mentions_in_source: "\u6765\u6E90\u63D0\u53CA",
    new_information: "\u65B0\u4FE1\u606F",
    definition: "\u5B9A\u4E49",
    key_characteristics: "\u5173\u952E\u7279\u5F81",
    applications: "\u5E94\u7528",
    related_concepts: "\u76F8\u5173\u6982\u5FF5",
    related_entities: "\u76F8\u5173\u5B9E\u4F53",
    source: "\u6765\u6E90",
    core_content: "\u6838\u5FC3\u5185\u5BB9",
    key_entities: "\u5173\u952E\u5B9E\u4F53",
    key_concepts: "\u5173\u952E\u6982\u5FF5",
    main_points: "\u8981\u70B9",
    resolved_contradictions: "\u5DF2\u89E3\u51B3\u7684\u77DB\u76FE",
    new_claim: "\u65B0\u4E3B\u5F20",
    existing_knowledge: "\u5DF2\u6709\u77E5\u8BC6",
    resolution_suggestion: "\u89E3\u51B3\u5EFA\u8BAE",
    source_page: "\u6765\u6E90\u9875\u9762",
    related_pages: "\u76F8\u5173\u9875\u9762",
    updated: "\u66F4\u65B0\u4E8E"
  },
  // v1.22.0: Traditional Chinese (zh-Hant) for HK/MO/TW/MY/SG users
  "zh-Hant": {
    basic_information: "\u57FA\u672C\u8CC7\u8A0A",
    description: "\u63CF\u8FF0",
    related_content: "\u76F8\u95DC\u5167\u5BB9",
    mentions_in_source: "\u4F86\u6E90\u63D0\u53CA",
    new_information: "\u65B0\u8CC7\u8A0A",
    definition: "\u5B9A\u7FA9",
    key_characteristics: "\u95DC\u9375\u7279\u5FB5",
    applications: "\u61C9\u7528",
    related_concepts: "\u76F8\u95DC\u6982\u5FF5",
    related_entities: "\u76F8\u95DC\u5BE6\u9AD4",
    source: "\u4F86\u6E90",
    core_content: "\u6838\u5FC3\u5167\u5BB9",
    key_entities: "\u95DC\u9375\u5BE6\u9AD4",
    key_concepts: "\u95DC\u9375\u6982\u5FF5",
    main_points: "\u8981\u9EDE",
    resolved_contradictions: "\u5DF2\u89E3\u6C7A\u7684\u77DB\u76FE",
    new_claim: "\u65B0\u4E3B\u5F35",
    existing_knowledge: "\u5DF2\u6709\u77E5\u8B58",
    resolution_suggestion: "\u89E3\u6C7A\u5EFA\u8B70",
    source_page: "\u4F86\u6E90\u9801\u9762",
    related_pages: "\u76F8\u95DC\u9801\u9762",
    updated: "\u66F4\u65B0\u65BC"
  },
  ja: {
    basic_information: "\u57FA\u672C\u60C5\u5831",
    description: "\u8AAC\u660E",
    related_content: "\u95A2\u9023\u30B3\u30F3\u30C6\u30F3\u30C4",
    mentions_in_source: "\u30BD\u30FC\u30B9\u3067\u306E\u8A00\u53CA",
    new_information: "\u65B0\u60C5\u5831",
    definition: "\u5B9A\u7FA9",
    key_characteristics: "\u4E3B\u306A\u7279\u5FB4",
    applications: "\u5FDC\u7528",
    related_concepts: "\u95A2\u9023\u6982\u5FF5",
    related_entities: "\u95A2\u9023\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3",
    source: "\u30BD\u30FC\u30B9",
    core_content: "\u6838\u5FC3\u5185\u5BB9",
    key_entities: "\u4E3B\u8981\u30A8\u30F3\u30C6\u30A3\u30C6\u30A3",
    key_concepts: "\u4E3B\u8981\u6982\u5FF5",
    main_points: "\u8981\u70B9",
    resolved_contradictions: "\u89E3\u6C7A\u6E08\u307F\u306E\u77DB\u76FE",
    new_claim: "\u65B0\u3057\u3044\u4E3B\u5F35",
    existing_knowledge: "\u65E2\u5B58\u306E\u77E5\u8B58",
    resolution_suggestion: "\u89E3\u6C7A\u6848",
    source_page: "\u30BD\u30FC\u30B9\u30DA\u30FC\u30B8",
    related_pages: "\u95A2\u9023\u30DA\u30FC\u30B8",
    updated: "\u66F4\u65B0\u65E5"
  },
  ko: {
    basic_information: "\uAE30\uBCF8 \uC815\uBCF4",
    description: "\uC124\uBA85",
    related_content: "\uAD00\uB828 \uCF58\uD150\uCE20",
    mentions_in_source: "\uCD9C\uCC98 \uC5B8\uAE09",
    new_information: "\uC0C8 \uC815\uBCF4",
    definition: "\uC815\uC758",
    key_characteristics: "\uC8FC\uC694 \uD2B9\uC9D5",
    applications: "\uC751\uC6A9",
    related_concepts: "\uAD00\uB828 \uAC1C\uB150",
    related_entities: "\uAD00\uB828 \uC5D4\uD2F0\uD2F0",
    source: "\uCD9C\uCC98",
    core_content: "\uD575\uC2EC \uB0B4\uC6A9",
    key_entities: "\uC8FC\uC694 \uC5D4\uD2F0\uD2F0",
    key_concepts: "\uC8FC\uC694 \uAC1C\uB150",
    main_points: "\uC8FC\uC694 \uC0AC\uD56D",
    resolved_contradictions: "\uD574\uACB0\uB41C \uBAA8\uC21C",
    new_claim: "\uC0C8 \uC8FC\uC7A5",
    existing_knowledge: "\uAE30\uC874 \uC9C0\uC2DD",
    resolution_suggestion: "\uD574\uACB0 \uC81C\uC548",
    source_page: "\uCD9C\uCC98 \uD398\uC774\uC9C0",
    related_pages: "\uAD00\uB828 \uD398\uC774\uC9C0",
    updated: "\uC5C5\uB370\uC774\uD2B8"
  },
  de: {
    basic_information: "Grundlegende Informationen",
    description: "Beschreibung",
    related_content: "Verwandte Inhalte",
    mentions_in_source: "Erw\xE4hnungen in der Quelle",
    new_information: "Neue Informationen",
    definition: "Definition",
    key_characteristics: "Hauptmerkmale",
    applications: "Anwendungen",
    related_concepts: "Verwandte Konzepte",
    related_entities: "Verwandte Entit\xE4ten",
    source: "Quelle",
    core_content: "Kerninhalt",
    key_entities: "Wichtige Entit\xE4ten",
    key_concepts: "Wichtige Konzepte",
    main_points: "Hauptpunkte",
    resolved_contradictions: "Aufgel\xF6ste Widerspr\xFCche",
    new_claim: "Neue Behauptung",
    existing_knowledge: "Bestehendes Wissen",
    resolution_suggestion: "L\xF6sungsvorschlag",
    source_page: "Quellseite",
    related_pages: "Verwandte Seiten",
    updated: "Aktualisiert"
  },
  fr: {
    basic_information: "Informations de base",
    description: "Description",
    related_content: "Contenu associ\xE9",
    mentions_in_source: "Mentions dans la source",
    new_information: "Nouvelles informations",
    definition: "D\xE9finition",
    key_characteristics: "Caract\xE9ristiques principales",
    applications: "Applications",
    related_concepts: "Concepts associ\xE9s",
    related_entities: "Entit\xE9s associ\xE9es",
    source: "Source",
    core_content: "Contenu principal",
    key_entities: "Entit\xE9s cl\xE9s",
    key_concepts: "Concepts cl\xE9s",
    main_points: "Points principaux",
    resolved_contradictions: "Contradictions r\xE9solues",
    new_claim: "Nouvelle affirmation",
    existing_knowledge: "Connaissances existantes",
    resolution_suggestion: "Suggestion de r\xE9solution",
    source_page: "Page source",
    related_pages: "Pages associ\xE9es",
    updated: "Mis \xE0 jour"
  },
  es: {
    basic_information: "Informaci\xF3n b\xE1sica",
    description: "Descripci\xF3n",
    related_content: "Contenido relacionado",
    mentions_in_source: "Menciones en la fuente",
    new_information: "Nueva informaci\xF3n",
    definition: "Definici\xF3n",
    key_characteristics: "Caracter\xEDsticas clave",
    applications: "Aplicaciones",
    related_concepts: "Conceptos relacionados",
    related_entities: "Entidades relacionadas",
    source: "Fuente",
    core_content: "Contenido principal",
    key_entities: "Entidades clave",
    key_concepts: "Conceptos clave",
    main_points: "Puntos principales",
    resolved_contradictions: "Contradicciones resueltas",
    new_claim: "Nueva afirmaci\xF3n",
    existing_knowledge: "Conocimiento existente",
    resolution_suggestion: "Sugerencia de resoluci\xF3n",
    source_page: "P\xE1gina de origen",
    related_pages: "P\xE1ginas relacionadas",
    updated: "Actualizado"
  },
  pt: {
    basic_information: "Informa\xE7\xF5es b\xE1sicas",
    description: "Descri\xE7\xE3o",
    related_content: "Conte\xFAdo relacionado",
    mentions_in_source: "Men\xE7\xF5es na fonte",
    new_information: "Novas informa\xE7\xF5es",
    definition: "Defini\xE7\xE3o",
    key_characteristics: "Caracter\xEDsticas principais",
    applications: "Aplica\xE7\xF5es",
    related_concepts: "Conceitos relacionados",
    related_entities: "Entidades relacionadas",
    source: "Fonte",
    core_content: "Conte\xFAdo principal",
    key_entities: "Entidades principais",
    key_concepts: "Conceitos principais",
    main_points: "Pontos principais",
    resolved_contradictions: "Contradi\xE7\xF5es resolvidas",
    new_claim: "Nova afirma\xE7\xE3o",
    existing_knowledge: "Conhecimento existente",
    resolution_suggestion: "Sugest\xE3o de resolu\xE7\xE3o",
    source_page: "P\xE1gina de origem",
    related_pages: "P\xE1ginas relacionadas",
    updated: "Atualizado"
  },
  it: {
    basic_information: "Informazioni di base",
    description: "Descrizione",
    related_content: "Contenuti correlati",
    mentions_in_source: "Menzioni nella sorgente",
    new_information: "Nuove informazioni",
    definition: "Definizione",
    key_characteristics: "Caratteristiche principali",
    applications: "Applicazioni",
    related_concepts: "Concetti correlati",
    related_entities: "Entit\xE0 correlate",
    source: "Sorgente",
    core_content: "Contenuto principale",
    key_entities: "Entit\xE0 chiave",
    key_concepts: "Concetti chiave",
    main_points: "Punti principali",
    resolved_contradictions: "Contraddizioni risolte",
    new_claim: "Nuova affermazione",
    existing_knowledge: "Conoscenza esistente",
    resolution_suggestion: "Suggerimento di risoluzione",
    source_page: "Pagina sorgente",
    related_pages: "Pagine correlate",
    updated: "Aggiornato"
  }
};
function getSectionLabels(settings) {
  const lang = settings.wikiLanguage || "en";
  return SECTION_LABELS[lang] || SECTION_LABELS.en;
}
var GRANULARITY_INSTRUCTIONS = {
  fine: "Extract ALL entities and concepts worth recording from the source, including those mentioned only once or tangentially.",
  standard: "Extract important and moderately important entities and concepts from the source. Ignore minor items mentioned only in passing.",
  coarse: "Extract only the most essential entities and concepts from the source \u2014 those without which the text cannot be understood. Quality over quantity.",
  minimal: "Extract only the most critical entities and concepts from the source \u2014 maximum 3 total items. Extreme selectivity for cost control.",
  custom: ""
  // placeholder — never used; getGranularityInstruction handles custom dynamically
};
var GRANULARITY_FIX_LIMITS = {
  fine: { maxEntities: 6, maxConcepts: 6 },
  standard: { maxEntities: 3, maxConcepts: 3 },
  coarse: { maxEntities: 2, maxConcepts: 2 },
  minimal: { maxEntities: 1, maxConcepts: 2 },
  custom: { maxEntities: 0, maxConcepts: 0 }
  // placeholder — never used
};
function getGranularityInstruction(settings) {
  var _a, _b;
  const granularity = settings.extractionGranularity || "standard";
  if (granularity === "custom") {
    const entityLimit = (_a = settings.customEntityLimit) != null ? _a : 5;
    const conceptLimit = (_b = settings.customConceptLimit) != null ? _b : 5;
    return `Extract at most ${entityLimit} entities and at most ${conceptLimit} concepts from the source. If you reach either limit, stop extracting that type.`;
  }
  return GRANULARITY_INSTRUCTIONS[granularity] || GRANULARITY_INSTRUCTIONS.standard;
}
function appendGranularityToPrompt(prompt, settings) {
  const instruction = getGranularityInstruction(settings);
  if (!instruction) return prompt;
  return `${prompt}

${instruction}`;
}
function getGranularityFixLimits(settings) {
  var _a, _b;
  const granularity = settings.extractionGranularity || "standard";
  if (granularity === "custom") {
    return {
      maxEntities: (_a = settings.customEntityLimit) != null ? _a : 5,
      maxConcepts: (_b = settings.customConceptLimit) != null ? _b : 5
    };
  }
  return GRANULARITY_FIX_LIMITS[granularity] || GRANULARITY_FIX_LIMITS.standard;
}
function applySectionLabels(prompt, settings) {
  const labels = getSectionLabels(settings);
  let result = prompt;
  for (const [key, label] of Object.entries(labels)) {
    result = result.replace(new RegExp(`\\{\\{section_${key}\\}\\}`, "g"), label);
  }
  return result;
}
function appendTagVocabularyToPrompt(prompt, settings) {
  const section = buildActiveTagVocabularySection(settings);
  return `${prompt}

${section}`;
}
async function buildSystemPrompt(settings, getSchemaContext, task) {
  var _a;
  const parts = [];
  const langDirective = buildWikiLanguageDirective(settings);
  if (langDirective) parts.push(langDirective);
  const schemaContext = await getSchemaContext(task);
  if (schemaContext) parts.push(schemaContext);
  const schemaHasTagVocab = (_a = schemaContext == null ? void 0 : schemaContext.includes("Active Tag Vocabulary")) != null ? _a : false;
  if (!schemaHasTagVocab) {
    const tagVocab = buildActiveTagVocabularySection(settings);
    if (tagVocab) parts.push(tagVocab);
  }
  return parts.length > 0 ? parts.join("\n\n") : void 0;
}
function buildActiveTagVocabularySection(settings) {
  const entities = getActiveEntityTags(settings);
  const concepts = getActiveConceptTags(settings);
  const lines = [];
  lines.push("## Active Tag Vocabulary (Issue #85 \u2014 user-controlled)");
  lines.push("");
  lines.push(
    "When assigning `type` to an entity or concept, you MUST use one of the following allowed values. Do NOT invent new types."
  );
  lines.push("");
  lines.push("**Entity types** (entity_type field \u2014 one of):");
  for (const t of entities) lines.push(`- ${t}`);
  lines.push("");
  lines.push("**Concept types** (concept_type field \u2014 one of):");
  for (const t of concepts) lines.push(`- ${t}`);
  lines.push("");
  lines.push(
    "If a discovered item does not clearly fit any of the above, choose the closest match. Do NOT emit a free-form type string \u2014 the frontmatter validator will reject it."
  );
  return lines.join("\n");
}

// src/wiki/lint/get-existing-pages.ts
async function getExistingWikiPages(app, wikiFolder) {
  const wikiFiles = app.vault.getMarkdownFiles().filter(
    (f) => f.path.startsWith(wikiFolder) && !f.path.includes("index.md") && !f.path.includes("log.md") && !f.path.includes("/schema/") && !f.path.includes("/contradictions/")
  );
  const pages = [];
  for (const f of wikiFiles) {
    const relPath = f.path.replace(wikiFolder + "/", "").replace(".md", "");
    const content = await app.vault.read(f);
    const fm = parseFrontmatter(content);
    pages.push({
      path: f.path,
      title: f.basename,
      wikiLink: `[[${relPath}|${f.basename}]]`,
      aliases: Array.isArray(fm == null ? void 0 : fm.aliases) ? fm.aliases : void 0
    });
  }
  return pages;
}

// src/core/json.ts
async function parseJsonResponse(response, repairFn) {
  console.debug("parseJsonResponse parsing started... response length:", response.length);
  try {
    let normalized = response.trim();
    normalized = normalized.replace(/<think\b[^>]*>[\s\S]*?<\/think>/gi, "");
    normalized = normalized.replace(/<thinking\b[^>]*>[\s\S]*?<\/thinking>/gi, "");
    normalized = normalized.trim();
    normalized = normalized.replace(/^```(?:json|markdown|md)?\s*\n?/, "");
    normalized = normalized.replace(/\n?```$/, "");
    normalized = normalized.trim();
    if (normalized.startsWith("{{")) {
      normalized = normalized.substring(1);
      console.debug('Prefill echo "{{" detected, removing leading {');
    } else if (normalized.length > 1 && normalized[0] === "{") {
      const afterFirst = normalized.substring(1).trimStart();
      if (afterFirst.startsWith("{") || afterFirst.startsWith("```")) {
        normalized = afterFirst;
        console.debug('Newline-separated "{\\n{" detected {\\n{\uFF0Cremoving leading {');
      }
    }
    if (normalized.length > 0 && normalized[0] !== "{") {
      const withBrace = "{" + normalized;
      try {
        console.debug("first char not '{', prepended '{' and parsed successfully");
        return JSON.parse(withBrace);
      } catch (e) {
        console.debug("prepending '{' still failed, continuing");
      }
    }
    try {
      return JSON.parse(normalized);
    } catch (directError) {
      const msg = directError instanceof SyntaxError ? directError.message : "";
      const afterMatch = msg.match(/after JSON at position (\d+)/);
      if (afterMatch) {
        const endPos = parseInt(afterMatch[1], 10);
        const prefix = normalized.substring(0, endPos);
        console.debug("extra content after JSON detected (position %d)\uFF0Cprefix extracted (length %d)", endPos, prefix.length);
        try {
          console.debug("prefix parsed successfully");
          return JSON.parse(prefix);
        } catch (e) {
          console.debug("prefix parse failed, continuing");
        }
      }
    }
    const firstBrace = normalized.indexOf("{");
    if (firstBrace !== -1) {
      const balanced = extractBalancedJson(normalized, firstBrace);
      if (balanced) {
        const fixed = fixCommonJsonIssues(balanced);
        try {
          return JSON.parse(fixed);
        } catch (braceError) {
          console.debug("brace-count extraction failed:", String(braceError).slice(0, 80));
        }
        if (repairFn) {
          try {
            const repaired = await repairFn(balanced);
            const cleanedLlm = repaired.trim().replace(/^```(?:json)?\s*\n?/, "").replace(/\n?```$/, "").trim();
            const final = fixCommonJsonIssues(cleanedLlm);
            return JSON.parse(final);
          } catch (llmError) {
            console.error("LLM repair also failed (brace-count):", String(llmError).slice(0, 80));
          }
        }
      }
    }
    const jsonMatch = normalized.match(/\{[\s\S]*\}/);
    if (jsonMatch) {
      const candidate = jsonMatch[0];
      const fixed = fixCommonJsonIssues(candidate);
      try {
        return JSON.parse(fixed);
      } catch (regexError) {
        console.debug("greedy regex extraction failed:", String(regexError).slice(0, 80));
      }
      if (repairFn) {
        try {
          const repaired = await repairFn(candidate);
          const cleanedLlm = repaired.trim().replace(/^```(?:json)?\s*\n?/, "").replace(/\n?```$/, "").trim();
          const final = fixCommonJsonIssues(cleanedLlm);
          return JSON.parse(final);
        } catch (llmError) {
          console.error("LLM repair also failed (greedy regex):", String(llmError).slice(0, 80));
        }
      }
    }
    console.error("JSON parse completely failed (length %d)", response.length);
    console.error("first 200 chars after normalization:", normalized.substring(0, 200));
    console.error("last 200 chars after normalization:", normalized.substring(Math.max(0, normalized.length - 200)));
    return null;
  } catch (error) {
    console.error("parseJsonResponse exception:", error);
    return null;
  }
}
function extractBalancedJson(text, startPos) {
  let depth = 0;
  let inString = false;
  let escape = false;
  for (let i = startPos; i < text.length; i++) {
    const ch = text[i];
    if (escape) {
      escape = false;
      continue;
    }
    if (ch === "\\" && inString) {
      escape = true;
      continue;
    }
    if (ch === '"') {
      inString = !inString;
      continue;
    }
    if (inString) continue;
    if (ch === "{") depth++;
    if (ch === "}") {
      depth--;
      if (depth === 0) {
        return text.substring(startPos, i + 1);
      }
    }
  }
  return null;
}
function fixCommonJsonIssues(json) {
  let fixed = json.replace(/,\s*\}/g, "}").replace(/,\s*\]/g, "]");
  fixed = escapeContentQuotes(fixed);
  fixed = fixed.replace(/"\s*\n\s*"/g, '",\n"');
  fixed = fixed.replace(/,\s*\}/g, "}").replace(/,\s*\]/g, "]");
  return fixed;
}
function escapeContentQuotes(json) {
  const out = [];
  let inString = false;
  let i = 0;
  while (i < json.length) {
    const ch = json[i];
    if (ch === "\\" && inString) {
      out.push(ch);
      i++;
      if (i < json.length) out.push(json[i]);
      i++;
      continue;
    }
    if (!inString && ch === '"') {
      inString = true;
      out.push(ch);
      i++;
      continue;
    }
    if (inString && ch === '"') {
      let peek = i + 1;
      while (peek < json.length && isJsonWhitespace(json[peek])) peek++;
      const nextCh = peek < json.length ? json[peek] : "";
      if (nextCh === ":" || nextCh === "," || nextCh === "}" || nextCh === "]" || peek >= json.length) {
        inString = false;
        out.push(ch);
      } else {
        out.push('\\"');
      }
      i++;
      continue;
    }
    out.push(ch);
    i++;
  }
  return out.join("");
}
function isJsonWhitespace(ch) {
  return ch === " " || ch === "	" || ch === "\n" || ch === "\r";
}

// src/core/dead-link-detector.ts
function findDeadLinkTarget(pages, targetName) {
  const targetBasename = targetName.includes("/") ? targetName.split("/").pop() : targetName;
  const lowerTarget = targetBasename.toLowerCase();
  let match = pages.find((p) => p.title.toLowerCase() === lowerTarget);
  if (!match) {
    match = pages.find(
      (p) => {
        var _a;
        return (_a = p.aliases) == null ? void 0 : _a.some((a) => a.toLowerCase() === lowerTarget);
      }
    );
  }
  return match;
}
function buildDeadLinkReplacement(page, wikiFolder) {
  const relPath = page.path.replace(wikiFolder + "/", "").replace(".md", "");
  return `[[${relPath}|${page.title}]]`;
}
function replaceDeadLink(content, targetName, replacement) {
  const linkRegex = /\[\[([^\]|#]+)(?:[|#][^\]]+)?\]\]/g;
  return content.replace(
    linkRegex,
    (fullMatch, capturedTarget) => {
      if (capturedTarget.trim() === targetName) return replacement;
      return fullMatch;
    }
  );
}

// src/wiki/lint/fix-dead-link.ts
var PLURAL_MAP = {
  entity: WIKI_SUBFOLDERS.entities,
  concept: WIKI_SUBFOLDERS.concepts
};
function makeRelPath(path, wikiFolder) {
  return path.replace(wikiFolder + "/", "").replace(/\.md$/i, "");
}
function replaceTargetLink(sourceContent, targetName, newLink) {
  const linkRegex = /\[\[([^\]|#]+)(?:[|#][^\]]+)?\]\]/g;
  return sourceContent.replace(
    linkRegex,
    (fullMatch, capturedTarget) => {
      if (capturedTarget.trim() === targetName) return newLink;
      return fullMatch;
    }
  );
}
function buildStubContent(params) {
  const { title, stubType, referringPageRel } = params;
  const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  const defaultTag = stubType === "entity" ? "other" : "term";
  return `---
type: ${stubType}
created: ${today}
sources: ["[[${referringPageRel}]]"]
tags: [${defaultTag}]
generation_complete: false
---
# ${title}

> Stub created by Fix Dead Links \u2014 referenced by [[${referringPageRel}]]. Will be filled by next ingest of an actual source that defines this entity.
`;
}
async function fixDeadLink(ctx, sourcePath, targetName) {
  const existingPages = await getExistingWikiPages(
    ctx.app,
    ctx.settings.wikiFolder
  );
  const sourceContent = await ctx.tryReadFile(sourcePath) || "(empty)";
  const targetBasename = targetName.includes("/") ? targetName.split("/").pop() : targetName;
  const preMatch = findDeadLinkTarget(existingPages, targetBasename);
  if (preMatch) {
    const newLink2 = buildDeadLinkReplacement(preMatch, ctx.settings.wikiFolder);
    const updatedContent2 = replaceDeadLink(sourceContent, targetName, newLink2);
    await ctx.createOrUpdateFile(sourcePath, updatedContent2);
    return `pre-check corrected (alias match): ${newLink2}`;
  }
  const pagesList = existingPages.filter((p) => {
    const bn = p.title || "";
    const hasPollutedBasename = /^(entities|concepts|sources)([^\s\-_a-zA-Z0-9])/.test(bn);
    return !hasPollutedBasename;
  }).map((p) => {
    var _a;
    const aliasSuffix = ((_a = p.aliases) == null ? void 0 : _a.length) ? ` \`aliases: ${p.aliases.join(", ")}\`` : "";
    return `- ${p.wikiLink}${aliasSuffix}`;
  }).join("\n");
  const prompt = PROMPTS.fixDeadLink.replace("{{source_content}}", sourceContent.substring(0, 2e3)).replace("{{target_name}}", targetName).replace("{{existing_pages}}", pagesList.substring(0, 3e3));
  const client = ctx.getClient();
  if (!client) return "no action taken (no client)";
  let response = await client.createMessage({
    model: ctx.settings.model,
    max_tokens: TOKENS_LINT_PAGE_FIX,
    system: await buildSystemPrompt(
      ctx.settings,
      ctx.getSchemaContext,
      "lint"
    ),
    messages: [{ role: "user", content: prompt }],
    response_format: { type: "json_object" },
    ...ctx.settings.disableThinking ? { enableThinking: false } : {}
  });
  if (!response) {
    console.debug(
      `fixDeadLink: empty response for target "${targetName}", retrying without JSON mode`
    );
    response = await client.createMessage({
      model: ctx.settings.model,
      max_tokens: TOKENS_LINT_PAGE_FIX,
      system: await buildSystemPrompt(
        ctx.settings,
        ctx.getSchemaContext,
        "lint"
      ),
      messages: [{ role: "user", content: prompt }],
      ...ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
  }
  const result = await parseJsonResponse(response);
  if ((result == null ? void 0 : result.action) === "correct" && result.correct_link) {
    let newLink2 = result.correct_link.trim();
    if (!newLink2.startsWith("[[")) {
      newLink2 = `[[${newLink2}]]`;
    }
    const updatedContent2 = replaceTargetLink(sourceContent, targetName, newLink2);
    await ctx.createOrUpdateFile(sourcePath, updatedContent2);
    return `corrected: ${newLink2}`;
  }
  if ((result == null ? void 0 : result.action) === "create_stub" && result.stub_title) {
    const sanitizedTitle = result.stub_title.replace(/^(entities|concepts|sources)([^\s\-_a-zA-Z0-9])/, "$2");
    const stubTitleLower = sanitizedTitle.toLowerCase();
    const safetySlug = slugify(sanitizedTitle).toLowerCase();
    const aliasMatch = existingPages.find(
      (p) => {
        var _a, _b;
        return p.title.toLowerCase() === stubTitleLower || ((_a = p.aliases) == null ? void 0 : _a.some((a) => a.toLowerCase() === stubTitleLower)) || slugify(p.title).toLowerCase() === safetySlug || ((_b = p.aliases) == null ? void 0 : _b.some((a) => slugify(a).toLowerCase() === safetySlug));
      }
    );
    if (aliasMatch) {
      const newLink3 = `[[${makeRelPath(aliasMatch.path, ctx.settings.wikiFolder)}|${aliasMatch.title}]]`;
      const updatedContent3 = replaceTargetLink(sourceContent, targetName, newLink3);
      await ctx.createOrUpdateFile(sourcePath, updatedContent3);
      return `safety-net corrected (alias match for stub): ${newLink3}`;
    }
    const stubType2 = result.stub_type || "entity";
    const stubDir2 = PLURAL_MAP[stubType2] || `${stubType2}s`;
    const stubSlug2 = slugify(sanitizedTitle, ctx.settings.slugCase === "preserve");
    const stubPath2 = `${ctx.settings.wikiFolder}/${stubDir2}/${stubSlug2}.md`;
    const sourceRel2 = makeRelPath(sourcePath, ctx.settings.wikiFolder);
    const stubContent2 = buildStubContent({
      title: sanitizedTitle,
      stubType: stubType2,
      wikiFolder: ctx.settings.wikiFolder,
      referringPageRel: sourceRel2
    });
    await ctx.createOrUpdateFile(stubPath2, stubContent2);
    const newLink2 = `[[${stubDir2}/${stubSlug2}|${sanitizedTitle}]]`;
    const updatedContent2 = replaceTargetLink(sourceContent, targetName, newLink2);
    await ctx.createOrUpdateFile(sourcePath, updatedContent2);
    return `stub created (unfilled): ${stubPath2} \u2014 will be filled by next ingest of a real source`;
  }
  const lowerTarget = targetBasename.toLowerCase();
  const targetSlug = slugify(targetBasename).toLowerCase();
  let match = existingPages.find(
    (p) => p.title.toLowerCase() === lowerTarget || slugify(p.title).toLowerCase() === targetSlug
  );
  if (!match) {
    match = existingPages.find(
      (p) => {
        var _a;
        return (_a = p.aliases) == null ? void 0 : _a.some(
          (a) => a.toLowerCase() === lowerTarget || slugify(a).toLowerCase() === targetSlug
        );
      }
    );
  }
  if (match) {
    const newLink2 = `[[${makeRelPath(match.path, ctx.settings.wikiFolder)}|${match.title}]]`;
    const updatedContent2 = replaceTargetLink(sourceContent, targetName, newLink2);
    await ctx.createOrUpdateFile(sourcePath, updatedContent2);
    return `fallback corrected: ${newLink2}`;
  }
  const cleanBasename = targetBasename.replace(/^(entities|concepts|sources)([^\s\-_a-zA-Z0-9])/, "$2");
  const stubType = targetName.includes("/entities/") ? "entity" : "concept";
  const stubDir = stubType === "entity" ? WIKI_SUBFOLDERS.entities : WIKI_SUBFOLDERS.concepts;
  const stubSlug = slugify(cleanBasename, ctx.settings.slugCase === "preserve");
  const stubPath = `${ctx.settings.wikiFolder}/${stubDir}/${stubSlug}.md`;
  const sourceRel = makeRelPath(sourcePath, ctx.settings.wikiFolder);
  const stubContent = buildStubContent({
    title: cleanBasename,
    stubType,
    wikiFolder: ctx.settings.wikiFolder,
    referringPageRel: sourceRel
  });
  await ctx.createOrUpdateFile(stubPath, stubContent);
  const newLink = `[[${stubDir}/${stubSlug}|${cleanBasename}]]`;
  const updatedContent = replaceTargetLink(sourceContent, targetName, newLink);
  await ctx.createOrUpdateFile(sourcePath, updatedContent);
  return `fallback stub created (unfilled): ${stubPath} \u2014 will be filled by next ingest of a real source`;
}

// src/core/prompt-builders.ts
function buildEmptyPagePrompt(template, context) {
  const truncatedIndex = context.wikiIndex.substring(0, 2e3);
  return template.replace(/\{\{page_type\}\}/g, context.pageType).replace(/\{\{existing_content\}\}/g, context.existingContent).replace(/\{\{wiki_index\}\}/g, truncatedIndex).replace(/\{\{section_labels\}\}/g, context.sectionLabelsHint).replace(/\{\{max_entities\}\}/g, String(context.maxEntities)).replace(/\{\{max_concepts\}\}/g, String(context.maxConcepts));
}
function cleanWikiIndex(indexContent) {
  return indexContent.split("\n").filter((line) => {
    if (/\[\[(entities|concepts|sources)\/[^\]]*\/\1[^\s\-_|\]]/.test(line)) return false;
    if (/\[\[(entities|concepts|sources)\/[^|\]]+\|(entities|concepts|sources)\//.test(line)) return false;
    return true;
  }).join("\n");
}
function correctLinkPollution(content) {
  const DISPLAY_POLLUTION_REGEX = /\[\[(entities|concepts|sources)\/[^|\]]+\|(entities|concepts|sources)\/[^|\]]+\]\]/g;
  const PATH_DUP_REGEX = /\[\[(entities|concepts|sources)\/\1([^\s\-_|\]]+)(\|[^\]]+)?\]\]/g;
  let cleaned = content.replace(
    DISPLAY_POLLUTION_REGEX,
    (match) => {
      const parts = match.match(/\[\[([^|\]]+)\|([^|\]]+)\]\]/);
      if (parts) {
        const path = parts[1];
        const pollutedDisplay = parts[2];
        const cleanDisplay = pollutedDisplay.replace(/^(entities|concepts|sources)\//, "");
        return `[[${path}|${cleanDisplay}]]`;
      }
      return match;
    }
  );
  cleaned = cleaned.replace(
    PATH_DUP_REGEX,
    (_match, folder, rest, display) => {
      const displayPart = display || "";
      return `[[${folder}/${rest}${displayPart}]]`;
    }
  );
  return cleaned;
}
function normalizeLLMPath(path, wikiFolder) {
  if (!path) return path;
  if (path.startsWith(wikiFolder + "/") || path === wikiFolder) {
    return path;
  }
  if (path.startsWith("wiki/")) {
    return wikiFolder + "/" + path.slice(5);
  }
  return wikiFolder + "/" + path;
}
function normalizeWikiLinkContent(content, wikiFolder) {
  if (!content || wikiFolder === "wiki") return content;
  return content.replace(/\[\[wiki\/([^\]]+)\]\]/g, (_match, rest) => {
    return `[[${wikiFolder}/${rest}]]`;
  });
}

// src/wiki/lint/utils.ts
function escapeRegex(s) {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function buildSectionLabelsHint(settings) {
  const labels = getSectionLabels(settings);
  const entityLabels = [
    `- ${labels.basic_information}`,
    `- ${labels.description}`,
    `- ${labels.related_entities}`,
    `- ${labels.related_concepts}`,
    `- ${labels.mentions_in_source}`
  ].join("\n");
  const conceptLabels = [
    `- ${labels.definition}`,
    `- ${labels.key_characteristics}`,
    `- ${labels.applications}`,
    `- ${labels.related_concepts}`,
    `- ${labels.related_entities}`,
    `- ${labels.mentions_in_source}`
  ].join("\n");
  const sourceLabels = [
    `- ${labels.source}`,
    `- ${labels.core_content}`,
    `- ${labels.key_entities}`,
    `- ${labels.key_concepts}`,
    `- ${labels.main_points}`
  ].join("\n");
  return `Entity pages use:
${entityLabels}

Concept pages use:
${conceptLabels}

Source pages use:
${sourceLabels}`;
}
function normalizeFrontmatterDates(content, dateStr) {
  const fmMatch = content.match(/^---\n([\s\S]*?)\n---/);
  if (!fmMatch) return content;
  const fmContent = fmMatch[1];
  const hasUpdated = /^updated:\s*.+$/m.test(fmContent);
  let newFm;
  if (hasUpdated) {
    newFm = fmContent.replace(/^updated:\s*.+$/m, `updated: ${dateStr}`);
  } else {
    newFm = fmContent + `
updated: ${dateStr}`;
  }
  return content.replace(/^---\n[\s\S]*?\n---/, `---
${newFm}
---`);
}
function fixDoubleNestedWikiLinks(content) {
  let fixed = 0;
  const doubleNestRegex = /\[\[\[\[([^\]|#]+)(?:[|#]([^\]]+))?\]\]\]\]/g;
  const result = content.replace(doubleNestRegex, (_fullMatch, target, display) => {
    fixed++;
    if (display) {
      return `[[${target}|${display}]]`;
    }
    return `[[${target}]]`;
  });
  return { fixed, content: result };
}
var EMPTY_CONTENT_STRIP = /[#*\-_>\s\n[\]|—]/g;
var MIN_SUBSTANTIVE_CHARS = 50;
var STUB_MARKER = "Auto-generated stub page";
function isPageEmpty(content) {
  if (content.includes(STUB_MARKER)) return true;
  const textBody = content.replace(/---[\s\S]*?---/, "").replace(EMPTY_CONTENT_STRIP, "").trim();
  return textBody.length < MIN_SUBSTANTIVE_CHARS;
}
function detectPollutedPages(pages) {
  const polluted = [];
  for (const p of pages) {
    const match = /^(entities|concepts|sources)([^\s\-_a-zA-Z0-9])/.exec(p.title);
    if (match) {
      polluted.push({
        path: p.path,
        title: p.title,
        cleanTitle: p.title.replace(/^(entities|concepts|sources)/, "")
      });
    }
  }
  return polluted;
}

// src/wiki/lint/fill-empty-page.ts
async function fillEmptyPage(ctx, pagePath, existingContent) {
  const content = existingContent != null ? existingContent : await ctx.tryReadFile(pagePath);
  if (content === null || content === void 0) {
    throw new Error(
      `Cannot expand empty page: file not found at "${pagePath}"`
    );
  }
  const beforeLen = content.length;
  const pageType = pagePath.includes(`/${WIKI_SUBFOLDERS.entities}/`) ? WIKI_SUBFOLDERS.entities : pagePath.includes(`/${WIKI_SUBFOLDERS.concepts}/`) ? WIKI_SUBFOLDERS.concepts : WIKI_SUBFOLDERS.sources;
  const indexPath = `${ctx.settings.wikiFolder}/index.md`;
  const rawWikiIndex = await ctx.tryReadFile(indexPath) || "";
  const wikiIndex = cleanWikiIndex(rawWikiIndex);
  const limits = getGranularityFixLimits(ctx.settings);
  const prompt = buildEmptyPagePrompt(PROMPTS.fillEmptyPage, {
    pageType,
    existingContent: content,
    wikiIndex,
    sectionLabelsHint: buildSectionLabelsHint(ctx.settings),
    maxEntities: limits.maxEntities,
    maxConcepts: limits.maxConcepts
  });
  const finalPrompt = applySectionLabels(prompt, ctx.settings);
  const client = ctx.getClient();
  if (!client) throw new Error("LLM client not initialized");
  const filledContent = await client.createMessage({
    model: ctx.settings.model,
    max_tokens: TOKENS_LINT_PAGE_FIX,
    system: await buildSystemPrompt(
      ctx.settings,
      ctx.getSchemaContext,
      "full"
    ),
    messages: [{ role: "user", content: finalPrompt }],
    ...ctx.settings.disableThinking ? { enableThinking: false } : {}
  });
  const cleaned = cleanMarkdownResponse(filledContent);
  const pollutionFree = correctLinkPollution(cleaned);
  const stubFree = pollutionFree.includes(STUB_MARKER) ? pollutionFree.split("\n").filter((line) => !line.includes(STUB_MARKER)).join("\n").trim() : pollutionFree;
  const textBody = stubFree.replace(/---[\s\S]*?---/, "").replace(EMPTY_CONTENT_STRIP, "").trim();
  if (textBody.length < MIN_SUBSTANTIVE_CHARS) {
    console.debug(
      `fillEmptyPage: LLM output still below threshold (${textBody.length} chars), writing anyway`
    );
  }
  const dateStr = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  const withDates = normalizeFrontmatterDates(stubFree, dateStr);
  const pageTypeSingular = pageType === WIKI_SUBFOLDERS.entities ? "entity" : pageType === WIKI_SUBFOLDERS.concepts ? "concept" : "source";
  const enforced = enforceFrontmatterConstraints(withDates, pageTypeSingular, ctx.settings);
  await ctx.createOrUpdateFile(pagePath, enforced);
  const pageRel = pagePath.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
  return `${pageRel} (${beforeLen} \u2192 ${enforced.length} chars)`;
}

// src/wiki/lint/delete-empty-stubs.ts
async function deleteEmptyStubs(ctx, wikiFolder) {
  const files = ctx.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(wikiFolder) && !f.path.endsWith("/index.md") && !f.path.includes("/schema/") && !f.path.includes("/sources/") && !f.path.includes("/contradictions/") && !f.path.includes("log.md"));
  let deleted = 0;
  let failed = 0;
  const errors = [];
  for (const file of files) {
    try {
      const content = await ctx.app.vault.read(file);
      if (!isPageEmpty(content)) continue;
      const fm = parseFrontmatter(content);
      if ((fm == null ? void 0 : fm.reviewed) === true) continue;
      await ctx.deleteFile(file.path);
      deleted++;
    } catch (e) {
      failed++;
      const errMsg2 = e instanceof Error ? e.message : String(e);
      errors.push(`${file.path}: ${errMsg2}`);
      console.error(`[deleteEmptyStubs] Failed: ${file.path}`, e);
    }
  }
  return { deleted, failed, errors };
}

// src/core/orphan-matcher.ts
function buildOrphanLinkPrompt(template, context) {
  const truncatedContent = context.orphanContent.substring(0, 2e3);
  const truncatedIndex = context.wikiIndex.substring(0, 3e3);
  return template.replace(/\{\{wikiFolder\}\}/g, context.wikiFolder).replace(/\{\{orphan_content\}\}/g, truncatedContent).replace(/\{\{wiki_index\}\}/g, truncatedIndex);
}
function validateOrphanLinkTarget(relatedContent, linkTarget) {
  return relatedContent.includes(linkTarget);
}
function buildOrphanLinkUpdate(relatedContent, suggestion, sectionHeader) {
  const header = `## ${sectionHeader}`;
  const section = relatedContent.includes(header) ? "" : `

${header}`;
  return `${relatedContent}${section}
- ${suggestion.linkText} ${suggestion.linkTarget}`;
}
function normalizeOrphanPagePath(pagePath, wikiFolder) {
  return pagePath.startsWith(wikiFolder) ? pagePath : `${wikiFolder}/${pagePath}`;
}

// src/wiki/lint/link-orphan.ts
async function linkOrphanPage(ctx, orphanPath) {
  var _a;
  const orphanContent = await ctx.tryReadFile(orphanPath);
  if (!orphanContent) return [];
  const indexPath = `${ctx.settings.wikiFolder}/index.md`;
  const rawWikiIndex = await ctx.tryReadFile(indexPath) || "";
  const wikiIndex = cleanWikiIndex(rawWikiIndex);
  const prompt = buildOrphanLinkPrompt(PROMPTS.linkOrphanPage, {
    orphanContent,
    wikiIndex,
    wikiFolder: ctx.settings.wikiFolder
  });
  const client = ctx.getClient();
  if (!client) return [];
  const response = await client.createMessage({
    model: ctx.settings.model,
    max_tokens: TOKENS_LINT_ORPHAN_FIX,
    system: await buildSystemPrompt(
      ctx.settings,
      ctx.getSchemaContext,
      "lint"
    ),
    messages: [{ role: "user", content: prompt }],
    response_format: { type: "json_object" },
    ...ctx.settings.disableThinking ? { enableThinking: false } : {}
  });
  const result = await parseJsonResponse(response);
  if (!((_a = result == null ? void 0 : result.related_pages) == null ? void 0 : _a.length)) return [];
  const linkedPages = [];
  const labels = getSectionLabels(ctx.settings);
  for (const related of result.related_pages) {
    related.page_path = normalizeLLMPath(related.page_path, ctx.settings.wikiFolder);
    const fullPath = normalizeOrphanPagePath(
      related.page_path,
      ctx.settings.wikiFolder
    );
    const relatedContent = await ctx.tryReadFile(fullPath);
    if (!relatedContent) continue;
    if (!validateOrphanLinkTarget(relatedContent, related.link_target)) {
      const updated = buildOrphanLinkUpdate(
        relatedContent,
        {
          pagePath: related.page_path,
          linkText: related.link_text,
          linkTarget: related.link_target
        },
        labels.related_pages
      );
      await ctx.createOrUpdateFile(fullPath, updated);
      linkedPages.push(related.page_path);
    }
  }
  return linkedPages;
}

// src/wiki/lint/merge-duplicates.ts
async function mergeDuplicatePages(ctx, targetPath, sourcePath) {
  var _a, _b;
  const targetContent = await ctx.tryReadFile(targetPath);
  const sourceContent = await ctx.tryReadFile(sourcePath);
  if (!targetContent || !sourceContent) {
    throw new Error(`Cannot merge: target or source page not found (target=${targetPath}, source=${sourcePath})`);
  }
  const sourceFm = parseFrontmatter(sourceContent);
  const targetFm = parseFrontmatter(targetContent);
  const sourceTitle = ((_a = sourcePath.split("/").pop()) == null ? void 0 : _a.replace(".md", "")) || "";
  const targetSources = Array.isArray(targetFm == null ? void 0 : targetFm.sources) ? targetFm.sources : [];
  const sourceSources = Array.isArray(sourceFm == null ? void 0 : sourceFm.sources) ? sourceFm.sources : [];
  const mergedSourcesSet = /* @__PURE__ */ new Set();
  const mergedSourcesList = [];
  for (const s of [...targetSources, ...sourceSources]) {
    const key = s.trim().toLowerCase();
    if (!mergedSourcesSet.has(key)) {
      mergedSourcesSet.add(key);
      mergedSourcesList.push(s);
    }
  }
  const targetAliases = Array.isArray(targetFm == null ? void 0 : targetFm.aliases) ? targetFm.aliases : [];
  const sourceAliases = Array.isArray(sourceFm == null ? void 0 : sourceFm.aliases) ? sourceFm.aliases : [];
  const extractH1 = (content) => {
    const bodyMatch = content.match(/^---[\s\S]*?\n---\n?([\s\S]*)/);
    if (!bodyMatch) return null;
    const h1Match = bodyMatch[1].trim().match(/^#\s+(.+?)(?:\n|$)/);
    return h1Match ? h1Match[1].trim() : null;
  };
  const sourceH1 = extractH1(sourceContent);
  const targetH1 = extractH1(targetContent);
  const allAliases = [...targetAliases, sourceTitle, ...sourceAliases];
  if (sourceH1 && sourceH1 !== sourceTitle) {
    allAliases.push(sourceH1);
  }
  const targetFilename = ((_b = targetPath.split("/").pop()) == null ? void 0 : _b.replace(".md", "")) || "";
  if (targetH1 && targetH1 !== targetFilename && !targetAliases.includes(targetH1)) {
    allAliases.unshift(targetH1);
  }
  const wikiSubfolders = [WIKI_SUBFOLDERS.entities, WIKI_SUBFOLDERS.concepts, WIKI_SUBFOLDERS.sources];
  const cleanAliases = allAliases.filter((a) => {
    if (!a) return false;
    for (const folder of wikiSubfolders) {
      if (a.startsWith(folder) && a.length > folder.length) return false;
    }
    return true;
  });
  const targetTitle = (targetFm == null ? void 0 : targetFm.title) || targetFilename;
  let dedupedAliases = cleanAliases.filter(
    (a, i) => a && a !== targetTitle && cleanAliases.indexOf(a) === i
  );
  const targetBodyMatch = targetContent.match(/^---[\s\S]*?\n---\n?([\s\S]*)/);
  const sourceBodyMatch = sourceContent.match(/^---[\s\S]*?\n---\n?([\s\S]*)/);
  const targetBody = targetBodyMatch ? targetBodyMatch[1].trim() : targetContent;
  const sourceBody = sourceBodyMatch ? sourceBodyMatch[1].trim() : sourceContent;
  const client = ctx.getClient();
  let mergedBody = "";
  let llmMergeSucceeded = false;
  if (client) {
    try {
      const prompt = PROMPTS.mergeDuplicatePages.replace("{{target_content}}", targetBody).replace("{{source_content}}", sourceBody);
      const mergedContent = await client.createMessage({
        model: ctx.settings.model,
        max_tokens: TOKENS_LINT_PAGE_FIX,
        system: await buildSystemPrompt(
          ctx.settings,
          ctx.getSchemaContext,
          "merge"
        ),
        messages: [{ role: "user", content: prompt }],
        ...ctx.settings.disableThinking ? { enableThinking: false } : {}
      });
      const cleaned = cleanMarkdownResponse(mergedContent);
      if (cleaned && cleaned.length > 100) {
        let parsed = null;
        try {
          parsed = await parseJsonResponse(cleaned);
        } catch (parseErr) {
          console.error(`mergeDuplicatePages: JSON parse failed for ${sourcePath} \u2192 ${targetPath}`, parseErr);
        }
        if (parsed == null ? void 0 : parsed.body) {
          mergedBody = parsed.body.trim();
          llmMergeSucceeded = true;
        } else if (!parsed) {
          console.warn(`mergeDuplicatePages: JSON parse returned null for ${sourcePath} \u2192 ${targetPath}, falling back to programmatic merge`);
        } else {
          console.warn(`mergeDuplicatePages: LLM response missing 'body' field for ${sourcePath} \u2192 ${targetPath}, falling back to programmatic merge`);
        }
        if ((parsed == null ? void 0 : parsed.aliases) && Array.isArray(parsed.aliases)) {
          for (const a of parsed.aliases) {
            if (a && a !== targetTitle && !dedupedAliases.includes(a)) {
              dedupedAliases.push(a);
            }
          }
        }
      }
    } catch (e) {
      const errMsg2 = e instanceof Error ? e.message : String(e);
      console.error(`LLM merge failed for ${sourcePath} \u2192 ${targetPath}: ${errMsg2}. Using programmatic merge.`, e);
    }
  }
  if (!mergedBody) {
    if (llmMergeSucceeded) {
      console.warn(`mergeDuplicatePages: LLM returned empty body for ${sourcePath} \u2192 ${targetPath}, using programmatic merge`);
    }
    mergedBody = targetBody;
    if (sourceBody) {
      mergedBody += "\n\n## From " + sourceTitle + "\n\n" + sourceBody;
    }
  }
  const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  const fmLines = ["---"];
  if (targetFm == null ? void 0 : targetFm.type) fmLines.push(`type: ${targetFm.type}`);
  fmLines.push(`created: ${(targetFm == null ? void 0 : targetFm.created) || today}`);
  fmLines.push(`updated: ${today}`);
  if (mergedSourcesList.length > 0) {
    fmLines.push(`${WIKI_SUBFOLDERS.sources}:`);
    for (const s of mergedSourcesList) fmLines.push(`  - "${s}"`);
  }
  const tags = Array.isArray(targetFm == null ? void 0 : targetFm.tags) ? targetFm.tags : [];
  if (tags.length > 0) {
    fmLines.push("tags:");
    for (const t of tags) fmLines.push(`  - ${t}`);
  }
  if (targetFm == null ? void 0 : targetFm.reviewed) fmLines.push("reviewed: true");
  if (dedupedAliases.length > 0) {
    fmLines.push("aliases:");
    for (const a of dedupedAliases) fmLines.push(`  - "${a}"`);
  }
  fmLines.push("---");
  const newContent = fmLines.join("\n") + "\n\n" + mergedBody;
  const pageType = targetPath.includes(`/${WIKI_SUBFOLDERS.entities}/`) ? "entity" : targetPath.includes(`/${WIKI_SUBFOLDERS.concepts}/`) ? "concept" : "source";
  const enforced = enforceFrontmatterConstraints(newContent, pageType, ctx.settings);
  await ctx.createOrUpdateFile(targetPath, enforced);
  const wikiFolder = ctx.settings.wikiFolder;
  const sourceRel = sourcePath.replace(wikiFolder + "/", "").replace(".md", "");
  const targetRel = targetPath.replace(wikiFolder + "/", "").replace(".md", "");
  const allWikiFiles = ctx.app.vault.getMarkdownFiles().filter(
    (f) => f.path.startsWith(wikiFolder) && f.path !== sourcePath
  );
  for (const file of allWikiFiles) {
    const content = await ctx.app.vault.read(file);
    if (content.includes(`[[${sourceRel}]]`) || content.includes(`[[${sourceRel}|`)) {
      const updated = content.replace(new RegExp(`\\[\\[${escapeRegex(sourceRel)}\\]\\]`, "g"), `[[${targetRel}]]`).replace(new RegExp(`\\[\\[${escapeRegex(sourceRel)}\\|`, "g"), `[[${targetRel}|`);
      if (updated !== content) {
        await ctx.createOrUpdateFile(file.path, updated);
      }
    }
  }
  await ctx.deleteFile(sourcePath);
  return `merged ${sourceRel} \u2192 ${targetRel}`;
}

// src/wiki/lint/fix-polluted-page.ts
async function fixPollutedPage(ctx, oldPath, newBasename) {
  const oldRel = oldPath.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
  const dir = oldRel.split("/").slice(0, -1).join("/");
  const newPath = `${ctx.settings.wikiFolder}/${dir}/${newBasename}.md`;
  const newRel = `${dir}/${newBasename}`;
  const existingAtNew = await ctx.tryReadFile(newPath);
  if (existingAtNew !== null) {
    console.debug(`fixPollutedPage: clean path already exists, merging ${oldRel} \u2192 ${newRel}`);
    await ctx.createOrUpdateFile(newPath, existingAtNew);
    await ctx.deleteFile(oldPath);
    return `merged ${oldRel} \u2192 ${newRel} (clean path existed)`;
  }
  const oldContent = await ctx.tryReadFile(oldPath);
  if (oldContent === null) {
    return `cannot fix ${oldRel}: file not found`;
  }
  await ctx.createOrUpdateFile(newPath, oldContent);
  await ctx.deleteFile(oldPath);
  const allPages = await getExistingWikiPages(ctx.app, ctx.settings.wikiFolder);
  let updatedCount = 0;
  for (const page of allPages) {
    const content = await ctx.tryReadFile(page.path);
    if (!content) continue;
    const newContent = content.replace(new RegExp(`\\[\\[${escapeRegex(oldRel)}\\|([^\\]]+)\\]\\]`, "g"), `[[${newRel}|$1]]`).replace(new RegExp(`\\[\\[${escapeRegex(oldRel)}\\]\\]`, "g"), `[[${newRel}]]`);
    if (content !== newContent) {
      await ctx.createOrUpdateFile(page.path, newContent);
      updatedCount++;
    }
  }
  return `renamed ${oldRel} \u2192 ${newRel} (${updatedCount} pages updated)`;
}

// src/wiki/contradictions.ts
var ContradictionManager = class {
  constructor(ctx) {
    this.ctx = ctx;
  }
  async noteContradiction(contradiction) {
    const pagePath = contradiction.source_page.replace(
      /\[\[(.+)\]\]/,
      `${this.ctx.settings.wikiFolder}/$1.md`
    );
    const existingContent = await this.ctx.tryReadFile(pagePath);
    if (!existingContent) return;
    const contradictionNote = `

## \u26A0\uFE0F Potential Contradiction

**Source claim**: ${contradiction.claim}

**Existing view**: ${contradiction.contradicted_by}

**Resolution suggestion**: ${contradiction.resolution}

---
*Flagged: ${(/* @__PURE__ */ new Date()).toISOString().split("T")[0]}*`;
    await this.ctx.createOrUpdateFile(pagePath, existingContent + contradictionNote);
    await this.trackContradiction(contradiction);
  }
  async trackContradiction(contradiction) {
    const contradictionsDir = `${this.ctx.settings.wikiFolder}/contradictions`;
    try {
      await this.ctx.app.vault.createFolder(contradictionsDir);
    } catch (e) {
    }
    const date = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
    const claimSlug = slugify(contradiction.claim.substring(0, 50));
    const filePath = `${contradictionsDir}/${claimSlug}-${date}.md`;
    if (await this.ctx.tryReadFile(filePath)) {
      console.debug("Contradiction already tracked:", filePath);
      return;
    }
    const pageRelPath = contradiction.source_page.replace(/\[\[(.+)\]\]/, "$1");
    const labels = getSectionLabels(this.ctx.settings);
    const content = `---
status: detected
detected: ${date}
source_page: "[[${pageRelPath}]]"
---

# Contradiction: ${contradiction.claim.substring(0, 60)}

## ${labels.new_claim}
${contradiction.claim}

## ${labels.existing_knowledge}
${contradiction.contradicted_by}

## ${labels.resolution_suggestion}
${contradiction.resolution}

## ${labels.source_page}
${contradiction.source_page}

---
*Auto-detected on ${date}*
`;
    await this.ctx.createOrUpdateFile(filePath, content);
    console.debug("Contradiction tracked:", filePath);
  }
  async getOpenContradictions() {
    const contradictionsDir = `${this.ctx.settings.wikiFolder}/contradictions`;
    const files = this.ctx.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(contradictionsDir));
    const results = [];
    for (const file of files) {
      const content = await this.ctx.app.vault.read(file);
      const fm = parseFrontmatter(content);
      const status = (fm == null ? void 0 : fm.status) || "detected";
      if (status === "resolved" || status === "suppressed") continue;
      const headerBlocks = content.split(/\n## /);
      const claimText = headerBlocks.length > 1 ? headerBlocks[1].replace(/^[^\n]+\n/, "").trim() : "";
      const sourcePageText = headerBlocks.length > 4 ? headerBlocks[4].replace(/^[^\n]+\n/, "").trim() : "";
      results.push({
        path: file.path,
        status,
        claim: claimText || file.basename,
        sourcePage: sourcePageText
      });
    }
    return results;
  }
  async updateContradictionStatus(filePath, newStatus) {
    const content = await this.ctx.tryReadFile(filePath);
    if (!content) {
      console.debug("Contradiction file not found:", filePath);
      return;
    }
    const updated = content.replace(/^status:\s*\S+/m, `status: ${newStatus}`);
    if (newStatus === "resolved") {
      const resolvedDate = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
      if (updated.includes("resolved:")) {
        const final = updated.replace(
          /^resolved:\s*\S*/m,
          `resolved: ${resolvedDate}`
        );
        await this.ctx.createOrUpdateFile(filePath, final);
      } else {
        const final = updated.replace(
          /^(detected:\s*\S+)/m,
          `$1
resolved: ${resolvedDate}`
        );
        await this.ctx.createOrUpdateFile(filePath, final);
      }
    } else {
      await this.ctx.createOrUpdateFile(filePath, updated);
    }
    console.debug(
      `Contradiction status updated: ${filePath} \u2192 ${newStatus}`
    );
  }
  async resolveContradiction(contradictionPath) {
    const contradictionContent = await this.ctx.tryReadFile(contradictionPath);
    if (!contradictionContent)
      throw new Error("Contradiction file not found");
    const fm = parseFrontmatter(contradictionContent);
    const sourcePage = (fm == null ? void 0 : fm.source_page) || "";
    const pagePath = sourcePage.replace(
      /\[\[(.+)\]\]/,
      `${this.ctx.settings.wikiFolder}/$1.md`
    );
    const existingContent = await this.ctx.tryReadFile(pagePath);
    if (!existingContent) throw new Error("Affected wiki page not found");
    const prompt = PROMPTS.resolveContradiction.replace("{{existing_content}}", existingContent.substring(0, 6e3)).replace(
      "{{contradiction_content}}",
      contradictionContent.substring(0, 3e3)
    );
    const finalPrompt = applySectionLabels(prompt, this.ctx.settings);
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    const fixedContent = await client.createMessage({
      model: this.ctx.settings.model,
      max_tokens: TOKENS_CONTRADICTION,
      system: await buildSystemPrompt(
        this.ctx.settings,
        this.ctx.getSchemaContext,
        "full"
      ),
      messages: [{ role: "user", content: finalPrompt }],
      ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
    const cleaned = cleanMarkdownResponse(fixedContent);
    await this.ctx.createOrUpdateFile(pagePath, cleaned);
    console.debug("Contradiction resolved:", contradictionPath);
  }
};

// src/core/sources-normalizer.ts
function normalizeSourcePath(raw, wikiFolder, preserveCase = true) {
  let s = raw.trim();
  if (!s || s === "[[]]") return "";
  if (s.startsWith("[[") && s.endsWith("]]")) {
    s = s.slice(2, -2).trim();
  }
  if (!s) return "";
  const pipeIdx = s.indexOf("|");
  if (pipeIdx !== -1) s = s.substring(0, pipeIdx).trim();
  if (!s) return "";
  if (s.endsWith(".md")) s = s.slice(0, -3);
  if (!s) return "";
  const wikiPrefix = wikiFolder + "/";
  if (s.startsWith(wikiPrefix)) {
    s = s.substring(wikiPrefix.length);
  } else if (s.startsWith("entities/") || s.startsWith("concepts/") || s.startsWith("sources/")) {
  } else {
    const filename = s.split("/").pop() || s;
    s = `sources/${computeSlug(filename, preserveCase)}`;
  }
  return s;
}
function normalizeSourcesField(rawList, wikiFolder, preserveCase = true) {
  const seen = /* @__PURE__ */ new Set();
  const result = [];
  for (const raw of rawList) {
    const normalized = normalizeSourcePath(String(raw), wikiFolder, preserveCase);
    if (!normalized) continue;
    if (seen.has(normalized)) continue;
    seen.add(normalized);
    result.push(`[[${normalized}]]`);
  }
  return result;
}
function scanPollutedSources(content, wikiFolder, preserveCase = true) {
  const entries = extractRawSourcesEntries(content);
  if (entries.length === 0) return false;
  return entries.some((e) => {
    const canonical = normalizeSourcePath(e, wikiFolder, preserveCase);
    if (!canonical) return true;
    const stripped = e.replace(/^\[\[/, "").replace(/\]\]$/, "");
    const changed = canonical !== stripped;
    if (changed) console.debug(`[scanPollutedSources] entry changed: "${e}" \u2192 canonical="${canonical}"`);
    return changed;
  });
}
function extractRawSourcesEntries(content) {
  const fmMatch = content.match(/^---\n([\s\S]*?)\n---/);
  if (!fmMatch) return [];
  const fmBody = fmMatch[1];
  const inlineMatch = fmBody.match(/^sources:\s*\[(.*)\]\s*$/m);
  if (inlineMatch) {
    return inlineMatch[1].split(",").map((s) => s.trim().replace(/^"|"$/g, "")).filter(Boolean);
  }
  const lines = fmBody.split("\n");
  const startIdx = lines.findIndex((l) => /^sources:\s*$/.test(l));
  if (startIdx === -1) return [];
  const result = [];
  for (let i = startIdx + 1; i < lines.length; i++) {
    const line = lines[i];
    const m = line.match(/^\s*-\s*"([^"]*)"/);
    if (m) result.push(m[1]);
    else if (line.trim() && !/^\s*-\s*/.test(line)) break;
  }
  return result;
}
function fixPollutedSources(content, wikiFolder, preserveCase = true) {
  const rawEntries = extractRawSourcesEntries(content);
  if (rawEntries.length === 0) return { fixed: 0, content };
  console.debug(`[fixPollutedSources] rawEntries (${rawEntries.length}): ${JSON.stringify(rawEntries)}`);
  const normalized = normalizeSourcesField(rawEntries, wikiFolder, preserveCase);
  console.debug(`[fixPollutedSources] normalized (${normalized.length}): ${JSON.stringify(normalized)}`);
  const fmMatch = content.match(/^---\n([\s\S]*?)\n---/);
  if (!fmMatch) return { fixed: 0, content };
  const fmBody = fmMatch[1];
  const isInline = /^[ \t]*sources:\s*\[.*\][ \t]*$/m.test(fmBody);
  let newFmBody;
  if (isInline) {
    const newLine = `sources: [${normalized.map((n) => `"${n}"`).join(", ")}]`;
    newFmBody = fmBody.replace(/^([ \t]*)sources:\s*\[(.*)\][ \t]*$/m, (_match, indent) => `${indent}${newLine}`);
  } else {
    const newBlock = `sources:
${normalized.map((n) => `  - "${n}"`).join("\n")}
`;
    newFmBody = fmBody.replace(/^([ \t]*)sources:\s*\n((?:[ \t]*-\s*"[^"]*"\s*\n?)+)/m, (_match, indent) => `${indent}${newBlock}`);
  }
  if (newFmBody === fmBody) {
    return { fixed: 0, content };
  }
  const newContent = content.substring(0, fmMatch.index) + `---
${newFmBody}
---` + content.substring(fmMatch.index + fmMatch[0].length);
  return { fixed: 1, content: newContent };
}

// src/wiki/prompts/constraints.ts
var UNIVERSAL_LINK_CONSTRAINTS = `LINK RULES (apply to ALL body text output):
- NEVER use [[sources/...]] in body text \u2014 sources/ is ONLY valid in the YAML frontmatter sources: field
- NEVER duplicate folder prefixes in display names: [[entities/Qwen|Qwen]] is CORRECT, [[entities/Qwen|entities/Qwen]] is WRONG
- ALWAYS use [[path|display]] format when referencing entities and concepts`;

// src/core/index-search.ts
function parseIndexForPages(indexContent) {
  const pages = [];
  const lineRegex = /- \[\[([^\]|]+)(?:\|[^\]]+)?\]\]\s*(?:`aliases:\s*([^`]+)`)?/g;
  let match;
  while ((match = lineRegex.exec(indexContent)) !== null) {
    const path = match[1];
    const aliasStr = match[2] || "";
    const title = path.split("/").pop() || path;
    const aliases = aliasStr.split(",").map((a) => a.trim()).filter(Boolean);
    pages.push({ path, title, aliases });
  }
  return pages;
}
function localKeywordMatch(query, pages) {
  const keywords = query.toLowerCase().split(/\s+/).filter((k) => k.length > 0);
  const scored = [];
  for (const page of pages) {
    let score = 0;
    const titleLower = page.title.toLowerCase();
    for (const kw of keywords) {
      if (titleLower.includes(kw)) score += 3;
      for (const alias of page.aliases) {
        if (alias.toLowerCase().includes(kw)) score += 2;
      }
    }
    if (score > 0) scored.push({ ...page, score });
  }
  return scored.sort((a, b) => b.score - a.score);
}
function matchExtractedToExisting(extractedNames, existingPages) {
  const pageSlugs = existingPages.map((p) => ({
    title: p.title,
    slug: computeSlug(p.title),
    aliasSlugs: (p.aliases || []).map((a) => computeSlug(a))
  }));
  const matched = /* @__PURE__ */ new Set();
  for (const name of extractedNames) {
    const targetSlug = computeSlug(name);
    const match = pageSlugs.find(
      (p) => p.slug === targetSlug || p.aliasSlugs.some((a) => a === targetSlug)
    );
    if (match) matched.add(match.title);
  }
  return [...matched];
}

// src/core/batch-limits.ts
var GRANULARITY_CONFIG = {
  fine: { initialBatchSize: 30, maxBatchesBase: 12, maxTotalItems: 100 },
  standard: { initialBatchSize: 20, maxBatchesBase: 6, maxTotalItems: 50 },
  coarse: { initialBatchSize: 10, maxBatchesBase: 3, maxTotalItems: 10 },
  minimal: { initialBatchSize: 5, maxBatchesBase: 1, maxTotalItems: 5 },
  custom: { initialBatchSize: 5, maxBatchesBase: 1, maxTotalItems: null }
};
var MIN_BATCH_SIZE = 5;
var MAX_TOKENS = 16e3;
var SHORT_CONTENT_THRESHOLD = 2e4;
var CHARS_PER_ITEM = 600;
function calculateBatchLimits(contentLength, granularity = "standard", customLimits) {
  var _a, _b;
  const config = { ...GRANULARITY_CONFIG[granularity] || GRANULARITY_CONFIG.standard };
  if (granularity === "custom" && customLimits) {
    const totalCap = ((_a = customLimits.entityCap) != null ? _a : MIN_BATCH_SIZE) + ((_b = customLimits.conceptCap) != null ? _b : MIN_BATCH_SIZE);
    if (totalCap > 10) {
      config.initialBatchSize = Math.min(CUSTOM_BATCH_SIZE_MAX, Math.max(MIN_BATCH_SIZE, totalCap));
      config.maxBatchesBase = Math.max(1, Math.ceil(totalCap / config.initialBatchSize));
    }
  }
  if (contentLength < SHORT_CONTENT_THRESHOLD && config.maxTotalItems !== null) {
    const reasonableCap = Math.max(5, Math.ceil(contentLength / CHARS_PER_ITEM));
    if (config.maxTotalItems > reasonableCap) {
      config.maxTotalItems = reasonableCap;
    }
  }
  const maxBatches = Math.min(
    config.maxBatchesBase * 3,
    Math.max(2, Math.ceil(contentLength / 2e3) + 2)
  );
  return {
    initialBatchSize: config.initialBatchSize,
    maxBatches,
    maxTotalItems: config.maxTotalItems,
    minBatchSize: MIN_BATCH_SIZE,
    responseFullnessThreshold: MAX_TOKENS * 0.7
  };
}
function adjustBatchSizeForResponse(currentBatchSize, responseLength, threshold = MAX_TOKENS * 0.7) {
  if (responseLength > threshold && currentBatchSize > MIN_BATCH_SIZE) {
    return Math.max(MIN_BATCH_SIZE, Math.floor(currentBatchSize * 0.75));
  }
  return currentBatchSize;
}
function getCustomTypeCaps(settings) {
  var _a, _b;
  if (settings.extractionGranularity !== "custom") {
    return { entityCap: null, conceptCap: null };
  }
  return {
    entityCap: (_a = settings.customEntityLimit) != null ? _a : 5,
    conceptCap: (_b = settings.customConceptLimit) != null ? _b : 5
  };
}

// src/core/convergence-detector.ts
function detectConvergence(rawTotal, currentBatchSize, batchSizeHalved, minBatchSize = 5) {
  const yieldRatio = rawTotal / currentBatchSize;
  if (yieldRatio < 0.5 && currentBatchSize > minBatchSize) {
    if (batchSizeHalved) {
      return {
        shouldStop: true,
        reason: `Low yield persists after halving (${rawTotal}/${currentBatchSize}), converged`,
        newBatchSize: currentBatchSize,
        newBatchSizeHalved: batchSizeHalved
      };
    }
    const newBatchSize = Math.max(minBatchSize, Math.floor(currentBatchSize / 2));
    return {
      shouldStop: false,
      reason: `Low yield (${rawTotal}/${currentBatchSize}), halving batch size`,
      newBatchSize,
      newBatchSizeHalved: true
    };
  }
  return {
    shouldStop: false,
    reason: null,
    newBatchSize: currentBatchSize,
    newBatchSizeHalved: batchSizeHalved
  };
}
function checkCumulativeLimits(totalEntities, totalConcepts, config) {
  const { customEntityCap, customConceptCap, maxTotalItems } = config;
  if (customEntityCap !== null && customConceptCap !== null) {
    if (totalEntities >= customEntityCap && totalConcepts >= customConceptCap) {
      return {
        shouldStop: true,
        reason: `Per-type limits reached (entities: ${totalEntities}/${customEntityCap}, concepts: ${totalConcepts}/${customConceptCap})`
      };
    }
    return { shouldStop: false, reason: null };
  }
  const cumulativeTotal = totalEntities + totalConcepts;
  if (maxTotalItems !== null && cumulativeTotal >= maxTotalItems) {
    return {
      shouldStop: true,
      reason: `Cumulative total reached limit ${maxTotalItems} (${cumulativeTotal} items)`
    };
  }
  return { shouldStop: false, reason: null };
}
function checkEmptyBatch(rawTotal, newTotal) {
  if (rawTotal === 0) {
    return { shouldStop: true, reason: "LLM returned empty array" };
  }
  if (newTotal === 0) {
    return { shouldStop: true, reason: "All items duplicate, extraction exhausted" };
  }
  return { shouldStop: false, reason: null };
}
function formatConvergenceStatus(batchNum, result) {
  if (result.shouldStop) {
    return `[Batch ${batchNum}] ${result.reason}, stopping`;
  }
  if (result.reason) {
    return `[Batch ${batchNum}] ${result.reason}`;
  }
  return `[Batch ${batchNum}] Yield normal, continuing`;
}

// src/core/batch-merger.ts
function createEmptyAccumulation() {
  return {
    entities: [],
    concepts: [],
    contradictions: [],
    relatedPages: [],
    keyPoints: [],
    extractedNames: /* @__PURE__ */ new Set()
  };
}
function mergeBatchResults(current, newBatch, customCaps) {
  const newEntities = newBatch.entities.filter((e) => {
    const name = e.name.trim().toLowerCase();
    return !current.extractedNames.has(name);
  });
  const newConcepts = newBatch.concepts.filter((c) => {
    const name = c.name.trim().toLowerCase();
    return !current.extractedNames.has(name);
  });
  let cappedEntities = newEntities;
  let cappedConcepts = newConcepts;
  if ((customCaps == null ? void 0 : customCaps.entityCap) !== null && (customCaps == null ? void 0 : customCaps.entityCap) !== void 0) {
    const remaining = Math.max(0, customCaps.entityCap - current.entities.length);
    cappedEntities = newEntities.slice(0, remaining);
  }
  if ((customCaps == null ? void 0 : customCaps.conceptCap) !== null && (customCaps == null ? void 0 : customCaps.conceptCap) !== void 0) {
    const remaining = Math.max(0, customCaps.conceptCap - current.concepts.length);
    cappedConcepts = newConcepts.slice(0, remaining);
  }
  const updatedExtractedNames = new Set(current.extractedNames);
  for (const e of cappedEntities) {
    updatedExtractedNames.add(e.name.trim().toLowerCase());
    for (const alias of e.aliases || []) {
      updatedExtractedNames.add(alias.trim().toLowerCase());
    }
  }
  for (const c of cappedConcepts) {
    updatedExtractedNames.add(c.name.trim().toLowerCase());
    for (const alias of c.aliases || []) {
      updatedExtractedNames.add(alias.trim().toLowerCase());
    }
  }
  return {
    newEntities: cappedEntities,
    newConcepts: cappedConcepts,
    allEntities: [...current.entities, ...cappedEntities],
    allConcepts: [...current.concepts, ...cappedConcepts],
    extractedNames: updatedExtractedNames
  };
}
function buildSourceAnalysis(filePath, fileBasename, accumulation, firstBatchData) {
  return {
    source_file: filePath,
    source_title: (firstBatchData == null ? void 0 : firstBatchData.sourceTitle) || fileBasename,
    summary: (firstBatchData == null ? void 0 : firstBatchData.summary) || "",
    entities: accumulation.entities,
    concepts: accumulation.concepts,
    contradictions: accumulation.contradictions,
    related_pages: accumulation.relatedPages,
    key_points: accumulation.keyPoints,
    created_pages: [],
    updated_pages: []
  };
}
function calculateBatchStats(batchNum, newItems, cumulative) {
  const totalNew = newItems.entities + newItems.concepts;
  return `[Batch ${batchNum}] New: ${newItems.entities} entities, ${newItems.concepts} concepts (total: ${totalNew}) | Cumulative: ${cumulative.entities} entities, ${cumulative.concepts} concepts`;
}

// src/wiki/source-analyzer.ts
function buildCompactSlugList(app, wikiFolder, sourcePath) {
  const wikiPrefix = wikiFolder + "/";
  const files = app.vault.getMarkdownFiles();
  const slugs = files.filter(
    (f) => f.path.startsWith(wikiPrefix) && !f.path.includes("/schema/") && !f.path.endsWith("/index.md") && f.path !== sourcePath
  ).map((f) => f.path.replace(wikiPrefix, "").replace(/\.md$/, "")).sort();
  return slugs.join("\n");
}
function normalizeBatchResponse(raw) {
  if (!raw) {
    return { validity: "unusable", data: emptyBatch() };
  }
  const entities = coerceToArray(raw.entities).filter((e) => {
    var _a;
    return (_a = e == null ? void 0 : e.name) == null ? void 0 : _a.trim();
  });
  const concepts = coerceToArray(raw.concepts).filter((c) => {
    var _a;
    return (_a = c == null ? void 0 : c.name) == null ? void 0 : _a.trim();
  });
  const relatedPages = coerceToArray(raw.related_pages).map((p) => {
    const match = String(p).match(/^\[\[(?:[^\]|]+\|)?([^\]]+)\]\]$/);
    return match ? match[1] : p;
  });
  const data = {
    entities,
    concepts,
    sourceTitle: typeof raw.source_title === "string" ? raw.source_title : null,
    summary: typeof raw.summary === "string" ? raw.summary : null,
    contradictions: coerceToArray(raw.contradictions),
    relatedPages,
    keyPoints: coerceToArray(raw.key_points)
  };
  if (entities.length === 0 && concepts.length === 0) {
    const bothKeysAbsent = raw.entities === void 0 && raw.concepts === void 0;
    return { validity: bothKeysAbsent ? "unusable" : "empty", data };
  }
  return { validity: "valid", data };
}
function emptyBatch() {
  return {
    entities: [],
    concepts: [],
    sourceTitle: null,
    summary: null,
    contradictions: [],
    relatedPages: [],
    keyPoints: []
  };
}
var SourceAnalyzer = class {
  constructor(ctx) {
    this.ctx = ctx;
  }
  async analyzeSource(file) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    console.debug("=== Source analysis started ===");
    console.debug("File:", file.path);
    const content = await this.ctx.app.vault.read(file);
    console.debug("File content length:", content.length);
    if (isBlankSource(content)) {
      console.debug("[Source analysis] blank body \u2014 skipping LLM call, returning null");
      return null;
    }
    console.debug("Existing Wiki pages count: \u2014 delayed until post-extraction matching");
    const limits = calculateBatchLimits(content.length, this.ctx.settings.extractionGranularity || "standard", {
      entityCap: this.ctx.settings.customEntityLimit,
      conceptCap: this.ctx.settings.customConceptLimit
    });
    const customTypeCaps = getCustomTypeCaps(this.ctx.settings);
    const baseMaxTokens = Math.max(MAX_TOKENS_BATCH, limits.initialBatchSize * TOKENS_PER_ITEM_BUDGET);
    const retryCap = baseMaxTokens * SOURCE_ANALYZER_RETRY_MULTIPLIER;
    console.debug(`[Batch limits] Initial size: ${limits.initialBatchSize}, Max batches: ${limits.maxBatches}, Max total: ${limits.maxTotalItems || "none"}, baseMaxTokens: ${baseMaxTokens}, retryCap: ${retryCap}`);
    let currentBatchSize = limits.initialBatchSize;
    let batchSizeHalved = false;
    let retryingBatch = false;
    const accumulation = createEmptyAccumulation();
    let firstBatchData = null;
    let finalBatchNum = 0;
    const granularityInstruction = getGranularityInstruction(this.ctx.settings);
    const tagVocabularySection = buildActiveTagVocabularySection(this.ctx.settings);
    const existingSlugs = buildCompactSlugList(this.ctx.app, this.ctx.settings.wikiFolder, file.path);
    const templateUntouched = PROMPTS.analyzeSource.replace("{{content}}", content).replace("{{existing_pages}}", "").replace("{{existing_slugs}}", existingSlugs);
    const batchMarker = "{{batch_context}}";
    const markerIdx = templateUntouched.indexOf(batchMarker);
    const staticPrefix = templateUntouched.substring(0, markerIdx);
    const suffixTemplate = templateUntouched.substring(markerIdx + batchMarker.length);
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    for (let batchNum = 0; batchNum < limits.maxBatches; batchNum++) {
      const isFirstBatch = batchNum === 0;
      let batchContext;
      if (isFirstBatch) {
        batchContext = "This is the first extraction round. Extract the most important entities and concepts from the source.";
      } else {
        const ctxLines = [];
        for (const e of accumulation.entities) {
          const line = ((_a = e.aliases) == null ? void 0 : _a.length) ? `${e.name} (aliases: ${e.aliases.join(", ")})` : e.name;
          ctxLines.push(line);
        }
        for (const c of accumulation.concepts) {
          const line = ((_b = c.aliases) == null ? void 0 : _b.length) ? `${c.name} (aliases: ${c.aliases.join(", ")})` : c.name;
          ctxLines.push(line);
        }
        const alreadyExtracted = ctxLines.length > 0 ? `

Already extracted from this source:
  [${ctxLines.join("; ")}]
  (including abbreviations, synonyms, and translations of these names)
Do NOT extract them again. If a candidate name is equivalent to any of the above \u2014 including their aliases \u2014 skip it.` : "";
        batchContext = `This is round ${batchNum + 1} of extraction. Extract the next batch of most important entities and concepts from the remaining content. If no more items are worth extracting, return empty arrays [] for entities and concepts.${alreadyExtracted}`;
      }
      const prompt = staticPrefix + batchContext + suffixTemplate.replace("{{granularity_instruction}}", granularityInstruction).replace(/{{batch_size}}/g, String(currentBatchSize));
      const langHint = `

CRITICAL LANGUAGE REQUIREMENT: Summaries, descriptions, source_title, and key_points in your JSON output MUST be written in ${WIKI_LANGUAGES[this.ctx.settings.wikiLanguage || "en"] || this.ctx.settings.wikiLanguage || "English"}. HOWEVER: entity names and concept names MUST be preserved in their original source language \u2014 NEVER translate names. mentions_in_source MUST be verbatim quotes from the source (preserve original language).`;
      const finalPrompt = prompt + langHint + "\n\n" + tagVocabularySection;
      console.debug(`[Batch ${batchNum + 1}/${limits.maxBatches}] LLM call started (batch_size=${currentBatchSize})...`);
      console.debug(`[Batch ${batchNum + 1}] Prompt length:`, prompt.length);
      if (isFirstBatch) {
        (_d = (_c = this.ctx).onProgress) == null ? void 0 : _d.call(_c, `Analyzing batch 1/${limits.maxBatches}...`);
      } else {
        (_f = (_e = this.ctx).onProgress) == null ? void 0 : _f.call(_e, `Analyzing batch ${batchNum + 1}/${limits.maxBatches} (${accumulation.entities.length} entities, ${accumulation.concepts.length} concepts so far)...`);
      }
      try {
        const systemPrompt = await this.ctx.buildSystemPrompt("analyze");
        const batchMaxTokens = Math.max(baseMaxTokens, currentBatchSize * TOKENS_PER_ITEM_BUDGET);
        console.debug(`[Batch ${batchNum + 1}] Provider:`, this.ctx.settings.provider, "| Model:", this.ctx.settings.model, "| Prompt:", finalPrompt.length, "chars", "| max_tokens:", batchMaxTokens);
        const response = await client.createMessage({
          model: this.ctx.settings.model,
          max_tokens: batchMaxTokens,
          system: systemPrompt,
          messages: [{ role: "user", content: finalPrompt }],
          response_format: { type: "json_object" },
          cacheBreakpoint: staticPrefix.length,
          maxTokensPerCall: retryCap
        });
        console.debug(`[Batch ${batchNum + 1}] Response length:`, response.length);
        (_h = (_g = this.ctx).onProgress) == null ? void 0 : _h.call(_g, `Analyzed batch ${batchNum + 1}, processing...`);
        const analysisData = await parseJsonResponse(response, async (malformedJson) => {
          const repairPrompt = `Fix the following malformed JSON. Only fix JSON syntax errors (unescaped quotes, trailing commas, missing brackets). Do NOT change any values or content. Output ONLY the fixed JSON, no other text.

${malformedJson}`;
          return await client.createMessage({
            model: this.ctx.settings.model,
            max_tokens: retryCap,
            // Repair may need full output if original was truncated at retryCap
            system: await this.ctx.buildSystemPrompt("analyze"),
            messages: [{ role: "user", content: repairPrompt }],
            response_format: { type: "json_object" },
            maxTokensPerCall: retryCap
          });
        });
        if (!analysisData) {
          console.error(`[Batch ${batchNum + 1}] JSON parse failed, skipping batch`);
          if (isFirstBatch) return null;
          break;
        }
        const { validity, data: norm } = normalizeBatchResponse(analysisData);
        if (isFirstBatch) {
          if (validity === "unusable") {
            console.error("\u274C Round 1 unusable \u2014 no entities or concepts:", {
              entities: !!(analysisData == null ? void 0 : analysisData.entities),
              concepts: !!(analysisData == null ? void 0 : analysisData.concepts)
            });
            return null;
          }
          if (!norm.sourceTitle) {
            console.debug("Round 1 missing source_title, falling back to filename:", file.basename);
          }
          firstBatchData = norm;
          accumulation.contradictions = norm.contradictions;
          accumulation.relatedPages = norm.relatedPages;
          accumulation.keyPoints = norm.keyPoints;
          const firstMergeResult = mergeBatchResults(accumulation, norm, customTypeCaps);
          accumulation.entities = firstMergeResult.allEntities;
          accumulation.concepts = firstMergeResult.allConcepts;
          accumulation.extractedNames = firstMergeResult.extractedNames;
        }
        if (validity === "empty") {
          console.debug(`[Batch ${batchNum + 1}] LLM returned empty arrays, stopping iteration`);
          break;
        }
        if (!isFirstBatch) {
          const mergeResult = mergeBatchResults(accumulation, norm, customTypeCaps);
          accumulation.entities = mergeResult.allEntities;
          accumulation.concepts = mergeResult.allConcepts;
          accumulation.extractedNames = mergeResult.extractedNames;
          const rawTotal = norm.entities.length + norm.concepts.length;
          const newTotal = mergeResult.newEntities.length + mergeResult.newConcepts.length;
          const statsMsg = calculateBatchStats(batchNum + 1, {
            entities: mergeResult.newEntities.length,
            concepts: mergeResult.newConcepts.length
          }, {
            entities: accumulation.entities.length,
            concepts: accumulation.concepts.length
          });
          console.debug(statsMsg);
          currentBatchSize = adjustBatchSizeForResponse(currentBatchSize, response.length, limits.responseFullnessThreshold);
          const emptyCheck = checkEmptyBatch(rawTotal, newTotal);
          if (emptyCheck.shouldStop) {
            console.debug(`[Batch ${batchNum + 1}] ${emptyCheck.reason}, stopping`);
            break;
          }
          const convergence = detectConvergence(rawTotal, currentBatchSize, batchSizeHalved, limits.minBatchSize);
          if (convergence.shouldStop) {
            console.debug(formatConvergenceStatus(batchNum + 1, convergence));
            break;
          }
          if (convergence.newBatchSizeHalved) {
            batchSizeHalved = true;
            currentBatchSize = convergence.newBatchSize;
          }
          const cumulativeCheck = checkCumulativeLimits(accumulation.entities.length, accumulation.concepts.length, {
            customEntityCap: customTypeCaps.entityCap,
            customConceptCap: customTypeCaps.conceptCap,
            maxTotalItems: limits.maxTotalItems
          });
          if (cumulativeCheck.shouldStop) {
            console.debug(`[Batch ${batchNum + 1}] ${cumulativeCheck.reason}, stopping`);
            break;
          }
        }
        finalBatchNum = batchNum + 1;
      } catch (error) {
        console.error(`[Batch ${batchNum + 1}] Call failed:`, error);
        if (isFirstBatch) {
          const providerName = this.ctx.settings.provider;
          const modelName = this.ctx.settings.model;
          const errMsg3 = error instanceof Error ? error.message : String(error);
          const lowerErr = errMsg3.toLowerCase();
          let userHint;
          if (lowerErr.includes("context") || lowerErr.includes("token") || lowerErr.includes("length") || lowerErr.includes("exceed")) {
            userHint = "The request was rejected because the source file is too large for this model's context window. Try: (1) switch to a model with a larger context window (e.g. 1M tokens) in Settings, (2) reduce the file size, or (3) use a provider that supports larger contexts.";
          } else if (lowerErr.includes("max_tokens")) {
            userHint = "The model rejected the max_tokens value. Try reducing it in Settings \u2192 LLM Configuration \u2192 Context Window.";
          } else if (lowerErr.includes("400")) {
            userHint = "The API returned a Bad Request error. Check that the model name is correct and supported by your provider.";
          } else if (lowerErr.includes("401") || lowerErr.includes("403")) {
            userHint = "Authentication failed. Check your API key in Settings.";
          } else if (lowerErr.includes("429")) {
            userHint = "Rate limit exceeded. Wait a moment and try again, or switch to a provider with higher limits.";
          } else {
            userHint = "Check your network connection, API key, and provider URL in Settings. If the error mentions SSL/TLS, try: (1) restart Obsidian, (2) check VPN/proxy settings, (3) verify the provider URL is correct.";
          }
          throw new Error(
            `Failed to connect to ${providerName} API (model: ${modelName}): ${errMsg3}. ${userHint}`
          );
        }
        const errMsg2 = error instanceof Error ? error.message : String(error);
        const isTruncation = errMsg2.toLowerCase().includes("truncated") || errMsg2.toLowerCase().includes("max_tokens");
        if (!retryingBatch && isTruncation && currentBatchSize > limits.minBatchSize) {
          currentBatchSize = Math.max(limits.minBatchSize, Math.floor(currentBatchSize * 0.5));
          console.warn(`[Batch ${batchNum + 1}] Truncation detected, halving batch size to ${currentBatchSize} and retrying`);
          retryingBatch = true;
          batchNum--;
          continue;
        }
        retryingBatch = false;
        console.warn(`[Batch ${batchNum + 1}] Non-first-round failure, keeping extracted items`);
        break;
      }
    }
    if (!firstBatchData && accumulation.entities.length === 0 && accumulation.concepts.length === 0) {
      return null;
    }
    const allExtractedNames = [
      ...accumulation.entities.map((e) => e.name),
      ...accumulation.concepts.map((c) => c.name)
    ];
    if (allExtractedNames.length > 0) {
      try {
        const existingPages = await getExistingWikiPages(this.ctx.app, this.ctx.settings.wikiFolder);
        accumulation.relatedPages = matchExtractedToExisting(allExtractedNames, existingPages);
        console.debug("[Related pages] Programmatic matching:", accumulation.relatedPages.length, "pages matched");
      } catch (err) {
        console.warn("[Related pages] Programmatic matching failed:", err);
      }
    }
    if (this.ctx.settings.extractionGranularity === "custom") {
      const eCap = (_i = this.ctx.settings.customEntityLimit) != null ? _i : 5;
      const cCap = (_j = this.ctx.settings.customConceptLimit) != null ? _j : 5;
      if (accumulation.entities.length > eCap) accumulation.entities = accumulation.entities.slice(0, eCap);
      if (accumulation.concepts.length > cCap) accumulation.concepts = accumulation.concepts.slice(0, cCap);
    }
    const analysis = buildSourceAnalysis(
      file.path,
      file.basename,
      accumulation,
      firstBatchData ? {
        sourceTitle: firstBatchData.sourceTitle,
        summary: firstBatchData.summary
      } : void 0
    );
    console.debug("=== Iterative extraction complete ===");
    console.debug("  - Total batches:", finalBatchNum);
    console.debug("  - Entities count:", accumulation.entities.length);
    console.debug("  - Concepts count:", accumulation.concepts.length);
    console.debug("  - Deduplicated names:", accumulation.extractedNames.size);
    return analysis;
  }
};

// src/wiki/page-factory.ts
var import_obsidian6 = require("obsidian");

// src/core/conflict-resolver.ts
function folderOf(pageType) {
  return pageType === "entity" ? WIKI_SUBFOLDERS.entities : WIKI_SUBFOLDERS.concepts;
}
function oppositeFolder(pageType) {
  return pageType === "entity" ? WIKI_SUBFOLDERS.concepts : WIKI_SUBFOLDERS.entities;
}
function slugMatchKeys(page) {
  const keys = /* @__PURE__ */ new Set();
  keys.add(computeSlug(page.title));
  for (const alias of page.aliases || []) {
    keys.add(computeSlug(alias));
  }
  return keys;
}
var ConflictResolver = class {
  constructor(wikiFolder, allPages) {
    this.wikiFolder = wikiFolder;
    this.allPages = allPages;
  }
  /**
   * Determine what to do with a newly extracted entity/concept.
   * Returns a ConflictResolution that the caller should follow.
   *
   * Resolution order (deterministic, first match wins):
   * 1. Same-type slug/alias match → merge into existing page
   * 2. Cross-type slug/alias match → merge into opposite folder page
   * 3. No match → create new page
   */
  resolve(check) {
    const folder = folderOf(check.pageType);
    const otherFolder = oppositeFolder(check.pageType);
    const sameTypePages = this.allPages.filter((p) => p.path.includes(`/${folder}/`));
    const otherTypePages = this.allPages.filter((p) => p.path.includes(`/${otherFolder}/`));
    const checkKey = check.slug.toLowerCase();
    const exactPath = `${this.wikiFolder}/${folder}/${check.slug}.md`;
    let match = sameTypePages.find((p) => p.path === exactPath);
    if (match) {
      return {
        action: "merge",
        targetPath: match.path,
        confidence: "high",
        reason: `Same-type exact path match: ${exactPath}`
      };
    }
    match = sameTypePages.find((p) => slugMatchKeys(p).has(checkKey));
    if (match) {
      return {
        action: "merge",
        targetPath: match.path,
        confidence: "high",
        reason: `Same-type slug/alias match: title=${match.title} slug=${check.slug}`
      };
    }
    const otherExactPath = `${this.wikiFolder}/${otherFolder}/${check.slug}.md`;
    match = otherTypePages.find((p) => p.path === otherExactPath || slugMatchKeys(p).has(checkKey));
    if (match) {
      return {
        action: "merge",
        targetPath: match.path,
        existingPath: match.path,
        existingType: otherFolder === WIKI_SUBFOLDERS.entities ? "entity" : "concept",
        aliasToAdd: check.name !== match.title ? check.name : null,
        confidence: "high",
        reason: `Cross-type collision: ${check.pageType} "${check.name}" \u2192 ${match.path}`
      };
    }
    return {
      action: "create",
      targetPath: `${this.wikiFolder}/${folder}/${check.slug}.md`,
      confidence: "high",
      reason: "No conflict found"
    };
  }
  // ── Interactive query helpers (for future use) ──────────────────
  /** Return the number of pages per type (for stats/diagnostics). */
  stats() {
    return {
      entities: this.allPages.filter((p) => p.path.includes("/entities/")).length,
      concepts: this.allPages.filter((p) => p.path.includes("/concepts/")).length
    };
  }
};

// src/core/related-link-corrector.ts
function correctRelatedLinkPrefixes(content, relatedEntities, relatedConcepts, relatedEntitiesLabel, relatedConceptsLabel, preserveCase) {
  const folderBySlug = /* @__PURE__ */ new Map();
  const ambiguous = /* @__PURE__ */ new Set();
  const index = (names, folder) => {
    for (const n of names != null ? names : []) {
      const s = slugify(n, preserveCase);
      if (!s) continue;
      const prev = folderBySlug.get(s);
      if (prev && prev !== folder) {
        ambiguous.add(s);
        continue;
      }
      folderBySlug.set(s, folder);
    }
  };
  index(relatedEntities, "entities");
  index(relatedConcepts, "concepts");
  const sectionFolder = /* @__PURE__ */ new Map();
  for (const h of [relatedEntitiesLabel.trim(), "Related Entities"]) sectionFolder.set(h, "entities");
  for (const h of [relatedConceptsLabel.trim(), "Related Concepts"]) sectionFolder.set(h, "concepts");
  const linkRe = /\[\[(entities|concepts|sources)\/([^\]|]+)(\|[^\]]*)?\]\]/g;
  let current;
  return content.split("\n").map((line) => {
    const header = /^#{1,6}\s+(.*?)\s*$/.exec(line);
    if (header) {
      current = sectionFolder.get(header[1].trim());
      return line;
    }
    if (!current) return line;
    return line.replace(linkRe, (full, folder, target, display) => {
      const s = slugify(target, preserveCase);
      const correct = !ambiguous.has(s) && folderBySlug.get(s) || current;
      if (correct === folder) return full;
      return `[[${correct}/${target}${display != null ? display : ""}]]`;
    });
  }).join("\n");
}

// src/core/report.ts
function nestReportUnderParent(report) {
  const lines = report.split("\n");
  let h1Stripped = false;
  const out = [];
  for (const line of lines) {
    const m = /^(#+)\s/.exec(line);
    if (m) {
      if (!h1Stripped && m[1].length === 1) {
        h1Stripped = true;
        continue;
      }
      out.push("#" + line);
      h1Stripped = true;
      continue;
    }
    out.push(line);
  }
  return out.join("\n");
}
function truncateMentions(mentions, maxChars = 500, sourcePath) {
  if (!mentions || mentions.length === 0) return "";
  let result = "";
  if (sourcePath) {
    const leftPath = sourcePath.replace(/\.md$/, "");
    const displayName = leftPath.split("/").pop() || leftPath;
    for (const m of mentions) {
      const line = `- ${m} \u2014 [[${leftPath}|${displayName}]]`;
      if (result.length + line.length + 1 > maxChars) {
        if (result.length > 0) break;
        const head = Math.max(0, maxChars - ` \u2014 [[${leftPath}|${displayName}]]`.length - 3);
        return `- ${m.substring(0, head)}... \u2014 [[${leftPath}|${displayName}]]`;
      }
      result += (result ? "\n" : "") + line;
    }
    return result;
  }
  for (const m of mentions) {
    const line = `- ${m}`;
    if (result.length + line.length + 1 > maxChars) {
      if (result.length > 0) break;
      return `- ${m.substring(0, Math.max(0, maxChars - 3))}...`;
    }
    result += (result ? "\n" : "") + line;
  }
  return result;
}

// src/wiki/page-factory.ts
function contextualizeError(error, name, pageType) {
  const msg = error instanceof Error ? error.message : String(error);
  return new Error(`Failed to create ${pageType} page "${name}": ${msg}`);
}
function mergeError(error, name, pageType) {
  const msg = error instanceof Error ? error.message : String(error);
  return new Error(`Failed to merge ${pageType} page "${name}": ${msg}`);
}
var PageFactory = class {
  constructor(ctx) {
    this.ctx = ctx;
  }
  // Append aliases to an existing wiki page, deduplicating against existing frontmatter.
  async appendAliases(pagePath, newAliases) {
    const content = await this.ctx.tryReadFile(pagePath);
    if (!content) return;
    const candidates = filterRedundantAliases(pagePath, newAliases);
    if (candidates.length === 0) return;
    const fm = parseFrontmatter(content);
    const existingAliases = Array.isArray(fm == null ? void 0 : fm.aliases) ? fm.aliases : [];
    const toAdd = candidates.filter((a) => !existingAliases.includes(a));
    if (toAdd.length === 0) return;
    const merged = [...existingAliases, ...toAdd];
    const aliasesLine = `aliases:
${merged.map((a) => `  - "${a}"`).join("\n")}`;
    const fmStart = content.indexOf("---");
    const fmEnd = content.indexOf("\n---", fmStart + 3);
    if (fmStart === -1 || fmEnd === -1) return;
    const fmText = content.substring(fmStart + 3, fmEnd);
    const body = content.substring(fmEnd + 4);
    let newFm;
    if (fmText.includes("aliases:")) {
      newFm = fmText.replace(/^aliases:[^\n]*(?:\n[ \t]+[^\n]*)*/m, aliasesLine);
    } else {
      newFm = fmText.trimEnd() + "\n" + aliasesLine;
    }
    const newContent = `---${newFm}
---${body}`;
    await this.ctx.createOrUpdateFile(pagePath, newContent);
    console.debug(`appendAliases: added ${toAdd.join(", ")} to ${pagePath}`);
  }
  // Determine the actual file path for a new entity/concept, using slug-based
  // matching first and falling back to LLM semantic resolution.
  // Returns { path: null, collision: {...} } when a cross-type collision is detected
  // (same name exists in the opposite folder). Callers must NOT create a new file in
  // that case, but should merge the new content into collision.targetPath so no
  // information from the source is lost.
  async resolvePagePath(name, pageType, summary) {
    const folder = pageType === "entity" ? WIKI_SUBFOLDERS.entities : WIKI_SUBFOLDERS.concepts;
    const otherFolder = pageType === "entity" ? WIKI_SUBFOLDERS.concepts : WIKI_SUBFOLDERS.entities;
    const slug = slugify(name, this.ctx.settings.slugCase === "preserve");
    const slugPath = `${this.ctx.settings.wikiFolder}/${folder}/${slug}.md`;
    const existing = await this.ctx.tryReadFile(slugPath);
    if (existing !== null) {
      const otherSlugPath = `${this.ctx.settings.wikiFolder}/${otherFolder}/${slug}.md`;
      const otherExisting = await this.ctx.tryReadFile(otherSlugPath);
      if (otherExisting !== null) {
        console.warn(`Historical cross-type duplicate detected: ${folder}/${slug}.md and ${otherFolder}/${slug}.md both exist \u2014 appending alias`);
        await this.appendAliases(otherSlugPath, [name]);
      }
      return { path: slugPath };
    }
    try {
      const allPages = await getExistingWikiPages(this.ctx.app, this.ctx.settings.wikiFolder);
      const resolver = new ConflictResolver(this.ctx.settings.wikiFolder, allPages);
      const cr = resolver.resolve({ name, slug, pageType });
      if (cr.action === "merge" && !cr.reason.includes("Cross-type")) {
        await this.appendAliases(cr.targetPath, [name]);
        return { path: cr.targetPath };
      }
      if (cr.action === "merge" && cr.reason.includes("Cross-type")) {
        await this.appendAliases(cr.targetPath, [name]);
        return {
          path: null,
          collision: {
            name,
            sourceType: pageType,
            targetType: cr.existingType || (otherFolder === WIKI_SUBFOLDERS.entities ? "entity" : "concept"),
            targetPath: cr.targetPath
          }
        };
      }
      const sameTypePages = allPages.filter((p) => p.path.includes(`/${folder}/`)).filter((p) => {
        const bn = p.title || "";
        return !/^(entities|concepts|sources)([^\s\-_a-zA-Z0-9])/.test(bn);
      });
      if (sameTypePages.length === 0) return { path: slugPath };
      const pagesList = sameTypePages.map((p) => {
        var _a;
        const aliasBlock = ((_a = p.aliases) == null ? void 0 : _a.length) ? `
  aliases: ${p.aliases.join(", ")}` : "";
        return `- path: ${p.path}
  title: ${p.title}${aliasBlock}`;
      }).join("\n");
      const client = this.ctx.getClient();
      if (!client) return { path: slugPath };
      const prompt = PROMPTS.resolveEntityDedup.replace("{{wikiFolder}}", this.ctx.settings.wikiFolder).replace("{{entity_name}}", name).replace("{{entity_type}}", pageType).replace("{{entity_summary}}", summary.substring(0, 300)).replace("{{page_type}}", pageType).replace("{{existing_pages}}", pagesList);
      const response = await client.createMessage({
        model: this.ctx.settings.model,
        max_tokens: TOKENS_DEDUP_RESOLUTION,
        system: await this.ctx.buildSystemPrompt("full"),
        messages: [{ role: "user", content: prompt }],
        response_format: { type: "json_object" },
        ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
      });
      const result = await parseJsonResponse(response);
      if ((result == null ? void 0 : result.match) && (result == null ? void 0 : result.path)) {
        result.path = normalizeLLMPath(result.path, this.ctx.settings.wikiFolder);
        console.debug(`Entity resolution: "${name}" matched existing page "${result.path}"`);
        await this.appendAliases(result.path, [name]);
        return { path: result.path };
      }
    } catch (error) {
      console.debug(`Entity resolution for "${name}" failed, using slug path:`, error);
    }
    return { path: slugPath };
  }
  async buildPagesListForPrompt(includePaths = []) {
    const allPages = await getExistingWikiPages(this.ctx.app, this.ctx.settings.wikiFolder);
    const cleanPages = allPages.filter((p) => {
      const bn = p.title || "";
      return !/^(entities|concepts|sources)([^\s\-_a-zA-Z0-9])/.test(bn);
    });
    const MAX_PAGES = 50;
    let pages = cleanPages;
    let truncated = false;
    if (cleanPages.length > MAX_PAGES) {
      const hasEntityExtra = includePaths.some((p) => p.includes("/entities/"));
      const hasConceptExtra = includePaths.some((p) => p.includes("/concepts/"));
      if (hasEntityExtra && !hasConceptExtra) {
        pages = allPages.filter((p) => p.path.includes("/entities/")).slice(0, MAX_PAGES);
      } else if (hasConceptExtra && !hasEntityExtra) {
        pages = allPages.filter((p) => p.path.includes("/concepts/")).slice(0, MAX_PAGES);
      } else {
        pages = allPages.slice(0, MAX_PAGES);
      }
      truncated = true;
    }
    const list = pages.map((p) => {
      var _a;
      const aliasSuffix = ((_a = p.aliases) == null ? void 0 : _a.length) ? ` \`aliases: ${p.aliases.join(", ")}\`` : "";
      return `- ${p.wikiLink}${aliasSuffix}`;
    }).join("\n");
    let result = list;
    if (includePaths.length > 0) {
      const newPages = includePaths.map((p) => {
        const relPath = p.replace(this.ctx.settings.wikiFolder + "/", "").replace(".md", "");
        const name = relPath.split("/").pop() || relPath;
        return `- [[${relPath}|${name}]]`;
      }).filter((entry) => !list.includes(entry));
      if (newPages.length > 0) {
        result = list + "\n" + newPages.join("\n");
      }
    }
    if (truncated) {
      result += `
(Wiki has ${allPages.length} pages total; showing first ${MAX_PAGES}. See index.md for the full list.)`;
    }
    return result;
  }
  async createOrUpdateEntityPage(entity, _analysis, sourceFile, extraPagePaths = [], sourceSlug) {
    return this.createOrUpdatePage(entity, "entity", sourceFile, extraPagePaths, sourceSlug);
  }
  async createOrUpdateConceptPage(concept, _analysis, sourceFile, extraPagePaths = [], sourceSlug) {
    return this.createOrUpdatePage(concept, "concept", sourceFile, extraPagePaths, sourceSlug);
  }
  // ── Generic page CRUD (entity/concept unified) ──────────────────────
  async createOrUpdatePage(info, pageType, sourceFile, extraPagePaths = [], sourceSlug) {
    var _a, _b;
    if (!info.name || info.name.trim().length === 0) {
      console.warn(`${pageType} name is empty, skipping creation`);
      return { path: null };
    }
    console.debug(`=== Creating/Updating ${pageType} page ===`);
    console.debug("name:", info.name);
    console.debug("type:", info.type);
    const result = await this.resolvePagePath(info.name, pageType, info.summary);
    if (result.path === null) {
      if (result.collision) {
        const { targetPath, targetType } = result.collision;
        const existingContent2 = await this.ctx.tryReadFile(targetPath);
        if (existingContent2) {
          const isReviewed2 = ((_a = parseFrontmatter(existingContent2)) == null ? void 0 : _a.reviewed) === true;
          if (isReviewed2) {
            await this.appendToReviewedPage(info, sourceFile, existingContent2, targetPath, sourceSlug);
          } else {
            await this.mergePage(info, targetType, sourceFile, existingContent2, extraPagePaths, targetPath, sourceSlug);
          }
          console.debug(`Cross-type collision: merged "${info.name}" content into ${targetType} page ${targetPath}`);
        }
      }
      return result;
    }
    console.debug("Resolved path:", result.path);
    const existingContent = await this.ctx.tryReadFile(result.path);
    if (!existingContent) {
      const createdPath = await this.createNewPage(info, pageType, sourceFile, extraPagePaths, result.path, sourceSlug);
      return { path: createdPath };
    }
    const isReviewed = ((_b = parseFrontmatter(existingContent)) == null ? void 0 : _b.reviewed) === true;
    if (isReviewed) {
      console.debug(`${pageType} page has reviewed: true, using minimal append mode:`, result.path);
      const updatedPath = await this.appendToReviewedPage(info, sourceFile, existingContent, result.path, sourceSlug);
      return { path: updatedPath };
    }
    const mergedPath = await this.mergePage(info, pageType, sourceFile, existingContent, extraPagePaths, result.path, sourceSlug);
    return { path: mergedPath };
  }
  async createNewPage(info, pageType, sourceFile, extraPagePaths, path, sourceSlug) {
    var _a, _b, _c;
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    try {
      const generatePrompt = pageType === "entity" ? PROMPTS.generateEntityPage : PROMPTS.generateConceptPage;
      const prompt = generatePrompt.replace("{{entity_name}}", info.name).replace("{{concept_name}}", info.name).replace("{{entity_type}}", info.type).replace("{{concept_type}}", info.type).replace("{{entity_summary}}", info.summary).replace("{{concept_summary}}", info.summary).replace("{{extraction_aliases}}", ((_a = info.aliases) == null ? void 0 : _a.length) ? `[${info.aliases.join(", ")}]` : "None").replace("{{mentions}}", truncateMentions(info.mentions_in_source, 500, sourceFile.path) || "No specific mentions").replace("{{related_entities}}", ((_b = info.related_entities) == null ? void 0 : _b.join(", ")) || "No related entities").replace("{{related_concepts}}", ((_c = info.related_concepts) == null ? void 0 : _c.join(", ")) || "No related concepts").replace("{{existing_pages}}", await this.buildPagesListForPrompt(extraPagePaths)).replace("{{related_content}}", "No existing content").replace("{{merge_strategy}}", "New page, no merge needed.").replace("{{date}}", (/* @__PURE__ */ new Date()).toISOString().split("T")[0]).replace(/\{\{source_file\}\}/g, sourceSlug ? `sources/${sourceSlug}` : sourceFile.path);
      const finalPrompt = appendTagVocabularyToPrompt(applySectionLabels(prompt, this.ctx.settings), this.ctx.settings);
      const pageContent = await client.createMessage({
        model: this.ctx.settings.model,
        max_tokens: TOKENS_PAGE_GENERATION,
        system: await this.ctx.buildSystemPrompt(pageType),
        messages: [{ role: "user", content: finalPrompt }],
        ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
      });
      const cleanedContent = cleanMarkdownResponse(pageContent);
      const enforcedContent = enforceFrontmatterConstraints(cleanedContent, pageType, this.ctx.settings);
      const labels = getSectionLabels(this.ctx.settings);
      const correctedContent = correctRelatedLinkPrefixes(
        enforcedContent,
        info.related_entities,
        info.related_concepts,
        labels.related_entities,
        labels.related_concepts,
        this.ctx.settings.slugCase === "preserve"
      );
      await this.ctx.createOrUpdateFile(path, correctedContent);
      return path;
    } catch (error) {
      throw contextualizeError(error, info.name, pageType);
    }
  }
  async mergePage(info, pageType, sourceFile, existingContent, extraPagePaths, path, sourceSlug) {
    var _a, _b, _c;
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    try {
      const { frontmatter, body: existingBody } = mergeFrontmatter(existingContent, sourceSlug ? `sources/${sourceSlug}` : sourceFile.path);
      const mergePrompt = pageType === "entity" ? PROMPTS.mergeEntityPage : PROMPTS.mergeConceptPage;
      const prompt = mergePrompt.replace("{{existing_body}}", existingBody).replace("{{new_source}}", sourceFile.basename).replace("{{entity_summary}}", info.summary).replace("{{concept_summary}}", info.summary).replace("{{mentions}}", truncateMentions(info.mentions_in_source, 500, sourceFile.path)).replace("{{related_entities}}", ((_a = info.related_entities) == null ? void 0 : _a.join(", ")) || "").replace("{{related_concepts}}", ((_b = info.related_concepts) == null ? void 0 : _b.join(", ")) || "").replace("{{key_details}}", ((_c = info.mentions_in_source) == null ? void 0 : _c.slice(0, 2).join("; ")) || "").replace("{{existing_pages}}", await this.buildPagesListForPrompt(extraPagePaths));
      const finalPrompt = appendTagVocabularyToPrompt(applySectionLabels(prompt, this.ctx.settings), this.ctx.settings);
      const mergedBody = await client.createMessage({
        model: this.ctx.settings.model,
        max_tokens: TOKENS_PAGE_GENERATION,
        system: await this.ctx.buildSystemPrompt("merge"),
        messages: [{ role: "user", content: finalPrompt }],
        ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
      });
      const cleanedBody = cleanMarkdownResponse(mergedBody);
      if (cleanedBody.trim() === "NO_NEW_CONTENT") {
        console.debug(`${pageType} page merge returned NO_NEW_CONTENT, keeping existing:`, path);
        return path;
      }
      const labels = getSectionLabels(this.ctx.settings);
      const correctedBody = correctRelatedLinkPrefixes(
        cleanedBody,
        info.related_entities,
        info.related_concepts,
        labels.related_entities,
        labels.related_concepts,
        this.ctx.settings.slugCase === "preserve"
      );
      const finalContent = `${frontmatter}

${correctedBody}`;
      await this.ctx.createOrUpdateFile(path, finalContent);
      return path;
    } catch (error) {
      throw mergeError(error, info.name, pageType);
    }
  }
  async appendToReviewedPage(info, sourceFile, existingContent, path, sourceSlug) {
    var _a;
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    try {
      const { frontmatter, body: existingBody } = mergeFrontmatter(existingContent, sourceSlug ? `sources/${sourceSlug}` : sourceFile.path);
      const prompt = PROMPTS.appendToReviewedPage.replace("{{existing_body}}", existingBody).replace("{{new_source}}", sourceFile.basename).replace("{{entity_summary}}", info.summary).replace("{{mentions}}", truncateMentions(info.mentions_in_source, 500, sourceFile.path)).replace("{{key_details}}", ((_a = info.mentions_in_source) == null ? void 0 : _a.slice(0, 2).join("; ")) || "").replace("{{constraints}}", UNIVERSAL_LINK_CONSTRAINTS);
      const finalPrompt = appendTagVocabularyToPrompt(applySectionLabels(prompt, this.ctx.settings), this.ctx.settings);
      const newContent = await client.createMessage({
        model: this.ctx.settings.model,
        max_tokens: TOKENS_APPEND_REVIEWED,
        system: await this.ctx.buildSystemPrompt("merge"),
        messages: [{ role: "user", content: finalPrompt }],
        ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
      });
      const cleanedContent = cleanMarkdownResponse(newContent);
      if (cleanedContent.trim() === "NO_NEW_CONTENT") {
        console.debug("Reviewed page has no new content, preserving existing:", path);
        return path;
      }
      const finalContent = `${frontmatter}

${cleanedContent}`;
      await this.ctx.createOrUpdateFile(path, finalContent);
      return path;
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      throw new Error(`Failed to update reviewed page "${info.name}": ${msg}`);
    }
  }
  async updateRelatedPage(pageName, analysis, sourceFile, sourceSlug) {
    var _a;
    const existingPages = await getExistingWikiPages(this.ctx.app, this.ctx.settings.wikiFolder);
    const page = existingPages.find((p) => p.title === pageName);
    if (!page) {
      console.debug("Related page not found:", pageName);
      return false;
    }
    const abstractFile = this.ctx.app.vault.getAbstractFileByPath(page.path);
    if (!(abstractFile instanceof import_obsidian6.TFile)) {
      console.debug("Related page is not a file:", pageName);
      return false;
    }
    const existingContent = await this.ctx.app.vault.read(abstractFile);
    const { frontmatter, body: existingBody } = mergeFrontmatter(existingContent, sourceSlug ? `sources/${sourceSlug}` : sourceFile.path);
    const newInfo = analysis.entities.find((e) => e.name === pageName) || analysis.concepts.find((c) => c.name === pageName);
    if (!newInfo) {
      await this.ctx.createOrUpdateFile(page.path, `${frontmatter}

${existingBody}`);
      return true;
    }
    if (((_a = parseFrontmatter(existingContent)) == null ? void 0 : _a.reviewed) === true) {
      await this.appendToReviewedPage(newInfo, sourceFile, existingContent, page.path);
      return true;
    }
    const prompt = PROMPTS.updateRelatedPage.replace("{{page_name}}", pageName).replace("{{existing_body}}", existingBody).replace("{{source_basename}}", sourceFile.basename).replace("{{new_info}}", JSON.stringify(newInfo)).replace("{{constraints}}", UNIVERSAL_LINK_CONSTRAINTS);
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    const updatedBody = await client.createMessage({
      model: this.ctx.settings.model,
      max_tokens: TOKENS_PAGE_GENERATION,
      system: await this.ctx.buildSystemPrompt("related"),
      messages: [{ role: "user", content: prompt }],
      ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
    const cleanedBody = cleanMarkdownResponse(updatedBody);
    const finalContent = `${frontmatter}

${cleanedBody}`;
    await this.ctx.createOrUpdateFile(page.path, finalContent);
    return true;
  }
};

// src/wiki/conversation-ingest.ts
function formatConversation(history) {
  return history.messages.map((msg) => {
    const role = msg.role === "user" ? "\u{1F464} User" : "\u{1F916} Wiki";
    const time = new Date(msg.timestamp).toLocaleTimeString();
    return `### ${role} (${time})

${msg.content}

---
`;
  }).join("\n");
}
var ConversationIngestor = class {
  constructor(ctx, pageFactory, orch) {
    this.ctx = ctx;
    this.pageFactory = pageFactory;
    this.orch = orch;
  }
  async ingestConversation(history) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
    const startTime = Date.now();
    const client = this.ctx.getClient();
    if (!client) {
      throw new Error("LLM Client not initialized");
    }
    console.debug("=== Starting conversation extraction ===");
    (_b = (_a = this.ctx).onProgress) == null ? void 0 : _b.call(_a, "Analyzing conversation...");
    const actualDate = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
    console.debug("[System time]", actualDate);
    const indexPath = `${this.ctx.settings.wikiFolder}/index.md`;
    const existingWikiIndex = await this.ctx.tryReadFile(indexPath) || "Wiki is empty";
    console.debug("[Wiki\u7D22\u5F15]", existingWikiIndex ? "\u5DF2\u8BFB\u53D6" : "\u4E3A\u7A7A");
    const conversationText = formatConversation(history);
    if (existingWikiIndex !== "Wiki is empty") {
      (_d = (_c = this.ctx).onProgress) == null ? void 0 : _d.call(_c, "Checking for existing knowledge...");
      try {
        const dedupResult = await this.checkDedup(existingWikiIndex, conversationText);
        if (dedupResult === "fully_redundant") {
          console.debug("Conversation fully covered by existing Wiki, skipping save");
          (_f = (_e = this.ctx).onProgress) == null ? void 0 : _f.call(_e, "This knowledge already exists in Wiki");
          return {
            sourceFile: `Conversation: ${((_h = (_g = history.messages[0]) == null ? void 0 : _g.content) == null ? void 0 : _h.substring(0, 50)) || "unknown"}`,
            createdPages: [],
            updatedPages: [],
            entitiesCreated: 0,
            conceptsCreated: 0,
            failedItems: [],
            collisions: [],
            contradictionsFound: 0,
            success: true,
            errorMessage: "Knowledge already exists in Wiki"
          };
        }
      } catch (error) {
        console.debug("Dedup check failed, proceeding with save:", error);
      }
    }
    const analysisPrompt = `You are a Wiki knowledge extraction assistant.

Existing Wiki Index (use this as reference for entity/concept names):
${existingWikiIndex}

User conversation with AI:
${conversationText}

Convert this conversation into structured Wiki pages.

Focus on:
1. Extracting key knowledge points (not full conversation log)
2. Identifying core concepts and entities discussed
3. Summarizing conversation topic and conclusions
4. Entity/concept names should match existing Wiki pages if possible

Actual conversation date: ${actualDate} (use this, do not generate date yourself)

Output JSON format:
{
  "source_title": "Semantic Topic Title (no date, describe the discussion topic)",
  "summary": "Conversation topic summary",
  "entities": [
    {
      "name": "Short Reference Name",
      "type": "person|organization|project|product|event|place|other",
      "summary": "Entity information summary",
      "mentions_in_source": ["Specific mentions in conversation"]
    }
  ],
  "concepts": [
    {
      "name": "Concept Name",
      "type": "theory|method|field|phenomenon|standard|term|other",
      "summary": "Concept definition",
      "mentions_in_source": ["Specific mentions in conversation"],
      "related_concepts": ["Related Concept 1", "Related Concept 2"]
    }
  ],
  "key_points": ["Point 1", "Point 2"],
  "created_pages": [],
  "updated_pages": []
}

CRITICAL RULES:
- source_title: Semantic title describing discussion topic (NOT date-based generic title)
- entity.name: Choose or extract appropriate name from Wiki index (maintain consistency with existing Wiki)
- concept.name: Same principle - reference Wiki index for concept names
- mentions_in_source: REQUIRED field - list actual mentions in conversation text
- If no entities/concepts found, use empty arrays [] (never omit the field)
- Names should be suitable for [[wiki-links]] referencing (judge appropriate naming based on Wiki index)`;
    const analysis = await client.createMessage({
      model: this.ctx.settings.model,
      max_tokens: TOKENS_CONVERSATION_EXTRACTION,
      system: await this.ctx.buildSystemPrompt("conversation"),
      messages: [{
        role: "user",
        content: analysisPrompt
      }],
      response_format: { type: "json_object" },
      ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
    const parsed = await parseJsonResponse(analysis, async (malformedJson) => {
      const repairPrompt = `Fix the following malformed JSON. Only fix JSON syntax errors (unescaped quotes, trailing commas, missing brackets). Do NOT change any values or content. Output ONLY the fixed JSON, no other text.

${malformedJson}`;
      return await client.createMessage({
        model: this.ctx.settings.model,
        max_tokens: TOKENS_PAGE_GENERATION,
        system: await this.ctx.buildSystemPrompt("conversation"),
        messages: [{ role: "user", content: repairPrompt }],
        response_format: { type: "json_object" },
        ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
      });
    });
    if (!parsed) {
      throw new Error("Conversation analysis JSON parsing failed");
    }
    console.debug("[LLM\u5206\u6790\u7ED3\u679C]", parsed);
    console.debug("[\u751F\u6210\u7684\u6807\u9898]", parsed.source_title);
    (_j = (_i = this.ctx).onProgress) == null ? void 0 : _j.call(_i, "Creating summary page...");
    await this.orch.ensureWikiStructure();
    const preserveCase = this.ctx.settings.slugCase === "preserve";
    const semanticSlug = slugify(parsed.source_title, preserveCase);
    const summaryPath = `${this.ctx.settings.wikiFolder}/sources/${semanticSlug}.md`;
    console.debug("[Semantic file path]", summaryPath);
    const convPlannedPaths = [summaryPath];
    for (const entity of parsed.entities) {
      convPlannedPaths.push(`${this.ctx.settings.wikiFolder}/entities/${slugify(entity.name, preserveCase)}.md`);
    }
    for (const concept of parsed.concepts) {
      convPlannedPaths.push(`${this.ctx.settings.wikiFolder}/concepts/${slugify(concept.name, preserveCase)}.md`);
    }
    const createdPagesList = convPlannedPaths.length > 0 ? convPlannedPaths.map((p) => {
      const relPath = p.replace(this.ctx.settings.wikiFolder + "/", "").replace(".md", "");
      const name = relPath.split("/").pop() || relPath;
      return `- [[${relPath}|${name}]]`;
    }).join("\n") : "(none)";
    const tags = parsed.concepts.map((c) => c.name).join(", ");
    const summaryPrompt = PROMPTS.generateSummaryPage.replace("{{source_title}}", parsed.source_title).replace("{{content}}", conversationText.substring(0, 500)).replace("{{analysis}}", JSON.stringify(parsed)).replace("{{created_pages_list}}", createdPagesList).replace(/{{source_file}}/g, `Conversation: ${parsed.source_title}`).replace(/{{date}}/g, actualDate).replace("{{tags}}", tags).replace("{{constraints}}", UNIVERSAL_LINK_CONSTRAINTS);
    const finalSummaryPrompt = applySectionLabels(summaryPrompt, this.ctx.settings);
    (_l = (_k = this.ctx).onProgress) == null ? void 0 : _l.call(_k, "Generating summary page...");
    const summaryPageContent = await client.createMessage({
      model: this.ctx.settings.model,
      max_tokens: TOKENS_CONVERSATION_PAGE,
      system: await this.ctx.buildSystemPrompt("summary"),
      messages: [{ role: "user", content: finalSummaryPrompt }],
      ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
    const cleanedSummary = cleanMarkdownResponse(summaryPageContent);
    await this.ctx.createOrUpdateFile(summaryPath, cleanedSummary);
    parsed.created_pages.push(summaryPath);
    const failedItems = [];
    const collisions = [];
    for (const entity of parsed.entities) {
      await this.orch.apiDelay();
      (_n = (_m = this.ctx).onProgress) == null ? void 0 : _n.call(_m, `Saving entity: ${entity.name}`);
      try {
        const entityResult = await this.pageFactory.createOrUpdateEntityPage(entity, parsed, { path: summaryPath, basename: semanticSlug }, convPlannedPaths);
        if (entityResult.path) {
          parsed.created_pages.push(entityResult.path);
        }
        if (entityResult.collision) {
          collisions.push(entityResult.collision);
        }
      } catch (error) {
        const reason = error instanceof Error ? error.message : String(error);
        console.error(`Conversation entity "${entity.name}" failed:`, error);
        failedItems.push({ type: "entity", name: entity.name, reason });
      }
    }
    for (const concept of parsed.concepts) {
      await this.orch.apiDelay();
      (_p = (_o = this.ctx).onProgress) == null ? void 0 : _p.call(_o, `Saving concept: ${concept.name}`);
      try {
        const conceptResult = await this.pageFactory.createOrUpdateConceptPage(concept, parsed, { path: summaryPath, basename: semanticSlug }, convPlannedPaths);
        if (conceptResult.path) {
          parsed.created_pages.push(conceptResult.path);
        }
        if (conceptResult.collision) {
          collisions.push(conceptResult.collision);
        }
      } catch (error) {
        const reason = error instanceof Error ? error.message : String(error);
        console.error(`Conversation concept "${concept.name}" failed:`, error);
        failedItems.push({ type: "concept", name: concept.name, reason });
      }
    }
    (_r = (_q = this.ctx).onProgress) == null ? void 0 : _r.call(_q, "Generating index...");
    await this.orch.generateIndex();
    parsed.contradictions = parsed.contradictions || [];
    await this.orch.updateLog("conversation", parsed);
    const entitiesCreated = parsed.created_pages.filter((p) => p.includes("/entities/")).length;
    const conceptsCreated = parsed.created_pages.filter((p) => p.includes("/concepts/")).length;
    const report = {
      sourceFile: `Conversation: ${parsed.source_title}`,
      createdPages: parsed.created_pages,
      updatedPages: parsed.updated_pages || [],
      entitiesCreated,
      conceptsCreated,
      failedItems,
      collisions,
      contradictionsFound: ((_s = parsed.contradictions) == null ? void 0 : _s.length) || 0,
      success: true,
      elapsedSeconds: Math.round((Date.now() - startTime) / 1e3)
    };
    console.debug("=== Conversation extraction complete ===");
    console.debug("Created pages:", parsed.created_pages);
    (_u = (_t = this.ctx).onDone) == null ? void 0 : _u.call(_t, report);
    return report;
  }
  async checkDedup(wikiIndex, conversationText) {
    const summary = conversationText.substring(0, 1500);
    const prompt = PROMPTS.dedupCheck.replace("{{wiki_index}}", wikiIndex.substring(0, 3e3)).replace("{{conversation_summary}}", summary);
    const client = this.ctx.getClient();
    if (!client) throw new Error("LLM client not initialized");
    const response = await client.createMessage({
      model: this.ctx.settings.model,
      max_tokens: TOKENS_QUERY_SAVE_DEDUP,
      system: await this.ctx.buildSystemPrompt("conversation"),
      messages: [{ role: "user", content: prompt }],
      response_format: { type: "json_object" },
      ...this.ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
    const parsed = await parseJsonResponse(response);
    return (parsed == null ? void 0 : parsed.status) || "entirely_new";
  }
};

// src/wiki/wiki-engine.ts
function dedupPages(paths) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const p of paths) {
    if (seen.has(p)) continue;
    seen.add(p);
    out.push(p);
  }
  return out;
}
var WikiEngine = class {
  constructor(app, settings, getLLMClient, schemaManager, onFileWrite, onProgress, onDone) {
    /**
     * #164: invoked when an interactive ingest hits a duplicate. Returns true to
     * re-ingest anyway, false to skip. Wired by main.ts to a confirmation modal;
     * left null for non-interactive (folder/watcher) ingest, which auto-skips.
     */
    this.onConfirmReingest = null;
    this.abortController = null;
    this.lintAbortController = null;
    this.wasCancelled = false;
    this.onIngestionStart = null;
    this.onIngestionEnd = null;
    this.onLintStart = null;
    this.onLintEnd = null;
    this.onStatusBarUpdate = null;
    this.pagesCache = null;
    this.pagesCacheTime = 0;
    this.PAGES_CACHE_TTL_MS = PAGES_CACHE_TTL_MS;
    // #164: ingested content-hash snapshot, cached on the same TTL/lifecycle as
    // pagesCache so back-to-back single-file ingests don't re-walk the vault.
    this.ingestedHashesCache = null;
    this.ingestedHashesCacheTime = 0;
    this.app = app;
    this.settings = settings;
    this.llmClient = null;
    this.getLLMClient = getLLMClient;
    this.schemaManager = schemaManager;
    this.onFileWrite = onFileWrite || null;
    this.onProgress = onProgress || null;
    this.onDone = onDone || null;
    const ctx = {
      app: this.app,
      settings: this.settings,
      getClient: () => this.getLLMClient(),
      createOrUpdateFile: (p, c) => this.createOrUpdateFile(p, c),
      deleteFile: (p) => this.deleteFile(p),
      tryReadFile: (p) => this.tryReadFile(p),
      buildSystemPrompt: (task) => buildSystemPrompt(this.settings, (t) => this.schemaManager.getSchemaContext(t), task),
      getSectionLabels: () => getSectionLabels(this.settings),
      getExistingWikiPages: () => getExistingWikiPages(this.app, this.settings.wikiFolder),
      getSchemaContext: (t) => this.schemaManager.getSchemaContext(t),
      onFileWrite: (path) => {
        var _a;
        return (_a = this.onFileWrite) == null ? void 0 : _a.call(this, path);
      },
      onProgress: (msg) => this.notifyProgress(msg),
      onDone: (report) => {
        var _a;
        return (_a = this.onDone) == null ? void 0 : _a.call(this, report);
      }
    };
    this.ctx = ctx;
    this.contradictionManager = new ContradictionManager(ctx);
    this.sourceAnalyzer = new SourceAnalyzer(ctx);
    this.pageFactory = new PageFactory(ctx);
    const orch = {
      ensureWikiStructure: () => this.ensureWikiStructure(),
      apiDelay: (ms) => this.apiDelay(ms),
      generateIndex: () => this.generateIndexFromEngine(),
      updateLog: (op, analysis) => this.updateLog(op, analysis)
    };
    this.conversationIngestor = new ConversationIngestor(ctx, this.pageFactory, orch);
  }
  setFileWriteCallback(cb) {
    this.onFileWrite = cb;
  }
  setProgressCallback(cb) {
    this.onProgress = cb;
  }
  getProgressCallback() {
    return this.onProgress;
  }
  setStatusBarUpdateCallback(cb) {
    this.onStatusBarUpdate = cb;
  }
  updateStatusBar(text) {
    var _a;
    (_a = this.onStatusBarUpdate) == null ? void 0 : _a.call(this, text);
  }
  notifyProgress(msg) {
    var _a;
    (_a = this.onProgress) == null ? void 0 : _a.call(this, msg);
    this.updateStatusBar(msg);
  }
  /**
   * Issue #170: stamp `generation_complete: true` on a wiki page after a
   * successful write. The pre-ingest requirement that pages carry this flag
   * is implicit — if it's missing, the page is treated as legacy (preserved).
   * This is best-effort: if re-read fails we just leave the file as-is; the
   * startup self-scan will catch any incomplete pages.
   */
  markPageComplete(path) {
    void (async () => {
      try {
        const current = await this.tryReadFile(path);
        if (!current) return;
        const flipped = setGenerationComplete(current, true);
        if (flipped === current) return;
        const file = this.app.vault.getAbstractFileByPath(path);
        if (file instanceof import_obsidian7.TFile) {
          await this.app.vault.process(file, () => flipped);
        }
      } catch (e) {
        console.warn(`[wiki-engine] markPageComplete failed for ${path}:`, e);
      }
    })();
  }
  setDoneCallback(cb) {
    this.onDone = cb;
  }
  setIngestionCallbacks(onStart, onEnd) {
    this.onIngestionStart = onStart;
    this.onIngestionEnd = onEnd;
  }
  setLintCallbacks(onStart, onEnd) {
    this.onLintStart = onStart;
    this.onLintEnd = onEnd;
  }
  cancelIngestion() {
    var _a;
    if (this.abortController) {
      this.abortController.abort();
      const msg = getText(this.settings.language, "ingestionCancelling");
      new import_obsidian7.Notice(msg, NOTICE_ABORT);
      (_a = this.onProgress) == null ? void 0 : _a.call(this, msg);
      console.debug("Ingestion cancellation requested");
    }
  }
  isIngesting() {
    return this.abortController !== null;
  }
  startLintOperation() {
    var _a;
    this.lintAbortController = new AbortController();
    (_a = this.onLintStart) == null ? void 0 : _a.call(this);
    return this.lintAbortController.signal;
  }
  cancelLint() {
    if (this.lintAbortController) {
      this.lintAbortController.abort();
      const msg = getText(this.settings.language, "ingestionCancelling");
      new import_obsidian7.Notice(msg, NOTICE_ABORT);
      console.debug("[lint] cancellation requested");
    }
  }
  isLintRunning() {
    return this.lintAbortController !== null;
  }
  endLintOperation() {
    var _a;
    if (this.lintAbortController === null) return;
    this.lintAbortController = null;
    (_a = this.onLintEnd) == null ? void 0 : _a.call(this);
  }
  checkCancelled() {
    var _a;
    if ((_a = this.abortController) == null ? void 0 : _a.signal.aborted) {
      throw new DOMException("Ingestion cancelled by user", "AbortError");
    }
  }
  // Proxy for lint-controller to access LintFixer methods without exposing the class
  async fixPollutedPage(oldPath, newBasename) {
    return fixPollutedPage(this.ctx, oldPath, newBasename);
  }
  /** Issue #137: get the current LLM client. All consumers (page-factory,
   * source-analyzer, conversation-ingestor, contradictions) get their client
   * via this getter, which forwards through the shared closure `() => this.llmClient`
   * that main.ts updates via `initializeLLMClient()`. */
  get client() {
    const c = this.getLLMClient();
    if (!c) throw new Error("LLM Client not initialized");
    return c;
  }
  async buildSystemPrompt(task) {
    return buildSystemPrompt(this.settings, (t) => this.schemaManager.getSchemaContext(t), task);
  }
  applySectionLabels(prompt) {
    return applySectionLabels(prompt, this.settings);
  }
  updateSettings(settings) {
    this.settings = settings;
    this.ctx.settings = settings;
  }
  /**
   * Build a shared dedup context for a folder/batch ingest run (#164). The
   * `ingested` snapshot reads content hashes from source-page frontmatter via the
   * (cached) metadata cache — no disk reads. Pass the same context to every
   * ingestSource call in the batch so within-batch duplicates are caught too.
   */
  createBatchContext() {
    return { seen: /* @__PURE__ */ new Set(), ingested: this.buildIngestedHashes() };
  }
  /**
   * Content hashes already present in the wiki, read from source-page
   * frontmatter. Cached on the same TTL as pagesCache and invalidated on every
   * file write (via invalidatePageCaches), so a fresh ingest is always seen on
   * the next call while back-to-back rejected/skip checks reuse one snapshot.
   * The returned set is read-only to callers (only `seen` is mutated per batch).
   */
  buildIngestedHashes() {
    var _a;
    const now = Date.now();
    if (this.ingestedHashesCache && now - this.ingestedHashesCacheTime < this.PAGES_CACHE_TTL_MS) {
      return this.ingestedHashesCache;
    }
    const hashes = /* @__PURE__ */ new Set();
    const prefix = (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/sources`) + "/";
    for (const f of this.app.vault.getMarkdownFiles()) {
      if (!f.path.startsWith(prefix)) continue;
      const fm = (_a = this.app.metadataCache.getFileCache(f)) == null ? void 0 : _a.frontmatter;
      if (typeof (fm == null ? void 0 : fm.contentHash) === "string" && fm.contentHash) hashes.add(fm.contentHash);
    }
    this.ingestedHashesCache = hashes;
    this.ingestedHashesCacheTime = Date.now();
    return hashes;
  }
  /** Invalidate both write-dependent caches. Called after every vault write/delete. */
  invalidatePageCaches() {
    this.pagesCache = null;
    this.ingestedHashesCache = null;
  }
  /**
   * Pre-ingest requirements gate (#164). Hard rejects: empty/whitespace/
   * frontmatter-only body, and incompatible file type. Uniqueness: content-hash
   * duplicates (within the batch and already in the wiki). Returns the first
   * failing reason, or null to proceed. On proceed, records the hash in the batch
   * so a later identical file in the same run is caught.
   */
  async checkRequirements(file, content, batch) {
    var _a;
    const contentRejection = checkContentRequirements({
      extension: file.extension,
      content,
      allowedExtensions: COMPATIBLE_SOURCE_EXTENSIONS
    });
    if (contentRejection) return contentRejection;
    const hash = hashBody(extractBody(content));
    if (batch == null ? void 0 : batch.seen.has(hash)) return { reason: "duplicate", detail: "duplicate of another file in this batch" };
    const ingested = (_a = batch == null ? void 0 : batch.ingested) != null ? _a : this.buildIngestedHashes();
    if (ingested.has(hash)) return { reason: "duplicate", detail: "content already ingested" };
    batch == null ? void 0 : batch.seen.add(hash);
    return null;
  }
  /** Map a rejection reason to its localized Notice key. */
  rejectionNoticeKey(reason) {
    if (reason === "incompatible-type") return "sourceRejectedType";
    if (reason === "duplicate") return "sourceRejectedDuplicate";
    return "sourceRejectedEmpty";
  }
  /** Log + (interactive only) notify + report a gate skip without creating any pages. */
  reportSkip(file, rejection, opts) {
    var _a;
    console.warn(`[Ingest skipped] ${file.path}: ${rejection.reason}${rejection.detail ? ` \u2014 ${rejection.detail}` : ""}`);
    if (opts == null ? void 0 : opts.interactive) {
      new import_obsidian7.Notice(
        getText(this.settings.language, this.rejectionNoticeKey(rejection.reason)).replace("{filename}", file.basename),
        NOTICE_NORMAL
      );
    }
    (_a = this.onDone) == null ? void 0 : _a.call(this, {
      sourceFile: file.path,
      createdPages: [],
      updatedPages: [],
      entitiesCreated: 0,
      conceptsCreated: 0,
      failedItems: [],
      collisions: [],
      contradictionsFound: 0,
      success: true,
      skipped: true,
      rejectedFiles: [{ path: file.path, reason: rejection.reason, detail: rejection.detail }],
      elapsedSeconds: 0
    });
  }
  async ingestSource(file, opts) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
    console.debug("=== Ingestion started ===");
    console.debug("Source file:", file.path);
    const fileContent = await this.app.vault.read(file);
    const rejection = (opts == null ? void 0 : opts.forceReingest) ? null : await this.checkRequirements(file, fileContent, opts == null ? void 0 : opts.batchCtx);
    if (rejection) {
      const confirmed = rejection.reason === "duplicate" && (opts == null ? void 0 : opts.interactive) && this.onConfirmReingest ? await this.onConfirmReingest(file, rejection) : false;
      if (!confirmed) {
        this.reportSkip(file, rejection, opts);
        return;
      }
    }
    const totalStartTime = Date.now();
    this.wasCancelled = false;
    this.abortController = new AbortController();
    (_a = this.onIngestionStart) == null ? void 0 : _a.call(this, file.basename);
    const LONG_SOURCE_LINE_THRESHOLD = 1e3;
    const lineCount = fileContent.split("\n").length;
    if (lineCount > LONG_SOURCE_LINE_THRESHOLD) {
      const sizeKB = Math.round(fileContent.length / 1024);
      new import_obsidian7.Notice(
        getText(this.settings.language, "longSourceNotice").replace("{filename}", file.basename).replace("{lines}", String(lineCount)).replace("{size}", sizeKB >= 1024 ? `${(sizeKB / 1024).toFixed(1)}MB` : `${sizeKB}KB`),
        NOTICE_NORMAL
      );
      console.debug(`[Long Source] ${file.basename}: ${lineCount} lines, ${sizeKB}KB \u2014 long ingestion expected`);
    }
    (_b = this.onProgress) == null ? void 0 : _b.call(this, `Analyzing: ${file.basename}`);
    const failedItems = [];
    const collisions = [];
    let analysis = null;
    try {
      await this.ensureWikiStructure();
      const analysisStart = Date.now();
      analysis = await this.sourceAnalyzer.analyzeSource(file);
      if (!analysis) {
        throw new Error(`Source analysis failed for "${file.basename}". Check the developer console (Ctrl+Shift+I) for network or API errors. If you see SSL/network errors, verify your provider URL and network connection.`);
      }
      const analysisTime = Date.now() - analysisStart;
      console.debug(`[Time] Source analysis phase: ${analysisTime}ms`);
      console.debug("Analysis result:", JSON.stringify(analysis, null, 2));
      this.checkCancelled();
      const totalSteps = 1 + analysis.entities.length + analysis.concepts.length + analysis.related_pages.length + 2;
      let step = 1;
      const plannedPaths = [];
      const preserveCase = this.settings.slugCase === "preserve";
      for (const entity of analysis.entities) {
        plannedPaths.push((0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/entities/${slugify(entity.name, preserveCase)}.md`));
      }
      for (const concept of analysis.concepts) {
        plannedPaths.push((0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/concepts/${slugify(concept.name, preserveCase)}.md`));
      }
      (_c = this.onProgress) == null ? void 0 : _c.call(this, `[${step}/${totalSteps}] Creating summary...`);
      await this.apiDelay();
      const sourceSlug = resolveSourceSlug(file.path, { preserveCase });
      const summaryStart = Date.now();
      const summaryPage = await this.createSummaryPage(file, analysis, plannedPaths, sourceSlug);
      const summaryTime = Date.now() - summaryStart;
      console.debug(`[Time] Summary page generation: ${summaryTime}ms`);
      analysis.created_pages.push(summaryPage);
      const pageGenStart = Date.now();
      let pageGenCount = 0;
      const concurrency = (_d = this.settings.pageGenerationConcurrency) != null ? _d : 1;
      const batchDelay = (_e = this.settings.batchDelayMs) != null ? _e : 300;
      if (concurrency > 1) {
        console.debug(`[Parallel] concurrency: ${concurrency}, batch delay: ${batchDelay}ms, total tasks: ${analysis.entities.length + analysis.concepts.length}`);
      } else {
        console.debug(`[Serial] generating pages sequentially, total tasks: ${analysis.entities.length + analysis.concepts.length}`);
      }
      const tasks = [
        ...analysis.entities.map((e, i) => ({ type: "entity", name: e.name, index: i })),
        ...analysis.concepts.map((c, i) => ({ type: "concept", name: c.name, index: i }))
      ];
      for (let i = 0; i < tasks.length; i += concurrency) {
        this.checkCancelled();
        const batch = tasks.slice(i, i + concurrency);
        const batchResults = await Promise.allSettled(
          batch.map(async (task) => {
            var _a2;
            step++;
            (_a2 = this.onProgress) == null ? void 0 : _a2.call(this, `[${step}/${totalSteps}] ${task.type === "entity" ? "Entity" : "Concept"}: ${task.name}`);
            if (task.type === "entity") {
              const entity = analysis.entities[task.index];
              try {
                const entityResult = await this.pageFactory.createOrUpdateEntityPage(entity, analysis, file, [], sourceSlug);
                if (entityResult.path) {
                  analysis.created_pages.push(entityResult.path);
                }
                if (entityResult.collision) {
                  collisions.push(entityResult.collision);
                  console.debug(`Entity "${entity.name}" \u2192 collision with ${entityResult.collision.targetType}`);
                }
                return { success: true, name: entity.name, type: "entity", collision: entityResult.collision };
              } catch (error) {
                const reason = error instanceof Error ? error.message : String(error);
                console.error(`Entity "${entity.name}" failed:`, reason);
                failedItems.push({ type: "entity", name: entity.name, reason });
                try {
                  await this.apiDelay(2e3);
                  const retryResult = await this.pageFactory.createOrUpdateEntityPage(entity, analysis, file, [], sourceSlug);
                  if (retryResult.path) {
                    analysis.created_pages.push(retryResult.path);
                    console.debug(`Entity "${entity.name}" recovered on retry`);
                    failedItems.pop();
                  }
                  if (retryResult.collision) {
                    collisions.push(retryResult.collision);
                  }
                } catch (e) {
                  console.error(`Entity "${entity.name}" retry also failed`);
                }
                return { success: false, name: entity.name, type: "entity", reason };
              }
            } else {
              const concept = analysis.concepts[task.index];
              try {
                const conceptResult = await this.pageFactory.createOrUpdateConceptPage(concept, analysis, file, [], sourceSlug);
                if (conceptResult.path) {
                  analysis.created_pages.push(conceptResult.path);
                }
                if (conceptResult.collision) {
                  collisions.push(conceptResult.collision);
                  console.debug(`Concept "${concept.name}" \u2192 collision with ${conceptResult.collision.targetType}`);
                }
                return { success: true, name: concept.name, type: "concept", collision: conceptResult.collision };
              } catch (error) {
                const reason = error instanceof Error ? error.message : String(error);
                console.error(`Concept "${concept.name}" failed:`, reason);
                failedItems.push({ type: "concept", name: concept.name, reason });
                try {
                  await this.apiDelay(2e3);
                  const retryResult = await this.pageFactory.createOrUpdateConceptPage(concept, analysis, file, [], sourceSlug);
                  if (retryResult.path) {
                    analysis.created_pages.push(retryResult.path);
                    console.debug(`Concept "${concept.name}" recovered on retry`);
                    failedItems.pop();
                  }
                  if (retryResult.collision) {
                    collisions.push(retryResult.collision);
                  }
                } catch (e) {
                  console.error(`Concept "${concept.name}" retry also failed`);
                }
                return { success: false, name: concept.name, type: "concept", reason };
              }
            }
          })
        );
        pageGenCount += batch.length;
        const batchNum = Math.floor(i / concurrency) + 1;
        const totalBatches = Math.ceil(tasks.length / concurrency);
        const succeeded = batchResults.filter((r) => r.status === "fulfilled" && r.value.success).length;
        const mode = concurrency > 1 ? "parallel" : "serial";
        const batchTime = Date.now() - pageGenStart;
        console.debug(`[${mode}batch ${batchNum}/${totalBatches}] ${succeeded}/${batch.length}  pages succeeded (concurrency: ${concurrency}, cumulative: ${batchTime}ms)`);
        if (i + concurrency < tasks.length) {
          await this.apiDelay(batchDelay);
        }
      }
      const pageGenTime = Date.now() - pageGenStart;
      console.debug(`[Time] Page generation phase complete: ${pageGenTime}ms (avg ${Math.round(pageGenTime / pageGenCount)}ms/page)`);
      const pageGenRateInfo = detectRateLimitFailures(
        failedItems.filter((f) => f.type === "entity" || f.type === "concept"),
        concurrency,
        batchDelay
      );
      if (pageGenRateInfo) {
        console.warn(`[Rate Limit] Page generation: ${pageGenRateInfo.count} item(s) failed with 429, suggested concurrency=${pageGenRateInfo.suggestedConcurrency}, delay=${pageGenRateInfo.suggestedDelay}ms`);
        new import_obsidian7.Notice(formatRateLimitNotice(pageGenRateInfo, this.settings.language), NOTICE_RATE_LIMIT);
      }
      const relatedStart = Date.now();
      const relatedConcurrency = (_f = this.settings.pageGenerationConcurrency) != null ? _f : 1;
      const relatedDelay = (_g = this.settings.batchDelayMs) != null ? _g : 300;
      const relatedTasks = analysis.related_pages.map((name, idx) => ({
        name,
        index: idx,
        stepNum: step + idx + 1
        // compute per-task step number
      }));
      let relatedCount = 0;
      const relatedTotal = relatedTasks.length;
      const relatedFailures = [];
      for (let i = 0; i < relatedTasks.length; i += relatedConcurrency) {
        this.checkCancelled();
        const batch = relatedTasks.slice(i, i + relatedConcurrency);
        const batchResults = await Promise.allSettled(
          batch.map(async (task) => {
            var _a2;
            (_a2 = this.onProgress) == null ? void 0 : _a2.call(this, `[${task.stepNum}/${totalSteps}] Updating: ${task.name}`);
            try {
              const updated2 = await this.pageFactory.updateRelatedPage(task.name, analysis, file, sourceSlug);
              return { success: updated2, name: task.name };
            } catch (error) {
              const reason = error instanceof Error ? error.message : String(error);
              console.error(`Related page "${task.name}" update failed:`, reason);
              try {
                await this.apiDelay(2e3);
                const updated2 = await this.pageFactory.updateRelatedPage(task.name, analysis, file, sourceSlug);
                console.debug(`Related page "${task.name}" recovered on retry`);
                return { success: updated2, name: task.name };
              } catch (e) {
                console.error(`Related page "${task.name}" retry also failed`);
                return { success: false, name: task.name, reason };
              }
            }
          })
        );
        batchResults.forEach((r, idx) => {
          if (r.status === "fulfilled" && r.value.success) {
            analysis.updated_pages.push(batch[idx].name);
            relatedCount++;
          } else {
            const reason = r.status === "fulfilled" ? r.value.reason || "unknown" : r.reason instanceof Error ? r.reason.message : String(r.reason || "unknown");
            const name = batch[idx].name;
            relatedFailures.push({ name, reason });
            console.error(`[Related Page] "${name}" failed: ${reason}`);
          }
        });
        if (i + relatedConcurrency < relatedTasks.length) {
          await this.apiDelay(relatedDelay);
        }
      }
      const relatedTime = Date.now() - relatedStart;
      const relatedModeLabel = relatedConcurrency > 1 ? `parallel(concurrency:${relatedConcurrency})` : "serial";
      console.debug(`[Time] Related page update phase complete: ${relatedTime}ms (${relatedModeLabel}, ${relatedCount}/${relatedTotal}  pages succeeded)`);
      step += relatedTotal;
      const relatedRateInfo = detectRateLimitFailures(
        relatedFailures,
        relatedConcurrency,
        relatedDelay
      );
      if (relatedRateInfo) {
        console.warn(`[Rate Limit] Related pages update: ${relatedRateInfo.count} item(s) failed with 429, suggested concurrency=${relatedRateInfo.suggestedConcurrency}, delay=${relatedRateInfo.suggestedDelay}ms`);
        new import_obsidian7.Notice(formatRateLimitNotice(relatedRateInfo, this.settings.language), NOTICE_RATE_LIMIT);
      }
      const contradictionStart = Date.now();
      for (const contradiction of analysis.contradictions) {
        try {
          await this.noteContradiction(contradiction);
        } catch (e) {
        }
      }
      const contradictionTime = Date.now() - contradictionStart;
      console.debug(`[Time] Contradiction recording phase: ${contradictionTime}ms (${analysis.contradictions.length} items)`);
      const indexStart = Date.now();
      step++;
      (_h = this.onProgress) == null ? void 0 : _h.call(this, `[${step}/${totalSteps}] Generating index...`);
      await this.generateIndexFromEngine();
      const totalTime = Date.now() - totalStartTime;
      const sourceSize = (_i = fileContent == null ? void 0 : fileContent.length) != null ? _i : 0;
      await this.updateLog("ingest", analysis, {
        durationSec: Math.round(totalTime / 1e3),
        model: this.settings.model,
        sourceBytes: sourceSize
      });
      const indexTime = Date.now() - indexStart;
      console.debug(`[Time] Index Index & log update: ${indexTime}ms`);
      const updated = analysis.updated_pages.length;
      const dedupedCreatedPages = dedupPages(analysis.created_pages);
      const entitiesCreated = dedupedCreatedPages.filter((p) => p.includes("/entities/")).length;
      const conceptsCreated = dedupedCreatedPages.filter((p) => p.includes("/concepts/")).length;
      const modeLabel = ((_j = this.settings.pageGenerationConcurrency) != null ? _j : 1) > 1 ? `parallel(concurrency:${this.settings.pageGenerationConcurrency})` : "serial";
      console.debug("=== Ingestion complete ===");
      console.debug(`Ingestion complete [${modeLabel}]: Created ${dedupedCreatedPages.length} pages (${entitiesCreated} entities + ${conceptsCreated} concepts), Updated ${updated} pages, ${collisions.length} cross-type collisions`);
      console.debug(`[Total time] ${totalTime}ms (${Math.round(totalTime / 1e3)}s)`);
      console.debug("[Phase breakdown]:");
      console.debug(`  - Source analysis: ${analysisTime}ms`);
      console.debug(`  - Summary page generation: ${summaryTime}ms`);
      console.debug(`  - Page gen (${concurrency}concurrency): ${pageGenTime}ms`);
      console.debug(`  - Related page update: ${relatedTime}ms`);
      console.debug(`  - Contradiction recording: ${contradictionTime}ms`);
      console.debug(`  - Index & log: ${indexTime}ms`);
      if (collisions.length > 0) {
        new import_obsidian7.Notice(getText(this.settings.language, "crossTypeCollisionNotice").replace("{count}", String(collisions.length)), NOTICE_NORMAL);
      }
      (_k = this.onDone) == null ? void 0 : _k.call(this, {
        sourceFile: file.path,
        createdPages: dedupedCreatedPages,
        updatedPages: analysis.updated_pages,
        entitiesCreated,
        conceptsCreated,
        failedItems,
        collisions,
        contradictionsFound: analysis.contradictions.length,
        success: true,
        elapsedSeconds: Math.round(totalTime / 1e3)
      });
    } catch (error) {
      const createdPages = dedupPages((analysis == null ? void 0 : analysis.created_pages) || []);
      if (error instanceof DOMException && error.name === "AbortError") {
        this.wasCancelled = true;
        console.debug("=== Ingestion cancelled by user ===");
        new import_obsidian7.Notice(getText(this.settings.language, "ingestionCancelled"), NOTICE_NORMAL);
        (_m = this.onDone) == null ? void 0 : _m.call(this, {
          sourceFile: file.path,
          createdPages,
          updatedPages: (analysis == null ? void 0 : analysis.updated_pages) || [],
          entitiesCreated: createdPages.filter((p) => p.includes("/entities/")).length,
          conceptsCreated: createdPages.filter((p) => p.includes("/concepts/")).length,
          failedItems,
          collisions,
          contradictionsFound: ((_l = analysis == null ? void 0 : analysis.contradictions) == null ? void 0 : _l.length) || 0,
          success: false,
          cancelled: true,
          errorMessage: "Cancelled by user",
          elapsedSeconds: Math.round((Date.now() - totalStartTime) / 1e3)
        });
        return;
      }
      console.error("=== Ingestion failed ===");
      console.error("Error:", error);
      const errorMsg = error instanceof Error ? error.message : String(error);
      (_o = this.onDone) == null ? void 0 : _o.call(this, {
        sourceFile: file.path,
        createdPages,
        updatedPages: (analysis == null ? void 0 : analysis.updated_pages) || [],
        entitiesCreated: createdPages.filter((p) => p.includes("/entities/")).length,
        conceptsCreated: createdPages.filter((p) => p.includes("/concepts/")).length,
        failedItems,
        collisions,
        contradictionsFound: ((_n = analysis == null ? void 0 : analysis.contradictions) == null ? void 0 : _n.length) || 0,
        success: false,
        errorMessage: errorMsg,
        elapsedSeconds: Math.round((Date.now() - totalStartTime) / 1e3)
      });
      throw error;
    } finally {
      this.abortController = null;
      (_p = this.onIngestionEnd) == null ? void 0 : _p.call(this);
    }
  }
  async apiDelay(ms) {
    await new Promise((resolve) => window.setTimeout(resolve, ms || 300));
  }
  async ensureWikiStructure() {
    const folders = [
      (0, import_obsidian7.normalizePath)(this.settings.wikiFolder),
      (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/entities`),
      (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/concepts`),
      (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/sources`)
    ];
    for (const folder of folders) {
      try {
        await this.app.vault.createFolder(folder);
        console.debug("Creating folder:", folder);
      } catch (e) {
      }
    }
    await this.schemaManager.ensureSchemaExists();
  }
  async createSummaryPage(file, analysis, plannedPaths = [], sourceSlug) {
    const preserveCase = this.settings.slugCase === "preserve";
    const slug = sourceSlug != null ? sourceSlug : slugify(file.basename, preserveCase);
    const path = (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/sources/${slug}.md`);
    const content = await this.app.vault.read(file);
    const existingSource = await this.tryReadFile(path);
    const existingFm = existingSource ? parseFrontmatter(existingSource) : null;
    const existingTags = Array.isArray(existingFm == null ? void 0 : existingFm.tags) && existingFm.tags.length > 0 ? existingFm.tags : null;
    const sourceTags = extractSourceTags(content);
    const tagsValue = existingTags ? existingTags.join(", ") : sourceTags.length > 0 ? sourceTags.join(", ") : analysis.concepts.map((c) => c.name).join(", ");
    const createdPagesList = plannedPaths.length > 0 ? plannedPaths.map((p) => {
      const relPath = p.replace(this.settings.wikiFolder + "/", "").replace(".md", "");
      const name = relPath.split("/").pop() || relPath;
      return `- [[${relPath}|${name}]]`;
    }).join("\n") : analysis.entities.map((e) => `- [[entities/${slugify(e.name, preserveCase)}|${e.name}]]`).join("\n") + "\n" + analysis.concepts.map((c) => `- [[concepts/${slugify(c.name, preserveCase)}|${c.name}]]`).join("\n");
    const prompt = PROMPTS.generateSummaryPage.replace("{{source_title}}", analysis.source_title).replace("{{content}}", content.substring(0, 500)).replace("{{analysis}}", JSON.stringify(analysis)).replace("{{created_pages_list}}", createdPagesList || "(none)").replace(/{{source_file}}/g, file.path).replace(/{{date}}/g, (/* @__PURE__ */ new Date()).toISOString().split("T")[0]).replace("{{tags}}", tagsValue).replace("{{constraints}}", UNIVERSAL_LINK_CONSTRAINTS);
    const finalPrompt = this.applySectionLabels(prompt);
    const pageContent = await this.client.createMessage({
      model: this.settings.model,
      max_tokens: TOKENS_PAGE_GENERATION,
      system: await this.buildSystemPrompt("summary"),
      messages: [{ role: "user", content: finalPrompt }],
      ...this.settings.disableThinking ? { enableThinking: false } : {}
    });
    const cleanedContent = cleanMarkdownResponse(pageContent);
    const stampedContent = upsertFrontmatterField(cleanedContent, "contentHash", hashBody(extractBody(content)));
    await this.createOrUpdateFile(path, stampedContent);
    return path;
  }
  async createOrUpdateFile(path, content) {
    var _a, _b, _c, _d, _e;
    console.debug("createOrUpdateFile:", path);
    const DISPLAY_POLLUTION_REGEX = /\[\[(entities|concepts|sources)\/([^|\]]+)\|(entities|concepts|sources)\/([^|\]]+)\]\]/g;
    if (DISPLAY_POLLUTION_REGEX.test(content)) {
      console.warn(
        `createOrUpdateFile: detected display-name pollution in ${path}, auto-correcting`
      );
      content = content.replace(
        DISPLAY_POLLUTION_REGEX,
        (_match, _folder, _path, _dupFolder, display) => {
          return `[[${_folder}/${_path}|${display}]]`;
        }
      );
    }
    const PATH_DUP_REGEX = /\[\[(entities|concepts|sources)\/\1([^\s\-_|\]]+)(\|[^\]]+)?\]\]/g;
    if (PATH_DUP_REGEX.test(content)) {
      console.warn(
        `createOrUpdateFile: detected path-prefix pollution in ${path}, auto-correcting`
      );
      content = content.replace(
        PATH_DUP_REGEX,
        (_match, folder, rest, display) => {
          const displayPart = display || "";
          return `[[${folder}/${rest}${displayPart}]]`;
        }
      );
    }
    const preserveCase = this.settings.slugCase === "preserve";
    const sourcesFix = fixPollutedSources(content, this.settings.wikiFolder, preserveCase);
    if (sourcesFix.fixed > 0) {
      console.warn(`createOrUpdateFile: normalized polluted sources field in ${path}`);
      content = sourcesFix.content;
    }
    for (let attempt = 0; attempt < 3; attempt++) {
      try {
        const file2 = this.app.vault.getAbstractFileByPath(path);
        if (file2 instanceof import_obsidian7.TFile) {
          console.debug(`Attempt ${attempt + 1}: File exists, updating:`, path);
          await this.app.vault.process(file2, () => content);
          console.debug("Update success:", path);
          this.markPageComplete(path);
          (_a = this.onFileWrite) == null ? void 0 : _a.call(this, path);
          this.invalidatePageCaches();
          return;
        }
        if (attempt === 0) {
          const resolved = this.resolveFileInVault(path);
          if (resolved instanceof import_obsidian7.TFile) {
            console.debug("createOrUpdateFile: resolved via directory scan:", path);
            await this.app.vault.process(resolved, () => content);
            console.debug("Update success (resolved path):", path);
            this.markPageComplete(path);
            (_b = this.onFileWrite) == null ? void 0 : _b.call(this, path);
            this.invalidatePageCaches();
            return;
          }
        }
        console.debug(`Attempt ${attempt + 1}: File not found, creating:`, path);
        await this.app.vault.create(path, content);
        console.debug("Create success:", path);
        this.markPageComplete(path);
        (_c = this.onFileWrite) == null ? void 0 : _c.call(this, path);
        this.invalidatePageCaches();
        return;
      } catch (error) {
        const errorMsg = error instanceof Error ? error.message : String(error);
        console.error(`Attempt ${attempt + 1} failed:`, errorMsg);
        if (errorMsg.includes("File already exists") || errorMsg.includes("already exists")) {
          let resolved = this.resolveFileInVault(path);
          if (!resolved) {
            const normalized = path.normalize();
            const allFiles = this.app.vault.getMarkdownFiles();
            resolved = allFiles.find((f) => f.path.normalize() === normalized) || null;
            if (resolved) console.debug("Retry found file via full scan:", path);
          }
          if (resolved instanceof import_obsidian7.TFile) {
            await this.app.vault.process(resolved, () => content);
            console.debug("Update succeeded after file resolution:", path);
            (_d = this.onFileWrite) == null ? void 0 : _d.call(this, path);
            this.invalidatePageCaches();
            return;
          }
          console.debug("File exists anomaly, retrying after 100ms:", path);
          await new Promise((resolve) => window.setTimeout(resolve, 100));
          continue;
        } else {
          console.error("Unhandled error:", path, error);
          throw error;
        }
      }
    }
    console.debug("3attempts exhausted, searching directory listing:", path);
    let file = this.resolveFileInVault(path);
    if (!file) {
      const normalized = path.normalize();
      const allFiles = this.app.vault.getMarkdownFiles();
      file = allFiles.find((f) => f.path.normalize() === normalized) || null;
      if (file) console.debug("createOrUpdateFile: resolved via full scan:", path);
    }
    if (file) {
      await this.app.vault.process(file, () => content);
      console.debug("Final update succeeded:", path);
      (_e = this.onFileWrite) == null ? void 0 : _e.call(this, path);
      this.invalidatePageCaches();
    } else {
      throw new Error(
        getText(this.settings.language, "fileWriteFailed").replace("{path}", path)
      );
    }
  }
  async deleteFile(path) {
    const file = this.app.vault.getAbstractFileByPath(path);
    if (file instanceof import_obsidian7.TFile) {
      await this.app.fileManager.trashFile(file);
      this.invalidatePageCaches();
      console.debug("deleteFile:", path);
    }
  }
  /** Resolve a vault path to TFile by listing parent directory children.
   *  macOS APFS stores filenames in NFD; JavaScript strings are NFC.
   *  When getAbstractFileByPath can't find a file that vault.create
   *  detected as existing, this fallback resolves the mismatch.
   *  Uses Unicode normalization so Chinese filenames compare correctly. */
  resolveFileInVault(path) {
    const lastSep = path.lastIndexOf("/");
    if (lastSep === -1) return null;
    const dirPath = path.substring(0, lastSep);
    const baseName = path.substring(lastSep + 1).normalize();
    const dir = this.app.vault.getAbstractFileByPath(dirPath);
    if (dir && dir instanceof import_obsidian7.TFolder) {
      for (const child of dir.children) {
        if (child instanceof import_obsidian7.TFile && child.name.normalize() === baseName) {
          return child;
        }
      }
    }
    return null;
  }
  async tryReadFile(path) {
    let file = null;
    try {
      const direct = this.app.vault.getAbstractFileByPath(path);
      if (direct instanceof import_obsidian7.TFile) file = direct;
    } catch (e) {
    }
    if (!file) {
      file = this.resolveFileInVault(path);
    }
    if (!file) {
      const normalized = path.normalize();
      const allFiles = this.app.vault.getMarkdownFiles();
      const matched = allFiles.find((f) => f.path.normalize() === normalized);
      if (matched) {
        console.debug("tryReadFile: resolved via full scan:", path);
        file = matched;
      }
    }
    if (!file) {
      console.debug("tryReadFile: all lookups failed for:", path);
      return null;
    }
    return await this.app.vault.read(file);
  }
  async regenerateDefaultSchema() {
    await this.schemaManager.regenerateDefaultSchema();
  }
  // ---- Lint-fix delegation ----
  getExistingWikiPages() {
    const now = Date.now();
    if (this.pagesCache && now - this.pagesCacheTime < this.PAGES_CACHE_TTL_MS) {
      return Promise.resolve(this.pagesCache);
    }
    return getExistingWikiPages(this.app, this.settings.wikiFolder).then((data) => {
      this.pagesCache = data;
      this.pagesCacheTime = Date.now();
      return data;
    });
  }
  async fixDeadLink(sourcePath, targetName) {
    return fixDeadLink(this.ctx, sourcePath, targetName);
  }
  async fillEmptyPage(pagePath, existingContent) {
    return fillEmptyPage(this.ctx, pagePath, existingContent);
  }
  // Issue #103: delete empty stubs without running full lint pipeline
  async deleteEmptyStubs(wikiFolder) {
    return deleteEmptyStubs(this.ctx, wikiFolder);
  }
  async linkOrphanPage(orphanPath) {
    return linkOrphanPage(this.ctx, orphanPath);
  }
  // ---- Contradiction delegation ----
  async noteContradiction(contradiction) {
    return this.contradictionManager.noteContradiction(contradiction);
  }
  async getOpenContradictions() {
    return this.contradictionManager.getOpenContradictions();
  }
  async updateContradictionStatus(filePath, newStatus) {
    return this.contradictionManager.updateContradictionStatus(filePath, newStatus);
  }
  async resolveContradiction(contradictionPath) {
    return this.contradictionManager.resolveContradiction(contradictionPath);
  }
  // ---- Conversation ingestion delegation ----
  async ingestConversation(history) {
    return this.conversationIngestor.ingestConversation(history);
  }
  formatConversation(history) {
    return formatConversation(history);
  }
  // ---- Index generation ----
  async generateIndexFromEngine() {
    await this.ensureWikiStructure();
    const entities = this.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(`${this.settings.wikiFolder}/entities/`));
    const concepts = this.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(`${this.settings.wikiFolder}/concepts/`));
    const sources = this.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(`${this.settings.wikiFolder}/sources/`));
    const totalPages = entities.length + concepts.length + sources.length;
    if (totalPages === 0) {
      const indexPath = (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/index.md`);
      await this.createOrUpdateFile(indexPath, `# Wiki Index

> No pages yet. Ingest sources to populate the Wiki.
`);
      return;
    }
    await this.generateFlatIndex(entities, concepts, sources);
  }
  async generateFlatIndex(entities, concepts, sources) {
    const lang = this.settings.wikiLanguage || "en";
    const langKey = lang in TEXTS.en.indexLabels ? lang : "en";
    const labels = TEXTS.en.indexLabels[langKey];
    let indexContent = `# Wiki Index

`;
    indexContent += `> ${labels.subtitle}

`;
    indexContent += `> Note: Text in backticks after page names shows aliases \u2014 alternative names, abbreviations, or translations.

`;
    indexContent += `## ${labels.entities}

`;
    for (const file of entities) {
      const summary = await this.getPageSummary(file);
      const aliases = await this.getPageAliases(file);
      const aliasStr = aliases.length > 0 ? ` \`aliases: ${aliases.join(", ")}\`` : "";
      indexContent += `- [[entities/${file.basename}|${file.basename}]]${aliasStr} - ${summary}
`;
    }
    indexContent += `
## ${labels.concepts}

`;
    for (const file of concepts) {
      const summary = await this.getPageSummary(file);
      const aliases = await this.getPageAliases(file);
      const aliasStr = aliases.length > 0 ? ` \`aliases: ${aliases.join(", ")}\`` : "";
      indexContent += `- [[concepts/${file.basename}|${file.basename}]]${aliasStr} - ${summary}
`;
    }
    indexContent += `
## ${labels.sources}

`;
    for (const file of sources) {
      const aliases = await this.getPageAliases(file);
      const aliasStr = aliases.length > 0 ? ` \`aliases: ${aliases.join(", ")}\`` : "";
      indexContent += `- [[sources/${file.basename}|${file.basename}]]${aliasStr}
`;
    }
    const indexPath = (0, import_obsidian7.normalizePath)(`${this.settings.wikiFolder}/index.md`);
    await this.createOrUpdateFile(indexPath, indexContent);
  }
  async getPageSummary(file) {
    var _a;
    const content = await this.app.vault.read(file);
    const lines = content.split("\n").filter((l) => l.trim() && !l.startsWith("#") && !l.startsWith("---"));
    return ((_a = lines[0]) == null ? void 0 : _a.substring(0, 100)) || "No summary";
  }
  async getPageAliases(file) {
    const content = await this.app.vault.read(file);
    const fm = parseFrontmatter(content);
    if ((fm == null ? void 0 : fm.aliases) && Array.isArray(fm.aliases) && fm.aliases.length > 0) {
      return fm.aliases.filter((a) => typeof a === "string" && a.trim().length > 0).map((a) => a.trim());
    }
    return [];
  }
  async updateLog(operation, analysis, metrics) {
    const logPath = `${this.settings.wikiFolder}/log.md`;
    const now = /* @__PURE__ */ new Date();
    const date = now.toISOString().split("T")[0];
    const time = now.toTimeString().slice(0, 5);
    const lang = this.settings.wikiLanguage || "en";
    const langKey = lang in TEXTS.en.logLabels ? lang : "en";
    const labels = TEXTS.en.logLabels[langKey];
    const h2Suffix = metrics ? this.formatIngestMetricsSuffix(metrics) : "";
    let entry = `

## [${date} ${time}] ${operation} | ${analysis.source_title}${h2Suffix}

`;
    entry += `**${labels.createdPages}**\uFF1A${dedupPages(analysis.created_pages).map((p) => `[[${p.replace(this.settings.wikiFolder + "/", "")}]]`).join(", ")}

`;
    entry += `**${labels.updatedPages}**\uFF1A${analysis.updated_pages.map((p) => `[[${p}]]`).join(", ")}

`;
    if (analysis.contradictions.length > 0) {
      entry += `**${labels.contradictionsFound}**\uFF1A
`;
      for (const c of analysis.contradictions) {
        entry += `- ${c.claim} vs ${c.source_page}
`;
      }
    }
    const existingLog = await this.tryReadFile(logPath) || `# Wiki ${lang === "zh" ? "\u64CD\u4F5C\u65E5\u5FD7" : "Operation Log"}

`;
    await this.createOrUpdateFile(logPath, existingLog + entry);
  }
  /** Format an ingest metrics suffix for the H2 line: ` · 28s · claude-sonnet-4 · 4.2KB`. */
  formatIngestMetricsSuffix(m) {
    const parts = [];
    if (typeof m.durationSec === "number" && m.durationSec > 0) {
      parts.push(`${m.durationSec}s`);
    }
    if (m.model) {
      parts.push(m.model.replace(/-\d{8}$/, ""));
    }
    if (typeof m.sourceBytes === "number" && m.sourceBytes > 0) {
      parts.push(this.formatBytes(m.sourceBytes));
    }
    return parts.length > 0 ? ` \xB7 ${parts.join(" \xB7 ")}` : "";
  }
  /** Render byte count with KB / MB units. */
  formatBytes(n) {
    if (n >= 1024 * 1024) return `${(n / 1024 / 1024).toFixed(1)}MB`;
    if (n >= 1024) return `${(n / 1024).toFixed(1)}KB`;
    return `${n}B`;
  }
  /** Merge a duplicate source page into a target page. */
  async mergeDuplicatePages(targetPath, sourcePath) {
    return mergeDuplicatePages(this.ctx, targetPath, sourcePath);
  }
  /** Append a lint-fix entry to the operation log. */
  async logLintFix(operation, details) {
    const logPath = `${this.settings.wikiFolder}/log.md`;
    const now = /* @__PURE__ */ new Date();
    const date = now.toISOString().split("T")[0];
    const time = now.toTimeString().slice(0, 5);
    const lang = this.settings.wikiLanguage || "en";
    const entry = `

## [${date} ${time}] ${operation}

${details}
`;
    try {
      let existingLog = await this.tryReadFile(logPath);
      if (!existingLog) {
        existingLog = `# Wiki ${lang === "zh" ? "\u64CD\u4F5C\u65E5\u5FD7" : "Operation Log"}

`;
      }
      const MAX_LOG_BYTES = 512 * 1024;
      const projectedSize = (existingLog.length + entry.length) * 2;
      if (projectedSize > MAX_LOG_BYTES) {
        const headerEnd = existingLog.indexOf("\n\n");
        const header = headerEnd > 0 ? existingLog.substring(0, headerEnd + 2) : "# Wiki Operation Log\n\n";
        const keepBytes = MAX_LOG_BYTES / 2;
        const trimmed = existingLog.substring(existingLog.length - keepBytes);
        const h2Idx = trimmed.indexOf("\n## ");
        existingLog = header + (h2Idx > 0 ? trimmed.substring(h2Idx + 1) : trimmed);
        console.warn(`[logLintFix] ${logPath} exceeded ${MAX_LOG_BYTES} bytes; trimmed oldest entries`);
      }
      await this.createOrUpdateFile(logPath, existingLog + entry);
    } catch (e) {
      console.error(`[logLintFix] failed to write ${logPath}:`, e);
      throw e;
    }
  }
};

// src/wiki/query-engine.ts
var import_obsidian8 = require("obsidian");
function renderThinkingBlocksUI(thinkingBlocks, language) {
  var _a, _b, _c;
  if (!thinkingBlocks || thinkingBlocks.length === 0) return null;
  const langTexts = (_a = TEXTS[language]) != null ? _a : TEXTS.en;
  const summaryLabel = (_b = langTexts == null ? void 0 : langTexts.queryThinkingSummary) != null ? _b : language === "zh" || language === "ja" || language === "ko" ? "\u601D\u8003\u8FC7\u7A0B" : "Thinking process";
  const stepsLabel = (_c = langTexts == null ? void 0 : langTexts.queryThinkingSteps) != null ? _c : language === "zh" ? "\u6B65" : language === "ja" ? "\u30B9\u30C6\u30C3\u30D7" : "steps";
  const doc2 = activeDocument;
  if (!doc2) return null;
  const details = doc2.createElement("details");
  details.className = "llm-wiki-query-thinking-block";
  const summary = doc2.createElement("summary");
  const count = thinkingBlocks.length;
  summary.textContent = count > 1 ? `\u{1F4AD} ${summaryLabel} (${count} ${stepsLabel})` : `\u{1F4AD} ${summaryLabel}`;
  details.appendChild(summary);
  for (const block of thinkingBlocks) {
    const pre = doc2.createElement("pre");
    pre.className = "llm-wiki-query-thinking-content";
    pre.textContent = block;
    details.appendChild(pre);
  }
  return details;
}
var SuggestSaveModal = class extends import_obsidian8.Modal {
  constructor(app, plugin, history, reason) {
    super(app);
    this.plugin = plugin;
    this.history = history;
    this.reason = reason || "";
  }
  onOpen() {
    const { contentEl } = this;
    const texts = TEXTS[this.plugin.settings.language];
    contentEl.addClass("llm-wiki-suggest-save-modal");
    contentEl.createEl("h3", { text: texts.querySuggestSaveTitle });
    contentEl.createEl("p", { text: texts.querySuggestSaveDesc });
    if (this.reason) {
      const reasonBox = contentEl.createDiv({ cls: "llm-wiki-suggest-save-reason" });
      reasonBox.createEl("strong", { text: "Reason: " });
      reasonBox.createSpan({ text: this.reason });
    }
    const buttonRow = contentEl.createDiv({ cls: "llm-wiki-suggest-save-buttons" });
    buttonRow.createEl("button", { text: texts.querySuggestSaveYes, cls: "mod-cta" }).addEventListener("click", () => {
      this.close();
      void this.doSave();
    });
    buttonRow.createEl("button", { text: texts.querySuggestSaveNo }).addEventListener("click", () => {
      this.close();
    });
  }
  async doSave() {
    const texts = TEXTS[this.plugin.settings.language];
    const progressNotice = new import_obsidian8.Notice(texts.savingToWiki, 0);
    const origProgress = this.plugin.wikiEngine.getProgressCallback();
    this.plugin.wikiEngine.setProgressCallback((msg) => {
      progressNotice.setMessage(msg);
    });
    try {
      const report = await this.plugin.wikiEngine.ingestConversation(this.history);
      this.plugin.settings.lastOfferedQueryHash = JSON.stringify(this.history.messages);
      void this.plugin.saveSettings();
      const summary = texts.saveSummary.replace("{entities}", String(report.entitiesCreated)).replace("{concepts}", String(report.conceptsCreated)).replace("{pages}", String(report.createdPages.length));
      new import_obsidian8.Notice(`${texts.saveToWikiSuccess}
${summary}`, NOTICE_NORMAL);
    } catch (error) {
      console.error("Save failed:", error);
      const errorMsg = error instanceof Error ? error.message : String(error);
      new import_obsidian8.Notice(texts.queryModalErrorPrefix + errorMsg, NOTICE_ERROR);
    } finally {
      progressNotice.hide();
      this.plugin.wikiEngine.setProgressCallback(origProgress);
    }
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};
var VIEW_TYPE_QUERY = "llm-wiki-query-view";
var QueryView = class extends import_obsidian8.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.plugin = plugin;
    this.history = {
      messages: plugin.settings.queryHistory || []
    };
    this.isStreaming = false;
    this.aborted = false;
    this.accumulatedResponse = "";
    this.currentResponseDiv = null;
    this.historyContainer = null;
    this.inputArea = null;
    this.sendBtn = null;
    this.historyCountDisplay = null;
    this.activeRenderComponent = new import_obsidian8.Component();
    this.pendingInput = "";
  }
  getViewType() {
    return VIEW_TYPE_QUERY;
  }
  getDisplayText() {
    return TEXTS[this.plugin.settings.language].queryModalTitle;
  }
  getIcon() {
    return "message-circle";
  }
  async onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    const texts = TEXTS[this.plugin.settings.language];
    contentEl.addClass("llm-wiki-query-view");
    contentEl.addClass("llm-wiki-query-content");
    const container = contentEl.createDiv({
      cls: "llm-wiki-query-container"
    });
    const header = container.createDiv({
      cls: "llm-wiki-query-header"
    });
    header.setText(texts.queryModalTitle);
    const hintText = container.createDiv({
      cls: "llm-wiki-query-hint"
    });
    hintText.setText(texts.queryModalHint);
    this.historyContainer = container.createDiv({
      cls: "llm-wiki-query-history"
    });
    this.history.messages.forEach((msg) => {
      this.renderHistoryMessage(msg.role, msg.content);
    });
    this.scrollToBottom();
    const inputContainer = container.createDiv({
      cls: "llm-wiki-query-input-container"
    });
    this.inputArea = inputContainer.createEl("textarea", {
      attr: {
        placeholder: texts.queryModalPlaceholder,
        rows: "3"
      },
      cls: "llm-wiki-query-textarea"
    });
    const modKey = import_obsidian8.Platform.isMacOS ? "Cmd" : "Ctrl";
    this.inputArea.addEventListener("keydown", (evt) => {
      if (evt.key === "Enter" && (evt.metaKey || evt.ctrlKey)) {
        evt.preventDefault();
        if (this.isStreaming) {
          this.stopGeneration();
        } else if (this.inputArea.value.trim()) {
          void this.sendMessage(this.inputArea.value);
        }
      }
    });
    const buttonRow = inputContainer.createDiv({
      cls: "llm-wiki-query-button-row"
    });
    this.sendBtn = buttonRow.createEl("button", {
      text: `${texts.queryModalSendButton} (${modKey}+Enter)`,
      cls: "llm-wiki-query-send-btn"
    });
    this.sendBtn.addEventListener("click", () => {
      if (this.isStreaming) {
        this.stopGeneration();
      } else if (this.inputArea.value.trim()) {
        void this.sendMessage(this.inputArea.value);
      }
    });
    buttonRow.createEl("button", {
      text: texts.queryModalSaveButton,
      cls: "llm-wiki-query-save-btn"
    }).addEventListener("click", () => {
      if (this.history.messages.length > 0) {
        void this.saveToWiki();
      }
    });
    buttonRow.createEl("button", {
      text: texts.queryModalClearButton,
      cls: "llm-wiki-query-clear-btn"
    }).addEventListener("click", () => {
      this.clearHistory();
    });
    this.historyCountDisplay = inputContainer.createDiv({
      cls: "llm-wiki-query-count"
    });
    const currentRounds = Math.floor(this.history.messages.length / 2);
    const maxRounds = this.plugin.settings.maxConversationHistory;
    this.historyCountDisplay.setText(
      texts.queryModalHistoryCount.replace("{}", currentRounds.toString()).replace("{}", maxRounds.toString())
    );
  }
  async onClose() {
    const { contentEl } = this;
    if (this.activeRenderComponent) {
      this.activeRenderComponent.unload();
      this.activeRenderComponent = null;
    }
    this.plugin.settings.queryHistory = this.history.messages;
    void this.plugin.saveSettings();
    contentEl.empty();
  }
  evaluateAndSuggestSave() {
    const assistantMessages = this.history.messages.filter((m) => m.role === "assistant");
    if (assistantMessages.length < 1) return;
    const hash = this.computeConversationHash();
    if (hash === this.plugin.settings.lastOfferedQueryHash) return;
    void this.evaluateWithLLM();
  }
  computeConversationHash() {
    return JSON.stringify(this.history.messages);
  }
  async evaluateWithLLM() {
    if (!this.plugin.llmClient) return;
    try {
      const conversationText = this.plugin.wikiEngine.formatConversation(this.history);
      const prompt = PROMPTS.evaluateConversationValue.replace("{{conversation}}", conversationText.substring(0, 3e3));
      const response = await this.plugin.llmClient.createMessage({
        model: this.plugin.settings.model,
        max_tokens: TOKENS_QUERY_SAVE_DEDUP,
        messages: [{ role: "user", content: prompt }],
        response_format: { type: "json_object" },
        ...this.plugin.settings.disableThinking ? { enableThinking: false } : {}
      });
      const parsed = await parseJsonResponse(response);
      if (parsed == null ? void 0 : parsed.valuable) {
        this.plugin.settings.lastOfferedQueryHash = this.computeConversationHash();
        void this.plugin.saveSettings();
        new SuggestSaveModal(this.app, this.plugin, this.history, parsed.reason || "").open();
      }
    } catch (e) {
    }
  }
  async sendMessage(userMessage) {
    var _a, _b;
    if (!userMessage.trim() || this.isStreaming) return;
    const texts = TEXTS[this.plugin.settings.language];
    this.history.messages.push({
      role: "user",
      content: userMessage,
      timestamp: Date.now()
    });
    this.renderHistoryMessage("user", userMessage);
    this.scrollToBottom();
    this.limitHistory();
    this.isStreaming = true;
    this.aborted = false;
    this.pendingInput = "";
    this.inputArea.value = "";
    this.sendBtn.setText(texts.queryModalStopButton);
    this.sendBtn.className = "llm-wiki-query-stop-btn";
    const messageDiv = this.historyContainer.createDiv({
      cls: "llm-wiki-query-message-wrapper llm-wiki-query-message-assistant llm-wiki-query-response-live"
    });
    messageDiv.createDiv({
      cls: "llm-wiki-query-message-label",
      text: "\u{1F916} Wiki"
    });
    const contentDiv = messageDiv.createDiv({
      cls: "llm-wiki-query-message-body markdown-reading-view"
    });
    const statusIndicator = messageDiv.createDiv({
      cls: "llm-wiki-query-status",
      text: texts.queryPhaseSearching
    });
    this.accumulatedResponse = "";
    let foundPagesInfo = "";
    const foundPrefix = texts.queryPhaseFoundPages.split("{count}")[0];
    const wikiContext = await this.buildWikiContext(userMessage, (phase) => {
      if (phase.startsWith(foundPrefix)) {
        foundPagesInfo = phase;
      }
      statusIndicator.setText(phase);
    });
    if (this.aborted) {
      statusIndicator.remove();
      this.finishGeneration(texts);
      return;
    }
    const conversationMessages = this.history.messages.map((msg) => ({
      role: msg.role,
      content: msg.content
    }));
    const startTime = Date.now();
    let statusTemplate = foundPagesInfo ? `${foundPagesInfo}, ${texts.queryPhaseGenerating}` : texts.queryPhaseGenerating;
    const timerInterval = window.setInterval(() => {
      const elapsed = Math.floor((Date.now() - startTime) / 1e3);
      statusIndicator.setText(statusTemplate.replace("{time}", elapsed.toString()));
    }, 1e3);
    const cleanupTimer = () => {
      window.clearInterval(timerInterval);
      statusIndicator.remove();
    };
    try {
      if ((_a = this.plugin.llmClient) == null ? void 0 : _a.createMessageStream) {
        let fullResponse = "";
        try {
          fullResponse = await this.plugin.llmClient.createMessageStream({
            model: this.plugin.settings.model,
            max_tokens: TOKENS_QUERY_LLM_SELECT,
            system: wikiContext,
            messages: conversationMessages,
            onChunk: (chunk) => {
              if (this.aborted) return;
              this.accumulatedResponse += chunk;
              this.renderMarkdownContent(this.accumulatedResponse, contentDiv);
              this.scrollToBottom();
            },
            ...this.plugin.settings.disableThinking ? { enableThinking: false } : {},
            ...this.plugin.settings.chatTemperature !== void 0 ? { temperature: this.plugin.settings.chatTemperature } : {}
          });
          cleanupTimer();
        } catch (streamErr) {
          if (this.aborted) {
            cleanupTimer();
            this.finishGeneration(texts);
            return;
          }
          console.warn("Streaming query failed, falling back to non-streaming:", streamErr);
          statusTemplate = foundPagesInfo ? `${foundPagesInfo}, ${texts.queryPhaseNonStreaming}` : texts.queryPhaseNonStreaming;
          if ((_b = this.plugin.llmClient) == null ? void 0 : _b.createMessage) {
            try {
              fullResponse = await this.plugin.llmClient.createMessage({
                model: this.plugin.settings.model,
                max_tokens: TOKENS_QUERY_LLM_SELECT,
                system: wikiContext,
                messages: conversationMessages,
                ...this.plugin.settings.disableThinking ? { enableThinking: false } : {},
                ...this.plugin.settings.chatTemperature !== void 0 ? { temperature: this.plugin.settings.chatTemperature } : {}
              });
              if (this.aborted) {
                cleanupTimer();
                this.finishGeneration(texts);
                return;
              }
              this.renderMarkdownContent(fullResponse, contentDiv);
              this.scrollToBottom();
            } catch (fallbackErr) {
              console.error("Non-streaming fallback also failed:", fallbackErr);
              const errMsg2 = fallbackErr instanceof Error ? fallbackErr.message : String(fallbackErr);
              contentDiv.createEl("p", {
                text: texts.queryModalErrorPrefix + errMsg2,
                cls: "llm-wiki-query-error"
              });
              cleanupTimer();
              this.finishGeneration(texts);
              return;
            }
          } else {
            const errMsg2 = streamErr instanceof Error ? streamErr.message : String(streamErr);
            contentDiv.createEl("p", {
              text: texts.queryModalErrorPrefix + errMsg2,
              cls: "llm-wiki-query-error"
            });
            cleanupTimer();
            this.finishGeneration(texts);
            return;
          }
          cleanupTimer();
        }
        if (fullResponse !== void 0 && fullResponse !== null) {
          this.renderMarkdownContent(fullResponse, contentDiv);
          this.scrollToBottom();
        }
        if (!this.aborted) {
          this.history.messages.push({
            role: "assistant",
            content: fullResponse,
            timestamp: Date.now()
          });
        }
      } else {
        statusTemplate = foundPagesInfo ? `${foundPagesInfo}, ${texts.queryPhaseNonStreaming}` : texts.queryPhaseNonStreaming;
        const response = await this.plugin.llmClient.createMessage({
          model: this.plugin.settings.model,
          max_tokens: TOKENS_QUERY_LLM_SELECT,
          system: wikiContext,
          messages: conversationMessages,
          ...this.plugin.settings.disableThinking ? { enableThinking: false } : {},
          ...this.plugin.settings.chatTemperature !== void 0 ? { temperature: this.plugin.settings.chatTemperature } : {}
        });
        if (this.aborted) {
          cleanupTimer();
          this.finishGeneration(texts);
          return;
        }
        this.history.messages.push({
          role: "assistant",
          content: response,
          timestamp: Date.now()
        });
        this.renderMarkdownContent(response, contentDiv);
        this.scrollToBottom();
        cleanupTimer();
      }
    } catch (error) {
      console.error("Query failed:", error);
      const errorMsg = error instanceof Error ? error.message : String(error);
      contentDiv.createEl("p", {
        text: texts.queryModalErrorPrefix + errorMsg,
        cls: "llm-wiki-query-error"
      });
      cleanupTimer();
    }
    const lastMsg = this.history.messages[this.history.messages.length - 1];
    if (lastMsg && lastMsg.role === "assistant" && messageDiv) {
      contentDiv.setAttribute("data-raw-content", lastMsg.content);
      this.addCopyButton(messageDiv, contentDiv);
      messageDiv.removeClass("llm-wiki-query-response-live");
    }
    this.finishGeneration(texts);
  }
  stopGeneration() {
    this.aborted = true;
    this.pendingInput = this.inputArea.value;
  }
  finishGeneration(texts) {
    this.isStreaming = false;
    this.currentResponseDiv = null;
    this.sendBtn.setText(`${texts.queryModalSendButton} (${import_obsidian8.Platform.isMacOS ? "Cmd" : "Ctrl"}+Enter)`);
    this.sendBtn.className = "llm-wiki-query-send-btn";
    if (this.pendingInput) {
      this.inputArea.value = this.pendingInput;
      this.pendingInput = "";
    }
    const currentRounds = Math.floor(this.history.messages.length / 2);
    const maxRounds = this.plugin.settings.maxConversationHistory;
    this.historyCountDisplay.setText(
      texts.queryModalHistoryCount.replace("{}", currentRounds.toString()).replace("{}", maxRounds.toString())
    );
  }
  renderMarkdownContent(content, container) {
    container.empty();
    const lower = content.toLowerCase();
    const hasThinkTags = lower.includes("<think");
    const { thinkingBlocks, visibleContent } = hasThinkTags ? extractThinkingBlocks(content) : { thinkingBlocks: [], visibleContent: content };
    const normalizedContent = normalizeWikiLinkContent(
      visibleContent,
      this.plugin.settings.wikiFolder
    );
    if (thinkingBlocks.length > 0) {
      const thinkingEl = renderThinkingBlocksUI(
        thinkingBlocks,
        this.plugin.settings.language
      );
      if (thinkingEl) container.appendChild(thinkingEl);
    }
    if (this.activeRenderComponent) {
      this.activeRenderComponent.unload();
      this.activeRenderComponent = null;
    }
    this.activeRenderComponent = new import_obsidian8.Component();
    this.activeRenderComponent.load();
    const sourcePath = this.plugin.settings.wikiFolder;
    void import_obsidian8.MarkdownRenderer.render(
      this.app,
      normalizedContent,
      container,
      sourcePath,
      this.activeRenderComponent
    );
    container.querySelectorAll(".internal-link").forEach((link) => {
      const el = link;
      const href = el.getAttribute("data-href") || el.getAttribute("href");
      if (!href) return;
      el.addEventListener("click", (evt) => {
        evt.preventDefault();
        evt.stopPropagation();
        void this.app.workspace.openLinkText(href, sourcePath);
      });
    });
  }
  scrollToBottom() {
    this.historyContainer.scrollTop = this.historyContainer.scrollHeight;
  }
  renderHistoryMessage(role, content) {
    const messageDiv = this.historyContainer.createDiv({
      cls: ["llm-wiki-query-message-wrapper", role === "user" ? "llm-wiki-query-message-user" : "llm-wiki-query-message-assistant"]
    });
    messageDiv.createDiv({
      cls: "llm-wiki-query-message-label",
      text: role === "user" ? "\u{1F464} You" : "\u{1F916} Wiki"
    });
    const bodyDiv = messageDiv.createDiv({
      cls: role === "user" ? "llm-wiki-query-message-body" : "llm-wiki-query-message-body markdown-reading-view"
    });
    if (role === "assistant") {
      bodyDiv.setAttribute("data-raw-content", content);
      this.renderMarkdownContent(content, bodyDiv);
      this.addCopyButton(messageDiv, bodyDiv);
    } else {
      bodyDiv.setText(content);
    }
  }
  addCopyButton(messageWrapper, bodyDiv) {
    const copyBtn = messageWrapper.createDiv({ cls: "llm-wiki-query-copy-btn" });
    copyBtn.setText("Copy");
    copyBtn.addEventListener("click", (evt) => {
      evt.stopPropagation();
      const raw = bodyDiv.getAttribute("data-raw-content") || "";
      navigator.clipboard.writeText(raw).then(() => {
        copyBtn.setText("Copied!");
        window.setTimeout(() => copyBtn.setText("Copy"), 1500);
      }).catch(() => {
        copyBtn.setText("Failed");
        window.setTimeout(() => copyBtn.setText("Copy"), 1500);
      });
    });
  }
  limitHistory() {
    const max = this.plugin.settings.maxConversationHistory;
    const totalMessages = this.history.messages.length;
    if (totalMessages > max * 2) {
      const keepCount = max * 2;
      this.history.messages = this.history.messages.slice(-keepCount);
      const texts = TEXTS[this.plugin.settings.language];
      new import_obsidian8.Notice(
        texts.historyTruncated.replace("{max}", String(max)),
        3e3
      );
      this.historyContainer.empty();
      this.history.messages.forEach((msg) => {
        this.renderHistoryMessage(msg.role, msg.content);
      });
    }
  }
  async saveToWiki() {
    if (this.history.messages.length === 0) return;
    const texts = TEXTS[this.plugin.settings.language];
    const progressNotice = new import_obsidian8.Notice(texts.savingToWiki, 0);
    const origProgress = this.plugin.wikiEngine.getProgressCallback();
    this.plugin.wikiEngine.setProgressCallback((msg) => {
      progressNotice.setMessage(msg);
    });
    try {
      const report = await this.plugin.wikiEngine.ingestConversation(this.history);
      this.plugin.settings.lastOfferedQueryHash = this.computeConversationHash();
      void this.plugin.saveSettings();
      const lang = this.plugin.settings.language;
      const summary = lang === "en" ? `${report.entitiesCreated} entities, ${report.conceptsCreated} concepts, ${report.createdPages.length} pages` : `${report.entitiesCreated} \u5B9E\u4F53, ${report.conceptsCreated} \u6982\u5FF5, ${report.createdPages.length} \u9875`;
      new import_obsidian8.Notice(`${texts.saveToWikiSuccess}
${summary}`, NOTICE_NORMAL);
    } catch (error) {
      console.error("Save failed:", error);
      const errorMsg = error instanceof Error ? error.message : String(error);
      const texts2 = TEXTS[this.plugin.settings.language];
      new import_obsidian8.Notice(texts2.queryModalErrorPrefix + errorMsg, NOTICE_ERROR);
    } finally {
      progressNotice.hide();
      this.plugin.wikiEngine.setProgressCallback(origProgress);
    }
  }
  clearHistory() {
    this.history.messages = [];
    this.historyContainer.empty();
    this.plugin.settings.queryHistory = [];
    void this.plugin.saveSettings();
    const texts = TEXTS[this.plugin.settings.language];
    new import_obsidian8.Notice(texts.historyCleared, NOTICE_BRIEF);
    const maxRounds = this.plugin.settings.maxConversationHistory;
    this.historyCountDisplay.setText(
      texts.queryModalHistoryCount.replace("{}", "0").replace("{}", maxRounds.toString())
    );
  }
  async buildWikiContext(userMessage, onProgress) {
    console.debug("=== buildWikiContext started ===");
    console.debug("User question:", userMessage);
    const texts = TEXTS[this.plugin.settings.language];
    try {
      const indexPath = `${this.plugin.settings.wikiFolder}/index.md`;
      console.debug("[Step 1] Index path:", indexPath);
      const indexContent = await this.plugin.wikiEngine.tryReadFile(indexPath);
      console.debug("[Step 1] Index content:", indexContent ? "loaded" : "not found");
      if (!indexContent) {
        console.debug("[Step 1] Wiki is empty, returning hint");
        const lang2 = this.plugin.settings.wikiLanguage || "en";
        const langName2 = WIKI_LANGUAGES[lang2] || lang2;
        return `IMPORTANT: You MUST write ALL responses in ${langName2}.

You are a Wiki assistant. The Wiki is empty. Please answer based on your knowledge and suggest the user ingest sources first.`;
      }
      onProgress == null ? void 0 : onProgress(texts.queryPhaseSearching);
      let relevantPages;
      const allPages = parseIndexForPages(indexContent);
      const localMatches = localKeywordMatch(userMessage, allPages);
      const highConfidence = localMatches.filter((p) => p.score >= 6);
      if (highConfidence.length >= 3) {
        console.debug("[Step 2] Local keyword high-confidence match, skipping LLM selection:", highConfidence.length, "pages");
        relevantPages = highConfidence.slice(0, 5).map((p) => p.path);
      } else if (localMatches.length > 0) {
        console.debug("[Step 2] Local keyword medium match, LLM refining from", localMatches.length, "candidates");
        const candidateList = localMatches.slice(0, 15).map(
          (p) => `- ${p.path} \`aliases: ${p.aliases.join(", ")}\``
        ).join("\n");
        relevantPages = await this.selectRelevantPagesWithLLM(userMessage, candidateList);
      } else {
        console.debug("[Step 2] No local keyword match, full LLM selection");
        relevantPages = await this.selectRelevantPagesWithLLM(userMessage, indexContent);
      }
      const pageNames = relevantPages.map((p) => p.split("/").pop() || p).join(", ");
      const foundText = texts.queryPhaseFoundPages.replace("{count}", relevantPages.length.toString()).replace("{pages}", pageNames);
      onProgress == null ? void 0 : onProgress(foundText);
      onProgress == null ? void 0 : onProgress(texts.queryPhaseLoadingPages);
      console.debug("[Step 3] Loading page content...");
      const pagesContent = await this.loadRelevantPages(relevantPages);
      console.debug("[Step 3] Pages loaded:", pagesContent.length);
      pagesContent.forEach((content, i) => {
        console.debug(`[Step 3] page content length:`, content.length);
      });
      onProgress == null ? void 0 : onProgress(texts.queryPhaseContextReady);
      const lang = this.plugin.settings.wikiLanguage || "en";
      const langName = WIKI_LANGUAGES[lang] || lang;
      const langDirective = `IMPORTANT: You MUST write ALL responses in ${langName}. Every answer, explanation, and label must be in ${langName}.`;
      const wikiContext = `${langDirective}

You are a Wiki assistant with access to a structured knowledge base.

Wiki Index:
${indexContent}

Relevant Wiki Pages (loaded with full content):
${pagesContent.length > 0 ? pagesContent.join("\n\n---\n\n") : "No directly relevant pages found in Wiki."}

Instructions:
- Answer based on the Wiki pages above (not general knowledge)
- Use ONLY Obsidian's wiki-link syntax: [[${this.plugin.settings.wikiFolder}/entities/page-name]] (NOT HTML links)
- Link format MUST include wiki folder: [[${this.plugin.settings.wikiFolder}/entities/page-name]]

CRITICAL RULES:
\u2705 CORRECT: [[${this.plugin.settings.wikiFolder}/entities/example-page]], [[${this.plugin.settings.wikiFolder}/concepts/example-concept]]
\u274C WRONG: <a href="...">, [link text](url), [[example-page]], [[entities/example-page]]
- Obsidian wiki-links use DOUBLE brackets: [[path]]
- NO HTML: Never use <a href="...">text</a>
- NO Markdown external links: Never use [text](url)
- Include ${this.plugin.settings.wikiFolder}/ prefix: Links must start with [[${this.plugin.settings.wikiFolder}/...

CITATION REQUIREMENTS:
- When referencing specific information from a Wiki page, include an inline wiki-link
- At the end of your answer, add a "## References" section (or "## \u53C2\u8003\u6587\u732E" for Chinese) listing all wiki pages you cited
- Format each reference as: [[${this.plugin.settings.wikiFolder}/path/page-name|Display Name]] \u2014 brief description
- Example:
  ## References
  1. [[${this.plugin.settings.wikiFolder}/concepts/example-concept|Example Concept]] \u2014 Core mechanism explanation
  2. [[${this.plugin.settings.wikiFolder}/entities/example-entity|Example Entity]] \u2014 Background and history

If Wiki lacks relevant information:
- Acknowledge it and suggest ingesting more sources
- Do NOT make up information outside Wiki

Respond in ${langName}`;
      console.debug("[Step 4] Wiki context built");
      console.debug("[Step 4] Context length:", wikiContext.length);
      return wikiContext;
    } catch (error) {
      console.error("[Error] buildWikiContext failed:", error);
      const lang2 = this.plugin.settings.wikiLanguage || "en";
      const langName2 = WIKI_LANGUAGES[lang2] || lang2;
      return `IMPORTANT: You MUST write ALL responses in ${langName2}.

You are a Wiki assistant. Failed to load Wiki context. Please answer based on your knowledge.`;
    }
  }
  async selectRelevantPagesWithLLM(query, indexContent) {
    console.debug("=== LLM page selection started ===");
    const prompt = `You are a Wiki page selector. Given a user query and the Wiki index, select the most relevant pages.

User Query: "${query}"

Wiki Index:
${indexContent}

Task:
1. Read the Wiki index above
2. Identify pages that are MOST relevant to the user's query
3. Pay special attention to \`[aliases]\` in backtick-brackets after page names \u2014 these are alternative names, abbreviations, or translations. A user asking "DSA" may be referring to a page with alias "DSA" under a different title.
4. Consider page titles, summaries, aliases, and semantic relevance
5. Select top 3-5 most relevant pages

Output Format (strict JSON):
{
  "relevant_pages": [
    "entities/page-name-1",
    "concepts/page-name-2",
    "sources/page-name-3"
  ]
}

Important:
- Output ONLY the JSON object, no other text
- Page paths should match the format in Wiki links: "entities/name", "concepts/name", "sources/name"
- If no pages are relevant, output: {"relevant_pages": []}`;
    try {
      console.debug("[LLM] Sending selection request...");
      const response = await this.plugin.llmClient.createMessage({
        model: this.plugin.settings.model,
        max_tokens: TOKENS_QUERY_PAGE_SELECT,
        messages: [{ role: "user", content: prompt }],
        response_format: { type: "json_object" },
        ...this.plugin.settings.disableThinking ? { enableThinking: false } : {}
      });
      console.debug("[LLM] Raw response:", response);
      const parsed = await parseJsonResponse(response);
      const pages = (parsed == null ? void 0 : parsed.relevant_pages) || [];
      console.debug("[Parse OK] Page list:", pages);
      return pages;
    } catch (error) {
      console.error("[LLM selection failed]", error);
      return [];
    }
  }
  async loadRelevantPages(pageTitles) {
    console.debug("=== loadRelevantPages started ===");
    console.debug("Page titles:", pageTitles);
    const pages = [];
    const wikiPrefix = this.plugin.settings.wikiFolder + "/";
    for (const title of pageTitles) {
      console.debug(`[Load Page] Processing title: "${title}"`);
      const normalizedTitle = title.startsWith(wikiPrefix) ? title.slice(wikiPrefix.length) : title;
      const pagePath = `${this.plugin.settings.wikiFolder}/${normalizedTitle}.md`;
      console.debug(`[Load Page] Full path: "${pagePath}"`);
      const content = await this.plugin.wikiEngine.tryReadFile(pagePath);
      console.debug(`[Load Page] File exists: ${content ? "yes" : "no"}`);
      if (content) {
        console.debug(`[Load Page] content length: ${content.length}`);
        console.debug(`[Load Page] First 100 chars: ${content.substring(0, 100)}`);
        const displayTitle = `${this.plugin.settings.wikiFolder}/${normalizedTitle}`;
        let body = content;
        if (body.length > MAX_PAGE_CONTENT_CHARS) {
          body = body.substring(0, MAX_PAGE_CONTENT_CHARS) + "\n\n... (truncated)";
          console.debug(`[Load Page] Truncated from ${content.length} to ${MAX_PAGE_CONTENT_CHARS} chars`);
        }
        pages.push(`## ${displayTitle}

${body}`);
      } else {
        console.warn(`[Load Page] Cannot read page: ${pagePath}`);
      }
    }
    console.debug(`[Load Page] Successfully loaded pages`);
    return pages;
  }
};

// src/ui/schema-diff-modal.ts
var import_obsidian9 = require("obsidian");

// src/core/diff.ts
function lineDiff(oldText, newText) {
  var _a, _b, _c, _d, _e;
  const a = splitLines(oldText);
  const b = splitLines(newText);
  const n = a.length;
  const m = b.length;
  const dp = new Int32Array((n + 1) * (m + 1));
  const stride = m + 1;
  for (let i2 = 1; i2 <= n; i2++) {
    for (let j2 = 1; j2 <= m; j2++) {
      if (a[i2 - 1] === b[j2 - 1]) {
        dp[i2 * stride + j2] = dp[(i2 - 1) * stride + (j2 - 1)] + 1;
      } else {
        dp[i2 * stride + j2] = Math.max(
          dp[(i2 - 1) * stride + j2],
          dp[i2 * stride + (j2 - 1)]
        );
      }
    }
  }
  const out = [];
  let i = n;
  let j = m;
  while (i > 0 && j > 0) {
    if (a[i - 1] === b[j - 1]) {
      out.push({ op: "eq", line: (_a = a[i - 1]) != null ? _a : "" });
      i--;
      j--;
    } else if (dp[(i - 1) * stride + j] > dp[i * stride + (j - 1)]) {
      out.push({ op: "del", line: (_b = a[i - 1]) != null ? _b : "" });
      i--;
    } else {
      out.push({ op: "add", line: (_c = b[j - 1]) != null ? _c : "" });
      j--;
    }
  }
  while (i > 0) {
    out.push({ op: "del", line: (_d = a[i - 1]) != null ? _d : "" });
    i--;
  }
  while (j > 0) {
    out.push({ op: "add", line: (_e = b[j - 1]) != null ? _e : "" });
    j--;
  }
  out.reverse();
  return out;
}
function splitLines(text) {
  if (text === "") return [];
  return text.split("\n");
}

// src/ui/schema-diff-modal-classes.ts
var DIFF_MODAL_CLASS = "llm-wiki-schema-diff-modal";
function applyDiffModalClasses(modalEl, contentEl) {
  modalEl.addClass(DIFF_MODAL_CLASS);
  contentEl.empty();
  contentEl.addClass(DIFF_MODAL_CLASS);
}
function removeDiffModalClasses(modalEl) {
  modalEl.removeClass(DIFF_MODAL_CLASS);
}
function normalizeEmptyMode(opts) {
  return opts.isEmpty ? opts.currentBody : opts.newBody;
}

// src/ui/schema-diff-modal.ts
var SchemaDiffModal = class extends import_obsidian9.Modal {
  constructor(app, options) {
    var _a;
    super(app);
    this.rows = [];
    // v1.22.0 #97: Regenerate UI hidden for v1.22 release. The field is
    // kept (commented) so the v1.23 sprint can re-enable by uncommenting
    // the textarea + button + this declaration.
    // private regenerateHint = '';
    this.isBusy = false;
    this.options = options;
    this.currentBody = options.currentBody;
    this.newBody = normalizeEmptyMode({
      isEmpty: (_a = options.isEmpty) != null ? _a : false,
      currentBody: options.currentBody,
      newBody: options.newBody
    });
    this.recompute();
  }
  /** Public API: the caller (e.g. onRegenerate handler) updates the new
   *  body after a re-generation round and calls this to refresh the diff.
   *  Also refreshes the rationale so the LLM's reasoning stays in sync
   *  with the new body — the LLM may write a different rationale
   *  after a Regenerate round. */
  setNewBody(newBody, options) {
    this.newBody = newBody;
    if (options) {
      if (options.rationale !== void 0) this.options.rationale = options.rationale;
      if (options.isEmpty !== void 0) this.options.isEmpty = options.isEmpty;
    }
    this.recompute();
    this.refresh();
  }
  /** Public API: the caller sets the current body when it loads
   *  asynchronously after construction. We must re-run normalizeEmptyMode
   *  here too — otherwise in empty mode the newBody stays "" (set at
   *  construction when currentBody was still "") and the diff becomes
   *  "all current lines are deletions" (left pane fully red). */
  setCurrentBody(currentBody) {
    var _a;
    this.currentBody = currentBody;
    this.newBody = normalizeEmptyMode({
      isEmpty: (_a = this.options.isEmpty) != null ? _a : false,
      currentBody,
      newBody: this.newBody
    });
    this.recompute();
    this.refresh();
  }
  recompute() {
    const ops = lineDiff(this.currentBody, this.newBody);
    const rows = [];
    let leftLine = 0;
    let rightLine = 0;
    for (const op of ops) {
      if (op.op === "eq") {
        leftLine++;
        rightLine++;
        rows.push({ leftLineNo: leftLine, leftText: op.line, rightLineNo: rightLine, rightText: op.line, op: "eq" });
      } else if (op.op === "del") {
        leftLine++;
        rows.push({ leftLineNo: leftLine, leftText: op.line, rightLineNo: null, rightText: "", op: "del" });
      } else {
        rightLine++;
        rows.push({ leftLineNo: null, leftText: "", rightLineNo: rightLine, rightText: op.line, op: "add" });
      }
    }
    this.rows = rows;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const t = (_b = TEXTS[(_a = this.options.language) != null ? _a : "en"]) != null ? _b : TEXTS.en;
    const { modalEl, contentEl } = this;
    applyDiffModalClasses(modalEl, contentEl);
    contentEl.createEl("h2", { text: (_c = t.schemaDiffTitle) != null ? _c : "Schema update preview" });
    if (this.options.rationale) {
      const rationaleSection = contentEl.createDiv({ cls: "llm-wiki-schema-diff-rationale" });
      rationaleSection.createEl("p", {
        text: this.options.rationale,
        cls: "llm-wiki-schema-diff-rationale-text"
      });
    }
    if (this.options.isEmpty) {
      const banner = contentEl.createDiv({ cls: "llm-wiki-schema-diff-empty-banner" });
      banner.createEl("p", {
        text: (_d = t.schemaDiffEmptyTitle) != null ? _d : "No changes recommended",
        cls: "llm-wiki-schema-diff-empty-title"
      });
    }
    const addCount = this.rows.filter((r) => r.op === "add").length;
    const delCount = this.rows.filter((r) => r.op === "del").length;
    contentEl.createEl("p", {
      text: ((_e = t.schemaDiffSummary) != null ? _e : "+{add} / -{del} lines").replace("{add}", String(addCount)).replace("{del}", String(delCount)),
      cls: "llm-wiki-schema-diff-summary"
    });
    const diffContainer = contentEl.createDiv({
      cls: "llm-wiki-schema-diff-panes"
    });
    const leftPane = diffContainer.createDiv({ cls: "llm-wiki-schema-diff-pane llm-wiki-schema-diff-pane-left" });
    const rightPane = diffContainer.createDiv({ cls: "llm-wiki-schema-diff-pane llm-wiki-schema-diff-pane-right" });
    this.renderPane(leftPane, rightPane);
    const btnRow = contentEl.createDiv({ cls: "llm-wiki-schema-diff-btn-row" });
    if (this.options.onOpenFile) {
      const openFileBtn = btnRow.createEl("button", {
        text: (_f = t.schemaDiffOpenFileBtn) != null ? _f : "Open file"
      });
      openFileBtn.addEventListener("click", () => {
        var _a2, _b2;
        (_b2 = (_a2 = this.options).onOpenFile) == null ? void 0 : _b2.call(_a2);
        this.close();
      });
    }
    const cancelBtn = btnRow.createEl("button", {
      text: (_g = t.cancelButton) != null ? _g : "Cancel"
    });
    cancelBtn.addEventListener("click", () => {
      var _a2, _b2;
      (_b2 = (_a2 = this.options).onCancel) == null ? void 0 : _b2.call(_a2);
      this.close();
    });
    const applyBtn = btnRow.createEl("button", {
      text: (_h = t.schemaDiffApplyBtn) != null ? _h : "Apply",
      cls: "mod-cta"
    });
    if (this.options.isEmpty) {
      applyBtn.disabled = true;
    }
    applyBtn.addEventListener("click", () => {
      if (this.isBusy) return;
      this.isBusy = true;
      applyBtn.disabled = true;
      void Promise.resolve(this.options.onApply()).then(() => {
        this.close();
      }).catch((err) => {
        this.isBusy = false;
        applyBtn.disabled = false;
        console.error("Schema apply failed:", err);
      });
    });
  }
  renderPane(leftPane, rightPane) {
    leftPane.empty();
    rightPane.empty();
    for (const row of this.rows) {
      leftPane.appendChild(this.renderCell(row.leftLineNo, row.leftText, row.op === "del", "left"));
      rightPane.appendChild(this.renderCell(row.rightLineNo, row.rightText, row.op === "add", "right"));
    }
  }
  renderCell(lineNo, text, highlighted, side) {
    const sideClass = side === "left" ? " llm-wiki-schema-diff-row-del" : " llm-wiki-schema-diff-row-add";
    const rowClass = "llm-wiki-schema-diff-row" + (highlighted ? sideClass : "");
    const contentClass = "llm-wiki-schema-diff-content" + (highlighted ? " llm-wiki-schema-diff-content-highlight" : "");
    const cell = activeDocument.createElement("div");
    cell.className = rowClass;
    const gutter = activeDocument.createElement("span");
    gutter.className = "llm-wiki-schema-diff-gutter";
    gutter.textContent = lineNo == null ? "" : String(lineNo);
    cell.appendChild(gutter);
    const content = activeDocument.createElement("span");
    content.className = contentClass;
    content.textContent = text;
    cell.appendChild(content);
    return cell;
  }
  refresh() {
    const { contentEl } = this;
    contentEl.empty();
    this.onOpen();
  }
  onClose() {
    removeDiffModalClasses(this.modalEl);
    this.contentEl.empty();
  }
};

// src/schema/apply-suggestion.ts
var import_obsidian10 = require("obsidian");

// src/core/backup-rotation.ts
var MAX_BACKUPS = 3;
function backupFilename(sourcePath, iso) {
  const safeIso = iso.replace(/:/g, "-");
  return `${sourcePath}.bak.${safeIso}`;
}
function rotateBackups(allPaths) {
  if (allPaths.length <= MAX_BACKUPS) return [];
  return allPaths.slice(0, allPaths.length - MAX_BACKUPS);
}

// src/schema/apply-suggestion.ts
async function applySchemaSuggestion(params) {
  var _a, _b;
  const { app, currentPath, newBody, onCacheInvalidate } = params;
  const now = (_a = params.now) != null ? _a : (() => /* @__PURE__ */ new Date());
  const file = app.vault.getAbstractFileByPath(currentPath);
  if (!(file instanceof import_obsidian10.TFile)) {
    return { success: false, reason: "source-missing" };
  }
  const originalContent = await app.vault.read(file);
  const iso = now().toISOString();
  const bakPath = backupFilename(currentPath, iso);
  await app.vault.create(bakPath, originalContent);
  const dir = currentPath.substring(0, currentPath.lastIndexOf("/"));
  const baseName = (_b = currentPath.split("/").pop()) != null ? _b : currentPath;
  const bakPrefix = `${dir}/${baseName}.bak.`;
  const allBackups = [];
  const vaultAny = app.vault;
  const filesToScan = vaultAny.getFiles ? vaultAny.getFiles() : vaultAny.getMarkdownFiles();
  for (const f of filesToScan) {
    if (f.path.startsWith(bakPrefix)) allBackups.push(f.path);
  }
  allBackups.sort();
  const toDelete = rotateBackups(allBackups);
  for (const p of toDelete) {
    const f = app.vault.getAbstractFileByPath(p);
    if (f instanceof import_obsidian10.TFile) await app.fileManager.trashFile(f);
  }
  const newContent = spliceBody(originalContent, newBody);
  await app.vault.modify(file, newContent);
  onCacheInvalidate == null ? void 0 : onCacheInvalidate();
  return { success: true, backupPath: bakPath };
}
function spliceBody(originalContent, newBody) {
  if (!originalContent.startsWith("---")) {
    return newBody;
  }
  const end = originalContent.indexOf("---", 3);
  if (end <= 0) {
    return newBody;
  }
  const frontmatter = originalContent.substring(0, end + 3);
  return `${frontmatter}

${newBody}`;
}

// src/schema/schema-manager.ts
var import_obsidian11 = require("obsidian");

// src/schema/parse-suggestion.ts
function parseSchemaSuggestion(raw) {
  var _a;
  if (!raw || raw.trim() === "") {
    return { changes_needed: false, suggestions: "" };
  }
  let parsed = null;
  try {
    const stripped = stripCodeFence(raw);
    const intermediate = JSON.parse(stripped);
    if (intermediate && typeof intermediate === "object") {
      parsed = intermediate;
    }
  } catch (e) {
    parsed = null;
  }
  if (!parsed) {
    return { changes_needed: false, suggestions: "" };
  }
  const newSchemaBody = parsed.new_schema_body ? extractBodyFromFull(stripCodeFence(parsed.new_schema_body)) : void 0;
  const changesNeeded = typeof parsed.changes_needed === "boolean" ? parsed.changes_needed : Boolean(newSchemaBody) || Boolean(parsed.suggestions);
  return {
    changes_needed: changesNeeded,
    newSchemaBody,
    suggestions: (_a = parsed.suggestions) != null ? _a : ""
  };
}
function stripCodeFence(s) {
  let result = s.replace(/^\s+/, "");
  const open = result.match(/^```[a-zA-Z0-9_-]*\n/);
  if (open) result = result.slice(open[0].length);
  if (result.endsWith("```")) result = result.slice(0, -3);
  return result;
}
function extractBodyFromFull(fullContent) {
  if (!fullContent.startsWith("---")) return fullContent;
  const end = fullContent.indexOf("---", 3);
  if (end <= 0) return fullContent;
  let body = fullContent.substring(end + 3);
  if (body.startsWith("\r\n\r\n")) body = body.slice(4);
  else if (body.startsWith("\n\n")) body = body.slice(2);
  else if (body.startsWith("\r\n")) body = body.slice(2);
  else if (body.startsWith("\n")) body = body.slice(1);
  return body;
}

// src/schema/schema-manager.ts
var SCHEMA_FILENAME = "schema/config.md";
var SUGGESTIONS_FILENAME = "schema/suggestions.md";
var TASK_SECTIONS = {
  analyze: ["Wiki Structure", "Classification Rules", "Naming Conventions"],
  summary: ["Wiki Structure", "Classification Rules"],
  entity: ["Entity Page Template", "Naming Conventions", "Classification Rules"],
  concept: ["Concept Page Template", "Naming Conventions", "Classification Rules"],
  related: ["Naming Conventions", "Classification Rules"],
  conversation: ["Wiki Structure", "Entity Page Template", "Concept Page Template", "Naming Conventions", "Classification Rules"],
  index: ["Wiki Structure"],
  lint: ["Maintenance Policies"],
  merge: ["Entity Page Template", "Concept Page Template", "Naming Conventions", "Classification Rules"],
  full: ["Wiki Structure", "Entity Page Template", "Concept Page Template", "Naming Conventions", "Classification Rules", "Maintenance Policies"]
};
function buildDefaultSchemaBody(settings) {
  const entityTags = settings ? getActiveEntityTags(settings) : [...VALID_ENTITY_TAGS];
  const conceptTags = settings ? getActiveConceptTags(settings) : [...VALID_CONCEPT_TAGS];
  const entityList = entityTags.join(", ");
  const conceptList = conceptTags.join(", ");
  return `# Wiki Schema Configuration

This file governs how the LLM builds and maintains your Wiki. Edit it freely.

## Wiki Structure
- Entity pages: \`entities/\` (${entityList})
- Concept pages: \`concepts/\` (${conceptList})
- Source pages: \`sources/\`
- Index: \`index.md\`
- Log: \`log.md\`

## Entity Page Template
Pages in \`entities/\` MUST follow this structure:

**Frontmatter fields:**
- \`type: entity\` \u2014 page category (MUST be exactly "entity")
- \`created:\` \u2014 ISO date of first creation
- \`sources:\` \u2014 array of source file wiki-links
- \`tags:\` \u2014 entity subtype, MUST be one of: ${entityList}
- \`aliases:\` (optional) \u2014 alternative names (translations, abbreviations)
- \`reviewed:\` (optional) \u2014 if true, page is human-verified and protected

**Sections:**
1. **Basic Information**: Type, source file link
2. **Description**: 3-6 sentences with concrete facts, bidirectional links
3. **Related Entities**: Links to related entities using [[entities/...]]
4. **Related Concepts**: Links to related concepts using [[concepts/...]]
5. **Mentions in Source**: Verbatim quotes with source attribution \u2014 see [Mentions Format](#mentions-format) below

## Concept Page Template
Pages in \`concepts/\` MUST follow this structure:

**Frontmatter fields:**
- \`type: concept\` \u2014 page category (MUST be exactly "concept")
- \`created:\` \u2014 ISO date of first creation
- \`sources:\` \u2014 array of source file wiki-links
- \`tags:\` \u2014 concept subtype, MUST be one of: ${conceptList}
- \`aliases:\` (optional) \u2014 alternative names (translations, abbreviations)
- \`reviewed:\` (optional) \u2014 if true, page is human-verified and protected

**Sections:**
1. **Definition**: Clear, concise definition
2. **Key Characteristics**: Bullet list of defining traits
3. **Applications**: Real-world usage scenarios
4. **Related Concepts**: Links using [[concepts/...]]
5. **Related Entities**: Links using [[entities/...]]
6. **Mentions in Source**: Verbatim quotes with source attribution \u2014 see [Mentions Format](#mentions-format) below

## Naming Conventions
- Filenames: lowercase-with-hyphens (slugified)
- Entity/concept names: Preserve original language from source, NEVER translate
- Wiki-links: Use full paths [[entities/page-name|Display Name]] or [[concepts/page-name|Display Name]]

## Source Page Template
Pages in \`sources/\` MUST follow this structure:

**Frontmatter fields:**
- \`type: source\` \u2014 page category (MUST be exactly "source")
- \`tags:\` \u2014 INHERITED from the source note's frontmatter (do NOT use LLM-derived concept names). The system programmatically populates this from the source file; the LLM must not overwrite it with extracted concept names. This preserves the user's existing tag vocabulary and prevents pollution from LLM hallucinations.
- \`sources:\` \u2014 array of related wiki page links created from this source
- \`created:\` / \`updated:\` \u2014 set by the system, see Date Fields below

**Sections:**
1. **Summary**: Brief description of the source content (2-4 sentences)
2. **Key Points**: Bullet list of main insights
3. **Mentioned Pages**: List of [[entities/...]] and [[concepts/...]] pages created from this source

## Date Fields
- \`created:\` and \`updated:\` are filled by the system programmatically \u2014 NEVER LLM-generated
- The LLM may produce wrong dates during extraction; the system overrides them post-write to ensure correctness
- \`created:\` is preserved on merge (older value kept); \`updated:\` is always set to the current date
- \`source_note:\` (optional) \u2014 wiki-link to the original source file

## Mentions Format
"Mentions in Source" entries use academic-footnote style with source attribution. The format is:
- "Verbatim quote in original language (optional translation)" \u2014 [[source-name|display-name]]

Rules:
- Quotes must be VERBATIM \u2014 never paraphrase, summarize, or translate away the original
- The source wiki-link is required so future page merges can trace each quote to its origin
- Multiple quotes from the same source go in the same block, separated by newlines

## Content Rules
- mentions_in_source MUST be VERBATIM quotes \u2014 never paraphrase or translate
- Summaries/descriptions should use the wiki output language
- Entity/concept names must match the source file's original language exactly
- All pages must include bidirectional links where relevant

## Classification Rules
- **type field:** entity | concept | source \u2014 the page category
- **tags field:** stores the subtype (entity_type or concept_type)
- Entity subtypes (valid tags for type=entity): ${entityList}
- Concept subtypes (valid tags for type=concept): ${conceptList}
- Source types: document, conversation, note
- **Rule:** tags MUST only contain values from the corresponding subtype list above. A tag not in the valid list will be removed by the system.

## Multi-Source Merge Rules
- Sources array: Append new sources, never overwrite
- Aliases: Append alternative names (translations, abbreviations) without overwriting existing ones
- reviewed flag: If true, preserve all existing content, only append genuinely new info
- Contradictions: Preserve both sides with attribution, add to ## Contradictions section
- NO_NEW_CONTENT: Return this signal if source adds nothing new

## Maintenance Policies
- Stale threshold: 90 days without updates
- Contradiction severity: warning, conflict, error
- Orphan page: no inbound links from other wiki pages
- Missing page: referenced by [[link]] but does not exist
`;
}
var SchemaManager = class {
  constructor(app, settings, getLLMClient) {
    this.cachedBody = null;
    this.cacheValid = false;
    this.app = app;
    this.settings = settings;
    this.getLLMClient = getLLMClient;
  }
  get client() {
    const c = this.getLLMClient();
    if (!c) throw new Error("LLM Client not initialized");
    return c;
  }
  getSchemaPath() {
    return `${this.settings.wikiFolder}/${SCHEMA_FILENAME}`;
  }
  getSuggestionsPath() {
    return `${this.settings.wikiFolder}/${SUGGESTIONS_FILENAME}`;
  }
  invalidateCache() {
    this.cacheValid = false;
    this.cachedBody = null;
  }
  updateSettings(settings) {
    this.settings = settings;
    this.invalidateCache();
  }
  async getSchemaContext(task = "full") {
    const schema = await this.loadSchema();
    if (!schema || !schema.body.trim()) return "";
    const body = schema.body.trim();
    const selectedBody = this.selectSections(body, task);
    if (!selectedBody.trim()) return "";
    return `You are operating with the following Wiki Schema configuration.
Follow these rules when creating, updating, or analyzing Wiki pages.

--- BEGIN SCHEMA ---
${selectedBody}
--- END SCHEMA ---`;
  }
  selectSections(body, task) {
    if (task === "full") return body;
    const wanted = TASK_SECTIONS[task];
    const sections = this.parseSections(body);
    const selected = sections.filter((s) => wanted.includes(s.heading));
    return selected.map((s) => `## ${s.heading}
${s.content}`).join("\n\n");
  }
  parseSections(body) {
    const result = [];
    const lines = body.split("\n");
    let currentHeading = "";
    let currentContent = [];
    for (const line of lines) {
      if (line.startsWith("## ")) {
        if (currentHeading) {
          result.push({ heading: currentHeading, content: currentContent.join("\n").trim() });
        }
        currentHeading = line.substring(3).trim();
        currentContent = [];
      } else if (currentHeading) {
        currentContent.push(line);
      }
    }
    if (currentHeading) {
      result.push({ heading: currentHeading, content: currentContent.join("\n").trim() });
    }
    return result;
  }
  async loadSchema() {
    if (this.cacheValid && this.cachedBody !== null) {
      return { version: 0, updated: "", auto_suggestion_count: 0, body: this.cachedBody };
    }
    const path = this.getSchemaPath();
    const file = this.app.vault.getAbstractFileByPath(path);
    if (!(file instanceof import_obsidian11.TFile)) return null;
    try {
      const content = await this.app.vault.read(file);
      const parsed = this.parseConfigFile(content);
      this.cachedBody = parsed.body;
      this.cacheValid = true;
      return parsed;
    } catch (e) {
      console.warn("Failed to read schema file, ignoring");
      return null;
    }
  }
  async ensureSchemaExists() {
    const path = this.getSchemaPath();
    const existing = this.app.vault.getAbstractFileByPath(path);
    if (existing instanceof import_obsidian11.TFile) return;
    const schemaFolder = `${this.settings.wikiFolder}/schema`;
    try {
      await this.app.vault.createFolder(schemaFolder);
    } catch (e) {
    }
    const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const body = buildDefaultSchemaBody(this.settings);
    const content = `---
version: 1
updated: ${today}
auto_suggestion_count: 0
---

${body}`;
    await this.app.vault.create(path, content);
    this.cachedBody = body;
    this.cacheValid = true;
    console.debug("Created default schema at:", path);
  }
  async regenerateDefaultSchema() {
    const path = this.getSchemaPath();
    const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const body = buildDefaultSchemaBody(this.settings);
    const content = `---
version: 1
updated: ${today}
auto_suggestion_count: 0
---

${body}`;
    const schemaFolder = `${this.settings.wikiFolder}/schema`;
    try {
      await this.app.vault.createFolder(schemaFolder);
    } catch (e) {
    }
    const existing = this.app.vault.getAbstractFileByPath(path);
    if (existing instanceof import_obsidian11.TFile) {
      await this.app.vault.process(existing, () => content);
    } else {
      await this.app.vault.create(path, content);
    }
    this.cachedBody = body;
    this.cacheValid = true;
    console.debug("Regenerated default schema at:", path);
  }
  async suggestSchemaUpdate(context) {
    var _a;
    const schema = await this.loadSchema();
    const schemaContent = (schema == null ? void 0 : schema.body) || "(No schema configured yet)";
    const userLanguage = (_a = WIKI_LANGUAGES[this.settings.language]) != null ? _a : "English";
    const prompt = PROMPTS.suggestSchemaUpdate.replace("{{schema_content}}", schemaContent).replace("{{analysis_context}}", context).replace("{{user_language}}", userLanguage);
    try {
      const response = await this.client.createMessage({
        model: this.settings.model,
        max_tokens: capMaxTokens(TOKENS_SCHEMA_SUGGESTION, this.settings),
        messages: [{ role: "user", content: prompt }],
        response_format: { type: "json_object" },
        // v1.22.0 #97: pass the full LLMClient interface — enableThinking
        // (user-controlled, NEVER hardcoded), maxTokensPerCall (truncation
        // retry), and temperature (custom sampling, per user settings).
        // These fields are ALL optional on LLMClient.createMessage; the
        // client wrapper decides whether to send them to the provider.
        ...this.settings.disableThinking ? { enableThinking: false } : {},
        maxTokensPerCall: this.settings.maxTokensPerCall || void 0,
        temperature: this.settings.extractionTemperature
      });
      const parsed = parseSchemaSuggestion(response);
      const suggestion = {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        source: "manual",
        changes_needed: parsed.changes_needed,
        suggestions: parsed.suggestions,
        newSchemaBody: parsed.newSchemaBody
      };
      await this.appendSuggestion(suggestion);
      return suggestion;
    } catch (error) {
      console.error("Schema suggestion failed:", error);
      return null;
    }
  }
  parseConfigFile(content) {
    let version = 0;
    let updated = "";
    let auto_suggestion_count = 0;
    let body = content;
    if (content.startsWith("---")) {
      const end = content.indexOf("---", 3);
      if (end > 0) {
        const fmLines = content.substring(3, end).trim().split("\n");
        for (const line of fmLines) {
          const colon = line.indexOf(":");
          if (colon > 0) {
            const key = line.substring(0, colon).trim();
            const value = line.substring(colon + 1).trim();
            if (key === "version") {
              version = parseInt(value) || 0;
            } else if (key === "updated") {
              updated = value;
            } else if (key === "auto_suggestion_count") {
              auto_suggestion_count = parseInt(value) || 0;
            }
          }
        }
        body = content.substring(end + 3).trim();
      }
    }
    return { version, updated, auto_suggestion_count, body };
  }
  async appendSuggestion(suggestion) {
    const path = this.getSuggestionsPath();
    const existing = this.app.vault.getAbstractFileByPath(path);
    const entry = `## Suggestion \u2014 ${suggestion.timestamp}

**Source:** ${suggestion.source}
**Changes needed:** ${suggestion.changes_needed ? "Yes" : "No"}

${suggestion.suggestions}

---
`;
    if (existing instanceof import_obsidian11.TFile) {
      await this.app.vault.process(existing, (current) => current + "\n" + entry);
    } else {
      const header = `# Schema Suggestions

> Suggestions for improving your Wiki Schema. Review and decide whether to apply them to \`schema/config.md\`.

---

`;
      await this.app.vault.create(path, header + entry);
    }
  }
};

// src/schema/auto-maintain.ts
var import_obsidian12 = require("obsidian");
var AutoMaintainManager = class {
  constructor(app, settings, wikiEngine, plugin, lintCallback) {
    // Watcher state
    this.debounceTimer = null;
    this.pendingFiles = /* @__PURE__ */ new Map();
    this.recentWrites = /* @__PURE__ */ new Set();
    this.firstChangeTimestamp = 0;
    this.MAX_DEBOUNCE_MS = 6e4;
    // Hard cap to prevent indefinite delay
    // Periodic lint state
    this.lintIntervalId = null;
    this.lastLintTimestamp = 0;
    this.lintCallback = null;
    // Whether watchers are currently active
    this.watching = false;
    this.lintScheduled = false;
    // Avoid re-processing on repeated metadataCache events for the same file version
    this.lastSeenPaths = /* @__PURE__ */ new Set();
    this.app = app;
    this.settings = settings;
    this.wikiEngine = wikiEngine;
    this.plugin = plugin;
    this.lintCallback = lintCallback || null;
  }
  // === File Watcher ===
  // Listen to four event types to cover all file-creation scenarios:
  // - vault.on('create')  → new files within Obsidian (Cmd+N, right-click→New)
  // - vault.on('rename')  → files MOVED into watched folder (drag in file explorer)
  // - vault.on('modify')  → content changes in watched files
  // - metadataCache.on('resolved') → external drag-drop, copy-paste from OS
  // workspace.onLayoutReady prevents startup noise from existing files.
  startWatching() {
    if (this.watching) return;
    this.app.workspace.onLayoutReady(() => {
      if (this.watching) return;
      this.plugin.registerEvent(
        this.app.vault.on("create", (file) => {
          this.onFileChanged(file);
        })
      );
      this.plugin.registerEvent(
        this.app.vault.on("rename", (file, _oldPath) => {
          this.onFileChanged(file);
        })
      );
      this.plugin.registerEvent(
        this.app.vault.on("modify", (file) => {
          this.onFileChanged(file);
        })
      );
      this.plugin.registerEvent(
        this.app.metadataCache.on("resolved", ((file) => {
          this.onFileChanged(file);
        }))
      );
      this.watching = true;
      console.debug("AutoMaintain: File watcher started (create+rename+modify+resolved)");
      const texts = TEXTS[this.settings.language];
      new import_obsidian12.Notice(texts.watcherActiveNotice, NOTICE_WATCHER);
    });
  }
  stopWatching() {
    this.clearDebounce();
    this.pendingFiles.clear();
    this.watching = false;
    console.debug("AutoMaintain: File watcher stopped");
  }
  // Check if a path falls within any watched folder
  isWatched(path) {
    if (this.settings.watchedFolders.length === 0) return false;
    return this.settings.watchedFolders.some((folder) => {
      const prefix = folder.endsWith("/") ? folder : `${folder}/`;
      return path.startsWith(prefix);
    });
  }
  onFileChanged(file) {
    var _a;
    if (!file) return;
    console.debug(`[AutoMaintain] event: ${file.path}, TFile: ${file instanceof import_obsidian12.TFile}`);
    if (!(file instanceof import_obsidian12.TFile)) return;
    if (!file.path.endsWith(".md")) return;
    if (!this.isWatched(file.path)) {
      console.debug(`[AutoMaintain] SKIP: ${file.path} (not watched). Watched: ${JSON.stringify(this.settings.watchedFolders)}`);
      return;
    }
    if (this.recentWrites.has(file.path)) {
      console.debug(`[AutoMaintain] SKIP: ${file.path} (recent write)`);
      return;
    }
    const mtime = ((_a = file.stat) == null ? void 0 : _a.mtime) || 0;
    const fileKey = `${file.path}::${mtime}`;
    if (this.lastSeenPaths.has(fileKey)) {
      console.debug(`[AutoMaintain] SKIP: ${file.path} (same mtime already seen)`);
      return;
    }
    this.lastSeenPaths.add(fileKey);
    window.setTimeout(() => {
      this.lastSeenPaths.delete(fileKey);
    }, 6e5);
    console.debug(`[AutoMaintain] DETECTED: ${file.path}, pending: ${this.pendingFiles.size + 1}`);
    this.pendingFiles.set(file.path, file);
    if (this.firstChangeTimestamp === 0) {
      this.firstChangeTimestamp = Date.now();
    }
    const elapsed = Date.now() - this.firstChangeTimestamp;
    const delay = Math.max(0, Math.min(
      this.settings.autoWatchDebounceMs,
      this.MAX_DEBOUNCE_MS - elapsed
    ));
    if (this.debounceTimer !== null) {
      window.clearTimeout(this.debounceTimer);
    }
    if (delay <= 0) {
      void this.processBatch();
    } else {
      this.debounceTimer = window.setTimeout(
        () => {
          void this.processBatch();
        },
        delay
      );
    }
  }
  markRecentWrite(path) {
    this.recentWrites.add(path);
    window.setTimeout(() => {
      this.recentWrites.delete(path);
    }, 12e4);
  }
  // Register a write that auto-maintain should not re-trigger on.
  // Called externally (e.g. from wiki-engine callbacks) for files
  // created outside the watcher→ingest flow (like ingestConversation).
  watchWrite(path) {
    if (this.isWatched(path)) {
      this.markRecentWrite(path);
    }
  }
  async processBatch() {
    this.debounceTimer = null;
    this.firstChangeTimestamp = 0;
    const files = Array.from(this.pendingFiles.values());
    this.pendingFiles.clear();
    if (files.length === 0) return;
    const sourceFiles = files.filter((f) => this.isWatched(f.path));
    if (sourceFiles.length === 0) return;
    const texts = TEXTS[this.settings.language];
    if (this.settings.autoWatchMode === "notify") {
      new import_obsidian12.Notice(
        texts.watchIngestNotice.replace("{count}", String(sourceFiles.length)),
        8e3
      );
    } else {
      new import_obsidian12.Notice(texts.autoIngestRunning.replace("{count}", String(sourceFiles.length)), NOTICE_SHORT);
      let successCount = 0;
      let failCount = 0;
      const batchCtx = this.wikiEngine.createBatchContext();
      for (const file of sourceFiles) {
        try {
          this.markRecentWrite(file.path);
          await this.wikiEngine.ingestSource(file, { batchCtx });
          successCount++;
        } catch (error) {
          console.error(`Auto-ingest failed for ${file.path}:`, error);
          failCount++;
        }
      }
      new import_obsidian12.Notice(
        texts.autoIngestComplete.replace("{success}", String(successCount)).replace("{fail}", String(failCount)),
        failCount > 0 ? 8e3 : 5e3
      );
    }
  }
  clearDebounce() {
    if (this.debounceTimer !== null) {
      window.clearTimeout(this.debounceTimer);
      this.debounceTimer = null;
    }
    this.firstChangeTimestamp = 0;
  }
  // === Periodic Lint ===
  schedulePeriodicLint() {
    if (this.lintScheduled) return;
    if (this.settings.periodicLint === "off") return;
    const intervalMs = this.settings.periodicLint === "hourly" ? 60 * 60 * 1e3 : this.settings.periodicLint === "daily" ? 24 * 60 * 60 * 1e3 : 7 * 24 * 60 * 60 * 1e3;
    this.lintIntervalId = this.plugin.registerInterval(
      window.setInterval(() => {
        void (async () => {
          console.debug("AutoMaintain: Periodic lint tick...");
          try {
            await this.runLint();
          } catch (error) {
            console.error("Scheduled lint failed:", error);
          }
        })();
      }, intervalMs)
    );
    this.lintScheduled = true;
    console.debug(`AutoMaintain: Periodic lint scheduled (${this.settings.periodicLint})`);
  }
  clearPeriodicLint() {
    this.lintScheduled = false;
    console.debug("AutoMaintain: Periodic lint cleared");
  }
  async runLint() {
    const hasChanges = this.hasSourceFilesChanged();
    if (!hasChanges && this.lastLintTimestamp > 0) {
      console.debug("AutoMaintain: No source changes since last lint, skipping");
      return;
    }
    const texts = TEXTS[this.settings.language];
    new import_obsidian12.Notice(texts.scheduledLintRunning, NOTICE_SHORT);
    this.lastLintTimestamp = Date.now();
    if (this.lintCallback) {
      await this.lintCallback();
    } else {
      const pages = await this.wikiEngine.getExistingWikiPages();
      const entities = pages.filter((p) => p.path.includes("/entities/")).length;
      const concepts = pages.filter((p) => p.path.includes("/concepts/")).length;
      const sources = pages.filter((p) => p.path.includes("/sources/")).length;
      new import_obsidian12.Notice(
        texts.wikiLintStats.replace("{pages}", String(pages.length)).replace("{entities}", String(entities)).replace("{concepts}", String(concepts)).replace("{sources}", String(sources)),
        5e3
      );
    }
  }
  hasSourceFilesChanged() {
    if (this.settings.watchedFolders.length === 0) return true;
    const allFiles = this.app.vault.getMarkdownFiles();
    const sourceFiles = allFiles.filter((f) => this.isWatched(f.path));
    for (const file of sourceFiles) {
      if (file.stat.mtime > this.lastLintTimestamp) {
        return true;
      }
    }
    return false;
  }
  // === Startup Quick Fixes ===
  // Runs low-level programmatic fixes on plugin load:
  // - Sources field normalize (Issue #81)
  // - Wiki folder structure verify
  // All operations are read-only unless a file actually needs fixing.
  async runStartupCheck() {
    await new Promise((resolve) => window.setTimeout(resolve, 3e3));
    const texts = TEXTS[this.settings.language];
    console.debug("[QuickFixes] ===== Startup quick fixes START =====");
    console.debug(`[QuickFixes] Settings: wikiFolder="${this.settings.wikiFolder}", startupCheck=${this.settings.startupCheck}, language=${this.settings.language}`);
    const wikiFolder = this.settings.wikiFolder;
    const requiredFolders = [
      `${wikiFolder}/entities`,
      `${wikiFolder}/concepts`,
      `${wikiFolder}/sources`,
      `${wikiFolder}/schema`
    ];
    let structureOk = true;
    const missingFolders = [];
    for (const folder of requiredFolders) {
      const folderObj = this.app.vault.getAbstractFileByPath(folder);
      if (!folderObj) {
        structureOk = false;
        missingFolders.push(folder);
      }
    }
    console.debug(`[QuickFixes] Phase 1: Wiki structure ${structureOk ? "OK" : "INCOMPLETE"}` + (missingFolders.length > 0 ? `, missing: ${missingFolders.join(", ")}` : ""));
    let sourcesFilesCleaned = 0;
    let sourcesEntriesCleaned = 0;
    let sourcesFilesScanned = 0;
    let sourcesFilesPolluted = 0;
    try {
      const wikiFiles = this.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(wikiFolder));
      console.debug(`[QuickFixes] Phase 2: scanning ${wikiFiles.length} files in "${wikiFolder}"`);
      const sourcesPreserveCase = this.settings.slugCase === "preserve";
      for (const file of wikiFiles) {
        sourcesFilesScanned += 1;
        const content = await this.app.vault.read(file);
        if (!scanPollutedSources(content, wikiFolder, sourcesPreserveCase)) continue;
        sourcesFilesPolluted += 1;
        console.debug(`[QuickFixes] Polluted sources detected in ${file.path}`);
        const { fixed, content: fixedContent } = fixPollutedSources(content, wikiFolder, sourcesPreserveCase);
        if (fixed > 0) {
          await this.app.vault.process(file, () => fixedContent);
          sourcesFilesCleaned += 1;
          sourcesEntriesCleaned += fixed;
          console.debug(`[QuickFixes] Fixed ${file.path} (${fixed} entry normalization)`);
        } else {
          console.debug(`[QuickFixes] Scan reported polluted but fix returned 0 for ${file.path} \u2014 possible bug`);
        }
      }
      console.debug(`[QuickFixes] Phase 2 complete: ${sourcesFilesScanned} scanned, ${sourcesFilesPolluted} polluted, ${sourcesFilesCleaned} fixed (${sourcesEntriesCleaned} entries)`);
    } catch (e) {
      console.warn("[QuickFixes] Phase 2 failed:", e);
    }
    let incompleteFilesScanned = 0;
    let incompleteFilesArchived = 0;
    try {
      const incomplete = await findIncompletePages(this.app, wikiFolder);
      incompleteFilesScanned = incomplete.length;
      if (incomplete.length > 0) {
        incompleteFilesArchived = await cleanIncompletePages(this.app, incomplete);
        console.debug(`[QuickFixes] Phase 3: ${incompleteFilesScanned} incomplete pages found, ${incompleteFilesArchived} archived`);
      } else {
        console.debug("[QuickFixes] Phase 3: no incomplete pages found");
      }
    } catch (e) {
      console.warn("[QuickFixes] Phase 3 failed:", e);
    }
    const pages = await this.wikiEngine.getExistingWikiPages();
    const entities = pages.filter((p) => p.path.includes("/entities/")).length;
    const concepts = pages.filter((p) => p.path.includes("/concepts/")).length;
    const sources = pages.filter((p) => p.path.includes("/sources/")).length;
    const indexPath = `${wikiFolder}/index.md`;
    const hasIndex = await this.wikiEngine.tryReadFile(indexPath);
    const indexStatus = hasIndex ? "" : " \u2014 index.md missing";
    console.debug(`[QuickFixes] Phase 4: Wiki health \u2014 ${pages.length} pages (entities=${entities}, concepts=${concepts}, sources=${sources})${indexStatus}`);
    const structureLabel = structureOk ? texts.startupCheckStructureOk : texts.startupCheckStructureMissing;
    const sourcesLabel = sourcesFilesCleaned > 0 ? texts.startupCheckSourcesCleaned.replace("{files}", String(sourcesFilesCleaned)).replace("{entries}", String(sourcesEntriesCleaned)) : texts.startupCheckSourcesClean;
    const incompleteLabel = incompleteFilesArchived > 0 ? texts.startupCheckIncompleteArchived.replace("{count}", String(incompleteFilesArchived)) : texts.startupCheckIncompleteClean;
    const summary = `${texts.startupCheckTitle}
${texts.startupCheckStructureLabel}: ${structureLabel}
${texts.startupCheckSourcesLabel}: ${sourcesLabel}
${incompleteLabel}
${texts.startupCheckSummary.replace("{pages}", String(pages.length)).replace("{entities}", String(entities)).replace("{concepts}", String(concepts)).replace("{sources}", String(sources))}
${texts.startupCheckDisableHint}`;
    console.debug("[QuickFixes] Notice payload:\n" + summary.split("\n").map((l) => "  " + l).join("\n"));
    console.debug("[QuickFixes] ===== Startup quick fixes COMPLETE =====");
    new import_obsidian12.Notice(summary, 1e4);
  }
  // === Full Stop ===
  stop() {
    this.stopWatching();
    this.clearPeriodicLint();
    this.clearDebounce();
    this.pendingFiles.clear();
  }
};

// src/wiki/lint/controller.ts
var import_obsidian14 = require("obsidian");

// src/wiki/lint/duplicate-detection.ts
function bigrams(s) {
  const result = /* @__PURE__ */ new Set();
  const normalized = s.toLowerCase().replace(/[^a-z0-9一-鿿]/g, "");
  for (let i = 0; i < normalized.length - 1; i++) {
    result.add(normalized.substring(i, i + 2));
  }
  return result;
}
function normalizeForMatch(s) {
  return s.toLowerCase().replace(/[\s\-_]+/g, "").replace(/[^a-z0-9一-鿿]/g, "");
}
var BODY_STOPWORDS = /* @__PURE__ */ new Set([
  "also",
  "are",
  "been",
  "being",
  "both",
  "but",
  "can",
  "could",
  "did",
  "does",
  "each",
  "from",
  "had",
  "has",
  "have",
  "into",
  "its",
  "may",
  "might",
  "must",
  "not",
  "only",
  "other",
  "our",
  "shall",
  "should",
  "than",
  "that",
  "the",
  "their",
  "them",
  "then",
  "there",
  "these",
  "they",
  "this",
  "those",
  "through",
  "was",
  "were",
  "what",
  "when",
  "where",
  "which",
  "while",
  "will",
  "with",
  "would",
  "your"
]);
function bodyWordSet(text) {
  return new Set(
    text.toLowerCase().replace(/[^\w\s一-鿿]/g, " ").split(/\s+/).filter((w) => w.length > 3 && !BODY_STOPWORDS.has(w))
  );
}
function computeJaccard(setA, setB) {
  if (setA.size === 0 || setB.size === 0) return 0;
  let intersection = 0;
  for (const item of setA) {
    if (setB.has(item)) intersection++;
  }
  const union = setA.size + setB.size - intersection;
  return union > 0 ? intersection / union : 0;
}
async function generateDuplicateCandidates(pages) {
  const YIELD_EVERY = 200;
  const YIELD_EVERY_PHASE1 = 50;
  const metas = [];
  const linkRegex = /\[\[([^\]|#]+)(?:[|#][^\]]+)?\]\]/g;
  for (let i = 0; i < pages.length; i++) {
    const page = pages[i];
    if (i > 0 && i % YIELD_EVERY_PHASE1 === 0) {
      await new Promise((resolve) => window.setTimeout(resolve, 0));
    }
    const fm = parseFrontmatter(page.content);
    const aliases = Array.isArray(fm == null ? void 0 : fm.aliases) ? fm.aliases : [];
    const links = /* @__PURE__ */ new Set();
    const body = page.content.replace(/---[\s\S]*?---/, "");
    let match;
    while ((match = linkRegex.exec(body)) !== null) {
      links.add(match[1].trim().toLowerCase());
    }
    const bodyText = body.replace(/\[\[[^\]]+\]\]/g, "");
    const bodyWords = bodyWordSet(bodyText);
    metas.push({ path: page.path, title: page.title, aliases, links, bodyWords });
  }
  const candidates = /* @__PURE__ */ new Map();
  const addCandidate = (pathA, pathB, reason, signal, score) => {
    const key = [pathA, pathB].sort().join("|||");
    if (!candidates.has(key)) {
      candidates.set(key, { target: pathA, source: pathB, reason, signal, score });
    } else if (score > candidates.get(key).score) {
      candidates.set(key, { target: pathA, source: pathB, reason, signal, score });
    }
  };
  let comparisonCount = 0;
  const YIELD_EVERY_COMPARISON = 500;
  for (let i = 0; i < metas.length; i++) {
    if (i > 0 && i % YIELD_EVERY === 0) {
      await new Promise((resolve) => window.setTimeout(resolve, 0));
    }
    for (let j = i + 1; j < metas.length; j++) {
      comparisonCount++;
      if (comparisonCount % YIELD_EVERY_COMPARISON === 0) {
        await new Promise((resolve) => window.setTimeout(resolve, 0));
      }
      const a = metas[i], b = metas[j];
      if (a.links.size === 0 || b.links.size === 0) continue;
      const jaccard = computeJaccard(a.links, b.links);
      if (jaccard >= 0.4) {
        const bodySim = computeJaccard(a.bodyWords, b.bodyWords);
        if (bodySim < 0.2) continue;
        addCandidate(a.path, b.path, `Shared wiki-links (${Math.round(jaccard * 100)}% overlap)`, "sharedLinks", jaccard);
      }
    }
  }
  for (let i = 0; i < metas.length; i++) {
    if (i > 0 && i % YIELD_EVERY === 0) {
      await new Promise((resolve) => window.setTimeout(resolve, 0));
    }
    for (let j = i + 1; j < metas.length; j++) {
      comparisonCount++;
      if (comparisonCount % YIELD_EVERY_COMPARISON === 0) {
        await new Promise((resolve) => window.setTimeout(resolve, 0));
      }
      const a = metas[i], b = metas[j];
      const namesA = [a.title, ...a.aliases];
      const namesB = [b.title, ...b.aliases];
      let maxSim = 0;
      for (const nameA of namesA) {
        for (const nameB of namesB) {
          const sim = computeJaccard(bigrams(nameA), bigrams(nameB));
          if (sim > maxSim) maxSim = sim;
        }
      }
      if (maxSim >= 0.4) {
        addCandidate(a.path, b.path, `Title/alias similarity (${Math.round(maxSim * 100)}% match)`, "bigram", maxSim);
      }
      const normalizedNamesA = namesA.map((n) => normalizeForMatch(n));
      const normalizedAliasesB = b.aliases.map((n) => normalizeForMatch(n));
      const normalizedTitleB = normalizeForMatch(b.title);
      let crossLangMatch = false;
      for (const normA of normalizedNamesA) {
        if (normA && (normalizedAliasesB.includes(normA) || normalizedTitleB === normA)) {
          addCandidate(a.path, b.path, "Cross-language match (alias or title overlap)", "crossLang", 1);
          crossLangMatch = true;
          break;
        }
      }
      if (!crossLangMatch) {
        const normalizedNamesB = namesB.map((n) => normalizeForMatch(n));
        const normalizedAliasesA = a.aliases.map((n) => normalizeForMatch(n));
        const normalizedTitleA = normalizeForMatch(a.title);
        for (const normB of normalizedNamesB) {
          if (normB && (normalizedAliasesA.includes(normB) || normalizedTitleA === normB)) {
            addCandidate(a.path, b.path, "Cross-language match (alias or title overlap)", "crossLang", 1);
            break;
          }
        }
      }
    }
  }
  for (let i = 0; i < metas.length; i++) {
    for (let j = i + 1; j < metas.length; j++) {
      const a = metas[i], b = metas[j];
      const lowerA = a.title.toLowerCase();
      const lowerB = b.title.toLowerCase();
      if (lowerA === lowerB && a.title !== b.title) {
        const [canonical, variant] = a.title < b.title ? [a, b] : [b, a];
        addCandidate(
          canonical.path,
          variant.path,
          `Case-variant duplicate: "${a.title}" \u2194 "${b.title}"`,
          "caseVariant",
          0.9
        );
      }
    }
  }
  return Array.from(candidates.values());
}

// src/wiki/lint/fix-runners.ts
var import_obsidian13 = require("obsidian");
function checkCancelled(signal) {
  if (signal == null ? void 0 : signal.aborted) {
    throw new DOMException("Lint cancelled by user", "AbortError");
  }
}
function makeMirroredNotice(ctx) {
  const notice = new import_obsidian13.Notice("", 0);
  return {
    setMessage(msg) {
      notice.setMessage(msg);
      ctx.wikiEngine.updateStatusBar(msg);
    },
    hide() {
      notice.hide();
      ctx.wikiEngine.updateStatusBar("");
    }
  };
}
async function runAliasCompletion(ctx, signal, aliasDeficientPages) {
  var _a, _b, _c;
  checkCancelled(signal);
  const client = ctx.llmClient;
  if (!client) return { filled: 0, results: [] };
  const t = TEXTS[ctx.settings.language];
  const concurrency = (_a = ctx.settings.pageGenerationConcurrency) != null ? _a : 1;
  const totalBatches = Math.ceil(aliasDeficientPages.length / concurrency);
  console.debug(`[Alias] Starting alias completion \u2014 ${aliasDeficientPages.length} pages, concurrency=${concurrency}, batches=${totalBatches}`);
  let filled = 0;
  const results = [];
  const fixNotice = new import_obsidian13.Notice("", 0);
  const aliasStartTime = Date.now();
  const aliasFailures = [];
  try {
    for (let i = 0; i < aliasDeficientPages.length; i += concurrency) {
      checkCancelled(signal);
      const batch = aliasDeficientPages.slice(i, i + concurrency);
      const batchNum = Math.floor(i / concurrency) + 1;
      const batchStartTime = Date.now();
      console.debug(`[Alias batch ${batchNum}/${totalBatches}] Processing ${batch.length} pages: ${batch.map((p) => p.basename).join(", ")}`);
      const batchResults = await Promise.allSettled(
        batch.map(async (page) => {
          var _a2;
          const pageRel = page.path.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
          fixNotice.setMessage(t.lintAliasesFilling.replace("{current}", String(Math.min(i + batch.length, aliasDeficientPages.length))).replace("{total}", String(aliasDeficientPages.length)).replace("{page}", page.basename));
          try {
            const bodyMatch = page.content.match(/^---[\s\S]*?\n---\n?([\s\S]*)/);
            const body = bodyMatch ? bodyMatch[1].trim() : "";
            const prompt = PROMPTS.generateAliases.replace("{{title}}", page.basename).replace("{{body}}", body.substring(0, 2e3));
            const response = await client.createMessage({
              model: ctx.settings.model,
              max_tokens: TOKENS_LINT_ALIAS_BATCH,
              system: buildWikiLanguageDirective(ctx.settings),
              messages: [{ role: "user", content: prompt }],
              response_format: { type: "json_object" }
            });
            const parsed = await parseJsonResponse(response);
            if ((_a2 = parsed == null ? void 0 : parsed.aliases) == null ? void 0 : _a2.length) {
              console.debug(`[Alias] ${page.basename}: generated ${parsed.aliases.length} aliases \u2192 [${parsed.aliases.join(", ")}]`);
              const fmEnd = page.content.indexOf("\n---", 3);
              if (fmEnd !== -1) {
                const aliasesYaml = "aliases:\n" + parsed.aliases.map((a) => `  - "${a}"`).join("\n");
                const updated = page.content.substring(0, fmEnd) + "\n" + aliasesYaml + page.content.substring(fmEnd);
                await ctx.app.vault.adapter.write(page.path, updated);
                results.push(`- [[${pageRel}]]: added ${parsed.aliases.length} aliases`);
                return { success: true, name: page.basename, count: parsed.aliases.length };
              } else {
                console.warn(`[Alias] ${page.basename}: frontmatter closing marker not found`);
              }
            }
            return { success: false, name: page.basename, reason: "No aliases generated" };
          } catch (e) {
            const errMsg2 = e instanceof Error ? e.message : String(e);
            console.error(`[Alias] ${page.basename}: generation failed \u2014 ${errMsg2}`);
            new import_obsidian13.Notice(t.lintAliasesFillFailed.replace("{page}", page.basename).replace("{error}", errMsg2), NOTICE_ERROR);
            return { success: false, name: page.basename, reason: errMsg2 };
          }
        })
      );
      let batchSuccess = 0;
      let batchFail = 0;
      for (const r of batchResults) {
        if (r.status === "fulfilled" && r.value.success) {
          filled++;
          batchSuccess++;
        } else {
          batchFail++;
          const failureName = r.status === "fulfilled" ? String(r.value.name || "unknown") : "promise-rejected";
          const failureReason = r.status === "fulfilled" ? String(r.value.reason || "unknown") : r.reason instanceof Error ? r.reason.message : String(r.reason || "unknown");
          aliasFailures.push({ name: failureName, reason: failureReason });
          if (r.status === "rejected") {
            console.error(`[Alias batch ${batchNum}] Promise rejected:`, r.reason);
          }
        }
      }
      const batchTime = Date.now() - batchStartTime;
      console.debug(`[Alias batch ${batchNum}/${totalBatches}] Done \u2014 success=${batchSuccess}, fail=${batchFail}, time=${batchTime}ms`);
      if (i + concurrency < aliasDeficientPages.length && ((_b = ctx.settings.batchDelayMs) != null ? _b : 300) > 0) {
        await new Promise((resolve) => {
          var _a2;
          return window.setTimeout(resolve, (_a2 = ctx.settings.batchDelayMs) != null ? _a2 : 300);
        });
      }
    }
  } finally {
    fixNotice.hide();
  }
  const aliasRateInfo = detectRateLimitFailures(aliasFailures, concurrency, (_c = ctx.settings.batchDelayMs) != null ? _c : 300);
  if (aliasRateInfo) {
    console.warn(`[Alias Rate Limit] ${aliasRateInfo.count} alias generation(s) failed with 429, suggested concurrency=${aliasRateInfo.suggestedConcurrency}, delay=${aliasRateInfo.suggestedDelay}ms`);
    new import_obsidian13.Notice(formatRateLimitNotice(aliasRateInfo, ctx.settings.language), NOTICE_RATE_LIMIT);
  }
  const totalTime = Date.now() - aliasStartTime;
  console.debug(`[Alias] All done \u2014 success=${filled}, fail=${aliasDeficientPages.length - filled}, totalTime=${totalTime}ms`);
  return { filled, results };
}
async function runDeadLinkFixes(ctx, signal, deadLinks) {
  checkCancelled(signal);
  const t = TEXTS[ctx.settings.language];
  const seen = /* @__PURE__ */ new Set();
  const unique = deadLinks.filter((dl) => {
    const key = `${dl.source}::${dl.target}`;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
  let fixed = 0;
  const results = [];
  const fixNotice = new import_obsidian13.Notice("", 0);
  try {
    for (let i = 0; i < unique.length; i++) {
      checkCancelled(signal);
      const dl = unique[i];
      fixNotice.setMessage(t.lintFixProgress.replace("{current}", String(i + 1)).replace("{total}", String(unique.length)).replace("{target}", dl.target));
      console.debug(`lintFix: dead link ${i + 1}/${unique.length}: ${dl.source} -> ${dl.target}`);
      try {
        const sourcePath = `${ctx.settings.wikiFolder}/${dl.source}.md`;
        const result = await ctx.wikiEngine.fixDeadLink(sourcePath, dl.target);
        console.debug(`Dead link fix: ${dl.source} -> ${dl.target}: ${result}`);
        if (!result.includes(t.lintFixNoAction)) {
          fixed++;
          results.push(`- [[${dl.source}]]: \`[[${dl.target}]]\` \u2192 ${result}`);
        }
      } catch (e) {
        console.error(`Failed to fix dead link: ${dl.source} -> ${dl.target}`, e);
        const errMsg2 = e instanceof Error ? e.message : String(e);
        new import_obsidian13.Notice(t.lintFixItemFailed.replace("{target}", dl.target).replace("{error}", errMsg2), NOTICE_ERROR);
      }
    }
  } finally {
    fixNotice.hide();
  }
  return { fixed, results };
}
async function runEmptyPageFixes(ctx, signal, emptyPages) {
  checkCancelled(signal);
  const t = TEXTS[ctx.settings.language];
  let filled = 0;
  const results = [];
  const fixNotice = new import_obsidian13.Notice("", 0);
  try {
    for (let i = 0; i < emptyPages.length; i++) {
      checkCancelled(signal);
      const ep = emptyPages[i];
      fixNotice.setMessage(t.lintFillProgress.replace("{current}", String(i + 1)).replace("{total}", String(emptyPages.length)).replace("{page}", ep.path));
      console.debug(`lintFix: fill empty page ${i + 1}/${emptyPages.length}: ${ep.path}`);
      try {
        const summary = await ctx.wikiEngine.fillEmptyPage(ep.path, ep.content);
        filled++;
        results.push(`- ${summary}`);
      } catch (e) {
        console.error(`Failed to expand empty page: ${ep.path}`, e);
        const errMsg2 = e instanceof Error ? e.message : String(e);
        new import_obsidian13.Notice(t.lintFillFailed.replace("{page}", ep.path).replace("{error}", errMsg2), NOTICE_ERROR);
      }
    }
  } finally {
    fixNotice.hide();
  }
  return { filled, results };
}
async function runOrphanFixes(ctx, signal, orphans) {
  checkCancelled(signal);
  const t = TEXTS[ctx.settings.language];
  const results = [];
  const fixNotice = new import_obsidian13.Notice("", 0);
  try {
    for (let i = 0; i < orphans.length; i++) {
      checkCancelled(signal);
      const op = orphans[i];
      const opRel = op.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
      fixNotice.setMessage(t.lintLinkProgress.replace("{current}", String(i + 1)).replace("{total}", String(orphans.length)).replace("{page}", opRel));
      console.debug(`lintFix: link orphan ${i + 1}/${orphans.length}: ${op}`);
      try {
        const linkedPages = await ctx.wikiEngine.linkOrphanPage(op);
        if (linkedPages.length > 0) {
          results.push(`- [[${opRel}]] linked from: ${linkedPages.map((p) => `[[${p}]]`).join(", ")}`);
        } else {
          results.push(`- [[${opRel}]]: no suitable linking targets found`);
        }
      } catch (e) {
        console.error(`Failed to link orphan: ${op}`, e);
        const errMsg2 = e instanceof Error ? e.message : String(e);
        new import_obsidian13.Notice(t.lintLinkItemFailed.replace("{page}", opRel).replace("{error}", errMsg2), NOTICE_ERROR);
      }
    }
  } finally {
    fixNotice.hide();
  }
  return { linked: results.length, results };
}
async function runDuplicateMerges(ctx, signal, duplicates) {
  checkCancelled(signal);
  const t = TEXTS[ctx.settings.language];
  let merged = 0;
  const results = [];
  const fixNotice = new import_obsidian13.Notice("", 0);
  try {
    for (let i = 0; i < duplicates.length; i++) {
      checkCancelled(signal);
      const d = duplicates[i];
      const sourceRel = d.source.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
      const targetRel = d.target.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
      fixNotice.setMessage(t.lintMergeProgress.replace("{current}", String(i + 1)).replace("{total}", String(duplicates.length)).replace("{source}", sourceRel).replace("{target}", targetRel));
      console.debug(`lintFix: merge duplicates ${i + 1}/${duplicates.length}: ${d.source} \u2192 ${d.target}`);
      try {
        const result = await ctx.wikiEngine.mergeDuplicatePages(d.target, d.source);
        merged++;
        results.push(`- ${d.source} \u2192 ${d.target}: ${result}`);
      } catch (e) {
        console.error(`Failed to merge duplicates: ${d.source} \u2192 ${d.target}`, e);
        const errMsg2 = e instanceof Error ? e.message : String(e);
        new import_obsidian13.Notice(t.lintMergeItemFailed.replace("{source}", sourceRel).replace("{target}", targetRel).replace("{error}", errMsg2), NOTICE_ERROR);
      }
    }
  } finally {
    fixNotice.hide();
  }
  return { merged, results };
}
async function runRetagViolations(ctx, signal, violations) {
  if (signal == null ? void 0 : signal.aborted) {
    throw new DOMException("Lint fix cancelled by user", "AbortError");
  }
  if (!ctx.llmClient) {
    return { fixed: 0, results: ["LLM client not initialized; retag skipped."] };
  }
  const t = TEXTS[ctx.settings.language];
  const fixNotice = new import_obsidian13.Notice(t.lintTagViolationFiring.replace("{current}", "0").replace("{total}", String(violations.length)).replace("{path}", ""), 0);
  const results = [];
  let fixed = 0;
  try {
    for (let i = 0; i < violations.length; i++) {
      if (signal == null ? void 0 : signal.aborted) {
        throw new DOMException("Lint fix cancelled by user", "AbortError");
      }
      const v = violations[i];
      fixNotice.setMessage(t.lintTagViolationFiring.replace("{current}", String(i + 1)).replace("{total}", String(violations.length)).replace("{path}", v.path));
      try {
        const file = ctx.app.vault.getAbstractFileByPath(v.path);
        if (!file) {
          results.push(`${v.path}: file not found`);
          continue;
        }
        if (!(file instanceof import_obsidian13.TFile)) {
          results.push(`${v.path}: not a regular file`);
          continue;
        }
        const content = await ctx.app.vault.read(file);
        const fmEnd = content.indexOf("\n---\n", 3);
        const body = fmEnd === -1 ? content : content.substring(fmEnd + 5);
        const bodyPreview = body.slice(0, 400).replace(/\n+/g, " ").trim();
        const validVocab = v.pageType === "entity" ? getActiveEntityTags(ctx.settings) : v.pageType === "concept" ? getActiveConceptTags(ctx.settings) : getActiveSourceTags(ctx.settings);
        const prompt = appendTagVocabularyToPrompt(
          `You are retagging a wiki page whose current tags fall outside the active vocabulary.

Page name: ${v.title}
Page type: ${v.pageType}
Current tags (some are invalid): [${v.currentTags.join(", ")}]
Invalid tags: [${v.invalidTags.join(", ")}]

Page summary (first 400 chars of body):
${bodyPreview}

Task: Return a JSON object with a single field "tags" that is an array of strings.
- Each value MUST be one of the allowed values listed in the Active Tag Vocabulary section below.
- The values should be the closest valid matches for what this page is actually about.
- Do NOT include any other fields. Do NOT include any explanatory text.
- If the page is genuinely about nothing in the vocabulary, return an empty array.
`,
          ctx.settings
        );
        const response = await ctx.llmClient.createMessage({
          model: ctx.settings.model,
          max_tokens: 256,
          messages: [{ role: "user", content: prompt }]
        });
        if (signal == null ? void 0 : signal.aborted) {
          throw new DOMException("Lint fix cancelled by user", "AbortError");
        }
        const parsed = await parseJsonResponse(response);
        const newTags = Array.isArray(parsed == null ? void 0 : parsed.tags) ? parsed.tags.map((t2) => String(t2).trim()).filter((t2) => t2.length > 0) : [];
        const safeNewTags = newTags.filter((t2) => validVocab.includes(t2));
        if (safeNewTags.length === 0) {
          results.push(`${v.path}: LLM kept no tags (no valid match)`);
          continue;
        }
        if (safeNewTags.length === v.currentTags.length && safeNewTags.every((t2) => v.currentTags.includes(t2))) {
          results.push(`${v.path}: no change`);
          continue;
        }
        const updated = enforceFrontmatterConstraints(
          content,
          // original content; only the tags: line will change
          v.pageType,
          ctx.settings
        );
        const rewritten = updated.replace(
          /tags:\s*\[[^\]]*\]/,
          `tags: [${safeNewTags.join(", ")}]`
        );
        await ctx.app.vault.adapter.write(v.path, rewritten);
        fixed++;
        results.push(`${v.path}: [${v.currentTags.join(", ")}] \u2192 [${safeNewTags.join(", ")}]`);
      } catch (e) {
        if (e instanceof DOMException && e.name === "AbortError") throw e;
        const errMsg2 = e instanceof Error ? e.message : String(e);
        console.error(
          `[runRetagViolations] ${v.path} (${v.pageType}) failed:`,
          e
        );
        results.push(`${v.path}: ${errMsg2}`);
        new import_obsidian13.Notice(t.lintTagViolationFailed.replace("{path}", v.path).replace("{error}", errMsg2), NOTICE_ERROR);
      }
    }
  } finally {
    fixNotice.hide();
  }
  return { fixed, results };
}

// src/wiki/lint/lint-analysis-context.ts
function buildLintAnalysisContext(input) {
  const lines = [];
  lines.push(`Wiki size: ${input.totalWikiPages} pages.`);
  lines.push(`Findings: ${input.orphanCount} orphan pages, ${input.deadLinkCount} dead links, ${input.pollutedPageCount} polluted pages, ${input.tagViolationCount} tag violations, ${input.duplicateCount} duplicates, ${input.ungroundedQuoteCount} ungrounded quotes, ${input.emptyPageCount} empty pages.`);
  if (input.sampleOrphans.length > 0) {
    lines.push(`Sample orphans: ${input.sampleOrphans.join(", ")}.`);
  }
  if (input.sampleDeadLinks.length > 0) {
    lines.push(`Sample dead link targets: ${input.sampleDeadLinks.join(", ")}.`);
  }
  if (input.sampleTagViolations.length > 0) {
    lines.push(`Sample tag violations: ${input.sampleTagViolations.join(", ")}.`);
  }
  if (input.contradictionReport) {
    lines.push(input.contradictionReport);
  }
  return lines.join("\n");
}

// src/wiki/lint/phases/preparation.ts
async function runPreparationPhase(ctx) {
  var _a, _b, _c;
  const wikiFiles = ctx.app.vault.getMarkdownFiles().filter((f) => f.path.startsWith(ctx.settings.wikiFolder) && !f.path.includes("index.md") && !f.path.includes("log.md") && !f.path.includes("/schema/") && !f.path.includes("/contradictions/"));
  const allVaultFiles = ctx.app.vault.getMarkdownFiles();
  const { known: knownTargets, knownLower: knownTargetsLower } = buildKnownTargets(allVaultFiles);
  const pageMap = /* @__PURE__ */ new Map();
  (_a = ctx.stageNotice) == null ? void 0 : _a.setMessage(
    getText(ctx.settings.language, "lintReadingPages").replace("{count}", String(wikiFiles.length))
  );
  ctx.wikiEngine.updateStatusBar(getText(ctx.settings.language, "lintStatusReading"));
  console.debug(`lintWiki: reading ${wikiFiles.length} wiki pages in parallel`);
  const BATCH_READ = 200;
  for (let i = 0; i < wikiFiles.length; i += BATCH_READ) {
    ctx.checkCancelled();
    const batch = wikiFiles.slice(i, i + BATCH_READ);
    const batchResults = await Promise.all(
      batch.map(async (file) => {
        const content = await ctx.app.vault.read(file);
        return { path: file.path, content, basename: file.basename };
      })
    );
    for (const r of batchResults) {
      pageMap.set(r.path, r);
    }
  }
  (_b = ctx.stageNotice) == null ? void 0 : _b.setMessage(
    getText(ctx.settings.language, "lintReadingPagesProgress").replace("{current}", String(wikiFiles.length)).replace("{total}", String(wikiFiles.length))
  );
  console.debug(`lintWiki: read ${wikiFiles.length}/${wikiFiles.length} pages`);
  (_c = ctx.stageNotice) == null ? void 0 : _c.setMessage(getText(ctx.settings.language, "lintScanningLinks"));
  let doubleNestFixes = 0;
  for (const [path, info] of pageMap) {
    const abstractFile = ctx.app.vault.getAbstractFileByPath(path);
    if (abstractFile) {
      await ctx.app.vault.process(abstractFile, (data) => {
        const { fixed, content } = fixDoubleNestedWikiLinks(data);
        if (fixed > 0) {
          doubleNestFixes += fixed;
          info.content = content;
          console.debug(`lintWiki: fixed ${fixed} double-nested link(s) in ${path}`);
        }
        return data;
      });
    }
  }
  const logPath = `${ctx.settings.wikiFolder}/log.md`;
  const logFile = ctx.app.vault.getAbstractFileByPath(logPath);
  if (logFile) {
    await ctx.app.vault.process(logFile, (data) => {
      const { fixed, content } = fixDoubleNestedWikiLinks(data);
      if (fixed > 0) {
        doubleNestFixes += fixed;
        console.debug(`lintWiki: fixed ${fixed} double-nested link(s) in log.md`);
      }
      return fixed > 0 ? content : data;
    });
  }
  if (doubleNestFixes > 0) {
    console.debug(`lintWiki: total ${doubleNestFixes} double-nested link(s) fixed`);
  }
  let sourcesNormalizedFiles = 0;
  let sourcesNormalizedEntries = 0;
  const sourcesPreserveCase = ctx.settings.slugCase === "preserve";
  for (const [path, info] of pageMap) {
    if (!scanPollutedSources(info.content, ctx.settings.wikiFolder, sourcesPreserveCase)) continue;
    const abstractFile = ctx.app.vault.getAbstractFileByPath(path);
    if (abstractFile) {
      const { fixed, content } = fixPollutedSources(info.content, ctx.settings.wikiFolder, sourcesPreserveCase);
      if (fixed > 0) {
        await ctx.app.vault.process(abstractFile, () => content);
        sourcesNormalizedFiles += 1;
        sourcesNormalizedEntries += fixed;
        info.content = content;
        console.debug(`lintWiki: normalized ${fixed} sources entry(ies) in ${path}`);
      }
    }
  }
  if (sourcesNormalizedFiles > 0) {
    console.debug(`lintWiki: sources normalized in ${sourcesNormalizedFiles} files (${sourcesNormalizedEntries} entries)`);
  }
  return {
    wikiFiles,
    pageMap,
    knownTargets,
    knownTargetsLower,
    doubleNestFixes,
    sourcesNormalizedFiles,
    sourcesNormalizedEntries
  };
}
function buildKnownTargets(allVaultFiles) {
  const known = /* @__PURE__ */ new Set();
  const knownLower = /* @__PURE__ */ new Set();
  const addTarget = (t) => {
    known.add(t);
    knownLower.add(t.toLowerCase());
  };
  for (const file of allVaultFiles) {
    const nameWithoutExt = file.basename.replace(".md", "");
    addTarget(file.basename);
    addTarget(nameWithoutExt);
    const relPath = file.path.replace(".md", "");
    addTarget(relPath);
    addTarget(file.path);
    const parts = relPath.split("/");
    for (let i = 1; i < parts.length; i++) {
      const subPath = parts.slice(i).join("/");
      addTarget(subPath);
      addTarget(subPath + ".md");
    }
  }
  return { known, knownLower };
}

// src/wiki/lint/scanners.ts
function detectAliasDeficiency(wikiFiles, pageMap) {
  const result = [];
  for (const file of wikiFiles) {
    if (file.path.includes("/entities/") || file.path.includes("/concepts/")) {
      const info = pageMap.get(file.path);
      if (info) {
        const fmMatch = info.content.match(/^---\n([\s\S]*?)\n---/);
        if (fmMatch && !fmMatch[1].includes("aliases:")) {
          result.push(info);
        }
      }
    }
  }
  return result;
}
function scanDeadLinks(pageMap, knownTargets, knownTargetsLower, wikiFolder) {
  const deadLinks = [];
  const seen = /* @__PURE__ */ new Set();
  const linkRegex = /\[\[([^\]|#]+)(?:[|#][^\]]+)?\]\]/g;
  for (const { path, content } of pageMap.values()) {
    let match;
    while ((match = linkRegex.exec(content)) !== null) {
      const target = match[1].trim();
      const targetLower = target.toLowerCase();
      if (!knownTargets.has(target) && !knownTargetsLower.has(targetLower)) {
        const parts = target.split("/");
        const sluggedBasename = parts[parts.length - 1].replace(/\s+/g, "-");
        const sluggedTarget = [...parts.slice(0, -1), sluggedBasename].join("/");
        const isSlugMatch = sluggedTarget !== target && (knownTargets.has(sluggedTarget) || knownTargetsLower.has(sluggedTarget.toLowerCase()));
        if (!isSlugMatch) {
          const source = path.replace(wikiFolder + "/", "").replace(".md", "");
          const key = `${source}::${target}`;
          if (!seen.has(key)) {
            seen.add(key);
            deadLinks.push({ source, target });
          }
        }
      }
    }
    linkRegex.lastIndex = 0;
  }
  return deadLinks;
}
function scanOrphans(pageMap, wikiFolder) {
  const incomingLinks = /* @__PURE__ */ new Map();
  const linkRegex = /\[\[([^\]|#]+)(?:[|#][^\]]+)?\]\]/g;
  for (const { path, content } of pageMap.values()) {
    const sourceRel = path.replace(wikiFolder + "/", "").replace(".md", "");
    let match;
    while ((match = linkRegex.exec(content)) !== null) {
      const target = match[1].trim();
      if (!incomingLinks.has(target)) incomingLinks.set(target, []);
      incomingLinks.get(target).push(sourceRel);
    }
    linkRegex.lastIndex = 0;
  }
  const orphans = [];
  for (const { path, basename, content } of pageMap.values()) {
    const fm = parseFrontmatter(content);
    const aliases = Array.isArray(fm == null ? void 0 : fm.aliases) ? fm.aliases : [];
    const relPath = path.replace(wikiFolder + "/", "").replace(".md", "");
    const nameWithoutExt = basename.replace(".md", "");
    const forms = [basename, nameWithoutExt, relPath, ...aliases];
    const parts = relPath.split("/");
    for (let i = 1; i < parts.length; i++) {
      const subPath = parts.slice(i).join("/");
      forms.push(subPath);
      forms.push(subPath + ".md");
    }
    const hasIncoming = forms.some((f) => incomingLinks.has(f) || incomingLinks.has(f.toLowerCase()));
    if (!hasIncoming) orphans.push(path);
  }
  return orphans;
}
function normalizeQuote(text) {
  return text.toLowerCase().replace(/[^\p{L}\p{N}\s]/gu, "").replace(/\s+/g, " ").trim();
}
function extractMentionsSection(content) {
  const match = content.match(/##\s+Mentions\s+in\s+Source\s*\n([\s\S]*?)(?=\n##\s|\n*$)/i);
  return match == null ? void 0 : match[1];
}
function extractSourceBody(content) {
  return content.replace(/^---\n[\s\S]*?\n---\n?/, "");
}
function isQuoteGrounded(quote, sourceBody) {
  if (sourceBody.includes(quote)) return true;
  const normalizedQuote = normalizeQuote(quote);
  if (normalizedQuote.length === 0) return false;
  return normalizeQuote(sourceBody).includes(normalizedQuote);
}
function scanQuoteGrounding(pageMap, sourceMap, wikiFolder) {
  var _a;
  const issues = [];
  const sourceBodies = Array.from(sourceMap.values()).map((s) => extractSourceBody(s.content));
  for (const [path, page] of pageMap) {
    if (!path.startsWith(wikiFolder + "/")) continue;
    const mentionsBlock = extractMentionsSection(page.content);
    if (!mentionsBlock) continue;
    const lineRegex = /^[-*]\s+"([^"]+)"(?:\s*[—-]\s*\[\[([^\]]+)\]\])?\s*$/gm;
    let match;
    while ((match = lineRegex.exec(mentionsBlock)) !== null) {
      const quote = match[1].trim();
      const linkTarget = (_a = match[2]) == null ? void 0 : _a.trim();
      if (linkTarget) {
        const sourcePath = linkTarget.endsWith(".md") ? linkTarget : `${linkTarget}.md`;
        const resolvedPath = sourcePath.startsWith(wikiFolder + "/") ? sourcePath : `${wikiFolder}/${sourcePath}`;
        const source = sourceMap.get(resolvedPath);
        const body = source ? extractSourceBody(source.content) : "";
        const grounded = source && isQuoteGrounded(quote, body);
        if (!grounded) {
          issues.push({
            pagePath: path,
            sourcePath: resolvedPath,
            quote,
            hasSourceLink: true
          });
        }
      } else {
        const grounded = sourceBodies.some((body) => isQuoteGrounded(quote, body));
        if (!grounded) {
          issues.push({
            pagePath: path,
            quote,
            hasSourceLink: false
          });
        }
      }
    }
  }
  issues.sort((a, b) => {
    const pathCmp = a.pagePath.localeCompare(b.pagePath);
    if (pathCmp !== 0) return pathCmp;
    return a.quote.localeCompare(b.quote);
  });
  return issues;
}
function scanTagViolations(pageMap, settings) {
  const validEntity = new Set(getActiveEntityTags(settings));
  const validConcept = new Set(getActiveConceptTags(settings));
  const validSource = new Set(getActiveSourceTags(settings));
  const violations = [];
  for (const [path, page] of pageMap) {
    const fm = parseFrontmatter(page.content);
    if (!fm) continue;
    const pageType = fm.type;
    if (pageType !== "entity" && pageType !== "concept" && pageType !== "source") continue;
    const validSet = pageType === "entity" ? validEntity : pageType === "concept" ? validConcept : validSource;
    const rawTags = fm.tags;
    let currentTags;
    if (Array.isArray(rawTags)) {
      currentTags = rawTags.map((t) => String(t).trim()).filter((t) => t.length > 0);
    } else if (typeof rawTags === "string" && rawTags.length > 0) {
      currentTags = [rawTags.trim()];
    } else {
      continue;
    }
    const invalidTags = currentTags.filter((t) => !validSet.has(t));
    if (invalidTags.length > 0) {
      violations.push({
        path,
        pageType,
        title: typeof fm.title === "string" ? fm.title : page.basename.replace(/\.md$/, ""),
        currentTags,
        invalidTags
      });
    }
  }
  violations.sort((a, b) => a.path.localeCompare(b.path));
  return violations;
}

// src/wiki/lint/phases/programmatic.ts
function runProgrammaticPhase(ctx, input) {
  var _a, _b;
  const aliasDeficientPages = detectAliasDeficiency(input.wikiFiles, input.pageMap);
  console.debug(`lintWiki: ${aliasDeficientPages.length} entity/concept pages missing aliases`);
  const orphans = scanOrphans(input.pageMap, ctx.settings.wikiFolder);
  const tagViolations = scanTagViolations(input.pageMap, ctx.settings);
  const allPages = Array.from(input.pageMap.values()).map(({ path, basename }) => ({
    path,
    title: basename
  }));
  const pollutedPages = detectPollutedPages(allPages);
  if (pollutedPages.length > 0) {
    console.warn(`[Lint] Detected ${pollutedPages.length} polluted page(s):`);
    for (const pp of pollutedPages) {
      console.warn(`  - ${pp.path} \u2192 should be "${pp.cleanTitle}"`);
    }
  }
  (_a = ctx.stageNotice) == null ? void 0 : _a.setMessage(getText(ctx.settings.language, "lintScanningLinks"));
  ctx.wikiEngine.updateStatusBar(getText(ctx.settings.language, "lintStatusScanningLinks"));
  console.debug("lintWiki: scanning dead links");
  const deadLinks = scanDeadLinks(
    input.pageMap,
    input.knownTargets,
    input.knownTargetsLower,
    ctx.settings.wikiFolder
  );
  (_b = ctx.stageNotice) == null ? void 0 : _b.setMessage(
    getText(ctx.settings.language, "lintScanningLinksProgress").replace("{current}", String(input.wikiFiles.length)).replace("{total}", String(input.wikiFiles.length))
  );
  const sourceMap = /* @__PURE__ */ new Map();
  for (const [path, page] of input.pageMap) {
    if (path.includes("/sources/")) {
      sourceMap.set(path, page);
    }
  }
  const ungroundedQuotes = scanQuoteGrounding(input.pageMap, sourceMap, ctx.settings.wikiFolder);
  console.debug(`lintWiki: ${ungroundedQuotes.length} ungrounded quote(s)`);
  return {
    aliasDeficientPages,
    emptyPages: [],
    orphans,
    tagViolations,
    pollutedPages,
    deadLinks,
    ungroundedQuotes,
    sourcesNormalizedFiles: 0,
    // populated by preparation phase caller
    sourcesNormalizedEntries: 0,
    doubleNestFixes: 0
  };
}

// src/wiki/lint/report-builder.ts
function buildLintReport(input) {
  const { settings, findings, duplicates, contradictionsReport, cleanedLLM, elapsedSeconds, totalPages } = input;
  const t = TEXTS[settings.language];
  const folder = settings.wikiFolder;
  const aliasDeficientPages = findings.aliasDeficientPages;
  const deadLinks = findings.deadLinks;
  const emptyPages = findings.emptyPages;
  const orphans = findings.orphans;
  const pollutedPages = findings.pollutedPages;
  const tagViolations = findings.tagViolations;
  const ungroundedQuotes = findings.ungroundedQuotes;
  const sourcesNormalizedFiles = findings.sourcesNormalizedFiles;
  const sourcesNormalizedEntries = findings.sourcesNormalizedEntries;
  const duplicatePaths = /* @__PURE__ */ new Set();
  for (const d of duplicates) {
    duplicatePaths.add(d.target);
    duplicatePaths.add(d.source);
  }
  let deadLinkFromDup = 0;
  for (const dl of deadLinks) {
    const sourcePath = `${folder}/${dl.source}.md`;
    const targetPath = `${folder}/${dl.target}.md`;
    if (duplicatePaths.has(sourcePath) || duplicatePaths.has(targetPath)) deadLinkFromDup++;
  }
  let orphanFromDup = 0;
  for (const op of orphans) {
    if (duplicatePaths.has(op)) orphanFromDup++;
  }
  let progReport = "";
  if (aliasDeficientPages.length > 0) {
    progReport += `## ${t.lintAliasesSection.replace("{count}", String(aliasDeficientPages.length))}

`;
    for (const p of aliasDeficientPages) {
      const rel = p.path.replace(folder + "/", "").replace(".md", "");
      progReport += t.lintAliasesItem.replace("{page}", rel) + "\n";
    }
    progReport += "\n";
  }
  if (duplicates.length > 0) {
    progReport += `## ${t.lintDuplicateSection.replace("{count}", String(duplicates.length))}

`;
    for (const d of duplicates) {
      const targetRel = d.target.replace(folder + "/", "").replace(".md", "");
      const sourceRel = d.source.replace(folder + "/", "").replace(".md", "");
      progReport += t.lintDuplicateItem.replace("{target}", targetRel).replace("{source}", sourceRel).replace("{reason}", d.reason) + "\n";
    }
    progReport += "\n";
  }
  if (deadLinks.length > 0) {
    const lines = [];
    for (const dl of deadLinks) {
      const sourcePath = `${folder}/${dl.source}.md`;
      const targetPath = `${folder}/${dl.target}.md`;
      const involvesDup = duplicatePaths.has(sourcePath) || duplicatePaths.has(targetPath);
      const dupFlag = involvesDup ? t.lintDeadLinkAffectedByDup : "";
      lines.push(t.lintDeadLinkItem.replace("{source}", dl.source).replace("{target}", dl.target).replace("{dupFlag}", dupFlag));
    }
    progReport += `## ${t.lintDeadLinkSection.replace("{count}", String(deadLinks.length))}

${lines.join("\n")}

`;
  }
  if (ungroundedQuotes.length > 0) {
    progReport += `## ${t.lintQuoteGroundingSection.replace("{count}", String(ungroundedQuotes.length))}

`;
    for (const q of ungroundedQuotes) {
      const pageRel = q.pagePath.replace(folder + "/", "").replace(".md", "");
      const sourceRel = q.sourcePath ? q.sourcePath.replace(folder + "/", "").replace(".md", "") : "";
      const sourceHint = q.hasSourceLink ? ` \u2192 [[${sourceRel}]]` : "";
      progReport += t.lintQuoteGroundingItem.replace("{page}", pageRel).replace("{source}", sourceRel).replace("{quote}", q.quote).replace("{sourceHint}", sourceHint) + "\n";
    }
    progReport += "\n";
  }
  if (emptyPages.length > 0) {
    progReport += `## ${t.lintEmptyPageSection.replace("{count}", String(emptyPages.length))}

`;
    for (const ep of emptyPages) {
      const rel = ep.path.replace(folder + "/", "").replace(".md", "");
      progReport += t.lintEmptyPageItem.replace("{page}", rel) + "\n";
    }
    progReport += "\n";
  }
  if (tagViolations.length > 0) {
    progReport += `## ${t.lintTagViolationSection.replace("{count}", String(tagViolations.length))}

`;
    for (const v of tagViolations) {
      const rel = v.path.replace(folder + "/", "").replace(".md", "");
      progReport += t.lintTagViolationItem.replace("{path}", rel).replace("{tags}", v.invalidTags.join(", ")) + "\n";
    }
    progReport += "\n";
  }
  if (pollutedPages.length > 0) {
    progReport += `## ${t.lintPollutedSection.replace("{count}", String(pollutedPages.length))}

`;
    for (const pp of pollutedPages) {
      const rel = pp.path.replace(folder + "/", "").replace(".md", "");
      progReport += t.lintPollutedItem.replace("{page}", rel).replace("{clean}", pp.cleanTitle) + "\n";
    }
    progReport += "\n";
  }
  if (sourcesNormalizedFiles > 0) {
    progReport += `## ${t.lintSourcesNormalizedSection}

`;
    progReport += t.lintSourcesNormalizedItem.replace("{files}", String(sourcesNormalizedFiles)).replace("{entries}", String(sourcesNormalizedEntries)) + "\n\n";
  }
  if (orphans.length > 0) {
    progReport += `## ${t.lintOrphanSection.replace("{count}", String(orphans.length))}

`;
    for (const op of orphans) {
      const rel = op.replace(folder + "/", "").replace(".md", "");
      const isDup = duplicatePaths.has(op);
      const dupFlag = isDup ? t.lintOrphanIsDuplicate : "";
      progReport += t.lintOrphanItem.replace("{page}", rel).replace("{dupFlag}", dupFlag) + "\n";
    }
    progReport += "\n";
  }
  if (!duplicates.length && !deadLinks.length && !emptyPages.length && !orphans.length && !ungroundedQuotes.length) {
    progReport += `${t.lintNoIssuesFound}

`;
  }
  const summaryText = t.lintReportSummary.replace("{total}", String(totalPages)).replace("{aliasesMissing}", String(aliasDeficientPages.length)).replace("{duplicates}", String(duplicates.length)).replace("{deadLinks}", String(deadLinks.length)).replace("{deadLinkFromDup}", String(deadLinkFromDup)).replace("{orphans}", String(orphans.length)).replace("{orphanFromDup}", String(orphanFromDup)).replace("{emptyPages}", String(emptyPages.length)).replace("{ungroundedQuotes}", String(ungroundedQuotes.length)).replace("{tagViolations}", String(tagViolations.length)).replace("{elapsedSeconds}", String(elapsedSeconds));
  if (aliasDeficientPages.length > 0) {
    const aliasPre = `> ${t.lintAliasesMissing.replace("{count}", String(aliasDeficientPages.length))}

`;
    progReport = aliasPre + progReport;
  }
  const llmHeading = cleanedLLM.startsWith("##") ? "" : t.lintLLMAnalysisHeading + "\n\n";
  return `# ${t.lintReportTitle}

> ${summaryText}

${progReport}${contradictionsReport}${llmHeading}${cleanedLLM}`;
}

// src/wiki/lint/controller.ts
function extractProgReport(fullReport) {
  const summaryEnd = fullReport.indexOf("\n\n", fullReport.indexOf("> "));
  if (summaryEnd < 0) return fullReport;
  const after = fullReport.indexOf("\n\n", summaryEnd + 2);
  if (after < 0) return "";
  return fullReport.slice(after + 2);
}
async function runLintWiki(ctx, signal) {
  var _a;
  if (!ctx.llmClient) {
    new import_obsidian14.Notice(TEXTS[ctx.settings.language].errorNoApiKey);
    return;
  }
  const checkCancelled2 = () => {
    if (signal == null ? void 0 : signal.aborted) {
      throw new DOMException("Lint cancelled by user", "AbortError");
    }
  };
  new import_obsidian14.Notice(TEXTS[ctx.settings.language].lintWikiStart);
  const lintStartTime = Date.now();
  const stageNotice = new import_obsidian14.Notice("", 0);
  try {
    const phaseCtx = {
      app: ctx.app,
      settings: ctx.settings,
      wikiEngine: ctx.wikiEngine,
      checkCancelled: checkCancelled2,
      stageNotice,
      totalPages: 0
    };
    const prep = await runPreparationPhase(phaseCtx);
    phaseCtx.totalPages = prep.wikiFiles.length;
    const findings = runProgrammaticPhase(phaseCtx, {
      wikiFiles: prep.wikiFiles,
      pageMap: prep.pageMap,
      knownTargets: prep.knownTargets,
      knownTargetsLower: prep.knownTargetsLower
    });
    findings.sourcesNormalizedFiles = prep.sourcesNormalizedFiles;
    findings.sourcesNormalizedEntries = prep.sourcesNormalizedEntries;
    findings.doubleNestFixes = prep.doubleNestFixes;
    const wikiFiles = prep.wikiFiles;
    const pageMap = prep.pageMap;
    const t = TEXTS[ctx.settings.language];
    const aliasDeficientPages = findings.aliasDeficientPages;
    const orphans = findings.orphans;
    const tagViolations = findings.tagViolations;
    const pollutedPages = findings.pollutedPages;
    const deadLinks = findings.deadLinks;
    const ungroundedQuotes = findings.ungroundedQuotes;
    const emptyPages = [];
    let duplicates = [];
    const entityConceptFiles = wikiFiles.filter(
      (f) => f.path.includes("/entities/") || f.path.includes("/concepts/")
    );
    if (entityConceptFiles.length >= 2 && ctx.llmClient) {
      ctx.wikiEngine.updateStatusBar(getText(ctx.settings.language, "lintStatusDuplicates"));
      stageNotice.setMessage(t.lintCheckingDuplicates);
      try {
        const pagesForDedup = [];
        for (const file of entityConceptFiles) {
          const info = pageMap.get(file.path);
          if (info) {
            pagesForDedup.push({ path: file.path, content: info.content, title: file.basename });
          }
        }
        const allCandidates = await generateDuplicateCandidates(pagesForDedup);
        const tier1 = [];
        const tier2 = [];
        for (const c of allCandidates) {
          if (c.signal === "crossLang" || c.signal === "caseVariant") {
            tier1.push(c);
          } else if (c.signal === "bigram") {
            (c.score >= 0.6 ? tier1 : tier2).push(c);
          } else if (c.signal === "sharedLinks") {
            tier2.push(c);
          }
        }
        console.debug(`lintWiki: ${allCandidates.length} candidates \u2192 Tier 1: ${tier1.length}, Tier 2: ${tier2.length}`);
        console.debug(`lintWiki: candidate breakdown by signal:`, {
          crossLang: allCandidates.filter((c) => c.signal === "crossLang").length,
          bigram: allCandidates.filter((c) => c.signal === "bigram").length,
          sharedLinks: allCandidates.filter((c) => c.signal === "sharedLinks").length
        });
        if (allCandidates.length === 0) {
          console.debug("lintWiki: no duplicate candidates found \u2014 wiki is clean");
        }
        const CANDIDATE_TOKENS = 30;
        const MAX_INPUT_TOKENS = 15e3;
        const BATCH_SIZE = 100;
        const maxTotalCandidates = Math.floor(MAX_INPUT_TOKENS / CANDIDATE_TOKENS);
        const verifyCandidates = [...tier1];
        const tier2Budget = Math.max(0, maxTotalCandidates - tier1.length);
        const tier2ToInclude = Math.min(tier2.length, tier2Budget);
        for (let i = 0; i < tier2ToInclude; i++) {
          verifyCandidates.push(tier2[i]);
        }
        console.debug(`lintWiki: sending ${verifyCandidates.length}/${maxTotalCandidates} candidates (Tier 1: ${tier1.length}, Tier 2: ${tier2ToInclude}/${tier2.length}, budget: ${MAX_INPUT_TOKENS} tokens)`);
        if (verifyCandidates.length > 0) {
          const batches = [];
          for (let i = 0; i < verifyCandidates.length; i += BATCH_SIZE) {
            batches.push(verifyCandidates.slice(i, i + BATCH_SIZE));
          }
          const concurrency = ctx.settings.pageGenerationConcurrency || 1;
          console.debug(`lintWiki: ${batches.length} batches, concurrency=${concurrency}`);
          const allDuplicates = [];
          const dedupFailures = [];
          for (let i = 0; i < batches.length; i += concurrency) {
            checkCancelled2();
            const chunk = batches.slice(i, i + concurrency);
            const batchStart = i + 1;
            const batchEnd = Math.min(i + concurrency, batches.length);
            const progressLabel = batchEnd > batchStart ? `${batchStart}-${batchEnd}/${batches.length}` : `${batchStart}/${batches.length}`;
            stageNotice.setMessage(t.lintCheckingDuplicatesProgress.replace("{current}", progressLabel));
            const results = await Promise.allSettled(
              chunk.map(async (batch, bi) => {
                var _a2;
                const batchNum = i + bi + 1;
                const candidateList = batch.map(
                  (c) => `- Candidate A: ${c.target}
  Candidate B: ${c.source}
  Signal: ${c.reason}`
                ).join("\n");
                const dedupPrompt = PROMPTS.lintDuplicateDetection.replace("{{wikiFolder}}", ctx.settings.wikiFolder).replace("{{candidates}}", candidateList).replace("{{total}}", String(pagesForDedup.length));
                console.debug(`lintWiki: batch ${batchNum}/${batches.length} \u2014 ${batch.length} candidates`);
                const dedupResponse = await ctx.llmClient.createMessage({
                  model: ctx.settings.model,
                  max_tokens: TOKENS_LINT_DEDUP_LLM,
                  messages: [{ role: "user", content: dedupPrompt }],
                  response_format: { type: "json_object" },
                  ...ctx.settings.disableThinking ? { enableThinking: false } : {}
                });
                const dedupResult = await parseJsonResponse(dedupResponse);
                console.debug(`lintWiki: batch ${batchNum}/${batches.length} \u2192 ${((_a2 = dedupResult == null ? void 0 : dedupResult.duplicates) == null ? void 0 : _a2.length) || 0} duplicates confirmed`);
                const rawDups = dedupResult == null ? void 0 : dedupResult.duplicates;
                return Array.isArray(rawDups) ? rawDups : [];
              })
            );
            for (const result of results) {
              if (result.status === "fulfilled") {
                const rawDups = Array.isArray(result.value) ? result.value : [];
                const validDups = rawDups.filter(
                  (d) => typeof d.target === "string" && d.target.length > 0 && typeof d.source === "string" && d.source.length > 0
                ).map((d) => ({
                  target: normalizeLLMPath(d.target, ctx.settings.wikiFolder),
                  source: normalizeLLMPath(d.source, ctx.settings.wikiFolder),
                  reason: d.reason
                }));
                allDuplicates.push(...validDups);
              } else {
                const reason = result.reason instanceof Error ? result.reason.message : String(result.reason || "unknown");
                console.error("lintWiki: duplicate detection batch failed:", reason);
                dedupFailures.push({ name: `batch-${i + 1}`, reason });
              }
            }
          }
          const dedupRateInfo = detectRateLimitFailures(dedupFailures, concurrency, (_a = ctx.settings.batchDelayMs) != null ? _a : 300);
          if (dedupRateInfo) {
            console.warn(`[Duplicate Rate Limit] ${dedupRateInfo.count} duplicate detection batch(es) failed with 429, suggested concurrency=${dedupRateInfo.suggestedConcurrency}, delay=${dedupRateInfo.suggestedDelay}ms`);
            new import_obsidian14.Notice(formatRateLimitNotice(dedupRateInfo, ctx.settings.language), NOTICE_RATE_LIMIT);
          }
          duplicates = allDuplicates;
          console.debug(`lintWiki: LLM confirmed ${duplicates.length} duplicate pairs total`);
        }
      } catch (e) {
        console.error("Duplicate detection failed:", e);
        const errMsg2 = e instanceof Error ? e.message : String(e);
        const errNotice = new import_obsidian14.Notice(t.lintDuplicateCheckFailedDetail.replace("{step}", "Layer 3 (LLM verify)").replace("{error}", errMsg2), 0);
        window.setTimeout(() => errNotice.hide(), NOTICE_RATE_LIMIT);
      }
    }
    const emptyPageDuplicates = /* @__PURE__ */ new Set();
    for (const d of duplicates) {
      emptyPageDuplicates.add(d.target);
      emptyPageDuplicates.add(d.source);
    }
    for (const { path, content } of pageMap.values()) {
      if (emptyPageDuplicates.has(path)) continue;
      if (isPageEmpty(content)) {
        emptyPages.push({ path, content });
      }
    }
    const duplicatePaths = /* @__PURE__ */ new Set();
    for (const d of duplicates) {
      duplicatePaths.add(d.target);
      duplicatePaths.add(d.source);
    }
    let deadLinkFromDup = 0;
    for (const dl of deadLinks) {
      const sourcePath = `${ctx.settings.wikiFolder}/${dl.source}.md`;
      const targetPath = `${ctx.settings.wikiFolder}/${dl.target}.md`;
      if (duplicatePaths.has(sourcePath) || duplicatePaths.has(targetPath)) deadLinkFromDup++;
    }
    let orphanFromDup = 0;
    for (const op of orphans) {
      if (duplicatePaths.has(op)) orphanFromDup++;
    }
    const findingsWithEmpty = {
      ...findings,
      emptyPages
    };
    let progReport = buildLintReport({
      settings: ctx.settings,
      findings: findingsWithEmpty,
      duplicates,
      contradictionsReport: "",
      cleanedLLM: "",
      elapsedSeconds: 0,
      totalPages: wikiFiles.length
    });
    progReport = extractProgReport(progReport);
    const openContradictions = await ctx.wikiEngine.getOpenContradictions();
    let contradictionsReport = "";
    const reviewOkItems = openContradictions.filter((c) => c.status === "review_ok");
    for (const c of reviewOkItems) {
      try {
        await ctx.wikiEngine.resolveContradiction(c.path);
        await ctx.wikiEngine.updateContradictionStatus(c.path, "resolved");
        console.debug("Auto-resolved contradiction:", c.path);
      } catch (error) {
        console.error("Failed to resolve contradiction:", c.path, error);
        await ctx.wikiEngine.updateContradictionStatus(c.path, "pending_fix");
      }
    }
    const remaining = await ctx.wikiEngine.getOpenContradictions();
    if (remaining.length > 0) {
      contradictionsReport = `## ${t.lintContradictionSection}

`;
      contradictionsReport += `- ${t.lintContradictionOpen.replace("{count}", String(remaining.length))}`;
      const resolvedCount = openContradictions.length - remaining.length;
      if (resolvedCount > 0) {
        contradictionsReport += ` ${t.lintContradictionAutoFixed.replace("{count}", String(resolvedCount))}`;
      }
      contradictionsReport += "\n";
      for (const c of remaining) {
        const relPath = c.path.replace(ctx.settings.wikiFolder + "/", "").replace(".md", "");
        const statusLabel = c.status === "detected" ? t.lintContradictionStatusDetected : c.status === "pending_fix" ? t.lintContradictionStatusPendingFix : c.status;
        contradictionsReport += t.lintContradictionItem.replace("{status}", statusLabel).replace("{page}", relPath).replace("{claim}", c.claim.substring(0, 80)) + "\n";
      }
      contradictionsReport += "\n";
    }
    const indexContent = await ctx.wikiEngine.tryReadFile(`${ctx.settings.wikiFolder}/index.md`) || "";
    let contentSample = "";
    const samplePages = wikiFiles.slice(0, 8);
    for (const file of samplePages) {
      const info = pageMap.get(file.path);
      if (info) {
        const body = info.content.substring(0, 600);
        contentSample += `
### ${info.basename}
${body}
`;
      }
    }
    const prompt = appendTagVocabularyToPrompt(
      appendGranularityToPrompt(
        t.lintAnalysisPrompt.replace("{index}", indexContent).replace("{total}", String(wikiFiles.length)).replace("{sample}", String(samplePages.length)).replace("{contentSample}", contentSample).replace("{progReport}", progReport || "No issues detected by programmatic checks."),
        ctx.settings
      ),
      ctx.settings
    );
    stageNotice.setMessage(t.lintAnalyzingLLM);
    ctx.wikiEngine.updateStatusBar(getText(ctx.settings.language, "lintStatusAnalyzing"));
    checkCancelled2();
    const llmReport = await ctx.llmClient.createMessage({
      model: ctx.settings.model,
      max_tokens: TOKENS_LINT_DEDUP_LLM,
      messages: [{ role: "user", content: prompt }],
      ...ctx.settings.disableThinking ? { enableThinking: false } : {}
    });
    const cleanedLLM = cleanMarkdownResponse(llmReport);
    const elapsedSeconds = Math.max(1, Math.round((Date.now() - lintStartTime) / 1e3));
    const summaryText = t.lintReportSummary.replace("{total}", String(wikiFiles.length)).replace("{aliasesMissing}", String(aliasDeficientPages.length)).replace("{duplicates}", String(duplicates.length)).replace("{deadLinks}", String(deadLinks.length)).replace("{deadLinkFromDup}", String(deadLinkFromDup)).replace("{orphans}", String(orphans.length)).replace("{orphanFromDup}", String(orphanFromDup)).replace("{emptyPages}", String(emptyPages.length)).replace("{ungroundedQuotes}", String(ungroundedQuotes.length)).replace("{tagViolations}", String(tagViolations.length)).replace("{elapsedSeconds}", String(elapsedSeconds));
    if (aliasDeficientPages.length > 0) {
      const aliasPre = `> ${t.lintAliasesMissing.replace("{count}", String(aliasDeficientPages.length))}

`;
      progReport = aliasPre + progReport;
    }
    const fullReport = `# ${t.lintReportTitle}

> ${summaryText}

${progReport}${contradictionsReport}${cleanedLLM.startsWith("##") ? "" : t.lintLLMAnalysisHeading + "\n\n"}${cleanedLLM}`;
    const counts = {
      deadLinks: deadLinks.length,
      emptyPages: emptyPages.length,
      pollutedPages: pollutedPages.length,
      orphans: orphans.length,
      duplicates: duplicates.length,
      pagesMissingAliases: aliasDeficientPages.length,
      tagViolations: tagViolations.length,
      ungroundedQuotes: ungroundedQuotes.length
    };
    const runFixPhase = async (fn) => {
      const signal2 = ctx.wikiEngine.startLintOperation();
      try {
        await fn(signal2);
      } finally {
        ctx.wikiEngine.endLintOperation();
      }
    };
    const fixCallbacks = {};
    const lintSummary = buildLintAnalysisContext({
      orphanCount: orphans.length,
      deadLinkCount: deadLinks.length,
      pollutedPageCount: pollutedPages.length,
      tagViolationCount: tagViolations.length,
      duplicateCount: duplicates.length,
      ungroundedQuoteCount: ungroundedQuotes.length,
      emptyPageCount: emptyPages.length,
      contradictionReport: contradictionsReport,
      sampleOrphans: orphans.slice(0, 5),
      sampleDeadLinks: deadLinks.slice(0, 5).map((dl) => dl.target),
      sampleTagViolations: tagViolations.slice(0, 5).map((tv) => tv.path),
      totalWikiPages: wikiFiles.length
    });
    fixCallbacks.onAnalyzeSchema = () => {
      void ctx.onAnalyzeSchema(lintSummary);
    };
    if (pollutedPages.length > 0) {
      fixCallbacks.onFixPollutedPages = () => {
        void runFixPhase(async (signal2) => {
          let fixed = 0;
          const fixNotice = makeMirroredNotice(ctx);
          try {
            for (const pp of pollutedPages) {
              if (signal2 == null ? void 0 : signal2.aborted) break;
              fixNotice.setMessage(t.lintFixingPolluted.replace("{current}", String(fixed + 1)).replace("{total}", String(pollutedPages.length)).replace("{title}", pp.title).replace("{newTitle}", pp.cleanTitle));
              try {
                await ctx.wikiEngine.fixPollutedPage(pp.path, pp.cleanTitle);
                fixed++;
              } catch (e) {
                console.error(`[Pollution Fix] Failed: ${pp.path}`, e);
              }
            }
            if (fixed > 0) {
              await ctx.wikiEngine.generateIndexFromEngine();
            }
            const msg = getText(ctx.settings.language, "lintPollutedFixed").replace("{fixed}", String(fixed)).replace("{total}", String(pollutedPages.length));
            new import_obsidian14.Notice(msg, 0);
          } finally {
            fixNotice.hide();
          }
        });
      };
    }
    if (aliasDeficientPages.length > 0) {
      fixCallbacks.onCompleteAliases = () => {
        void runFixPhase(async (signal2) => {
          const { filled, results } = await runAliasCompletion(ctx, signal2, aliasDeficientPages);
          if (filled > 0) {
            await ctx.wikiEngine.generateIndexFromEngine();
            await ctx.wikiEngine.logLintFix("Complete Aliases", results.join("\n"));
          }
          const msg = t.lintAliasesFilled.replace("{filled}", String(filled)).replace("{total}", String(aliasDeficientPages.length)) + (filled > 0 ? "\n" + t.lintFixIndexUpdated : "");
          new import_obsidian14.Notice(msg, 0);
        });
      };
    }
    if (deadLinks.length > 0) {
      fixCallbacks.onFixDeadLinks = () => {
        void runFixPhase(async (signal2) => {
          const { fixed, results } = await runDeadLinkFixes(ctx, signal2, deadLinks);
          if (fixed > 0) {
            await ctx.wikiEngine.generateIndexFromEngine();
            await ctx.wikiEngine.logLintFix("Fix Dead Links", results.join("\n"));
          }
          const msg = t.lintFixDeadComplete.replace("{fixed}", String(fixed)).replace("{total}", String(deadLinks.length)) + (fixed > 0 ? "\n" + t.lintFixIndexUpdated : "");
          new import_obsidian14.Notice(msg, 0);
        });
      };
    }
    if (emptyPages.length > 0) {
      fixCallbacks.onFillEmptyPages = () => {
        void runFixPhase(async (signal2) => {
          const { filled, results } = await runEmptyPageFixes(ctx, signal2, emptyPages);
          if (filled > 0) {
            await ctx.wikiEngine.generateIndexFromEngine();
            await ctx.wikiEngine.logLintFix("Expand Empty Pages", results.join("\n"));
          }
          const msg = t.lintFillComplete.replace("{filled}", String(filled)).replace("{total}", String(emptyPages.length)) + (filled > 0 ? "\n" + t.lintFixIndexUpdated : "");
          new import_obsidian14.Notice(msg, 0);
        });
      };
    }
    fixCallbacks.onDeleteEmptyStubs = () => {
      void runFixPhase(async (signal2) => {
        const result = await ctx.wikiEngine.deleteEmptyStubs(ctx.settings.wikiFolder);
        if (result.deleted > 0) {
          await ctx.wikiEngine.generateIndexFromEngine();
          await ctx.wikiEngine.logLintFix("Delete Empty Stubs", `Deleted ${result.deleted} empty stubs`);
        }
        const parts = [];
        if (result.deleted > 0) {
          parts.push(t.lintDeleteCompleted.replace("{count}", String(result.deleted)));
        }
        if (result.failed > 0) {
          parts.push(t.lintDeleteFailed.replace("{failed}", String(result.failed)).replace("{total}", String(result.deleted + result.failed)));
        }
        if (parts.length === 0) {
          parts.push(t.lintDeleteCompleted.replace("{count}", "0"));
        }
        new import_obsidian14.Notice(parts.join("\n"), 0);
      });
    };
    if (orphans.length > 0) {
      fixCallbacks.onLinkOrphans = () => {
        void runFixPhase(async (signal2) => {
          const { linked, results } = await runOrphanFixes(ctx, signal2, orphans);
          if (linked > 0) {
            await ctx.wikiEngine.generateIndexFromEngine();
            await ctx.wikiEngine.logLintFix("Link Orphan Pages", results.join("\n"));
          }
          const msg = t.lintLinkComplete.replace("{linked}", String(linked)) + (linked > 0 ? "\n" + t.lintFixIndexUpdated : "");
          new import_obsidian14.Notice(msg, 0);
        });
      };
    }
    if (duplicates.length > 0) {
      fixCallbacks.onMergeDuplicates = () => {
        void runFixPhase(async (signal2) => {
          const { merged, results } = await runDuplicateMerges(ctx, signal2, duplicates);
          if (merged > 0) {
            await ctx.wikiEngine.generateIndexFromEngine();
            await ctx.wikiEngine.logLintFix("Merge Duplicate Pages", results.join("\n"));
          }
          const msg = t.lintMergeComplete.replace("{merged}", String(merged)).replace("{total}", String(duplicates.length)) + (merged > 0 ? "\n" + t.lintFixIndexUpdated : "");
          new import_obsidian14.Notice(msg, 0);
        });
      };
    }
    if (tagViolations.length > 0) {
      fixCallbacks.onRetagViolations = () => {
        void runFixPhase(async (signal2) => {
          const { fixed, results } = await runRetagViolations(ctx, signal2, tagViolations);
          if (fixed > 0) {
            await ctx.wikiEngine.generateIndexFromEngine();
            await ctx.wikiEngine.logLintFix("Retag Tag Violations", results.join("\n"));
          }
          const msg = fixed > 0 ? t.lintTagViolationFixed.replace("{fixed}", String(fixed)).replace("{total}", String(tagViolations.length)) : t.lintTagViolationFixedNone;
          new import_obsidian14.Notice(msg, 0);
        });
      };
    }
    const totalFixable = deadLinks.length + emptyPages.length + orphans.length + duplicates.length;
    const totalFixableIncludingAliases = totalFixable + aliasDeficientPages.length + pollutedPages.length;
    if (totalFixableIncludingAliases > 0) {
      fixCallbacks.onFixAll = () => {
        void (async () => {
          const signal2 = ctx.wikiEngine.startLintOperation();
          try {
            const allResults = [];
            const fixAllNotice = makeMirroredNotice(ctx);
            let pollutedFixed = 0;
            let aliasesFilled = 0;
            let duplicatesMerged = 0;
            let deadLinksFixed = 0;
            let orphansLinked = 0;
            let emptyPagesFilled = 0;
            let tagsRetagged = 0;
            fixAllNotice.setMessage("Smart fix: Phase -1 \u2014 Fixing polluted pages...");
            if (pollutedPages.length > 0) {
              for (const pp of pollutedPages) {
                try {
                  const result = await ctx.wikiEngine.fixPollutedPage(pp.path, pp.cleanTitle);
                  console.debug(`[Pollution Fix] ${result}`);
                  pollutedFixed++;
                } catch (e) {
                  console.error(`[Pollution Fix] Failed: ${pp.path}`, e);
                }
              }
              if (pollutedFixed > 0) {
                allResults.push(`## Fix Polluted Pages
Fixed ${pollutedFixed}/${pollutedPages.length} polluted pages`);
              }
            }
            fixAllNotice.setMessage("Smart fix: Phase 0 \u2014 Completing aliases...");
            if (aliasDeficientPages.length > 0) {
              const { filled, results } = await runAliasCompletion(ctx, signal2, aliasDeficientPages);
              aliasesFilled = filled;
              if (filled > 0) {
                allResults.push(`## Complete Aliases
${results.join("\n")}`);
                console.debug(`Smart fix: Completed ${filled} aliases, improving duplicate detection accuracy`);
              }
            }
            fixAllNotice.setMessage("Smart fix: Phase 1 \u2014 Merging duplicates...");
            if (duplicates.length > 0) {
              const { merged, results } = await runDuplicateMerges(ctx, signal2, duplicates);
              duplicatesMerged = merged;
              if (merged > 0) {
                allResults.push(`## Merge Duplicate Pages
${results.join("\n")}`);
                console.debug(`Smart fix: Merged ${merged} duplicates, dead links and orphans may be auto-resolved`);
              }
            }
            fixAllNotice.setMessage("Smart fix: Phase 2 \u2014 Fixing dead links...");
            if (deadLinks.length > 0) {
              const { fixed, results } = await runDeadLinkFixes(ctx, signal2, deadLinks);
              deadLinksFixed = fixed;
              if (fixed > 0) {
                allResults.push(`## Fix Dead Links
${results.join("\n")}`);
                console.debug(`Smart fix: Fixed ${fixed} dead links`);
              }
            }
            fixAllNotice.setMessage("Smart fix: Phase 3 \u2014 Linking orphan pages...");
            if (orphans.length > 0) {
              const { linked, results } = await runOrphanFixes(ctx, signal2, orphans);
              orphansLinked = linked;
              if (linked > 0) {
                allResults.push(`## Link Orphan Pages
${results.join("\n")}`);
                console.debug(`Smart fix: Linked ${linked} orphan pages`);
              }
            }
            fixAllNotice.setMessage("Smart fix: Phase 4 \u2014 Expanding empty pages...");
            if (emptyPages.length > 0) {
              const { filled, results } = await runEmptyPageFixes(ctx, signal2, emptyPages);
              emptyPagesFilled = filled;
              if (filled > 0) {
                allResults.push(`## Expand Empty Pages
${results.join("\n")}`);
                console.debug(`Smart fix: Expanded ${filled} empty pages`);
              }
            }
            fixAllNotice.setMessage("Smart fix: Phase 5 \u2014 Retagging out-of-vocabulary tag pages...");
            if (tagViolations.length > 0) {
              const { fixed, results } = await runRetagViolations(ctx, signal2, tagViolations);
              tagsRetagged = fixed;
              if (fixed > 0) {
                allResults.push(`## Retag Tag Violations
${results.join("\n")}`);
                console.debug(`Smart fix: Retagged ${fixed} tag violations`);
              }
            }
            fixAllNotice.hide();
            if (allResults.length > 0) {
              await ctx.wikiEngine.generateIndexFromEngine();
              await ctx.wikiEngine.logLintFix("Smart Fix All (Causality-Aware with Aliases)", allResults.join("\n\n"));
            }
            const phases = [];
            if (pollutedPages.length > 0) {
              phases.push({
                label: `\u{1F9F9} Fix polluted pages (${pollutedPages.length})`,
                detail: `${pollutedFixed}/${pollutedPages.length}`
              });
            }
            if (aliasDeficientPages.length > 0) {
              phases.push({
                label: t.lintAliasesCompleteBtn.replace("{count}", String(aliasDeficientPages.length)),
                detail: `${aliasesFilled}/${aliasDeficientPages.length}`
              });
            }
            if (duplicates.length > 0) {
              phases.push({
                label: t.lintModalMergeDuplicates.replace("{count}", String(duplicates.length)),
                detail: `${duplicatesMerged}/${duplicates.length}`
              });
            }
            if (deadLinks.length > 0) {
              phases.push({
                label: t.lintModalFixDeadLinks.replace("{count}", String(deadLinks.length)),
                detail: `${deadLinksFixed}/${deadLinks.length}`
              });
            }
            if (tagViolations.length > 0) {
              phases.push({
                label: t.lintTagViolationRetagBtn.replace("{count}", String(tagViolations.length)),
                detail: `${tagsRetagged}/${tagViolations.length}`
              });
            }
            if (orphans.length > 0) {
              phases.push({
                label: t.lintModalLinkOrphans.replace("{count}", String(orphans.length)),
                detail: `${orphansLinked}/${orphans.length}`
              });
            }
            if (emptyPages.length > 0) {
              phases.push({
                label: t.lintModalExpandEmpty.replace("{count}", String(emptyPages.length)),
                detail: `${emptyPagesFilled}/${emptyPages.length}`
              });
            }
            new FixReportModal(ctx.app, phases, ctx.settings.language).open();
          } finally {
            ctx.wikiEngine.endLintOperation();
          }
        })();
      };
    }
    stageNotice.hide();
    const logReport = nestReportUnderParent(fullReport);
    await ctx.wikiEngine.logLintFix(t.lintReportTitle, logReport);
    if (ctx.settings.autoSmartFix && fixCallbacks.onFixAll) {
      new import_obsidian14.Notice(getText(ctx.settings.language, "autoSmartFixNotice"));
      void fixCallbacks.onFixAll();
    } else {
      new LintReportModal(ctx.app, fullReport, fixCallbacks, counts, ctx.settings.language).open();
    }
    await ctx.wikiEngine.generateIndexFromEngine();
    new import_obsidian14.Notice(TEXTS[ctx.settings.language].lintWikiComplete);
  } catch (error) {
    stageNotice == null ? void 0 : stageNotice.hide();
    if (error instanceof DOMException && error.name === "AbortError") {
      new import_obsidian14.Notice(getText(ctx.settings.language, "ingestionCancelled"), NOTICE_NORMAL);
      console.debug("Lint cancelled by user");
      return;
    }
    const errMsg2 = error instanceof Error ? error.message : String(error);
    new import_obsidian14.Notice(TEXTS[ctx.settings.language].lintWikiFailed + ": " + errMsg2, 0);
    console.error(error);
  }
}

// src/main.ts
function getThinkingControlCacheKey(settings) {
  var _a, _b;
  return ((_a = settings.baseUrl) == null ? void 0 : _a.trim()) || ((_b = PREDEFINED_PROVIDERS[settings.provider]) == null ? void 0 : _b.baseUrl) || "";
}
function createLLMClient(settings) {
  var _a, _b, _c, _d, _e;
  let client;
  if (settings.provider === "anthropic") {
    client = new AnthropicClient(settings.apiKey.trim());
  } else if (settings.provider === "anthropic-compatible") {
    const baseUrl = (_a = settings.baseUrl) == null ? void 0 : _a.trim();
    if (baseUrl) {
      client = new AnthropicCompatibleClient(settings.apiKey.trim(), baseUrl);
    } else {
      client = new AnthropicClient(settings.apiKey.trim());
    }
  } else {
    const providerConfig = PREDEFINED_PROVIDERS[settings.provider];
    const baseUrl = ((_b = settings.baseUrl) == null ? void 0 : _b.trim()) || (providerConfig == null ? void 0 : providerConfig.baseUrl) || void 0;
    const apiKey = settings.provider === "ollama" || settings.provider === "lmstudio" ? settings.apiKey.trim() || "lmstudio" : settings.apiKey.trim();
    client = new OpenAICompatibleClient(apiKey, baseUrl);
  }
  if (client instanceof OpenAICompatibleClient) {
    const cacheKey = getThinkingControlCacheKey(settings);
    console.debug("[CREATE-LLM] cacheKey:", cacheKey, "has cache?", cacheKey && ((_c = settings.thinkingControlCache) == null ? void 0 : _c[cacheKey]) !== void 0, "cache value:", (_d = settings.thinkingControlCache) == null ? void 0 : _d[cacheKey]);
    if (cacheKey && ((_e = settings.thinkingControlCache) == null ? void 0 : _e[cacheKey]) !== void 0) {
      const cached = settings.thinkingControlCache[cacheKey];
      client.thinkingControlDialect = typeof cached === "boolean" ? cached ? "anthropic" : "none" : cached;
      console.debug("[CREATE-LLM] set dialect to:", client.thinkingControlDialect);
    }
    client.language = settings.language;
  }
  return wrapWithAdvancedSettings(client, {
    maxTokensPerCall: settings.maxTokensPerCall,
    extractionTemperature: settings.extractionTemperature,
    chatTemperature: settings.chatTemperature,
    repetitionPenalty: settings.repetitionPenalty
  });
}
var LLMWikiPlugin = class extends import_obsidian15.Plugin {
  constructor() {
    super(...arguments);
    this.llmClient = null;
    this.progressNotice = null;
    this.ingestStatusBar = null;
    // Tracks the current document position during a folder batch ingest so the
    // status bar can show "[current/total] <doc> · …". Null for single-file ingest.
    this.batchProgress = null;
  }
  async onload() {
    await this.loadSettings();
    this.cleanupVocabularyTags();
    this.initializeLLMClient();
    this.schemaManager = new SchemaManager(
      this.app,
      this.settings,
      () => this.llmClient
    );
    this.wikiEngine = new WikiEngine(
      this.app,
      this.settings,
      () => this.llmClient,
      this.schemaManager,
      // Delayed evaluation: this closure captures autoMaintainManager by reference,
      // but the variable is assigned at :68 below. By the time wikiEngine calls
      // this callback (during file writes), autoMaintainManager is guaranteed
      // to exist. This is intentional — reordering the assignments would break it.
      (path) => this.autoMaintainManager.watchWrite(path),
      (msg) => this.showProgress(msg),
      (report) => this.onIngestDone(report)
    );
    this.wikiEngine.onConfirmReingest = (file) => new Promise((resolve) => {
      const lang = this.settings.language;
      new ConfirmModal(this.app, {
        title: getText(lang, "reingestConfirmTitle"),
        body: getText(lang, "reingestConfirmBody").replace("{filename}", file.basename),
        confirmText: getText(lang, "reingestConfirmYes"),
        cancelText: getText(lang, "reingestConfirmNo"),
        onChoice: resolve
      }).open();
    });
    this.autoMaintainManager = new AutoMaintainManager(
      this.app,
      this.settings,
      this.wikiEngine,
      this,
      () => this.lintWiki()
    );
    if (this.settings.autoWatchSources) {
      this.autoMaintainManager.startWatching();
    }
    this.autoMaintainManager.schedulePeriodicLint();
    if (this.settings.startupCheck) {
      void this.autoMaintainManager.runStartupCheck();
    }
    this.registerView(
      VIEW_TYPE_QUERY,
      (leaf) => new QueryView(leaf, this)
    );
    const t = TEXTS[this.settings.language];
    this.addCommand({
      id: "ingest-source",
      name: t.cmdIngestSource,
      callback: () => this.selectSourceToIngest()
    });
    this.addCommand({
      id: "ingest-folder",
      name: t.cmdIngestFolder,
      callback: () => this.selectFolderToIngest()
    });
    this.addCommand({
      id: "query-wiki",
      name: t.cmdQueryWiki,
      callback: () => this.queryWiki()
    });
    this.addCommand({
      id: "lint-wiki",
      name: t.cmdLintWiki,
      callback: () => this.lintWiki()
    });
    this.addCommand({
      id: "regenerate-index",
      name: t.cmdRegenerateIndex,
      callback: () => {
        void (async () => {
          new import_obsidian15.Notice(getText(this.settings.language, "regenerateIndexCompleted") + "...");
          try {
            await this.wikiEngine.generateIndexFromEngine();
            new import_obsidian15.Notice(getText(this.settings.language, "regenerateIndexCompleted"));
          } catch (err) {
            console.error("Regenerate index failed:", err);
            new import_obsidian15.Notice(getText(this.settings.language, "operationFailed") + (err instanceof Error ? err.message : String(err)));
          }
        })();
      }
    });
    this.addCommand({
      id: "cancel-ingestion",
      name: t.cmdCancelIngestion,
      callback: () => {
        if (this.wikiEngine.isIngesting()) {
          this.wikiEngine.cancelIngestion();
        } else if (this.wikiEngine.isLintRunning()) {
          this.wikiEngine.cancelLint();
        }
      }
    });
    this.addCommand({
      id: "ingest-active-file",
      name: t.cmdIngestActiveFile,
      callback: () => this.ingestActiveFile()
    });
    this.addCommand({
      id: "view-ingestion-history",
      name: t.cmdViewHistory,
      callback: () => {
        new HistoryModal(this.app, {
          language: this.settings.language,
          wikiFolder: this.settings.wikiFolder || "wiki"
        }).open();
      }
    });
    this.addRibbonIcon("sticker", t.cmdIngestActiveFile, () => {
      this.ingestActiveFile();
    });
    this.addRibbonIcon("message-circle", t.cmdQueryWiki, () => {
      this.queryWiki();
    });
    this.ingestStatusBar = this.addStatusBarItem();
    this.ingestStatusBar.addClass("llm-wiki-status-bar");
    this.ingestStatusBar.addClass("llm-wiki-status-bar-hidden");
    this.ingestStatusBar.setText("LLM wiki");
    this.ingestStatusBar.onclick = () => {
      if (this.wikiEngine.isIngesting()) {
        this.wikiEngine.cancelIngestion();
      } else if (this.wikiEngine.isLintRunning()) {
        this.wikiEngine.cancelLint();
      }
    };
    this.wikiEngine.setIngestionCallbacks(
      (filename) => {
        const label = getText(this.settings.language, "ingestionStatusBar");
        if (this.ingestStatusBar) {
          this.ingestStatusBar.setText(
            buildIngestStatusBarText(label, filename, this.batchProgress)
          );
          this.ingestStatusBar.removeClass("llm-wiki-status-bar-hidden");
        }
      },
      () => {
        if (this.ingestStatusBar) {
          this.ingestStatusBar.addClass("llm-wiki-status-bar-hidden");
        }
      }
    );
    this.wikiEngine.setLintCallbacks(
      () => {
        const label = getText(this.settings.language, "lintStatusBar");
        if (this.ingestStatusBar) {
          this.ingestStatusBar.setText(label);
          this.ingestStatusBar.removeClass("llm-wiki-status-bar-hidden");
        }
      },
      () => {
        if (this.ingestStatusBar) {
          this.ingestStatusBar.addClass("llm-wiki-status-bar-hidden");
        }
      }
    );
    this.addSettingTab(new LLMWikiSettingTab(this.app, this));
    console.debug("LLM Wiki Plugin loaded - Karpathy implementation");
  }
  onunload() {
    var _a;
    (_a = this.autoMaintainManager) == null ? void 0 : _a.stop();
    console.debug("LLM Wiki Plugin unloaded");
  }
  async loadSettings() {
    var _a;
    const savedData = await this.loadData();
    const { settings, applied } = applySettingsMigrations(savedData);
    this.settings = settings;
    if (savedData && !savedData.wikiLanguage) {
      this.settings.wikiLanguage = this.settings.language;
      await this.saveData(this.settings);
    }
    if (!Array.isArray(this.settings.watchedFolders)) {
      this.settings.watchedFolders = [];
      console.debug("loadSettings: watchedFolders was not an array, reset to []");
    }
    if (applied.length > 0) {
      console.debug(`loadSettings: applied migrations: ${applied.join(", ")}`);
    }
    if (savedData && !("llmReady" in savedData)) {
      const hasConfig = savedData.provider && (((_a = savedData.apiKey) == null ? void 0 : _a.trim()) || savedData.provider === "ollama") && savedData.model;
      this.settings.llmReady = !!hasConfig;
      if (hasConfig) {
        console.debug("loadSettings: existing user with config detected, llmReady = true");
      }
    }
  }
  /**
   * Issue #85 v2: Migrate v1 textarea CSV (which may contain untrimmed
   * whitespace, empty entries, or case-variant duplicates from manual
   * editing) into the canonical form the chip input uses. Idempotent.
   * Called once on onload() before any UI renders so users see clean
   * chips immediately on first reload after upgrade.
   */
  cleanupVocabularyTags() {
    const fields = [
      "customEntityTags",
      "customConceptTags"
    ];
    let changed = false;
    for (const field of fields) {
      const current = this.settings[field];
      if (!current) continue;
      const cleaned = normalizeVocabularyCsv(current);
      if (cleaned !== current) {
        this.settings[field] = cleaned;
        changed = true;
      }
    }
    if (changed) void this.saveSettings();
  }
  async saveSettings() {
    var _a;
    await this.saveData(this.settings);
    this.initializeLLMClient();
    (_a = this.schemaManager) == null ? void 0 : _a.updateSettings(this.settings);
    if (this.wikiEngine) {
      this.wikiEngine.updateSettings(this.settings);
      console.debug("[saveSettings] wikiEngine provider updated to:", this.settings.provider);
    }
    if (this.autoMaintainManager) {
      this.autoMaintainManager.settings = this.settings;
      this.autoMaintainManager.stop();
      if (this.settings.autoWatchSources) {
        this.autoMaintainManager.startWatching();
      }
      this.autoMaintainManager.schedulePeriodicLint();
    }
  }
  initializeLLMClient() {
    var _a;
    if (!((_a = this.settings.apiKey) == null ? void 0 : _a.trim()) && this.settings.provider !== "ollama") {
      this.llmClient = null;
      return;
    }
    try {
      this.llmClient = createLLMClient(this.settings);
      console.debug("LLM Client initialized:", this.settings.provider);
    } catch (error) {
      console.error("LLM Client initialization failed:", error);
      this.llmClient = null;
    }
  }
  showProgress(msg) {
    if (this.progressNotice) {
      this.progressNotice.setMessage(msg);
    } else {
      this.progressNotice = new import_obsidian15.Notice(msg, 0);
    }
  }
  dismissProgress() {
    if (this.progressNotice) {
      this.progressNotice.hide();
      this.progressNotice = null;
    }
  }
  onIngestDone(report) {
    this.dismissProgress();
    new IngestReportModal(this.app, report, this.settings.language).open();
  }
  // ==================== Ingestion ====================
  async isAlreadyIngested(sourceFile) {
    const slug = slugify(sourceFile.basename, this.settings.slugCase === "preserve");
    const wikiPath = `${this.settings.wikiFolder}/sources/${slug}.md`;
    try {
      const file = this.app.vault.getAbstractFileByPath(wikiPath);
      if (!(file instanceof import_obsidian15.TFile)) return false;
      try {
        const content = await this.app.vault.read(file);
        const fm = parseFrontmatter(content);
        if (fm && fm.sources) {
          const normalizedSources = fm.sources.map((s) => {
            const trimmed = s.trim();
            if (trimmed.startsWith("[[") && trimmed.endsWith("]]")) {
              return trimmed.slice(2, -2).trim();
            }
            return trimmed;
          });
          return normalizedSources.includes(sourceFile.path);
        }
        return true;
      } catch (e) {
        return true;
      }
    } catch (e) {
      return false;
    }
  }
  selectSourceToIngest() {
    if (!this.requireLLMReady()) return;
    if (!this.llmClient) {
      new import_obsidian15.Notice(TEXTS[this.settings.language].errorNoApiKey);
      return;
    }
    new FileSuggestModal(this.app, this.settings.wikiFolder, (file) => {
      this.showProgress(`Ingesting: ${file.basename}`);
      this.wikiEngine.ingestSource(file, { interactive: true }).catch((e) => {
        console.error("Single ingest failed:", e);
        const errMsg2 = e instanceof Error ? e.message : String(e);
        new import_obsidian15.Notice(TEXTS[this.settings.language].errorIngestFailed + errMsg2, NOTICE_ERROR);
      });
    }).open();
  }
  ingestActiveFile() {
    if (!this.requireLLMReady()) return;
    if (!this.llmClient) {
      new import_obsidian15.Notice(TEXTS[this.settings.language].errorNoApiKey);
      return;
    }
    const activeFile = this.app.workspace.getActiveFile();
    if (!activeFile) {
      new import_obsidian15.Notice(getText(this.settings.language, "noActiveFile"), NOTICE_NORMAL);
      return;
    }
    this.showProgress(`Ingesting: ${activeFile.basename}`);
    this.wikiEngine.ingestSource(activeFile, { interactive: true }).catch((e) => {
      console.error("Ingest active file failed:", e);
      const errMsg2 = e instanceof Error ? e.message : String(e);
      new import_obsidian15.Notice(TEXTS[this.settings.language].errorIngestFailed + errMsg2, NOTICE_ERROR);
    });
  }
  selectFolderToIngest() {
    if (!this.requireLLMReady()) return;
    if (!this.llmClient) {
      new import_obsidian15.Notice(TEXTS[this.settings.language].errorNoApiKey);
      return;
    }
    new FolderSuggestModal(this.app, this.settings.wikiFolder, (folder) => {
      void (async () => {
        const allowedExts = COMPATIBLE_SOURCE_EXTENSIONS;
        const files = this.app.vault.getFiles().filter((f) => f.path.startsWith(folder.path) && allowedExts.includes(f.extension.toLowerCase()));
        if (files.length === 0) {
          const msg = TEXTS[this.settings.language].selectFolderNoMdFiles.replace("{path}", folder.path);
          new import_obsidian15.Notice(msg);
          return;
        }
        this.showProgress("Checking for already-ingested files...");
        const alreadyIngestedFiles = [];
        const newFiles = [];
        for (const file of files) {
          if (await this.isAlreadyIngested(file)) {
            alreadyIngestedFiles.push(file);
          } else {
            newFiles.push(file);
          }
        }
        const totalFiles = files.length;
        const skippedCount = alreadyIngestedFiles.length;
        const ingestCount = newFiles.length;
        if (skippedCount > 0) {
          const texts2 = TEXTS[this.settings.language];
          new import_obsidian15.Notice(
            texts2.batchIngestSkipNotice.replace("{skipped}", String(skippedCount)).replace("{total}", String(totalFiles)).replace("{new}", String(ingestCount)),
            6e3
          );
        }
        if (ingestCount === 0) {
          this.wikiEngine.setDoneCallback((report) => this.onIngestDone(report));
          const texts2 = TEXTS[this.settings.language];
          new import_obsidian15.Notice(texts2.batchIngestAllIngested.replace("{total}", String(totalFiles)), NOTICE_NORMAL);
          return;
        }
        const reports = [];
        this.wikiEngine.setDoneCallback((report) => {
          reports.push(report);
        });
        const texts = TEXTS[this.settings.language];
        this.showProgress(texts.batchIngestStarting.replace("{count}", String(ingestCount)).replace("{folder}", folder.name));
        const batchCtx = this.wikiEngine.createBatchContext();
        for (let i = 0; i < newFiles.length; i++) {
          const file = newFiles[i];
          try {
            this.batchProgress = { current: i + 1, total: ingestCount };
            this.showProgress(`[${i + 1}/${ingestCount}] ${file.basename}`);
            console.debug(`(${i + 1}/${ingestCount}) ingesting: ${file.path}`);
            await this.wikiEngine.ingestSource(file, { batchCtx });
            if (this.wikiEngine.wasCancelled) {
              console.debug(`Folder ingestion cancelled at file ${i + 1}/${ingestCount}`);
              break;
            }
            console.debug(`(${i + 1}/${ingestCount}) ingestion success: ${file.path}`);
          } catch (error) {
            console.error(`(${i + 1}/${ingestCount}) ingestion failed: ${file.path}`, error);
            const errMsg2 = error instanceof Error ? error.message : String(error);
            new import_obsidian15.Notice(texts.errorIngestFailed + file.basename + ": " + errMsg2, NOTICE_ERROR);
          }
        }
        this.batchProgress = null;
        this.wikiEngine.setDoneCallback((report) => this.onIngestDone(report));
        this.dismissProgress();
        if (reports.length > 0) {
          const allCreated = [...new Set(reports.flatMap((r) => r.createdPages))];
          const allUpdated = [...new Set(reports.flatMap((r) => r.updatedPages))];
          const totalEntities = reports.reduce((sum, r) => sum + r.entitiesCreated, 0);
          const totalConcepts = reports.reduce((sum, r) => sum + r.conceptsCreated, 0);
          const totalContradictions = reports.reduce((sum, r) => sum + r.contradictionsFound, 0);
          const totalElapsed = reports.reduce((sum, r) => sum + (r.elapsedSeconds || 0), 0);
          const allFailedItems = reports.flatMap((r) => r.failedItems);
          const allCollisions = reports.flatMap((r) => r.collisions || []);
          const allRejectedFiles = reports.flatMap((r) => r.rejectedFiles || []);
          const allSuccess = reports.every((r) => r.success);
          const aggregated = {
            sourceFile: `${reports.length} files from ${folder.path}`,
            createdPages: allCreated,
            updatedPages: allUpdated,
            entitiesCreated: totalEntities,
            conceptsCreated: totalConcepts,
            failedItems: allFailedItems,
            collisions: allCollisions,
            contradictionsFound: totalContradictions,
            success: allSuccess,
            elapsedSeconds: totalElapsed,
            skippedFiles: skippedCount,
            totalFilesInFolder: totalFiles,
            rejectedFiles: allRejectedFiles
          };
          new IngestReportModal(this.app, aggregated, this.settings.language).open();
        } else {
          const texts2 = TEXTS[this.settings.language];
          new import_obsidian15.Notice(texts2.batchIngestComplete.replace("{success}", "0").replace("{total}", String(ingestCount)).replace("{fail}", String(ingestCount)), 1e4);
        }
      })().catch((e) => console.error(e));
    }).open();
  }
  // ==================== Query ====================
  queryWiki() {
    if (!this.requireLLMReady()) return;
    if (!this.llmClient) {
      new import_obsidian15.Notice(TEXTS[this.settings.language].errorNoApiKey);
      return;
    }
    void this.activateQueryView();
  }
  async activateQueryView() {
    const { workspace } = this.app;
    const existing = workspace.getLeavesOfType(VIEW_TYPE_QUERY);
    if (existing.length > 0) {
      await workspace.revealLeaf(existing[0]);
      return;
    }
    const leaf = workspace.getRightLeaf(false);
    if (!leaf) return;
    await leaf.setViewState({ type: VIEW_TYPE_QUERY, active: true });
    await workspace.revealLeaf(leaf);
  }
  // ==================== Lint ====================
  async lintWiki() {
    if (!this.requireLLMReady()) return;
    const signal = this.wikiEngine.startLintOperation();
    try {
      await runLintWiki({
        app: this.app,
        settings: this.settings,
        llmClient: this.llmClient,
        wikiEngine: this.wikiEngine,
        onAnalyzeSchema: (context) => {
          void this.suggestSchemaUpdate(context);
        }
      }, signal);
    } finally {
      this.wikiEngine.endLintOperation();
    }
  }
  // ==================== Schema ====================
  async suggestSchemaUpdate(context) {
    await runSchemaAnalyze({
      settings: this.settings,
      llmClient: this.llmClient,
      wikiEngine: this.wikiEngine,
      schemaManager: this.schemaManager,
      requireLLMReady: () => this.requireLLMReady(),
      openSchemaDiffModal: (suggestion) => this.openSchemaDiffModal(suggestion),
      lintAnalysisContext: context
    });
  }
  /** v1.22.0 #97: open the SchemaDiffModal with the LLM's proposed new body. */
  openSchemaDiffModal(suggestion) {
    var _a, _b, _c;
    const t = (_a = TEXTS[this.settings.language]) != null ? _a : TEXTS.en;
    console.debug("[SchemaDiffModal] suggestion.suggestions =", JSON.stringify(suggestion.suggestions), "changes_needed =", suggestion.changes_needed, "newSchemaBody length =", (_b = suggestion.newSchemaBody) == null ? void 0 : _b.length);
    const isEmpty = !suggestion.changes_needed;
    const modal = new SchemaDiffModal(this.app, {
      currentBody: "",
      newBody: (_c = suggestion.newSchemaBody) != null ? _c : "",
      language: this.settings.language,
      isEmpty,
      rationale: suggestion.suggestions,
      onOpenFile: () => {
        const path = `${this.settings.wikiFolder}/schema/config.md`;
        void this.app.workspace.openLinkText(path, "", false);
      },
      onApply: async () => {
        var _a2, _b2;
        const result = await applySchemaSuggestion({
          app: this.app,
          currentPath: `${this.settings.wikiFolder}/schema/config.md`,
          newBody: (_a2 = suggestion.newSchemaBody) != null ? _a2 : "",
          onCacheInvalidate: () => this.schemaManager.invalidateCache()
        });
        if (result.success) {
          const restore = t.schemaDiffRestoreHint ? t.schemaDiffRestoreHint.replace("{path}", result.backupPath) : `Backup saved to ${result.backupPath}. To restore, rename that file back to wiki/schema/config.md in your file explorer.`;
          new import_obsidian15.Notice(restore, 0);
        } else {
          new import_obsidian15.Notice((_b2 = t.schemaDiffFailed) != null ? _b2 : "Schema apply failed: " + result.reason, NOTICE_ERROR);
        }
      },
      onRegenerate: async (userHint) => {
        var _a2;
        const newSuggestion = await this.regenerateSchemaWithHint(suggestion, userHint);
        if (newSuggestion == null ? void 0 : newSuggestion.newSchemaBody) {
          modal.setNewBody(newSuggestion.newSchemaBody, {
            rationale: newSuggestion.suggestions,
            isEmpty: !newSuggestion.changes_needed
          });
        } else {
          new import_obsidian15.Notice((_a2 = t.schemaRegenerateNoBody) != null ? _a2 : "Regeneration succeeded but the LLM did not return a new body.", NOTICE_ERROR);
        }
      }
    });
    modal.open();
    void this.schemaManager.loadSchema().then((loaded) => {
      var _a2;
      modal.setCurrentBody((_a2 = loaded == null ? void 0 : loaded.body) != null ? _a2 : "");
    });
  }
  /** v1.22.0 #97: re-call the LLM with the user's refinement hint. */
  async regenerateSchemaWithHint(baseSuggestion, userHint) {
    const ctx = `${baseSuggestion.suggestions || ""}

User refinement: ${userHint || "(none)"}`;
    return await this.schemaManager.suggestSchemaUpdate(ctx);
  }
  // ==================== Connection Test ====================
  async testLLMConnection() {
    var _a, _b;
    const t = TEXTS[this.settings.language] || TEXTS.en;
    const isOllama = this.settings.provider === "ollama";
    if (!isOllama && (!this.settings.apiKey || this.settings.apiKey.trim() === "")) {
      return { success: false, message: t.errorNoApiKey || "API Key is not configured" };
    }
    try {
      const testClient = createLLMClient(this.settings);
      const testResponse = await testClient.createMessage({
        model: this.settings.model,
        max_tokens: TOKENS_QUERY_MODEL_DETECT,
        messages: [{
          role: "user",
          content: 'Test connection. Please reply "Connection successful".'
        }]
      });
      console.debug("Test response:", testResponse);
      this.settings.llmReady = true;
      if (this.settings.disableThinking && (testClient instanceof OpenAICompatibleClient || testClient instanceof AnthropicCompatibleClient || testClient instanceof AnthropicClient)) {
        let detectedDialect = "anthropic";
        let probeSucceeded = false;
        try {
          await testClient.createMessage({
            model: this.settings.model,
            max_tokens: 1,
            messages: [{ role: "user", content: "think" }],
            enableThinking: false
          });
          detectedDialect = "anthropic";
          probeSucceeded = true;
        } catch (e) {
          if (testClient instanceof OpenAICompatibleClient) {
            const savedDialect = testClient.thinkingControlDialect;
            testClient.thinkingControlDialect = "openai";
            try {
              await testClient.createMessage({
                model: this.settings.model,
                max_tokens: 1,
                messages: [{ role: "user", content: "think" }],
                enableThinking: false
              });
              detectedDialect = "openai";
              probeSucceeded = true;
            } catch (e2) {
              detectedDialect = "none";
            } finally {
              testClient.thinkingControlDialect = savedDialect;
            }
          } else {
            detectedDialect = "none";
          }
        }
        const cacheKey = getThinkingControlCacheKey(this.settings);
        if (probeSucceeded) {
          if (testClient instanceof OpenAICompatibleClient) {
            testClient.thinkingControlDialect = detectedDialect;
          }
          if (cacheKey) {
            this.settings.thinkingControlCache = {
              ...this.settings.thinkingControlCache,
              [cacheKey]: detectedDialect
            };
            console.debug(`Thinking control dialect for ${cacheKey}: ${detectedDialect}`);
          }
        } else {
          if (testClient instanceof OpenAICompatibleClient) {
            testClient.thinkingControlDialect = "none";
          }
          if (cacheKey) {
            this.settings.thinkingControlCache = {
              ...this.settings.thinkingControlCache,
              [cacheKey]: "none"
            };
            console.debug(`Thinking control dialect for ${cacheKey}: none (both probed tiers failed)`);
          }
        }
        if (probeSucceeded && testClient instanceof OpenAICompatibleClient && this.settings.advancedSettingsMode === "custom" && cacheKey && (this.settings.repetitionPenalty !== void 0 && this.settings.repetitionPenalty !== 0 || this.settings.extractionTemperature !== void 0 || this.settings.chatTemperature !== void 0)) {
          try {
            await testClient.createMessage({
              model: this.settings.model,
              max_tokens: 1,
              messages: [{ role: "user", content: "param-probe" }],
              enableThinking: false,
              temperature: (_a = this.settings.extractionTemperature) != null ? _a : this.settings.chatTemperature,
              repetition_penalty: this.settings.repetitionPenalty
            });
            console.debug("Advanced parameters accepted by", cacheKey);
          } catch (probeErr) {
            console.debug(
              `Advanced parameters probe failed for ${cacheKey}; unsupported fields cached. Error: ${probeErr.message}`
            );
          }
        }
      }
      await this.saveSettings();
      if (this.wikiEngine) {
        const isInit = await this.isWikiInitialized();
        if (!isInit) {
          try {
            await this.wikiEngine.ensureWikiStructure();
            console.debug("Wiki structure auto-initialized");
          } catch (initError) {
            console.warn("Auto wiki init failed:", initError);
          }
        }
      }
      const providerName = ((_b = PREDEFINED_PROVIDERS[this.settings.provider]) == null ? void 0 : _b.nameEn) || this.settings.provider;
      this.initializeLLMClient();
      return {
        success: true,
        message: `\u2705 ${t.testConnectionSuccessful || "Connection successful"}${t.testConnectionProvider ? ": " : ""}${providerName}`
      };
    } catch (error) {
      console.error("Connection test failed:", error);
      this.settings.llmReady = false;
      await this.saveSettings();
      const errorMsg = error instanceof Error ? error.message : String(error);
      return {
        success: false,
        message: `\u274C ${t.testConnectionFailed || "Connection failed"}: ${errorMsg || t.errorUnknown || "Unknown error"}`
      };
    }
  }
  /**
   * Check if wiki structure exists by IO inspection (no persistent flag).
   * Handles custom wikiFolder changes gracefully.
   */
  async isWikiInitialized() {
    const wikiFolder = this.settings.wikiFolder || "wiki";
    const requiredFolders = [
      `${wikiFolder}/entities`,
      `${wikiFolder}/concepts`,
      `${wikiFolder}/sources`,
      `${wikiFolder}/schema`
    ];
    for (const folder of requiredFolders) {
      const folderObj = this.app.vault.getAbstractFileByPath(folder);
      if (!folderObj) return false;
    }
    return true;
  }
  requireLLMReady() {
    if (this.settings.llmReady) return true;
    new import_obsidian15.Notice(getText(this.settings.language, "llmNotReady"), NOTICE_ERROR);
    return false;
  }
};

/* nosourcemap */