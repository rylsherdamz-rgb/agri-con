
export default function FloatingAIButtonComponent() {
    return (
    // this is the floating button Component where a small ai chatting will show when click to be able to ask,
    // which place will have good harvest, weather, and other things in there that will be usable and have access to
    // the blockchain or oracle to know what are the correct transaction are
    <div>

    </div>

    )
}
