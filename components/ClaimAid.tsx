// this will have the checkout status of the request of the aid,
// the aid relief is based on the farmers profit history, flood history, if in the past, if
// it has been hit by a storm or what to aid them but this take a bit of time for verification

// have other file component in here that show status of the aid and the history of funds release, from the pool for the transaction successful
// and the aid succeful and if succesfull the logs will also show in inside a component
// loop to see all of it

export default function ClaimAid() {
    return (
    <div className="">

    </div>
    )
}
